# Research context

The confirmed bounded task is to revise the existing method figure and its manuscript caption using `materials/method.txt`, not to collect new results or review the whole paper.

Editable starting source: `figures/flow.svg`. Manuscript context and intended placement: `paper/main.tex`, 85 mm width. The current sketch confuses the training-only penalty with the inference path and its caption overstates what a method diagram establishes.

Create a corrected editable figure, actually render it to `figures/flow.png`, open the raster with a visual-capable host action, and examine it at manuscript size against the paragraph and caption. Return both output paths and the editable SVG, with any remaining limits. Different existing renderers and visual-opening tools are acceptable. Do not install tools, call external models or services, create data, or claim that reading SVG text was visual inspection.
