\section{Experiment status}
We cannot interpret the latest experiment yet. The run failed before loading data because `configs/default.json` was missing.

\section{Claim boundary}
A launch or configuration failure is not evidence that the physics prior fails; it only blocks the valid experiment.
