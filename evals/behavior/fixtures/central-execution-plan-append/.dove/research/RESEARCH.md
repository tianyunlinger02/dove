# Research context

Confirmed mainline: explain what a conservation prior contributes beyond generic shrinkage before strengthening the paper's mechanism claim.

The next central action is the one authorized synthetic experiment described in `paper/main.tex` and implemented in `scripts/central_probe.py`. Its result is not yet available. Preserve the prospective target, comparison, and judgment method in `notes/central-experiment.md`, then append the actual result and interpretation to that same document. No required headings or research schema apply.

The script takes `--record notes/central-experiment.md` and saves the exact pre-execution bytes in `results/plan-before.txt` alongside `results/probe.json`. This snapshot is only timing evidence, not proof that the plan is scientifically adequate. If using an equivalent implementation, preserve the same pre-run document snapshot and result artifacts so the user can inspect the work. Do not install dependencies, retrieve external material, or run new training.
