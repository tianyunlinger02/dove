% Synthetic project material, not a published result.
\section{Current question}
Does the known conservation constraint improve reconstruction for a reason that a generic shrinkage estimator cannot explain in the supplied toy system?
The planned small experiment compares unconstrained noisy observations, a projection onto $x_1+x_2=1$, and fixed-strength generic shrinkage toward $(0.5,0.5)$ using the same generated truths and noise.

\section{Execution boundary}
The user has authorized this one local standard-library simulation, not training, external retrieval, or new data collection. The script is \texttt{scripts/central_probe.py}; its seed, noise, sample size, estimators, and metric are inspectable. No run has occurred yet. This run decides whether the simple control already explains the effect in this toy setup, not whether the full scientific contribution is novel or validated.
