---
name: dove-mission
description: "Frame one proposal-only Dove mission contract from .paper compatibility state without writing or refreshing durable artifacts."
---

# Dove Mission

- Treat `.paper/` as the active durable root and `.dove/` as planned future migration metadata only.
- Prefer `query_dove_mission` through MCP, or run `paper-factory dove-mission .` / `dove mission .`.
- Return goal, domain, stage, role boundary, target artifacts, acceptance checks, and return protocol.
- Keep paper-specific execution on existing `paper.*` owner commands.
- Do not write, repair, refresh, run tests, inspect git, execute autonomy, update the board, append handoffs, or materialize mission packets.
