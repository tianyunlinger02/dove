---
name: dove-board
description: "Read the compatibility-backed Dove mission board from .paper without writing, refreshing, running tests, or inspecting git."
---

# Dove Board

- Treat `.paper/` as the active durable root and `.dove/` as planned future migration metadata only.
- Prefer `query_dove_mission_board` through MCP, or run `paper-factory dove-board .` / `dove board .`.
- Return board goal, domain, stage, role boundary, mission queues, counts, filters, and return protocol.
- Treat packet paths as declared durable evidence, not as hidden git inspection.
- Do not write, repair, refresh, run tests, inspect git, execute autonomy, update the board, append handoffs, or materialize mission packets.
