#!/usr/bin/env node

// src/mcp/server.mjs
import process2 from "node:process";

// src/core/schema.mjs
import crypto from "node:crypto";

// src/core/command-manifest.mjs
var CORE_INSTALL_PATHS = [
  "README.md",
  "docs/README.md",
  "docs/INSTALL.md",
  "docs/USAGE.md",
  "docs/PACKAGING.md",
  "docs/CAPABILITY_MATRIX.md",
  "dist/index.mjs",
  "bin/dove-package.mjs",
  "mcp/dove-state-server-package.mjs",
  "scripts/doctor-mcp-probe-package.mjs"
];
var DEFAULT_HOST_ADAPTERS = ["opencode"];
var PROJECT_HOST_IDS = ["opencode", "codex", "cursor", "agents"];
var USER_HOST_IDS = ["claude"];
var HOST_IDS = [...PROJECT_HOST_IDS, ...USER_HOST_IDS];
var HOST_DEFINITIONS = {
  opencode: { label: "OpenCode", scope: "project", jsonChecks: [".opencode.json"] },
  codex: { label: "Codex", scope: "project", jsonChecks: [] },
  cursor: { label: "Cursor", scope: "project", jsonChecks: [] },
  agents: { label: "Shared agent skills", scope: "project", jsonChecks: [] },
  claude: { label: "Claude Code user commands", scope: "user", jsonChecks: [] }
};
var MANAGED_HOST_ADAPTER_PATHS = {
  opencode: [".opencode/commands/dove*.md", ".opencode/skills/dove-*", ".opencode.json"],
  codex: [".codex/skills/dove-*"],
  cursor: [".cursor/commands/dove-*.md"],
  agents: [".agents/skills/dove-*", "AGENTS.md"]
};
var MANAGED_PACKAGE_PATHS = [
  ".opencode/commands/dove*.md",
  ".opencode/skills/dove-*",
  ".opencode.json",
  ".codex/skills/dove-*",
  ".cursor/commands/dove-*.md",
  ".agents/skills/dove-*",
  "AGENTS.md",
  ...CORE_INSTALL_PATHS
];
var OPENCODE_ROLE_SKILL_PATHS = [
  ".opencode/skills/dove-pipeline/SKILL.md",
  ".opencode/skills/dove-planner/SKILL.md",
  ".opencode/skills/dove-builder/SKILL.md",
  ".opencode/skills/dove-researcher/SKILL.md",
  ".opencode/skills/dove-reviewer/SKILL.md",
  ".opencode/skills/dove-rebuttal-strategist/SKILL.md",
  ".opencode/skills/dove-experiment-planning/SKILL.md",
  ".opencode/skills/dove-version-analyst/SKILL.md",
  ".opencode/skills/dove-claim-gate/SKILL.md",
  ".opencode/skills/dove-review-loop/SKILL.md",
  ".opencode/skills/dove-citation-discipline/SKILL.md",
  ".opencode/skills/dove-rebuttal/SKILL.md"
];
var TASK_SCOPED_WRITE_TOOL_IDS = /* @__PURE__ */ new Set([
  "update_research_brief",
  "register_source",
  "upsert_note",
  "upsert_claims",
  "upsert_plan",
  "upsert_outline",
  "upsert_draft",
  "set_section_status",
  "upsert_figure_plan",
  "run_figure_workflow",
  "record_dove_mission_pass",
  "run_experience_workflow",
  "run_audio_review",
  "run_dove_review_loop",
  "prepare_figure_generation",
  "import_figure_generation",
  "build_rebuttal",
  "append_review_log",
  "run_review_loop",
  "prepare_isolated_review",
  "import_isolated_review",
  "upsert_revision_plan",
  "normalize_rebuttal_issues",
  "build_rebuttal_strategy",
  "upsert_experiment_plan",
  "upsert_experiment_result",
  "run_experiment_audit",
  "bridge_result_to_claim",
  "create_version_snapshot",
  "compare_versions"
]);
var TASK_SCOPED_WRITE_CONSTRAINTS = [
  "Before any task-scoped write, resolve the operator's target to an existing durable `.dove/task-packets` packet; never use the latest-created packet as the only implicit target.",
  "If no explicit packetId, natural-language target, or linked artifact is supplied and more than one packet candidate exists, stop and use confirmation UX before writing.",
  "If target resolution is ambiguous or multiple candidates share the top confidence, use confirmation UX to select a packet; `.dove/state.json.settings.taskTargetResolution.autoSelect` may only select a unique high-confidence candidate.",
  "Reject the write when explicit packet ids or linked artifact ids point to conflicting durable packets."
];
var AGENT_WORKFLOW_CONSTRAINTS = [
  "Use status first only for Dove state, next-step, blocker, task-choice, or task-binding questions; for fix, implement, research, verify, write, review, experiment, or figure prompts, start by producing or inspecting substantive material and use status only as supporting context.",
  "For ordinary prompts that ask to bind, save, deposit, archive, or \u6C89\u6DC0 results to a main task, resolve the durable packet first, register external URLs/templates/guidelines as packet-bound sources, then synthesize internal findings through `upsert_note` or `record_document_evidence` instead of treating the synthesis as an external source.",
  "When reporting research or venue results, separate snapshot-backed or registered sources from candidate links, blocked retrieval candidates, and internal notes/documents; do not put unverified candidates under a generic `Sources:` list.",
  "When work depends on current external information, public web material, provider/tool documentation, scholarly discovery, venue policy, or ecosystem behavior, run a visible bounded search or retrieval step first; use Dove's read-only public no-key network search tools when available, and treat returned items as candidates until verified.",
  "Do not let durable packets, status navigation, receipts, validators, or evidence ledgers substitute for real progress; before bookkeeping, produce or inspect a substantive artifact such as literature synthesis, experiment execution, baseline analysis, result interpretation, claim stress test, draft content, review finding, or code/test evidence.",
  "Treat `preActionGuidance` as read-only guidance that automatically recalls applicable lessons from `.dove/meta/operator-lessons.json`; recording lessons remains explicit through `/dove:lessons` and `record_operator_lesson` only.",
  "Frame work through Planner, Builder, and Reviewer primary roles; researcher, experiment-planner, revision-lead, rebuttal-lead, version-analyst, and review-loop are subagents/modes under those roles, not public slash surfaces.",
  "Planner output must be executable: every new or plan-derived mission needs canonical `executionContract.action`, `implementation`, `convergence.criteria`, and `failureRoutes`; do not invent substitute child missions or infer child work from the parent title when explicit child mission details are missing.",
  "Builder completion requires a result summary plus real evidence/artifact/validation/verification paths and `verifiedCriteria` that covers every `executionContract.convergence.criteria` item; read-only status, summary-only output, or unknown step status must not mark work complete.",
  "Reviewer and audit work may inspect evidence and record explicit review state, but must stay read-only with respect to Builder outputs unless an operator explicitly asks to record review/revision artifacts.",
  "Treat status as the project command center and mission as a durable work contract/progress object; rank missing executable contracts, missing source/material inputs, ready Builder execution, verification gaps, reviewer/audit needs, and reconciliation above optional mission details.",
  'Dove `.dove/` durable state participates in host rollback only through host-tracked file edits: request `mutationMode: "patch-plan"`, inspect the returned operations, and apply them with the host\'s tracked file-edit mechanism. Direct CLI/MCP `direct-process` writes remain functional but rollback-unverified; git presence is not proof, and host rollback must not be routed through `reset_dove_version`.',
  "Every read or query path is strictly zero-write: it must return `writes: []`, must not bootstrap or refresh derived state, and must not be combined with a mutation in the same query call.",
  "Public follow-through recording is advisory bookkeeping only and never grants execution authority; public `materialize_guidance_packet` and `launch_dove_mission` fail closed because only a trusted system-owned runtime approval can authorize those transitions.",
  "Never create hidden runtime, scheduler, daemon, background continuation, or unconfirmed writes; mission stops after contract materialization and handoff, auto is a separately confirmed bounded foreground run, and operator advances only from canonical supplied `taskResults[]`."
];
var COMMAND_SURFACES_BASE = [
  {
    id: "dove.init",
    title: "Dove init",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Create or update the single project-level Dove goal as the unique level-0 task.",
    requiredTools: ["init_dove_goal"],
    constraints: ["There is exactly one level-0 init task; update it instead of creating a second root.", "Use init only for the global project goal, then route concrete work through `/dove:mission` or `/dove:auto`."]
  },
  {
    id: "dove.mission",
    title: "Dove mission",
    domain: "generic",
    category: "mutation",
    policy: "explicit-approval",
    summary: "Convert a user demand into a confirmable Dove task contract, then hand off to the recommended next workflow.",
    requiredTools: ["create_dove_task"],
    constraints: ["Require an existing init goal or make the proposed init goal explicit before converting user demand into a mission task contract.", "Treat the operator input as natural-language demand, not as an already-created task.", "Return a proposal-only mission contract first: title, stage, domain, level, dependencies, blockers, autonomous checklist proposal, compact task card, durable `workContract`, and canonical `executionContract` with action, implementation, materials/readFirst requirements, convergence criteria, evidence requirements, and failure routes.", "After returning the proposal, use interactive confirmation controls when the host supports them (for example Claude Code AskUserQuestion) with options: approve and materialize the contract, adjust the contract, or cancel. Approval must replay the proposal's complete returned `confirmArgs`; when using the listed CLI route, run the exact confirmation command returned by that proposal, including its proposal token and fixed mutation mode. Never reconstruct a fresh `dove mission --goal ... --confirmed` request.", "Classify each task as `plan`, `execute`, or `audit` and as `paper`, `experiment`, or `engineering` before writing.", "User-created mission tasks default to level 3, while explicit operator-created levels 1, 2, 3, or deeper are allowed under the level-0 init goal.", "Autonomously decide whether a checklist is needed; system-created checklist/subtask packets must be children of their mission and must have level greater than the parent mission level.", "After materialization, stop at the contract handoff: return `nextAction`, `recommendedNextCommand`, `recommendedRoutes`, and `handoffRoutes` from the task's work contract instead of executing or recording work. This natural handoff does not grant execution authority.", "Do not claim the mission performed source, note, draft, figure, experiment, review, code, or provider work; execution belongs to a separately invoked and authorized `dove.auto`, `dove.operator`, domain workflow, or explicit tool call after the contract exists.", "Do not call `record_dove_mission_pass` as part of `/dove:mission`; result recording is a separate explicit tool for work that already happened.", "If required materials are missing while defining the contract, keep the contract proposal honest about blockers and evidence expectations instead of inventing results.", "When the converted task is a planning task, its done criteria should require explicit executable child mission contracts before any later execution flow can mark it completed.", "Default the converted user-level mission to level 3 and `pending`; any converted child missions may be level 4, 5, or deeper and must also default to `pending`."]
  },
  {
    id: "dove.auto",
    title: "Dove auto",
    domain: "generic",
    category: "mutation",
    policy: "explicit-approval",
    summary: "Convert demand like mission intake, then after confirmation run a few approved work rounds until completion or a blocker is reached.",
    requiredTools: ["run_dove_auto"],
    constraints: ["Use the same demand-to-task intake and classification model as `/dove:mission` before autonomous execution starts.", "Allow `/dove:auto` to be invoked directly on a new user demand or an existing durable task; it does not require running `/dove:mission` first.", "Return a proposal-only auto contract first: either a converted `proposedTask` with checklist proposal and executable `executionContract`, or a `selectedTask` from durable packet selection with current contract readiness, plus compact task/auto cards, confirmation args, and max iteration budget.", "Use interactive confirmation controls when the host supports them (for example Claude Code AskUserQuestion) before passing `confirmed: true`; options should approve and run bounded auto, adjust target/contract, or cancel.", "When an existing task target is missing or ambiguous, present indexed packet choices through confirmation UX instead of guessing.", "Require explicit operator confirmation before execution beyond task creation or selection.", "Run in the current foreground call only; do not schedule background or daemon continuation after the response ends.", "Use `.dove/state.json.settings.auto.maxIterations` as the default foreground iteration limit; the default is 3.", "Record each foreground iteration and stop reason in `.dove/runtime/results.json`, and return a localized `resultCard` summary without persisting the UX-only card in runtime results.", "May internally call public Dove workflows such as source, note, experience, figure, draft, review, review-loop, rebuttal, lessons, and status as needed.", "For source-research and current-information tasks, run the foreground search or retrieval pass before confirmed execution: use read-only public no-key network search when available, collect concrete URLs/DOIs/templates/guidelines, extract enough synthesis text, then call confirmed `run_dove_auto` once with explicit `steps` for both `dove.source` and `dove.note` so provenance and synthesis are deposited in the same auto run.", "Do not claim source research succeeded when Dove network search, host search, or fetch tools return zero results, unavailable providers, safety errors, or no concrete URLs/snippets; switch to another allowed foreground retrieval path or stop at an explicit host boundary.", 'If host search/fetch/shell/MCP safety classification or tool availability fails before Dove can perform the intended workflow, call `record_dove_mission_pass` for the packet with `resultStatus: "blocked"`, `boundaryType: "host-tool-blocked"`, the failed tool in `requiredActions`, and `nextAction: "project:dove.status"`; do not leave the task in-progress.', "Do not call confirmed `run_dove_auto` with only a packet id for source-research tasks; that only records a `source-requires-host-provenance` boundary and does not advance the research.", "Do not auto-run source, note, draft, experience, or review-loop steps without the material they need: source needs title/locator provenance, note needs synthesis content, draft needs body content, experience needs a goal/title/idea/experimentId, and review-loop needs packet-owned substantive review artifacts.", "Stop at completed, blocked, killed, authority/review boundary, missing provider credentials, conflicting packet target, or step-budget exhaustion.", "When a boundary is reached, persist the first-class boundary with required inputs/actions, role handoff, and next command; do not continue through hidden background work.", "Do not claim host/code/provider/experiment work was completed without real evidence, verification evidence, and `verifiedCriteria` coverage for the executable contract; stop at an awaiting-host/provider, missing-materials, or verification-failed boundary instead."]
  },
  {
    id: "dove.status",
    title: "Dove status",
    domain: "generic",
    category: "mutation",
    policy: "explicit-approval",
    summary: "Show a natural Dove status home that explains the current situation, the smallest useful next action, and how to expand only when needed.",
    requiredTools: ["query_dove_status", "apply_dove_status_adjustments"],
    constraints: ["Use status as the unified whole-project situation home; do not expose separate plan, checklist, audit, return, orchestration, missions, board, or list slash surfaces.", 'First call `query_dove_status` without `detail: "full"` to obtain the compact read-only Dove project situation home, but do not present saved `.dove/` files as the live development situation. Explain the live development situation from host-visible context first: current user request, current session work, known worktree state when available, latest validation/test evidence, active implementation blockers, and what was just completed or is still pending. If live context was not inspected, say which live checks were skipped in ordinary terms such as tests, worktree, or manual artifacts; do not name `.dove/`, durable context, MCP/schema fields, or storage files in the default answer.', "When MCP tools are unavailable and you must inspect `.dove/` files directly, synthesize the same compact public situation home instead of narrating the file schema: translate fields and tool ids into user actions such as provide or import the SVG, rerun figure validation, import the reviewer handoff, or record the experiment result. Default prose should say saved project state or standing guidance instead of `.dove/`, durable state, lessons, context files, or schema names.", "Keep `query_dove_status` read-only: it must return `proposalOnly: true`, `noAutoApply: true`, and `writes: []`.", "Keep durable rollback, boundary, gap, runtime, provider, artifact-path, and schema-field diagnostics in full/debug expansion; default compact status should describe the practical situation instead of printing internal governance or storage fields, and should translate internal figure-check records into current-figure review issues.", "Use compact `statusHome` as a public situation contract: `headline`, `scope`, `currentContext`, `nextStep`, `needsAttention`, `changes`, `showMore`, and explicit expansion handles. Render those fields as natural action guidance, not as a fixed heading template or a debug dump.", "Do not make mission lists, packet ids, phase ids, role labels, version ids, boundary/gap codes, blocked counts, execution-gap counts, repair-frontier names, route strings such as `project:dove.source`, raw artifact fields such as `sourceSvgPath`/`finalSvgPath`, low-level tool ids such as `validate_figure_pipeline`, claim-bridge jargon, or required-evidence blocks the default body of `/dove:status`; default status must answer `what should I do next?` with one recovery or continuation action, and `statusHome.optionalMissionDetails` remains collapsed unless the operator explicitly asks for `show current missions`, `\u6709\u54EA\u4E9B mission`, `--missions`, `showMissions`, or `includeMissionDetails`.", "For normal mission-list prompts, call compact `query_dove_status` with `showMissions: true` or `includeMissionDetails: true` and expand `statusHome.optionalMissionDetails`; do not add or require `/dove:missions`, `/dove:board`, `/dove:list`, and do not route mission-list questions to `/dove:mission`.", "Treat `query_dove_mission_board` as a low-level MCP/debug board, not the default host route for ordinary mission-list prompts.", 'Do not read a saved full status result file or request `detail: "full"` unless the operator explicitly asks to expand/debug full details.', "When `statusHome.blockersAndReconciliation.completionConsistency.status` is `needs-reconciliation`, present it as a legacy consistency issue: a done parent mission still has open checklist children. Do not recommend blanket-marking children done/completed; say to verify child evidence first, then either mark covered children done through confirmed status adjustment or reopen the parent mission.", "Use compact `nextStep` and `needsAttention` for the default explanation; read `actionableBoundaries`, `boundaryActionCards`, durable context, and current boundary metadata only in full/debug or when the operator asks for details.", "Use compact cards for status adjustment previews when available. Do not print raw internal dumps such as raw status-count objects, recent completed mission recaps, or recent killed mission recaps; if showing optional mission details, keep the `done` group collapsed unless the operator asks to expand it.", 'Boundary types are first-class metadata, not machine status choices; keep status choices exactly `["pending", "ready", "in-progress", "blocked", "completed", "killed", "archived"]`; `archived` is a cleanup lifecycle state, not deletion.', "When the host supports interactive confirmation controls, do not ask whether to modify mission statuses during default `/dove:status`; use a single confirmation dialog only when the operator clearly asks to change states or calls `query_dove_status` with `requestStatusAdjustment`/`includeStatusAdjustmentPreview`; build compact adjustment cards from adjustable missions excluding `completed`, `killed`, and `archived`; do not paginate by mission count or collect choices across multiple dialogs.", "The single confirmation dialog must provide a no-change path and a change/provide-adjustment-details path; if the operator does not provide parseable `packetId -> status` adjustments in that single dialog, do not call a mutation tool and instead ask for a clear adjustment format.", 'For status adjustment choices, preserve exactly `["pending", "ready", "in-progress", "blocked", "completed", "killed", "archived"]` as the machine status enum; default status hides archived missions and `includeArchived` may show archived counts/details without deleting evidence.', "Only call `apply_dove_status_adjustments` with `confirmed: true` after that single dialog yields explicit operator-confirmed status adjustments, then show the localized `resultCard` summary.", "Killing a mission is now a status choice in this UX, not a standalone public slash command."]
  },
  {
    id: "dove.operator",
    title: "Dove operator",
    domain: "generic",
    category: "mutation",
    policy: "explicit-approval",
    summary: "Run one approved operator pass over safe built-in steps, explicit results, and optional blocker-investigation planning.",
    requiredTools: ["run_dove_operator"],
    constraints: ["First call `run_dove_operator` without confirmation to return the proposal-only execution contract with compact `queueSummary`, small `queuePreview`, and `writes: []`; do not request full queue arrays unless the operator explicitly asks for `includeQueueDetails: true`.", "Use interactive confirmation controls when the host supports them before passing `confirmed: true`.", "Run in the current foreground call only; do not schedule background or daemon continuation after the response ends.", "Use only canonical `taskResults[]` for host-supplied results; reject aliases, maps, inferred results, and result fields outside each canonical task result object.", "For ready and in-progress missions, run one safe internal workflow step when available or collect one real host pass result in order; pass per-task results to `run_dove_operator` with evidence, verificationEvidencePaths, and verifiedCriteria so Dove records lifecycle and runtime state only when the executable contract is covered.", "Do not advance lifecycle, open a synthetic progress boundary, or persist a runtime result for a host-pass-required mission when its canonical `taskResults[]` entry is missing; leave that mission unchanged and return the required material/actions.", "Do not claim real engineering, paper, or experiment work happened when neither a safe internal step nor an actual host pass result exists; host results without convergence coverage must become explicit verification/material boundaries with no fake execution.", "When no safe internal step, missing step material, or actual host pass result exists, do not persist an `awaiting-host-pass-result` boundary just to show activity; return the material-specific requiredActions and keep durable writes empty unless another real operator action occurred.", "If a host-side search/fetch/shell/MCP safety classifier or tool-availability failure prevents collecting the pass result, pass a blocked task result with boundaryType `host-tool-blocked` and requiredActions naming the failed host tool instead of leaving the mission in-progress.", "Preserve durable role handoff metadata while running queue passes; do not expose planner/builder/reviewer as separate slash commands.", 'For blocked missions, default to proposal-only blocker-investigation guidance and do not write child missions; create pending child investigation plan missions only when the operator explicitly requests canonical `blockerInvestigationMode: "create"`, then report created and reused counts separately in the localized `resultCard` summary.']
  },
  {
    id: "dove.lessons",
    title: "Dove lessons",
    domain: "generic",
    category: "mutation",
    policy: "governed-bookkeeping",
    summary: "Inspect or record global and task-bound lessons that future Dove work must obey.",
    requiredTools: ["query_operator_lessons", "record_operator_lesson"],
    constraints: ["Record only distilled lessons with problem, decision, pitfall, validation, and next-time guidance.", "When recording a task-specific lesson and multiple tasks exist, return an indexed task list and wait for the operator to choose.", "Allow manual global lessons when no task binding is intended.", "Surface applicable must-obey lessons before later task mutations.", "Do not import or cite ignored raw runtime traces."]
  },
  {
    id: "dove.version",
    title: "Dove version",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Create a direction-change point and clear active non-init work with confirmation.",
    requiredTools: ["reset_dove_version"],
    constraints: ["For direction changes, snapshot the current direction before resetting active tasks.", "Do not use `/dove:version` as a `.dove` rollback restore entrypoint; host/context rollback belongs to the host and can cover Dove workflow artifacts only when patch-plan operations are applied through host-tracked file edits.", "Preserve the level-0 init goal and required global or task lessons during direction-change resets.", "Return a clean status summary and recommend `/dove:mission` for the next direction after direction reset."]
  },
  {
    id: "dove.source",
    title: "Dove source",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Collect and organize external material such as web pages, papers, venue templates, reviewer guidelines, rankings, APIs, or operator-provided sources for the selected task.",
    requiredTools: ["query_sources", "register_source", "verify_source"],
    constraints: ["Treat source as external information intake, not internal note consolidation; pressure-test summaries and writing-style synthesis belong in note or document evidence.", "Use `register_source` with `sources: [...]` for batch provenance capture when the operator provides multiple URLs/templates/guidelines at once.", "When current public information or scholarly discovery is needed, use read-only public no-key network search or visible retrieval to find candidate material first, then capture the real title, locator, DOI/URL, source identity, and citation details when registering it.", "Every `register_source` write creates or refreshes a candidate only; registration never issues positive verification and candidate material cannot support claims.", "Public `verify_source` is rejection-only. Positive verification is publicly unavailable and can only come from a trusted internal transition bound to captured material, the current source fingerprint, and the packet.", "Dove network search results are candidate material, not durable source evidence; snippets alone must not become registered sources or claims.", "Never call `register_source` with only a packet id; every new source must include a real title or locator, and source-research auto runs must collect those URLs/templates/guidelines before writing.", "Treat host search output such as `Did 0 searches`, zero results, empty result sets, or unavailable search as a hard retrieval failure; do not describe it as finding official sources, and do not infer locators from memory or prior transcript context.", "Do not call `register_source` when search/fetch returned zero results, safe-domain verification failed, retrieval was blocked, or a network search provider reports unavailable; record or surface a `host-tool-blocked` boundary until verifiable source evidence exists.", "If the host denies or blocks the boundary-recording mutation, stop and report that no durable source or boundary update was written; do not retry another mutating Dove call such as patch-plan without explicit operator approval.", "Use explicit configured providers or operator-provided material; do not hide network/provider calls.", "Link each source to the resolved durable task packet through packetIds.", "Treat source and source-verification JSON ledgers as bookkeeping, not completion artifacts; use typed source:<id> completion evidence only when the latest verification decision is verified and both fingerprint and packet binding match.", "For reviewer-guideline or \u5BA1\u7A3F\u504F\u597D research, stay in Builder/researcher source intake unless the operator asks for an independent audit of an artifact."]
  },
  {
    id: "dove.note",
    title: "Dove note",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Organize task-bound internal synthesis from registered sources, existing materials, pressure-test results, and operator notes for the selected task.",
    requiredTools: ["upsert_note"],
    constraints: ["Treat note as internal information consolidation, not external source discovery; external URLs/templates/guidelines must already be registered as sources when they are evidence.", "Use notes to synthesize verified sources, compare candidates, record open questions, or explain why a candidate could not yet become evidence; keep unverified search snippets labeled as candidates.", "For bind, save, deposit, or archive requests, write the synthesized findings here or in `record_document_evidence` after source details is registered.", "Do not create a new note without real synthesis content: summary, quote, claim, or open question.", "Link notes to the resolved durable task packet through packetIds and to relevant sourceIds/artifacts."]
  },
  {
    id: "dove.figure",
    title: "Dove figure",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Turn one user-described figure intent into materials, optional generation/import, caption support, checks, and a clear answer about whether this figure is usable now.",
    requiredTools: ["run_figure_workflow"],
    constraints: ["Treat the user request as one figure intent; the default result should say whether this figure is ready, what is missing, or what to do next without exposing the material/import/check pipeline as separate user chores.", "Treat the default figure path as a hand-drawn SVG plan: record the backlog item, material bundle, generation prompt, and waiting-for-output state instead of routing the operator to a separate low-level figure-plan write.", "Use the built-in OpenAI image provider only when the operator explicitly selects it or config sets it as default; require `OPENAI_API_KEY` through env-var secret reference and do not store inline API keys.", "Resolve the durable task packet before any figure workflow write, then analyze linked sections, claims, experiments, sources, notes, review concerns, and material hints automatically.", "Use redacted Dove config and env-var secret references for external drawing providers; never store inline API keys, tokens, or secrets.", "Treat lexical, XML, safety, caption, provenance, and other structural figure QA as diagnostic preflight only, not authoritative validation.", "Set figure status to `validated` only when the current final SVG hash has current authorized independent Reviewer proof in addition to clean diagnostics, durable provenance, and caption support.", "Captions must explain the figure purpose and linked evidence."]
  },
  {
    id: "dove.experience",
    title: "Dove experience",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Convert an idea into experiment goals/plans/results and bridge validated outcomes into claims or conclusions.",
    requiredTools: ["run_experience_workflow"],
    constraints: ["Use this as the combined experiment and claim workflow; do not expose separate public experiment or claim-gate slash commands.", "Make experiment goals, success criteria, result evidence, audit status, and claim impact explicit.", "Do not create a placeholder experience plan without a real goal, title, idea, or experimentId.", "Do not promote unsupported results into claims."]
  },
  {
    id: "dove.draft",
    title: "Dove draft",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Generate or modify paper draft content from prompts, existing materials, experience, figures, and review information.",
    requiredTools: ["upsert_draft", "set_section_status"],
    constraints: ["Draft as completely as current evidence allows.", "Do not create or update a draft without body content; use `set_section_status` for metadata-only updates.", "Use explicit placeholders for missing evidence or citations inside real draft content instead of fabricating support.", "Incorporate applicable source, note, experience, figure, and review context linked to the resolved task."]
  },
  {
    id: "dove.review",
    title: "Dove review",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Run a local evidence-aware review pass over selected task materials and produce concrete revision guidance.",
    requiredTools: ["run_review_loop"],
    constraints: ["Default review performs a local evidence-aware structural preflight over claims, sources, notes, drafts, experiments, figures, and recorded concerns for the selected task or paper pipeline.", "Do not treat a verdict string or a clean local structural scan as authoritative review progress; return concrete findings, action items, missing evidence, or a preflight result.", "A `coherent` result requires current authorized independent Reviewer proof bound to every reviewed artifact hash; without that proof, stop at the Reviewer-owned proof boundary.", "Use isolated audio or external reviewer mode only when the operator explicitly asks for isolated/audio/parallel reviewer review; ordinary review should not inherit broad private transcripts or pretend an external review ran."]
  },
  {
    id: "dove.review-loop",
    title: "Dove review loop",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Run one local Reviewer preflight pass and return an explicit Builder handoff when revision is required.",
    requiredTools: ["run_dove_review_loop"],
    constraints: ["Run exactly one Reviewer pass for the selected packet and descendants.", "Treat local structural inspection as preflight only; it cannot issue an authoritative `coherent` result without current authorized independent Reviewer proof bound to the complete reviewed artifact hashes.", "Do not modify draft, experience, experiment, or other Builder-owned material in this call.", "If substantive findings make the result non-coherent, return concrete requiredActions and an explicit Builder handoff; if only authoritative proof is missing, retain Reviewer ownership instead of routing a fake Builder revision.", "Revisions and any later Reviewer pass must be separate explicit calls.", "Do not claim iteration counts, configured rounds, review-until-coherent behavior, or authoritative independent review from a local scan; do not prepare isolated/audio review unless the operator explicitly asks for that mode."]
  },
  {
    id: "dove.rebuttal",
    title: "Dove rebuttal",
    domain: "generic",
    category: "mutation",
    policy: "guarded-mutation",
    summary: "Normalize reviewer issues, build a rebuttal strategy, and draft submission/revision responses.",
    requiredTools: ["normalize_rebuttal_issues", "build_rebuttal_strategy", "build_rebuttal"],
    constraints: ["Normalize reviewer issues before drafting responses.", "Keep rebuttal and revision response work author-side.", "Link each response to claims, draft sections, experiments, or explicit unresolved placeholders."]
  }
];
var COMMAND_UX_DETAILS = {
  "dove.init": {
    dailyFlow: ["Use this when the workspace needs one clear Dove goal or that goal needs an explicit refresh.", "Keep concrete work out of init; after the goal is set, move the actual request to mission or auto."],
    targetingBehavior: "No task target is needed because this updates the project-level goal.",
    confirmationBehavior: "Update the existing goal rather than creating another root goal.",
    expectedOutcome: "The workspace has one clear project goal and the next practical step is a mission or auto run.",
    examples: ["/dove:init Make Dove a local-first research and engineering workflow", "/dove:init Refresh the project goal around daily Dove usability"]
  },
  "dove.mission": {
    dailyFlow: ["Use this for one concrete user demand that should become a tracked mission/task contract and hand off naturally to the right next workflow.", "Describe the desired outcome in normal language; Dove should propose the task, explain the evidence it will need, and wait for approval before materializing the contract."],
    targetingBehavior: "Creates a new task contract under the project goal, or helps set the project goal first when the workspace is new.",
    confirmationBehavior: "Show the proposed task in plain language, then ask whether to materialize that exact proposal, adjust it, or cancel; confirmation must reuse the returned task id and proposal digest rather than re-infer a new contract.",
    expectedOutcome: "After approval, the contract exists with recommended next routes; mission does not execute work or grant execution authority, so auto, operator, domain workflows, or explicit tools require their own invocation and authority.",
    examples: ["/dove:mission Fix the status dashboard next-action mismatch", "/dove:mission Turn the latest review feedback into one executable task"]
  },
  "dove.auto": {
    dailyFlow: ["Use this when the user wants Dove to continue through a few approved steps after the task is clear.", "When the work depends on current outside information, public docs, papers, or provider behavior, run a visible no-key search/retrieval step early, register useful material as candidates, and stop at the trust boundary unless current trusted internal verification makes the material eligible as evidence.", "Auto may start from a new demand or an existing task, but it still needs an understandable proposal before spending its work limit."],
    targetingBehavior: "Use the selected task when it is obvious; otherwise ask the operator to choose or approve a new task.",
    confirmationBehavior: "Require a separate explicit approval of the target, work limit, and visible steps before running; mission confirmation never confirms auto.",
    expectedOutcome: "An approved in-session run completes useful work with evidence or stops at a clear blocker, review need, missing material, or work limit.",
    examples: ["/dove:auto Continue the current Dove UX improvement task for up to three approved rounds", "/dove:auto Run the selected task until completion or an explicit blocker"]
  },
  "dove.status": {
    dailyFlow: ["Use this to answer the ordinary operator question: what should I do next?", "Default output should read like a project assistant: briefly explain the current situation, name the smallest useful next action, and mention expansion only when it helps. Do not impose a fixed four-line template or a numbered checklist by default.", "If saved project facts have to be inspected directly, translate them into user actions instead of repeating file names, ids, route names, tool names, or storage terms."],
    targetingBehavior: "Default output is not a mission board or audit report; keep mission lists, raw counts, and extra details collapsed unless the operator asks to expand.",
    confirmationBehavior: "Do not ask for status changes during default status; preview or apply changes only after an explicit status-change request and one clear confirmation step.",
    expectedOutcome: "The operator gets a short, natural status answer with one useful next step and enough context to decide whether to expand.",
    examples: ["/dove:status", "/dove:status Show what is blocked and what the next step is"]
  },
  "dove.operator": {
    dailyFlow: ["Use this to see which tracked work can move now and run one approved operator pass.", "Do not claim real work happened unless a safe built-in step ran or a canonical `taskResults[]` entry supplied the real pass result."],
    targetingBehavior: "Works over the active work queue rather than one ad hoc target.",
    confirmationBehavior: "Preview the practical queue situation first; require approval before accepting canonical results or creating blocker-investigation work.",
    expectedOutcome: "Runnable work moves with evidence; a task waiting for an external pass remains unchanged when no canonical result is supplied, and blocked work gets a concrete investigation option only when requested.",
    examples: ["/dove:operator", "/dove:operator Run one confirmed queue pass with real results"]
  },
  "dove.lessons": {
    dailyFlow: ["Use this when a closed task yields reusable guidance that future Dove work should obey.", "Keep lesson entries explicit and short: problem, decision, pitfall, validation, and next-time guidance."],
    targetingBehavior: "Can add global lessons or bind a lesson to a selected task.",
    confirmationBehavior: "When task binding is ambiguous, show task choices and wait for the operator.",
    expectedOutcome: "Applicable lessons are recalled later as standing guidance without importing raw traces or adding new lessons implicitly.",
    examples: ["/dove:lessons Add that status should not show completed or killed mission lists", "/dove:lessons Show lessons that apply to the selected task"]
  },
  "dove.version": {
    dailyFlow: ["Use this when the project direction changes enough that active non-root work should be cleared.", "Treat it as a direction reset, not as a general undo command."],
    targetingBehavior: "Operates on the current workspace direction and active tasks.",
    confirmationBehavior: "Require a reason before clearing active work.",
    expectedOutcome: "A direction reset stores the old direction, clears active non-root work, and points the next step at a fresh mission.",
    examples: ["/dove:version Change direction to focus on result-card usability", "/dove:version Reset active tasks after a major project direction change"]
  },
  "dove.source": {
    dailyFlow: ["Use this to add external information such as papers, web findings, venue templates, reviewer guidelines, rankings, API docs, citations, or operator-provided links or material.", "When the user asks for current outside information or scholarly material, run read-only public no-key network search or visible retrieval as candidate discovery, then register useful material as candidates with real identity and locator details.", "For bind, save, deposit, or archive prompts, add external candidate material first, then use note or document evidence for synthesis.", "Keep source intake separate from internal notes and pressure-test summaries."],
    targetingBehavior: "Resolve or confirm the task before adding external source candidates; batch multiple sources when the operator provides them together.",
    confirmationBehavior: "If no unique task target is available, ask for task selection instead of guessing.",
    expectedOutcome: "The selected task has candidate source details. Public verification may reject a candidate, but positive verification is unavailable on the public surface.",
    examples: ["/dove:source Add these CVPR author/reviewer guideline URLs to the selected task", "/dove:source Add venue templates and ranking pages before writing the synthesis note"]
  },
  "dove.note": {
    dailyFlow: ["Use this to consolidate internal information from added sources, existing materials, pressure-test results, or operator notes.", "Use source for external material; use note for project-local synthesis, candidate comparison, open questions, and writing-style or reviewer-preference summaries.", "For bind/save/deposit/\u6C89\u6DC0 prompts, add the synthesized result here or in document evidence after source details are available."],
    targetingBehavior: "Resolve or confirm the task before writing notes.",
    confirmationBehavior: "If the target is missing or ambiguous, ask for task confirmation before writing.",
    expectedOutcome: "The selected task has internal notes linked to relevant sources and materials.",
    examples: ["/dove:note Summarize what the registered venue sources imply for this task", "/dove:note Capture the pressure-test finding and link it to registered sources"]
  },
  "dove.figure": {
    dailyFlow: ["Use this when the user describes the figure they want once, including where it should help the paper or task.", "Dove should gather linked materials, prepare generation or import, draft caption support, run diagnostic checks on the current figure, and say what remains before validation.", "Lexical and structural SVG checks are preflight only; validated requires current authorized independent proof for the current final SVG hash.", "By default, prepare a hand-drawn SVG plan and tell the operator when SVG output is needed.", "Use OpenAI image generation only for an explicit drawing request with OPENAI_API_KEY supplied through the environment."],
    targetingBehavior: "Resolve the figure request to one task before writing; do not make the user reason about paths or workspace-wide extra details unless they explicitly ask for details.",
    confirmationBehavior: "Ask for task confirmation when the figure target is unclear; external drawing calls require explicit safe configuration.",
    expectedOutcome: "The operator gets a clear current-figure result: ready for review, missing materials, awaiting SVG or drawing output, or needing current-figure fixes.",
    examples: ["/dove:figure Draw a workflow diagram for the mission-auto-status loop", "/dove:figure Prepare the main results figure and caption support"]
  },
  "dove.experience": {
    dailyFlow: ["Use this as the experiment and evidence workflow: turn ideas into experiment plans, results, audits, and claim impact.", "Do not treat experience as general retrospectives; use lessons for reusable operator guidance."],
    targetingBehavior: "Resolve the experiment or evidence work to one task before writing.",
    confirmationBehavior: "Ask for task confirmation when the experiment, result, or claim signal does not identify one task.",
    expectedOutcome: "Experiment plans, reviewed results, and claim impact are connected to the selected task.",
    examples: ["/dove:experience Design an experiment to validate retrieval quality", "/dove:experience Import this experiment result and connect it to the claim"]
  },
  "dove.draft": {
    dailyFlow: ["Use this to generate or revise paper sections from the selected task, evidence, notes, sources, experiences, figures, and review findings.", "Write as much as current evidence supports and leave explicit placeholders for gaps."],
    targetingBehavior: "Resolve the draft request to one task before changing draft content.",
    confirmationBehavior: "Ask for task confirmation when the section or task target is ambiguous.",
    expectedOutcome: "Draft content or section status is updated with evidence-aware placeholders where needed.",
    examples: ["/dove:draft Draft the methods section from linked evidence", "/dove:draft Revise the introduction using the latest review findings"]
  },
  "dove.review": {
    dailyFlow: ["Use this for a local evidence-aware structural preflight over the selected task materials.", "Inspect concrete claims, sources, notes, drafts, experiments, figures, and recorded concerns; do not substitute a verdict label or a clean local scan for authoritative review work.", "A coherent result requires current authorized independent Reviewer proof bound to all reviewed artifact hashes.", "Use separate isolated or audio review only when the operator explicitly asks for that mode."],
    targetingBehavior: "Resolve the review to one task and the exact materials being reviewed.",
    confirmationBehavior: "If the target or reviewed material is unclear, ask for the material instead of guessing or falling back to a status panel.",
    expectedOutcome: "The operator gets concrete findings, action items, missing evidence, or a Reviewer-owned proof boundary; coherent is authoritative only with current independent proof.",
    examples: ["/dove:review Check whether the current draft is supported by evidence", "/dove:review Review the selected task materials before marking them done"]
  },
  "dove.review-loop": {
    dailyFlow: ["Use this for one Reviewer pass over the selected packet materials.", "Local structural inspection is preflight only; it records concrete findings or stops at the Reviewer-owned proof boundary, but never edits Builder-owned draft or experience material.", "Only current authorized independent proof over all reviewed artifact hashes can support coherent.", "When substantive revision is required, hand the required actions to a Builder and invoke review again only after that separate revision call."],
    targetingBehavior: "Resolve the pass to one task and its packet-owned materials before reviewing.",
    confirmationBehavior: "This command performs one visible Reviewer pass only; there is no implicit Reviewer-to-Builder-to-Reviewer cycle.",
    expectedOutcome: "One packet-scoped review result, with a Builder handoff only for substantive findings and retained Reviewer ownership when authoritative proof is the missing requirement.",
    examples: ["/dove:review-loop Run one independent evidence-aware review pass", "/dove:review-loop Review this packet and hand required revisions to Builder"]
  },
  "dove.rebuttal": {
    dailyFlow: ["Use this to organize reviewer issues, build response strategy, and draft evidence-backed rebuttal or revision text.", "Keep rebuttal work author-side and linked to claims, sections, experiments, or explicit gaps."],
    targetingBehavior: "Resolve the rebuttal work to one task and the reviewer issues being answered.",
    confirmationBehavior: "Do not draft final responses from unsupported issues or missing evidence.",
    expectedOutcome: "Reviewer issues, strategy, and response drafts are ready with usable evidence links.",
    examples: ["/dove:rebuttal Normalize reviewer issues and build the response strategy", "/dove:rebuttal Draft an evidence-backed response for the missing-experiment concern"]
  }
};
var COMMAND_ADAPTER_CONSTRAINTS = {
  "dove.init": [
    "Keep init limited to the project goal; do not start concrete research, writing, review, or engineering work here.",
    "After the goal is set, name the practical next Dove surface in ordinary language instead of exposing storage details."
  ],
  "dove.mission": [
    "Propose the task first, then ask whether to materialize that exact proposal, adjust it, or cancel; never rebuild a different contract from a bare confirmation.",
    "After approval, materialize the contract only and hand off naturally to the recommended next workflow; mission itself does not execute source, note, draft, figure, experiment, review, code, or provider work and grants no execution authority.",
    "Report the created contract and recommended next routes without claiming completion or execution progress.",
    "If the contract is planning work, its done criteria must require explicit executable child mission contracts before any later execution flow can mark it completed."
  ],
  "dove.auto": [
    "Propose the target, work limit, and visible steps before running, and require a separate explicit auto confirmation even if mission materialization was already confirmed.",
    "Run only in the current approved interaction; never schedule hidden background continuation.",
    "For research or current-information work, use visible search/retrieval when needed, register real candidate material, and claim evidence-backed success only when current trusted internal verification makes the referenced source eligible; otherwise stop at the trust boundary.",
    "Stop clearly at completion, blocker, review need, missing material, or budget limit."
  ],
  "dove.status": [
    "Keep the status query strictly zero-write; status adjustments are a separate confirmed mutation and are never part of the query call.",
    "Start with the live situation the current session can actually see, then fold in saved project state only as background guidance.",
    "Answer the operator's ordinary next-step question in short natural prose: the current situation, the smallest useful action, and why it matters when helpful.",
    "Default status is not a mission board or audit report; keep mission lists, raw identifiers, raw counts, route names, low-level fields, and extra details collapsed unless the operator asks to expand.",
    "When the operator asks to show missions, expand mission details inside this status surface instead of inventing separate list, board, or mission-board commands.",
    "Only preview or apply status changes after an explicit status-change request, using one confirmation step and a clear no-change path.",
    "For legacy parent/child consistency issues, tell the operator to verify child evidence first, then either mark covered children done through confirmed status adjustment or reopen the parent.",
    "After confirmed status changes, return a localized human summary instead of a raw update log."
  ],
  "dove.operator": [
    "Preview the practical queue situation before asking for approval.",
    "Run one operator pass only after confirmation.",
    "Accept externally performed work results only through canonical taskResults[]; a task awaiting that work remains unchanged when no matching result is supplied.",
    "Do not claim work without real results; leave it waiting for evidence instead.",
    "Create blocker-investigation tasks only when the operator explicitly asks for them."
  ],
  "dove.lessons": [
    "Add only distilled guidance: problem, decision, pitfall, validation, and next-time behavior.",
    "Do not import raw transcripts or noisy runtime traces as lessons.",
    "When task binding is unclear, ask the operator to choose the task before writing."
  ],
  "dove.version": [
    "Treat this as a direction reset, not as a general undo command.",
    "Require a short reason before clearing active work.",
    "Preserve the project goal and reusable lessons, then point the operator to the next mission."
  ],
  "dove.source": [
    "Only add external material when it has a real title, locator, citation, URL, or operator-provided links or material.",
    "Use public no-key network search or visible retrieval for candidate discovery when current outside information is needed, then register useful retrieved material as candidates with real identity and locator details.",
    "Registration creates candidate material only. Public verification may reject a candidate but cannot issue positive verification; do not present a candidate as trusted evidence.",
    "If search or fetch finds no real material, say no source candidate was added and explain the next retrieval step.",
    "When the target task is unclear, ask the operator to choose from visible context instead of inspecting project state.",
    "When source work cannot finish here, say the source material is ready and has not yet been added to the task; the natural Chinese phrasing is `\u8FD9\u6761\u6765\u6E90\u8FD8\u6CA1\u52A0\u5165\u4EFB\u52A1`.",
    "Batch multiple sources when the operator provides them together.",
    "Keep source intake separate from synthesis; use note or document evidence for summaries and conclusions."
  ],
  "dove.note": [
    "Write notes only when there is real synthesis content: summary, quote, claim, or open question.",
    "External material should be added through source first when it is used as evidence.",
    "Connect the note to the selected task and relevant materials without showing raw internal identifiers."
  ],
  "dove.figure": [
    "Treat the user request as one figure intent and answer whether this figure is ready, waiting for SVG output, missing materials, missing drawing configuration, or needs current-figure fixes.",
    "Use the hand-drawn SVG plan as the normal default path and tell the operator when SVG output is needed.",
    "Use OpenAI image generation only when explicitly selected or configured; the OpenAI key must come from the OPENAI_API_KEY environment variable, never inline text.",
    "Resolve the target task before updating figure state, then gather linked sections, claims, experiments, sources, notes, review concerns, and material hints automatically.",
    "Describe imported output with source support, caption, and clean preflight checks as ready for independent review, not validated; validated additionally requires current authorized independent Reviewer proof for the exact final SVG path and hash.",
    "Captions must explain the figure purpose and linked evidence; default replies should not make the operator reason about paths or workspace-wide extra details."
  ],
  "dove.experience": [
    "Require a real experiment goal, title, idea, result, or outcome before writing.",
    "Make success criteria, result evidence, audit state, and claim impact understandable to the operator.",
    "Do not promote unsupported results into claims."
  ],
  "dove.draft": [
    "Draft or revise only when body content or a clear section-status change is provided.",
    "Use explicit placeholders for missing evidence or citations instead of fabricating support.",
    "Use linked sources, notes, experience, figures, and review findings when they are available."
  ],
  "dove.review": [
    "Inspect real project materials and report concrete review findings, action items, missing evidence, or a structural preflight result; a coherent result additionally requires current authorized independent Reviewer proof bound to every reviewed artifact hash.",
    "Do not substitute a status panel, task list, verdict string, or clean local scan for authoritative review work.",
    "Use separate isolated or audio review only when the operator explicitly asks for that mode, and keep private writer/reviewer transcripts out of ordinary review replies.",
    "Report review outcomes in plain language."
  ],
  "dove.review-loop": [
    "Run exactly one visible local Reviewer preflight pass over the selected packet materials; do not describe the local scan itself as independent or authoritative.",
    "Do not revise draft, experiment, experience, or other Builder-owned material in this call.",
    "When changes are required, return concrete required actions and an explicit Builder handoff.",
    "Invoke review again only after a separate explicit Builder revision call."
  ],
  "dove.rebuttal": [
    "Organize reviewer issues before drafting responses.",
    "Keep rebuttal work on the author side.",
    "Link each response to evidence, draft sections, experiments, or explicit unresolved gaps."
  ]
};
var COMMAND_SURFACES = COMMAND_SURFACES_BASE.map((surface) => {
  const withUx = {
    ...surface,
    adapterConstraints: COMMAND_ADAPTER_CONSTRAINTS[surface.id] ?? [],
    constraints: [...AGENT_WORKFLOW_CONSTRAINTS, ...surface.constraints ?? []],
    ux: COMMAND_UX_DETAILS[surface.id]
  };
  const hasTaskScopedWrite = (surface.requiredTools ?? []).some((toolId) => TASK_SCOPED_WRITE_TOOL_IDS.has(toolId));
  if (!hasTaskScopedWrite) {
    return withUx;
  }
  return {
    ...withUx,
    constraints: [...withUx.constraints, ...TASK_SCOPED_WRITE_CONSTRAINTS]
  };
});
var COMMAND_SURFACE_BY_ID = Object.fromEntries(COMMAND_SURFACES.map((surface) => [surface.id, surface]));
var DIRECT_PROCESS_ADAPTER_COMMAND_IDS = COMMAND_SURFACES.filter((surface) => surface.category === "mutation" && surface.id !== "dove.status" && surface.id !== "dove.lessons").map((surface) => surface.id);
function commandIdToSlug(commandId) {
  return commandId.replace(/^dove\./, "");
}
function hostCommandSlug(commandId) {
  return commandIdToSlug(commandId).replace(/\./g, "-");
}
function adapterPathForCommand(hostId, command) {
  const commandId = typeof command === "string" ? command : command.id;
  const hostSlug = hostCommandSlug(commandId);
  switch (hostId) {
    case "opencode":
      return `.opencode/commands/${commandId}.md`;
    case "cursor":
      return `.cursor/commands/dove-${hostSlug}.md`;
    case "codex":
      return `.codex/skills/dove-${hostSlug}/SKILL.md`;
    case "agents":
      return `.agents/skills/dove-${hostSlug}/SKILL.md`;
    case "claude":
      return `commands/dove/${hostSlug}.md`;
    default:
      throw new Error(`Unknown host adapter: ${hostId}`);
  }
}
function commandAdapterPathsForHost(hostId) {
  return COMMAND_SURFACES.map((command) => adapterPathForCommand(hostId, command));
}
var HOST_ADAPTERS = Object.fromEntries(PROJECT_HOST_IDS.map((hostId) => {
  const commandPaths = commandAdapterPathsForHost(hostId);
  const extraPaths = hostId === "opencode" ? [...OPENCODE_ROLE_SKILL_PATHS, ".opencode.json"] : hostId === "agents" ? ["AGENTS.md"] : [];
  return [hostId, {
    label: HOST_DEFINITIONS[hostId].label,
    paths: [...commandPaths, ...extraPaths],
    requiredPaths: [...commandPaths, ...extraPaths],
    jsonChecks: HOST_DEFINITIONS[hostId].jsonChecks
  }];
}));

// src/core/dove-domain.mjs
var DOVE_WORKFLOW_KERNEL_VERSION = "dove-mission-kernel-v1";
var DOVE_MISSION_LIFECYCLE_STAGES = ["goal", "design", "checklist", "execution", "audit", "return"];
var DOVE_DOMAIN_IDS = ["paper", "engineering", "experiment", "review", "general"];
var DOVE_DOMAIN_GUIDANCE = [
  {
    id: "paper",
    label: "Paper",
    summary: "Paper writing, research, claims, citations, rebuttal, figures, and versioned manuscript work.",
    stageRoutes: {
      goal: "project:dove.source",
      design: "project:dove.mission",
      checklist: "project:dove.status",
      execution: "project:dove.draft",
      audit: "project:dove.review",
      return: "project:dove.status"
    },
    returnEvidence: ["claim/evidence coverage", "checklist status", "review verdict", "version comparison"]
  },
  {
    id: "engineering",
    label: "Engineering",
    summary: "Normal engineering requirements, implementation work, tests, regressions, and code review framed as the same Dove mission lifecycle.",
    stageRoutes: {
      goal: "project:dove.mission",
      design: "project:dove.mission",
      checklist: "project:dove.status",
      execution: "project:dove.mission or project:dove.auto",
      audit: "project:dove.review",
      return: "project:dove.status"
    },
    returnEvidence: ["changed files", "tests or validation output", "review notes", "acceptance checklist"]
  },
  {
    id: "experiment",
    label: "Experiment",
    summary: "Experiment plans, runs, result interpretation, audit findings, and result-to-claim traceability.",
    stageRoutes: {
      goal: "project:dove.experience",
      design: "project:dove.experience",
      checklist: "project:dove.status",
      execution: "project:dove.experience",
      audit: "project:dove.experience",
      return: "project:dove.experience"
    },
    returnEvidence: ["experiment audit", "result log", "claim bridge", "review verdict"]
  },
  {
    id: "review",
    label: "Review",
    summary: "Independent critique, reviewer concerns, isolated review handoffs, and acceptance pressure.",
    stageRoutes: {
      goal: "project:dove.mission",
      design: "project:dove.review",
      checklist: "project:dove.status",
      execution: "project:dove.review",
      audit: "project:dove.review",
      return: "project:dove.status"
    },
    returnEvidence: ["review report", "concern state", "revision plan", "acceptance verdict"]
  },
  {
    id: "general",
    label: "General",
    summary: "General bounded research or workflow work that still uses one mission, one board, and one return protocol.",
    stageRoutes: {
      goal: "project:dove.mission",
      design: "project:dove.mission",
      checklist: "project:dove.status",
      execution: "project:dove.mission",
      audit: "project:dove.review",
      return: "project:dove.status"
    },
    returnEvidence: ["task packet", "handoff", "audit summary", "acceptance checklist"]
  }
];
var DOVE_PRIMARY_ROLES = [
  {
    id: "planner",
    label: "Planner",
    summary: "Sets destination, scope, constraints, priorities, and acceptance criteria."
  },
  {
    id: "builder",
    label: "Builder",
    summary: "Performs writing, coding, experiments, data work, implementation, and revision."
  },
  {
    id: "reviewer",
    label: "Reviewer",
    summary: "Independently audits returned work, concerns, evidence, tests, and acceptance."
  }
];
var DOVE_PRIMARY_ROLE_IDS = DOVE_PRIMARY_ROLES.map((role) => role.id);

// src/core/schema.mjs
var SCHEMA_VERSION = 6;
var PACKAGE_VERSION = "0.2.0";
var DOVE_TASK_STAGES = ["plan", "execute", "audit"];
var DOVE_TASK_DOMAINS = ["paper", "experiment", "engineering"];
var DOVE_TASK_STATUSES = ["pending", "ready", "in-progress", "blocked", "completed", "killed", "archived"];
var DOVE_ARCHIVED_TASK_STATUSES = ["archived", "archived-with-lineage"];
var DOVE_BOUNDARY_TYPES = [
  "needs-confirmation",
  "needs-task-selection",
  "awaiting-host-pass",
  "awaiting-host-pass-result",
  "awaiting-host-results",
  "host-tool-blocked",
  "blocked-boundary",
  "needs-review",
  "awaiting-provider-output",
  "awaiting-review-output",
  "missing-required-materials",
  "missing-secret-env",
  "missing-executable-contract",
  "plan-output-not-executable",
  "verification-failed",
  "debug-retry-required",
  "fix-required",
  "provider-failed",
  "workflow-error-boundary"
];
var DOVE_BOUNDARY_STATUSES = ["open", "resolved"];
var DOVE_HANDOFF_STATUSES = ["none", "pending", "accepted", "completed", "blocked"];
var DOVE_TASK_CREATOR_KINDS = ["user", "system"];
var DOVE_AUDIO_CONTEXT_POLICY = "final-plan-results-and-explicit-artifacts-only";
var DOVE_RESPONSE_LANGUAGES = ["zh", "en"];
var DEFAULT_DOVE_RESPONSE_LANGUAGE = "zh";
var DOVE_DOCUMENT_KINDS = ["draft", "note", "review", "figure", "experiment", "source", "claim-support", "operator-note", "implementation-summary", "decision-record", "other"];
var DOVE_DOCUMENT_STATUSES = ["planned", "created", "active", "superseded", "archived", "published"];
var DOVE_DOCUMENT_EVIDENCE_SCOPES = ["internal", "external", "mixed"];
var DOVE_RESPONSE_LANGUAGE_ALIASES = {
  zh: "zh",
  cn: "zh",
  chinese: "zh",
  "zh-cn": "zh",
  "zh-hans": "zh",
  \u4E2D\u6587: "zh",
  \u6C49\u8BED: "zh",
  \u82F1\u6587: "en",
  \u82F1\u8BED: "en",
  en: "en",
  english: "en",
  "en-us": "en",
  "en-gb": "en"
};
function normalizeDoveResponseLanguage(value, fallback = DEFAULT_DOVE_RESPONSE_LANGUAGE, options = {}) {
  const normalizedFallback = DOVE_RESPONSE_LANGUAGES.includes(fallback) ? fallback : DEFAULT_DOVE_RESPONSE_LANGUAGE;
  if (typeof value !== "string" || value.trim().length === 0) {
    return normalizedFallback;
  }
  const normalized = DOVE_RESPONSE_LANGUAGE_ALIASES[value.trim().toLowerCase()];
  if (normalized) {
    return normalized;
  }
  if (options.strict === true) {
    throw new Error(`Unsupported Dove response language: ${value}. Supported values: ${DOVE_RESPONSE_LANGUAGES.join(", ")}.`);
  }
  return normalizedFallback;
}
var DEFAULT_SECTION_ORDER = [
  ["abstract", "Abstract"],
  ["introduction", "Introduction"],
  ["related-work", "Related Work"],
  ["method", "Method"],
  ["experiments", "Experiments"],
  ["limitations", "Limitations"],
  ["conclusion", "Conclusion"],
  ["rebuttal", "Rebuttal Notes"]
];
var PIPELINE_STAGE_ORDER = [
  "init",
  "sources",
  "notes",
  "research",
  "plan",
  "outline",
  "draft",
  "experiments",
  "citations",
  "review",
  "rebuttal",
  "versions",
  "checklist"
];
var PAPER_LIFECYCLE_TAXONOMY_VERSION = "paper-lifecycle-v1";
var PAPER_LIFECYCLE_FAMILIES = [
  {
    id: "objective",
    label: "Objective",
    summary: "Research goal, thesis, venue strategy, and acceptance target.",
    roleHints: ["planner"],
    artifactPathKeys: ["state", "project", "researchContract", "researchBrief", "researchAgenda"]
  },
  {
    id: "structure",
    label: "Structure",
    summary: "Paper organization, sections, drafts, figures, checklists, and versions.",
    roleHints: ["planner", "builder"],
    artifactPathKeys: ["plan", "outline", "draftsDir", "checklist", "figuresIndex", "figureMaterials", "figureGenerations", "figureCaptions", "figureQa", "versionsIndex", "versionComparisons"]
  },
  {
    id: "campaign",
    label: "Campaign",
    summary: "Multi-cycle research programs, approvals, and explicit foreground runtime state.",
    roleHints: ["planner"],
    artifactPathKeys: ["programsIndex", "campaignsIndex", "programRuns", "programApprovals", "runtimeControllerState", "runtimeResults"]
  },
  {
    id: "work-unit",
    label: "Work Unit",
    summary: "Board, handoff, task-packet, and context/action surfaces that make work resumable.",
    roleHints: ["planner", "builder", "reviewer"],
    artifactPathKeys: ["orchestrationBoard", "orchestrationHandoffs", "taskPacketsIndex", "workspaceIndex", "workspaceArtifactMap", "actionContextsDir", "packetContextsDir"]
  },
  {
    id: "concern",
    label: "Concern",
    summary: "Reviewer concerns, revision pressure, rebuttal issues, and isolated review handoffs.",
    roleHints: ["reviewer", "builder"],
    artifactPathKeys: ["reviewState", "reviewConcerns", "reviewDebateLog", "adversarialReviewState", "revisionPlan", "rebuttalIssues", "rebuttalStrategy", "isolatedReviewsDir"]
  },
  {
    id: "audit",
    label: "Audit",
    summary: "Inspection, validation, governance proof, figure QA, version comparison, and meta reports.",
    roleHints: ["reviewer", "planner"],
    artifactPathKeys: ["experimentAudits", "reviewLog", "figureQa", "versionComparisons", "metaGovernanceCoverage", "metaRecommendations", "metaOptimizerReport"]
  },
  {
    id: "knowledge",
    label: "Knowledge",
    summary: "Sources, notes, document evidence, claims, bibliography, wiki, and long-horizon memory.",
    roleHints: ["builder"],
    artifactPathKeys: ["sources", "notes", "evidence", "documentsLedger", "claims", "bibliography", "citationLog", "wiki", "wikiEntities", "wikiRelations", "metaLongHorizonMemory", "metaOperatorLessons"]
  }
];
var PAPER_LIFECYCLE_FAMILY_IDS = PAPER_LIFECYCLE_FAMILIES.map((family) => family.id);
var PAPER_LIFECYCLE_FAMILY_BY_ID = Object.fromEntries(PAPER_LIFECYCLE_FAMILIES.map((family) => [family.id, family]));
var PAPER_MAJOR_CHANGE_PROTOCOL_STAGES = ["design", "checklist", "implementation", "acceptance"];
var PAPER_MAJOR_CHANGE_SIGNALS = [
  "objective-or-thesis-change",
  "structure-or-section-change",
  "core-claim-change",
  "experiment-interpretation-change",
  "reviewer-concern-or-rebuttal-change",
  "figure-set-change",
  "version-or-finalization-change",
  "campaign-or-program-change"
];
var TASK_PACKET_TARGET_FIELDS = [
  "packetId",
  "taskPacketId",
  "missionPacketId",
  "packetIds",
  "target",
  "packetTarget",
  "taskName"
];
function governanceScopeMetadata(mutationScope, options = {}) {
  return {
    mutationScope,
    requiresPacketTarget: Boolean(options.requiresPacketTarget),
    targetFields: options.requiresPacketTarget ? TASK_PACKET_TARGET_FIELDS : [],
    artifactFields: Array.isArray(options.artifactFields) ? options.artifactFields : []
  };
}
function taskScopedMutationMetadata(artifactFields = []) {
  return governanceScopeMetadata("task-scoped-write", { requiresPacketTarget: true, artifactFields });
}
var GOVERNANCE_GUARDED_MUTATION_SCOPE_METADATA = {
  "upsert-orchestration-board": governanceScopeMetadata("workspace-global"),
  "init-dove-goal": governanceScopeMetadata("task-root"),
  "create-dove-task": governanceScopeMetadata("task-materialization"),
  "run-dove-auto": governanceScopeMetadata("task-autonomy"),
  "apply-dove-status-adjustments": governanceScopeMetadata("task-lifecycle"),
  "run-dove-operator": governanceScopeMetadata("task-autonomy"),
  "kill-dove-task": governanceScopeMetadata("task-lifecycle"),
  "reset-dove-version": governanceScopeMetadata("task-version-reset"),
  "run-experience-workflow": taskScopedMutationMetadata(["id", "experimentId", "claimId", "resultId"]),
  "prepare-audio-review": taskScopedMutationMetadata(["runId", "artifactPaths", "finalPlanPaths", "finalResultPaths"]),
  "import-audio-review": taskScopedMutationMetadata(["runId", "handoffPath", "reportPath"]),
  "run-audio-review": taskScopedMutationMetadata(["runId", "artifactPaths", "finalPlanPaths", "finalResultPaths"]),
  "run-dove-review-loop": taskScopedMutationMetadata(["runId", "scope", "artifactPaths"]),
  "append-handoff": governanceScopeMetadata("workspace-global"),
  "register-source": taskScopedMutationMetadata(["sourceId", "sourceIds", "url", "title"]),
  "verify-source": taskScopedMutationMetadata(["sourceId"]),
  "upsert-note": taskScopedMutationMetadata(["id", "sourceIds", "claimIds", "sectionId"]),
  "upsert-claims": taskScopedMutationMetadata(["id", "sourceIds", "noteIds", "experimentIds", "sectionId"]),
  "upsert-plan": taskScopedMutationMetadata(["milestoneIds", "sectionIds"]),
  "upsert-outline": taskScopedMutationMetadata(["sectionId", "sectionIds"]),
  "upsert-draft": taskScopedMutationMetadata(["sectionId"]),
  "set-section-status": taskScopedMutationMetadata(["sectionId"]),
  "upsert-figure-plan": taskScopedMutationMetadata(["id", "figureId", "claimIds", "sectionId"]),
  "run-figure-workflow": taskScopedMutationMetadata(["id", "figureId", "runId", "captionId", "claimIds", "sectionId"]),
  "prepare-figure-generation": taskScopedMutationMetadata(["id", "figureId", "runId", "claimIds", "sectionId"]),
  "import-figure-generation": taskScopedMutationMetadata(["id", "figureId", "runId", "captionId"]),
  "sync-citations": governanceScopeMetadata("derived-refresh"),
  "refresh-wiki": governanceScopeMetadata("derived-refresh"),
  "build-rebuttal": taskScopedMutationMetadata(["rebuttalIssueIds", "issueIds"]),
  "append-review-log": taskScopedMutationMetadata(["reviewId", "findingIds", "concernIds"]),
  "upsert-revision-plan": taskScopedMutationMetadata(["reviewId", "findingIds", "concernIds"]),
  "run-review-loop": taskScopedMutationMetadata(["scope", "stage"]),
  "prepare-isolated-review": taskScopedMutationMetadata(["runId", "scope", "artifactPaths"]),
  "import-isolated-review": taskScopedMutationMetadata(["runId", "handoffPath", "reportPath"]),
  "run-isolated-review": taskScopedMutationMetadata(["runId", "scope", "artifactPaths"]),
  "update-research-brief": taskScopedMutationMetadata(["sourceIds", "noteIds", "claimIds"]),
  "upsert-experiment-plan": taskScopedMutationMetadata(["id", "experimentId", "claimId"]),
  "upsert-experiment-result": taskScopedMutationMetadata(["id", "resultId", "experimentId", "claimId"]),
  "run-experiment-audit": taskScopedMutationMetadata(["resultId", "experimentId", "auditIds"]),
  "bridge-experiment-result-to-claim": taskScopedMutationMetadata(["resultId", "experimentId", "claimId", "auditIds"]),
  "normalize-rebuttal-issues": taskScopedMutationMetadata(["issueIds", "rebuttalIssueIds"]),
  "build-rebuttal-strategy": taskScopedMutationMetadata(["issueIds", "rebuttalIssueIds"]),
  "create-version-snapshot": taskScopedMutationMetadata(["id", "versionId"]),
  "compare-versions": taskScopedMutationMetadata([]),
  "materialize-guidance-packet": governanceScopeMetadata("task-materialization"),
  "launch-dove-mission": governanceScopeMetadata("task-materialization"),
  "record-dove-mission-pass": taskScopedMutationMetadata(["runId", "evidenceLinks", "artifactRefs"]),
  "record-document-evidence": taskScopedMutationMetadata(["id", "documentId", "documentPath", "artifactRefs", "evidenceLinks", "sourceRefs", "claimIds"])
};
var GOVERNANCE_GUARDED_MUTATIONS = [
  { id: "upsert-orchestration-board", action: "Updating the orchestration board", artifactPath: ".dove/orchestration/board.json", surfaceBindings: { coreFunction: "upsertOrchestrationBoard", mcpTool: "upsert_orchestration_board", commandIds: [] } },
  { id: "init-dove-goal", action: "Creating or updating the unique Dove init goal", artifactPath: ".dove/task-packets", surfaceBindings: { coreFunction: "initDoveGoal", mcpTool: "init_dove_goal", commandIds: ["dove.init"] } },
  { id: "create-dove-task", action: "Converting a user demand into a classified Dove task under the init goal", artifactPath: ".dove/task-packets", surfaceBindings: { coreFunction: "createDoveTask", mcpTool: "create_dove_task", commandIds: ["dove.mission"] } },
  { id: "record-dove-mission-pass", action: "Explicitly recording execution results for an existing Dove mission or task", artifactPath: ".dove/runtime/results.json", surfaceBindings: { coreFunction: "recordDoveMissionPass", mcpTool: "record_dove_mission_pass", commandIds: [] } },
  { id: "run-dove-auto", action: "Running demand-to-task intake and bounded autonomous task completion", artifactPath: ".dove/runtime/results.json", surfaceBindings: { coreFunction: "runDoveAuto", mcpTool: "run_dove_auto", commandIds: ["dove.auto"] } },
  { id: "apply-dove-status-adjustments", action: "Applying explicitly confirmed Dove task status adjustments", artifactPath: ".dove/task-packets", surfaceBindings: { coreFunction: "applyDoveStatusAdjustments", mcpTool: "apply_dove_status_adjustments", commandIds: ["dove.status"] } },
  { id: "run-dove-operator", action: "Running one confirmed foreground operator pass over safe internal steps, explicit host results, and blocker planning", artifactPath: ".dove/runtime/results.json", surfaceBindings: { coreFunction: "runDoveOperator", mcpTool: "run_dove_operator", commandIds: ["dove.operator"] } },
  { id: "kill-dove-task", action: "Killing a non-init Dove task", artifactPath: ".dove/task-packets", surfaceBindings: { coreFunction: "killDoveTask", mcpTool: "kill_dove_task", commandIds: [] } },
  { id: "reset-dove-version", action: "Resetting active Dove tasks for a new version direction", artifactPath: ".dove/versions/index.json", surfaceBindings: { coreFunction: "resetDoveVersion", mcpTool: "reset_dove_version", commandIds: ["dove.version"] } },
  { id: "run-experience-workflow", action: "Planning, recording, auditing, and bridging experience evidence into claims", artifactPath: ".dove/experiments/results.json", surfaceBindings: { coreFunction: "runExperienceWorkflow", mcpTool: "run_experience_workflow", commandIds: ["dove.experience"] } },
  { id: "prepare-audio-review", action: "Preparing an isolated audio review input bundle", artifactPath: ".dove/audio/reviews", surfaceBindings: { coreFunction: "prepareAudioReview", mcpTool: "prepare_audio_review", commandIds: [] } },
  { id: "import-audio-review", action: "Importing an isolated audio review handoff", artifactPath: ".dove/audio/reviews", surfaceBindings: { coreFunction: "importAudioReview", mcpTool: "import_audio_review", commandIds: [] } },
  { id: "run-audio-review", action: "Running an explicitly requested isolated audio review over declared task artifacts", artifactPath: ".dove/audio/reviews", surfaceBindings: { coreFunction: "runAudioReview", mcpTool: "run_audio_review", commandIds: [] } },
  { id: "run-dove-review-loop", action: "Running limited local review, draft, and experience iterations", artifactPath: ".dove/reviews/log.md", surfaceBindings: { coreFunction: "runDoveReviewLoop", mcpTool: "run_dove_review_loop", commandIds: ["dove.review-loop"] } },
  { id: "append-handoff", action: "Appending a durable handoff", artifactPath: ".dove/orchestration/handoffs.md", surfaceBindings: { coreFunction: "appendHandoff", mcpTool: "append_handoff", commandIds: [] } },
  { id: "register-source", action: "Registering a source candidate", artifactPath: ".dove/sources/index.json", surfaceBindings: { coreFunction: "registerSource", mcpTool: "register_source", commandIds: ["dove.source"] } },
  { id: "verify-source", action: "Verifying or rejecting a source", artifactPath: ".dove/sources/verifications.json", surfaceBindings: { coreFunction: "verifySource", mcpTool: "verify_source", commandIds: [] } },
  { id: "upsert-note", action: "Recording a structured note", artifactPath: ".dove/notes/index.json", surfaceBindings: { coreFunction: "upsertNote", mcpTool: "upsert_note", commandIds: ["dove.note"] } },
  { id: "upsert-claims", action: "Updating evidence-backed claims", artifactPath: ".dove/evidence/index.json", surfaceBindings: { coreFunction: "upsertClaims", mcpTool: "upsert_claims", commandIds: ["dove.experience"] } },
  { id: "upsert-plan", action: "Updating the Dove mission plan", artifactPath: ".dove/plans/current-plan.md", surfaceBindings: { coreFunction: "upsertPlan", mcpTool: "upsert_plan", commandIds: [] } },
  { id: "upsert-outline", action: "Updating the paper outline", artifactPath: ".dove/outline/current-outline.md", surfaceBindings: { coreFunction: "upsertOutline", mcpTool: "upsert_outline", commandIds: [] } },
  { id: "upsert-draft", action: "Updating a draft section", artifactPath: ".dove/drafts", surfaceBindings: { coreFunction: "upsertDraft", mcpTool: "upsert_draft", commandIds: ["dove.draft"] } },
  { id: "set-section-status", action: "Updating a section status", artifactPath: ".dove/state.json", surfaceBindings: { coreFunction: "setSectionStatus", mcpTool: "set_section_status", commandIds: ["dove.draft"] } },
  { id: "upsert-figure-plan", action: "Updating the figure plan", artifactPath: ".dove/figures/index.json", surfaceBindings: { coreFunction: "upsertFigurePlan", mcpTool: "upsert_figure_plan", commandIds: ["dove.figure"] } },
  { id: "run-figure-workflow", action: "Running the figure workflow from one user intent through materials, generation/import, caption, and QA", artifactPath: ".dove/figures/generations.json", surfaceBindings: { coreFunction: "runFigureWorkflow", mcpTool: "run_figure_workflow", commandIds: ["dove.figure"] } },
  { id: "prepare-figure-generation", action: "Preparing figure generation materials and input bundle", artifactPath: ".dove/figures/generations.json", surfaceBindings: { coreFunction: "prepareFigureGeneration", mcpTool: "prepare_figure_generation", commandIds: ["dove.figure"] } },
  { id: "import-figure-generation", action: "Importing generated figure output and caption", artifactPath: ".dove/figures/generations.json", surfaceBindings: { coreFunction: "importFigureGeneration", mcpTool: "import_figure_generation", commandIds: ["dove.figure"] } },
  { id: "sync-citations", action: "Updating citation artifacts", artifactPath: ".dove/bibliography/citation-log.md", surfaceBindings: { coreFunction: "syncCitations", mcpTool: "sync_citations", commandIds: [] } },
  { id: "refresh-wiki", action: "Refreshing the wiki", artifactPath: ".dove/wiki/index.md", surfaceBindings: { coreFunction: "refreshWiki", mcpTool: "refresh_wiki", commandIds: [] } },
  { id: "build-rebuttal", action: "Building the rebuttal draft", artifactPath: ".dove/drafts/rebuttal.md", surfaceBindings: { coreFunction: "buildRebuttal", mcpTool: "build_rebuttal", commandIds: ["dove.rebuttal"] } },
  { id: "append-review-log", action: "Recording a manual review log", artifactPath: ".dove/reviews/log.md", surfaceBindings: { coreFunction: "appendReviewLog", mcpTool: "append_review_log", commandIds: [] } },
  { id: "upsert-revision-plan", action: "Updating the revision plan", artifactPath: ".dove/revision-plans/current-plan.md", surfaceBindings: { coreFunction: "upsertRevisionPlan", mcpTool: "upsert_revision_plan", commandIds: ["dove.draft"] } },
  { id: "run-review-loop", action: "Running the local evidence-aware review loop", artifactPath: ".dove/reviews/log.md", surfaceBindings: { coreFunction: "runReviewLoop", mcpTool: "run_review_loop", commandIds: ["dove.review"] } },
  { id: "prepare-isolated-review", action: "Preparing an explicit isolated reviewer input bundle", artifactPath: ".dove/reviews/isolated", surfaceBindings: { coreFunction: "prepareIsolatedReview", mcpTool: "prepare_isolated_review", commandIds: [] } },
  { id: "import-isolated-review", action: "Importing an explicit isolated reviewer handoff", artifactPath: ".dove/reviews/isolated", surfaceBindings: { coreFunction: "importIsolatedReview", mcpTool: "import_isolated_review", commandIds: [] } },
  { id: "run-isolated-review", action: "Running an explicit isolated parallel-session reviewer handoff", artifactPath: ".dove/reviews/isolated", surfaceBindings: { coreFunction: "runIsolatedReview", mcpTool: null, commandIds: [], cliCommand: "isolated-review" } },
  { id: "update-research-brief", action: "Updating the research brief", artifactPath: ".dove/research/brief.md", surfaceBindings: { coreFunction: "updateResearchBrief", mcpTool: "update_research_brief", commandIds: ["dove.source"] } },
  { id: "upsert-experiment-plan", action: "Updating an experiment plan", artifactPath: ".dove/experiments/plans.json", surfaceBindings: { coreFunction: "upsertExperimentPlan", mcpTool: "upsert_experiment_plan", commandIds: ["dove.experience"] } },
  { id: "upsert-experiment-result", action: "Updating an experiment result", artifactPath: ".dove/experiments/results.json", surfaceBindings: { coreFunction: "upsertExperimentResult", mcpTool: "upsert_experiment_result", commandIds: ["dove.experience"] } },
  { id: "run-experiment-audit", action: "Running an experiment audit", artifactPath: ".dove/experiments/audits.json", surfaceBindings: { coreFunction: "runExperimentAudit", mcpTool: "run_experiment_audit", commandIds: ["dove.experience"] } },
  { id: "bridge-experiment-result-to-claim", action: "Bridging an experiment result to a claim", artifactPath: ".dove/claims/bridge-log.json", surfaceBindings: { coreFunction: "bridgeExperimentResultToClaim", mcpTool: "bridge_result_to_claim", commandIds: ["dove.experience"] } },
  { id: "normalize-rebuttal-issues", action: "Normalizing rebuttal issues", artifactPath: ".dove/rebuttal/issues.json", surfaceBindings: { coreFunction: "normalizeRebuttalIssues", mcpTool: "normalize_rebuttal_issues", commandIds: ["dove.rebuttal"] } },
  { id: "build-rebuttal-strategy", action: "Building the rebuttal strategy", artifactPath: ".dove/rebuttal/strategy.md", surfaceBindings: { coreFunction: "buildRebuttalStrategy", mcpTool: "build_rebuttal_strategy", commandIds: ["dove.rebuttal"] } },
  { id: "create-version-snapshot", action: "Creating a version snapshot", artifactPath: ".dove/versions/index.json", surfaceBindings: { coreFunction: "createVersionSnapshot", mcpTool: "create_version_snapshot", commandIds: ["dove.version"] } },
  { id: "compare-versions", action: "Comparing versions", artifactPath: ".dove/versions/comparisons.json", surfaceBindings: { coreFunction: "compareVersions", mcpTool: "compare_versions", commandIds: ["dove.version"] } },
  { id: "materialize-guidance-packet", action: "Materializing accepted packet-only guidance into a durable task packet and follow-through record without creating program authority or executing work", artifactPath: ".dove/task-packets", surfaceBindings: { coreFunction: "materializeGuidancePacket", mcpTool: "materialize_guidance_packet", commandIds: [] } },
  { id: "launch-dove-mission", action: "Launching a governed Dove mission by materializing packet-only accepted guidance into the authoritative .dove task-packet store without creating program authority or executing work", artifactPath: ".dove/task-packets", surfaceBindings: { coreFunction: "launchDoveMission", mcpTool: "launch_dove_mission", commandIds: [] } },
  { id: "record-document-evidence", action: "Recording an explicit document/evidence ledger entry without capturing raw transcripts or overwriting existing documents", artifactPath: ".dove/documents/ledger.json", surfaceBindings: { coreFunction: "recordDocumentEvidence", mcpTool: "record_document_evidence", commandIds: [] } }
].map((entry) => ({
  ...entry,
  ...GOVERNANCE_GUARDED_MUTATION_SCOPE_METADATA[entry.id] ?? governanceScopeMetadata("workspace-global")
}));
var GOVERNANCE_EXEMPT_MUTATION_SCOPE_METADATA = {
  "ensure-workspace": governanceScopeMetadata("bootstrap"),
  "record-operator-lesson": governanceScopeMetadata("governance-bookkeeping"),
  "record-operator-follow-through": governanceScopeMetadata("governance-bookkeeping"),
  "plan-campaign": governanceScopeMetadata("governance-bookkeeping"),
  "revoke-program-approval": governanceScopeMetadata("governance-bookkeeping"),
  "init-project": governanceScopeMetadata("bootstrap"),
  "sync-checklist": governanceScopeMetadata("derived-refresh"),
  "validate-figure-pipeline": governanceScopeMetadata("inspection-only"),
  "classify-workflow-intent": governanceScopeMetadata("inspection-only"),
  "load-board": governanceScopeMetadata("read-helper"),
  "refresh-durable-surfaces": governanceScopeMetadata("derived-refresh"),
  "publish-dove-status": governanceScopeMetadata("derived-refresh"),
  "publish-dove-global-status": governanceScopeMetadata("derived-refresh"),
  "serve-dove-global-status": governanceScopeMetadata("derived-refresh"),
  "configure-claude-code-gateway-defaults": governanceScopeMetadata("host-install-bootstrap"),
  "summarize-session-journal": governanceScopeMetadata("governance-bookkeeping")
};
var GOVERNANCE_EXEMPT_MUTATIONS = [
  { id: "ensure-workspace", action: "Explicitly bootstrapping the canonical Dove workspace and starter artifacts remains exempt because it establishes storage without approving or executing task work.", artifactPath: ".dove/state.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-07-13T00:00:00.000Z", lastReviewedAt: "2026-07-13T00:00:00.000Z", reasonCode: "explicit-workspace-bootstrap", reviewCadence: "per-project", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "ensureWorkspace", mcpTool: "ensure_workspace", commandIds: [] } },
  { id: "record-operator-lesson", action: "Recording distilled operator lessons remains explicitly exempt because it is reflective bookkeeping and does not approve, materialize, or execute work.", artifactPath: ".dove/meta/operator-lessons.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-05-07T00:00:00.000Z", lastReviewedAt: "2026-05-07T00:00:00.000Z", reasonCode: "retrospective-bookkeeping", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "recordOperatorLesson", mcpTool: "record_operator_lesson", commandIds: ["dove.lessons"] } },
  { id: "record-operator-follow-through", action: "Recording a manual operator follow-through decision remains explicitly exempt; this public surface cannot write program linkage, runtime retry or timing state, or runtime-owned terminal and executing statuses.", artifactPath: ".dove/meta/operator-follow-through.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-07-11T00:00:00.000Z", reasonCode: "governance-ledger-maintenance", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "recordOperatorFollowThrough", mcpTool: "record_operator_follow_through", commandIds: [] } },
  { id: "plan-campaign", action: "Recording a multi-cycle campaign plan remains exempt because it only records planner-supervised campaign intent and does not approve or execute bounded program work.", artifactPath: ".dove/programs/campaigns.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-25T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "campaign-planning-bookkeeping", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "planCampaign", mcpTool: "plan_campaign", commandIds: [] } },
  { id: "revoke-program-approval", action: "Revoking a program approval remains exempt because it withdraws authority rather than executing new work.", artifactPath: ".dove/programs/approvals.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "approval-withdrawal", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "revokeProgramApproval", mcpTool: "revoke_program_approval", commandIds: [] } },
  { id: "init-project", action: "Project initialization bootstraps the workspace and is explicitly exempt from follow-through gating.", artifactPath: ".dove/state.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-04-17T00:00:00.000Z", reasonCode: "workspace-bootstrap", reviewCadence: "per-project", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "initProject", mcpTool: "init_project", commandIds: [] } },
  { id: "sync-checklist", action: "Checklist syncing remains exempt because it summarizes debt instead of executing it.", artifactPath: ".dove/checklists/current.md", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "summary-sync", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "syncChecklist", mcpTool: "sync_checklist", commandIds: [] } },
  { id: "validate-figure-pipeline", action: "Figure validation is an inspection path and remains exempt from follow-through execution gating.", artifactPath: ".dove/figures/qa.json", ownerRole: "researcher", approvedByRole: "researcher", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "inspection-only", reviewCadence: "per-change", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "validateFigurePipeline", mcpTool: "validate_figure_pipeline", commandIds: ["dove.figure"] } },
  { id: "classify-workflow-intent", action: "Workflow intent classification is analytical and remains exempt.", artifactPath: ".dove/meta/recommendations.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "analysis-only", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "classifyWorkflowIntent", mcpTool: null, commandIds: [] } },
  { id: "load-board", action: "Board loading is a read helper and is explicitly exempt.", artifactPath: ".dove/orchestration/board.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-04-17T00:00:00.000Z", reasonCode: "read-helper", reviewCadence: "per-release", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "loadBoard", mcpTool: null, commandIds: [] } },
  { id: "refresh-durable-surfaces", action: "Durable surface refresh is a proposal-only summarization step and remains exempt.", artifactPath: ".dove/workspace/index.json", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "summary-refresh", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "refreshDurableSurfaces", mcpTool: null, commandIds: ["dove.status"] } },
  { id: "publish-dove-status", action: "Publishing sanitized public status artifacts remains exempt because it derives a read-only external summary from durable Dove state without approving, materializing, or executing work.", artifactPath: ".dove/public", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-06-16T00:00:00.000Z", lastReviewedAt: "2026-06-16T00:00:00.000Z", reasonCode: "public-derived-status-refresh", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "publishDoveStatus", mcpTool: "publish_dove_status", commandIds: [] } },
  { id: "publish-dove-global-status", action: "Publishing the global sanitized public status index remains exempt because it only derives a static aggregate from explicit project .dove/public artifacts without approving, materializing, executing work, scanning the computer, or starting external services.", artifactPath: "xdg:dove/public", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-06-17T00:00:00.000Z", lastReviewedAt: "2026-06-17T00:00:00.000Z", reasonCode: "global-public-derived-status-refresh", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "publishDoveGlobalStatus", mcpTool: "publish_dove_global_status", commandIds: [] } },
  { id: "serve-dove-global-status", action: "Serving the global sanitized public status directory remains exempt only as an explicit foreground operator command that publishes the static aggregate once, serves that public directory over loopback, and optionally starts a visible Cloudflare tunnel without scanning the computer, daemonizing, scheduling refreshes, or exposing raw Dove state.", artifactPath: "xdg:dove/public", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-06-17T00:00:00.000Z", lastReviewedAt: "2026-06-17T00:00:00.000Z", reasonCode: "global-public-explicit-foreground-serving", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "runGlobalStatusServingForeground", mcpTool: null, commandIds: [], cliCommand: "serve-global-status" } },
  { id: "configure-claude-code-gateway-defaults", action: "Claude Code gateway default configuration remains exempt because it is an explicit host install/sync bootstrap step that writes only allowlisted non-secret compatibility switches, not Dove task state.", artifactPath: "claude:user-config", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-07-08T00:00:00.000Z", lastReviewedAt: "2026-07-08T00:00:00.000Z", reasonCode: "host-install-bootstrap", reviewCadence: "per-release", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "configureClaudeCodeGatewayDefaults", mcpTool: null, commandIds: [], cliCommand: "install/sync --host claude" } },
  { id: "summarize-session-journal", action: "Session summarization is reflective and remains exempt from execution gating.", artifactPath: ".dove/sessions/LATEST_SUMMARY.md", ownerRole: "planner", approvedByRole: "planner", approvedAt: "2026-04-15T00:00:00.000Z", lastReviewedAt: "2026-05-05T00:00:00.000Z", reasonCode: "reflective-summary", reviewCadence: "per-session", sunsetAt: "2099-12-31T00:00:00.000Z", surfaceBindings: { coreFunction: "summarizeSessionJournal", mcpTool: "summarize_session_journal", commandIds: [] } }
].map((entry) => ({
  ...entry,
  ...GOVERNANCE_EXEMPT_MUTATION_SCOPE_METADATA[entry.id] ?? governanceScopeMetadata("governance-bookkeeping")
}));
function resolveResumeCommandForPhase(phase) {
  switch (phase) {
    case "sources":
    case "research":
      return "project:dove.source";
    case "notes":
      return "project:dove.note";
    case "plan":
    case "outline":
      return "project:dove.mission";
    case "draft":
      return "project:dove.draft";
    case "experiments":
      return "project:dove.experience";
    case "review":
      return "project:dove.review";
    case "rebuttal":
      return "project:dove.rebuttal";
    case "versions":
      return "project:dove.version";
    case "checklist":
      return "project:dove.status";
    default:
      return "project:dove.status";
  }
}
var PRIMARY_ROLE_IDS = ["planner", "builder", "reviewer"];
var SUBAGENT_ROLE_IDS = [
  "researcher",
  "experiment-planner",
  "revision-lead",
  "rebuttal-lead",
  "version-analyst"
];
var ROLE_HIERARCHY = {
  planner: {
    id: "planner",
    label: "Planner",
    kind: "primary",
    manuallySwitchable: true,
    parentRole: null,
    charter: "Acts as mentor, PI, and editor: sets direction, prioritizes work, coordinates handoffs, and supervises governance.",
    subagents: ["task-planner", "orchestration-manager", "governance-checker", "priority-ranker", "version-analyst"]
  },
  builder: {
    id: "builder",
    label: "Builder",
    kind: "primary",
    manuallySwitchable: true,
    parentRole: null,
    charter: "Acts as the mission builder: writes, implements, revises, gathers evidence, plans experiments, interprets results, and prepares responses.",
    subagents: ["researcher", "experiment-planner", "result-analyst", "paper-writer", "revision-lead"]
  },
  reviewer: {
    id: "reviewer",
    label: "Reviewer",
    kind: "primary",
    manuallySwitchable: true,
    parentRole: null,
    charter: "Acts as the independent critic: attacks claims, checks evidence and methods, records concerns, and issues verdicts.",
    subagents: ["claim-critic", "evidence-auditor", "experiment-auditor", "methodology-critic", "novelty-critic"]
  },
  researcher: {
    id: "researcher",
    label: "Researcher",
    kind: "subagent",
    manuallySwitchable: false,
    parentRole: "builder",
    charter: "Builder-side specialist for sources, notes, claims, evidence maps, and durable research briefs."
  },
  "experiment-planner": {
    id: "experiment-planner",
    label: "Experiment Planner",
    kind: "subagent",
    manuallySwitchable: false,
    parentRole: "builder",
    charter: "Builder-side specialist for claim-driven experiment plans, baselines, metrics, ablations, and result-to-claim closure."
  },
  "revision-lead": {
    id: "revision-lead",
    label: "Revision Lead",
    kind: "subagent",
    manuallySwitchable: false,
    parentRole: "builder",
    aliasOf: "rebuttal-lead",
    charter: "Builder-side specialist for turning reviewer concerns into revision plans, response matrices, and rebuttal drafts."
  },
  "rebuttal-lead": {
    id: "rebuttal-lead",
    label: "Rebuttal Lead",
    kind: "legacy-subagent",
    manuallySwitchable: false,
    parentRole: "builder",
    canonicalRole: "revision-lead",
    charter: "Legacy name for builder-side revision and rebuttal response work."
  },
  "version-analyst": {
    id: "version-analyst",
    label: "Version Analyst",
    kind: "subagent",
    manuallySwitchable: false,
    parentRole: "planner",
    charter: "Planner-side audit specialist for version diffs, regression checks, and concern-resolution evidence."
  }
};
var ROLE_IDS = [
  ...PRIMARY_ROLE_IDS,
  ...SUBAGENT_ROLE_IDS
];
function roleCanActAs(actorRole, expectedRole) {
  if (actorRole === expectedRole) {
    return true;
  }
  const actor = ROLE_HIERARCHY[actorRole];
  const expected = ROLE_HIERARCHY[expectedRole];
  return expected?.parentRole === actorRole || actor?.parentRole === expectedRole;
}
var ARTIFACT_PATHS = {
  doveRoot: ".dove",
  state: ".dove/state.json",
  readme: ".dove/README.md",
  publicDir: ".dove/public",
  publicStatusJson: ".dove/public/status.json",
  publicStatusMarkdown: ".dove/public/status.md",
  publicStatusHtml: ".dove/public/index.html",
  documentsDir: ".dove/documents",
  documentsLedger: ".dove/documents/ledger.json",
  project: ".dove/project.md",
  researchContract: ".dove/contracts/research-contract.md",
  orchestrationBoard: ".dove/orchestration/board.json",
  orchestrationHandoffs: ".dove/orchestration/handoffs.md",
  taskPacketsDir: ".dove/task-packets",
  taskPacketsPacketsDir: ".dove/task-packets/packets",
  taskPacketsIndex: ".dove/task-packets/index.json",
  roleContextsDir: ".dove/context/roles",
  phaseContextsDir: ".dove/context/phases",
  packetContextsDir: ".dove/context/packets",
  artifactContextsDir: ".dove/context/artifacts",
  actionContextsDir: ".dove/context/actions",
  sessionJournal: ".dove/sessions/journal.json",
  sessionSummary: ".dove/sessions/LATEST_SUMMARY.md",
  workspaceDir: ".dove/workspace",
  workspaceIndex: ".dove/workspace/index.json",
  workspaceArtifactMap: ".dove/workspace/artifact-map.json",
  doveRootManifest: ".dove/manifest.json",
  programsDir: ".dove/programs",
  programsIndex: ".dove/programs/index.json",
  programRuns: ".dove/programs/runs.json",
  programApprovals: ".dove/programs/approvals.json",
  campaignsIndex: ".dove/programs/campaigns.json",
  workflowPackDir: ".dove/workflow-pack",
  workflowBoundaries: ".dove/workflow-pack/boundaries.json",
  researchBrief: ".dove/research/brief.md",
  researchAgenda: ".dove/research/agenda.json",
  plan: ".dove/plans/current-plan.md",
  outline: ".dove/outline/current-outline.md",
  findings: ".dove/findings.md",
  experimentLog: ".dove/experiments/EXPERIMENT_LOG.md",
  experimentPlans: ".dove/experiments/plans.json",
  experimentResults: ".dove/experiments/results.json",
  experimentAudits: ".dove/experiments/audits.json",
  sources: ".dove/sources/index.json",
  sourceVerifications: ".dove/sources/verifications.json",
  notes: ".dove/notes/index.json",
  evidence: ".dove/evidence/index.json",
  claims: ".dove/claims/CLAIMS_FROM_RESULTS.md",
  claimBridgeLog: ".dove/claims/bridge-log.json",
  draftsDir: ".dove/drafts",
  reviewLog: ".dove/reviews/log.md",
  reviewReport: ".dove/reviews/LATEST_REVIEW_REPORT.md",
  reviewState: ".dove/reviews/REVIEW_STATE.json",
  reviewConcerns: ".dove/reviews/concerns.json",
  reviewDebateLog: ".dove/reviews/debate-log.md",
  adversarialReviewState: ".dove/reviews/adversarial-state.json",
  isolatedReviewsDir: ".dove/reviews/isolated",
  audioReviewsDir: ".dove/audio/reviews",
  revisionPlan: ".dove/revision-plans/current-plan.md",
  wiki: ".dove/wiki/index.md",
  queryPack: ".dove/wiki/query_pack.md",
  navigationReport: ".dove/wiki/navigation.md",
  wikiEntities: ".dove/wiki/entities.json",
  wikiRelations: ".dove/wiki/relations.json",
  checklist: ".dove/checklists/current.md",
  bibliography: ".dove/bibliography/references.bib",
  citationLog: ".dove/bibliography/citation-log.md",
  figuresReadme: ".dove/figures/README.md",
  figuresIndex: ".dove/figures/index.json",
  figureBriefs: ".dove/figures/briefs.json",
  figureSegments: ".dove/figures/segments.json",
  figureTemplates: ".dove/figures/templates.json",
  figureEditableIndex: ".dove/figures/editable-index.json",
  figureFinalIndex: ".dove/figures/final-index.json",
  figureMaterials: ".dove/figures/materials.json",
  figureGenerations: ".dove/figures/generations.json",
  figureCaptions: ".dove/figures/captions.json",
  figureQa: ".dove/figures/qa.json",
  rebuttalIssues: ".dove/rebuttal/issues.json",
  rebuttalStrategy: ".dove/rebuttal/strategy.md",
  rebuttalResponseDraft: ".dove/rebuttal/response-draft.md",
  versionsIndex: ".dove/versions/index.json",
  versionComparisons: ".dove/versions/comparisons.json",
  versionComparisonReport: ".dove/versions/LATEST_COMPARISON.md",
  versionSnapshotsDir: ".dove/versions/snapshots",
  runtimeDir: ".dove/runtime",
  runtimeControllerState: ".dove/runtime/controller-state.json",
  runtimeContinuation: ".dove/runtime/continuation.json",
  runtimeLeases: ".dove/runtime/leases.json",
  runtimeEvents: ".dove/runtime/events.json",
  runtimeResults: ".dove/runtime/results.json",
  mutationsDir: ".dove/mutations",
  mutationsIndex: ".dove/mutations/index.json",
  metaDir: ".dove/meta",
  metaEvents: ".dove/meta/events.json",
  metaExecutionBridgeCandidates: ".dove/meta/execution-bridge-candidates.json",
  metaGovernanceCoverage: ".dove/meta/governance-coverage.json",
  metaGovernanceCoverageReport: ".dove/meta/governance-coverage-report.json",
  metaGovernanceCoverageReportMarkdown: ".dove/meta/LATEST_GOVERNANCE_COVERAGE_REPORT.md",
  metaLongHorizonMemory: ".dove/meta/long-horizon-memory.json",
  metaOperatorLessons: ".dove/meta/operator-lessons.json",
  metaOperatorFollowThrough: ".dove/meta/operator-follow-through.json",
  metaOperatorFollowThroughTransitions: ".dove/meta/operator-follow-through-transitions.json",
  metaOperatorPlaybooks: ".dove/meta/operator-playbooks.json",
  metaRemediationPacks: ".dove/meta/remediation-packs.json",
  metaRecommendations: ".dove/meta/recommendations.json",
  metaOptimizerState: ".dove/meta/optimizer-state.json",
  metaOptimizerReport: ".dove/meta/LATEST_OPTIMIZER_REPORT.md"
};
var AUTONOMY_ALLOWED_STEP_TYPES = [
  "refresh-research-brief",
  "refresh-wiki",
  "upsert-note",
  "run-experiment-audit",
  "bridge-result-to-claim",
  "run-review-loop"
];
function normalizeAutonomyAllowedStepType(value, fallback = "refresh-research-brief") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  if (AUTONOMY_ALLOWED_STEP_TYPES.includes(normalized)) {
    return normalized;
  }
  return fallback;
}
function digestText(value) {
  return crypto.createHash("sha256").update(String(value)).digest("hex");
}
function createManagedArtifactMeta(kind, relativePath) {
  const seed = JSON.stringify({ kind, relativePath, schema: SCHEMA_VERSION, pack: PACKAGE_VERSION });
  return {
    revisionId: `schema-v${SCHEMA_VERSION}:${kind}`,
    templateHash: digestText(seed),
    generatedByVersion: PACKAGE_VERSION,
    managedKind: kind,
    path: relativePath
  };
}
function createContinuationState(overrides = {}) {
  const source = overrides && typeof overrides === "object" && !Array.isArray(overrides) ? overrides : {};
  const responseLanguage = source.responseLanguage ?? source.language ?? DEFAULT_DOVE_RESPONSE_LANGUAGE;
  const { responseLanguage: _responseLanguage, language: _language, ...stateOverrides } = source;
  return {
    status: "ready-to-resume",
    lastCheckpoint: defaultDisplayText(responseLanguage, "lastCheckpoint"),
    checkpointHistory: [],
    updatedAt: null,
    ...stateOverrides,
    checkpointHistory: Array.isArray(stateOverrides.checkpointHistory) ? stateOverrides.checkpointHistory : []
  };
}
function defaultSections() {
  return Object.fromEntries(
    DEFAULT_SECTION_ORDER.map(([id, title]) => [
      id,
      {
        id,
        title,
        status: "planned",
        summary: "",
        draftPath: `.dove/drafts/${id}.md`,
        claimIds: []
      }
    ])
  );
}
function defaultRoleRoster() {
  return PRIMARY_ROLE_IDS.map((roleId) => {
    const role = ROLE_HIERARCHY[roleId];
    return {
      id: role.id,
      label: role.label,
      kind: role.kind,
      manuallySwitchable: role.manuallySwitchable,
      parentRole: role.parentRole,
      charter: role.charter,
      subagents: role.subagents ?? []
    };
  });
}
function createDefaultBoard(stateOverrides = {}) {
  const responseLanguage = stateOverrides.settings?.responseLanguage ?? stateOverrides.responseLanguage ?? DEFAULT_DOVE_RESPONSE_LANGUAGE;
  const objective = stateOverrides.dove?.objective ?? defaultDisplayText(responseLanguage, "objective");
  const phase = stateOverrides.pipeline?.currentStage ?? "init";
  return {
    version: 2,
    paperObjective: objective,
    currentPhase: phase,
    intentType: "plan",
    assignedRole: "planner",
    currentFocus: defaultDisplayText(responseLanguage, "currentFocus"),
    nextAction: defaultDisplayText(responseLanguage, "nextAction"),
    continuationState: createContinuationState({ responseLanguage }),
    reviewRequiredBeforeFinalize: false,
    tasks: [],
    blockers: [],
    evidenceLinks: [],
    experimentIds: [],
    rebuttalIssueIds: [],
    unresolvedBlockersByRole: {},
    versionLineage: {
      currentVersionId: null,
      parentVersionId: null,
      snapshotIds: []
    },
    activeComparisonTargets: [],
    roleRoster: defaultRoleRoster(),
    updatedAt: (/* @__PURE__ */ new Date(0)).toISOString()
  };
}
function normalizeTaskTargetResolutionSettings(raw = {}, base = { autoSelect: true, autoSelectMinScore: 0.55, recordResolution: true }) {
  const source = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  const rawScore = source.autoSelectMinScore;
  const autoSelectMinScore = typeof rawScore === "number" && Number.isFinite(rawScore) ? Math.min(1, Math.max(0, rawScore)) : base.autoSelectMinScore;
  return {
    autoSelect: typeof source.autoSelect === "boolean" ? source.autoSelect : base.autoSelect,
    autoSelectMinScore,
    recordResolution: typeof source.recordResolution === "boolean" ? source.recordResolution : base.recordResolution
  };
}
function normalizeTaskModelSettings(raw = {}, base = { uniqueInitLevel: 0, userDefaultLevel: 3, autoClassifyMissionTasks: true }) {
  const source = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  return {
    uniqueInitLevel: Number.isFinite(source.uniqueInitLevel) ? source.uniqueInitLevel : base.uniqueInitLevel,
    userDefaultLevel: Number.isFinite(source.userDefaultLevel) ? source.userDefaultLevel : base.userDefaultLevel,
    autoClassifyMissionTasks: typeof source.autoClassifyMissionTasks === "boolean" ? source.autoClassifyMissionTasks : base.autoClassifyMissionTasks
  };
}
function normalizeReviewLoopSettings(raw = {}, base = { maxIterations: 3 }) {
  const source = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  const maxIterations = Number.isFinite(source.maxIterations) ? Math.max(1, Math.floor(source.maxIterations)) : base.maxIterations;
  return { maxIterations };
}
function normalizeAutoSettings(raw = {}, base = { maxIterations: 3 }) {
  const source = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  const maxIterations = Number.isFinite(source.maxIterations) ? Math.max(1, Math.floor(source.maxIterations)) : base.maxIterations;
  return { maxIterations };
}
function normalizeAudioIsolationSettings(raw = {}, base = { defaultContextPolicy: DOVE_AUDIO_CONTEXT_POLICY }) {
  const source = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  return {
    defaultContextPolicy: normalizeString(source.defaultContextPolicy, base.defaultContextPolicy)
  };
}
function normalizeSettings(raw = {}, base = null) {
  const defaults = base ?? {
    strictMode: false,
    responseLanguage: DEFAULT_DOVE_RESPONSE_LANGUAGE,
    taskTargetResolution: normalizeTaskTargetResolutionSettings(),
    taskModel: normalizeTaskModelSettings(),
    auto: normalizeAutoSettings(),
    reviewLoop: normalizeReviewLoopSettings(),
    audioIsolation: normalizeAudioIsolationSettings()
  };
  const source = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
  return {
    ...defaults,
    ...source,
    strictMode: Boolean(source.strictMode ?? defaults.strictMode),
    responseLanguage: normalizeDoveResponseLanguage(source.responseLanguage ?? source.language, defaults.responseLanguage),
    taskTargetResolution: normalizeTaskTargetResolutionSettings(source.taskTargetResolution, defaults.taskTargetResolution),
    taskModel: normalizeTaskModelSettings(source.taskModel, defaults.taskModel),
    auto: normalizeAutoSettings(source.auto, defaults.auto),
    reviewLoop: normalizeReviewLoopSettings(source.reviewLoop, defaults.reviewLoop),
    audioIsolation: normalizeAudioIsolationSettings(source.audioIsolation, defaults.audioIsolation)
  };
}
function createDefaultSettings(overrides = {}) {
  return normalizeSettings(overrides);
}
function defaultDisplayText(language, key) {
  const normalized = normalizeDoveResponseLanguage(language);
  const values = {
    title: {
      zh: "\u672A\u547D\u540D\u4EFB\u52A1\u5DE5\u4F5C\u533A",
      en: "Untitled Mission Workspace"
    },
    objective: {
      zh: "\u8BB0\u5F55 Dove \u4EFB\u52A1\u76EE\u6807\u4E0E\u8D21\u732E\u3002",
      en: "Capture the Dove mission goal and contribution."
    },
    thesis: {
      zh: "\u7528\u4E00\u53E5\u8BDD\u63CF\u8FF0\u8BBA\u6587\u9886\u57DF\u4E3B\u5F20\u6216\u4EFB\u52A1\u7ED3\u679C\u3002",
      en: "Describe the paper-domain claim or mission outcome in one sentence."
    },
    currentFocus: {
      zh: "\u5BF9\u9F50\u770B\u677F\u5E76\u9009\u62E9\u4E0B\u4E00\u4E2A\u6301\u4E45\u6B65\u9AA4\u3002",
      en: "Align the board and choose the next durable step."
    },
    nextAction: {
      zh: "\u8FD0\u884C project:dove.mission\uFF0C\u5728 init \u76EE\u6807\u4E0B\u521B\u5EFA\u4E0B\u4E00\u4E2A\u4EFB\u52A1\u3002",
      en: "Run project:dove.mission to create the next task under the init goal."
    },
    lastCheckpoint: {
      zh: "\u5DE5\u4F5C\u533A\u5DF2\u521D\u59CB\u5316\u3002",
      en: "Workspace bootstrapped."
    }
  };
  return values[key]?.[normalized] ?? values[key]?.en ?? "";
}
function createDefaultState(overrides = {}) {
  const baseSettings = createDefaultSettings(overrides.settings);
  const responseLanguage = baseSettings.responseLanguage;
  const base = {
    version: SCHEMA_VERSION,
    dove: {
      title: defaultDisplayText(responseLanguage, "title"),
      venue: "Unspecified",
      objective: defaultDisplayText(responseLanguage, "objective"),
      deadline: "",
      thesis: defaultDisplayText(responseLanguage, "thesis"),
      audience: "TBD"
    },
    pipeline: {
      currentStage: "init",
      lastCompletedStage: null,
      resumeCommand: "project:dove.status",
      updatedAt: (/* @__PURE__ */ new Date(0)).toISOString()
    },
    orchestration: {
      boardPath: ARTIFACT_PATHS.orchestrationBoard,
      handoffPath: ARTIFACT_PATHS.orchestrationHandoffs,
      phase: "init",
      intentType: "plan",
      assignedRole: "planner",
      currentFocus: defaultDisplayText(responseLanguage, "currentFocus"),
      nextAction: defaultDisplayText(responseLanguage, "nextAction"),
      continuationState: createContinuationState({ responseLanguage }),
      reviewRequiredBeforeFinalize: false,
      activeTaskIds: [],
      blockerIds: [],
      evidenceLinks: [],
      experimentIds: [],
      rebuttalIssueIds: [],
      currentVersionId: null,
      activeComparisonTargets: []
    },
    sections: defaultSections(),
    artifacts: {
      ...ARTIFACT_PATHS
    },
    reviews: {
      lastVerdict: "not-reviewed",
      lastReviewedAt: null,
      openItems: [],
      unresolvedConcernIds: []
    },
    settings: baseSettings
  };
  const incomingDove = overrides.dove ?? overrides.paper ?? {};
  return {
    ...base,
    ...overrides,
    dove: {
      ...base.dove,
      ...incomingDove
    },
    pipeline: {
      ...base.pipeline,
      ...overrides.pipeline ?? {}
    },
    orchestration: normalizeOrchestration(overrides.orchestration, base.orchestration),
    sections: normalizeSections(overrides.sections),
    artifacts: {
      ...ARTIFACT_PATHS,
      ...overrides.artifacts ?? {}
    },
    reviews: {
      ...base.reviews,
      ...overrides.reviews ?? {},
      openItems: Array.isArray(overrides.reviews?.openItems) ? overrides.reviews.openItems : [],
      unresolvedConcernIds: Array.isArray(overrides.reviews?.unresolvedConcernIds) ? overrides.reviews.unresolvedConcernIds : []
    },
    settings: normalizeSettings(overrides.settings, base.settings),
    paper: void 0
  };
}
function normalizeSections(rawSections) {
  const merged = defaultSections();
  if (!rawSections || typeof rawSections !== "object") {
    return merged;
  }
  for (const [id, section] of Object.entries(rawSections)) {
    merged[id] = {
      ...merged[id] ?? {
        id,
        title: id,
        status: "planned",
        summary: "",
        draftPath: `.dove/drafts/${id}.md`,
        claimIds: []
      },
      ...section,
      id,
      draftPath: section?.draftPath ?? `.dove/drafts/${id}.md`,
      claimIds: Array.isArray(section?.claimIds) ? section.claimIds : []
    };
  }
  return merged;
}
function normalizeArray(value) {
  return Array.isArray(value) ? value : [];
}
function normalizeString(value, fallback) {
  return typeof value === "string" && value.trim() ? value : fallback;
}
function normalizeStringArray(value, fallback = []) {
  const source = Array.isArray(value) ? value : fallback;
  return source.filter((item) => typeof item === "string" && item.trim());
}
function normalizeObjectArray(value) {
  return Array.isArray(value) ? value.filter((item) => item && typeof item === "object" && !Array.isArray(item)) : [];
}
function normalizeObject(value, fallback = {}) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : fallback;
}
function normalizeBoolean(value, fallback) {
  return typeof value === "boolean" ? value : fallback;
}
function normalizeNumber(value, fallback) {
  return Number.isFinite(value) ? value : fallback;
}
function normalizeStringArrayRecord(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return {};
  }
  return Object.fromEntries(
    Object.entries(value).map(([key, items]) => [key, normalizeStringArray(items)])
  );
}
function createLifecycleFamilySummary(overrides = {}) {
  return PAPER_LIFECYCLE_FAMILIES.map((family) => {
    const incoming = overrides[family.id] && typeof overrides[family.id] === "object" && !Array.isArray(overrides[family.id]) ? overrides[family.id] : {};
    return {
      id: family.id,
      label: family.label,
      summary: family.summary,
      roleHints: normalizeStringArray(incoming.roleHints, family.roleHints),
      artifactPathKeys: normalizeStringArray(incoming.artifactPathKeys, family.artifactPathKeys),
      artifactCount: normalizeNumber(incoming.artifactCount, 0),
      activePacketCount: normalizeNumber(incoming.activePacketCount, 0),
      packetCount: normalizeNumber(incoming.packetCount, 0)
    };
  });
}
function normalizeLifecycleFamilyId(value, fallback = null) {
  const normalized = typeof value === "string" ? value.trim() : "";
  return PAPER_LIFECYCLE_FAMILY_IDS.includes(normalized) ? normalized : fallback;
}
function normalizeDoveDomainId(value, fallback = "paper") {
  const normalized = typeof value === "string" ? value.trim() : "";
  return DOVE_DOMAIN_IDS.includes(normalized) ? normalized : fallback;
}
function normalizeDoveMissionLifecycleStage(value, fallback = "goal") {
  const normalized = typeof value === "string" ? value.trim() : "";
  return DOVE_MISSION_LIFECYCLE_STAGES.includes(normalized) ? normalized : fallback;
}
function normalizeDoveBoundaryType(value, fallback = "workflow-error-boundary") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  return DOVE_BOUNDARY_TYPES.includes(normalized) ? normalized : fallback;
}
function normalizeDoveBoundaryStatus(value, fallback = "open") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  return DOVE_BOUNDARY_STATUSES.includes(normalized) ? normalized : fallback;
}
function normalizeDoveHandoffStatus(value, fallback = "none") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  return DOVE_HANDOFF_STATUSES.includes(normalized) ? normalized : fallback;
}
function normalizeDovePrimaryRoleId(value, fallback = "builder") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  return DOVE_PRIMARY_ROLE_IDS.includes(normalized) ? normalized : fallback;
}
function normalizeDoveBoundary(value, fallback = null) {
  const source = normalizeObject(value, fallback);
  if (!source || Object.keys(source).length === 0) {
    return fallback;
  }
  const type = normalizeDoveBoundaryType(source.type ?? source.boundaryType, fallback?.type ?? "workflow-error-boundary");
  const status = normalizeDoveBoundaryStatus(source.status ?? source.boundaryStatus, fallback?.status ?? "open");
  return {
    ...source,
    id: normalizeString(source.id ?? source.boundaryId, fallback?.id ?? `boundary-${type}`),
    type,
    status,
    packetId: normalizeString(source.packetId ?? source.taskPacketId, fallback?.packetId ?? null),
    runId: normalizeString(source.runId, fallback?.runId ?? null),
    sourceSurface: normalizeString(source.sourceSurface ?? source.surface, fallback?.sourceSurface ?? null),
    command: normalizeString(source.command, fallback?.command ?? null),
    reason: normalizeString(source.reason ?? source.stopReason, fallback?.reason ?? ""),
    summary: normalizeString(source.summary, fallback?.summary ?? ""),
    requiredInputs: normalizeStringArray(source.requiredInputs, fallback?.requiredInputs ?? []),
    requiredActions: normalizeStringArray(source.requiredActions, fallback?.requiredActions ?? []),
    ownerRole: normalizeDovePrimaryRoleId(source.ownerRole, fallback?.ownerRole ?? "builder"),
    nextRole: normalizeDovePrimaryRoleId(source.nextRole, fallback?.nextRole ?? source.ownerRole ?? "builder"),
    createdAt: source.createdAt ?? fallback?.createdAt ?? null,
    resolvedAt: source.resolvedAt ?? fallback?.resolvedAt ?? null,
    resolution: normalizeString(source.resolution, fallback?.resolution ?? null)
  };
}
function normalizeDoveHandoff(value, fallback = null) {
  const source = normalizeObject(value, fallback);
  if (!source || Object.keys(source).length === 0) {
    return fallback;
  }
  const status = normalizeDoveHandoffStatus(source.status, fallback?.status ?? "pending");
  const toRole = normalizeDovePrimaryRoleId(source.toRole ?? source.nextRole, fallback?.toRole ?? "builder");
  return {
    ...source,
    id: normalizeString(source.id ?? source.handoffId, fallback?.id ?? `handoff-${toRole}`),
    status,
    fromRole: normalizeDovePrimaryRoleId(source.fromRole ?? source.ownerRole, fallback?.fromRole ?? "planner"),
    toRole,
    reason: normalizeString(source.reason, fallback?.reason ?? ""),
    summary: normalizeString(source.summary, fallback?.summary ?? ""),
    boundaryId: normalizeString(source.boundaryId, fallback?.boundaryId ?? null),
    sourceRunId: normalizeString(source.sourceRunId ?? source.runId, fallback?.sourceRunId ?? null),
    requestedAt: source.requestedAt ?? fallback?.requestedAt ?? null,
    acceptedAt: source.acceptedAt ?? fallback?.acceptedAt ?? null,
    completedAt: source.completedAt ?? fallback?.completedAt ?? null
  };
}
var DOVE_EXECUTION_CHAIN_TYPES = [
  "paper-source-note-draft-review",
  "experiment-plan-result-audit",
  "engineering-host-pass-verify",
  "plan-to-executable-missions"
];
function normalizeExecutionStrings(value, fallback = []) {
  const values = Array.isArray(value) ? value : typeof value === "string" && value.trim() ? [value] : fallback;
  return Array.from(new Set(values.map((item) => String(item).trim()).filter(Boolean)));
}
function normalizeExecutionFile(value) {
  const source = normalizeObject(value, null);
  if (!source) {
    return null;
  }
  const file = {
    path: normalizeString(source.path, null),
    action: normalizeString(source.action, null),
    target: normalizeString(source.target, null),
    change: normalizeString(source.change, null)
  };
  return Object.values(file).some(Boolean) ? file : null;
}
function normalizeExecutionRoute(value) {
  const source = normalizeObject(value, null);
  if (!source) {
    return null;
  }
  const route = {
    on: normalizeString(source.on, null),
    boundaryType: normalizeDoveBoundaryType(source.boundaryType, "blocked-boundary"),
    nextAction: normalizeString(source.nextAction, null),
    requiredActions: normalizeExecutionStrings(source.requiredActions)
  };
  return route.on || route.nextAction || route.requiredActions.length > 0 ? route : null;
}
function normalizeDoveExecutionContract(value, fallback = null) {
  const source = normalizeObject(value, null);
  const base = normalizeObject(fallback, {});
  if (!source && Object.keys(base).length === 0) {
    return null;
  }
  const materialSource = normalizeObject(source?.materials, base.materials ?? {});
  const convergenceSource = normalizeObject(source?.convergence, base.convergence ?? {});
  const chainType = normalizeString(source?.chainType, base.chainType ?? "engineering-host-pass-verify");
  return {
    chainType: DOVE_EXECUTION_CHAIN_TYPES.includes(chainType) ? chainType : "engineering-host-pass-verify",
    roleSequence: normalizeExecutionStrings(source?.roleSequence, base.roleSequence ?? ["planner", "builder", "reviewer"]).map((role) => normalizeDovePrimaryRoleId(role, "builder")),
    readFirst: normalizeExecutionStrings(source?.readFirst, base.readFirst ?? []),
    action: normalizeString(source?.action, base.action ?? ""),
    implementation: normalizeExecutionStrings(source?.implementation, base.implementation ?? []),
    files: normalizeObjectArray(source?.files ?? base.files).map(normalizeExecutionFile).filter(Boolean),
    materials: {
      requiredInputs: normalizeExecutionStrings(materialSource.requiredInputs),
      requiredArtifacts: normalizeExecutionStrings(materialSource.requiredArtifacts),
      sourceRefs: normalizeExecutionStrings(materialSource.sourceRefs),
      artifactRefs: normalizeExecutionStrings(materialSource.artifactRefs)
    },
    convergence: {
      criteria: normalizeExecutionStrings(convergenceSource.criteria),
      verificationCommands: normalizeExecutionStrings(convergenceSource.verificationCommands),
      evidenceRequired: normalizeExecutionStrings(convergenceSource.evidenceRequired),
      definitionOfDone: normalizeString(convergenceSource.definitionOfDone, "")
    },
    failureRoutes: normalizeObjectArray(source?.failureRoutes ?? base.failureRoutes).map(normalizeExecutionRoute).filter(Boolean)
  };
}
function doveExecutionContractReadiness(value) {
  const contract = normalizeDoveExecutionContract(value, null);
  if (!contract) {
    return {
      ready: false,
      status: "missing-executable-contract",
      missing: ["executionContract"],
      criteria: [],
      evidenceRequired: [],
      requiredMaterials: []
    };
  }
  const missing = [
    contract.action ? null : "action",
    contract.implementation.length > 0 ? null : "implementation",
    contract.convergence.criteria.length > 0 ? null : "convergence.criteria",
    contract.failureRoutes.length > 0 ? null : "failureRoutes"
  ].filter(Boolean);
  return {
    ready: missing.length === 0,
    status: missing.length === 0 ? "ready" : "missing-executable-contract",
    missing,
    criteria: contract.convergence.criteria,
    evidenceRequired: contract.convergence.evidenceRequired,
    requiredMaterials: [
      ...contract.materials.requiredInputs,
      ...contract.materials.requiredArtifacts,
      ...contract.materials.sourceRefs,
      ...contract.materials.artifactRefs
    ]
  };
}
function normalizeDoveVerifiedCriteria(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return value.map((item) => {
    if (typeof item === "string" && item.trim()) {
      return { criterion: item.trim(), status: "verified", evidencePaths: [] };
    }
    const source = normalizeObject(item, null);
    if (!source) {
      return null;
    }
    const criterion = normalizeString(source.criterion, null);
    if (!criterion) {
      return null;
    }
    return {
      criterion,
      status: normalizeString(source.status, "verified"),
      evidencePaths: normalizeExecutionStrings(source.evidencePaths)
    };
  }).filter(Boolean);
}
function doveExecutionCriteriaCoverage(contractValue, verifiedCriteriaValue) {
  const contract = normalizeDoveExecutionContract(contractValue, null);
  const required = contract?.convergence.criteria ?? [];
  const verified = normalizeDoveVerifiedCriteria(verifiedCriteriaValue).filter((item) => ["verified", "passed", "met"].includes(String(item.status).trim().toLowerCase()));
  const verifiedKeys = new Set(verified.map((item) => item.criterion.trim().toLowerCase()));
  const missing = required.filter((criterion) => !verifiedKeys.has(criterion.trim().toLowerCase()));
  return {
    complete: required.length === 0 || missing.length === 0,
    required,
    verified,
    missing
  };
}
function normalizeDoveExecutionReceipt(value, fallback = null) {
  const source = normalizeObject(value, null);
  const base = normalizeObject(fallback, {});
  if (!source && Object.keys(base).length === 0) {
    return null;
  }
  const lifecycleSource = normalizeObject(source?.lifecycleTransition, base.lifecycleTransition ?? {});
  const coverageSource = normalizeObject(source?.criteriaCoverage, base.criteriaCoverage ?? null);
  const receipt = {
    receiptId: normalizeString(source?.receiptId, base.receiptId ?? normalizeString(source?.id, base.id ?? null)),
    runId: normalizeString(source?.runId, base.runId ?? null),
    packetId: normalizeString(source?.packetId, base.packetId ?? null),
    command: normalizeString(source?.command, base.command ?? null),
    surface: normalizeString(source?.surface, base.surface ?? null),
    actionType: normalizeString(source?.actionType, base.actionType ?? null),
    startedAt: normalizeString(source?.startedAt, base.startedAt ?? null),
    completedAt: normalizeString(source?.completedAt, base.completedAt ?? null),
    status: normalizeString(source?.status, base.status ?? null),
    outcome: normalizeString(source?.outcome, base.outcome ?? null),
    resultSummary: normalizeString(source?.resultSummary, base.resultSummary ?? normalizeString(source?.summary, base.summary ?? null)),
    publicSafeSummary: normalizeString(source?.publicSafeSummary, base.publicSafeSummary ?? null),
    nextAction: normalizeString(source?.nextAction, base.nextAction ?? null),
    lifecycleTransition: {
      previousStatus: normalizeString(lifecycleSource.previousStatus, null),
      nextStatus: normalizeString(lifecycleSource.nextStatus, null)
    },
    artifactRefs: normalizeExecutionStrings(source?.artifactRefs, base.artifactRefs ?? []),
    artifactPaths: normalizeExecutionStrings(source?.artifactPaths, base.artifactPaths ?? []),
    evidenceLinks: normalizeExecutionStrings(source?.evidenceLinks, base.evidenceLinks ?? []),
    evidencePaths: normalizeExecutionStrings(source?.evidencePaths, base.evidencePaths ?? []),
    validationEvidencePaths: normalizeExecutionStrings(source?.validationEvidencePaths, base.validationEvidencePaths ?? []),
    verificationEvidencePaths: normalizeExecutionStrings(source?.verificationEvidencePaths, base.verificationEvidencePaths ?? []),
    verifiedCriteria: normalizeDoveVerifiedCriteria(source?.verifiedCriteria ?? base.verifiedCriteria),
    criteriaCoverage: coverageSource ? {
      complete: normalizeBoolean(coverageSource.complete, false),
      required: normalizeExecutionStrings(coverageSource.required),
      missing: normalizeExecutionStrings(coverageSource.missing),
      verified: normalizeDoveVerifiedCriteria(coverageSource.verified)
    } : null,
    validationGateResults: normalizeObjectArray(source?.validationGateResults ?? base.validationGateResults),
    boundary: normalizeObject(source?.boundary, base.boundary ?? null)
  };
  return Object.fromEntries(Object.entries(receipt).filter(([, item]) => item !== null));
}
function createDoveRoleSummary(overrides = {}) {
  const roleOverrides = Object.fromEntries(normalizeObjectArray(overrides).map((role) => [role.id, role]));
  return DOVE_PRIMARY_ROLES.map((role) => ({
    ...role,
    ...normalizeObject(roleOverrides[role.id]),
    id: role.id,
    label: normalizeString(roleOverrides[role.id]?.label, role.label),
    summary: normalizeString(roleOverrides[role.id]?.summary, role.summary)
  }));
}
function createDoveDomainGuidance(overrides = {}) {
  const domainOverrides = Object.fromEntries(normalizeObjectArray(overrides).map((domain) => [domain.id, domain]));
  return DOVE_DOMAIN_GUIDANCE.map((domain) => {
    const incoming = normalizeObject(domainOverrides[domain.id]);
    const incomingStageRoutes = normalizeObject(incoming.stageRoutes);
    return {
      ...domain,
      ...incoming,
      id: domain.id,
      label: normalizeString(incoming.label, domain.label),
      summary: normalizeString(incoming.summary, domain.summary),
      stageRoutes: Object.fromEntries(DOVE_MISSION_LIFECYCLE_STAGES.map((stage) => [stage, normalizeString(incomingStageRoutes[stage], domain.stageRoutes[stage])])),
      returnEvidence: normalizeStringArray(incoming.returnEvidence, domain.returnEvidence)
    };
  });
}
function createDoveAuthorityManifest() {
  return {
    version: 1,
    status: "authoritative",
    strategy: "dove-direct",
    activeDurableRoot: ARTIFACT_PATHS.doveRoot,
    authoritativeRoot: ARTIFACT_PATHS.doveRoot,
    manifestPath: ARTIFACT_PATHS.doveRootManifest,
    currentWriteAuthority: ARTIFACT_PATHS.doveRoot,
    legacyRoot: ".paper",
    dualRootInvariant: {
      allowed: false,
      doveRootAuthoritative: true,
      legacyRootAuthoritative: false,
      reason: ".dove is the only authoritative durable root for Dove."
    },
    phases: [
      {
        id: "dove-direct-authority",
        status: "active",
        summary: "Dove owns the package identity, command language, MCP identity, and .dove durable root."
      }
    ],
    nextDecision: "Use Dove surfaces directly; old .paper state is not read as runtime authority."
  };
}
function normalizeDoveAuthorityManifest(raw = {}, fallback = null) {
  const base = fallback ?? createDoveAuthorityManifest();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const dualRootInvariant = normalizeObject(raw.dualRootInvariant);
  return {
    ...base,
    ...raw,
    version: 1,
    status: normalizeString(raw.status, base.status),
    strategy: normalizeString(raw.strategy, base.strategy),
    activeDurableRoot: normalizeString(raw.activeDurableRoot, base.activeDurableRoot),
    authoritativeRoot: normalizeString(raw.authoritativeRoot, base.authoritativeRoot),
    manifestPath: normalizeString(raw.manifestPath, base.manifestPath),
    currentWriteAuthority: normalizeString(raw.currentWriteAuthority, base.currentWriteAuthority),
    legacyRoot: normalizeString(raw.legacyRoot, base.legacyRoot),
    dualRootInvariant: {
      ...base.dualRootInvariant,
      ...dualRootInvariant,
      allowed: normalizeBoolean(dualRootInvariant.allowed, base.dualRootInvariant.allowed),
      doveRootAuthoritative: normalizeBoolean(dualRootInvariant.doveRootAuthoritative, base.dualRootInvariant.doveRootAuthoritative),
      legacyRootAuthoritative: normalizeBoolean(dualRootInvariant.legacyRootAuthoritative, base.dualRootInvariant.legacyRootAuthoritative),
      reason: normalizeString(dualRootInvariant.reason, base.dualRootInvariant.reason)
    },
    phases: normalizeObjectArray(raw.phases).length > 0 ? normalizeObjectArray(raw.phases).map((phase) => ({
      id: normalizeString(phase.id, "unknown"),
      status: normalizeString(phase.status, "unknown"),
      summary: normalizeString(phase.summary, "No summary provided.")
    })) : base.phases,
    nextDecision: normalizeString(raw.nextDecision, base.nextDecision)
  };
}
function createDoveWorkspaceKernel() {
  return {
    kernelVersion: DOVE_WORKFLOW_KERNEL_VERSION,
    unified: true,
    explicitOnly: true,
    noHiddenRuntime: true,
    identity: {
      productName: "Dove",
      packageName: "dove",
      publicCli: "dove",
      commandPrefix: "project:dove.",
      activeDurableRoot: ARTIFACT_PATHS.doveRoot,
      durableRootStatus: "authoritative",
      overview: "Dove is the product, CLI, MCP identity, command language, and durable workspace authority."
    },
    authorityManifest: createDoveAuthorityManifest(),
    currentDomain: "paper",
    domainIds: DOVE_DOMAIN_IDS,
    primaryRoleIds: DOVE_PRIMARY_ROLE_IDS,
    primaryRoles: createDoveRoleSummary(),
    domainGuidance: createDoveDomainGuidance(),
    missionLifecycle: {
      stages: DOVE_MISSION_LIFECYCLE_STAGES,
      currentStage: "goal",
      paperProtocolStages: PAPER_MAJOR_CHANGE_PROTOCOL_STAGES,
      overview: "Dove missions close through goal, design, checklist, execution, audit, and return."
    },
    missionModel: {
      workUnitName: "mission",
      sourcePacketName: "mission-packet",
      domainField: "doveDomain",
      goalField: "goal",
      returnArtifactName: "handoff",
      acceptanceField: "acceptance",
      durableRoot: ARTIFACT_PATHS.doveRoot
    },
    missionCount: 0,
    activeMissionCount: 0,
    reviewNeededMissionCount: 0,
    domainCounts: Object.fromEntries(DOVE_DOMAIN_IDS.map((domainId) => [domainId, 0])),
    currentMissionFamily: null,
    overview: "Dove is active: paper-domain workflows and engineering missions share one authoritative mission kernel."
  };
}
function normalizeWorkspaceDove(raw = {}, fallback = null) {
  const base = fallback ?? createDoveWorkspaceKernel();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const identity = normalizeObject(raw.identity);
  const authorityManifest = normalizeObject(raw.authorityManifest);
  const missionLifecycle = normalizeObject(raw.missionLifecycle);
  const missionModel = normalizeObject(raw.missionModel);
  const domainCounts = normalizeObject(raw.domainCounts);
  const { taskCenteredCommands: _legacyTaskCenteredCommands, ...rawDove } = raw;
  return {
    ...base,
    ...rawDove,
    kernelVersion: normalizeString(raw.kernelVersion, base.kernelVersion),
    unified: normalizeBoolean(raw.unified, base.unified),
    explicitOnly: normalizeBoolean(raw.explicitOnly, base.explicitOnly),
    noHiddenRuntime: normalizeBoolean(raw.noHiddenRuntime, base.noHiddenRuntime),
    identity: {
      ...base.identity,
      ...identity,
      productName: normalizeString(identity.productName, base.identity.productName),
      packageName: normalizeString(identity.packageName, base.identity.packageName),
      publicCli: normalizeString(identity.publicCli, base.identity.publicCli),
      commandPrefix: normalizeString(identity.commandPrefix, base.identity.commandPrefix),
      activeDurableRoot: normalizeString(identity.activeDurableRoot, base.identity.activeDurableRoot),
      durableRootStatus: normalizeString(identity.durableRootStatus, base.identity.durableRootStatus),
      overview: normalizeString(identity.overview, base.identity.overview)
    },
    authorityManifest: normalizeDoveAuthorityManifest(authorityManifest, base.authorityManifest),
    currentDomain: normalizeDoveDomainId(raw.currentDomain, base.currentDomain),
    domainIds: DOVE_DOMAIN_IDS,
    primaryRoleIds: DOVE_PRIMARY_ROLE_IDS,
    primaryRoles: createDoveRoleSummary(raw.primaryRoles),
    domainGuidance: createDoveDomainGuidance(raw.domainGuidance),
    missionLifecycle: {
      ...base.missionLifecycle,
      ...missionLifecycle,
      stages: normalizeStringArray(missionLifecycle.stages, DOVE_MISSION_LIFECYCLE_STAGES).filter((stage) => DOVE_MISSION_LIFECYCLE_STAGES.includes(stage)),
      currentStage: normalizeDoveMissionLifecycleStage(missionLifecycle.currentStage, base.missionLifecycle.currentStage),
      paperProtocolStages: normalizeStringArray(missionLifecycle.paperProtocolStages, PAPER_MAJOR_CHANGE_PROTOCOL_STAGES).filter((stage) => PAPER_MAJOR_CHANGE_PROTOCOL_STAGES.includes(stage)),
      overview: normalizeString(missionLifecycle.overview, base.missionLifecycle.overview)
    },
    missionModel: {
      ...base.missionModel,
      ...missionModel,
      workUnitName: normalizeString(missionModel.workUnitName, base.missionModel.workUnitName),
      sourcePacketName: normalizeString(missionModel.sourcePacketName, base.missionModel.sourcePacketName),
      domainField: normalizeString(missionModel.domainField, base.missionModel.domainField),
      goalField: normalizeString(missionModel.goalField, base.missionModel.goalField),
      returnArtifactName: normalizeString(missionModel.returnArtifactName, base.missionModel.returnArtifactName),
      acceptanceField: normalizeString(missionModel.acceptanceField, base.missionModel.acceptanceField),
      durableRoot: normalizeString(missionModel.durableRoot, base.missionModel.durableRoot)
    },
    missionCount: normalizeNumber(raw.missionCount, base.missionCount),
    activeMissionCount: normalizeNumber(raw.activeMissionCount, base.activeMissionCount),
    reviewNeededMissionCount: normalizeNumber(raw.reviewNeededMissionCount, base.reviewNeededMissionCount),
    domainCounts: Object.fromEntries(DOVE_DOMAIN_IDS.map((domainId) => [domainId, normalizeNumber(domainCounts[domainId], base.domainCounts[domainId] ?? 0)])),
    currentMissionFamily: normalizeLifecycleFamilyId(raw.currentMissionFamily, base.currentMissionFamily),
    overview: normalizeString(raw.overview, base.overview)
  };
}
function normalizeWorkspaceLifecycle(raw = {}, fallback = null) {
  const base = fallback ?? {
    taxonomyVersion: PAPER_LIFECYCLE_TAXONOMY_VERSION,
    familyIds: PAPER_LIFECYCLE_FAMILY_IDS,
    families: createLifecycleFamilySummary(),
    artifactCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0])),
    activePacketCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0])),
    packetCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0])),
    boardFamily: null,
    boardPhaseFamily: null,
    topFamilies: [],
    protocol: {
      stages: PAPER_MAJOR_CHANGE_PROTOCOL_STAGES,
      majorChangeSignals: PAPER_MAJOR_CHANGE_SIGNALS,
      overview: "Major paper changes should close through design, checklist, implementation, and acceptance."
    }
  };
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const familyOverrides = Object.fromEntries(normalizeObjectArray(raw.families).map((family) => [family.id, family]));
  const artifactCounts = normalizeObject(raw.artifactCounts);
  const activePacketCounts = normalizeObject(raw.activePacketCounts);
  const packetCounts = normalizeObject(raw.packetCounts);
  const protocol = normalizeObject(raw.protocol);
  return {
    ...base,
    ...raw,
    taxonomyVersion: normalizeString(raw.taxonomyVersion, PAPER_LIFECYCLE_TAXONOMY_VERSION),
    familyIds: PAPER_LIFECYCLE_FAMILY_IDS,
    families: createLifecycleFamilySummary(familyOverrides),
    artifactCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, normalizeNumber(artifactCounts[familyId], 0)])),
    activePacketCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, normalizeNumber(activePacketCounts[familyId], 0)])),
    packetCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, normalizeNumber(packetCounts[familyId], 0)])),
    boardFamily: normalizeLifecycleFamilyId(raw.boardFamily, base.boardFamily),
    boardPhaseFamily: normalizeLifecycleFamilyId(raw.boardPhaseFamily, base.boardPhaseFamily),
    topFamilies: normalizeStringArray(raw.topFamilies).filter((familyId) => PAPER_LIFECYCLE_FAMILY_IDS.includes(familyId)),
    protocol: {
      ...base.protocol,
      ...protocol,
      stages: normalizeStringArray(protocol.stages, PAPER_MAJOR_CHANGE_PROTOCOL_STAGES).filter((stage) => PAPER_MAJOR_CHANGE_PROTOCOL_STAGES.includes(stage)),
      majorChangeSignals: normalizeStringArray(protocol.majorChangeSignals, PAPER_MAJOR_CHANGE_SIGNALS).filter((signal) => PAPER_MAJOR_CHANGE_SIGNALS.includes(signal)),
      overview: normalizeString(protocol.overview, base.protocol.overview)
    }
  };
}
function normalizeContinuation(value) {
  if (!value || typeof value !== "object") {
    return createContinuationState();
  }
  return createContinuationState(value);
}
function normalizeOrchestration(raw = {}, defaults = {}) {
  return {
    ...defaults,
    ...raw,
    continuationState: normalizeContinuation(raw.continuationState ?? defaults.continuationState),
    activeTaskIds: normalizeArray(raw.activeTaskIds),
    blockerIds: normalizeArray(raw.blockerIds),
    evidenceLinks: normalizeArray(raw.evidenceLinks),
    experimentIds: normalizeArray(raw.experimentIds),
    rebuttalIssueIds: normalizeArray(raw.rebuttalIssueIds),
    activeComparisonTargets: normalizeArray(raw.activeComparisonTargets)
  };
}
function normalizeState(raw = {}) {
  if (!raw || typeof raw !== "object") {
    return createDefaultState();
  }
  if (raw.version === 1) {
    return createDefaultState({
      dove: {
        title: raw.projectTitle ?? defaultDisplayText(DEFAULT_DOVE_RESPONSE_LANGUAGE, "title"),
        venue: raw.venue ?? "Unspecified",
        objective: raw.objective ?? defaultDisplayText(DEFAULT_DOVE_RESPONSE_LANGUAGE, "objective"),
        deadline: raw.deadline ?? "",
        thesis: defaultDisplayText(DEFAULT_DOVE_RESPONSE_LANGUAGE, "thesis"),
        audience: "TBD"
      },
      pipeline: {
        currentStage: raw.currentPhase ?? "init",
        lastCompletedStage: null,
        resumeCommand: "project:dove.status",
        updatedAt: raw.updatedAt ?? (/* @__PURE__ */ new Date(0)).toISOString()
      },
      orchestration: {
        phase: raw.currentPhase ?? "init"
      }
    });
  }
  if (raw.version === 2) {
    return createDefaultState({
      ...raw,
      version: SCHEMA_VERSION,
      orchestration: {
        phase: raw.pipeline?.currentStage ?? "init",
        assignedRole: raw.pipeline?.currentStage === "review" ? "reviewer" : "planner",
        activeTaskIds: [],
        blockerIds: [],
        evidenceLinks: [],
        experimentIds: [],
        rebuttalIssueIds: [],
        currentVersionId: null,
        activeComparisonTargets: []
      },
      pipeline: {
        ...raw.pipeline ?? {},
        resumeCommand: raw.pipeline?.resumeCommand ?? "project:dove.status"
      }
    });
  }
  const defaults = createDefaultState({ settings: raw.settings });
  return {
    ...defaults,
    ...raw,
    version: SCHEMA_VERSION,
    dove: {
      ...defaults.dove,
      ...raw.dove ?? raw.paper ?? {}
    },
    paper: void 0,
    pipeline: {
      ...defaults.pipeline,
      ...raw.pipeline ?? {}
    },
    orchestration: normalizeOrchestration(raw.orchestration, defaults.orchestration),
    sections: normalizeSections(raw.sections),
    artifacts: {
      ...ARTIFACT_PATHS,
      ...raw.artifacts ?? {}
    },
    reviews: {
      ...defaults.reviews,
      ...raw.reviews ?? {},
      openItems: Array.isArray(raw.reviews?.openItems) ? raw.reviews.openItems : [],
      unresolvedConcernIds: Array.isArray(raw.reviews?.unresolvedConcernIds) ? raw.reviews.unresolvedConcernIds : []
    },
    settings: normalizeSettings(raw.settings, defaults.settings)
  };
}
function normalizeWorkspaceIndex(raw = {}) {
  const base = createWorkspaceIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const workQueues = normalizeObject(raw.workQueues);
  const resumeGuidance = normalizeObject(raw.resumeGuidance);
  const contextSurfaces = normalizeObject(raw.contextSurfaces);
  const behaviorDiscipline = normalizeObject(raw.behaviorDiscipline);
  const dependencyHealth = normalizeObject(raw.dependencyHealth);
  const repairFrontier = normalizeObject(raw.repairFrontier);
  const metaOptimize = normalizeObject(raw.metaOptimize);
  const runtime = normalizeObject(raw.runtime);
  const programs = normalizeObject(raw.programs);
  const campaigns = normalizeObject(raw.campaigns);
  const autonomyLoops = normalizeObject(raw.autonomyLoops);
  const lifecycle = normalizeObject(raw.lifecycle);
  const dove = normalizeObject(raw.dove);
  const latestVersions = normalizeObject(raw.latestVersions);
  return {
    ...base,
    ...raw,
    version: base.version,
    managed: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.workspaceIndex),
    boardPhase: normalizeString(raw.boardPhase, base.boardPhase),
    boardAssignedRole: normalizeString(raw.boardAssignedRole, base.boardAssignedRole),
    boardIntentType: normalizeString(raw.boardIntentType, base.boardIntentType),
    currentFocus: normalizeString(raw.currentFocus, base.currentFocus),
    nextAction: normalizeString(raw.nextAction, base.nextAction),
    continuationState: normalizeContinuation(raw.continuationState),
    activePackets: normalizeObjectArray(raw.activePackets),
    workQueues: {
      ready: normalizeObjectArray(workQueues.ready),
      waiting: normalizeObjectArray(workQueues.waiting),
      reviewNeeded: normalizeObjectArray(workQueues.reviewNeeded),
      handoff: normalizeObjectArray(workQueues.handoff),
      stale: normalizeObjectArray(workQueues.stale),
      archived: normalizeObjectArray(workQueues.archived)
    },
    ownershipSummary: normalizeObjectArray(raw.ownershipSummary),
    packetLifecycleCounts: normalizeObject(raw.packetLifecycleCounts),
    handoffObligations: normalizeObjectArray(raw.handoffObligations),
    resumeGuidance: {
      ...base.resumeGuidance,
      ...resumeGuidance,
      command: normalizeString(resumeGuidance.command, base.resumeGuidance.command),
      summary: normalizeString(resumeGuidance.summary, base.resumeGuidance.summary),
      prioritizedPacketIds: normalizeStringArray(resumeGuidance.prioritizedPacketIds),
      packetContextPaths: normalizeStringArray(resumeGuidance.packetContextPaths),
      handoffCandidateIds: normalizeStringArray(resumeGuidance.handoffCandidateIds)
    },
    contextSurfaces: {
      ...base.contextSurfaces,
      ...contextSurfaces,
      currentRoleContextPath: normalizeString(contextSurfaces.currentRoleContextPath, base.contextSurfaces.currentRoleContextPath),
      currentPhaseContextPath: normalizeString(contextSurfaces.currentPhaseContextPath, base.contextSurfaces.currentPhaseContextPath),
      currentActionContextPath: normalizeString(contextSurfaces.currentActionContextPath, base.contextSurfaces.currentActionContextPath),
      prioritizedArtifactContextPaths: normalizeStringArray(contextSurfaces.prioritizedArtifactContextPaths),
      prioritizedPacketActionContextPaths: normalizeStringArray(contextSurfaces.prioritizedPacketActionContextPaths)
    },
    behaviorDiscipline: {
      ...base.behaviorDiscipline,
      ...behaviorDiscipline,
      summary: normalizeString(behaviorDiscipline.summary, base.behaviorDiscipline.summary),
      explicitOnly: normalizeBoolean(behaviorDiscipline.explicitOnly, base.behaviorDiscipline.explicitOnly),
      noHiddenRuntime: normalizeBoolean(behaviorDiscipline.noHiddenRuntime, base.behaviorDiscipline.noHiddenRuntime),
      requiredReadOrder: normalizeStringArray(behaviorDiscipline.requiredReadOrder)
    },
    dependencyHealth: {
      blockedPacketIds: normalizeStringArray(dependencyHealth.blockedPacketIds),
      healthyPacketIds: normalizeStringArray(dependencyHealth.healthyPacketIds),
      waitingPacketIds: normalizeStringArray(dependencyHealth.waitingPacketIds),
      stalePacketIds: normalizeStringArray(dependencyHealth.stalePacketIds),
      missingDependencyIds: normalizeStringArray(dependencyHealth.missingDependencyIds),
      orphanPacketIds: normalizeStringArray(dependencyHealth.orphanPacketIds)
    },
    repairFrontier: {
      ...base.repairFrontier,
      ...repairFrontier,
      count: Number.isFinite(repairFrontier.count) ? repairFrontier.count : base.repairFrontier.count,
      relationIssueCount: Number.isFinite(repairFrontier.relationIssueCount) ? repairFrontier.relationIssueCount : base.repairFrontier.relationIssueCount,
      relationFamilyIssueCount: Number.isFinite(repairFrontier.relationFamilyIssueCount) ? repairFrontier.relationFamilyIssueCount : base.repairFrontier.relationFamilyIssueCount,
      managedArtifactIssueCount: Number.isFinite(repairFrontier.managedArtifactIssueCount) ? repairFrontier.managedArtifactIssueCount : base.repairFrontier.managedArtifactIssueCount,
      governanceIssueCount: Number.isFinite(repairFrontier.governanceIssueCount) ? repairFrontier.governanceIssueCount : base.repairFrontier.governanceIssueCount,
      topDegradedFamilyIds: normalizeStringArray(repairFrontier.topDegradedFamilyIds),
      topDegradedGroupIds: normalizeStringArray(repairFrontier.topDegradedGroupIds),
      taxonomyOverview: normalizeString(repairFrontier.taxonomyOverview, base.repairFrontier.taxonomyOverview),
      relationFamilySummaries: normalizeObjectArray(repairFrontier.relationFamilySummaries),
      relationGroupSummaries: normalizeObjectArray(repairFrontier.relationGroupSummaries),
      prioritizedItems: normalizeObjectArray(repairFrontier.prioritizedItems)
    },
    metaOptimize: normalizeWorkspaceMetaOptimize(metaOptimize, base.metaOptimize),
    runtime: normalizeWorkspaceRuntime(runtime, base.runtime),
    programs: normalizeWorkspacePrograms(programs, base.programs),
    campaigns: normalizeWorkspaceCampaigns(campaigns, base.campaigns),
    autonomyLoops: normalizeWorkspaceAutonomyLoops(autonomyLoops, base.autonomyLoops),
    lifecycle: normalizeWorkspaceLifecycle(lifecycle, base.lifecycle),
    dove: normalizeWorkspaceDove(dove, base.dove),
    activeRoles: normalizeStringArray(raw.activeRoles),
    unresolvedConcernIds: normalizeStringArray(raw.unresolvedConcernIds),
    mostRecentSessions: normalizeObjectArray(raw.mostRecentSessions),
    latestVersions: {
      ...base.latestVersions,
      ...latestVersions,
      currentVersionId: latestVersions.currentVersionId ?? base.latestVersions.currentVersionId,
      activeTargets: normalizeStringArray(latestVersions.activeTargets),
      latestSnapshotIds: normalizeStringArray(latestVersions.latestSnapshotIds)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeRuntimeLogEntries(raw = {}) {
  const entries = [...normalizeObjectArray(raw.items), ...normalizeObjectArray(raw.entries)];
  const keyed = /* @__PURE__ */ new Map();
  const unkeyed = [];
  for (const entry of entries) {
    const id = normalizeString(entry.id ?? entry.runId ?? entry.eventId, null);
    if (id) {
      keyed.set(id, entry);
    } else {
      unkeyed.push(entry);
    }
  }
  return [...unkeyed, ...keyed.values()];
}
function normalizeRuntimeControllerState(raw = {}) {
  const base = createRuntimeControllerState();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  const lastRun = normalizeObject(raw.lastRun);
  return {
    ...base,
    ...raw,
    version: base.version,
    explicitInvocationOnly: normalizeBoolean(raw.explicitInvocationOnly, base.explicitInvocationOnly),
    noDaemon: normalizeBoolean(raw.noDaemon, base.noDaemon),
    selectionPolicy: normalizeString(raw.selectionPolicy, base.selectionPolicy),
    boundedStepPolicy: normalizeString(raw.boundedStepPolicy, base.boundedStepPolicy),
    lastRun: Object.keys(lastRun).length === 0 ? null : {
      runId: normalizeString(lastRun.runId, null),
      status: normalizeString(lastRun.status, null),
      outcome: normalizeString(lastRun.outcome, null),
      selectedPacketId: normalizeString(lastRun.selectedPacketId, null),
      leaseId: normalizeString(lastRun.leaseId, null),
      actorRole: normalizeString(lastRun.actorRole, null),
      envelopeWorkerRole: normalizeString(lastRun.envelopeWorkerRole, null),
      programId: normalizeString(lastRun.programId, null),
      programRunId: normalizeString(lastRun.programRunId, null),
      approvalId: normalizeString(lastRun.approvalId, null),
      startedAt: lastRun.startedAt ?? null,
      completedAt: lastRun.completedAt ?? null,
      summary: normalizeString(lastRun.summary, "")
    },
    summary: {
      ...base.summary,
      ...summary,
      lastRunId: normalizeString(summary.lastRunId, base.summary.lastRunId),
      lastStatus: normalizeString(summary.lastStatus, base.summary.lastStatus),
      lastOutcome: normalizeString(summary.lastOutcome, base.summary.lastOutcome),
      lastSelectedPacketId: normalizeString(summary.lastSelectedPacketId, base.summary.lastSelectedPacketId),
      lastEnvelopeWorkerRole: normalizeString(summary.lastEnvelopeWorkerRole, base.summary.lastEnvelopeWorkerRole),
      lastProgramId: normalizeString(summary.lastProgramId, base.summary.lastProgramId),
      lastProgramRunId: normalizeString(summary.lastProgramRunId, base.summary.lastProgramRunId),
      lastApprovalId: normalizeString(summary.lastApprovalId, base.summary.lastApprovalId),
      lastProgramOutcome: normalizeString(summary.lastProgramOutcome, base.summary.lastProgramOutcome),
      requestCount: normalizeNumber(summary.requestCount, base.summary.requestCount),
      acceptedRequestCount: normalizeNumber(summary.acceptedRequestCount, base.summary.acceptedRequestCount),
      executingRequestCount: normalizeNumber(summary.executingRequestCount, base.summary.executingRequestCount),
      staleRequestCount: normalizeNumber(summary.staleRequestCount, base.summary.staleRequestCount),
      overdueExecutionCount: normalizeNumber(summary.overdueExecutionCount, base.summary.overdueExecutionCount),
      dueReviewCount: normalizeNumber(summary.dueReviewCount, base.summary.dueReviewCount),
      checkpointCount: normalizeNumber(summary.checkpointCount, base.summary.checkpointCount),
      escalationCount: normalizeNumber(summary.escalationCount, base.summary.escalationCount),
      lastCheckpointPacketId: normalizeString(summary.lastCheckpointPacketId, base.summary.lastCheckpointPacketId),
      lastCheckpointSummary: normalizeString(summary.lastCheckpointSummary, base.summary.lastCheckpointSummary),
      lastCheckpointAt: summary.lastCheckpointAt ?? base.summary.lastCheckpointAt,
      lastEscalationPacketId: normalizeString(summary.lastEscalationPacketId, base.summary.lastEscalationPacketId),
      lastEscalationFollowThroughId: normalizeString(summary.lastEscalationFollowThroughId, base.summary.lastEscalationFollowThroughId),
      lastEscalationAt: summary.lastEscalationAt ?? base.summary.lastEscalationAt,
      overview: normalizeString(summary.overview, base.summary.overview),
      controllerStatePath: normalizeString(summary.controllerStatePath, base.summary.controllerStatePath),
      leasesPath: normalizeString(summary.leasesPath, base.summary.leasesPath),
      eventsPath: normalizeString(summary.eventsPath, base.summary.eventsPath),
      resultsPath: normalizeString(summary.resultsPath, base.summary.resultsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeRuntimeLeasesIndex(raw = {}) {
  const base = createRuntimeLeasesIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    explicitInvocationOnly: normalizeBoolean(raw.explicitInvocationOnly, base.explicitInvocationOnly),
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      activeLeaseCount: normalizeNumber(summary.activeLeaseCount, base.summary.activeLeaseCount),
      activePacketIds: normalizeStringArray(summary.activePacketIds),
      activeLeaseIds: normalizeStringArray(summary.activeLeaseIds),
      overview: normalizeString(summary.overview, base.summary.overview),
      leasesPath: normalizeString(summary.leasesPath, base.summary.leasesPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeRuntimeContinuationIndex(raw = {}) {
  const base = createRuntimeContinuationIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    explicitInvocationOnly: normalizeBoolean(raw.explicitInvocationOnly, base.explicitInvocationOnly),
    noDaemon: normalizeBoolean(raw.noDaemon, base.noDaemon),
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      continuationCount: normalizeNumber(summary.continuationCount, base.summary.continuationCount),
      currentKind: normalizeString(summary.currentKind, base.summary.currentKind),
      currentPacketId: normalizeString(summary.currentPacketId, base.summary.currentPacketId),
      currentProgramRunId: normalizeString(summary.currentProgramRunId, base.summary.currentProgramRunId),
      currentCommand: normalizeString(summary.currentCommand, base.summary.currentCommand),
      overview: normalizeString(summary.overview, base.summary.overview),
      continuationPath: normalizeString(summary.continuationPath, base.summary.continuationPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeRuntimeEventsIndex(raw = {}) {
  const base = createRuntimeEventsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    explicitInvocationOnly: normalizeBoolean(raw.explicitInvocationOnly, base.explicitInvocationOnly),
    entries: normalizeRuntimeLogEntries(raw),
    summary: {
      ...base.summary,
      ...summary,
      eventCount: normalizeNumber(summary.eventCount, base.summary.eventCount),
      lastEventType: normalizeString(summary.lastEventType, base.summary.lastEventType),
      lastRunId: normalizeString(summary.lastRunId, base.summary.lastRunId),
      overview: normalizeString(summary.overview, base.summary.overview),
      eventsPath: normalizeString(summary.eventsPath, base.summary.eventsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeRuntimeResultsIndex(raw = {}) {
  const base = createRuntimeResultsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    explicitInvocationOnly: normalizeBoolean(raw.explicitInvocationOnly, base.explicitInvocationOnly),
    entries: normalizeRuntimeLogEntries(raw),
    summary: {
      ...base.summary,
      ...summary,
      runCount: normalizeNumber(summary.runCount, base.summary.runCount),
      completedCount: normalizeNumber(summary.completedCount, base.summary.completedCount),
      noopCount: normalizeNumber(summary.noopCount, base.summary.noopCount),
      errorCount: normalizeNumber(summary.errorCount, base.summary.errorCount),
      checkpointCount: normalizeNumber(summary.checkpointCount, base.summary.checkpointCount),
      escalationCount: normalizeNumber(summary.escalationCount, base.summary.escalationCount),
      lastRunId: normalizeString(summary.lastRunId, base.summary.lastRunId),
      lastStatus: normalizeString(summary.lastStatus, base.summary.lastStatus),
      lastOutcome: normalizeString(summary.lastOutcome, base.summary.lastOutcome),
      lastCheckpointPacketId: normalizeString(summary.lastCheckpointPacketId, base.summary.lastCheckpointPacketId),
      lastCheckpointSummary: normalizeString(summary.lastCheckpointSummary, base.summary.lastCheckpointSummary),
      lastCheckpointAt: summary.lastCheckpointAt ?? base.summary.lastCheckpointAt,
      lastEscalationPacketId: normalizeString(summary.lastEscalationPacketId, base.summary.lastEscalationPacketId),
      lastEscalationFollowThroughId: normalizeString(summary.lastEscalationFollowThroughId, base.summary.lastEscalationFollowThroughId),
      lastEscalationAt: summary.lastEscalationAt ?? base.summary.lastEscalationAt,
      overview: normalizeString(summary.overview, base.summary.overview),
      resultsPath: normalizeString(summary.resultsPath, base.summary.resultsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeProgramsIndex(raw = {}) {
  const base = createProgramsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      programCount: normalizeNumber(summary.programCount, base.summary.programCount),
      activeCount: normalizeNumber(summary.activeCount, base.summary.activeCount),
      blockedCount: normalizeNumber(summary.blockedCount, base.summary.blockedCount),
      topProgramIds: normalizeStringArray(summary.topProgramIds),
      overview: normalizeString(summary.overview, base.summary.overview),
      programsPath: normalizeString(summary.programsPath, base.summary.programsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeCampaignsIndex(raw = {}) {
  const base = createCampaignsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      campaignCount: normalizeNumber(summary.campaignCount, base.summary.campaignCount),
      plannedCount: normalizeNumber(summary.plannedCount, base.summary.plannedCount),
      activeCount: normalizeNumber(summary.activeCount, base.summary.activeCount),
      reviewNeededCount: normalizeNumber(summary.reviewNeededCount, base.summary.reviewNeededCount),
      completedCount: normalizeNumber(summary.completedCount, base.summary.completedCount),
      blockedCount: normalizeNumber(summary.blockedCount, base.summary.blockedCount),
      topCampaignIds: normalizeStringArray(summary.topCampaignIds),
      overview: normalizeString(summary.overview, base.summary.overview),
      campaignsPath: normalizeString(summary.campaignsPath, base.summary.campaignsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeProgramRunsIndex(raw = {}) {
  const base = createProgramRunsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      runCount: normalizeNumber(summary.runCount, base.summary.runCount),
      approvedCount: normalizeNumber(summary.approvedCount, base.summary.approvedCount),
      activeCount: normalizeNumber(summary.activeCount, base.summary.activeCount),
      reviewNeededCount: normalizeNumber(summary.reviewNeededCount, base.summary.reviewNeededCount),
      blockedCount: normalizeNumber(summary.blockedCount, base.summary.blockedCount),
      reviewCheckpointRunCount: normalizeNumber(summary.reviewCheckpointRunCount, base.summary.reviewCheckpointRunCount),
      topRunIds: normalizeStringArray(summary.topRunIds),
      overview: normalizeString(summary.overview, base.summary.overview),
      runsPath: normalizeString(summary.runsPath, base.summary.runsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeProgramApprovalsIndex(raw = {}) {
  const base = createProgramApprovalsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      approvalCount: normalizeNumber(summary.approvalCount, base.summary.approvalCount),
      approvedCount: normalizeNumber(summary.approvedCount, base.summary.approvedCount),
      revokedCount: normalizeNumber(summary.revokedCount, base.summary.revokedCount),
      consumedCount: normalizeNumber(summary.consumedCount, base.summary.consumedCount),
      topApprovalIds: normalizeStringArray(summary.topApprovalIds),
      overview: normalizeString(summary.overview, base.summary.overview),
      approvalsPath: normalizeString(summary.approvalsPath, base.summary.approvalsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeWorkflowBoundaries(raw = {}) {
  const base = createWorkflowBoundaries();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const managedArtifacts = normalizeObject(raw.managedArtifacts);
  return {
    ...base,
    ...raw,
    version: base.version,
    managedPaths: normalizeStringArray(raw.managedPaths, base.managedPaths),
    doveBootstrapOnlyPaths: normalizeStringArray(raw.doveBootstrapOnlyPaths, base.doveBootstrapOnlyPaths),
    userOwnedPaths: normalizeStringArray(raw.userOwnedPaths, base.userOwnedPaths),
    managedArtifacts: {
      codePack: {
        ...createManagedArtifactMeta("managed-replaceable", "src"),
        ...normalizeObject(managedArtifacts.codePack)
      },
      workflowBoundaries: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.workflowBoundaries),
      workspaceIndex: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.workspaceIndex),
      doveRootManifest: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.doveRootManifest),
      executionBridgeCandidates: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.metaExecutionBridgeCandidates),
      operatorLessons: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.metaOperatorLessons),
      remediationPacks: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.metaRemediationPacks),
      documentsLedger: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.documentsLedger)
    },
    notes: normalizeStringArray(raw.notes, base.notes),
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function createSourcesIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createNotesIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createEvidenceIndex() {
  return { version: 3, claims: [], updatedAt: null };
}
function normalizeDocumentKind(value, fallback = "other") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return DOVE_DOCUMENT_KINDS.includes(normalized) ? normalized : fallback;
}
function normalizeDocumentStatus(value, fallback = "created") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return DOVE_DOCUMENT_STATUSES.includes(normalized) ? normalized : fallback;
}
function normalizeDocumentEvidenceScope(value, fallback = "internal") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return DOVE_DOCUMENT_EVIDENCE_SCOPES.includes(normalized) ? normalized : fallback;
}
function normalizeUniqueStringArray(value, fallback = []) {
  return Array.from(new Set(normalizeStringArray(value, fallback).map((item) => item.trim()).filter(Boolean)));
}
function createDocumentLedgerSummary(entries = []) {
  const lastEntry = entries.at(-1) ?? null;
  return {
    documentCount: entries.length,
    internalEvidenceCount: entries.filter((entry) => entry.evidenceScope === "internal").length,
    externalEvidenceCount: entries.filter((entry) => entry.evidenceScope === "external").length,
    mixedEvidenceCount: entries.filter((entry) => entry.evidenceScope === "mixed").length,
    publicSafeCount: entries.filter((entry) => entry.publicSafe === true).length,
    lastDocumentId: lastEntry?.documentId ?? null,
    lastUpdatedAt: lastEntry?.updatedAt ?? lastEntry?.createdAt ?? null,
    overview: entries.length > 0 ? `${entries.length} document evidence entr${entries.length === 1 ? "y" : "ies"} recorded.` : "No document evidence has been recorded yet.",
    ledgerPath: ARTIFACT_PATHS.documentsLedger
  };
}
function normalizeDocumentLedgerEntry(value, index = 0) {
  const source = normalizeObject(value);
  const id = normalizeString(source.id, `document-entry-${index + 1}`);
  const documentId = normalizeString(source.documentId, id);
  const evidenceScope = normalizeDocumentEvidenceScope(source.evidenceScope ?? source.scope, "internal");
  const publicSafe = normalizeBoolean(source.publicSafe, false);
  return {
    id,
    packetId: normalizeString(source.packetId ?? source.taskPacketId ?? source.missionPacketId ?? source.taskId, null),
    documentId,
    title: normalizeString(source.title, documentId),
    documentPath: normalizeString(source.documentPath ?? source.path, null),
    documentKind: normalizeDocumentKind(source.documentKind ?? source.kind, "other"),
    status: normalizeDocumentStatus(source.status, "created"),
    evidenceScope,
    publicSafe,
    summary: normalizeString(source.summary, ""),
    context: normalizeString(source.context, ""),
    sourceRefs: normalizeUniqueStringArray(source.sourceRefs ?? source.sourceIds),
    artifactRefs: normalizeUniqueStringArray(source.artifactRefs ?? source.artifactPaths),
    evidenceLinks: normalizeUniqueStringArray(source.evidenceLinks ?? source.evidencePaths),
    claimIds: normalizeUniqueStringArray(source.claimIds ?? source.claims),
    createdAt: normalizeString(source.createdAt, null),
    updatedAt: normalizeString(source.updatedAt, source.createdAt ?? null),
    createdBy: normalizeString(source.createdBy ?? source.actorRole, "operator"),
    appendOnly: true,
    rawTranscriptIncluded: false,
    privateReasoningIncluded: false,
    environmentIncluded: false
  };
}
function createDocumentLedgerIndex() {
  const entries = [];
  return {
    version: 1,
    entries,
    summary: createDocumentLedgerSummary(entries),
    updatedAt: null
  };
}
function normalizeDocumentLedgerIndex(raw = {}) {
  const source = normalizeObject(raw);
  const entries = normalizeObjectArray(source.entries).map((entry, index) => normalizeDocumentLedgerEntry(entry, index));
  return {
    version: 1,
    entries,
    summary: createDocumentLedgerSummary(entries),
    updatedAt: normalizeString(source.updatedAt, entries.at(-1)?.updatedAt ?? entries.at(-1)?.createdAt ?? null)
  };
}
function createMutationProvenanceSummary(entries) {
  const patchPlanCount = entries.filter((entry) => entry.mutationMode === "patch-plan").length;
  const directProcessCount = entries.filter((entry) => entry.mutationMode === "direct-process").length;
  const lastEntry = entries.at(-1) ?? null;
  return {
    mutationCount: entries.length,
    patchPlanCount,
    directProcessCount,
    lastMutationId: lastEntry?.id ?? null,
    lastMutationMode: lastEntry?.mutationMode ?? null,
    lastAppliedBy: lastEntry?.appliedBy ?? null,
    hostRollbackEligible: lastEntry?.hostRollbackEligible ?? false,
    hostRollbackIneligibleReason: lastEntry?.hostRollbackIneligibleReason ?? null,
    recommendedMutationMode: lastEntry?.recommendedMutationMode ?? null,
    rollbackAdvice: lastEntry?.rollbackAdvice ?? null,
    hostCheckpointVerified: false,
    doveRestoreSupported: false
  };
}
function normalizeMutationProvenanceEntry(raw = {}, index = 0) {
  const source = normalizeObject(raw);
  const mutationMode = source.mutationMode === "patch-plan" ? "patch-plan" : "direct-process";
  const writesApplied = normalizeBoolean(source.writesApplied, mutationMode === "direct-process");
  return {
    id: normalizeString(source.id ?? source.mutationId, `mutation-${index + 1}`),
    actionId: normalizeString(source.actionId, "unspecified"),
    packetId: normalizeString(source.packetId, null),
    mutationMode,
    mutationModeSource: normalizeString(source.mutationModeSource, "unknown"),
    writesApplied,
    appliedBy: normalizeString(source.appliedBy, mutationMode === "patch-plan" ? "host-tracked-file-edits-required" : "node-fs"),
    hostId: normalizeString(source.hostId, "unknown"),
    hostRollbackEligible: normalizeBoolean(source.hostRollbackEligible, mutationMode === "patch-plan"),
    hostTrackedFileEditsRequired: normalizeBoolean(source.hostTrackedFileEditsRequired, mutationMode === "patch-plan"),
    hostCheckpointVerified: false,
    directProcessWritesAreRollbackSafe: false,
    externalWriteCaptureVerified: false,
    doveRestoreSupported: false,
    hostRollbackIneligibleReason: normalizeString(source.hostRollbackIneligibleReason, mutationMode === "patch-plan" ? null : "direct-process writes are not verified host rollback-safe"),
    recommendedMutationMode: normalizeString(source.recommendedMutationMode, mutationMode === "patch-plan" ? null : "patch-plan"),
    rollbackAdvice: normalizeString(source.rollbackAdvice, mutationMode === "patch-plan" ? null : "Use mutationMode: patch-plan for host-tracked rollback eligibility."),
    operationCount: Number.isFinite(source.operationCount) ? Math.max(0, Math.floor(source.operationCount)) : normalizeUniqueStringArray(source.paths).length,
    paths: normalizeUniqueStringArray(source.paths),
    createdAt: normalizeString(source.createdAt, null)
  };
}
function createMutationProvenanceIndex() {
  const entries = [];
  return {
    version: 1,
    entries,
    summary: createMutationProvenanceSummary(entries),
    updatedAt: null
  };
}
function normalizeMutationProvenanceIndex(raw = {}) {
  const source = normalizeObject(raw);
  const entries = normalizeObjectArray(source.entries).map((entry, index) => normalizeMutationProvenanceEntry(entry, index));
  return {
    version: 1,
    entries,
    summary: createMutationProvenanceSummary(entries),
    updatedAt: normalizeString(source.updatedAt, entries.at(-1)?.createdAt ?? null)
  };
}
function createTaskPacketsIndex() {
  return {
    version: 4,
    items: [],
    taskModel: {
      uniqueInitLevel: 0,
      userDefaultLevel: 3,
      activeInitId: null,
      activeTaskIds: []
    },
    lifecycleCounts: {},
    stageCounts: Object.fromEntries(DOVE_TASK_STAGES.map((stage) => [stage, 0])),
    domainCounts: Object.fromEntries(DOVE_TASK_DOMAINS.map((domain) => [domain, 0])),
    levelCounts: {},
    lifecycleFamilyCounts: Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0])),
    dependencyHealth: {
      blockedPacketIds: [],
      readyPacketIds: [],
      stalePacketIds: [],
      missingDependencyIds: []
    },
    updatedAt: null
  };
}
function createRuntimeControllerState() {
  return {
    version: 1,
    explicitInvocationOnly: true,
    noDaemon: true,
    selectionPolicy: "planner-materialized-guidance-v2",
    boundedStepPolicy: "planner-control-plane-worker-step-v1",
    lastRun: null,
    summary: {
      lastRunId: null,
      lastStatus: "never-run",
      lastOutcome: "not-started",
      lastSelectedPacketId: null,
      lastEnvelopeWorkerRole: null,
      lastProgramId: null,
      lastProgramRunId: null,
      lastApprovalId: null,
      lastProgramOutcome: "not-started",
      requestCount: 0,
      acceptedRequestCount: 0,
      executingRequestCount: 0,
      staleRequestCount: 0,
      overdueExecutionCount: 0,
      dueReviewCount: 0,
      checkpointCount: 0,
      escalationCount: 0,
      lastCheckpointPacketId: null,
      lastCheckpointSummary: null,
      lastCheckpointAt: null,
      lastEscalationPacketId: null,
      lastEscalationFollowThroughId: null,
      lastEscalationAt: null,
      overview: "No autonomous control-plane run has been executed yet.",
      controllerStatePath: ARTIFACT_PATHS.runtimeControllerState,
      leasesPath: ARTIFACT_PATHS.runtimeLeases,
      eventsPath: ARTIFACT_PATHS.runtimeEvents,
      resultsPath: ARTIFACT_PATHS.runtimeResults
    },
    updatedAt: null
  };
}
function createRuntimeContinuationIndex() {
  return {
    version: 1,
    explicitInvocationOnly: true,
    noDaemon: true,
    items: [],
    summary: {
      continuationCount: 0,
      currentKind: null,
      currentPacketId: null,
      currentProgramRunId: null,
      currentCommand: null,
      overview: "No explicit autonomy continuation is currently pending.",
      continuationPath: ARTIFACT_PATHS.runtimeContinuation
    },
    updatedAt: null
  };
}
function createRuntimeLeasesIndex() {
  return {
    version: 1,
    explicitInvocationOnly: true,
    items: [],
    summary: {
      activeLeaseCount: 0,
      activePacketIds: [],
      activeLeaseIds: [],
      overview: "No autonomous control-plane leases are currently active.",
      leasesPath: ARTIFACT_PATHS.runtimeLeases
    },
    updatedAt: null
  };
}
function createRuntimeEventsIndex() {
  return {
    version: 1,
    explicitInvocationOnly: true,
    entries: [],
    summary: {
      eventCount: 0,
      lastEventType: null,
      lastRunId: null,
      overview: "No autonomous control-plane events have been recorded yet.",
      eventsPath: ARTIFACT_PATHS.runtimeEvents
    },
    updatedAt: null
  };
}
function createRuntimeResultsIndex() {
  return {
    version: 1,
    explicitInvocationOnly: true,
    entries: [],
    summary: {
      runCount: 0,
      completedCount: 0,
      noopCount: 0,
      errorCount: 0,
      checkpointCount: 0,
      escalationCount: 0,
      lastRunId: null,
      lastStatus: "never-run",
      lastOutcome: "not-started",
      lastCheckpointPacketId: null,
      lastCheckpointSummary: null,
      lastCheckpointAt: null,
      lastEscalationPacketId: null,
      lastEscalationFollowThroughId: null,
      lastEscalationAt: null,
      overview: "No autonomous control-plane results have been recorded yet.",
      resultsPath: ARTIFACT_PATHS.runtimeResults
    },
    updatedAt: null
  };
}
function createReviewState() {
  return {
    version: 3,
    lastVerdict: "not-reviewed",
    lastReviewedAt: null,
    history: [],
    openItems: [],
    unresolvedConcernIds: [],
    escalatedConcernIds: [],
    pendingAuthorResponseIds: [],
    pendingReviewerRulingIds: [],
    reviewRound: 0,
    reviewerIndependence: {
      reviewerRole: "reviewer",
      responseOwnerRoles: [],
      separationMaintained: true
    }
  };
}
function createReviewConcernsIndex() {
  return { version: 2, items: [], updatedAt: null };
}
function createAdversarialReviewState() {
  return {
    version: 2,
    round: 0,
    unresolvedConcernIds: [],
    escalatedConcernIds: [],
    pendingAuthorResponseIds: [],
    pendingReviewerRulingIds: [],
    concernStatusCounts: {},
    escalationThresholds: { high: 1, medium: 2, low: 3 },
    lastAuditIds: [],
    lastBridgeIds: [],
    updatedAt: null
  };
}
function createFiguresIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureBriefsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureSegmentsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureTemplatesIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureEditableIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureFinalIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureQaIndex() {
  return { version: 1, items: [], issues: [], updatedAt: null };
}
function createFigureMaterialsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureGenerationsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createFigureCaptionsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createResearchAgenda() {
  return {
    version: 1,
    objective: "\u8BB0\u5F55\u8BBA\u6587\u76EE\u6807\u4E0E\u8D21\u732E\u3002",
    agenda: [],
    evidenceBacklog: [],
    updatedAt: null
  };
}
function createProgramsIndex() {
  return {
    version: 1,
    items: [],
    summary: {
      programCount: 0,
      activeCount: 0,
      blockedCount: 0,
      topProgramIds: [],
      overview: "No research programs have been recorded yet.",
      programsPath: ARTIFACT_PATHS.programsIndex
    },
    updatedAt: null
  };
}
function createCampaignsIndex() {
  return {
    version: 1,
    items: [],
    summary: {
      campaignCount: 0,
      plannedCount: 0,
      activeCount: 0,
      reviewNeededCount: 0,
      completedCount: 0,
      blockedCount: 0,
      topCampaignIds: [],
      overview: "No multi-cycle research campaigns have been recorded yet.",
      campaignsPath: ARTIFACT_PATHS.campaignsIndex
    },
    updatedAt: null
  };
}
function createProgramRunsIndex() {
  return {
    version: 1,
    items: [],
    summary: {
      runCount: 0,
      approvedCount: 0,
      activeCount: 0,
      reviewNeededCount: 0,
      blockedCount: 0,
      reviewCheckpointRunCount: 0,
      topRunIds: [],
      overview: "No approved program runs have been recorded yet.",
      runsPath: ARTIFACT_PATHS.programRuns
    },
    updatedAt: null
  };
}
function createProgramApprovalsIndex() {
  return {
    version: 1,
    items: [],
    summary: {
      approvalCount: 0,
      approvedCount: 0,
      revokedCount: 0,
      consumedCount: 0,
      topApprovalIds: [],
      overview: "No program approvals have been recorded yet.",
      approvalsPath: ARTIFACT_PATHS.programApprovals
    },
    updatedAt: null
  };
}
function createSessionJournal() {
  return { version: 1, entries: [], updatedAt: null };
}
function createExperimentPlansIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createExperimentResultsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createExperimentAuditsIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createClaimBridgeLog() {
  return { version: 1, items: [], updatedAt: null };
}
function createMetaEventsIndex() {
  return {
    version: 1,
    proposalOnly: true,
    items: [],
    updatedAt: null
  };
}
function createMetaLongHorizonMemory() {
  return {
    version: 1,
    proposalOnly: true,
    historyWindowSize: 30,
    horizon: {
      sessionEntriesAnalyzed: 0,
      reviewRoundsObserved: 0,
      versionComparisonsAnalyzed: 0,
      auditRecordsAnalyzed: 0,
      bridgeRecordsAnalyzed: 0
    },
    summary: {
      familyCount: 0,
      recurringFamilyCount: 0,
      risingFamilyCount: 0,
      stableFamilyCount: 0,
      coolingFamilyCount: 0,
      snapshotCount: 0,
      lastObservedAt: null,
      lastAction: "unchanged",
      topFamilyIds: [],
      topTaxonomyFamilyIds: [],
      topTaxonomyGroupIds: [],
      pressureAreas: [],
      overview: "No long-horizon workflow memory has been summarized yet."
    },
    historyPolicy: {
      mode: "deterministic-noop-drift-guard-v1",
      lastAction: "unchanged",
      reason: "No long-horizon snapshots have been recorded yet.",
      comparedAt: null,
      lastMeaningfulChangeAt: null
    },
    history: [],
    families: [],
    updatedAt: null
  };
}
function normalizeMetaLongHorizonMemory(raw = {}) {
  const base = createMetaLongHorizonMemory();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const horizon = normalizeObject(raw.horizon);
  const summary = normalizeObject(raw.summary);
  const historyPolicy = normalizeObject(raw.historyPolicy);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    historyWindowSize: normalizeNumber(raw.historyWindowSize, base.historyWindowSize),
    horizon: {
      ...base.horizon,
      ...horizon,
      sessionEntriesAnalyzed: normalizeNumber(horizon.sessionEntriesAnalyzed, base.horizon.sessionEntriesAnalyzed),
      reviewRoundsObserved: normalizeNumber(horizon.reviewRoundsObserved, base.horizon.reviewRoundsObserved),
      versionComparisonsAnalyzed: normalizeNumber(horizon.versionComparisonsAnalyzed, base.horizon.versionComparisonsAnalyzed),
      auditRecordsAnalyzed: normalizeNumber(horizon.auditRecordsAnalyzed, base.horizon.auditRecordsAnalyzed),
      bridgeRecordsAnalyzed: normalizeNumber(horizon.bridgeRecordsAnalyzed, base.horizon.bridgeRecordsAnalyzed)
    },
    summary: {
      ...base.summary,
      ...summary,
      familyCount: normalizeNumber(summary.familyCount, base.summary.familyCount),
      recurringFamilyCount: normalizeNumber(summary.recurringFamilyCount, base.summary.recurringFamilyCount),
      risingFamilyCount: normalizeNumber(summary.risingFamilyCount, base.summary.risingFamilyCount),
      stableFamilyCount: normalizeNumber(summary.stableFamilyCount, base.summary.stableFamilyCount),
      coolingFamilyCount: normalizeNumber(summary.coolingFamilyCount, base.summary.coolingFamilyCount),
      snapshotCount: normalizeNumber(summary.snapshotCount, base.summary.snapshotCount),
      lastObservedAt: summary.lastObservedAt ?? base.summary.lastObservedAt,
      lastAction: normalizeString(summary.lastAction, base.summary.lastAction),
      topFamilyIds: normalizeStringArray(summary.topFamilyIds),
      topTaxonomyFamilyIds: normalizeStringArray(summary.topTaxonomyFamilyIds),
      topTaxonomyGroupIds: normalizeStringArray(summary.topTaxonomyGroupIds),
      pressureAreas: normalizeStringArray(summary.pressureAreas),
      overview: normalizeString(summary.overview, base.summary.overview)
    },
    historyPolicy: {
      ...base.historyPolicy,
      ...historyPolicy,
      mode: normalizeString(historyPolicy.mode, base.historyPolicy.mode),
      lastAction: normalizeString(historyPolicy.lastAction, base.historyPolicy.lastAction),
      reason: normalizeString(historyPolicy.reason, base.historyPolicy.reason),
      comparedAt: historyPolicy.comparedAt ?? base.historyPolicy.comparedAt,
      lastMeaningfulChangeAt: historyPolicy.lastMeaningfulChangeAt ?? base.historyPolicy.lastMeaningfulChangeAt
    },
    history: normalizeObjectArray(raw.history),
    families: normalizeObjectArray(raw.families),
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function createMetaRemediationPacksIndex() {
  return {
    version: 1,
    proposalOnly: true,
    packs: [],
    summary: {
      packCount: 0,
      topPackIds: [],
      topClusterIds: [],
      actionableCount: 0,
      partiallyActionableCount: 0,
      advisoryCount: 0,
      readinessOverview: "No proposal-only remediation packs have been generated yet.",
      overview: "No proposal-only remediation packs have been generated yet.",
      packsPath: ARTIFACT_PATHS.metaRemediationPacks
    },
    sourceArtifacts: [
      ARTIFACT_PATHS.metaRecommendations,
      ARTIFACT_PATHS.metaLongHorizonMemory,
      ARTIFACT_PATHS.reviewConcerns,
      ARTIFACT_PATHS.figureQa,
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.navigationReport,
      ARTIFACT_PATHS.sessionSummary
    ],
    updatedAt: null
  };
}
function normalizeMetaRemediationPacksIndex(raw = {}) {
  const base = createMetaRemediationPacksIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    packs: normalizeObjectArray(raw.packs),
    summary: {
      ...base.summary,
      ...summary,
      packCount: normalizeNumber(summary.packCount, base.summary.packCount),
      topPackIds: normalizeStringArray(summary.topPackIds),
      topClusterIds: normalizeStringArray(summary.topClusterIds),
      actionableCount: normalizeNumber(summary.actionableCount, base.summary.actionableCount),
      partiallyActionableCount: normalizeNumber(summary.partiallyActionableCount, base.summary.partiallyActionableCount),
      advisoryCount: normalizeNumber(summary.advisoryCount, base.summary.advisoryCount),
      readinessOverview: normalizeString(summary.readinessOverview, base.summary.readinessOverview),
      overview: normalizeString(summary.overview, base.summary.overview),
      packsPath: normalizeString(summary.packsPath, base.summary.packsPath)
    },
    sourceArtifacts: normalizeStringArray(raw.sourceArtifacts, base.sourceArtifacts),
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function cleanUniqueStrings(value, fallback = []) {
  return Array.from(new Set(normalizeStringArray(value, fallback).map((item) => item.trim()).filter(Boolean)));
}
function cleanOptionalString(value, fallback = null) {
  const normalized = normalizeString(value, fallback);
  return typeof normalized === "string" ? normalized.trim() : normalized;
}
function isTrellisTaskSourceArtifact(value) {
  const normalized = String(value ?? "").trim().replace(/\\/g, "/").replace(/^\.\/+/, "");
  return normalized === ".trellis/tasks" || normalized.startsWith(".trellis/tasks/") || normalized === "trellis/tasks" || normalized.startsWith("trellis/tasks/");
}
function normalizeOperatorLessonStatus(value) {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return ["active", "retired", "superseded", "archived"].includes(normalized) ? normalized : "active";
}
function summarizeOperatorLessons(lessons = []) {
  const activeLessons2 = lessons.filter((lesson) => lesson.status === "active");
  const countBy4 = (items) => items.reduce((accumulator, item) => {
    accumulator[item] = (accumulator[item] ?? 0) + 1;
    return accumulator;
  }, {});
  const topKeys = (items) => Object.entries(countBy4(items)).sort((left, right) => right[1] - left[1] || left[0].localeCompare(right[0])).slice(0, 8).map(([key]) => key);
  const sortedActive = [...activeLessons2].sort((left, right) => String(right.updatedAt ?? right.createdAt ?? "").localeCompare(String(left.updatedAt ?? left.createdAt ?? "")) || left.id.localeCompare(right.id));
  return {
    lessonCount: lessons.length,
    activeLessonCount: activeLessons2.length,
    topLessonIds: sortedActive.slice(0, 8).map((lesson) => lesson.id),
    topTags: topKeys(activeLessons2.flatMap((lesson) => lesson.tags ?? [])),
    topDomains: topKeys(activeLessons2.map((lesson) => lesson.domain).filter(Boolean)),
    overview: activeLessons2.length > 0 ? `${activeLessons2.length} active distilled operator lessons are available for reuse.` : "No distilled operator lessons have been recorded yet.",
    lessonsPath: ARTIFACT_PATHS.metaOperatorLessons
  };
}
function normalizeOperatorLesson(raw = {}, index = 0) {
  const title = cleanOptionalString(raw.title, `Operator lesson ${index + 1}`);
  const actorRole = cleanOptionalString(raw.actorRole, "planner");
  const createdAt = cleanOptionalString(raw.createdAt, null);
  const updatedAt = cleanOptionalString(raw.updatedAt, createdAt);
  const sourceArtifacts = cleanUniqueStrings(raw.sourceArtifacts).filter((artifactPath) => !isTrellisTaskSourceArtifact(artifactPath));
  return {
    ...raw,
    id: cleanOptionalString(raw.id, `lesson-${index + 1}`),
    title,
    problem: cleanOptionalString(raw.problem, "No problem statement recorded."),
    decisions: cleanUniqueStrings(raw.decisions),
    pitfalls: cleanUniqueStrings(raw.pitfalls),
    validation: cleanUniqueStrings(raw.validation),
    nextTime: cleanUniqueStrings(raw.nextTime),
    domain: normalizeDoveDomainId(raw.domain, "engineering"),
    stage: normalizeDoveMissionLifecycleStage(raw.stage, "return"),
    actorRole: ROLE_IDS.includes(actorRole) ? actorRole : "planner",
    tags: cleanUniqueStrings(raw.tags),
    sourceType: cleanOptionalString(raw.sourceType, "manual-retrospective"),
    sourceId: cleanOptionalString(raw.sourceId, null),
    sourceArtifacts,
    packetIds: cleanUniqueStrings(raw.packetIds),
    recommendationIds: cleanUniqueStrings(raw.recommendationIds),
    playbookIds: cleanUniqueStrings(raw.playbookIds),
    remediationPackIds: cleanUniqueStrings(raw.remediationPackIds),
    status: normalizeOperatorLessonStatus(raw.status),
    createdAt,
    updatedAt
  };
}
function createMetaOperatorLessonsIndex() {
  return {
    version: 1,
    referenceOnly: true,
    explicitOnly: true,
    noAutoCapture: true,
    noAutoApply: true,
    lessons: [],
    summary: summarizeOperatorLessons([]),
    sourceArtifacts: [
      ARTIFACT_PATHS.sessionSummary,
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.metaOperatorPlaybooks,
      ARTIFACT_PATHS.metaRemediationPacks,
      ARTIFACT_PATHS.metaLongHorizonMemory
    ],
    updatedAt: null
  };
}
function normalizeMetaOperatorLessonsIndex(raw = {}) {
  const base = createMetaOperatorLessonsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const lessons = normalizeObjectArray(raw.lessons).map(normalizeOperatorLesson);
  const sourceArtifacts = cleanUniqueStrings(raw.sourceArtifacts, base.sourceArtifacts).filter((artifactPath) => !isTrellisTaskSourceArtifact(artifactPath));
  return {
    ...base,
    ...raw,
    version: base.version,
    referenceOnly: base.referenceOnly,
    explicitOnly: base.explicitOnly,
    noAutoCapture: base.noAutoCapture,
    noAutoApply: base.noAutoApply,
    lessons,
    summary: summarizeOperatorLessons(lessons),
    sourceArtifacts: sourceArtifacts.length > 0 ? sourceArtifacts : base.sourceArtifacts,
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function createMetaOperatorPlaybooksIndex() {
  return {
    version: 1,
    proposalOnly: true,
    playbooks: [],
    summary: {
      playbookCount: 0,
      topPlaybookIds: [],
      topTaxonomyFamilyIds: [],
      actionableCount: 0,
      partiallyActionableCount: 0,
      advisoryCount: 0,
      readinessOverview: "No proposal-only family-level operator playbooks have been generated yet.",
      overview: "No proposal-only family-level operator playbooks have been generated yet.",
      playbooksPath: ARTIFACT_PATHS.metaOperatorPlaybooks
    },
    sourceArtifacts: [
      ARTIFACT_PATHS.wikiRelations,
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.metaRecommendations,
      ARTIFACT_PATHS.metaLongHorizonMemory,
      ARTIFACT_PATHS.metaRemediationPacks,
      ARTIFACT_PATHS.metaOptimizerReport
    ],
    updatedAt: null
  };
}
function createMetaExecutionBridgeCandidatesIndex() {
  return {
    version: 1,
    proposalOnly: true,
    noAutoApply: true,
    candidates: [],
    summary: {
      candidateCount: 0,
      topCandidateIds: [],
      candidateTypeCounts: {},
      overview: "No proposal-only execution bridge candidates have been generated yet.",
      candidatesPath: ARTIFACT_PATHS.metaExecutionBridgeCandidates
    },
    sourceArtifacts: [
      ARTIFACT_PATHS.metaOperatorPlaybooks,
      ARTIFACT_PATHS.metaRemediationPacks,
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.metaOptimizerReport
    ],
    updatedAt: null
  };
}
function createMetaGovernanceCoverageIndex() {
  return {
    version: 1,
    proposalOnly: true,
    guardedMutations: [],
    exemptMutations: [],
    summary: {
      guardedCount: 0,
      exemptCount: 0,
      overview: "No governance coverage matrix has been generated yet.",
      coveragePath: ARTIFACT_PATHS.metaGovernanceCoverage
    },
    updatedAt: null
  };
}
function createMetaGovernanceCoverageReport() {
  return {
    version: 1,
    status: "pending",
    guardedIds: [],
    exemptIds: [],
    surfaceBindingAudit: {
      uncoveredTools: [],
      uncoveredCommands: [],
      uncoveredCoreFunctions: [],
      uncoveredNegativeCoverage: []
    },
    summary: {
      guardedCount: 0,
      exemptCount: 0,
      overview: "No governance coverage proof has been generated yet.",
      reportPath: ARTIFACT_PATHS.metaGovernanceCoverageReport,
      markdownPath: ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown
    },
    updatedAt: null
  };
}
function normalizeMetaGovernanceCoverageReport(raw = {}) {
  const base = createMetaGovernanceCoverageReport();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const audit = normalizeObject(raw.surfaceBindingAudit);
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    status: normalizeString(raw.status, base.status),
    guardedIds: normalizeStringArray(raw.guardedIds),
    exemptIds: normalizeStringArray(raw.exemptIds),
    surfaceBindingAudit: {
      ...base.surfaceBindingAudit,
      ...audit,
      uncoveredTools: normalizeStringArray(audit.uncoveredTools),
      uncoveredCommands: normalizeStringArray(audit.uncoveredCommands),
      uncoveredCoreFunctions: normalizeStringArray(audit.uncoveredCoreFunctions),
      uncoveredNegativeCoverage: normalizeStringArray(audit.uncoveredNegativeCoverage)
    },
    summary: {
      ...base.summary,
      ...summary,
      guardedCount: normalizeNumber(summary.guardedCount, base.summary.guardedCount),
      exemptCount: normalizeNumber(summary.exemptCount, base.summary.exemptCount),
      overview: normalizeString(summary.overview, base.summary.overview),
      reportPath: normalizeString(summary.reportPath, base.summary.reportPath),
      markdownPath: normalizeString(summary.markdownPath, base.summary.markdownPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeMetaGovernanceCoverageIndex(raw = {}) {
  const base = createMetaGovernanceCoverageIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    guardedMutations: normalizeObjectArray(raw.guardedMutations),
    exemptMutations: normalizeObjectArray(raw.exemptMutations),
    summary: {
      ...base.summary,
      ...summary,
      guardedCount: normalizeNumber(summary.guardedCount, base.summary.guardedCount),
      exemptCount: normalizeNumber(summary.exemptCount, base.summary.exemptCount),
      overview: normalizeString(summary.overview, base.summary.overview),
      coveragePath: normalizeString(summary.coveragePath, base.summary.coveragePath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function createMetaOperatorFollowThroughIndex() {
  return {
    version: 1,
    proposalOnly: true,
    explicitOnly: true,
    items: [],
    summary: {
      itemCount: 0,
      acknowledgedCount: 0,
      acceptedForExecutionCount: 0,
      executingCount: 0,
      overdueExecutionCount: 0,
      criticalOverdueExecutionCount: 0,
      deferredCount: 0,
      acceptedRiskCount: 0,
      closedCount: 0,
      supersededCount: 0,
      invalidStatusCount: 0,
      staleCount: 0,
      dueDeferredCount: 0,
      topSourceIds: [],
      overview: "No operator follow-through decisions have been recorded yet.",
      followThroughPath: ARTIFACT_PATHS.metaOperatorFollowThrough
    },
    sourceArtifacts: [
      ARTIFACT_PATHS.metaRemediationPacks,
      ARTIFACT_PATHS.metaOperatorPlaybooks,
      ARTIFACT_PATHS.metaExecutionBridgeCandidates,
      ARTIFACT_PATHS.metaOptimizerReport,
      ARTIFACT_PATHS.workspaceIndex
    ],
    updatedAt: null
  };
}
function createMetaOperatorFollowThroughTransitionsIndex() {
  return {
    version: 1,
    proposalOnly: true,
    explicitOnly: true,
    transitions: [],
    summary: {
      transitionCount: 0,
      overview: "No operator follow-through transitions have been recorded yet.",
      transitionsPath: ARTIFACT_PATHS.metaOperatorFollowThroughTransitions
    },
    updatedAt: null
  };
}
function normalizeMetaOperatorFollowThroughTransitionsIndex(raw = {}) {
  const base = createMetaOperatorFollowThroughTransitionsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    explicitOnly: normalizeBoolean(raw.explicitOnly, base.explicitOnly),
    transitions: normalizeObjectArray(raw.transitions),
    summary: {
      ...base.summary,
      ...summary,
      transitionCount: normalizeNumber(summary.transitionCount, base.summary.transitionCount),
      overview: normalizeString(summary.overview, base.summary.overview),
      transitionsPath: normalizeString(summary.transitionsPath, base.summary.transitionsPath)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeMetaOperatorFollowThroughIndex(raw = {}) {
  const base = createMetaOperatorFollowThroughIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    explicitOnly: normalizeBoolean(raw.explicitOnly, base.explicitOnly),
    items: normalizeObjectArray(raw.items),
    summary: {
      ...base.summary,
      ...summary,
      itemCount: normalizeNumber(summary.itemCount, base.summary.itemCount),
      acknowledgedCount: normalizeNumber(summary.acknowledgedCount, base.summary.acknowledgedCount),
      acceptedForExecutionCount: normalizeNumber(summary.acceptedForExecutionCount, base.summary.acceptedForExecutionCount),
      executingCount: normalizeNumber(summary.executingCount, base.summary.executingCount),
      overdueExecutionCount: normalizeNumber(summary.overdueExecutionCount, base.summary.overdueExecutionCount),
      criticalOverdueExecutionCount: normalizeNumber(summary.criticalOverdueExecutionCount, base.summary.criticalOverdueExecutionCount),
      deferredCount: normalizeNumber(summary.deferredCount, base.summary.deferredCount),
      acceptedRiskCount: normalizeNumber(summary.acceptedRiskCount, base.summary.acceptedRiskCount),
      closedCount: normalizeNumber(summary.closedCount, base.summary.closedCount),
      supersededCount: normalizeNumber(summary.supersededCount, base.summary.supersededCount),
      invalidStatusCount: normalizeNumber(summary.invalidStatusCount, base.summary.invalidStatusCount),
      staleCount: normalizeNumber(summary.staleCount, base.summary.staleCount),
      dueDeferredCount: normalizeNumber(summary.dueDeferredCount, base.summary.dueDeferredCount),
      topSourceIds: normalizeStringArray(summary.topSourceIds),
      overview: normalizeString(summary.overview, base.summary.overview),
      followThroughPath: normalizeString(summary.followThroughPath, base.summary.followThroughPath)
    },
    sourceArtifacts: normalizeStringArray(raw.sourceArtifacts, base.sourceArtifacts),
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeMetaExecutionBridgeCandidatesIndex(raw = {}) {
  const base = createMetaExecutionBridgeCandidatesIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    noAutoApply: normalizeBoolean(raw.noAutoApply, base.noAutoApply),
    candidates: normalizeObjectArray(raw.candidates),
    summary: {
      ...base.summary,
      ...summary,
      candidateCount: normalizeNumber(summary.candidateCount, base.summary.candidateCount),
      topCandidateIds: normalizeStringArray(summary.topCandidateIds),
      candidateTypeCounts: normalizeObject(summary.candidateTypeCounts),
      overview: normalizeString(summary.overview, base.summary.overview),
      candidatesPath: normalizeString(summary.candidatesPath, base.summary.candidatesPath)
    },
    sourceArtifacts: normalizeStringArray(raw.sourceArtifacts, base.sourceArtifacts),
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeMetaOperatorPlaybooksIndex(raw = {}) {
  const base = createMetaOperatorPlaybooksIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    playbooks: normalizeObjectArray(raw.playbooks),
    summary: {
      ...base.summary,
      ...summary,
      playbookCount: normalizeNumber(summary.playbookCount, base.summary.playbookCount),
      topPlaybookIds: normalizeStringArray(summary.topPlaybookIds),
      topTaxonomyFamilyIds: normalizeStringArray(summary.topTaxonomyFamilyIds),
      actionableCount: normalizeNumber(summary.actionableCount, base.summary.actionableCount),
      partiallyActionableCount: normalizeNumber(summary.partiallyActionableCount, base.summary.partiallyActionableCount),
      advisoryCount: normalizeNumber(summary.advisoryCount, base.summary.advisoryCount),
      readinessOverview: normalizeString(summary.readinessOverview, base.summary.readinessOverview),
      overview: normalizeString(summary.overview, base.summary.overview),
      playbooksPath: normalizeString(summary.playbooksPath, base.summary.playbooksPath)
    },
    sourceArtifacts: normalizeStringArray(raw.sourceArtifacts, base.sourceArtifacts),
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function createMetaRecommendationsIndex() {
  return {
    version: 3,
    proposalOnly: true,
    items: [],
    clusters: [],
    ranking: {
      method: "durable-signal-frontier-v1",
      signals: [
        "priority",
        "recurrenceCount",
        "evidenceDensity",
        "crossSessionRecurrence",
        "repairFrontierOverlap",
        "auditCriticality",
        "bridgeCriticality",
        "queueChurn",
        "taxonomyFamilyPressure",
        "taxonomyGroupPressure"
      ],
      tieBreakOrder: ["score-desc", "priority-rank", "cluster-rank", "cluster-id", "category", "id"]
    },
    frontier: {
      recommendationCount: 0,
      criticalCount: 0,
      clusterCount: 0,
      frontierScore: 0,
      topClusterIds: [],
      topRecommendationIds: [],
      activeSignalTypes: [],
      topTaxonomyFamilyIds: [],
      topTaxonomyGroupIds: [],
      pressureAreas: [],
      taxonomyOverview: "No typed wiki taxonomy pressure is currently active in the optimizer frontier.",
      frontierSummary: "No proposal-only optimizer recommendations have been generated yet.",
      rankingMethod: "durable-signal-frontier-v1"
    },
    summary: {
      recommendationCount: 0,
      criticalCount: 0,
      clusterCount: 0,
      frontierScore: 0,
      categories: {},
      signalTypes: [],
      topClusterIds: [],
      topRecommendationIds: [],
      topTaxonomyFamilyIds: [],
      topTaxonomyGroupIds: [],
      pressureAreas: [],
      taxonomyOverview: "No typed wiki taxonomy pressure is currently active in the optimizer frontier.",
      clusterMembership: {},
      topClusters: []
    },
    updatedAt: null
  };
}
function normalizeMetaRecommendationsIndex(raw = {}) {
  const base = createMetaRecommendationsIndex();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const ranking = normalizeObject(raw.ranking);
  const frontier = normalizeObject(raw.frontier);
  const summary = normalizeObject(raw.summary);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    items: normalizeObjectArray(raw.items),
    clusters: normalizeObjectArray(raw.clusters),
    ranking: {
      ...base.ranking,
      ...ranking,
      method: normalizeString(ranking.method, base.ranking.method),
      signals: normalizeStringArray(ranking.signals, base.ranking.signals),
      tieBreakOrder: normalizeStringArray(ranking.tieBreakOrder, base.ranking.tieBreakOrder)
    },
    frontier: {
      ...base.frontier,
      ...frontier,
      recommendationCount: normalizeNumber(frontier.recommendationCount, base.frontier.recommendationCount),
      criticalCount: normalizeNumber(frontier.criticalCount, base.frontier.criticalCount),
      clusterCount: normalizeNumber(frontier.clusterCount, base.frontier.clusterCount),
      frontierScore: normalizeNumber(frontier.frontierScore, base.frontier.frontierScore),
      topClusterIds: normalizeStringArray(frontier.topClusterIds),
      topRecommendationIds: normalizeStringArray(frontier.topRecommendationIds),
      activeSignalTypes: normalizeStringArray(frontier.activeSignalTypes),
      topTaxonomyFamilyIds: normalizeStringArray(frontier.topTaxonomyFamilyIds),
      topTaxonomyGroupIds: normalizeStringArray(frontier.topTaxonomyGroupIds),
      pressureAreas: normalizeStringArray(frontier.pressureAreas),
      taxonomyOverview: normalizeString(frontier.taxonomyOverview, base.frontier.taxonomyOverview),
      frontierSummary: normalizeString(frontier.frontierSummary, base.frontier.frontierSummary),
      rankingMethod: normalizeString(frontier.rankingMethod, base.frontier.rankingMethod)
    },
    summary: {
      ...base.summary,
      ...summary,
      recommendationCount: normalizeNumber(summary.recommendationCount, base.summary.recommendationCount),
      criticalCount: normalizeNumber(summary.criticalCount, base.summary.criticalCount),
      clusterCount: normalizeNumber(summary.clusterCount, base.summary.clusterCount),
      frontierScore: normalizeNumber(summary.frontierScore, base.summary.frontierScore),
      categories: normalizeObject(summary.categories),
      signalTypes: normalizeStringArray(summary.signalTypes),
      topClusterIds: normalizeStringArray(summary.topClusterIds),
      topRecommendationIds: normalizeStringArray(summary.topRecommendationIds),
      topTaxonomyFamilyIds: normalizeStringArray(summary.topTaxonomyFamilyIds),
      topTaxonomyGroupIds: normalizeStringArray(summary.topTaxonomyGroupIds),
      pressureAreas: normalizeStringArray(summary.pressureAreas),
      taxonomyOverview: normalizeString(summary.taxonomyOverview, base.summary.taxonomyOverview),
      clusterMembership: normalizeStringArrayRecord(summary.clusterMembership),
      topClusters: normalizeObjectArray(summary.topClusters)
    },
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function createMetaOptimizerState() {
  return {
    version: 5,
    proposalOnly: true,
    sourceArtifacts: [
      ARTIFACT_PATHS.sessionJournal,
      ARTIFACT_PATHS.reviewConcerns,
      ARTIFACT_PATHS.adversarialReviewState,
      ARTIFACT_PATHS.experimentAudits,
      ARTIFACT_PATHS.claimBridgeLog,
      ARTIFACT_PATHS.figureQa,
      ARTIFACT_PATHS.versionComparisons,
      ARTIFACT_PATHS.metaLongHorizonMemory,
      ARTIFACT_PATHS.metaOperatorLessons,
      ARTIFACT_PATHS.metaExecutionBridgeCandidates,
      ARTIFACT_PATHS.metaOperatorFollowThrough,
      ARTIFACT_PATHS.metaOperatorFollowThroughTransitions,
      ARTIFACT_PATHS.metaOperatorPlaybooks,
      ARTIFACT_PATHS.metaRemediationPacks,
      ARTIFACT_PATHS.orchestrationBoard,
      ARTIFACT_PATHS.workspaceIndex
    ],
    frontier: {
      recommendationCount: 0,
      criticalCount: 0,
      clusterCount: 0,
      frontierScore: 0,
      activeSignalTypes: [],
      topClusterIds: [],
      topRecommendationIds: [],
      topClusters: [],
      topTaxonomyFamilyIds: [],
      topTaxonomyGroupIds: [],
      pressureAreas: [],
      taxonomyOverview: "No typed wiki taxonomy pressure is currently active in the optimizer frontier.",
      frontierSummary: "No proposal-only optimizer recommendations have been generated yet.",
      rankingMethod: "durable-signal-frontier-v1",
      tieBreakOrder: ["score-desc", "priority-rank", "cluster-rank", "cluster-id", "category", "id"],
      reportPath: ARTIFACT_PATHS.metaOptimizerReport,
      recommendationsPath: ARTIFACT_PATHS.metaRecommendations,
      statePath: ARTIFACT_PATHS.metaOptimizerState,
      longHorizonPath: ARTIFACT_PATHS.metaLongHorizonMemory,
      remediationPacksPath: ARTIFACT_PATHS.metaRemediationPacks
    },
    clusters: [],
    remediationPacks: {
      packCount: 0,
      topPackIds: [],
      topClusterIds: [],
      actionableCount: 0,
      partiallyActionableCount: 0,
      advisoryCount: 0,
      readinessOverview: "No proposal-only remediation packs have been generated yet.",
      overview: "No proposal-only remediation packs have been generated yet.",
      packsPath: ARTIFACT_PATHS.metaRemediationPacks
    },
    followThrough: {
      itemCount: 0,
      acknowledgedCount: 0,
      acceptedForExecutionCount: 0,
      executingCount: 0,
      overdueExecutionCount: 0,
      criticalOverdueExecutionCount: 0,
      deferredCount: 0,
      acceptedRiskCount: 0,
      closedCount: 0,
      supersededCount: 0,
      invalidStatusCount: 0,
      staleCount: 0,
      dueDeferredCount: 0,
      topSourceIds: [],
      overview: "No operator follow-through decisions have been recorded yet.",
      followThroughPath: ARTIFACT_PATHS.metaOperatorFollowThrough
    },
    operatorLessons: summarizeOperatorLessons([]),
    executionBridgeCandidates: {
      candidateCount: 0,
      topCandidateIds: [],
      overview: "No proposal-only execution bridge candidates have been generated yet.",
      candidatesPath: ARTIFACT_PATHS.metaExecutionBridgeCandidates
    },
    governanceCoverage: {
      guardedCount: 0,
      exemptCount: 0,
      overview: "No governance coverage matrix has been summarized yet.",
      coveragePath: ARTIFACT_PATHS.metaGovernanceCoverage
    },
    operatorPlaybooks: {
      playbookCount: 0,
      topPlaybookIds: [],
      topTaxonomyFamilyIds: [],
      actionableCount: 0,
      partiallyActionableCount: 0,
      advisoryCount: 0,
      readinessOverview: "No proposal-only family-level operator playbooks have been generated yet.",
      overview: "No proposal-only family-level operator playbooks have been generated yet.",
      playbooksPath: ARTIFACT_PATHS.metaOperatorPlaybooks
    },
    longHorizon: {
      familyCount: 0,
      recurringFamilyCount: 0,
      risingFamilyCount: 0,
      stableFamilyCount: 0,
      coolingFamilyCount: 0,
      snapshotCount: 0,
      lastObservedAt: null,
      lastAction: "unchanged",
      topFamilyIds: [],
      topTaxonomyFamilyIds: [],
      topTaxonomyGroupIds: [],
      pressureAreas: [],
      overview: "No long-horizon workflow memory has been summarized yet.",
      memoryPath: ARTIFACT_PATHS.metaLongHorizonMemory
    },
    lastRefreshedAt: null,
    updatedAt: null
  };
}
function normalizeMetaOptimizerState(raw = {}) {
  const base = createMetaOptimizerState();
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const frontier = normalizeObject(raw.frontier);
  const longHorizon = normalizeObject(raw.longHorizon);
  const operatorLessons = normalizeObject(raw.operatorLessons);
  return {
    ...base,
    ...raw,
    version: base.version,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    sourceArtifacts: normalizeStringArray(raw.sourceArtifacts, base.sourceArtifacts),
    frontier: {
      ...base.frontier,
      ...frontier,
      recommendationCount: normalizeNumber(frontier.recommendationCount, base.frontier.recommendationCount),
      criticalCount: normalizeNumber(frontier.criticalCount, base.frontier.criticalCount),
      clusterCount: normalizeNumber(frontier.clusterCount, base.frontier.clusterCount),
      frontierScore: normalizeNumber(frontier.frontierScore, base.frontier.frontierScore),
      activeSignalTypes: normalizeStringArray(frontier.activeSignalTypes),
      topClusterIds: normalizeStringArray(frontier.topClusterIds),
      topRecommendationIds: normalizeStringArray(frontier.topRecommendationIds),
      topClusters: normalizeObjectArray(frontier.topClusters),
      topTaxonomyFamilyIds: normalizeStringArray(frontier.topTaxonomyFamilyIds),
      topTaxonomyGroupIds: normalizeStringArray(frontier.topTaxonomyGroupIds),
      pressureAreas: normalizeStringArray(frontier.pressureAreas),
      taxonomyOverview: normalizeString(frontier.taxonomyOverview, base.frontier.taxonomyOverview),
      frontierSummary: normalizeString(frontier.frontierSummary, base.frontier.frontierSummary),
      rankingMethod: normalizeString(frontier.rankingMethod, base.frontier.rankingMethod),
      tieBreakOrder: normalizeStringArray(frontier.tieBreakOrder, base.frontier.tieBreakOrder),
      reportPath: normalizeString(frontier.reportPath, base.frontier.reportPath),
      recommendationsPath: normalizeString(frontier.recommendationsPath, base.frontier.recommendationsPath),
      statePath: normalizeString(frontier.statePath, base.frontier.statePath),
      longHorizonPath: normalizeString(frontier.longHorizonPath, base.frontier.longHorizonPath),
      remediationPacksPath: normalizeString(frontier.remediationPacksPath, base.frontier.remediationPacksPath)
    },
    clusters: normalizeObjectArray(raw.clusters),
    operatorPlaybooks: {
      ...base.operatorPlaybooks,
      ...normalizeObject(raw.operatorPlaybooks),
      playbookCount: normalizeNumber(raw.operatorPlaybooks?.playbookCount, base.operatorPlaybooks.playbookCount),
      topPlaybookIds: normalizeStringArray(raw.operatorPlaybooks?.topPlaybookIds),
      topTaxonomyFamilyIds: normalizeStringArray(raw.operatorPlaybooks?.topTaxonomyFamilyIds),
      actionableCount: normalizeNumber(raw.operatorPlaybooks?.actionableCount, base.operatorPlaybooks.actionableCount),
      partiallyActionableCount: normalizeNumber(raw.operatorPlaybooks?.partiallyActionableCount, base.operatorPlaybooks.partiallyActionableCount),
      advisoryCount: normalizeNumber(raw.operatorPlaybooks?.advisoryCount, base.operatorPlaybooks.advisoryCount),
      readinessOverview: normalizeString(raw.operatorPlaybooks?.readinessOverview, base.operatorPlaybooks.readinessOverview),
      overview: normalizeString(raw.operatorPlaybooks?.overview, base.operatorPlaybooks.overview),
      playbooksPath: normalizeString(raw.operatorPlaybooks?.playbooksPath, base.operatorPlaybooks.playbooksPath)
    },
    executionBridgeCandidates: {
      ...base.executionBridgeCandidates,
      ...normalizeObject(raw.executionBridgeCandidates),
      candidateCount: normalizeNumber(raw.executionBridgeCandidates?.candidateCount, base.executionBridgeCandidates.candidateCount),
      topCandidateIds: normalizeStringArray(raw.executionBridgeCandidates?.topCandidateIds),
      overview: normalizeString(raw.executionBridgeCandidates?.overview, base.executionBridgeCandidates.overview),
      candidatesPath: normalizeString(raw.executionBridgeCandidates?.candidatesPath, base.executionBridgeCandidates.candidatesPath)
    },
    governanceCoverage: {
      ...base.governanceCoverage,
      ...normalizeObject(raw.governanceCoverage),
      guardedCount: normalizeNumber(raw.governanceCoverage?.guardedCount, base.governanceCoverage.guardedCount),
      exemptCount: normalizeNumber(raw.governanceCoverage?.exemptCount, base.governanceCoverage.exemptCount),
      overview: normalizeString(raw.governanceCoverage?.overview, base.governanceCoverage.overview),
      coveragePath: normalizeString(raw.governanceCoverage?.coveragePath, base.governanceCoverage.coveragePath)
    },
    remediationPacks: {
      ...base.remediationPacks,
      ...normalizeObject(raw.remediationPacks),
      packCount: normalizeNumber(raw.remediationPacks?.packCount, base.remediationPacks.packCount),
      topPackIds: normalizeStringArray(raw.remediationPacks?.topPackIds),
      topClusterIds: normalizeStringArray(raw.remediationPacks?.topClusterIds),
      actionableCount: normalizeNumber(raw.remediationPacks?.actionableCount, base.remediationPacks.actionableCount),
      partiallyActionableCount: normalizeNumber(raw.remediationPacks?.partiallyActionableCount, base.remediationPacks.partiallyActionableCount),
      advisoryCount: normalizeNumber(raw.remediationPacks?.advisoryCount, base.remediationPacks.advisoryCount),
      readinessOverview: normalizeString(raw.remediationPacks?.readinessOverview, base.remediationPacks.readinessOverview),
      overview: normalizeString(raw.remediationPacks?.overview, base.remediationPacks.overview),
      packsPath: normalizeString(raw.remediationPacks?.packsPath, base.remediationPacks.packsPath)
    },
    followThrough: {
      ...base.followThrough,
      ...normalizeObject(raw.followThrough),
      itemCount: normalizeNumber(raw.followThrough?.itemCount, base.followThrough.itemCount),
      acknowledgedCount: normalizeNumber(raw.followThrough?.acknowledgedCount, base.followThrough.acknowledgedCount),
      acceptedForExecutionCount: normalizeNumber(raw.followThrough?.acceptedForExecutionCount, base.followThrough.acceptedForExecutionCount),
      executingCount: normalizeNumber(raw.followThrough?.executingCount, base.followThrough.executingCount),
      overdueExecutionCount: normalizeNumber(raw.followThrough?.overdueExecutionCount, base.followThrough.overdueExecutionCount),
      criticalOverdueExecutionCount: normalizeNumber(raw.followThrough?.criticalOverdueExecutionCount, base.followThrough.criticalOverdueExecutionCount),
      deferredCount: normalizeNumber(raw.followThrough?.deferredCount, base.followThrough.deferredCount),
      acceptedRiskCount: normalizeNumber(raw.followThrough?.acceptedRiskCount, base.followThrough.acceptedRiskCount),
      closedCount: normalizeNumber(raw.followThrough?.closedCount, base.followThrough.closedCount),
      supersededCount: normalizeNumber(raw.followThrough?.supersededCount, base.followThrough.supersededCount),
      invalidStatusCount: normalizeNumber(raw.followThrough?.invalidStatusCount, base.followThrough.invalidStatusCount),
      staleCount: normalizeNumber(raw.followThrough?.staleCount, base.followThrough.staleCount),
      dueDeferredCount: normalizeNumber(raw.followThrough?.dueDeferredCount, base.followThrough.dueDeferredCount),
      topSourceIds: normalizeStringArray(raw.followThrough?.topSourceIds),
      overview: normalizeString(raw.followThrough?.overview, base.followThrough.overview),
      followThroughPath: normalizeString(raw.followThrough?.followThroughPath, base.followThrough.followThroughPath)
    },
    operatorLessons: {
      ...base.operatorLessons,
      ...operatorLessons,
      lessonCount: normalizeNumber(operatorLessons.lessonCount, base.operatorLessons.lessonCount),
      activeLessonCount: normalizeNumber(operatorLessons.activeLessonCount, base.operatorLessons.activeLessonCount),
      topLessonIds: normalizeStringArray(operatorLessons.topLessonIds),
      topTags: normalizeStringArray(operatorLessons.topTags),
      topDomains: normalizeStringArray(operatorLessons.topDomains),
      overview: normalizeString(operatorLessons.overview, base.operatorLessons.overview),
      lessonsPath: normalizeString(operatorLessons.lessonsPath, base.operatorLessons.lessonsPath)
    },
    longHorizon: {
      ...base.longHorizon,
      ...longHorizon,
      familyCount: normalizeNumber(longHorizon.familyCount, base.longHorizon.familyCount),
      recurringFamilyCount: normalizeNumber(longHorizon.recurringFamilyCount, base.longHorizon.recurringFamilyCount),
      risingFamilyCount: normalizeNumber(longHorizon.risingFamilyCount, base.longHorizon.risingFamilyCount),
      stableFamilyCount: normalizeNumber(longHorizon.stableFamilyCount, base.longHorizon.stableFamilyCount),
      coolingFamilyCount: normalizeNumber(longHorizon.coolingFamilyCount, base.longHorizon.coolingFamilyCount),
      snapshotCount: normalizeNumber(longHorizon.snapshotCount, base.longHorizon.snapshotCount),
      lastObservedAt: longHorizon.lastObservedAt ?? base.longHorizon.lastObservedAt,
      lastAction: normalizeString(longHorizon.lastAction, base.longHorizon.lastAction),
      topFamilyIds: normalizeStringArray(longHorizon.topFamilyIds),
      topTaxonomyFamilyIds: normalizeStringArray(longHorizon.topTaxonomyFamilyIds),
      topTaxonomyGroupIds: normalizeStringArray(longHorizon.topTaxonomyGroupIds),
      pressureAreas: normalizeStringArray(longHorizon.pressureAreas),
      overview: normalizeString(longHorizon.overview, base.longHorizon.overview),
      memoryPath: normalizeString(longHorizon.memoryPath, base.longHorizon.memoryPath)
    },
    lastRefreshedAt: raw.lastRefreshedAt ?? base.lastRefreshedAt,
    updatedAt: raw.updatedAt ?? base.updatedAt
  };
}
function normalizeWorkspaceAutonomyLoops(raw = {}, fallback = null) {
  const base = fallback ?? createWorkspaceIndex().autonomyLoops;
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  return {
    ...base,
    ...raw,
    contractVersion: normalizeString(raw.contractVersion, base.contractVersion),
    activeLifecycleState: normalizeString(raw.activeLifecycleState, base.activeLifecycleState),
    loopCount: normalizeNumber(raw.loopCount, base.loopCount),
    blockedCount: normalizeNumber(raw.blockedCount, base.blockedCount),
    readyCount: normalizeNumber(raw.readyCount, base.readyCount),
    closedCount: normalizeNumber(raw.closedCount, base.closedCount),
    currentLoopId: normalizeString(raw.currentLoopId, base.currentLoopId),
    nextSafeAction: normalizeString(raw.nextSafeAction, base.nextSafeAction),
    explicitOnly: normalizeBoolean(raw.explicitOnly, base.explicitOnly),
    noHiddenRuntime: normalizeBoolean(raw.noHiddenRuntime, base.noHiddenRuntime),
    families: normalizeStringArray(raw.families),
    loops: normalizeObjectArray(raw.loops),
    approvalPointers: normalizeStringArray(raw.approvalPointers),
    runtimePointers: normalizeStringArray(raw.runtimePointers),
    followThroughPointers: normalizeStringArray(raw.followThroughPointers),
    blockerIds: normalizeStringArray(raw.blockerIds),
    closureStates: normalizeStringArray(raw.closureStates),
    lifecycleStates: normalizeStringArray(raw.lifecycleStates),
    safeExecutionPath: normalizeString(raw.safeExecutionPath, base.safeExecutionPath),
    overview: normalizeString(raw.overview, base.overview)
  };
}
function normalizeWorkspaceMetaOptimize(raw = {}, fallback = null) {
  const base = fallback ?? createWorkspaceIndex().metaOptimize;
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  const longHorizon = normalizeObject(raw.longHorizon);
  const remediationPacks = normalizeObject(raw.remediationPacks);
  const operatorPlaybooks = normalizeObject(raw.operatorPlaybooks);
  const executionBridgeCandidates = normalizeObject(raw.executionBridgeCandidates);
  const governanceCoverage = normalizeObject(raw.governanceCoverage);
  const followThrough = normalizeObject(raw.followThrough);
  const operatorLessons = normalizeObject(raw.operatorLessons);
  const autonomyLoops = normalizeObject(raw.autonomyLoops);
  const baseGovernanceCoverage = normalizeObject(base.governanceCoverage, {
    guardedCount: 0,
    exemptCount: 0,
    overview: "No governance coverage matrix has been summarized yet.",
    coveragePath: ARTIFACT_PATHS.metaGovernanceCoverage
  });
  return {
    ...base,
    ...raw,
    proposalOnly: normalizeBoolean(raw.proposalOnly, base.proposalOnly),
    recommendationCount: normalizeNumber(raw.recommendationCount, base.recommendationCount),
    criticalCount: normalizeNumber(raw.criticalCount, base.criticalCount),
    clusterCount: normalizeNumber(raw.clusterCount, base.clusterCount),
    frontierScore: normalizeNumber(raw.frontierScore, base.frontierScore),
    activeSignalTypes: normalizeStringArray(raw.activeSignalTypes),
    topClusterIds: normalizeStringArray(raw.topClusterIds),
    topRecommendationIds: normalizeStringArray(raw.topRecommendationIds),
    topClusters: normalizeObjectArray(raw.topClusters),
    topTaxonomyFamilyIds: normalizeStringArray(raw.topTaxonomyFamilyIds),
    topTaxonomyGroupIds: normalizeStringArray(raw.topTaxonomyGroupIds),
    pressureAreas: normalizeStringArray(raw.pressureAreas),
    taxonomyOverview: normalizeString(raw.taxonomyOverview, base.taxonomyOverview),
    frontierSummary: normalizeString(raw.frontierSummary, base.frontierSummary),
    rankingMethod: normalizeString(raw.rankingMethod, base.rankingMethod),
    tieBreakOrder: normalizeStringArray(raw.tieBreakOrder, base.tieBreakOrder),
    reportPath: normalizeString(raw.reportPath, base.reportPath),
    recommendationsPath: normalizeString(raw.recommendationsPath, base.recommendationsPath),
    statePath: normalizeString(raw.statePath, base.statePath),
    longHorizonPath: normalizeString(raw.longHorizonPath, base.longHorizonPath),
    remediationPacks: {
      ...base.remediationPacks,
      ...remediationPacks,
      packCount: normalizeNumber(remediationPacks.packCount, base.remediationPacks.packCount),
      topPackIds: normalizeStringArray(remediationPacks.topPackIds),
      topClusterIds: normalizeStringArray(remediationPacks.topClusterIds),
      actionableCount: normalizeNumber(remediationPacks.actionableCount, base.remediationPacks.actionableCount),
      partiallyActionableCount: normalizeNumber(remediationPacks.partiallyActionableCount, base.remediationPacks.partiallyActionableCount),
      advisoryCount: normalizeNumber(remediationPacks.advisoryCount, base.remediationPacks.advisoryCount),
      readinessOverview: normalizeString(remediationPacks.readinessOverview, base.remediationPacks.readinessOverview),
      overview: normalizeString(remediationPacks.overview, base.remediationPacks.overview),
      packsPath: normalizeString(remediationPacks.packsPath, base.remediationPacks.packsPath)
    },
    executionBridgeCandidates: {
      ...base.executionBridgeCandidates,
      ...executionBridgeCandidates,
      candidateCount: normalizeNumber(executionBridgeCandidates.candidateCount, base.executionBridgeCandidates.candidateCount),
      topCandidateIds: normalizeStringArray(executionBridgeCandidates.topCandidateIds),
      overview: normalizeString(executionBridgeCandidates.overview, base.executionBridgeCandidates.overview),
      candidatesPath: normalizeString(executionBridgeCandidates.candidatesPath, base.executionBridgeCandidates.candidatesPath)
    },
    governanceCoverage: {
      ...baseGovernanceCoverage,
      ...governanceCoverage,
      guardedCount: normalizeNumber(governanceCoverage.guardedCount, baseGovernanceCoverage.guardedCount),
      exemptCount: normalizeNumber(governanceCoverage.exemptCount, baseGovernanceCoverage.exemptCount),
      overview: normalizeString(governanceCoverage.overview, baseGovernanceCoverage.overview),
      coveragePath: normalizeString(governanceCoverage.coveragePath, baseGovernanceCoverage.coveragePath)
    },
    followThrough: {
      ...base.followThrough,
      ...followThrough,
      itemCount: normalizeNumber(followThrough.itemCount, base.followThrough.itemCount),
      acknowledgedCount: normalizeNumber(followThrough.acknowledgedCount, base.followThrough.acknowledgedCount),
      acceptedForExecutionCount: normalizeNumber(followThrough.acceptedForExecutionCount, base.followThrough.acceptedForExecutionCount),
      deferredCount: normalizeNumber(followThrough.deferredCount, base.followThrough.deferredCount),
      acceptedRiskCount: normalizeNumber(followThrough.acceptedRiskCount, base.followThrough.acceptedRiskCount),
      closedCount: normalizeNumber(followThrough.closedCount, base.followThrough.closedCount),
      supersededCount: normalizeNumber(followThrough.supersededCount, base.followThrough.supersededCount),
      staleCount: normalizeNumber(followThrough.staleCount, base.followThrough.staleCount),
      dueDeferredCount: normalizeNumber(followThrough.dueDeferredCount, base.followThrough.dueDeferredCount),
      topSourceIds: normalizeStringArray(followThrough.topSourceIds),
      overview: normalizeString(followThrough.overview, base.followThrough.overview),
      followThroughPath: normalizeString(followThrough.followThroughPath, base.followThrough.followThroughPath)
    },
    operatorLessons: {
      ...base.operatorLessons,
      ...operatorLessons,
      lessonCount: normalizeNumber(operatorLessons.lessonCount, base.operatorLessons.lessonCount),
      activeLessonCount: normalizeNumber(operatorLessons.activeLessonCount, base.operatorLessons.activeLessonCount),
      topLessonIds: normalizeStringArray(operatorLessons.topLessonIds),
      topTags: normalizeStringArray(operatorLessons.topTags),
      topDomains: normalizeStringArray(operatorLessons.topDomains),
      overview: normalizeString(operatorLessons.overview, base.operatorLessons.overview),
      lessonsPath: normalizeString(operatorLessons.lessonsPath, base.operatorLessons.lessonsPath)
    },
    operatorPlaybooks: {
      ...base.operatorPlaybooks,
      ...operatorPlaybooks,
      playbookCount: normalizeNumber(operatorPlaybooks.playbookCount, base.operatorPlaybooks.playbookCount),
      topPlaybookIds: normalizeStringArray(operatorPlaybooks.topPlaybookIds),
      topTaxonomyFamilyIds: normalizeStringArray(operatorPlaybooks.topTaxonomyFamilyIds),
      actionableCount: normalizeNumber(operatorPlaybooks.actionableCount, base.operatorPlaybooks.actionableCount),
      partiallyActionableCount: normalizeNumber(operatorPlaybooks.partiallyActionableCount, base.operatorPlaybooks.partiallyActionableCount),
      advisoryCount: normalizeNumber(operatorPlaybooks.advisoryCount, base.operatorPlaybooks.advisoryCount),
      readinessOverview: normalizeString(operatorPlaybooks.readinessOverview, base.operatorPlaybooks.readinessOverview),
      overview: normalizeString(operatorPlaybooks.overview, base.operatorPlaybooks.overview),
      playbooksPath: normalizeString(operatorPlaybooks.playbooksPath, base.operatorPlaybooks.playbooksPath)
    },
    autonomyLoops: normalizeWorkspaceAutonomyLoops(autonomyLoops, base.autonomyLoops),
    longHorizon: {
      ...base.longHorizon,
      ...longHorizon,
      familyCount: normalizeNumber(longHorizon.familyCount, base.longHorizon.familyCount),
      recurringFamilyCount: normalizeNumber(longHorizon.recurringFamilyCount, base.longHorizon.recurringFamilyCount),
      risingFamilyCount: normalizeNumber(longHorizon.risingFamilyCount, base.longHorizon.risingFamilyCount),
      stableFamilyCount: normalizeNumber(longHorizon.stableFamilyCount, base.longHorizon.stableFamilyCount),
      coolingFamilyCount: normalizeNumber(longHorizon.coolingFamilyCount, base.longHorizon.coolingFamilyCount),
      snapshotCount: normalizeNumber(longHorizon.snapshotCount, base.longHorizon.snapshotCount),
      lastObservedAt: longHorizon.lastObservedAt ?? base.longHorizon.lastObservedAt,
      lastAction: normalizeString(longHorizon.lastAction, base.longHorizon.lastAction),
      topFamilyIds: normalizeStringArray(longHorizon.topFamilyIds),
      topTaxonomyFamilyIds: normalizeStringArray(longHorizon.topTaxonomyFamilyIds),
      topTaxonomyGroupIds: normalizeStringArray(longHorizon.topTaxonomyGroupIds),
      pressureAreas: normalizeStringArray(longHorizon.pressureAreas),
      overview: normalizeString(longHorizon.overview, base.longHorizon.overview),
      memoryPath: normalizeString(longHorizon.memoryPath, base.longHorizon.memoryPath)
    }
  };
}
function createRebuttalIssuesIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createVersionsIndex() {
  return {
    version: 1,
    currentVersionId: null,
    items: [],
    lineage: [],
    updatedAt: null
  };
}
function createVersionComparisonsIndex() {
  return { version: 1, items: [], activeTargets: [], updatedAt: null };
}
function createWikiEntitiesIndex() {
  return { version: 1, items: [], updatedAt: null };
}
function createWikiRelationsIndex() {
  return {
    version: 3,
    items: [],
    summary: {
      totalRelations: 0,
      healthyCount: 0,
      degradedCount: 0,
      relationTypeCounts: {},
      integrityReasonCounts: {},
      repairFrontier: [],
      taxonomyRepairFrontier: [],
      taxonomy: {
        familyCount: 0,
        groupCount: 0,
        degradedFamilyCount: 0,
        degradedGroupCount: 0,
        familyCounts: {},
        groupCounts: {},
        topDegradedFamilyIds: [],
        topDegradedGroupIds: [],
        families: [],
        groups: [],
        overview: "No typed wiki relation taxonomy has been summarized yet."
      }
    },
    updatedAt: null
  };
}
function createWorkspaceIndex() {
  return {
    version: 9,
    managed: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.workspaceIndex),
    boardPhase: "init",
    boardAssignedRole: "planner",
    boardIntentType: "plan",
    currentFocus: "\u5BF9\u9F50\u6301\u4E45\u5DE5\u4F5C\u6D41\u72B6\u6001\u3002",
    nextAction: "\u5237\u65B0\u770B\u677F\u5E76\u9009\u62E9\u4E0B\u4E00\u4E2A\u89D2\u8272\u8D1F\u8D23\u7684\u6B65\u9AA4\u3002",
    continuationState: createContinuationState(),
    activePackets: [],
    workQueues: {
      ready: [],
      waiting: [],
      reviewNeeded: [],
      handoff: [],
      stale: [],
      archived: []
    },
    ownershipSummary: [],
    packetLifecycleCounts: {},
    handoffObligations: [],
    resumeGuidance: {
      command: "project:dove.status",
      summary: "\u5237\u65B0\u770B\u677F\u5E76\u9009\u62E9\u4E0B\u4E00\u4E2A\u89D2\u8272\u8D1F\u8D23\u7684\u6B65\u9AA4\u3002",
      prioritizedPacketIds: [],
      packetContextPaths: [],
      handoffCandidateIds: []
    },
    contextSurfaces: {
      currentRoleContextPath: `${ARTIFACT_PATHS.roleContextsDir}/planner.json`,
      currentPhaseContextPath: `${ARTIFACT_PATHS.phaseContextsDir}/init.json`,
      currentActionContextPath: `${ARTIFACT_PATHS.actionContextsDir}/current.json`,
      prioritizedArtifactContextPaths: [],
      prioritizedPacketActionContextPaths: []
    },
    behaviorDiscipline: {
      summary: "Read the closest role, phase, packet, and artifact context before mutating durable workflow state.",
      explicitOnly: true,
      noHiddenRuntime: true,
      requiredReadOrder: []
    },
    dependencyHealth: {
      blockedPacketIds: [],
      healthyPacketIds: [],
      waitingPacketIds: [],
      stalePacketIds: [],
      missingDependencyIds: [],
      orphanPacketIds: []
    },
    repairFrontier: {
      count: 0,
      relationIssueCount: 0,
      relationFamilyIssueCount: 0,
      managedArtifactIssueCount: 0,
      governanceIssueCount: 0,
      topDegradedFamilyIds: [],
      topDegradedGroupIds: [],
      taxonomyOverview: "No degraded typed wiki relation families are currently summarized.",
      relationFamilySummaries: [],
      relationGroupSummaries: [],
      prioritizedItems: []
    },
    metaOptimize: {
      proposalOnly: true,
      recommendationCount: 0,
      criticalCount: 0,
      clusterCount: 0,
      frontierScore: 0,
      activeSignalTypes: [],
      topClusterIds: [],
      topRecommendationIds: [],
      topClusters: [],
      topTaxonomyFamilyIds: [],
      topTaxonomyGroupIds: [],
      pressureAreas: [],
      taxonomyOverview: "No typed wiki taxonomy pressure is currently active in the optimizer frontier.",
      frontierSummary: "No proposal-only optimizer recommendations have been generated yet.",
      rankingMethod: "durable-signal-frontier-v1",
      tieBreakOrder: ["score-desc", "priority-rank", "cluster-rank", "cluster-id", "category", "id"],
      reportPath: ARTIFACT_PATHS.metaOptimizerReport,
      recommendationsPath: ARTIFACT_PATHS.metaRecommendations,
      statePath: ARTIFACT_PATHS.metaOptimizerState,
      longHorizonPath: ARTIFACT_PATHS.metaLongHorizonMemory,
      remediationPacks: {
        packCount: 0,
        topPackIds: [],
        topClusterIds: [],
        actionableCount: 0,
        partiallyActionableCount: 0,
        advisoryCount: 0,
        readinessOverview: "No proposal-only remediation packs have been generated yet.",
        overview: "No proposal-only remediation packs have been generated yet.",
        packsPath: ARTIFACT_PATHS.metaRemediationPacks
      },
      governanceCoverage: {
        guardedCount: 0,
        exemptCount: 0,
        overview: "No governance coverage matrix has been summarized yet.",
        coveragePath: ARTIFACT_PATHS.metaGovernanceCoverage
      },
      followThrough: {
        itemCount: 0,
        acknowledgedCount: 0,
        acceptedForExecutionCount: 0,
        deferredCount: 0,
        acceptedRiskCount: 0,
        closedCount: 0,
        supersededCount: 0,
        staleCount: 0,
        dueDeferredCount: 0,
        topSourceIds: [],
        overview: "No operator follow-through decisions have been recorded yet.",
        followThroughPath: ARTIFACT_PATHS.metaOperatorFollowThrough
      },
      operatorLessons: summarizeOperatorLessons([]),
      executionBridgeCandidates: {
        candidateCount: 0,
        topCandidateIds: [],
        overview: "No proposal-only execution bridge candidates have been generated yet.",
        candidatesPath: ARTIFACT_PATHS.metaExecutionBridgeCandidates
      },
      operatorPlaybooks: {
        playbookCount: 0,
        topPlaybookIds: [],
        topTaxonomyFamilyIds: [],
        actionableCount: 0,
        partiallyActionableCount: 0,
        advisoryCount: 0,
        readinessOverview: "No proposal-only family-level operator playbooks have been generated yet.",
        overview: "No proposal-only family-level operator playbooks have been generated yet.",
        playbooksPath: ARTIFACT_PATHS.metaOperatorPlaybooks
      },
      longHorizon: {
        familyCount: 0,
        recurringFamilyCount: 0,
        risingFamilyCount: 0,
        stableFamilyCount: 0,
        coolingFamilyCount: 0,
        snapshotCount: 0,
        lastObservedAt: null,
        lastAction: "unchanged",
        topFamilyIds: [],
        topTaxonomyFamilyIds: [],
        topTaxonomyGroupIds: [],
        pressureAreas: [],
        overview: "No long-horizon workflow memory has been summarized yet.",
        memoryPath: ARTIFACT_PATHS.metaLongHorizonMemory
      }
    },
    runtime: {
      explicitInvocationOnly: true,
      noDaemon: true,
      selectionPolicy: "planner-materialized-guidance-v2",
      boundedStepPolicy: "planner-control-plane-worker-step-v1",
      controllerStatePath: ARTIFACT_PATHS.runtimeControllerState,
      leasesPath: ARTIFACT_PATHS.runtimeLeases,
      eventsPath: ARTIFACT_PATHS.runtimeEvents,
      resultsPath: ARTIFACT_PATHS.runtimeResults,
      lastRunId: null,
      lastStatus: "never-run",
      lastOutcome: "not-started",
      lastSelectedPacketId: null,
      lastEnvelopeWorkerRole: null,
      lastProgramId: null,
      lastProgramRunId: null,
      lastApprovalId: null,
      lastProgramOutcome: "not-started",
      requestCount: 0,
      acceptedRequestCount: 0,
      executingRequestCount: 0,
      staleRequestCount: 0,
      overdueExecutionCount: 0,
      dueReviewCount: 0,
      checkpointCount: 0,
      escalationCount: 0,
      lastCheckpointPacketId: null,
      lastCheckpointSummary: null,
      lastCheckpointAt: null,
      lastEscalationPacketId: null,
      lastEscalationFollowThroughId: null,
      lastEscalationAt: null,
      continuationCount: 0,
      currentContinuationKind: null,
      currentContinuationPacketId: null,
      currentContinuationProgramRunId: null,
      currentContinuationCommand: null,
      activeLeaseCount: 0,
      activeLeasePacketIds: [],
      eventCount: 0,
      resultCount: 0,
      lastEventType: null,
      overview: "No autonomous control-plane run has been executed yet."
    },
    programs: {
      programCount: 0,
      activeCount: 0,
      blockedCount: 0,
      approvedRunCount: 0,
      reviewNeededRunCount: 0,
      reviewCheckpointRunCount: 0,
      consumedApprovalCount: 0,
      topProgramIds: [],
      topRunIds: [],
      topApprovalIds: [],
      currentProgramId: null,
      currentProgramRunId: null,
      currentApprovalId: null,
      currentReviewCheckpointRunId: null,
      currentReviewCheckpointPacketId: null,
      currentReviewCheckpointSummary: null,
      lastProgramOutcome: "not-started",
      overview: "No program-level research operating surfaces are active yet.",
      programsPath: ARTIFACT_PATHS.programsIndex,
      runsPath: ARTIFACT_PATHS.programRuns,
      approvalsPath: ARTIFACT_PATHS.programApprovals
    },
    campaigns: {
      campaignCount: 0,
      plannedCount: 0,
      activeCount: 0,
      reviewNeededCount: 0,
      completedCount: 0,
      blockedCount: 0,
      topCampaignIds: [],
      currentCampaignId: null,
      currentCampaignStatus: null,
      currentCampaignStepCount: 0,
      currentCampaignCompletedStepCount: 0,
      currentCampaignReviewNeededStepCount: 0,
      currentCampaignNextStepId: null,
      currentCampaignNextAction: null,
      overview: "No multi-cycle research campaigns have been recorded yet.",
      campaignsPath: ARTIFACT_PATHS.campaignsIndex
    },
    autonomyLoops: {
      contractVersion: "unified-autonomy-loop-v1",
      activeLifecycleState: "board-ready",
      loopCount: 4,
      blockedCount: 0,
      readyCount: 0,
      closedCount: 0,
      currentLoopId: "board-role-artifact-handoff",
      nextSafeAction: "Refresh the board and choose the next explicit operator action.",
      explicitOnly: true,
      noHiddenRuntime: true,
      families: [],
      loops: [],
      approvalPointers: [],
      runtimePointers: [ARTIFACT_PATHS.runtimeControllerState],
      followThroughPointers: [ARTIFACT_PATHS.metaOperatorFollowThrough],
      blockerIds: [],
      closureStates: [],
      lifecycleStates: [],
      safeExecutionPath: "project:dove.mission -> project:dove.auto",
      overview: "Unified autonomy loop skeleton is explicit, file-first, and foreground-only."
    },
    lifecycle: normalizeWorkspaceLifecycle(),
    dove: createDoveWorkspaceKernel(),
    activeRoles: [],
    unresolvedConcernIds: [],
    mostRecentSessions: [],
    latestVersions: {
      currentVersionId: null,
      activeTargets: []
    },
    updatedAt: null
  };
}
function normalizeWorkspaceRuntime(raw = {}, fallback = null) {
  const base = fallback ?? createWorkspaceIndex().runtime;
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  return {
    ...base,
    ...raw,
    explicitInvocationOnly: normalizeBoolean(raw.explicitInvocationOnly, base.explicitInvocationOnly),
    noDaemon: normalizeBoolean(raw.noDaemon, base.noDaemon),
    selectionPolicy: normalizeString(raw.selectionPolicy, base.selectionPolicy),
    boundedStepPolicy: normalizeString(raw.boundedStepPolicy, base.boundedStepPolicy),
    controllerStatePath: normalizeString(raw.controllerStatePath, base.controllerStatePath),
    leasesPath: normalizeString(raw.leasesPath, base.leasesPath),
    eventsPath: normalizeString(raw.eventsPath, base.eventsPath),
    resultsPath: normalizeString(raw.resultsPath, base.resultsPath),
    lastRunId: normalizeString(raw.lastRunId, base.lastRunId),
    lastStatus: normalizeString(raw.lastStatus, base.lastStatus),
    lastOutcome: normalizeString(raw.lastOutcome, base.lastOutcome),
    lastSelectedPacketId: normalizeString(raw.lastSelectedPacketId, base.lastSelectedPacketId),
    lastEnvelopeWorkerRole: normalizeString(raw.lastEnvelopeWorkerRole, base.lastEnvelopeWorkerRole),
    lastProgramId: normalizeString(raw.lastProgramId, base.lastProgramId),
    lastProgramRunId: normalizeString(raw.lastProgramRunId, base.lastProgramRunId),
    lastApprovalId: normalizeString(raw.lastApprovalId, base.lastApprovalId),
    lastProgramOutcome: normalizeString(raw.lastProgramOutcome, base.lastProgramOutcome),
    requestCount: normalizeNumber(raw.requestCount, base.requestCount),
    acceptedRequestCount: normalizeNumber(raw.acceptedRequestCount, base.acceptedRequestCount),
    executingRequestCount: normalizeNumber(raw.executingRequestCount, base.executingRequestCount),
    staleRequestCount: normalizeNumber(raw.staleRequestCount, base.staleRequestCount),
    overdueExecutionCount: normalizeNumber(raw.overdueExecutionCount, base.overdueExecutionCount),
    dueReviewCount: normalizeNumber(raw.dueReviewCount, base.dueReviewCount),
    checkpointCount: normalizeNumber(raw.checkpointCount, base.checkpointCount),
    escalationCount: normalizeNumber(raw.escalationCount, base.escalationCount),
    continuationCount: normalizeNumber(raw.continuationCount, base.continuationCount),
    lastCheckpointPacketId: normalizeString(raw.lastCheckpointPacketId, base.lastCheckpointPacketId),
    lastCheckpointSummary: normalizeString(raw.lastCheckpointSummary, base.lastCheckpointSummary),
    lastCheckpointAt: raw.lastCheckpointAt ?? base.lastCheckpointAt,
    lastEscalationPacketId: normalizeString(raw.lastEscalationPacketId, base.lastEscalationPacketId),
    lastEscalationFollowThroughId: normalizeString(raw.lastEscalationFollowThroughId, base.lastEscalationFollowThroughId),
    lastEscalationAt: raw.lastEscalationAt ?? base.lastEscalationAt,
    currentContinuationKind: normalizeString(raw.currentContinuationKind, base.currentContinuationKind),
    currentContinuationPacketId: normalizeString(raw.currentContinuationPacketId, base.currentContinuationPacketId),
    currentContinuationProgramRunId: normalizeString(raw.currentContinuationProgramRunId, base.currentContinuationProgramRunId),
    currentContinuationCommand: normalizeString(raw.currentContinuationCommand, base.currentContinuationCommand),
    activeLeaseCount: normalizeNumber(raw.activeLeaseCount, base.activeLeaseCount),
    activeLeasePacketIds: normalizeStringArray(raw.activeLeasePacketIds),
    eventCount: normalizeNumber(raw.eventCount, base.eventCount),
    resultCount: normalizeNumber(raw.resultCount, base.resultCount),
    lastEventType: normalizeString(raw.lastEventType, base.lastEventType),
    overview: normalizeString(raw.overview, base.overview)
  };
}
function normalizeWorkspacePrograms(raw = {}, fallback = null) {
  const base = fallback ?? createWorkspaceIndex().programs;
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  return {
    ...base,
    ...raw,
    programCount: normalizeNumber(raw.programCount, base.programCount),
    activeCount: normalizeNumber(raw.activeCount, base.activeCount),
    blockedCount: normalizeNumber(raw.blockedCount, base.blockedCount),
    approvedRunCount: normalizeNumber(raw.approvedRunCount, base.approvedRunCount),
    reviewNeededRunCount: normalizeNumber(raw.reviewNeededRunCount, base.reviewNeededRunCount),
    reviewCheckpointRunCount: normalizeNumber(raw.reviewCheckpointRunCount, base.reviewCheckpointRunCount),
    consumedApprovalCount: normalizeNumber(raw.consumedApprovalCount, base.consumedApprovalCount),
    topProgramIds: normalizeStringArray(raw.topProgramIds),
    topRunIds: normalizeStringArray(raw.topRunIds),
    topApprovalIds: normalizeStringArray(raw.topApprovalIds),
    currentProgramId: normalizeString(raw.currentProgramId, base.currentProgramId),
    currentProgramRunId: normalizeString(raw.currentProgramRunId, base.currentProgramRunId),
    currentApprovalId: normalizeString(raw.currentApprovalId, base.currentApprovalId),
    currentReviewCheckpointRunId: normalizeString(raw.currentReviewCheckpointRunId, base.currentReviewCheckpointRunId),
    currentReviewCheckpointPacketId: normalizeString(raw.currentReviewCheckpointPacketId, base.currentReviewCheckpointPacketId),
    currentReviewCheckpointSummary: normalizeString(raw.currentReviewCheckpointSummary, base.currentReviewCheckpointSummary),
    lastProgramOutcome: normalizeString(raw.lastProgramOutcome, base.lastProgramOutcome),
    overview: normalizeString(raw.overview, base.overview),
    programsPath: normalizeString(raw.programsPath, base.programsPath),
    runsPath: normalizeString(raw.runsPath, base.runsPath),
    approvalsPath: normalizeString(raw.approvalsPath, base.approvalsPath)
  };
}
function normalizeWorkspaceCampaigns(raw = {}, fallback = null) {
  const base = fallback ?? createWorkspaceIndex().campaigns;
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return base;
  }
  return {
    ...base,
    ...raw,
    campaignCount: normalizeNumber(raw.campaignCount, base.campaignCount),
    plannedCount: normalizeNumber(raw.plannedCount, base.plannedCount),
    activeCount: normalizeNumber(raw.activeCount, base.activeCount),
    reviewNeededCount: normalizeNumber(raw.reviewNeededCount, base.reviewNeededCount),
    completedCount: normalizeNumber(raw.completedCount, base.completedCount),
    blockedCount: normalizeNumber(raw.blockedCount, base.blockedCount),
    topCampaignIds: normalizeStringArray(raw.topCampaignIds),
    currentCampaignId: normalizeString(raw.currentCampaignId, base.currentCampaignId),
    currentCampaignStatus: normalizeString(raw.currentCampaignStatus, base.currentCampaignStatus),
    currentCampaignStepCount: normalizeNumber(raw.currentCampaignStepCount, base.currentCampaignStepCount),
    currentCampaignCompletedStepCount: normalizeNumber(raw.currentCampaignCompletedStepCount, base.currentCampaignCompletedStepCount),
    currentCampaignReviewNeededStepCount: normalizeNumber(raw.currentCampaignReviewNeededStepCount, base.currentCampaignReviewNeededStepCount),
    currentCampaignNextStepId: normalizeString(raw.currentCampaignNextStepId, base.currentCampaignNextStepId),
    currentCampaignNextAction: normalizeString(raw.currentCampaignNextAction, base.currentCampaignNextAction),
    overview: normalizeString(raw.overview, base.overview),
    campaignsPath: normalizeString(raw.campaignsPath, base.campaignsPath)
  };
}
function createWorkflowBoundaries() {
  const doveBootstrapOnlyPaths = [
    ".dove/README.md",
    ".dove/project.md",
    ".dove/contracts/research-contract.md",
    ".dove/orchestration/board.json",
    ".dove/orchestration/handoffs.md",
    ".dove/task-packets/index.json",
    ".dove/research/brief.md",
    ".dove/research/agenda.json",
    ".dove/plans/current-plan.md",
    ".dove/outline/current-outline.md",
    ".dove/findings.md",
    ".dove/experiments/EXPERIMENT_LOG.md",
    ".dove/experiments/plans.json",
    ".dove/experiments/results.json",
    ".dove/experiments/audits.json",
    ".dove/sources/index.json",
    ".dove/notes/index.json",
    ".dove/evidence/index.json",
    ".dove/documents/ledger.json",
    ".dove/claims/CLAIMS_FROM_RESULTS.md",
    ".dove/claims/bridge-log.json",
    ".dove/reviews/log.md",
    ".dove/reviews/REVIEW_STATE.json",
    ".dove/reviews/concerns.json",
    ".dove/reviews/debate-log.md",
    ".dove/reviews/adversarial-state.json",
    ".dove/revision-plans/current-plan.md",
    ".dove/wiki/index.md",
    ".dove/wiki/query_pack.md",
    ".dove/wiki/navigation.md",
    ".dove/wiki/entities.json",
    ".dove/wiki/relations.json",
    ".dove/checklists/current.md",
    ".dove/bibliography/references.bib",
    ".dove/bibliography/citation-log.md",
    ".dove/figures/README.md",
    ".dove/figures/index.json",
    ".dove/figures/briefs.json",
    ".dove/figures/segments.json",
    ".dove/figures/templates.json",
    ".dove/figures/editable-index.json",
    ".dove/figures/final-index.json",
    ".dove/figures/materials.json",
    ".dove/figures/generations.json",
    ".dove/figures/captions.json",
    ".dove/figures/qa.json",
    ".dove/rebuttal/issues.json",
    ".dove/rebuttal/strategy.md",
    ".dove/rebuttal/response-draft.md",
    ".dove/versions/index.json",
    ".dove/versions/comparisons.json",
    ".dove/versions/LATEST_COMPARISON.md",
    ".dove/meta/events.json",
    ".dove/meta/long-horizon-memory.json",
    ".dove/meta/operator-lessons.json",
    ".dove/meta/operator-playbooks.json",
    ".dove/meta/remediation-packs.json",
    ".dove/meta/recommendations.json",
    ".dove/meta/optimizer-state.json",
    ".dove/meta/LATEST_OPTIMIZER_REPORT.md",
    ".dove/runtime/controller-state.json",
    ".dove/runtime/leases.json",
    ".dove/runtime/events.json",
    ".dove/runtime/results.json",
    ".dove/programs/index.json",
    ".dove/programs/runs.json",
    ".dove/programs/approvals.json",
    ".dove/sessions/journal.json",
    ".dove/sessions/LATEST_SUMMARY.md",
    ".dove/workspace/index.json",
    ".dove/manifest.json",
    ".dove/workflow-pack/boundaries.json"
  ];
  return {
    version: 3,
    primaryRoleIds: PRIMARY_ROLE_IDS,
    subagentRoleIds: SUBAGENT_ROLE_IDS,
    roleHierarchy: ROLE_HIERARCHY,
    neutralCorePaths: CORE_INSTALL_PATHS,
    defaultHostAdapters: DEFAULT_HOST_ADAPTERS,
    availableHostAdapters: HOST_IDS,
    managedHostAdapterPaths: MANAGED_HOST_ADAPTER_PATHS,
    managedPaths: MANAGED_PACKAGE_PATHS,
    doveBootstrapOnlyPaths,
    userOwnedPaths: [
      ".dove/drafts",
      ".dove/sources",
      ".dove/notes",
      ".dove/evidence",
      ".dove/documents",
      ".dove/experiments",
      ".dove/reviews",
      ".dove/rebuttal",
      ".dove/versions/snapshots",
      ".dove/task-packets/packets",
      ".dove/context/roles",
      ".dove/context/phases",
      ".dove/context/packets",
      ".dove/context/artifacts",
      ".dove/context/actions",
      ".dove/sessions",
      ARTIFACT_PATHS.metaOperatorLessons,
      ARTIFACT_PATHS.workspaceArtifactMap
    ],
    managedArtifacts: {
      codePack: createManagedArtifactMeta("managed-replaceable", "src"),
      workflowBoundaries: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.workflowBoundaries),
      workspaceIndex: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.workspaceIndex),
      doveRootManifest: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.doveRootManifest),
      operatorLessons: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.metaOperatorLessons),
      documentsLedger: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.documentsLedger),
      programsIndex: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.programsIndex),
      programRuns: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.programRuns),
      programApprovals: createManagedArtifactMeta("bootstrap-only", ARTIFACT_PATHS.programApprovals)
    },
    notes: [
      "Pack installs and syncs should bootstrap missing .dove artifacts but should not overwrite user-authored workspace state.",
      ".dove remains the durable source of truth and is treated as workspace data, not a managed code payload.",
      "Managed replaceable code and bootstrap-only workspace artifacts now advertise revision/template metadata for clearer reconciliation."
    ],
    updatedAt: null
  };
}

// src/core/workspace.mjs
import fs3 from "node:fs";
import path4 from "node:path";

// src/core/follow-through-authority.mjs
import crypto2 from "node:crypto";
function normalizeStringArray2(value) {
  return Array.isArray(value) ? Array.from(
    new Set(
      value.map((item) => String(item).trim()).filter(Boolean)
    )
  ).sort((left, right) => left.localeCompare(right)) : [];
}
function normalizeAuthorityItems(value, project) {
  return Array.isArray(value) ? value.map(project).sort(
    (left, right) => stableJson(left).localeCompare(stableJson(right))
  ) : [];
}
function stableJson(value) {
  if (Array.isArray(value)) {
    return `[${value.map(stableJson).join(",")}]`;
  }
  if (value && typeof value === "object") {
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${stableJson(value[key])}`).join(",")}}`;
  }
  return JSON.stringify(value);
}
function conversionPathAuthority(value) {
  return normalizeAuthorityItems(
    value,
    (item) => ({
      targetType: item.targetType ?? null,
      targetId: item.targetId ?? null,
      assignedRole: item.assignedRole ?? null,
      priority: item.priority ?? null,
      suggestedTitle: item.suggestedTitle ?? null,
      rationale: item.rationale ?? null,
      nextStep: item.nextStep ?? null
    })
  );
}
function remediationPackAuthority(source = {}) {
  return {
    id: source.id ?? null,
    clusterId: source.clusterId ?? null,
    acceptanceCriteria: normalizeStringArray2(
      source.acceptanceCriteria
    ),
    manualNextActions: normalizeStringArray2(
      source.manualNextActions
    ),
    conversionPaths: conversionPathAuthority(
      source.rankedConversionPaths ?? source.conversionHints
    ),
    reviewConcerns: normalizeAuthorityItems(
      source.reviewConcerns,
      (item) => ({
        id: item.id ?? null,
        summary: item.summary ?? null,
        status: item.status ?? null,
        severity: item.severity ?? null,
        responseOwnerRole: item.responseOwnerRole ?? null,
        linkedArtifactPaths: normalizeStringArray2(
          item.linkedArtifactPaths
        ),
        linkedAuditIds: normalizeStringArray2(
          item.linkedAuditIds
        ),
        linkedBridgeIds: normalizeStringArray2(
          item.linkedBridgeIds
        )
      })
    ),
    figureQa: normalizeAuthorityItems(
      source.figureQa,
      (item) => ({
        id: item.id ?? null,
        figureId: item.figureId ?? null,
        code: item.code ?? null,
        summary: item.summary ?? null,
        severity: item.severity ?? null,
        artifactPaths: normalizeStringArray2(
          item.artifactPaths
        )
      })
    )
  };
}
function operatorPlaybookAuthority(source = {}) {
  return {
    id: source.id ?? null,
    taxonomyFamilyId: source.taxonomyFamilyId ?? null,
    remediationPackIds: normalizeStringArray2(
      source.remediationPackIds
    ),
    acceptanceCriteria: normalizeStringArray2(
      source.acceptanceCriteria
    ),
    manualNextActions: normalizeStringArray2(
      source.manualNextActions
    ),
    conversionPaths: conversionPathAuthority(
      source.rankedConversionPaths ?? source.conversionHints
    )
  };
}
function executionBridgeAuthority(source = {}) {
  return {
    id: source.id ?? null,
    candidateType: source.candidateType ?? null,
    targetArtifact: source.targetArtifact ?? null,
    targetId: source.targetId ?? null,
    suggestedTitle: source.suggestedTitle ?? null,
    suggestedSummary: source.suggestedSummary ?? null,
    suggestedNextStep: source.suggestedNextStep ?? null,
    suggestedAcceptanceCriteria: normalizeStringArray2(
      source.suggestedAcceptanceCriteria
    ),
    sourceRemediationPackIds: normalizeStringArray2(
      source.sourceRemediationPackIds
    ),
    sourcePlaybookIds: normalizeStringArray2(
      source.sourcePlaybookIds
    ),
    sourceConversionPath: source.sourceConversionPath ? {
      targetType: source.sourceConversionPath.targetType ?? null,
      targetId: source.sourceConversionPath.targetId ?? null,
      assignedRole: source.sourceConversionPath.assignedRole ?? null
    } : null
  };
}
function followThroughAuthorityState(item = {}, operatingState = {}) {
  const authorityStatuses = /* @__PURE__ */ new Set([
    "accepted-for-execution",
    "executing",
    "accepted-risk"
  ]);
  if (!authorityStatuses.has(item.status)) {
    return { required: false, trusted: true, reason: null };
  }
  if (item.authorityProvenance !== "trusted-system-transition") {
    return {
      required: true,
      trusted: false,
      reason: "untrusted-authority-provenance"
    };
  }
  if (!item.programId || !item.programRunId || !item.approvalId) {
    return {
      required: true,
      trusted: false,
      reason: "missing-program-authority-lineage"
    };
  }
  const program = (operatingState.programs?.items ?? []).find(
    (candidate) => candidate.id === item.programId
  ) ?? null;
  const programRun = (operatingState.programRuns?.items ?? []).find(
    (candidate) => candidate.id === item.programRunId
  ) ?? null;
  const approval = (operatingState.programApprovals?.items ?? []).find(
    (candidate) => candidate.id === item.approvalId
  ) ?? null;
  if (!program || !programRun || !approval) {
    return {
      required: true,
      trusted: false,
      reason: "program-authority-not-found"
    };
  }
  if (programRun.programId !== program.id || programRun.approvalId !== approval.id || approval.programId !== program.id || approval.programRunId !== programRun.id || !programRun.linkedFollowThroughIds?.includes(item.id)) {
    return {
      required: true,
      trusted: false,
      reason: "program-authority-lineage-mismatch"
    };
  }
  if (approval.status !== "approved" || !["approved", "active"].includes(programRun.status) || !["active", "planned"].includes(program.status) || approval.expiresAt && Date.parse(approval.expiresAt) <= Date.now()) {
    return {
      required: true,
      trusted: false,
      reason: "program-authority-inactive"
    };
  }
  const claim = approval.activeExecutionClaim ?? null;
  if (!claim || !["claimed", "applied"].includes(claim.status) || claim.followThroughId !== item.id || claim.packetId !== item.linkedTargetId || claim.sourceType !== item.sourceType || claim.sourceId !== item.sourceId || claim.sourceFingerprint !== item.sourceFingerprint || programRun.activeExecutionClaimId !== claim.claimId) {
    return {
      required: true,
      trusted: false,
      reason: "runtime-authority-claim-mismatch"
    };
  }
  return { required: true, trusted: true, reason: null };
}
function followThroughSourceAuthorityFingerprint(sourceType, source) {
  let authority;
  if (sourceType === "remediation-pack") {
    authority = remediationPackAuthority(source);
  } else if (sourceType === "operator-playbook") {
    authority = operatorPlaybookAuthority(source);
  } else if (sourceType === "execution-bridge") {
    authority = executionBridgeAuthority(source);
  } else {
    throw new Error(
      `Unsupported follow-through authority source type: ${sourceType}.`
    );
  }
  return crypto2.createHash("sha256").update(stableJson({ sourceType, authority })).digest("hex");
}

// src/core/mutation-backend.mjs
import { AsyncLocalStorage } from "node:async_hooks";
import crypto3 from "node:crypto";
import fs2 from "node:fs";
import path2 from "node:path";

// src/core/contained-write.mjs
import fs from "node:fs";
import path from "node:path";
function pathEscapesRoot(relativePath) {
  return relativePath === ".." || relativePath.startsWith(`..${path.sep}`) || path.isAbsolute(relativePath);
}
function existingAncestor(candidatePath) {
  let currentPath = candidatePath;
  while (!fs.existsSync(currentPath)) {
    const parentPath = path.dirname(currentPath);
    if (parentPath === currentPath) {
      break;
    }
    currentPath = parentPath;
  }
  return currentPath;
}
function resolveCanonicalContainedWrite(root, candidatePath, options = {}) {
  const label = options.label ?? "Write path";
  const resolvedRoot = path.resolve(root);
  const canonicalRoot = fs.realpathSync.native(resolvedRoot);
  const requestedPath = path.isAbsolute(candidatePath) ? path.resolve(candidatePath) : path.resolve(resolvedRoot, candidatePath);
  const requestedRelative = path.relative(resolvedRoot, requestedPath);
  if (!requestedRelative || pathEscapesRoot(requestedRelative)) {
    throw new Error(`${label} must stay inside the canonical root: ${candidatePath}`);
  }
  let currentPath = resolvedRoot;
  for (const component of requestedRelative.split(path.sep)) {
    currentPath = path.join(currentPath, component);
    let stat;
    try {
      stat = fs.lstatSync(currentPath);
    } catch (error) {
      if (error?.code === "ENOENT") {
        break;
      }
      throw error;
    }
    if (stat.isSymbolicLink()) {
      throw new Error(`${label} must not contain symbolic links: ${candidatePath}`);
    }
  }
  const canonicalAncestor = fs.realpathSync.native(existingAncestor(requestedPath));
  const canonicalRelative = path.relative(canonicalRoot, canonicalAncestor);
  if (pathEscapesRoot(canonicalRelative)) {
    throw new Error(`${label} must stay inside the canonical root: ${candidatePath}`);
  }
  return {
    root: canonicalRoot,
    relativePath: requestedRelative.split(path.sep).join("/"),
    fullPath: path.join(canonicalRoot, requestedRelative)
  };
}

// src/core/mutation-backend.mjs
var mutationStorage = new AsyncLocalStorage();
var DIRECT_PROCESS_ROLLBACK_REASON = "direct-process writes are performed by the Dove process, not by host-tracked file edits; native programming-terminal rollback cannot be verified for those writes.";
var PATCH_PLAN_ROLLBACK_ADVICE = "Use mutationMode: patch-plan and apply the returned operations through host-tracked file edits before relying on host rollback.";
function sha256(content) {
  return crypto3.createHash("sha256").update(content).digest("hex");
}
function normalizeMutationMode(value) {
  if (value === void 0) {
    return "direct-process";
  }
  if (value === "patch-plan" || value === "direct-process") {
    return value;
  }
  throw new Error("mutationMode must be either patch-plan or direct-process when explicitly provided.");
}
function normalizeRelativePath(relativePath) {
  if (typeof relativePath !== "string" || !relativePath.trim()) {
    throw new Error("Mutation path must be a non-empty relative path.");
  }
  const normalized = path2.posix.normalize(relativePath.replace(/\\/g, "/"));
  if (path2.isAbsolute(relativePath) || normalized === "." || normalized.startsWith("../") || normalized === "..") {
    throw new Error(`Mutation path must stay inside the project: ${relativePath}`);
  }
  return normalized;
}
function classifyScope(relativePath) {
  if (relativePath === ".dove" || relativePath.startsWith(".dove/")) {
    return ".dove";
  }
  if (relativePath.startsWith(".opencode/") || relativePath.startsWith(".cursor/") || relativePath.startsWith(".codex/") || relativePath.startsWith(".agents/")) {
    return "generated-adapter";
  }
  return "explicit-external-output";
}
function serializeJson(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function resultDeclaresWrites(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value) && Array.isArray(value.writes) && value.writes.length > 0;
}
function readDiskText(root, relativePath, fallback = "") {
  const fullPath = path2.join(root, relativePath);
  if (!fs2.existsSync(fullPath)) {
    return fallback;
  }
  return fs2.readFileSync(fullPath, "utf8");
}
function diskFileState(root, relativePath) {
  const fullPath = path2.join(root, relativePath);
  if (!fs2.existsSync(fullPath)) {
    return { exists: false, sha256: null };
  }
  return { exists: true, sha256: sha256(fs2.readFileSync(fullPath)) };
}
function buildMutationId() {
  return `mutation-${crypto3.randomUUID()}`;
}
var MutationContext = class {
  constructor(root, options = {}) {
    const resolvedRoot = path2.resolve(root);
    this.root = fs2.realpathSync.native(resolvedRoot);
    this.id = options.id ?? buildMutationId();
    this.actionId = options.actionId ?? "unspecified";
    this.mutationMode = normalizeMutationMode(options.mutationMode);
    this.mutationModeSource = options.mutationMode === "patch-plan" || options.mutationMode === "direct-process" ? "explicit" : "default";
    this.hostId = options.hostId ?? "unknown";
    this.packetId = options.packetId ?? null;
    this.createdAt = options.createdAt ?? (/* @__PURE__ */ new Date()).toISOString();
    this.overlay = /* @__PURE__ */ new Map();
    this.operationsByPath = /* @__PURE__ */ new Map();
    this.operationOrder = [];
    this.lifecycle = "active";
  }
  get patchPlanMode() {
    return this.mutationMode === "patch-plan";
  }
  assertActive(operation = "MutationContext operation") {
    if (this.lifecycle !== "active") {
      throw new Error(`${operation} cannot use a ${this.lifecycle} MutationContext.`);
    }
  }
  resolve(relativePath) {
    this.assertActive("Mutation path resolution");
    const normalized = normalizeRelativePath(relativePath);
    return resolveCanonicalContainedWrite(this.root, normalized, { label: "Mutation path" });
  }
  fileExists(relativePath) {
    const { relativePath: normalized, fullPath } = this.resolve(relativePath);
    return this.overlay.has(normalized) || fs2.existsSync(fullPath);
  }
  readText(relativePath, fallback = "") {
    const { relativePath: normalized } = this.resolve(relativePath);
    if (this.overlay.has(normalized)) {
      return this.overlay.get(normalized);
    }
    return readDiskText(this.root, normalized, fallback);
  }
  readJson(relativePath, fallback) {
    const text3 = this.readText(relativePath, null);
    if (text3 === null) {
      return typeof fallback === "function" ? fallback() : structuredClone(fallback);
    }
    return JSON.parse(text3);
  }
  writeJson(relativePath, value) {
    return this.writeText(relativePath, serializeJson(value), "write-json");
  }
  writeJsonIfChanged(relativePath, value) {
    const nextContent = serializeJson(value);
    const currentContent = this.readText(relativePath, null);
    if (currentContent === nextContent) {
      return false;
    }
    this.writeText(relativePath, nextContent, "write-json");
    return true;
  }
  writeText(relativePath, content, kind = "write-text") {
    return this.writeContent(relativePath, String(content ?? ""), { kind, encoding: "utf8" });
  }
  writeBinary(relativePath, content, kind = "write-binary") {
    if (this.patchPlanMode) {
      throw new Error("Binary mutations require direct-process mode; patch-plan cannot safely represent binary output.");
    }
    const buffer = Buffer.isBuffer(content) ? content : Buffer.from(content);
    return this.writeContent(relativePath, buffer, { kind, encoding: "binary" });
  }
  writeContent(relativePath, content, { kind, encoding }) {
    const { relativePath: normalized, fullPath } = this.resolve(relativePath);
    const existing = this.operationsByPath.get(normalized);
    const initial = existing ? { exists: existing.previousExists, sha256: existing.previousSha256 } : diskFileState(this.root, normalized);
    const operation = {
      operationId: existing?.operationId ?? `op-${crypto3.randomUUID()}`,
      mutationId: this.id,
      actionId: this.actionId,
      packetId: this.packetId,
      relativePath: normalized,
      kind,
      encoding,
      ...encoding === "utf8" ? { content } : { byteLength: content.byteLength },
      previousExists: initial.exists,
      previousSha256: initial.sha256,
      expectedPreviousSha256: initial.sha256,
      nextSha256: sha256(content),
      scope: classifyScope(normalized),
      rollbackEligibility: this.patchPlanMode ? "host-tracked-file-edits-required" : "direct-process-unverified"
    };
    if (!existing) {
      this.operationOrder.push(normalized);
    }
    this.operationsByPath.set(normalized, operation);
    if (encoding === "utf8") {
      this.overlay.set(normalized, content);
    }
    if (!this.patchPlanMode) {
      fs2.mkdirSync(path2.dirname(fullPath), { recursive: true });
      fs2.writeFileSync(fullPath, content, encoding === "utf8" ? "utf8" : void 0);
    }
    return operation;
  }
  appendText(relativePath, content) {
    const previous = this.readText(relativePath, "");
    return this.writeText(relativePath, `${previous}${String(content ?? "")}`, "append-as-write");
  }
  ensureFile(relativePath, content) {
    if (this.fileExists(relativePath)) {
      return false;
    }
    this.writeText(relativePath, content, "ensure-file");
    return true;
  }
  ensureDirectory(relativePath) {
    const { relativePath: normalized, fullPath } = this.resolve(relativePath);
    if (fs2.existsSync(fullPath) || this.operationsByPath.has(normalized)) {
      return false;
    }
    const operation = {
      operationId: `op-${crypto3.randomUUID()}`,
      mutationId: this.id,
      actionId: this.actionId,
      packetId: this.packetId,
      relativePath: normalized,
      kind: "ensure-directory",
      encoding: null,
      previousExists: false,
      previousSha256: null,
      expectedPreviousSha256: null,
      nextSha256: null,
      scope: classifyScope(normalized),
      rollbackEligibility: this.patchPlanMode ? "host-tracked-file-edits-required" : "direct-process-unverified"
    };
    this.operationOrder.push(normalized);
    this.operationsByPath.set(normalized, operation);
    if (!this.patchPlanMode) {
      fs2.mkdirSync(fullPath, { recursive: true });
    }
    return true;
  }
  operations() {
    return this.operationOrder.map((relativePath) => this.operationsByPath.get(relativePath)).filter(Boolean);
  }
  recordProvenance() {
    const operations = this.operations().filter((operation) => operation.relativePath !== ARTIFACT_PATHS.mutationsIndex);
    if (operations.length === 0) {
      return;
    }
    const currentText = this.readText(ARTIFACT_PATHS.mutationsIndex, null);
    const currentIndex = currentText === null ? createMutationProvenanceIndex() : normalizeMutationProvenanceIndex(JSON.parse(currentText));
    const entry = {
      id: this.id,
      actionId: this.actionId,
      packetId: this.packetId,
      mutationMode: this.mutationMode,
      mutationModeSource: this.mutationModeSource,
      writesApplied: !this.patchPlanMode,
      appliedBy: this.patchPlanMode ? "host-tracked-file-edits-required" : "node-fs",
      hostId: this.hostId,
      hostRollbackEligible: this.patchPlanMode,
      hostTrackedFileEditsRequired: this.patchPlanMode,
      hostCheckpointVerified: false,
      directProcessWritesAreRollbackSafe: false,
      externalWriteCaptureVerified: false,
      doveRestoreSupported: false,
      hostRollbackIneligibleReason: this.patchPlanMode ? null : DIRECT_PROCESS_ROLLBACK_REASON,
      recommendedMutationMode: this.patchPlanMode ? null : "patch-plan",
      rollbackAdvice: this.patchPlanMode ? null : PATCH_PLAN_ROLLBACK_ADVICE,
      operationCount: operations.length,
      paths: operations.map((operation) => operation.relativePath),
      directoryEffectCount: operations.filter((operation) => operation.kind === "ensure-directory").length,
      directoryPaths: operations.filter((operation) => operation.kind === "ensure-directory").map((operation) => operation.relativePath),
      createdAt: this.createdAt
    };
    const nextEntries = [...currentIndex.entries.filter((item) => item.id !== this.id), entry];
    this.writeJson(ARTIFACT_PATHS.mutationsIndex, normalizeMutationProvenanceIndex({
      ...currentIndex,
      entries: nextEntries,
      updatedAt: this.createdAt
    }));
  }
  summary(options = {}) {
    const operations = this.operations();
    const writesApplied = options.writesApplied ?? (!this.patchPlanMode && operations.length > 0);
    return {
      mutationId: this.id,
      actionId: this.actionId,
      mutationMode: this.mutationMode,
      mutationModeSource: this.mutationModeSource,
      writesApplied,
      operationCount: operations.length,
      paths: operations.map((operation) => operation.relativePath),
      directoryEffectCount: operations.filter((operation) => operation.kind === "ensure-directory").length,
      directoryPaths: operations.filter((operation) => operation.kind === "ensure-directory").map((operation) => operation.relativePath),
      hostRollbackEligible: this.patchPlanMode,
      hostRollbackIneligibleReason: this.patchPlanMode ? null : DIRECT_PROCESS_ROLLBACK_REASON,
      recommendedMutationMode: this.patchPlanMode ? null : "patch-plan",
      rollbackAdvice: this.patchPlanMode ? null : PATCH_PLAN_ROLLBACK_ADVICE,
      externalWriteCaptureVerified: false,
      doveRestoreSupported: false
    };
  }
  finish(result = {}) {
    this.assertActive("MutationContext finish");
    this.recordProvenance();
    const operations = this.operations();
    const writesApplied = !this.patchPlanMode && (operations.length > 0 || resultDeclaresWrites(result));
    const metadata = {
      mutationId: this.id,
      mutationMode: this.mutationMode,
      mutationModeSource: this.mutationModeSource,
      writesApplied,
      hostRollbackEligible: this.patchPlanMode,
      hostTrackedFileEditsRequired: this.patchPlanMode,
      directProcessWritesAreRollbackSafe: false,
      externalWriteCaptureVerified: false,
      doveRestoreSupported: false,
      hostRollbackIneligibleReason: this.patchPlanMode ? null : DIRECT_PROCESS_ROLLBACK_REASON,
      recommendedMutationMode: this.patchPlanMode ? null : "patch-plan",
      rollbackAdvice: this.patchPlanMode ? null : PATCH_PLAN_ROLLBACK_ADVICE,
      mutationSummary: this.summary({ writesApplied })
    };
    if (this.patchPlanMode) {
      metadata.mutationPlan = {
        presentation: "dove-mutation-plan",
        mutationId: this.id,
        actionId: this.actionId,
        hostId: this.hostId,
        packetId: this.packetId,
        workspaceRealpath: fs2.realpathSync.native(this.root),
        mutationModeSource: this.mutationModeSource,
        createdAt: this.createdAt,
        writesApplied: false,
        hostTrackedFileEditsRequired: true,
        directProcessWritesAreRollbackSafe: false,
        externalWriteCaptureVerified: false,
        doveRestoreSupported: false,
        operations
      };
    }
    this.lifecycle = "finished";
    if (result && typeof result === "object" && !Array.isArray(result)) {
      return { ...result, ...metadata };
    }
    return { result, ...metadata };
  }
  abort() {
    if (this.lifecycle === "active") {
      this.lifecycle = "aborted";
    }
  }
};
function createMutationContext(root, options = {}) {
  return new MutationContext(root, options);
}
function runWithMutationContext(root, options, callback) {
  const context = createMutationContext(root, options);
  return mutationStorage.run(context, () => {
    try {
      const result = callback(context);
      if (result && typeof result.then === "function") {
        return result.then(
          (resolved) => context.finish(resolved),
          (error) => {
            context.abort();
            throw error;
          }
        );
      }
      return context.finish(result);
    } catch (error) {
      context.abort();
      throw error;
    }
  });
}
function currentMutationContext(root) {
  const context = mutationStorage.getStore();
  if (!context || context.lifecycle !== "active") {
    return null;
  }
  if (root) {
    try {
      if (fs2.realpathSync.native(path2.resolve(root)) !== context.root) {
        return null;
      }
    } catch {
      return null;
    }
  }
  return context;
}
function isPatchPlanMode(root) {
  return currentMutationContext(root)?.patchPlanMode === true;
}

// src/core/workspace-bootstrap.mjs
import path3 from "node:path";
var WORKSPACE_BOOTSTRAP_DIRECTORIES = [
  ARTIFACT_PATHS.doveRoot,
  ARTIFACT_PATHS.publicDir,
  ".dove/contracts",
  ".dove/orchestration",
  ARTIFACT_PATHS.taskPacketsDir,
  ARTIFACT_PATHS.taskPacketsPacketsDir,
  ".dove/context",
  ARTIFACT_PATHS.roleContextsDir,
  ARTIFACT_PATHS.phaseContextsDir,
  ARTIFACT_PATHS.packetContextsDir,
  ARTIFACT_PATHS.artifactContextsDir,
  ARTIFACT_PATHS.actionContextsDir,
  ".dove/sessions",
  ARTIFACT_PATHS.workspaceDir,
  ARTIFACT_PATHS.programsDir,
  ARTIFACT_PATHS.workflowPackDir,
  ".dove/research",
  ".dove/plans",
  ".dove/outline",
  ".dove/sources",
  ".dove/notes",
  ".dove/evidence",
  ARTIFACT_PATHS.documentsDir,
  ".dove/claims",
  ".dove/drafts",
  ".dove/experiments",
  ".dove/reviews",
  ".dove/revision-plans",
  ".dove/wiki",
  ".dove/checklists",
  ".dove/bibliography",
  ".dove/figures",
  ".dove/rebuttal",
  ".dove/versions",
  ARTIFACT_PATHS.runtimeDir,
  ARTIFACT_PATHS.mutationsDir,
  ARTIFACT_PATHS.metaDir,
  ARTIFACT_PATHS.versionSnapshotsDir
];
function createStarterMarkdown(state) {
  const zh = state.settings?.responseLanguage !== "en";
  const tbd = zh ? "\u5F85\u5B9A" : "TBD";
  return {
    [ARTIFACT_PATHS.readme]: zh ? `# .dove \u5DE5\u4F5C\u533A

\u6B64\u76EE\u5F55\u662F Dove \u7684\u6301\u4E45\u4E8B\u5B9E\u6E90\u3002

- \u547D\u4EE4\u3001\u6280\u80FD\u548C MCP \u90FD\u5E94\u6C47\u805A\u5230\u8FD9\u4E9B\u6587\u4EF6\u3002
- \\.dove/orchestration/ \u4E2D\u7684\u7F16\u6392\u770B\u677F\u662F\u89C4\u8303\u5DE5\u4F5C\u8DDF\u8E2A\u5668\u3002
- \u6301\u4E45\u4EFB\u52A1\u5305\u3001\u89D2\u8272\u6E05\u5355\u548C\u4F1A\u8BDD\u6458\u8981\u5E94\u4FDD\u5B58\u5728 \\.dove/ \u5185\u3002
- \u7814\u7A76\u3001\u5B9E\u9A8C\u3001rebuttal \u548C\u7248\u672C\u6F14\u5316\u90FD\u5E94\u4FDD\u6301\u6587\u4EF6\u4F18\u5148\u3001\u53EF\u6062\u590D\u3002
- \u5B8C\u6574\u6027\u4EA7\u7269\u8BA9\u610F\u56FE\u3001\u7EED\u63A5\u3001\u5BA1\u67E5\u548C\u7ED3\u679C\u5230\u4E3B\u5F20\u7684\u8F6C\u79FB\u53EF\u5BA1\u8BA1\u3002
` : `# .dove workspace

This directory is the durable source of truth for Dove.

- Commands, skills, and MCP should converge on these files.
- The orchestration board in \\.dove/orchestration/ is the canonical work tracker.
- Durable task packets, role manifests, and session summaries should stay inside \\.dove/.
- Research, experiments, rebuttal, and version evolution should remain file-first and resumable.
- Phase-2 integrity artifacts make intent, continuation, review, and result-to-claim transitions auditable.
`,
    [ARTIFACT_PATHS.project]: zh ? `# \u9879\u76EE\u7B80\u62A5

- \u5DE5\u4F5C\u6807\u9898: ${state.dove.title}
- \u76EE\u6807 venue: ${state.dove.venue}
- \u76EE\u6807: ${state.dove.objective}
- \u622A\u6B62\u65F6\u95F4: ${state.dove.deadline || tbd}

## Thesis

${state.dove.thesis}

## Audience

${state.dove.audience}
` : `# Project brief

- Working title: ${state.dove.title}
- Venue: ${state.dove.venue}
- Objective: ${state.dove.objective}
- Deadline: ${state.dove.deadline || tbd}

## Thesis

${state.dove.thesis}

## Audience

${state.dove.audience}
`,
    [ARTIFACT_PATHS.researchContract]: zh ? `# \u7814\u7A76\u5951\u7EA6

## \u8303\u56F4

- \u6807\u9898: ${state.dove.title}
- Venue: ${state.dove.venue}
- \u76EE\u6807: ${state.dove.objective}

## \u8BC1\u636E\u7B56\u7565

- \u4E0D\u51ED\u8BB0\u5FC6\u5F15\u7528\u3002
- \u6BCF\u4E2A\u4E3B\u8981 claim \u90FD\u5E94\u6620\u5C04\u5230 source\u3001note\u3001experiment \u6216\u660E\u786E\u6807\u8BB0\u7684 gap\u3002
- Review findings \u548C rebuttal issues \u5FC5\u987B\u8F6C\u5316\u4E3A\u6301\u4E45\u884C\u52A8\u9879\u3002
- \u6700\u7EC8\u5B9A\u7A3F claim \u9700\u8981\u4E00\u6B21\u663E\u5F0F\u9A8C\u8BC1\u3002
` : `# Research contract

## Scope

- Title: ${state.dove.title}
- Venue: ${state.dove.venue}
- Objective: ${state.dove.objective}

## Evidence policy

- No citation from memory.
- Every major claim should map to a source, note, experiment, or clearly marked gap.
- Review findings and rebuttal issues must become durable action items.
- Finalization claims require an explicit verification pass.
`,
    [ARTIFACT_PATHS.researchBrief]: zh ? `# \u7814\u7A76\u7B80\u62A5

## \u76EE\u6807

${state.dove.objective}

## \u8BAE\u7A0B

- \u6F84\u6E05\u8BBA\u6587\u76EE\u6807\u4E0E\u8D21\u732E\u3002
- \u5728\u63D0\u51FA\u5F3A claim \u524D\u6269\u5C55\u8BC1\u636E\u57FA\u7840\u3002

## \u5BF9\u6BD4\u76EE\u6807

- \u8BC4\u4F30\u524D\u5728\u6B64\u6DFB\u52A0\u6D3B\u8DC3\u5BF9\u6BD4\u76EE\u6807\u3002
` : `# Research brief

## Objective

${state.dove.objective}

## Agenda

- Clarify the paper objective and contribution.
- Expand the evidence base before making strong claims.

## Comparison targets

- Add active comparison targets here before evaluation.
`,
    [ARTIFACT_PATHS.plan]: zh ? `# \u5F53\u524D Dove \u4EFB\u52A1\u8BA1\u5212

## \u4E00\u53E5\u8BDD thesis

${state.dove.thesis}

## \u8BA1\u5212\u7AE0\u8282

${Object.values(state.sections).map((section) => `- [ ] ${section.title}`).join("\n")}

## \u7ACB\u5373\u4E0B\u4E00\u6B65

\u8FD0\u884C \`project:dove.status\` \u5BF9\u9F50\u770B\u677F\u5E76\u9009\u62E9\u4E0B\u4E00\u4E2A Dove \u5DE5\u4F5C\u6D41\u3002
` : `# Current Dove mission plan

## One-sentence thesis

${state.dove.thesis}

## Planned sections

${Object.values(state.sections).map((section) => `- [ ] ${section.title}`).join("\n")}

## Immediate next step

Run \`project:dove.status\` to align the board and choose the next Dove surface.
`,
    [ARTIFACT_PATHS.outline]: zh ? `# \u5F53\u524D outline

${Object.values(state.sections).map((section) => `## ${section.title}

- \u76EE\u6807: \u5F85\u5B9A
- \u8BC1\u636E: \u5F85\u5B9A
`).join("\n")}` : `# Current outline

${Object.values(state.sections).map((section) => `## ${section.title}

- Goal: TBD
- Evidence: TBD
`).join("\n")}`,
    [ARTIFACT_PATHS.findings]: zh ? `# \u53D1\u73B0

\u5728\u8F6C\u5316\u4E3A claims \u524D\uFF0C\u5728\u6B64\u8BB0\u5F55\u5173\u952E\u5B9E\u8BC1\u6216\u5206\u6790\u7ED3\u8BBA\u3002
` : `# Findings

Capture key empirical or analytical takeaways here before turning them into claims.
`,
    [ARTIFACT_PATHS.claims]: zh ? `# \u7ED3\u679C claims

\u53EA\u5217\u51FA\u53EF\u8FFD\u6EAF\u5230 sources\u3001notes \u6216 experiments \u7684 claims\u3002
` : `# Claims from results

List only claims that can be traced to sources, notes, or experiments.
`,
    [ARTIFACT_PATHS.experimentLog]: zh ? `# \u5B9E\u9A8C\u65E5\u5FD7

\u5728\u6B64\u8BB0\u5F55\u8BA1\u5212\u8FD0\u884C\u3001\u8BBE\u7F6E\u3001\u7ED3\u679C\u548C\u5931\u8D25\u6848\u4F8B\u3002
` : `# Experiment log

Document planned runs, settings, outcomes, and failure cases here.
`,
    [ARTIFACT_PATHS.orchestrationHandoffs]: zh ? `# Handoffs

## 1970-01-01T00:00:00.000Z \u2014 planner -> researcher

- \u9636\u6BB5: init
- \u610F\u56FE: plan
- \u6458\u8981: \u5DE5\u4F5C\u533A\u5DF2\u521D\u59CB\u5316\u3002
- \u5F53\u524D\u7126\u70B9: \u6F84\u6E05\u8BBA\u6587\u76EE\u6807\u3002
- \u4E0B\u4E00\u6B65: \u6CE8\u518C\u6838\u5FC3 sources\u3002
- \u540E\u7EED\u52A8\u4F5C:
  - \u6CE8\u518C\u6838\u5FC3 sources\u3002
  - \u521B\u5EFA\u7B2C\u4E00\u4EFD\u7814\u7A76\u7B80\u62A5\u3002
- \u8BC1\u636E\u94FE\u63A5: \u65E0
- \u963B\u585E: \u65E0
` : `# Handoffs

## 1970-01-01T00:00:00.000Z \u2014 planner -> researcher

- Phase: init
- Intent: plan
- Summary: Workspace initialized.
- Current focus: Clarify the paper objective.
- Next action: Register core sources.
- Next actions:
  - Register core sources.
  - Create the first research brief.
- Evidence links: none
- Blockers: none
`,
    [ARTIFACT_PATHS.reviewLog]: zh ? `# \u5BA1\u67E5\u65E5\u5FD7

\u6B64\u6587\u4EF6\u53EA\u8FFD\u52A0\u5199\u5165\u3002

## 1970-01-01T00:00:00.000Z \u2014 bootstrap

- \u8303\u56F4: \u5DE5\u4F5C\u533A\u521D\u59CB\u5316
- \u7ED3\u8BBA: not-reviewed
- \u6458\u8981: \u5DF2\u521B\u5EFA starter \u5BA1\u67E5\u65E5\u5FD7\u3002
- \u884C\u52A8\u9879:
  - \u8D77\u8349\u524D\u5148\u5EFA\u7ACB source \u548C note \u57FA\u7840\u3002
` : `# Review log

This file is append-only.

## 1970-01-01T00:00:00.000Z \u2014 bootstrap

- Scope: workspace initialization
- Verdict: not-reviewed
- Summary: Starter review log created.
- Action items:
  - Build the source and note base before drafting.
`,
    [ARTIFACT_PATHS.reviewDebateLog]: zh ? `# Debate log

\u7528\u6B64\u6587\u4EF6\u4EE5\u6301\u4E45\u6587\u672C\u8BB0\u5F55\u5BF9\u6297\u5F0F\u5BA1\u67E5\u8F6E\u6B21\u3001rebuttal \u548C\u88C1\u5B9A\u3002
` : `# Debate log

Use this file to capture adversarial review rounds, rebuttals, and rulings in durable prose.
`,
    [ARTIFACT_PATHS.revisionPlan]: zh ? `# \u5F53\u524D revision plan

\u5C1A\u672A\u7531 review loop \u751F\u6210 revision plan\u3002
` : `# Current revision plan

No review loop has generated a revision plan yet.
`,
    [ARTIFACT_PATHS.wiki]: zh ? `# Paper wiki

## Thesis

${state.dove.thesis}

## \u8BC1\u636E\u6E05\u5355

- \u5F85\u5B9A

## Reviewer concerns

- \u5F85\u5B9A

## \u7248\u672C lineage

- \u8FD8\u6CA1\u6709 snapshots\u3002
` : `# Paper wiki

## Thesis

${state.dove.thesis}

## Evidence inventory

- TBD

## Reviewer concerns

- TBD

## Version lineage

- No snapshots yet.
`,
    [ARTIFACT_PATHS.queryPack]: zh ? `# Query pack

- \u4ECD\u7F3A\u5C11\u54EA\u4E9B\u8BC1\u636E\uFF1F
- \u54EA\u4E9B claims \u652F\u6491\u8F83\u5F31\uFF1F
- \u8D77\u8349\u66F4\u5F3A\u7ED3\u8BBA\u524D\u9700\u8981\u8FD0\u884C\u54EA\u4E9B\u5B9E\u9A8C\uFF1F
- \u54EA\u4E9B rebuttal issues \u4ECD\u672A\u89E3\u51B3\uFF1F
` : `# Query pack

- What evidence is still missing?
- Which claims are weakly supported?
- Which experiments need to be run before drafting stronger conclusions?
- Which rebuttal issues remain unresolved?
`,
    [ARTIFACT_PATHS.navigationReport]: zh ? `# Navigation

## \u4EFB\u52A1\u56FE

- \u5C1A\u672A\u751F\u6210 task packets\u3002

## Open questions

- \u5C1A\u672A\u8BB0\u5F55 open questions\u3002

## Decisions

- \u5C1A\u672A\u8BB0\u5F55 durable decisions\u3002

## Lineage

- \u5C1A\u672A\u8BB0\u5F55 durable lineage summary\u3002
` : `# Navigation

## Task graph

- No task packets generated yet.

## Open questions

- No open questions recorded yet.

## Decisions

- No durable decisions recorded yet.

## Lineage

- No durable lineage summary recorded yet.
`,
    [ARTIFACT_PATHS.checklist]: zh ? `# Dove \u4EFB\u52A1\u68C0\u67E5\u6E05\u5355

## \u4EFB\u52A1\u72B6\u6001

- [ ] \u4FDD\u6301 .dove/task-packets/index.json \u6700\u65B0
- [ ] \u5185\u90E8\u5DE5\u4F5C\u6D41\u4F7F\u7528\u770B\u677F\u65F6\uFF0C\u4FDD\u6301 .dove/orchestration/board.json \u4E0E\u6D3B\u8DC3\u4EFB\u52A1\u5BF9\u9F50

## \u4EFB\u52A1\u8BC1\u636E

- [ ] \u58F0\u660E\u76EE\u6807\u4EA7\u7269\u548C\u9A8C\u6536\u8BC1\u636E
- [ ] \u5C06 sources\u3001notes\u3001\u53D8\u66F4\u6587\u4EF6\u6216\u9A8C\u8BC1\u8F93\u51FA\u94FE\u63A5\u5230\u6D3B\u8DC3\u4EFB\u52A1
- [ ] \u4FDD\u6301\u5DE5\u4F5C\u533A\u7D22\u5F15\u548C\u4EFB\u52A1\u5305\u53EF\u6062\u590D

## \u6267\u884C

- [ ] \u901A\u8FC7 project:dove.mission\u3001project:dove.auto \u6216\u9876\u5C42\u9884\u8BBE\u547D\u4EE4\u8DEF\u7531\u5177\u4F53\u5DE5\u4F5C
- [ ] \u4F7F\u7528 project:dove.status \u68C0\u67E5\u963B\u585E\u3001\u5BA1\u67E5\u5C31\u7EEA\u5EA6\u548C\u5B8C\u6210\u8BC1\u636E

## \u5BA1\u67E5\u4E0E\u5173\u95ED

- [ ] \u5B8C\u6210\u9700\u5BA1\u67E5\u5DE5\u4F5C\u524D\u8FD0\u884C project:dove.review \u6216 project:dove.review-loop
- [ ] \u6807\u8BB0\u5B8C\u6210\u524D\u786E\u8BA4\u5B8C\u6210\u8BC1\u636E\u5DF2\u7ECF\u6301\u4E45\u5316
` : `# Dove mission checklist

## Task state

- [ ] Keep .dove/task-packets/index.json current
- [ ] Keep .dove/orchestration/board.json aligned with active tasks when internal workflows use it

## Mission evidence

- [ ] Declare target artifacts and acceptance evidence
- [ ] Keep sources, notes, changed files, or validation output linked to the active task
- [ ] Keep the workspace index and task packets resumable

## Execution

- [ ] Route concrete work through project:dove.mission, project:dove.auto, or the top-level preset commands
- [ ] Use project:dove.status to inspect blockers, review readiness, and completion evidence

## Review and closure

- [ ] Run project:dove.review or project:dove.review-loop before finalizing reviewed work
- [ ] Confirm completion evidence is durable before marking work complete
`,
    [ARTIFACT_PATHS.bibliography]: "",
    [ARTIFACT_PATHS.citationLog]: zh ? `# Citation log

\u5728\u6B64\u8DDF\u8E2A sources \u7684\u6CE8\u518C\u548C\u9A8C\u8BC1\u72B6\u6001\u3002
` : `# Citation log

Track registration and verification status for sources here.
`,
    [ARTIFACT_PATHS.figuresReadme]: zh ? `# Figures backlog

\u6B64\u76EE\u5F55\u7528\u4E8E figure/table \u89C4\u5212\u548C\u751F\u6210\u4EA7\u7269\u3002Dove \u4F1A\u8BB0\u5F55 figure briefs\u3001\u6240\u9700\u6750\u6599\u3001generation input bundles\u3001\u5BFC\u5165\u8F93\u51FA\u3001captions \u548C QA state\uFF0C\u8BA9 figures \u53EF\u6062\u590D\u4E14\u53EF\u94FE\u63A5\u8BC1\u636E\u3002
` : `# Figures backlog

Use this directory for figure/table planning and generation artifacts. Dove records figure briefs, required materials, generation input bundles, imported outputs, captions, and QA state so figures remain resumable and evidence-linked.
`,
    [ARTIFACT_PATHS.rebuttalStrategy]: zh ? `# Rebuttal strategy

\u5C1A\u672A\u751F\u6210 issue strategy\u3002
` : `# Rebuttal strategy

No issue strategy has been generated yet.
`,
    [ARTIFACT_PATHS.rebuttalResponseDraft]: zh ? `# Rebuttal response draft

\u89C4\u8303\u5316 reviewer issues \u540E\uFF0C\u5728\u6B64\u8D77\u8349\u7B80\u6D01\u4E14\u6709\u8BC1\u636E\u652F\u6491\u7684\u56DE\u5E94\u3002
` : `# Rebuttal response draft

Draft concise, evidence-backed responses here after normalizing reviewer issues.
`,
    [ARTIFACT_PATHS.versionComparisonReport]: zh ? `# Latest version comparison

\u5C1A\u672A\u751F\u6210 comparison\u3002
` : `# Latest version comparison

No comparison has been generated yet.
`,
    [ARTIFACT_PATHS.metaOptimizerReport]: zh ? `# Latest optimizer report

- Proposal only: true
- \u5C1A\u672A\u751F\u6210 meta-optimization recommendations\u3002
` : `# Latest optimizer report

- Proposal only: true
- No meta-optimization recommendations have been generated yet.
`,
    [ARTIFACT_PATHS.sessionSummary]: zh ? `# Latest session summary

- \u5C1A\u672A\u751F\u6210 durable session summary\u3002
` : `# Latest session summary

- No durable session summary has been generated yet.
`,
    [path3.join(ARTIFACT_PATHS.draftsDir, "README.md")]: zh ? `# Drafts

\u6BCF\u4E2A markdown \u6587\u4EF6\u4FDD\u5B58\u4E00\u4E2A section\u3002
` : `# Drafts

Store one section per markdown file.
`,
    [path3.join(ARTIFACT_PATHS.claims.replace("CLAIMS_FROM_RESULTS.md", "README.md"))]: zh ? `# Claims

\u6B64\u76EE\u5F55\u4FDD\u5B58\u6709\u8BC1\u636E\u652F\u6491\u7684 claim artifacts\u3002
` : `# Claims

This directory holds evidence-grounded claim artifacts.
`
  };
}
function createWorkspaceBootstrapJsonArtifacts(state) {
  return [
    [ARTIFACT_PATHS.orchestrationBoard, () => createDefaultBoard(state)],
    [ARTIFACT_PATHS.sources, createSourcesIndex],
    [ARTIFACT_PATHS.sourceVerifications, () => ({ version: 1, items: [], updatedAt: null })],
    [ARTIFACT_PATHS.notes, createNotesIndex],
    [ARTIFACT_PATHS.evidence, createEvidenceIndex],
    [ARTIFACT_PATHS.documentsLedger, createDocumentLedgerIndex],
    [ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex],
    [ARTIFACT_PATHS.reviewState, createReviewState],
    [ARTIFACT_PATHS.reviewConcerns, createReviewConcernsIndex],
    [ARTIFACT_PATHS.adversarialReviewState, createAdversarialReviewState],
    [ARTIFACT_PATHS.figuresIndex, createFiguresIndex],
    [ARTIFACT_PATHS.figureBriefs, createFigureBriefsIndex],
    [ARTIFACT_PATHS.figureSegments, createFigureSegmentsIndex],
    [ARTIFACT_PATHS.figureTemplates, createFigureTemplatesIndex],
    [ARTIFACT_PATHS.figureEditableIndex, createFigureEditableIndex],
    [ARTIFACT_PATHS.figureFinalIndex, createFigureFinalIndex],
    [ARTIFACT_PATHS.figureMaterials, createFigureMaterialsIndex],
    [ARTIFACT_PATHS.figureGenerations, createFigureGenerationsIndex],
    [ARTIFACT_PATHS.figureCaptions, createFigureCaptionsIndex],
    [ARTIFACT_PATHS.figureQa, createFigureQaIndex],
    [ARTIFACT_PATHS.researchAgenda, createResearchAgenda],
    [ARTIFACT_PATHS.experimentPlans, createExperimentPlansIndex],
    [ARTIFACT_PATHS.experimentResults, createExperimentResultsIndex],
    [ARTIFACT_PATHS.experimentAudits, createExperimentAuditsIndex],
    [ARTIFACT_PATHS.claimBridgeLog, createClaimBridgeLog],
    [ARTIFACT_PATHS.metaEvents, createMetaEventsIndex],
    [ARTIFACT_PATHS.metaExecutionBridgeCandidates, createMetaExecutionBridgeCandidatesIndex],
    [ARTIFACT_PATHS.metaGovernanceCoverage, createMetaGovernanceCoverageIndex],
    [ARTIFACT_PATHS.metaGovernanceCoverageReport, createMetaGovernanceCoverageReport],
    [ARTIFACT_PATHS.metaLongHorizonMemory, createMetaLongHorizonMemory],
    [ARTIFACT_PATHS.metaOperatorLessons, createMetaOperatorLessonsIndex],
    [ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex],
    [ARTIFACT_PATHS.metaOperatorFollowThroughTransitions, createMetaOperatorFollowThroughTransitionsIndex],
    [ARTIFACT_PATHS.metaOperatorPlaybooks, createMetaOperatorPlaybooksIndex],
    [ARTIFACT_PATHS.metaRemediationPacks, createMetaRemediationPacksIndex],
    [ARTIFACT_PATHS.metaRecommendations, createMetaRecommendationsIndex],
    [ARTIFACT_PATHS.metaOptimizerState, createMetaOptimizerState],
    [ARTIFACT_PATHS.rebuttalIssues, createRebuttalIssuesIndex],
    [ARTIFACT_PATHS.versionsIndex, createVersionsIndex],
    [ARTIFACT_PATHS.versionComparisons, createVersionComparisonsIndex],
    [ARTIFACT_PATHS.wikiEntities, createWikiEntitiesIndex],
    [ARTIFACT_PATHS.wikiRelations, createWikiRelationsIndex],
    [ARTIFACT_PATHS.sessionJournal, createSessionJournal],
    [ARTIFACT_PATHS.runtimeControllerState, createRuntimeControllerState],
    [ARTIFACT_PATHS.runtimeContinuation, createRuntimeContinuationIndex],
    [ARTIFACT_PATHS.runtimeLeases, createRuntimeLeasesIndex],
    [ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex],
    [ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex],
    [ARTIFACT_PATHS.mutationsIndex, createMutationProvenanceIndex],
    [ARTIFACT_PATHS.programsIndex, createProgramsIndex],
    [ARTIFACT_PATHS.programRuns, createProgramRunsIndex],
    [ARTIFACT_PATHS.programApprovals, createProgramApprovalsIndex],
    [ARTIFACT_PATHS.campaignsIndex, createCampaignsIndex],
    [ARTIFACT_PATHS.workspaceIndex, createWorkspaceIndex],
    [ARTIFACT_PATHS.doveRootManifest, createDoveAuthorityManifest],
    [ARTIFACT_PATHS.workflowBoundaries, createWorkflowBoundaries]
  ];
}
function createManagedWorkspaceJsonArtifacts() {
  return [
    [ARTIFACT_PATHS.workflowBoundaries, createWorkflowBoundaries, normalizeWorkflowBoundaries],
    [ARTIFACT_PATHS.documentsLedger, createDocumentLedgerIndex, normalizeDocumentLedgerIndex],
    [ARTIFACT_PATHS.metaExecutionBridgeCandidates, createMetaExecutionBridgeCandidatesIndex, normalizeMetaExecutionBridgeCandidatesIndex],
    [ARTIFACT_PATHS.metaGovernanceCoverage, createMetaGovernanceCoverageIndex, normalizeMetaGovernanceCoverageIndex],
    [ARTIFACT_PATHS.metaGovernanceCoverageReport, createMetaGovernanceCoverageReport, normalizeMetaGovernanceCoverageReport],
    [ARTIFACT_PATHS.metaLongHorizonMemory, createMetaLongHorizonMemory, normalizeMetaLongHorizonMemory],
    [ARTIFACT_PATHS.metaOperatorLessons, createMetaOperatorLessonsIndex, normalizeMetaOperatorLessonsIndex],
    [ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex, normalizeMetaOperatorFollowThroughIndex],
    [ARTIFACT_PATHS.metaOperatorFollowThroughTransitions, createMetaOperatorFollowThroughTransitionsIndex, normalizeMetaOperatorFollowThroughTransitionsIndex],
    [ARTIFACT_PATHS.metaOperatorPlaybooks, createMetaOperatorPlaybooksIndex, normalizeMetaOperatorPlaybooksIndex],
    [ARTIFACT_PATHS.metaRemediationPacks, createMetaRemediationPacksIndex, normalizeMetaRemediationPacksIndex],
    [ARTIFACT_PATHS.metaRecommendations, createMetaRecommendationsIndex, normalizeMetaRecommendationsIndex],
    [ARTIFACT_PATHS.metaOptimizerState, createMetaOptimizerState, normalizeMetaOptimizerState],
    [ARTIFACT_PATHS.runtimeControllerState, createRuntimeControllerState, normalizeRuntimeControllerState],
    [ARTIFACT_PATHS.runtimeContinuation, createRuntimeContinuationIndex, normalizeRuntimeContinuationIndex],
    [ARTIFACT_PATHS.runtimeLeases, createRuntimeLeasesIndex, normalizeRuntimeLeasesIndex],
    [ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex, normalizeRuntimeEventsIndex],
    [ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex, normalizeRuntimeResultsIndex],
    [ARTIFACT_PATHS.mutationsIndex, createMutationProvenanceIndex, normalizeMutationProvenanceIndex],
    [ARTIFACT_PATHS.programsIndex, createProgramsIndex, normalizeProgramsIndex],
    [ARTIFACT_PATHS.programRuns, createProgramRunsIndex, normalizeProgramRunsIndex],
    [ARTIFACT_PATHS.programApprovals, createProgramApprovalsIndex, normalizeProgramApprovalsIndex],
    [ARTIFACT_PATHS.campaignsIndex, createCampaignsIndex, normalizeCampaignsIndex],
    [ARTIFACT_PATHS.workspaceIndex, createWorkspaceIndex, normalizeWorkspaceIndex],
    [ARTIFACT_PATHS.doveRootManifest, createDoveAuthorityManifest, normalizeDoveAuthorityManifest]
  ];
}

// src/core/workspace.mjs
function assertNoPolicyOverrideArgs(args = {}, actionLabel = "This operation") {
  if (!args || typeof args !== "object" || Array.isArray(args)) {
    return;
  }
  const fields = Object.keys(args).filter((key) => key.startsWith("policyOverride"));
  if (fields.length > 0) {
    throw new Error(`${actionLabel} does not accept retired policy override fields: ${fields.join(", ")}.`);
  }
}
var WORKFLOW_BOUNDARIES = createWorkflowBoundaries();
function isBootstrapManagedDovePath(relativePath) {
  return relativePath === ARTIFACT_PATHS.state || WORKFLOW_BOUNDARIES.doveBootstrapOnlyPaths.includes(relativePath);
}
function nowIso() {
  return (/* @__PURE__ */ new Date()).toISOString();
}
function resolvePath(root, relativePath) {
  return path4.join(root, relativePath);
}
function ensureDir(dirPath) {
  fs3.mkdirSync(dirPath, { recursive: true });
}
function cloneFallback(fallback) {
  return typeof fallback === "function" ? fallback() : structuredClone(fallback);
}
function requireMutationContext(root, operation) {
  const context = currentMutationContext(root);
  if (!context) {
    throw new Error(`${operation} requires an active MutationContext.`);
  }
  return context;
}
function fileExists(root, relativePath) {
  const context = currentMutationContext(root);
  if (context) {
    return context.fileExists(relativePath);
  }
  return fs3.existsSync(resolvePath(root, relativePath));
}
function readJson(root, relativePath, fallback) {
  const context = currentMutationContext(root);
  if (context) {
    return context.readJson(relativePath, fallback);
  }
  const fullPath = resolvePath(root, relativePath);
  if (!fs3.existsSync(fullPath)) {
    return cloneFallback(fallback);
  }
  try {
    return JSON.parse(fs3.readFileSync(fullPath, "utf8"));
  } catch (error) {
    throw new Error(`Malformed JSON in ${relativePath}: ${error.message}`);
  }
}
function writeJson(root, relativePath, value) {
  return requireMutationContext(root, "writeJson").writeJson(relativePath, value);
}
function writeJsonIfChanged(root, relativePath, value) {
  return requireMutationContext(root, "writeJsonIfChanged").writeJsonIfChanged(relativePath, value);
}
function reconcileManagedJsonArtifact(root, relativePath, fallback, normalize) {
  const normalized = normalize(readJson(root, relativePath, fallback));
  writeJsonIfChanged(root, relativePath, normalized);
  return normalized;
}
function readText(root, relativePath, fallback = "") {
  const context = currentMutationContext(root);
  if (context) {
    return context.readText(relativePath, fallback);
  }
  const fullPath = resolvePath(root, relativePath);
  if (!fs3.existsSync(fullPath)) {
    return fallback;
  }
  return fs3.readFileSync(fullPath, "utf8");
}
function writeText(root, relativePath, content) {
  return requireMutationContext(root, "writeText").writeText(relativePath, content);
}
function writeBinary(root, relativePath, content) {
  return requireMutationContext(root, "writeBinary").writeBinary(relativePath, content);
}
function appendText(root, relativePath, content) {
  return requireMutationContext(root, "appendText").appendText(relativePath, content);
}
function ensureFile(root, relativePath, content) {
  return requireMutationContext(root, "ensureFile").ensureFile(relativePath, content);
}
function ensureWorkspace(root) {
  const context = requireMutationContext(root, "ensureWorkspace");
  const state = normalizeState(readJson(root, ARTIFACT_PATHS.state, createDefaultState));
  const created = [];
  for (const relativeDir of WORKSPACE_BOOTSTRAP_DIRECTORIES) {
    context.ensureDirectory(relativeDir);
  }
  if (!fileExists(root, ARTIFACT_PATHS.state)) {
    writeJson(root, ARTIFACT_PATHS.state, state);
    created.push(ARTIFACT_PATHS.state);
  }
  for (const [relativePath, content] of Object.entries(createStarterMarkdown(state))) {
    if (isBootstrapManagedDovePath(relativePath) && ensureFile(root, relativePath, content)) {
      created.push(relativePath);
    }
  }
  for (const [relativePath, factory] of createWorkspaceBootstrapJsonArtifacts(state)) {
    if (isBootstrapManagedDovePath(relativePath) && !fileExists(root, relativePath)) {
      writeJson(root, relativePath, factory());
      created.push(relativePath);
    }
  }
  for (const [relativePath, fallback, normalize] of createManagedWorkspaceJsonArtifacts()) {
    reconcileManagedJsonArtifact(root, relativePath, fallback, normalize);
  }
  return { root, created };
}
function loadState(root) {
  return normalizeState(readJson(root, ARTIFACT_PATHS.state, createDefaultState));
}
function saveState(root, state) {
  const normalized = normalizeState(state);
  writeJson(root, ARTIFACT_PATHS.state, normalized);
  return normalized;
}
function listArtifacts(root) {
  return Object.fromEntries(
    Object.entries(ARTIFACT_PATHS).map(([key, relativePath]) => {
      const fullPath = resolvePath(root, relativePath);
      return [key, { path: relativePath, exists: fs3.existsSync(fullPath) }];
    })
  );
}
function listDraftFiles(root) {
  const draftsDir = resolvePath(root, ARTIFACT_PATHS.draftsDir);
  if (!fs3.existsSync(draftsDir)) {
    return [];
  }
  return fs3.readdirSync(draftsDir, { withFileTypes: true }).filter((entry) => entry.isFile() && entry.name.endsWith(".md") && entry.name !== "README.md").map((entry) => entry.name);
}
function extractCitationKeysFromText(content) {
  const keys = [];
  const pattern = /\[cite:([^\]]+)\]/g;
  let match;
  while ((match = pattern.exec(content)) !== null) {
    const key = match[1]?.trim();
    if (key) {
      keys.push(key);
    }
  }
  return keys;
}
function targetArtifactContainsId(root, artifactPath, targetId) {
  if (!artifactPath || !targetId) {
    return false;
  }
  const fullPath = resolvePath(root, artifactPath);
  if (!fs3.existsSync(fullPath)) {
    return false;
  }
  const extension = path4.extname(artifactPath).toLowerCase();
  if (extension === ".json") {
    try {
      const value = JSON.parse(fs3.readFileSync(fullPath, "utf8"));
      const queue = [value];
      while (queue.length > 0) {
        const current = queue.shift();
        if (current === targetId) {
          return true;
        }
        if (Array.isArray(current)) {
          queue.push(...current);
          continue;
        }
        if (current && typeof current === "object") {
          queue.push(...Object.values(current));
        }
      }
      return false;
    } catch {
      return false;
    }
  }
  return fs3.readFileSync(fullPath, "utf8").includes(String(targetId));
}
function assertFollowThroughReady(root, actionLabel, args = {}) {
  assertNoPolicyOverrideArgs(args, actionLabel);
  const ledger = normalizeMetaOperatorFollowThroughIndex(readJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex));
  const operatingState = {
    programs: normalizeProgramsIndex(readJson(root, ARTIFACT_PATHS.programsIndex, createProgramsIndex)),
    programRuns: normalizeProgramRunsIndex(readJson(root, ARTIFACT_PATHS.programRuns, createProgramRunsIndex)),
    programApprovals: normalizeProgramApprovalsIndex(readJson(root, ARTIFACT_PATHS.programApprovals, createProgramApprovalsIndex))
  };
  const actionRequiredItems = (ledger.items ?? []).filter((item) => {
    const status = item.status;
    const authorityState = followThroughAuthorityState(item, operatingState);
    const invalidStatus = Boolean(item.invalidStatus) || !["acknowledged", "accepted-for-execution", "executing", "deferred", "accepted-risk", "closed", "superseded"].includes(status) || !authorityState.trusted;
    const dueDeferred = status === "deferred" && item.deferUntil && String(item.deferUntil) <= nowIso();
    const targetBound = !["accepted-for-execution", "executing", "closed"].includes(status) ? true : targetArtifactContainsId(root, item.linkedTargetArtifact, item.linkedTargetId);
    const acceptedExecutionOpen = status === "accepted-for-execution" || status === "executing";
    return invalidStatus || Boolean(item.stale) || dueDeferred || !targetBound || acceptedExecutionOpen || status === "accepted-risk";
  }).map((item) => item.id);
  if (actionRequiredItems.length > 0) {
    throw new Error(`${actionLabel} is blocked while operator follow-through still requires action: ${actionRequiredItems.join(", ")}.`);
  }
}
function assertGovernanceMutationRegistered(actionId, expectedMode) {
  const guarded = new Map(GOVERNANCE_GUARDED_MUTATIONS.map((entry) => [entry.id, entry]));
  const exempt = new Map(GOVERNANCE_EXEMPT_MUTATIONS.map((entry) => [entry.id, entry]));
  if (expectedMode === "guarded") {
    if (!guarded.has(actionId)) {
      throw new Error(`Governance registry missing guarded mutation entry: ${actionId}`);
    }
    return;
  }
  if (expectedMode === "exempt") {
    const entry = exempt.get(actionId);
    if (!entry) {
      throw new Error(`Governance registry missing exempt mutation entry: ${actionId}`);
    }
    if (entry.sunsetAt && entry.sunsetAt <= nowIso()) {
      throw new Error(`Governance exempt entry expired: ${actionId}`);
    }
    return;
  }
  throw new Error(`Unknown governance mutation mode: ${expectedMode}`);
}

// src/core/i18n.mjs
import fs5 from "node:fs";
import path6 from "node:path";

// src/core/config.mjs
import fs4 from "node:fs";
import os from "node:os";
import path5 from "node:path";
var DEFAULT_TIMEOUT_MS = 12e4;
var DEFAULT_MAX_PROMPT_CHARS = 2e4;
var DEFAULT_MAX_SVG_BYTES = 1e6;
var DEFAULT_GLOBAL_STATUS_ORIGIN_PORT = 8787;
var DEFAULT_NETWORK_SEARCH_TIMEOUT_MS = 12e3;
var DEFAULT_NETWORK_SEARCH_MAX_RESULTS = 8;
var GPT_IMAGE2_PROVIDER_ID = "gpt-image2";
var GPT_IMAGE2_MODEL = "gpt-image-2";
var OPENAI_IMAGE_ENDPOINT = "https://api.openai.com/v1/images/generations";
var SECRET_KEY_PATTERN = /(?:api[-_]?key|token|secret|password|authorization|bearer)/i;
var INLINE_BEARER_VALUE_PATTERN = /\bBearer\s+[A-Za-z0-9._~+/=-]+/i;
var ENV_REF_PATTERN = /^[A-Z_][A-Z0-9_]*$/;
var LOOPBACK_HOSTS = /* @__PURE__ */ new Set(["127.0.0.1", "localhost", "::1"]);
var HOSTNAME_LABEL_PATTERN = /^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$/i;
var TUNNEL_NAME_PATTERN = /^[A-Za-z0-9_.-]+$/;
var NETWORK_SEARCH_CREDENTIAL_KEY_PATTERN = /(?:api[-_]?key|token|secret|password|authorization|bearer|credential|headers?)/i;
var DEFAULT_NETWORK_SEARCH_PROVIDER_IDS = ["openalex", "crossref", "arxiv", "europe-pmc"];
var DEFAULT_DOVE_CONFIG = {
  version: 1,
  language: DEFAULT_DOVE_RESPONSE_LANGUAGE,
  figureGeneration: {
    defaultProviderId: null,
    providers: [],
    maxPromptChars: DEFAULT_MAX_PROMPT_CHARS,
    maxSvgBytes: DEFAULT_MAX_SVG_BYTES
  },
  networkSearch: {
    enabled: true,
    defaultProviderIds: DEFAULT_NETWORK_SEARCH_PROVIDER_IDS,
    disabledProviderIds: [],
    providerSettings: {},
    timeoutMs: DEFAULT_NETWORK_SEARCH_TIMEOUT_MS,
    maxResults: DEFAULT_NETWORK_SEARCH_MAX_RESULTS
  },
  globalStatus: {
    outputDir: null,
    projects: [],
    auth: {
      enabled: false,
      username: "dove",
      password: null,
      passwordEnv: null
    },
    cloudflare: {
      enabled: false,
      domain: null,
      tunnelName: null,
      cloudflaredPath: "cloudflared",
      originHost: "127.0.0.1",
      originPort: DEFAULT_GLOBAL_STATUS_ORIGIN_PORT,
      configPath: null,
      credentialsFile: null,
      tokenEnv: null,
      dnsResolverAddrs: []
    }
  }
};
function isPlainObject(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function clone(value) {
  return structuredClone(value);
}
function normalizePositiveInteger(value, fallback, min, max) {
  const numeric = typeof value === "number" ? value : Number(value);
  if (!Number.isFinite(numeric)) {
    return fallback;
  }
  return Math.min(max, Math.max(min, Math.trunc(numeric)));
}
function normalizeBoolean2(value, fallback = false) {
  if (typeof value === "boolean") {
    return value;
  }
  if (typeof value === "string") {
    const normalized = value.trim().toLowerCase();
    if (["1", "true", "yes", "on"].includes(normalized)) {
      return true;
    }
    if (["0", "false", "no", "off"].includes(normalized)) {
      return false;
    }
  }
  return fallback;
}
function normalizeString2(value) {
  return typeof value === "string" && value.trim().length > 0 ? value.trim() : null;
}
function normalizeStringArray3(value) {
  const rawItems = Array.isArray(value) ? value : typeof value === "string" ? value.split(",") : [];
  return Array.from(new Set(rawItems.map((item) => normalizeString2(item)).filter(Boolean)));
}
function isGptImage2Identifier(value) {
  const normalized = normalizeString2(value)?.toLowerCase().replace(/[-_]/g, "") ?? null;
  return normalized === "gptimage2";
}
function inferFigureProviderType(providerType, providerId, endpoint, command, model) {
  const explicitType = normalizeString2(providerType);
  if (explicitType) {
    return explicitType;
  }
  if (isGptImage2Identifier(providerId) || isGptImage2Identifier(model)) {
    return "openai-image";
  }
  return endpoint ? "http-json" : command ? "external-command" : "external-command";
}
function expandHomePath(value) {
  const normalized = normalizeString2(value);
  if (!normalized) {
    return null;
  }
  if (normalized === "~") {
    return os.homedir();
  }
  if (normalized.startsWith("~/")) {
    return path5.join(os.homedir(), normalized.slice(2));
  }
  return normalized;
}
function resolveAbsolutePath(value) {
  const expanded = expandHomePath(value);
  return expanded ? path5.resolve(expanded) : null;
}
function resolveDoveGlobalStatusOutputDir(value = null, env = process.env) {
  const explicit = resolveAbsolutePath(value);
  if (explicit) {
    return explicit;
  }
  const xdgDataHome = resolveAbsolutePath(env.XDG_DATA_HOME);
  return path5.join(xdgDataHome ?? path5.join(os.homedir(), ".local", "share"), "dove", "public");
}
function normalizeEnvRef(value) {
  const normalized = normalizeString2(value);
  if (!normalized) {
    return null;
  }
  if (!ENV_REF_PATTERN.test(normalized)) {
    throw new Error(`Dove config env secret reference must be an environment variable name: ${normalized}`);
  }
  return normalized;
}
function isAllowedSecretReference(key) {
  return /Env$/.test(key) || /EnvVar$/.test(key);
}
function isAllowedInlineSecretPath(configPath) {
  return configPath === "globalStatus.auth.password" || configPath.endsWith(".globalStatus.auth.password");
}
function assertNoInlineSecrets(value, configPath = "config") {
  if (typeof value === "string") {
    if (INLINE_BEARER_VALUE_PATTERN.test(value)) {
      throw new Error(`Dove config must not contain inline bearer credentials at ${configPath}; use an env-var reference instead.`);
    }
    return;
  }
  if (Array.isArray(value)) {
    value.forEach((item, index) => assertNoInlineSecrets(item, `${configPath}[${index}]`));
    return;
  }
  if (!isPlainObject(value)) {
    return;
  }
  for (const [key, item] of Object.entries(value)) {
    const nextPath = `${configPath}.${key}`;
    if (SECRET_KEY_PATTERN.test(key) && !isAllowedSecretReference(key) && !isAllowedInlineSecretPath(nextPath)) {
      throw new Error(`Dove config must not contain inline secret field ${nextPath}; use an env-var reference such as apiKeyEnv instead.`);
    }
    assertNoInlineSecrets(item, nextPath);
  }
}
function assertNoNetworkSearchCredentials(value, configPath = "networkSearch") {
  if (Array.isArray(value)) {
    value.forEach((item, index) => assertNoNetworkSearchCredentials(item, `${configPath}[${index}]`));
    return;
  }
  if (!isPlainObject(value)) {
    return;
  }
  for (const [key, item] of Object.entries(value)) {
    const nextPath = `${configPath}.${key}`;
    if (NETWORK_SEARCH_CREDENTIAL_KEY_PATTERN.test(key)) {
      throw new Error(`Dove networkSearch supports only public no-key providers and must not contain credential field ${nextPath}.`);
    }
    assertNoNetworkSearchCredentials(item, nextPath);
  }
}
function readOptionalJsonFile(filePath) {
  if (!filePath || !fs4.existsSync(filePath)) {
    return null;
  }
  try {
    return JSON.parse(fs4.readFileSync(filePath, "utf8"));
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    throw new Error(`Failed to read Dove config ${filePath}: ${message}`);
  }
}
function mergeConfig(base, override) {
  if (!isPlainObject(override)) {
    return clone(base);
  }
  const next = clone(base);
  for (const [key, value] of Object.entries(override)) {
    if (Array.isArray(value)) {
      next[key] = clone(value);
      continue;
    }
    if (isPlainObject(value) && isPlainObject(next[key])) {
      next[key] = mergeConfig(next[key], value);
      continue;
    }
    next[key] = clone(value);
  }
  return next;
}
function configPaths(root, env) {
  if (normalizeString2(env.DOVE_CONFIG_PATH)) {
    return [path5.resolve(env.DOVE_CONFIG_PATH)];
  }
  const paths = [];
  if (normalizeString2(env.XDG_CONFIG_HOME)) {
    paths.push(path5.join(env.XDG_CONFIG_HOME, "dove", "config.json"));
  }
  paths.push(path5.join(os.homedir(), ".config", "dove", "config.json"));
  paths.push(path5.join(root, ".dove", "config.json"));
  paths.push(path5.join(root, ".dove", "config.local.json"));
  return Array.from(new Set(paths));
}
function envConfig(env) {
  const language = normalizeString2(env.DOVE_LANGUAGE ?? env.DOVE_RESPONSE_LANGUAGE);
  const providerId = normalizeString2(env.DOVE_FIGURE_PROVIDER_ID ?? env.DOVE_FIGURE_PROVIDER);
  const providerType = normalizeString2(env.DOVE_FIGURE_PROVIDER_TYPE);
  const endpoint = normalizeString2(env.DOVE_FIGURE_ENDPOINT);
  const command = normalizeString2(env.DOVE_FIGURE_COMMAND);
  const model = normalizeString2(env.DOVE_FIGURE_MODEL);
  const apiKeyEnv = normalizeString2(env.DOVE_FIGURE_API_KEY_ENV);
  const hasProviderOverride = Boolean(providerId || providerType || endpoint || command || model || apiKeyEnv);
  const figureGeneration = {};
  const next = {};
  if (language) {
    next.language = language;
  }
  if (providerId) {
    figureGeneration.defaultProviderId = providerId;
  }
  if (env.DOVE_FIGURE_MAX_PROMPT_CHARS) {
    figureGeneration.maxPromptChars = env.DOVE_FIGURE_MAX_PROMPT_CHARS;
  }
  if (env.DOVE_FIGURE_MAX_SVG_BYTES) {
    figureGeneration.maxSvgBytes = env.DOVE_FIGURE_MAX_SVG_BYTES;
  }
  if (hasProviderOverride) {
    const id = providerId ?? (isGptImage2Identifier(model) ? GPT_IMAGE2_PROVIDER_ID : "env-figure-provider");
    const type = inferFigureProviderType(providerType, id, endpoint, command, model);
    figureGeneration.defaultProviderId = id;
    figureGeneration.providers = [{
      id,
      type,
      endpoint,
      command,
      model: model ?? (type === "openai-image" ? GPT_IMAGE2_MODEL : null),
      apiKeyEnv: apiKeyEnv ?? (type === "openai-image" ? "OPENAI_API_KEY" : null),
      imageSize: env.DOVE_FIGURE_IMAGE_SIZE,
      imageQuality: env.DOVE_FIGURE_IMAGE_QUALITY,
      imageBackground: env.DOVE_FIGURE_IMAGE_BACKGROUND,
      timeoutMs: env.DOVE_FIGURE_TIMEOUT_MS,
      maxPromptChars: env.DOVE_FIGURE_MAX_PROMPT_CHARS,
      maxSvgBytes: env.DOVE_FIGURE_MAX_SVG_BYTES
    }];
  }
  if (Object.keys(figureGeneration).length > 0) {
    next.figureGeneration = figureGeneration;
  }
  return Object.keys(next).length > 0 ? next : null;
}
function normalizeProvider(rawProvider) {
  if (!isPlainObject(rawProvider)) {
    throw new Error("Dove figure provider config entries must be objects.");
  }
  assertNoInlineSecrets(rawProvider, "figureGeneration.providers[]");
  const id = normalizeString2(rawProvider.id);
  if (!id) {
    throw new Error("Dove figure provider config requires a non-empty id.");
  }
  const type = inferFigureProviderType(rawProvider.type, id, rawProvider.endpoint, rawProvider.command, rawProvider.model);
  if (!["http-json", "external-command", "openai-image"].includes(type)) {
    throw new Error(`Unsupported Dove figure provider type: ${type}`);
  }
  const provider = {
    id,
    type,
    timeoutMs: normalizePositiveInteger(rawProvider.timeoutMs, DEFAULT_TIMEOUT_MS, 1e3, 6e5),
    maxPromptChars: normalizePositiveInteger(rawProvider.maxPromptChars, DEFAULT_MAX_PROMPT_CHARS, 1e3, 2e5),
    maxSvgBytes: normalizePositiveInteger(rawProvider.maxSvgBytes, DEFAULT_MAX_SVG_BYTES, 1e3, 1e7)
  };
  const model = normalizeString2(rawProvider.model) ?? (type === "openai-image" ? GPT_IMAGE2_MODEL : null);
  if (model) {
    provider.model = model;
  }
  const endpoint = normalizeString2(rawProvider.endpoint) ?? (type === "openai-image" ? OPENAI_IMAGE_ENDPOINT : null);
  if (endpoint) {
    provider.endpoint = endpoint;
  }
  const command = normalizeString2(rawProvider.command);
  if (command) {
    provider.command = command;
  }
  const apiKeyEnv = normalizeEnvRef(rawProvider.apiKeyEnv ?? (type === "openai-image" ? "OPENAI_API_KEY" : null));
  if (apiKeyEnv) {
    provider.apiKeyEnv = apiKeyEnv;
  }
  const imageSize = normalizeString2(rawProvider.imageSize ?? rawProvider.size);
  if (imageSize) {
    provider.imageSize = imageSize;
  }
  const imageQuality = normalizeString2(rawProvider.imageQuality ?? rawProvider.quality);
  if (imageQuality) {
    provider.imageQuality = imageQuality;
  }
  const imageBackground = normalizeString2(rawProvider.imageBackground ?? rawProvider.background);
  if (imageBackground) {
    provider.imageBackground = imageBackground;
  }
  if ((type === "http-json" || type === "openai-image") && !provider.endpoint) {
    throw new Error(`Dove figure provider ${id} uses ${type} but has no endpoint.`);
  }
  if (type === "external-command" && !provider.command) {
    throw new Error(`Dove figure provider ${id} uses external-command but has no command.`);
  }
  return provider;
}
function createGptImage2FigureProvider(overrides = {}) {
  const source = isPlainObject(overrides) ? overrides : {};
  return normalizeProvider({
    id: GPT_IMAGE2_PROVIDER_ID,
    type: "openai-image",
    endpoint: OPENAI_IMAGE_ENDPOINT,
    model: GPT_IMAGE2_MODEL,
    apiKeyEnv: "OPENAI_API_KEY",
    imageSize: "1024x1024",
    ...source
  });
}
function normalizeNetworkProviderIds(value, fallback = []) {
  const ids = normalizeStringArray3(value).map((item) => item.toLowerCase());
  return ids.length > 0 ? ids : [...fallback];
}
function normalizeNetworkProviderSettings(rawSettings = {}) {
  const source = isPlainObject(rawSettings) ? rawSettings : {};
  assertNoNetworkSearchCredentials(source, "networkSearch.providerSettings");
  const settings = {};
  for (const [rawId, rawValue] of Object.entries(source)) {
    const id = normalizeString2(rawId)?.toLowerCase();
    if (!id) {
      continue;
    }
    const providerSource = isPlainObject(rawValue) ? rawValue : {};
    const providerSettings = {};
    if (providerSource.enabled !== void 0) {
      providerSettings.enabled = normalizeBoolean2(providerSource.enabled, true);
    }
    if (providerSource.timeoutMs !== void 0) {
      providerSettings.timeoutMs = normalizePositiveInteger(providerSource.timeoutMs, DEFAULT_NETWORK_SEARCH_TIMEOUT_MS, 1e3, 6e4);
    }
    settings[id] = providerSettings;
  }
  return settings;
}
function normalizeNetworkSearchConfig(rawConfig = {}) {
  const source = isPlainObject(rawConfig) ? rawConfig : {};
  assertNoNetworkSearchCredentials(source, "networkSearch");
  return {
    enabled: normalizeBoolean2(source.enabled, true),
    defaultProviderIds: normalizeNetworkProviderIds(source.defaultProviderIds ?? source.defaultProviders, DEFAULT_NETWORK_SEARCH_PROVIDER_IDS),
    disabledProviderIds: normalizeNetworkProviderIds(source.disabledProviderIds ?? source.disabledProviders, []),
    providerSettings: normalizeNetworkProviderSettings(source.providerSettings),
    timeoutMs: normalizePositiveInteger(source.timeoutMs, DEFAULT_NETWORK_SEARCH_TIMEOUT_MS, 1e3, 6e4),
    maxResults: normalizePositiveInteger(source.maxResults ?? source.limit, DEFAULT_NETWORK_SEARCH_MAX_RESULTS, 1, 50)
  };
}
function normalizeFigureGenerationConfig(rawConfig = {}) {
  const source = isPlainObject(rawConfig) ? rawConfig : {};
  assertNoInlineSecrets(source, "figureGeneration");
  const hasExplicitProviders = Array.isArray(source.providers);
  const providers = hasExplicitProviders ? source.providers.map(normalizeProvider) : [];
  const defaultProviderId = normalizeString2(source.defaultProviderId) ?? (hasExplicitProviders ? providers[0]?.id : null) ?? null;
  if (defaultProviderId && !providers.some((provider) => provider.id === defaultProviderId) && !isGptImage2Identifier(defaultProviderId)) {
    throw new Error(`Dove figureGeneration.defaultProviderId references unknown provider: ${defaultProviderId}`);
  }
  return {
    defaultProviderId,
    providers,
    maxPromptChars: normalizePositiveInteger(source.maxPromptChars, DEFAULT_MAX_PROMPT_CHARS, 1e3, 2e5),
    maxSvgBytes: normalizePositiveInteger(source.maxSvgBytes, DEFAULT_MAX_SVG_BYTES, 1e3, 1e7)
  };
}
function normalizeGlobalStatusProject(rawProject) {
  const source = typeof rawProject === "string" ? { root: rawProject } : rawProject;
  if (!isPlainObject(source)) {
    return null;
  }
  if (source.enabled === false) {
    return null;
  }
  const root = resolveAbsolutePath(source.root ?? source.path ?? source.workspace ?? source.workspaceRoot);
  if (!root) {
    return null;
  }
  return {
    root,
    id: normalizeString2(source.id),
    slug: normalizeString2(source.slug),
    title: normalizeString2(source.title ?? source.name),
    enabled: true
  };
}
function normalizeGlobalStatusProjects(rawProjects = []) {
  const projects = Array.isArray(rawProjects) ? rawProjects : [];
  const byRoot = /* @__PURE__ */ new Map();
  for (const rawProject of projects) {
    const project = normalizeGlobalStatusProject(rawProject);
    if (!project) {
      continue;
    }
    byRoot.set(project.root, project);
  }
  return Array.from(byRoot.values());
}
function normalizeHostname(value, label) {
  const normalized = normalizeString2(value);
  if (!normalized) {
    return null;
  }
  if (/^[a-z][a-z0-9+.-]*:\/\//i.test(normalized) || normalized.includes("/") || normalized.includes(":")) {
    throw new Error(`Dove global status Cloudflare ${label} must be a bare hostname: ${normalized}`);
  }
  const hostname = normalized.toLowerCase();
  if (hostname.length > 253 || hostname.startsWith(".") || hostname.endsWith(".")) {
    throw new Error(`Dove global status Cloudflare ${label} must be a valid hostname: ${normalized}`);
  }
  const labels = hostname.split(".");
  if (labels.length < 2 || !labels.every((item) => HOSTNAME_LABEL_PATTERN.test(item))) {
    throw new Error(`Dove global status Cloudflare ${label} must be a valid hostname: ${normalized}`);
  }
  return hostname;
}
function normalizeLoopbackHost(value) {
  const normalized = normalizeString2(value) ?? "127.0.0.1";
  if (!LOOPBACK_HOSTS.has(normalized)) {
    throw new Error(`Dove global status serving host must be loopback-only: ${normalized}`);
  }
  return normalized;
}
function normalizeTunnelName(value) {
  const normalized = normalizeString2(value);
  if (!normalized) {
    return null;
  }
  if (!TUNNEL_NAME_PATTERN.test(normalized)) {
    throw new Error(`Dove global status Cloudflare tunnelName must contain only letters, digits, dots, dashes, or underscores: ${normalized}`);
  }
  return normalized;
}
function normalizeDnsResolverAddrs(value) {
  const rawItems = Array.isArray(value) ? value : value === void 0 || value === null ? [] : [value];
  const items = rawItems.map(normalizeString2).filter(Boolean);
  for (const item of items) {
    if (!item.includes(":") || item.includes("://") || item.includes("/") || /\s/.test(item)) {
      throw new Error(`Dove global status Cloudflare dnsResolverAddrs entries must be address:port values: ${item}`);
    }
  }
  return Array.from(new Set(items));
}
function isPathInside(childPath2, parentPath) {
  const relative = path5.relative(parentPath, childPath2);
  return relative === "" || !relative.startsWith("..") && !path5.isAbsolute(relative);
}
function assertOutsidePublicDir(filePath, outputDir, label) {
  if (!filePath || !outputDir) {
    return;
  }
  if (isPathInside(filePath, outputDir)) {
    throw new Error(`Dove global status Cloudflare ${label} must not be inside the public output directory.`);
  }
}
function normalizeGlobalStatusCloudflareConfig(rawConfig = {}, rawGlobalStatus = {}, outputDir = null) {
  const source = isPlainObject(rawConfig) ? rawConfig : {};
  assertNoInlineSecrets(source, "globalStatus.cloudflare");
  const enabled = normalizeBoolean2(source.enabled ?? rawGlobalStatus.cloudflareEnabled, false);
  const domain = normalizeHostname(source.domain ?? source.hostname ?? rawGlobalStatus.cloudflareDomain ?? rawGlobalStatus.cloudflareHostname, "domain");
  const tunnelName = normalizeTunnelName(source.tunnelName ?? rawGlobalStatus.cloudflareTunnelName);
  const cloudflaredPath = normalizeString2(source.cloudflaredPath ?? source.command ?? rawGlobalStatus.cloudflaredPath) ?? "cloudflared";
  const originHost = normalizeLoopbackHost(source.originHost ?? source.host ?? rawGlobalStatus.cloudflareOriginHost);
  const originPort = normalizePositiveInteger(source.originPort ?? source.port ?? rawGlobalStatus.cloudflareOriginPort, DEFAULT_GLOBAL_STATUS_ORIGIN_PORT, 1, 65535);
  const configPath = resolveAbsolutePath(source.configPath ?? source.configFile ?? rawGlobalStatus.cloudflareConfigPath);
  const credentialsFile = resolveAbsolutePath(source.credentialsFile ?? rawGlobalStatus.cloudflareCredentialsFile);
  const tokenEnv = normalizeEnvRef(source.tokenEnv ?? rawGlobalStatus.cloudflareTokenEnv);
  const dnsResolverAddrs = normalizeDnsResolverAddrs(source.dnsResolverAddrs ?? rawGlobalStatus.cloudflareDnsResolverAddrs);
  assertOutsidePublicDir(configPath, outputDir, "configPath");
  assertOutsidePublicDir(credentialsFile, outputDir, "credentialsFile");
  return {
    enabled,
    domain,
    tunnelName,
    cloudflaredPath,
    originHost,
    originPort,
    configPath,
    credentialsFile,
    tokenEnv,
    dnsResolverAddrs
  };
}
function normalizeGlobalStatusAuthConfig(rawConfig = {}, rawGlobalStatus = {}) {
  const source = isPlainObject(rawConfig) ? rawConfig : {};
  assertNoInlineSecrets(source, "globalStatus.auth");
  return {
    enabled: normalizeBoolean2(source.enabled ?? rawGlobalStatus.authEnabled, false),
    username: normalizeString2(source.username ?? source.user ?? rawGlobalStatus.authUsername ?? rawGlobalStatus.authUser) ?? "dove",
    password: normalizeString2(source.password ?? rawGlobalStatus.authPassword),
    passwordEnv: normalizeEnvRef(source.passwordEnv ?? source.passwordEnvVar ?? rawGlobalStatus.authPasswordEnv ?? rawGlobalStatus.authPasswordEnvVar)
  };
}
function normalizeGlobalStatusConfig(rawConfig = {}) {
  const source = isPlainObject(rawConfig) ? rawConfig : {};
  assertNoInlineSecrets(source, "globalStatus");
  const outputDir = resolveAbsolutePath(source.outputDir ?? source.publicDir ?? source.directory);
  return {
    outputDir,
    projects: normalizeGlobalStatusProjects(source.projects),
    auth: normalizeGlobalStatusAuthConfig(source.auth, source),
    cloudflare: normalizeGlobalStatusCloudflareConfig(source.cloudflare, source, outputDir)
  };
}
function normalizeDoveConfig(rawConfig) {
  const source = isPlainObject(rawConfig) ? rawConfig : {};
  assertNoInlineSecrets(source, "doveConfig");
  const publicStatusSource = isPlainObject(source.publicStatus) ? source.publicStatus : {};
  const globalStatusSource = isPlainObject(source.globalStatus) ? source.globalStatus : {};
  const globalStatusOutputDir = globalStatusSource.outputDir ?? globalStatusSource.publicDir ?? globalStatusSource.directory ?? publicStatusSource.outputDir ?? publicStatusSource.publicDir ?? publicStatusSource.directory;
  const globalStatusProjects = Array.isArray(globalStatusSource.projects) && globalStatusSource.projects.length > 0 ? globalStatusSource.projects : publicStatusSource.projects;
  return {
    version: 1,
    language: normalizeDoveResponseLanguage(source.language ?? source.responseLanguage, DEFAULT_DOVE_RESPONSE_LANGUAGE, { strict: true }),
    figureGeneration: normalizeFigureGenerationConfig(source.figureGeneration),
    networkSearch: normalizeNetworkSearchConfig(source.networkSearch),
    globalStatus: normalizeGlobalStatusConfig({
      ...globalStatusSource,
      outputDir: globalStatusOutputDir,
      projects: globalStatusProjects
    })
  };
}
function loadDoveConfig(root, env = process.env) {
  let config = clone(DEFAULT_DOVE_CONFIG);
  for (const configPath of configPaths(root, env)) {
    const fileConfig = readOptionalJsonFile(configPath);
    if (fileConfig) {
      assertNoInlineSecrets(fileConfig, configPath);
      config = mergeConfig(config, fileConfig);
    }
  }
  const environmentConfig = envConfig(env);
  if (environmentConfig) {
    config = mergeConfig(config, environmentConfig);
  }
  return normalizeDoveConfig(config);
}
function loadFigureGenerationConfig(root, env = process.env) {
  return loadDoveConfig(root, env).figureGeneration;
}
function loadNetworkSearchConfig(root, env = process.env) {
  return loadDoveConfig(root, env).networkSearch;
}
function loadExplicitDoveLanguageConfig(root, env = process.env) {
  let language = null;
  for (const configPath of configPaths(root, env)) {
    const fileConfig = readOptionalJsonFile(configPath);
    if (isPlainObject(fileConfig) && (fileConfig.language !== void 0 || fileConfig.responseLanguage !== void 0)) {
      language = fileConfig.language ?? fileConfig.responseLanguage;
    }
  }
  const environmentLanguage = normalizeString2(env.DOVE_LANGUAGE ?? env.DOVE_RESPONSE_LANGUAGE);
  if (environmentLanguage) {
    language = environmentLanguage;
  }
  return language ? normalizeDoveResponseLanguage(language, DEFAULT_DOVE_RESPONSE_LANGUAGE, { strict: true }) : null;
}
function loadDoveLanguageConfig(root, env = process.env) {
  return loadDoveConfig(root, env).language;
}
function redactDoveConfig(config) {
  if (typeof config === "string") {
    return config.replace(INLINE_BEARER_VALUE_PATTERN, "Bearer <redacted>");
  }
  if (Array.isArray(config)) {
    return config.map((item) => redactDoveConfig(item));
  }
  if (!isPlainObject(config)) {
    return config;
  }
  const redacted = {};
  for (const [key, value] of Object.entries(config)) {
    if (SECRET_KEY_PATTERN.test(key) && !isAllowedSecretReference(key)) {
      redacted[key] = "<redacted>";
      continue;
    }
    redacted[key] = redactDoveConfig(value);
  }
  return redacted;
}

// src/core/i18n.mjs
function isPlainObject2(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function firstExplicitLanguage(...sources) {
  for (const source of sources) {
    if (!isPlainObject2(source)) {
      continue;
    }
    const value = source.responseLanguage ?? source.language;
    if (typeof value === "string" && value.trim()) {
      return value;
    }
  }
  return null;
}
function readStateLanguage(root) {
  if (!root) {
    return null;
  }
  const statePath = path6.join(root, ARTIFACT_PATHS.state);
  if (!fs5.existsSync(statePath)) {
    return null;
  }
  try {
    const state = normalizeState(JSON.parse(fs5.readFileSync(statePath, "utf8")));
    return state.settings?.responseLanguage ?? null;
  } catch {
    return null;
  }
}
function resolveDoveResponseLanguage(root, args = {}, options = {}) {
  const argLanguage = firstExplicitLanguage(args, args.settings);
  if (argLanguage) {
    return normalizeDoveResponseLanguage(argLanguage, DEFAULT_DOVE_RESPONSE_LANGUAGE, { strict: true });
  }
  const env = options.env ?? process.env;
  const configLanguage = options.configLanguage ?? loadExplicitDoveLanguageConfig(root, env);
  if (configLanguage) {
    return configLanguage;
  }
  const stateLanguage = options.state?.settings?.responseLanguage ?? readStateLanguage(root);
  if (stateLanguage) {
    return normalizeDoveResponseLanguage(stateLanguage, DEFAULT_DOVE_RESPONSE_LANGUAGE);
  }
  return loadDoveLanguageConfig(root, env);
}
var TEXT = {
  taskFallbackTitle: {
    zh: "Dove \u4EFB\u52A1",
    en: "Dove task"
  },
  executePlannedWork: {
    zh: "\u6267\u884C\u5DF2\u89C4\u5212\u7684 Dove \u5DE5\u4F5C",
    en: "Execute planned Dove work"
  },
  checklistItem: {
    zh: ({ index }) => `\u68C0\u67E5\u9879 ${index}`,
    en: ({ index }) => `Checklist item ${index}`
  },
  checklistClarifyTitle: {
    zh: "\u6F84\u6E05\u8303\u56F4\u4E0E\u9A8C\u6536\u8BC1\u636E",
    en: "Clarify scope and acceptance evidence"
  },
  checklistClarifySummary: {
    zh: "\u786E\u8BA4\u4EFB\u52A1\u8303\u56F4\u3001\u963B\u585E\u9879\u3001\u76EE\u6807\u4EA7\u7269\u548C\u8BC1\u636E\u671F\u671B\u3002",
    en: "Confirm the task scope, blockers, artifact targets, and evidence expectations."
  },
  checklistWorkTitle: {
    zh: "\u6267\u884C\u6838\u5FC3\u4EFB\u52A1\u5DE5\u4F5C",
    en: "Perform the core mission work"
  },
  checklistWorkSummary: {
    zh: "\u901A\u8FC7\u5408\u9002\u7684\u9876\u5C42\u5DE5\u4F5C\u6D41\u5B8C\u6210\u5206\u7C7B\u540E\u7684 Dove \u4EFB\u52A1\u3002",
    en: "Carry out the classified Dove task through the appropriate top-level workflow."
  },
  checklistReviewTitle: {
    zh: "\u8FD0\u884C\u72EC\u7ACB\u5BA1\u67E5\u5E76\u8BB0\u5F55\u53D1\u73B0",
    en: "Run independent review and record findings"
  },
  checklistValidateTitle: {
    zh: "\u9A8C\u8BC1\u8BC1\u636E\u5E76\u8BB0\u5F55\u5B8C\u6210\u72B6\u6001",
    en: "Validate evidence and record completion state"
  },
  checklistValidateSummary: {
    zh: "\u5173\u95ED\u524D\u5BF9\u7167\u58F0\u660E\u7684\u8BC1\u636E\u671F\u671B\u68C0\u67E5\u4EA7\u7269\u3002",
    en: "Check the produced artifacts against the declared evidence expectations before closure."
  },
  initNextActionDisplay: {
    zh: "\u8FD0\u884C project:dove.mission\uFF0C\u5728 init \u76EE\u6807\u4E0B\u521B\u5EFA\u4E0B\u4E00\u4E2A\u4EFB\u52A1\u3002",
    en: "Run project:dove.mission to create the next task under the init goal."
  },
  createTaskConfirmMessage: {
    zh: "\u8BF7\u5148\u6279\u51C6\u8FD9\u4EFD\u9700\u6C42\u5230\u4EFB\u52A1\u7684\u4EFB\u52A1\u5951\u7EA6\uFF1B\u6279\u51C6\u540E Dove \u53EA\u4F1A\u7269\u5316\u5408\u540C\u5E76\u4EA4\u63A5\u63A8\u8350\u7684\u540E\u7EED\u6D41\u7A0B\u3002",
    en: "Approve this demand-to-task mission contract before Dove materializes it and hands off to the recommended next workflow."
  },
  createTaskMaterializedMessage: {
    zh: "\u5DF2\u6279\u51C6\u7684\u9700\u6C42\u5230\u4EFB\u52A1\u5951\u7EA6\u5DF2\u7ECF\u7269\u5316\u3002\u8BF7\u6CBF\u63A8\u8350\u8DEF\u7EBF\u7EE7\u7EED\u63A8\u8FDB\u771F\u5B9E\u5DE5\u4F5C\uFF1Bmission \u672C\u8EAB\u4E0D\u8BB0\u5F55\u6267\u884C\u7ED3\u679C\u3002",
    en: "The approved demand-to-task mission contract is materialized. Continue through the recommended route; mission itself does not record execution results."
  },
  createTaskMaterializationPlannedMessage: {
    zh: "\u5DF2\u751F\u6210\u6279\u51C6\u5408\u540C\u7684\u5F85\u5E94\u7528\u5199\u5165\u8BA1\u5212\uFF0C\u4F46\u5408\u540C\u5C1A\u672A\u7269\u5316\u3002\u8BF7\u7531 host \u5E94\u7528 mutation plan \u540E\u518D\u4EA4\u63A5\u540E\u7EED\u6D41\u7A0B\u3002",
    en: "A host-applied write plan was prepared for the approved contract, but the contract is not materialized yet. Apply the mutation plan before handing off."
  },
  workContractPurpose: {
    zh: ({ title, stage, domain }) => `\u628A\u201C${title}\u201D\u8F6C\u6210\u53EF\u6267\u884C\u3001\u53EF\u9A8C\u8BC1\u3001\u53EF\u7EED\u63A5\u7684 ${stage}/${domain} \u5DE5\u4F5C\u5408\u540C\u3002`,
    en: ({ title, stage, domain }) => `Turn \u201C${title}\u201D into an executable, verifiable, resumable ${stage}/${domain} work contract.`
  },
  workContractDeliverablesPlan: {
    zh: ({ title }) => [`${title} \u7684\u53EF\u6267\u884C\u8BA1\u5212\u548C\u62C6\u5206\u540E\u7684\u540E\u7EED\u4EFB\u52A1\u3002`, "\u660E\u786E\u8BC1\u636E\u7F3A\u53E3\u3001\u963B\u585E\u6761\u4EF6\u548C\u4E0B\u4E00\u6B65\u8DEF\u7EBF\u3002"],
    en: ({ title }) => [`An executable plan for ${title} with follow-up tasks.`, "Explicit evidence gaps, blockers, and next routes."]
  },
  workContractDeliverablesPaper: {
    zh: ({ title }) => [`${title} \u5BF9\u5E94\u7684\u8349\u7A3F\u3001\u6539\u5199\u6216\u8BBA\u6587\u4EA7\u7269\u3002`, "\u652F\u6491\u8BE5\u4EA7\u7269\u7684 source\u3001note\u3001claim\u3001figure \u6216 review \u8BC1\u636E\u5F15\u7528\u3002"],
    en: ({ title }) => [`A draft, revision, or paper artifact for ${title}.`, "Source, note, claim, figure, or review evidence references supporting the artifact."]
  },
  workContractDeliverablesExperiment: {
    zh: ({ title }) => [`${title} \u7684\u5B9E\u9A8C\u8BA1\u5212\u3001\u7ED3\u679C\u6216\u5BA1\u8BA1\u8BB0\u5F55\u3002`, "\u628A\u5B9E\u9A8C\u7ED3\u679C\u6865\u63A5\u5230 claim \u6216\u660E\u786E\u4E0D\u80FD\u6865\u63A5\u7684\u539F\u56E0\u3002"],
    en: ({ title }) => [`An experiment plan, result, or audit record for ${title}.`, "A bridge from result to claim, or an explicit reason it cannot be bridged."]
  },
  workContractDeliverablesAudit: {
    zh: ({ title }) => [`${title} \u7684\u72EC\u7ACB\u68C0\u67E5\u7ED3\u8BBA\u3002`, "\u53EF\u6267\u884C\u7684\u4FEE\u8BA2\u9879\u3001\u8BC1\u636E\u7F3A\u53E3\u6216\u963B\u585E\u8FB9\u754C\u3002"],
    en: ({ title }) => [`Independent review findings for ${title}.`, "Actionable revisions, evidence gaps, or blocker boundaries."]
  },
  workContractDeliverablesEngineering: {
    zh: ({ title }) => [`${title} \u5BF9\u5E94\u7684\u4EE3\u7801\u3001\u914D\u7F6E\u3001\u6587\u6863\u6216\u6D4B\u8BD5\u6539\u52A8\u3002`, "\u80FD\u590D\u73B0\u5B9E\u8D28\u8FDB\u5C55\u7684\u9A8C\u8BC1\u8BC1\u636E\u3002"],
    en: ({ title }) => [`Code, configuration, documentation, or test changes for ${title}.`, "Validation evidence that proves real progress."]
  },
  workContractEvidenceDefault: {
    zh: "\u5217\u51FA\u5B9E\u9645\u4FEE\u6539\u3001\u4EA7\u7269\u8DEF\u5F84\u3001\u6D4B\u8BD5/\u9A8C\u8BC1\u8F93\u51FA\uFF0C\u6216\u8BB0\u5F55\u65E0\u6CD5\u7EE7\u7EED\u7684\u660E\u786E\u8FB9\u754C\u3002",
    en: "List actual changes, artifact paths, test/validation output, or an explicit boundary that blocks continuation."
  },
  workContractDoneDefault: {
    zh: "\u4EA4\u4ED8\u7269\u5DF2\u7ECF\u4EA7\u51FA\uFF0C\u8BC1\u636E\u53EF\u4EE5\u88AB status/review \u8FFD\u8E2A\uFF0C\u4E14\u4E0B\u4E00\u6B65\u4E0D\u662F\u91CD\u65B0\u89E3\u91CA\u9700\u6C42\u3002",
    en: "Deliverables exist, evidence is traceable by status/review, and the next step is not re-explaining the demand."
  },
  workContractOutOfScopeDefault: {
    zh: ["\u4E0D\u865A\u6784\u5B9E\u9A8C\u3001\u8BBA\u6587\u3001review \u6216\u5DE5\u7A0B\u7ED3\u679C\u3002", "\u4E0D\u5728\u6CA1\u6709\u771F\u5B9E\u8BC1\u636E\u65F6\u628A\u4EFB\u52A1\u6807\u8BB0 completed\u3002", "\u4E0D\u542F\u52A8\u9690\u85CF\u540E\u53F0\u6267\u884C\u3001daemon \u6216 scheduler\u3002"],
    en: ["Do not fabricate experiment, paper, review, or engineering results.", "Do not mark the task completed without real evidence.", "Do not start hidden background execution, daemons, or schedulers."]
  },
  workContractImpactPlan: {
    zh: "\u628A\u6A21\u7CCA\u65B9\u5411\u5207\u6210\u53EF\u6267\u884C\u4EFB\u52A1\uFF0C\u540E\u7EED status \u80FD\u76F4\u63A5\u663E\u793A\u8BE5\u505A\u54EA\u4E00\u6B65\u3002",
    en: "Turns a vague direction into executable tasks so status can show the next concrete step."
  },
  workContractImpactPaper: {
    zh: "\u628A\u8BBA\u6587\u63A8\u8FDB\u7ED1\u5B9A\u5230\u53EF\u68C0\u67E5\u7684\u8349\u7A3F/\u8BC1\u636E\u4EA7\u7269\uFF0C\u51CF\u5C11\u53EA\u521B\u5EFA\u4EFB\u52A1\u4F46\u4E0D\u77E5\u9053\u600E\u4E48\u5199\u7684\u60C5\u51B5\u3002",
    en: "Binds paper progress to inspectable draft/evidence artifacts instead of creating a task with no writing path."
  },
  workContractImpactExperiment: {
    zh: "\u628A\u5B9E\u9A8C\u63A8\u8FDB\u7ED1\u5B9A\u5230\u8BA1\u5212\u3001\u7ED3\u679C\u3001\u5BA1\u8BA1\u548C claim \u6865\u63A5\uFF0C\u907F\u514D\u53EA\u8BB0\u5F55\u60F3\u6CD5\u3002",
    en: "Binds experiment progress to planning, results, audit, and claim bridging instead of only recording an idea."
  },
  workContractImpactAudit: {
    zh: "\u628A\u68C0\u67E5\u7ED3\u679C\u8F6C\u6210\u53EF\u4FEE\u8BA2\u3001\u53EF\u9A8C\u8BC1\u7684\u4E0B\u4E00\u6B65\uFF0C\u800C\u4E0D\u662F\u505C\u5728\u6CDB\u6CDB review\u3002",
    en: "Turns review into actionable, verifiable revision steps instead of generic feedback."
  },
  workContractImpactEngineering: {
    zh: "\u628A\u5DE5\u7A0B\u9700\u6C42\u7ED1\u5B9A\u5230\u6539\u52A8\u548C\u9A8C\u8BC1\u8BC1\u636E\uFF0Cstatus \u53EF\u4EE5\u7EE7\u7EED\u63A8\u8FDB\u800C\u4E0D\u662F\u53EA\u5C55\u793A\u4EFB\u52A1\u540D\u3002",
    en: "Binds engineering work to changes and validation evidence so status can continue the work instead of only showing a task name."
  },
  workContractRoutePrimaryLabel: {
    zh: "\u9996\u9009\u63A8\u8FDB\u8DEF\u7EBF",
    en: "Primary route"
  },
  workContractRoutePrimaryWhen: {
    zh: "\u9700\u8981\u6309\u5408\u540C\u4EA7\u51FA\u7B2C\u4E00\u6279\u771F\u5B9E\u4EA4\u4ED8\u7269\u548C\u8BC1\u636E\u65F6\u4F7F\u7528\u3002",
    en: "Use when producing the first real deliverables and evidence for the contract."
  },
  workContractRouteAutoLabel: {
    zh: "\u591A\u8F6E\u81EA\u52A8\u63A8\u8FDB",
    en: "Multi-round auto route"
  },
  workContractRouteAutoWhen: {
    zh: "\u5DF2\u6709\u4EFB\u52A1\u5408\u540C\uFF0C\u60F3\u8BA9 Dove \u5728\u524D\u53F0\u591A\u8F6E\u63A8\u8FDB\u76F4\u5230\u5B8C\u6210\u6216\u8FB9\u754C\u65F6\u4F7F\u7528\u3002",
    en: "Use when a task contract exists and Dove should run foreground iterations until completion or a boundary."
  },
  workContractRouteReviewLabel: {
    zh: "\u5BA1\u67E5/\u9A8C\u6536\u8DEF\u7EBF",
    en: "Review route"
  },
  workContractRouteReviewWhen: {
    zh: "\u9700\u8981\u72EC\u7ACB\u68C0\u67E5\u8BC1\u636E\u3001\u8349\u7A3F\u3001\u7ED3\u679C\u6216\u5B8C\u6210\u72B6\u6001\u65F6\u4F7F\u7528\u3002",
    en: "Use when evidence, drafts, results, or completion state need independent review."
  },
  missionPassSelectMessage: {
    zh: "\u8BB0\u5F55\u4EFB\u52A1\u6267\u884C\u7ED3\u679C\u524D\uFF0C\u8BF7\u5148\u901A\u8FC7 index \u6216 packetId \u9009\u62E9\u4E00\u4E2A\u6301\u4E45\u4EFB\u52A1\u3002",
    en: "Select a durable task by index or packetId before recording the mission pass."
  },
  missionPassCompletedSummary: {
    zh: "\u4EFB\u52A1\u6267\u884C\u5DF2\u5B8C\u6210\u3002",
    en: "Mission pass completed."
  },
  missionPassBlockedSummary: {
    zh: "\u4EFB\u52A1\u6267\u884C\u9047\u5230\u963B\u585E\u3002",
    en: "Mission pass reached a blocker."
  },
  missionPassProgressSummary: {
    zh: "\u4EFB\u52A1\u6267\u884C\u5DF2\u8BB0\u5F55\u8FDB\u5C55\u3002",
    en: "Mission pass recorded progress."
  },
  killSelectMessage: {
    zh: "\u6740\u6B7B\u4EFB\u52A1\u524D\uFF0C\u8BF7\u5148\u901A\u8FC7 index \u6216 packetId \u9009\u62E9\u4E00\u4E2A\u975E init \u4EFB\u52A1\u3002",
    en: "Select a non-init task by index or packetId before killing it."
  },
  killReason: {
    zh: "\u64CD\u4F5C\u8005\u6740\u6B7B\u4E86\u8BE5\u4EFB\u52A1\u3002",
    en: "Operator killed this task."
  },
  lifecycleKillReason: {
    zh: "\u64CD\u4F5C\u8005\u5C06\u8BE5\u4EFB\u52A1\u6807\u8BB0\u4E3A killed\u3002",
    en: "Operator marked this task killed."
  },
  statusAdjustConfirmMessage: {
    zh: "\u5728\u6301\u4E45\u4EFB\u52A1\u751F\u547D\u5468\u671F\u72B6\u6001\u53D8\u66F4\u524D\uFF0C\u8BF7\u786E\u8BA4\u9009\u4E2D\u7684 Dove \u4EFB\u52A1\u72B6\u6001\u8C03\u6574\u3002",
    en: "Confirm the selected Dove task status adjustments before durable task lifecycle state changes."
  },
  statusAdjustMissingReason: {
    zh: "\u6BCF\u4E2A\u8C03\u6574\u90FD\u9700\u8981 packetId \u548C\u4E00\u4E2A\u6709\u6548\u7684 Dove \u4EFB\u52A1\u72B6\u6001\u3002",
    en: "Each adjustment requires packetId and one valid Dove task status."
  },
  statusAdjustNoPacketReason: {
    zh: "\u6CA1\u6709\u6301\u4E45\u4EFB\u52A1\u5305\u5339\u914D\u8BE5 packetId\u3002",
    en: "No durable task packet matches this packetId."
  },
  statusAdjustInitReason: {
    zh: "level-0 init \u4EFB\u52A1\u4E0D\u80FD\u901A\u8FC7\u72B6\u6001 UX \u8C03\u6574\u3002",
    en: "The level-0 init task is not adjusted through status UX."
  },
  statusAdjustSameReason: {
    zh: "\u4EFB\u52A1\u5DF2\u7ECF\u5904\u4E8E\u8BF7\u6C42\u7684\u72B6\u6001\u3002",
    en: "Task already has the requested status."
  },
  blockerTitle: {
    zh: ({ title }) => `\u8C03\u67E5 ${title} \u7684\u963B\u585E\u539F\u56E0`,
    en: ({ title }) => `Investigate blocker for ${title}`
  },
  blockerSummary: {
    zh: ({ title }) => `\u627E\u51FA\u5E76\u89E3\u91CA\u963B\u6B62 ${title} \u7EE7\u7EED\u63A8\u8FDB\u7684\u539F\u56E0\u3002`,
    en: ({ title }) => `Find and explain the blocker preventing ${title} from advancing.`
  },
  blockerEvidenceWhy: {
    zh: ({ id }) => `\u89E3\u91CA ${id} \u4E3A\u4EC0\u4E48\u88AB\u963B\u585E`,
    en: ({ id }) => `Explain why ${id} is blocked`
  },
  blockerEvidenceAction: {
    zh: "\u5EFA\u8BAE\u4E0B\u4E00\u72B6\u6001\u6216\u5177\u4F53\u89E3\u9664\u963B\u585E\u52A8\u4F5C",
    en: "Recommend the next state or concrete unblock action"
  },
  operatorConfirmMessage: {
    zh: "\u8BF7\u5148\u786E\u8BA4\uFF0C\u7136\u540E Dove operator \u53EA\u4F1A\u8FD0\u884C\u5B89\u5168\u5185\u90E8\u6B65\u9AA4\u3001\u8BB0\u5F55\u4F60\u663E\u5F0F\u63D0\u4F9B\u7684 host \u7ED3\u679C\uFF0C\u6216\u4E3A\u963B\u585E\u4EFB\u52A1\u521B\u5EFA\u8C03\u67E5\u8BA1\u5212\uFF1B\u7F3A\u5C11 host \u7ED3\u679C\u7684\u4EFB\u52A1\u4F1A\u4FDD\u6301\u4E0D\u53D8\u5E76\u8FD4\u56DE\u6240\u9700\u8BC1\u636E\u3002",
    en: "Confirm before Dove operator runs only safe internal steps, records explicitly supplied host results, or creates blocker-investigation plan missions; missions missing host results remain unchanged and return required evidence."
  },
  operatorAwaitingStopReason: {
    zh: "\u672A\u63D0\u4F9B\u4E3B\u673A\u4FA7\u4EFB\u52A1\u7ED3\u679C\uFF0C\u56E0\u6B64 Dove \u4E0D\u58F0\u79F0\u5DF2\u7ECF\u6267\u884C\u3002",
    en: "No host-supplied task result was provided, so Dove did not claim execution."
  },
  operatorResultSummary: {
    zh: "\u5DF2\u8BB0\u5F55 operator \u6267\u884C\u7ED3\u679C\u3002",
    en: "Operator pass result recorded."
  },
  versionReason: {
    zh: "\u65B9\u5411\u5DF2\u53D8\u66F4\u3002",
    en: "Direction changed."
  },
  versionTitle: {
    zh: "Dove \u65B9\u5411\u91CD\u7F6E",
    en: "Dove direction reset"
  },
  versionNextActionDisplay: {
    zh: "\u8FD0\u884C project:dove.mission \u5904\u7406\u4E0B\u4E00\u4E2A\u65B9\u5411\u3002",
    en: "Run project:dove.mission for the next direction."
  },
  autoSelectionConfirmMessage: {
    zh: "\u8BF7\u5148\u6279\u51C6\u9009\u4E2D\u7684\u6301\u4E45\u4EFB\u52A1\uFF0C\u7136\u540E /dove:auto \u624D\u4F1A\u8FD0\u884C\u6709\u8FB9\u754C\u7684\u524D\u53F0\u8FED\u4EE3\u3002",
    en: "Approve the selected durable task before /dove:auto runs bounded foreground iterations."
  },
  autoDemandConfirmMessage: {
    zh: "\u8BF7\u5148\u6279\u51C6\u8FD9\u4EFD\u9700\u6C42\u5230\u4EFB\u52A1\u7684 auto \u5951\u7EA6\uFF0C\u7136\u540E Dove \u624D\u4F1A\u5C06\u5176\u7269\u5316\u5E76\u8FD0\u884C\u6709\u8FB9\u754C\u7684\u524D\u53F0\u8FED\u4EE3\u3002",
    en: "Approve this demand-to-task auto contract before Dove materializes it and runs bounded foreground iterations."
  },
  autoSelectExistingMessage: {
    zh: "\u5728 /dove:auto \u8FD0\u884C\u524D\uFF0C\u8BF7\u4F7F\u7528\u786E\u8BA4 UX \u901A\u8FC7 index \u6216 packetId \u9009\u62E9\u4E00\u4E2A\u73B0\u6709\u6301\u4E45\u4EFB\u52A1\u3002",
    en: "Use confirmation UX to select an existing durable task by index or packetId before /dove:auto runs."
  },
  autoSelectOrDemandMessage: {
    zh: "\u5728 /dove:auto \u8FD0\u884C\u524D\uFF0C\u8BF7\u4F7F\u7528\u786E\u8BA4 UX \u9009\u62E9\u4E00\u4E2A\u6301\u4E45\u4EFB\u52A1\uFF0C\u6216\u63D0\u4F9B\u4E00\u4E2A\u65B0\u9700\u6C42\u3002",
    en: "Use confirmation UX to select a durable task or provide a new demand before /dove:auto runs."
  },
  autoSelectExistingConfirmedMessage: {
    zh: "\u5728 /dove:auto \u8FD0\u884C\u524D\uFF0C\u8BF7\u901A\u8FC7 index \u6216 packetId \u9009\u62E9\u4E00\u4E2A\u73B0\u6709\u6301\u4E45\u4EFB\u52A1\u3002",
    en: "Select an existing durable task by index or packetId before /dove:auto runs."
  },
  autoProvideTargetMessage: {
    zh: "\u5728 /dove:auto \u8FD0\u884C\u524D\uFF0C\u8BF7\u63D0\u4F9B\u4EFB\u52A1\u76EE\u6807\u6216\u65B0\u76EE\u6807\u3002",
    en: "Provide a task target or a new goal before /dove:auto runs."
  },
  autoNoStepStopReason: {
    zh: "\u672C\u6B21 auto \u8FED\u4EE3\u672A\u63D0\u4F9B\u5177\u4F53\u4E14\u5B89\u5168\u7684\u5DE5\u4F5C\u6D41\u6B65\u9AA4\u3002",
    en: "No concrete safe workflow step was supplied for this auto iteration."
  },
  durableContextNoticeSummary: {
    zh: "Dove \u7684 .dove \u72B6\u6001\u662F\u9879\u76EE\u5185\u6587\u4EF6\u7CFB\u7EDF\u4E8B\u5B9E\u6E90\uFF1B\u8981\u8BA9\u8FD9\u4E9B\u5DE5\u4F5C\u6D41\u4EA7\u7269\u6709\u8D44\u683C\u968F Claude \u6216\u5176\u4ED6\u7F16\u7A0B\u7EC8\u7AEF\u56DE\u6EDA\uFF0C\u5E94\u4F7F\u7528 mutationMode: patch-plan \u5E76\u7531 host \u7684\u53D7\u8FFD\u8E2A\u6587\u4EF6\u7F16\u8F91\u673A\u5236\u5E94\u7528\uFF1BCLI/MCP direct-process \u5199\u5165\u4E0D\u88AB\u58F0\u660E\u4E3A\u5DF2\u9A8C\u8BC1\u53EF\u56DE\u6EDA\u3002",
    en: "Dove .dove state is an in-project filesystem source of truth; to make those workflow artifacts eligible for Claude or other programming-terminal rollback, use mutationMode: patch-plan and apply it through host-tracked file edits. CLI/MCP direct-process writes are not declared verified rollback-safe."
  },
  durableContextNoticeRecovery: {
    zh: "\u5982\u679C host \u56DE\u6EDA\u540E .dove \u6CA1\u6709\u4E00\u8D77\u56DE\u6EDA\uFF0C\u4E0D\u8981\u4F7F\u7528 reset_dove_version \u5F53\u6062\u590D\u5165\u53E3\uFF1B\u6539\u7528 patch-plan \u8BA9 host tracked edits \u5E94\u7528\u540E\u518D\u4F9D\u8D56 host \u56DE\u6EDA\u3002direct-process \u4ECD\u53EF\u7528\u4E8E\u529F\u80FD\u4F18\u5148\u5199\u5165\uFF0C\u4F46\u56DE\u6EDA\u8986\u76D6\u4FDD\u6301 unverified\u3002",
    en: "If .dove did not roll back with the host rollback, do not use reset_dove_version as a restore path; use patch-plan so host tracked edits apply the files before relying on host rollback. direct-process remains available for functional writes, but rollback coverage stays unverified."
  },
  statusHomeInitTitle: {
    zh: "\u5148\u521B\u5EFA Dove \u9879\u76EE\u76EE\u6807",
    en: "Create the Dove project goal first"
  },
  statusHomeInitWhy: {
    zh: "\u5F53\u524D\u5DE5\u4F5C\u533A\u8FD8\u6CA1\u6709 level-0 init \u76EE\u6807\uFF0C\u540E\u7EED\u4EFB\u52A1\u9700\u8981\u5148\u6302\u5230\u8FD9\u4E2A\u6839\u76EE\u6807\u4E0B\u3002",
    en: "The workspace has no level-0 init goal yet, and later work needs that root."
  },
  statusHomeCreateMissionTitle: {
    zh: "\u521B\u5EFA\u4E0B\u4E00\u4E2A\u5177\u4F53 mission",
    en: "Create the next concrete mission"
  },
  statusHomeCreateMissionWhy: {
    zh: "\u5F53\u524D\u6CA1\u6709\u9700\u8981\u7EE7\u7EED\u63A8\u8FDB\u7684\u6D3B\u8DC3\u4EFB\u52A1\uFF0C\u4E0B\u4E00\u6B65\u5E94\u4ECE\u4E00\u4E2A\u771F\u5B9E\u9700\u6C42\u5F00\u59CB\u3002",
    en: "There is no active task to continue, so the next step should start from a real demand."
  },
  statusHomeBoundaryTitle: {
    zh: ({ title }) => `\u5904\u7406 ${title} \u7684\u7B49\u5F85\u8FB9\u754C`,
    en: ({ title }) => `Resolve waiting boundary for ${title}`
  },
  statusHomeBoundaryWhy: {
    zh: ({ reason }) => reason || "\u4EFB\u52A1\u505C\u5728\u4E00\u4E2A\u9700\u8981\u8865\u8F93\u5165\u3001\u8BC1\u636E\u6216\u4EBA\u5DE5\u52A8\u4F5C\u7684\u8FB9\u754C\u3002",
    en: ({ reason }) => reason || "The task is stopped at a boundary that needs input, evidence, or operator action."
  },
  statusHomeContinuationTitle: {
    zh: ({ title }) => `\u7EE7\u7EED ${title}`,
    en: ({ title }) => `Continue ${title}`
  },
  statusHomeContinuationWhy: {
    zh: "runtime \u91CC\u6709\u660E\u786E\u7684\u524D\u53F0\u7EED\u8DD1\u7EBF\u7D22\uFF0C\u53EF\u4EE5\u4ECE\u8FD9\u91CC\u6062\u590D\u3002",
    en: "Runtime state has an explicit foreground continuation hint for this task."
  },
  statusHomeBlockedTitle: {
    zh: ({ title }) => `\u89E3\u9664 ${title} \u7684\u963B\u585E`,
    en: ({ title }) => `Unblock ${title}`
  },
  statusHomeBlockedWhy: {
    zh: ({ reason }) => reason || "\u4EFB\u52A1\u5F53\u524D\u5E26\u6709\u963B\u585E\u4FE1\u53F7\uFF0C\u9700\u8981\u5148\u89E3\u91CA\u6216\u89E3\u9664\u963B\u585E\u3002",
    en: ({ reason }) => reason || "The task currently has a blocker signal that needs explanation or removal."
  },
  statusHomeReviewTitle: {
    zh: "\u5904\u7406\u5F85\u5BA1\u67E5\u95EE\u9898",
    en: "Handle pending review issues"
  },
  statusHomeReviewWhy: {
    zh: "\u5F53\u524D review state \u91CC\u8FD8\u6709\u672A\u89E3\u51B3\u5173\u6CE8\u70B9\u3002",
    en: "The current review state still has unresolved concerns."
  },
  statusHomeContinueTitle: {
    zh: ({ title }) => `\u63A8\u8FDB ${title}`,
    en: ({ title }) => `Advance ${title}`
  },
  statusHomeContinueWhy: {
    zh: "\u4EFB\u52A1\u5DF2\u6709\u4E0B\u4E00\u6B65\u547D\u4EE4\uFF0C\u53EF\u4EE5\u7EE7\u7EED\u4E00\u6B21\u524D\u53F0\u63A8\u8FDB\u3002",
    en: "The task already has a next command and can continue with one foreground step."
  },
  statusHomeReconcileTitle: {
    zh: ({ count }) => `\u6838\u5BF9 ${count} \u4E2A done \u7236 mission \u7684 checklist \u4E00\u81F4\u6027`,
    en: ({ count }) => `Reconcile checklist consistency for ${count} done parent mission${count === 1 ? "" : "s"}`
  },
  statusHomeReconcileWhy: {
    zh: "\u5DF2\u6709\u7236 mission \u662F done\uFF0C\u4F46\u4E0B\u9762\u8FD8\u6709 open checklist \u5B50\u9879\uFF1B\u5148\u6838\u5BF9\u8FD9\u4E9B\u5B50\u9879\u662F\u5426\u88AB\u7236\u4EFB\u52A1\u8BC1\u636E\u8986\u76D6\uFF0C\u8986\u76D6\u624D\u9010\u9879\u6807 done\uFF0C\u5426\u5219\u5E94\u91CD\u5F00\u7236\u4EFB\u52A1\u3002",
    en: "A parent mission is done while checklist children remain open; verify whether the parent evidence covers each child, mark covered children done, or reopen the parent."
  },
  statusHomeReconcileDoneCriteria: {
    zh: "\u6BCF\u4E2A open checklist \u5B50\u9879\u90FD\u6709\u5BF9\u5E94\u8BC1\u636E\uFF0C\u6216\u7236 mission \u88AB\u9000\u56DE open \u72B6\u6001\u7EE7\u7EED\u5904\u7406\u3002",
    en: "Each open checklist child has matching evidence, or the parent mission is moved back to an open state for continued work."
  },
  boundaryActionContinueLabel: {
    zh: "\u7EE7\u7EED/\u6062\u590D\u524D\u53F0\u6267\u884C",
    en: "Continue or resume foreground work"
  },
  boundaryActionEvidenceLabel: {
    zh: "\u8865\u771F\u5B9E\u7ED3\u679C\u6216\u8BC1\u636E",
    en: "Provide real result or evidence"
  },
  boundaryActionReviewLabel: {
    zh: "\u9001\u72EC\u7ACB review",
    en: "Send to independent review"
  },
  boundaryActionStatusLabel: {
    zh: "\u8C03\u6574\u4EFB\u52A1\u72B6\u6001",
    en: "Adjust task status"
  },
  boundaryActionKillLabel: {
    zh: "\u901A\u8FC7 status \u6807\u8BB0 killed",
    en: "Mark killed through status"
  },
  compactCardScope: {
    zh: ({ stage, domain, status }) => `${stage}/${domain} \xB7 ${status}`,
    en: ({ stage, domain, status }) => `${stage}/${domain} \xB7 ${status}`
  },
  compactCardNoEvidence: {
    zh: "\u5C1A\u672A\u5217\u51FA\u8BC1\u636E\u8981\u6C42\u3002",
    en: "No evidence requirement is listed yet."
  },
  compactCardNoAutomaticExecution: {
    zh: "\u4E0D\u4F1A\u81EA\u52A8\u6267\u884C\uFF1B\u9700\u8981\u663E\u5F0F\u786E\u8BA4\u3002",
    en: "No automatic execution; explicit confirmation is required."
  },
  compactCardFirstActionFallback: {
    zh: "\u786E\u8BA4\u540E\u6267\u884C\u4E0B\u4E00\u6B65\u524D\u53F0\u52A8\u4F5C\u3002",
    en: "After confirmation, run the next foreground action."
  },
  compactCardBoundaryFallback: {
    zh: "\u5982\u679C\u7F3A\u5C11\u771F\u5B9E\u8BC1\u636E\uFF0CDove \u4F1A\u8BB0\u5F55\u8FB9\u754C\u800C\u4E0D\u662F\u58F0\u79F0\u5B8C\u6210\u3002",
    en: "If real evidence is missing, Dove records a boundary instead of claiming completion."
  },
  resultCardHappenedFallback: {
    zh: "\u5DF2\u8BB0\u5F55\u672C\u6B21 Dove \u547D\u4EE4\u7ED3\u679C\u3002",
    en: "Recorded this Dove command result."
  },
  resultCardNoEvidence: {
    zh: "\u672C\u6B21\u7ED3\u679C\u6CA1\u6709\u8BB0\u5F55\u663E\u5F0F\u8BC1\u636E\u3002",
    en: "No explicit evidence was recorded for this result."
  },
  resultCardNoValidation: {
    zh: "\u672C\u6B21\u7ED3\u679C\u6CA1\u6709\u8BB0\u5F55\u663E\u5F0F\u9A8C\u8BC1\u8F93\u51FA\u3002",
    en: "No explicit validation output was recorded for this result."
  },
  resultCardCodeNotInspected: {
    zh: "\u672C\u6B21\u5DE5\u5177\u6CA1\u6709\u68C0\u67E5\u4EE3\u7801\u6539\u52A8\uFF1B\u4E0D\u8981\u4ECE\u672C\u5361\u7247\u63A8\u65AD\u4EE3\u7801\u6587\u4EF6\u3002",
    en: "This tool did not inspect code changes; do not infer changed code files from this card."
  },
  resultCardNoDurableWrites: {
    zh: "\u672C\u6B21\u7ED3\u679C\u6CA1\u6709\u58F0\u660E\u65B0\u7684\u6301\u4E45\u5199\u5165\u3002",
    en: "This result did not declare new durable writes."
  },
  resultCardDurableWritesHidden: {
    zh: "\u5DF2\u66F4\u65B0 Dove \u8BB0\u5F55\uFF1B\u5B8C\u6574\u7EC6\u8282\u53EF\u5C55\u5F00\u67E5\u770B\u3002",
    en: "Dove records were updated; expand for full details."
  },
  resultCardEvidenceHidden: {
    zh: "\u5DF2\u8BB0\u5F55\u8BC1\u636E\u6307\u9488\uFF1B\u9ED8\u8BA4\u6458\u8981\u9690\u85CF\u5185\u90E8\u8DEF\u5F84\u3002",
    en: "Evidence pointers were recorded; internal paths are hidden in the default summary."
  },
  resultCardValidationHidden: {
    zh: "\u5DF2\u8BB0\u5F55\u9A8C\u8BC1\u8F93\u51FA\uFF1B\u9ED8\u8BA4\u6458\u8981\u9690\u85CF\u5185\u90E8\u8DEF\u5F84\u3002",
    en: "Validation output was recorded; internal paths are hidden in the default summary."
  },
  resultCardDetailsAvailable: {
    zh: "\u9700\u8981\u5B8C\u6574\u7EC6\u8282\u65F6\u518D\u5C55\u5F00 full/debug\u3002",
    en: "Expand full/debug only when full details are needed."
  },
  resultCardRuntimeResultRecorded: {
    zh: "\u5DF2\u8BB0\u5F55 runtime result\u3002",
    en: "Runtime result recorded."
  },
  resultCardTaskPacketUpdated: {
    zh: "\u5DF2\u66F4\u65B0\u4EFB\u52A1 packet\u3002",
    en: "Task packet updated."
  },
  resultCardTaskIndexUpdated: {
    zh: "\u5DF2\u66F4\u65B0 task packet index\u3002",
    en: "Task packet index updated."
  },
  resultCardRuntimeEventRecorded: {
    zh: "\u5DF2\u8BB0\u5F55 runtime/lifecycle event\u3002",
    en: "Runtime/lifecycle event recorded."
  },
  resultCardReviewInputPrepared: {
    zh: "\u5DF2\u51C6\u5907\u9694\u79BB review \u8F93\u5165\u5305\u3002",
    en: "Isolated review input bundle prepared."
  },
  resultCardReviewImported: {
    zh: "\u5DF2\u5BFC\u5165\u9694\u79BB\u5BA1\u6838\u7ED3\u679C\u3002",
    en: "Isolated review result imported."
  },
  resultCardNextStatus: {
    zh: "\u67E5\u770B Dove status\u3002",
    en: "Open Dove status."
  },
  resultCardNextProvideEvidence: {
    zh: "\u8865\u771F\u5B9E\u7ED3\u679C\u6216\u8BC1\u636E\u3002",
    en: "Provide real result or evidence."
  },
  resultCardNextImportReview: {
    zh: "\u5BFC\u5165\u5BA1\u6838\u7ED3\u679C\u3002",
    en: "Import the review result."
  },
  resultCardNextAdjustStatus: {
    zh: "\u901A\u8FC7 status \u8C03\u6574\u4EFB\u52A1\u72B6\u6001\u3002",
    en: "Adjust task status through status."
  },
  evidenceResolutionNoArtifacts: {
    zh: "\u672C\u6B21\u6CA1\u6709\u63D0\u4F9B\u7528\u4E8E\u9009\u62E9\u4EFB\u52A1\u7684 artifact/evidence \u8DEF\u5F84\u3002",
    en: "No artifact/evidence path was provided for task selection."
  },
  evidenceResolutionNoSelectedPacket: {
    zh: "\u672C\u6B21\u6CA1\u6709\u9009\u4E2D\u53EF\u63A5\u6536\u8BC1\u636E\u7684\u6301\u4E45\u4EFB\u52A1\u3002",
    en: "No durable task was selected to receive evidence."
  },
  evidenceResolutionAcceptedSelf: {
    zh: "\u8BC1\u636E\u5DF2\u7ECF\u5C5E\u4E8E\u5F53\u524D\u4EFB\u52A1\uFF0C\u53EF\u4EE5\u76F4\u63A5\u8BB0\u5F55\u5230\u8BE5\u4EFB\u52A1\u3002",
    en: "The evidence already belongs to the selected task and can be recorded there."
  },
  evidenceResolutionAcceptedDescendant: {
    zh: "\u8BC1\u636E\u5C5E\u4E8E\u5F53\u524D\u4EFB\u52A1\u7684\u5B50\u4EFB\u52A1\uFF0C\u53EF\u4EE5\u5F52\u5165\u8FD9\u4E2A\u7236\u4EFB\u52A1\u7ED3\u679C\u3002",
    en: "The evidence belongs to a descendant task and can be rolled up into this parent result."
  },
  evidenceResolutionNoExistingOwner: {
    zh: "\u63D0\u4F9B\u7684\u8BC1\u636E\u5C1A\u672A\u88AB\u5176\u4ED6\u4EFB\u52A1\u58F0\u660E\uFF1B\u672C\u6B21\u4F1A\u5F52\u5165\u5F53\u524D\u9009\u4E2D\u7684\u4EFB\u52A1\u3002",
    en: "The provided evidence is not claimed by another task; this pass can attach it to the selected task."
  },
  evidenceResolutionConflict: {
    zh: "\u63D0\u4F9B\u7684\u8BC1\u636E\u5DF2\u88AB\u5176\u4ED6\u4EFB\u52A1\u58F0\u660E\uFF1B\u9700\u8981\u9009\u62E9\u6B63\u786E packet\uFF0C\u6216\u6539\u7528\u5F53\u524D\u4EFB\u52A1/\u5B50\u4EFB\u52A1\u7684\u8BC1\u636E\u3002",
    en: "The provided evidence is already claimed by another task; choose the correct packet or use evidence from the selected task/descendants."
  },
  resultCardHandoffResolveBoundary: {
    zh: ({ ownerRole }) => `\u7531 ${ownerRole || "\u5F53\u524D\u89D2\u8272"} \u5904\u7406\u5F53\u524D\u8FB9\u754C\uFF0C\u8865\u9F50\u8F93\u5165\u6216\u8BC1\u636E\u540E\u518D\u56DE\u5230 status\u3002`,
    en: ({ ownerRole }) => `${ownerRole || "the current role"} should resolve the current boundary, provide required input/evidence, then return to status.`
  },
  resultCardHandoffRoleTransfer: {
    zh: ({ ownerRole, nextRole }) => `\u4ECE ${ownerRole || "\u5F53\u524D\u89D2\u8272"} \u4EA4\u63A5\u7ED9 ${nextRole || "\u4E0B\u4E00\u89D2\u8272"} \u5904\u7406\u5F53\u524D\u8FB9\u754C\u3002`,
    en: ({ ownerRole, nextRole }) => `Hand off from ${ownerRole || "the current role"} to ${nextRole || "the next role"} to resolve the current boundary.`
  },
  resultCardHandoffProvideEvidence: {
    zh: "\u9700\u8981\u8865\u771F\u5B9E\u6267\u884C\u7ED3\u679C\u6216\u8BC1\u636E\uFF1BDove \u4E0D\u4F1A\u5047\u88C5\u5DF2\u7ECF\u5B8C\u6210\u3002",
    en: "Real execution results or evidence are required; Dove will not pretend the work is complete."
  },
  resultCardHandoffImportReview: {
    zh: "\u7B49\u5F85 reviewer \u8F93\u51FA\u5BA1\u6838\u7ED3\u679C\u6216\u62A5\u544A\u540E\u5BFC\u5165\uFF1B\u4E0D\u8981\u5BFC\u5165 reviewer \u79C1\u6709\u8BB0\u5F55\u3002",
    en: "Wait for the reviewer result or report, then import it; do not import reviewer private notes."
  },
  resultCardHandoffAddressReview: {
    zh: "review \u5DF2\u8FD4\u56DE\u9700\u8981\u5904\u7406\u7684\u95EE\u9898\uFF1B\u4E0B\u4E00\u6B65\u5E94\u521B\u5EFA\u6216\u63A8\u8FDB\u4FEE\u590D\u4EFB\u52A1\u3002",
    en: "Review returned issues to address; next create or advance a fix task."
  },
  resultCardNextCreateFixMission: {
    zh: "\u521B\u5EFA\u6216\u63A8\u8FDB\u4FEE\u590D mission\u3002",
    en: "Create or advance a fix mission."
  },
  defaultDoveTitle: {
    zh: "\u672A\u547D\u540D\u4EFB\u52A1\u5DE5\u4F5C\u533A",
    en: "Untitled Mission Workspace"
  },
  defaultDoveObjective: {
    zh: "\u8BB0\u5F55 Dove \u4EFB\u52A1\u76EE\u6807\u4E0E\u8D21\u732E\u3002",
    en: "Capture the Dove mission goal and contribution."
  },
  defaultDoveThesis: {
    zh: "\u7528\u4E00\u53E5\u8BDD\u63CF\u8FF0\u8BBA\u6587\u9886\u57DF\u4E3B\u5F20\u6216\u4EFB\u52A1\u7ED3\u679C\u3002",
    en: "Describe the paper-domain claim or mission outcome in one sentence."
  },
  defaultCurrentFocus: {
    zh: "\u5BF9\u9F50\u770B\u677F\u5E76\u9009\u62E9\u4E0B\u4E00\u4E2A\u6301\u4E45\u6B65\u9AA4\u3002",
    en: "Align the board and choose the next durable step."
  },
  queryFallbackGoal: {
    zh: "\u4ECE\u5F53\u524D\u5DE5\u4F5C\u533A\u754C\u5B9A\u4E00\u4E2A\u6709\u8FB9\u754C\u7684 Dove \u4EFB\u52A1\u3002",
    en: "Frame one bounded Dove mission from the current workspace."
  },
  returnProtocol: {
    zh: ({ checks }) => `\u8FD4\u56DE\u65F6\u643A\u5E26 ${checks}\u3002`,
    en: ({ checks }) => `Return with ${checks}.`
  },
  routeSelectedReason: {
    zh: ({ selectedCommand, nextCommand }) => `\u4E3A\u4E86\u5F97\u5230\u786E\u5B9A\u6027\u7684\u3001\u65E0\u5199\u5165\u7684 Dove \u8DEF\u7531\u7ED3\u679C\uFF0C\u4ECE\u9886\u57DF\u8DEF\u7531 ${nextCommand} \u4E2D\u9009\u62E9\u4E86 ${selectedCommand}\u3002`,
    en: ({ selectedCommand, nextCommand }) => `Selected ${selectedCommand} from domain route ${nextCommand} for a deterministic no-write Dove routing result.`
  },
  routeAutoReason: {
    zh: "\u672C\u6B21 Dove \u8DEF\u7531\u67E5\u8BE2\u5DF2\u663E\u5F0F\u5141\u8BB8\u81EA\u52A8\u6267\u884C\u3002",
    en: "Auto execution was explicitly allowed for this Dove routing query."
  },
  routeDefaultReason: {
    zh: ({ stage, domain, selectedCommand }) => `\u9886\u57DF ${domain} \u7684\u4EFB\u52A1\u9636\u6BB5 ${stage} \u6620\u5C04\u5230 ${selectedCommand}\u3002`,
    en: ({ stage, domain, selectedCommand }) => `Mission stage ${stage} in domain ${domain} maps to ${selectedCommand}.`
  },
  projectTitleFallback: {
    zh: "Dove \u9879\u76EE",
    en: "Dove project"
  },
  boardBlockerFallback: {
    zh: ({ index }) => `\u770B\u677F\u963B\u585E ${index}`,
    en: ({ index }) => `Board blocker ${index}`
  },
  taskBlockedBySummary: {
    zh: ({ id, blockers }) => `${id} \u88AB ${blockers} \u963B\u585E`,
    en: ({ id, blockers }) => `${id} is blocked by ${blockers}`
  },
  taskMarkedBlockedSummary: {
    zh: ({ id }) => `${id} \u88AB\u6807\u8BB0\u4E3A blocked`,
    en: ({ id }) => `${id} is marked blocked`
  },
  returnReadyVerdict: {
    zh: "\u57FA\u4E8E\u5F53\u524D\u6301\u4E45\u8BC1\u636E\uFF0C\u8FD4\u56DE\u5DF2\u5C31\u7EEA\u3002",
    en: "Return is ready from the current durable evidence."
  },
  returnNotReadyVerdict: {
    zh: "\u8FD4\u56DE\u5C1A\u672A\u5C31\u7EEA\uFF1B\u5173\u95ED\u524D\u8BF7\u5148\u6267\u884C\u4E0B\u4E00\u4E2A Dove \u547D\u4EE4\u3002",
    en: "Return is not ready; follow the next Dove command before closure."
  },
  lessonRitualWhen: {
    zh: "\u8FD4\u56DE\u5DF2\u5C31\u7EEA\u4E14\u6709\u503C\u5F97\u4FDD\u7559\u7684\u53EF\u590D\u7528\u7ECF\u9A8C\u4E4B\u540E",
    en: "after return is ready and reusable experience is worth preserving"
  }
};
function doveText(language, key, params = {}) {
  const normalized = normalizeDoveResponseLanguage(language);
  const entry = TEXT[key];
  const value = entry?.[normalized] ?? entry?.en ?? key;
  return typeof value === "function" ? value(params) : value;
}

// src/core/navigation.mjs
import crypto5 from "node:crypto";
import fs6 from "node:fs";
import path8 from "node:path";

// src/core/program-operating-state.mjs
function readProgramOperatingState(root) {
  return {
    programs: normalizeProgramsIndex(
      readJson(
        root,
        ARTIFACT_PATHS.programsIndex,
        createProgramsIndex
      )
    ),
    programRuns: normalizeProgramRunsIndex(
      readJson(
        root,
        ARTIFACT_PATHS.programRuns,
        createProgramRunsIndex
      )
    ),
    programApprovals: normalizeProgramApprovalsIndex(
      readJson(
        root,
        ARTIFACT_PATHS.programApprovals,
        createProgramApprovalsIndex
      )
    )
  };
}

// src/core/task-packets.mjs
import crypto4 from "node:crypto";
import path7 from "node:path";
function slugify(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function normalizeStringArray4(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return value.filter((item) => typeof item === "string").map((item) => item.trim()).filter(Boolean);
}
function normalizeObject2(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function uniqueStrings(values = []) {
  return Array.from(new Set(values.filter((item) => typeof item === "string").map((item) => item.trim()).filter(Boolean)));
}
function normalizeAllowed(value, allowed, fallback) {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return allowed.includes(normalized) ? normalized : fallback;
}
function normalizeLevel(value, fallback = 3) {
  return Number.isFinite(value) ? Math.max(0, Math.floor(value)) : fallback;
}
function normalizeTaskStatus(value, fallback = "pending") {
  return normalizeAllowed(value, DOVE_TASK_STATUSES, fallback);
}
function normalizePrimaryRole(value, fallback = "builder") {
  return normalizeAllowed(value, DOVE_PRIMARY_ROLE_IDS, fallback);
}
function readJsonReadOnly(root, relativePath, fallback = null) {
  return readJson(root, relativePath, fallback);
}
function packetFilePath(packetId) {
  return path7.join(ARTIFACT_PATHS.taskPacketsPacketsDir, `${packetId}.json`);
}
function packetContextPath(packetId) {
  return path7.join(ARTIFACT_PATHS.packetContextsDir, `${packetId}.json`);
}
function readOptionalJson(root, relativePath) {
  try {
    return readJsonReadOnly(root, relativePath, null);
  } catch {
    return null;
  }
}
function normalizeTaskPacketId(value) {
  return slugify(value);
}
function deterministicBoundedTaskPacketId(prefix, identity, maxIdLength = 160) {
  const normalizedPrefix = normalizeTaskPacketId(prefix);
  const normalizedIdentity = normalizeTaskPacketId(identity);
  const digest = crypto4.createHash("sha256").update(`${normalizedPrefix}\0${normalizedIdentity}`).digest("hex").slice(0, 12);
  const boundedPrefix = normalizedPrefix.slice(0, 72).replace(/-+$/u, "") || "task";
  const availableIdentityLength = Math.max(1, maxIdLength - boundedPrefix.length - digest.length - 2);
  return normalizeTaskPacketId(`${boundedPrefix}-${normalizedIdentity.slice(0, availableIdentityLength)}-${digest}`);
}
function readTaskTargetResolutionSettings(root) {
  const state = readJsonReadOnly(root, ARTIFACT_PATHS.state, createDefaultState);
  return normalizeSettings(state.settings).taskTargetResolution;
}
function normalizePacketCandidate(root, packet = {}) {
  const id = normalizeTaskPacketId(packet.id ?? packet.packetId ?? packet.sourceId ?? packet.title);
  const filePacket = readOptionalJson(root, packet.packetPath ?? packetFilePath(id));
  const context = readOptionalJson(root, packet.packetContextPath ?? packetContextPath(id));
  const merged = { ...packet, ...normalizeObject2(filePacket) };
  const contextObject = normalizeObject2(context);
  const creatorKind = normalizeAllowed(merged.creatorKind ?? contextObject.creatorKind, DOVE_TASK_CREATOR_KINDS, "user");
  const levelFallback = creatorKind === "system" ? 2 : 3;
  const level = normalizeLevel(merged.level ?? contextObject.level, levelFallback);
  const status = normalizeTaskStatus(merged.status ?? merged.lifecycleStatus ?? contextObject.status, "pending");
  return {
    ...merged,
    id,
    title: merged.title ?? id,
    summary: merged.summary ?? "",
    parentId: merged.parentId ?? contextObject.parentId ?? null,
    rootId: merged.rootId ?? contextObject.rootId ?? (level === 0 ? id : null),
    level,
    creatorKind,
    stage: normalizeAllowed(merged.stage ?? merged.missionStage ?? contextObject.stage, DOVE_TASK_STAGES, "plan"),
    domain: normalizeAllowed(merged.domain ?? merged.doveDomain ?? contextObject.domain, DOVE_TASK_DOMAINS, "engineering"),
    status,
    lifecycleStatus: merged.lifecycleStatus ?? status,
    dependencies: uniqueStrings([...normalizeStringArray4(merged.dependencies), ...normalizeStringArray4(contextObject.dependencies)]),
    blockedBy: uniqueStrings([...normalizeStringArray4(merged.blockedBy), ...normalizeStringArray4(contextObject.blockedBy)]),
    completedAt: merged.completedAt ?? contextObject.completedAt ?? null,
    blockedReason: merged.blockedReason ?? merged.blockerReason ?? contextObject.blockedReason ?? contextObject.blockerReason ?? null,
    killedAt: merged.killedAt ?? contextObject.killedAt ?? null,
    killReason: merged.killReason ?? contextObject.killReason ?? null,
    ownerRole: normalizePrimaryRole(merged.ownerRole ?? contextObject.ownerRole, "builder"),
    nextRole: normalizePrimaryRole(merged.nextRole ?? contextObject.nextRole, merged.ownerRole ?? contextObject.ownerRole ?? "builder"),
    boundary: normalizeDoveBoundary(merged.boundary ?? contextObject.boundary, null),
    boundaryHistory: Array.isArray(merged.boundaryHistory ?? contextObject.boundaryHistory) ? (merged.boundaryHistory ?? contextObject.boundaryHistory).map((item) => normalizeDoveBoundary(item, null)).filter(Boolean) : [],
    handoff: normalizeDoveHandoff(merged.handoff ?? contextObject.handoff, null),
    lastTransition: normalizeObject2(merged.lastTransition ?? contextObject.lastTransition),
    lessonIds: uniqueStrings([...normalizeStringArray4(merged.lessonIds), ...normalizeStringArray4(contextObject.lessonIds)]),
    artifactRefs: uniqueStrings([...normalizeStringArray4(merged.artifactRefs), ...normalizeStringArray4(contextObject.artifactRefs)]),
    contextPolicy: merged.contextPolicy ?? contextObject.contextPolicy ?? DOVE_AUDIO_CONTEXT_POLICY,
    sourceType: merged.sourceType ?? contextObject.sourceType ?? null,
    sourceId: merged.sourceId ?? contextObject.sourceId ?? id,
    currentFocus: merged.currentFocus ?? contextObject.currentFocus ?? "",
    nextAction: merged.nextAction ?? contextObject.nextAction ?? "",
    packetPath: merged.packetPath ?? packetFilePath(id),
    packetContextPath: merged.packetContextPath ?? packetContextPath(id),
    claimIds: uniqueStrings([...normalizeStringArray4(merged.claimIds), ...normalizeStringArray4(contextObject.claimIds)]),
    sourceIds: uniqueStrings([...normalizeStringArray4(merged.sourceIds), ...normalizeStringArray4(contextObject.sourceIds)]),
    noteIds: uniqueStrings([...normalizeStringArray4(merged.noteIds), ...normalizeStringArray4(contextObject.noteIds)]),
    experimentIds: uniqueStrings([...normalizeStringArray4(merged.experimentIds), ...normalizeStringArray4(contextObject.experiments), ...normalizeStringArray4(contextObject.experimentIds)]),
    rebuttalIssueIds: uniqueStrings([...normalizeStringArray4(merged.rebuttalIssueIds), ...normalizeStringArray4(contextObject.rebuttalIssueIds)]),
    versionIds: uniqueStrings([...normalizeStringArray4(merged.versionIds), ...normalizeStringArray4(contextObject.versionIds)]),
    resultIds: uniqueStrings([...normalizeStringArray4(merged.resultIds), ...normalizeStringArray4(contextObject.resultIds)]),
    auditIds: uniqueStrings([...normalizeStringArray4(merged.auditIds), ...normalizeStringArray4(contextObject.auditIds)]),
    outputPaths: uniqueStrings([...normalizeStringArray4(merged.outputPaths), ...normalizeStringArray4(contextObject.outputPaths)]),
    evidenceLinks: uniqueStrings([...normalizeStringArray4(merged.evidenceLinks), ...normalizeStringArray4(contextObject.evidenceLinks)]),
    validationEvidencePaths: uniqueStrings([...normalizeStringArray4(merged.validationEvidencePaths), ...normalizeStringArray4(contextObject.validationEvidencePaths)]),
    verificationEvidencePaths: uniqueStrings([...normalizeStringArray4(merged.verificationEvidencePaths), ...normalizeStringArray4(contextObject.verificationEvidencePaths)]),
    verifiedCriteria: normalizeDoveVerifiedCriteria(merged.verifiedCriteria ?? contextObject.verifiedCriteria),
    executionContract: normalizeDoveExecutionContract(merged.executionContract, null),
    context: contextObject
  };
}
function readTaskPacketCatalog(root) {
  const index = readJsonReadOnly(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex);
  const items = Array.isArray(index.items) ? index.items : [];
  const resultsIndex = readOptionalJson(root, ARTIFACT_PATHS.experimentResults);
  const auditsIndex = readOptionalJson(root, ARTIFACT_PATHS.experimentAudits);
  const results = Array.isArray(resultsIndex?.items) ? resultsIndex.items : [];
  const audits = Array.isArray(auditsIndex?.items) ? auditsIndex.items : [];
  const byId = /* @__PURE__ */ new Map();
  for (const item of items) {
    const packet = normalizePacketCandidate(root, item);
    if (packet.id) {
      const packetExperimentIds = new Set(packet.experimentIds);
      packet.resultIds = uniqueStrings([
        ...packet.resultIds,
        ...results.filter((result) => packetExperimentIds.has(result.experimentId)).map((result) => result.id)
      ]);
      packet.auditIds = uniqueStrings([
        ...packet.auditIds,
        ...audits.filter((audit) => packetExperimentIds.has(audit.experimentId) || packet.resultIds.includes(audit.resultId)).map((audit) => audit.id)
      ]);
      byId.set(packet.id, packet);
    }
  }
  return {
    index,
    packets: Array.from(byId.values()),
    byId
  };
}
function flattenFieldValues(value) {
  if (Array.isArray(value)) {
    return value.flatMap((item) => flattenFieldValues(item));
  }
  if (value && typeof value === "object") {
    return [];
  }
  return typeof value === "string" && value.trim() ? [value.trim()] : [];
}
function collectValuesFromObjects(objects, fields = []) {
  const values = [];
  for (const object of objects) {
    if (!object || typeof object !== "object" || Array.isArray(object)) {
      continue;
    }
    for (const field of fields) {
      values.push(...flattenFieldValues(object[field]));
    }
  }
  return uniqueStrings(values);
}
function requestObjects(args = {}) {
  return [args, args.plan, args.result, args.audit, args.payload].filter((item) => item && typeof item === "object" && !Array.isArray(item));
}
function explicitPacketIds(objects = []) {
  const values = collectValuesFromObjects(objects, ["packetId", "taskPacketId", "missionPacketId", "taskId"]);
  const packetIds = collectValuesFromObjects(objects, ["packetIds", "taskPacketIds", "missionPacketIds", "taskIds"]);
  return uniqueStrings([...values, ...packetIds].map((value) => normalizeTaskPacketId(value)));
}
function targetTexts(objects = [], fields = []) {
  return collectValuesFromObjects(objects, fields).filter((value) => !explicitPacketIds(objects).includes(normalizeTaskPacketId(value)));
}
function artifactValues(objects = [], fields = []) {
  return collectValuesFromObjects(objects, fields);
}
function packetArtifactSet(packet) {
  return new Set(uniqueStrings([
    packet.id,
    packet.sourceId,
    packet.packetPath,
    packet.packetContextPath,
    packet.parentId,
    packet.rootId,
    ...normalizeStringArray4(packet.dependencies),
    ...normalizeStringArray4(packet.blockedBy),
    ...normalizeStringArray4(packet.lessonIds),
    ...normalizeStringArray4(packet.artifactRefs),
    ...normalizeStringArray4(packet.claimIds),
    ...normalizeStringArray4(packet.sourceIds),
    ...normalizeStringArray4(packet.noteIds),
    ...normalizeStringArray4(packet.experimentIds),
    ...normalizeStringArray4(packet.rebuttalIssueIds),
    ...normalizeStringArray4(packet.versionIds),
    ...normalizeStringArray4(packet.resultIds),
    ...normalizeStringArray4(packet.auditIds),
    ...normalizeStringArray4(packet.outputPaths),
    ...normalizeStringArray4(packet.evidenceLinks),
    ...normalizeStringArray4(packet.validationEvidencePaths),
    ...normalizeStringArray4(packet.verificationEvidencePaths),
    ...normalizeStringArray4(packet.verifiedCriteria?.flatMap((item) => item.evidencePaths ?? []))
  ]));
}
function packetTextValues(packet) {
  return uniqueStrings([
    packet.id,
    packet.title,
    packet.summary,
    packet.currentFocus,
    packet.nextAction,
    packet.stage,
    packet.domain,
    packet.creatorKind,
    packet.sourceId,
    packet.sourceType ? `${packet.sourceType}:${packet.sourceId}` : null
  ].filter(Boolean));
}
function normalizeTextForMatch(value) {
  return String(value ?? "").normalize("NFKC").toLocaleLowerCase().replace(/[\s\p{P}\p{S}_-]+/gu, "").trim();
}
function textScore(packet, targets = []) {
  let score = 0;
  const values = packetTextValues(packet);
  for (const target of targets) {
    const normalizedTarget = slugify(target);
    const textTarget = normalizeTextForMatch(target);
    for (const value of values) {
      const normalizedValue = slugify(value);
      const textValue = normalizeTextForMatch(value);
      if (textTarget && textValue && textValue === textTarget) {
        score = Math.max(score, 1);
      } else if (textTarget && textValue && (textValue.includes(textTarget) || textTarget.includes(textValue))) {
        score = Math.max(score, 0.75);
      } else if (normalizedTarget !== "item" && normalizedValue !== "item" && normalizedValue === normalizedTarget) {
        score = Math.max(score, normalizedTarget.length >= 12 ? 1 : 0.65);
      } else if (normalizedTarget !== "item" && normalizedValue !== "item" && (normalizedValue.includes(normalizedTarget) || normalizedTarget.includes(normalizedValue))) {
        score = Math.max(score, normalizedTarget.length >= 12 ? 0.75 : 0.55);
      } else if (String(value).toLowerCase().includes(String(target).toLowerCase())) {
        score = Math.max(score, 0.6);
      }
    }
  }
  return score;
}
function artifactScore(packet, values = []) {
  if (values.length === 0) {
    return 0;
  }
  const artifactSet = packetArtifactSet(packet);
  let matches = 0;
  for (const value of values) {
    if (artifactSet.has(value) || artifactSet.has(slugify(value))) {
      matches += 1;
    }
  }
  return matches > 0 ? Math.min(1, 0.7 + matches / values.length * 0.3) : 0;
}
function lifecycleScore(packet) {
  if (["active", "in-progress", "current"].includes(packet.lifecycleStatus) || ["active", "in-progress", "current"].includes(packet.status)) {
    return 0.4;
  }
  if (["waiting", "queued", "pending", "planned", "open"].includes(packet.lifecycleStatus) || ["waiting", "queued", "pending", "planned", "open"].includes(packet.status)) {
    return 0.25;
  }
  return 0.1;
}
function scorePackets(packets, targets, artifacts) {
  return packets.map((packet) => {
    const targetText = textScore(packet, targets);
    const artifact = artifactScore(packet, artifacts);
    const lifecycle = targets.length === 0 ? lifecycleScore(packet) : 0;
    const score = Math.max(targetText, artifact, lifecycle);
    return {
      packet,
      packetId: packet.id,
      score,
      matchedBy: {
        targetText,
        artifact,
        lifecycle
      }
    };
  }).filter((candidate) => candidate.score > 0).sort((left, right) => right.score - left.score || left.packetId.localeCompare(right.packetId));
}
function candidateScoreText(score) {
  return Number.isFinite(score) ? score.toFixed(2) : "0.00";
}
function candidateSummary(candidate = {}) {
  const packet = candidate.packet ?? {};
  const summary = {
    packetId: candidate.packetId,
    title: packet.title ?? candidate.title ?? candidate.packetId,
    status: packet.status ?? candidate.status,
    level: packet.level ?? candidate.level,
    score: Number.isFinite(candidate.score) ? candidate.score : 0,
    matchedBy: candidate.matchedBy ?? {}
  };
  if (candidate.relation) {
    summary.relation = candidate.relation;
  }
  const matchedArtifacts = uniqueStrings(candidate.matchedArtifacts ?? []);
  if (matchedArtifacts.length > 0) {
    summary.matchedArtifacts = matchedArtifacts;
  }
  return summary;
}
function resolutionError(reason, candidates = [], details = {}) {
  const summaries = candidates.map((candidate) => candidateSummary(candidate));
  const suggestedActions = uniqueStrings(details.suggestedActions ?? details.artifactResolution?.suggestedActions ?? []);
  const candidateText = summaries.length > 0 ? ` Candidates: ${summaries.map((candidate) => `${candidate.packetId}(${candidate.title ?? candidate.packetId}, score=${candidateScoreText(candidate.score)})`).join(", ")}.` : "";
  const suggestionText = suggestedActions.length > 0 ? ` Suggested actions: ${suggestedActions.join("; ")}.` : "";
  const error = new Error(`${reason}${candidateText}${suggestionText} Provide packetId or confirm one candidate explicitly before writing.`);
  error.code = "TASK_PACKET_RESOLUTION_REQUIRED";
  error.reason = reason;
  error.candidates = summaries;
  error.suggestedActions = suggestedActions;
  if (details.artifactResolution) {
    error.artifactResolution = details.artifactResolution;
  }
  if (details.resolutionErrorCode) {
    error.resolutionErrorCode = details.resolutionErrorCode;
  }
  return error;
}
function hasResolutionSignal(targets = [], artifacts = []) {
  return targets.length > 0 || artifacts.length > 0;
}
function hasTiedTopCandidate(candidates = []) {
  return candidates.length > 1 && candidates[0].score === candidates[1].score;
}
function normalizeRelationId(value) {
  return typeof value === "string" && value.trim() ? normalizeTaskPacketId(value) : null;
}
function buildPacketRelationMap(packets = []) {
  const relationMap = /* @__PURE__ */ new Map();
  for (const packet of packets) {
    const packetId = normalizeRelationId(packet?.id);
    if (packetId) {
      relationMap.set(packetId, packet);
    }
  }
  return relationMap;
}
function isDescendantPacket(ancestor, candidate, packetsById) {
  const ancestorId = normalizeRelationId(ancestor?.id);
  let parentId = normalizeRelationId(candidate?.parentId);
  const seen = /* @__PURE__ */ new Set();
  while (ancestorId && parentId && !seen.has(parentId)) {
    if (parentId === ancestorId) {
      return true;
    }
    seen.add(parentId);
    const parent = packetsById.get(parentId);
    parentId = normalizeRelationId(parent?.parentId);
  }
  return false;
}
function packetRelation(selected, candidate, packetsById) {
  const selectedId = normalizeRelationId(selected?.id);
  const candidateId = normalizeRelationId(candidate?.id);
  if (!selectedId || !candidateId) {
    return "unrelated";
  }
  if (candidateId === selectedId) {
    return "self";
  }
  if (isDescendantPacket(selected, candidate, packetsById)) {
    return "descendant";
  }
  if (isDescendantPacket(candidate, selected, packetsById)) {
    return "ancestor";
  }
  const selectedParentId = normalizeRelationId(selected?.parentId);
  const candidateParentId = normalizeRelationId(candidate?.parentId);
  if (selectedParentId && candidateParentId && selectedParentId === candidateParentId) {
    return "sibling";
  }
  const selectedRootId = normalizeRelationId(selected?.rootId);
  const candidateRootId = normalizeRelationId(candidate?.rootId);
  if (selectedRootId && candidateRootId && selectedRootId !== candidateRootId) {
    return "other-root";
  }
  return "unrelated";
}
function matchedArtifactsForPacket(packet, artifacts = []) {
  const artifactSet = packetArtifactSet(packet);
  return uniqueStrings(artifacts.filter((artifact) => artifactSet.has(artifact) || artifactSet.has(slugify(artifact))));
}
function artifactMatchSummary(packet, artifacts = [], relation = null) {
  const score = artifactScore(packet, artifacts);
  return {
    packetId: packet.id,
    title: packet.title ?? packet.id,
    status: packet.status,
    level: packet.level,
    relation,
    score,
    matchedBy: { artifact: score },
    matchedArtifacts: matchedArtifactsForPacket(packet, artifacts)
  };
}
function packetSelectionSummary(packet) {
  return packet ? {
    packetId: packet.id,
    title: packet.title ?? packet.id,
    status: packet.status,
    level: packet.level
  } : null;
}
function artifactConflictSuggestedActions(selectedPacket, requestedArtifacts = [], conflictingMatches = []) {
  const selectedId = selectedPacket?.packetId ?? "selected-packet";
  const ownerActions = conflictingMatches.map((match) => {
    const artifacts = match.matchedArtifacts?.length > 0 ? ` for ${match.matchedArtifacts.join(", ")}` : "";
    return `write-to-owner-packet ${match.packetId}${artifacts}`;
  });
  return uniqueStrings([
    ...ownerActions,
    `choose-or-create-descendant-of ${selectedId} before attaching new artifacts`,
    `do-not-attach-unrelated-artifacts ${requestedArtifacts.join(", ") || "requested artifacts"} to ${selectedId}`
  ]);
}
function analyzeArtifactConsistency(packet, packets, artifacts = []) {
  const requestedArtifacts = uniqueStrings(artifacts);
  const selectedPacketId = normalizeRelationId(packet?.id);
  const selectedPacket = packetSelectionSummary(packet);
  if (!packet || requestedArtifacts.length === 0) {
    return {
      selectedPacketId,
      selectedPacket,
      requestedArtifacts,
      matchingPackets: [],
      acceptedMatches: [],
      conflictingMatches: [],
      hasConflict: false,
      suggestedActions: [],
      explanationCode: requestedArtifacts.length === 0 ? "no-artifacts" : "no-selected-packet"
    };
  }
  const packetsById = buildPacketRelationMap(packets);
  const matchingPackets = packets.filter((candidate) => artifactScore(candidate, requestedArtifacts) > 0).map((candidate) => artifactMatchSummary(candidate, requestedArtifacts, packetRelation(packet, candidate, packetsById)));
  const acceptedMatches = matchingPackets.filter((match) => ["self", "descendant"].includes(match.relation));
  const conflictingMatches = matchingPackets.filter((match) => !["self", "descendant"].includes(match.relation));
  const hasConflict = conflictingMatches.length > 0;
  const explanationCode = hasConflict ? "artifact-conflict" : acceptedMatches.some((match) => match.relation === "descendant") ? "accepted-descendant-artifacts" : acceptedMatches.some((match) => match.relation === "self") ? "accepted-self-artifacts" : "no-existing-artifact-owner";
  const suggestedActions = hasConflict ? artifactConflictSuggestedActions(selectedPacket, requestedArtifacts, conflictingMatches) : [];
  return {
    selectedPacketId,
    selectedPacket,
    requestedArtifacts,
    matchingPackets,
    acceptedMatches,
    conflictingMatches,
    hasConflict,
    suggestedActions,
    explanationCode
  };
}
function ensureArtifactConsistency(packet, packets, artifacts = []) {
  const artifactResolution = analyzeArtifactConsistency(packet, packets, artifacts);
  if (artifactResolution.hasConflict) {
    throw resolutionError(`Task target artifact ids conflict with packet ${packet.id}.`, artifactResolution.conflictingMatches.map((match) => ({
      packetId: match.packetId,
      title: match.title,
      status: match.status,
      level: match.level,
      relation: match.relation,
      score: match.score,
      matchedBy: match.matchedBy,
      matchedArtifacts: match.matchedArtifacts
    })), {
      artifactResolution,
      suggestedActions: artifactResolution.suggestedActions,
      resolutionErrorCode: artifactResolution.explanationCode
    });
  }
  return artifactResolution;
}
function resolveDurableTaskPacket(root, args = {}, options = {}) {
  const settings = options.settings ?? readTaskTargetResolutionSettings(root);
  const catalog = readTaskPacketCatalog(root);
  const objects = requestObjects(args);
  const explicitIds = explicitPacketIds(objects);
  const targets = targetTexts(objects, options.targetFields ?? []);
  const artifacts = artifactValues(objects, options.artifactFields ?? []);
  if (explicitIds.length > 1) {
    throw resolutionError(`Task target fields point to multiple packet ids: ${explicitIds.join(", ")}.`);
  }
  if (explicitIds.length === 1) {
    const packet = catalog.byId.get(explicitIds[0]);
    if (!packet) {
      throw resolutionError(`Task target packet ${explicitIds[0]} does not exist in ${ARTIFACT_PATHS.taskPacketsIndex}.`);
    }
    const artifactResolution2 = ensureArtifactConsistency(packet, catalog.packets, artifacts);
    return {
      packet,
      packetId: packet.id,
      resolution: {
        mode: "explicit-packet-id",
        autoSelect: settings.autoSelect,
        candidates: [{ packetId: packet.id, score: 1 }],
        artifactResolution: artifactResolution2
      },
      artifactIds: artifacts,
      artifactResolution: artifactResolution2
    };
  }
  if (catalog.packets.length === 0) {
    throw new Error(`Task-scoped write requires a durable task packet before writing. No packets exist in ${ARTIFACT_PATHS.taskPacketsIndex}. Launch or materialize a Dove mission packet first.`);
  }
  const candidates = scorePackets(catalog.packets, targets, artifacts);
  if (candidates.length === 0) {
    throw resolutionError("Task-scoped write could not resolve a durable task packet.");
  }
  const top = candidates[0];
  const explicitResolutionSignal = hasResolutionSignal(targets, artifacts);
  if (!explicitResolutionSignal && candidates.length > 1) {
    throw resolutionError("Task target requires confirmation because no packetId, natural-language target, or linked artifact was provided.", candidates);
  }
  if (!settings.autoSelect && candidates.length > 1) {
    throw resolutionError("Task target is ambiguous and autoSelect is false.", candidates);
  }
  if (settings.autoSelect && hasTiedTopCandidate(candidates)) {
    throw resolutionError("Task target requires confirmation because multiple durable packets have the same top confidence.", candidates);
  }
  if (settings.autoSelect && top.matchedBy.targetText > 0 && top.score < settings.autoSelectMinScore) {
    throw resolutionError(`Task target confidence ${top.score.toFixed(2)} is below autoSelectMinScore ${settings.autoSelectMinScore}.`, candidates);
  }
  const artifactResolution = ensureArtifactConsistency(top.packet, catalog.packets, artifacts);
  return {
    packet: top.packet,
    packetId: top.packet.id,
    resolution: {
      mode: settings.autoSelect ? "auto-selected" : "unique-candidate",
      autoSelect: settings.autoSelect,
      candidates: candidates.map((candidate) => ({
        packetId: candidate.packetId,
        score: candidate.score,
        matchedBy: candidate.matchedBy
      })),
      selectedScore: top.score,
      artifactResolution
    },
    artifactIds: artifacts,
    artifactResolution
  };
}
function assertResolvedTaskPacket(root, args = {}, options = {}) {
  return resolveDurableTaskPacket(root, args, options);
}

// src/core/navigation.mjs
function normalizeStringArray5(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean))) : [];
}
var GOVERNANCE_TERMINAL_LIFECYCLES = /* @__PURE__ */ new Set(["archived", "archived-with-lineage"]);
var GOVERNANCE_ACTIVE_LIFECYCLES = /* @__PURE__ */ new Set(["active", "ready", "ready-for-handoff", "review-needed", "stale"]);
var FIRST_CLASS_DOVE_CREATOR_KINDS = /* @__PURE__ */ new Set(["user", "system"]);
function isIsoTimestamp(value) {
  return typeof value === "string" && !Number.isNaN(Date.parse(value));
}
function normalizeUpdatedAt(value) {
  return isIsoTimestamp(value) ? value : null;
}
function uniqueSorted(items = []) {
  return Array.from(new Set(items.filter(Boolean))).sort((left, right) => left.localeCompare(right));
}
function lifecycleSortRank(packet) {
  const order = {
    stale: 0,
    "review-needed": 1,
    "ready-for-handoff": 2,
    blocked: 3,
    waiting: 4,
    active: 5,
    queued: 6,
    "archived-with-lineage": 7,
    archived: 8
  };
  return order[packet.lifecycleStatus] ?? 99;
}
function sortPacketsForQueue(items = []) {
  return [...items].sort((left, right) => {
    const lifecycleDelta = lifecycleSortRank(left) - lifecycleSortRank(right);
    if (lifecycleDelta !== 0) {
      return lifecycleDelta;
    }
    return (left.id ?? "").localeCompare(right.id ?? "");
  });
}
function hasLineageLinks(packet = {}) {
  return [
    packet.parentPacketId,
    ...packet.childPacketIds ?? [],
    ...packet.claimIds ?? [],
    ...packet.noteIds ?? [],
    ...packet.experimentIds ?? [],
    ...packet.rebuttalIssueIds ?? [],
    ...packet.versionIds ?? [],
    ...packet.outputPaths ?? [],
    ...packet.evidenceLinks ?? []
  ].filter(Boolean).length > 0;
}
function derivePacketLifecycle(packet = {}, existing = {}) {
  const explicit = typeof packet.lifecycleStatus === "string" ? packet.lifecycleStatus.trim().toLowerCase() : "";
  if (explicit) {
    return explicit;
  }
  const normalizedStatus2 = typeof packet.status === "string" ? packet.status.trim().toLowerCase() : "pending";
  const continuationStatus = typeof packet.continuationState?.status === "string" ? packet.continuationState.status.trim().toLowerCase() : "";
  const updatedAt = normalizeUpdatedAt(packet.updatedAt) ?? normalizeUpdatedAt(existing.updatedAt) ?? normalizeUpdatedAt(packet.continuationState?.updatedAt);
  const isStale = updatedAt ? Date.now() - Date.parse(updatedAt) > 1e3 * 60 * 60 * 24 * 7 : false;
  const dependencyCount = normalizeStringArray5(packet.dependencies).length;
  const lineageLinked = hasLineageLinks({ ...existing, ...packet });
  if (["done", "resolved", "cancelled", "retired", "archived"].includes(normalizedStatus2) || continuationStatus === "completed") {
    return lineageLinked ? "archived-with-lineage" : "archived";
  }
  if (["review-needed", "needs-review", "awaiting-review", "review"].includes(normalizedStatus2) || continuationStatus === "review-needed") {
    return "review-needed";
  }
  if (dependencyCount > 0 || continuationStatus === "blocked") {
    return normalizedStatus2 === "blocked" ? "blocked" : "waiting";
  }
  if (packet.assignedRole && packet.boardAssignedRole && packet.assignedRole !== packet.boardAssignedRole && !["pending", "planned", "queued", "open"].includes(normalizedStatus2)) {
    return "ready-for-handoff";
  }
  if (isStale) {
    return "stale";
  }
  if (["in-progress", "active", "current", "working"].includes(normalizedStatus2) || continuationStatus === "in-progress") {
    return "active";
  }
  if (["pending", "planned", "queued", "open", "paused", "blocked"].includes(normalizedStatus2) || continuationStatus === "ready-to-resume") {
    return "waiting";
  }
  return "queued";
}
function buildPacketDependencyHealth(packet, packetById) {
  const dependencies = normalizeStringArray5(packet.dependencies);
  const missingDependencyIds = dependencies.filter((dependencyId) => !packetById.has(dependencyId));
  const blockingPacketIds = dependencies.filter((dependencyId) => {
    const dependency = packetById.get(dependencyId);
    return dependency && !GOVERNANCE_TERMINAL_LIFECYCLES.has(dependency.lifecycleStatus);
  });
  return {
    state: missingDependencyIds.length > 0 ? "missing-dependencies" : blockingPacketIds.length > 0 ? "blocked-by-dependencies" : dependencies.length > 0 ? "ready-after-dependencies" : "clear",
    dependencyIds: dependencies,
    missingDependencyIds,
    blockingPacketIds
  };
}
function summarizePacket(packet) {
  const materializedFrom = packet.materialization?.sourceType && packet.materialization?.sourceId ? `${packet.materialization.sourceType}:${packet.materialization.sourceId}` : null;
  return {
    id: packet.id,
    title: packet.title,
    status: packet.status,
    lifecycleStatus: packet.lifecycleStatus,
    lifecycleFamily: packet.lifecycleFamily ?? lifecycleFamilyForPacket(packet),
    doveDomain: doveDomainForPacket(packet),
    assignedRole: packet.assignedRole,
    phase: packet.phase,
    nextAction: packet.nextAction,
    dependencyState: packet.dependencyHealth?.state ?? "clear",
    packetPath: packet.packetPath ?? null,
    packetContextPath: packet.packetContextPath ?? null,
    sourceType: packet.sourceType ?? null,
    sourceId: packet.sourceId ?? null,
    materializedFrom,
    evidenceLinks: normalizeStringArray5(packet.evidenceLinks),
    outputPaths: normalizeStringArray5(packet.outputPaths),
    claimIds: normalizeStringArray5(packet.claimIds),
    noteIds: normalizeStringArray5(packet.noteIds),
    experimentIds: normalizeStringArray5(packet.experimentIds),
    rebuttalIssueIds: normalizeStringArray5(packet.rebuttalIssueIds),
    versionIds: normalizeStringArray5(packet.versionIds)
  };
}
function describePacketProvenance(packet = {}) {
  if (packet.materialization?.sourceType && packet.materialization?.sourceId) {
    return `materialized-from=${packet.materialization.sourceType}:${packet.materialization.sourceId}`;
  }
  if (packet.sourceType && packet.sourceId && packet.sourceType !== "task") {
    return `source=${packet.sourceType}:${packet.sourceId}`;
  }
  return null;
}
function slugify2(value) {
  const normalized = String(value).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
  if (normalized.length <= 160) {
    return normalized;
  }
  const digest = crypto5.createHash("sha256").update(normalized).digest("hex").slice(0, 12);
  return `${normalized.slice(0, 147).replace(/-+$/u, "")}-${digest}`;
}
function isTrellisTaskSourceArtifact2(value) {
  const normalized = String(value ?? "").trim().replace(/\\/g, "/").replace(/^\.\/+/, "");
  return normalized === ".trellis/tasks" || normalized.startsWith(".trellis/tasks/") || normalized === "trellis/tasks" || normalized.startsWith("trellis/tasks/");
}
function normalizeOptionalString(value, fallback = null) {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeQuestion(question, fallbackPrefix, index) {
  if (typeof question === "string") {
    return {
      id: `${fallbackPrefix}-question-${index + 1}`,
      summary: question,
      status: "open",
      origin: fallbackPrefix
    };
  }
  return {
    id: slugify2(question?.id ?? `${fallbackPrefix}-question-${index + 1}`),
    summary: question?.summary ?? question?.text ?? `Question ${index + 1}`,
    status: question?.status ?? "open",
    origin: question?.origin ?? fallbackPrefix
  };
}
function normalizeDecision(decision, fallbackPrefix, index) {
  if (typeof decision === "string") {
    return {
      id: `${fallbackPrefix}-decision-${index + 1}`,
      summary: decision,
      rationale: "",
      origin: fallbackPrefix,
      recordedAt: null
    };
  }
  return {
    id: slugify2(decision?.id ?? `${fallbackPrefix}-decision-${index + 1}`),
    summary: decision?.summary ?? `Decision ${index + 1}`,
    rationale: decision?.rationale ?? "",
    origin: decision?.origin ?? fallbackPrefix,
    recordedAt: decision?.recordedAt ?? null
  };
}
function packetFilePath2(packetId) {
  return path8.join(ARTIFACT_PATHS.taskPacketsPacketsDir, `${packetId}.json`);
}
function loadExistingPacketMap(root, packets = []) {
  return new Map((packets ?? []).map((packet) => {
    const packetId = packet.id;
    const packetPath = packet.packetPath ?? packetFilePath2(packetId);
    const fileValue = readJson(root, packetPath, packet);
    return [packetId, { ...packet, ...fileValue, packetPath }];
  }));
}
function mergeQuestions(incoming, existing) {
  const merged = new Map((existing ?? []).map((question) => [question.id, question]));
  for (const question of incoming ?? []) {
    merged.set(question.id, question);
  }
  return Array.from(merged.values());
}
function mergeDecisions(incoming, existing) {
  const merged = new Map((existing ?? []).map((decision) => [decision.id, decision]));
  for (const decision of incoming ?? []) {
    merged.set(decision.id, decision);
  }
  return Array.from(merged.values());
}
function isFirstClassDoveTaskPacket(packet = {}) {
  return Number.isFinite(packet.level) && FIRST_CLASS_DOVE_CREATOR_KINDS.has(packet.creatorKind) && typeof packet.rootId === "string" && packet.rootId.length > 0;
}
function normalizePacket(packet = {}) {
  const lifecycleStatus = derivePacketLifecycle(packet);
  const updatedAt = normalizeUpdatedAt(packet.updatedAt) ?? nowIso();
  const workerRole = ROLE_IDS.includes(packet.autonomyEnvelope?.workerRole) ? packet.autonomyEnvelope.workerRole : null;
  const controllerRole = ROLE_IDS.includes(packet.autonomyEnvelope?.controllerRole) ? packet.autonomyEnvelope.controllerRole : null;
  const doveDomain = normalizeDoveDomainId(packet.doveDomain ?? packet.missionDomain ?? packet.domain, null);
  return {
    ...packet,
    id: slugify2(packet.id),
    sourceType: packet.sourceType ?? "task",
    sourceId: packet.sourceId ?? packet.id,
    title: packet.title ?? packet.id,
    summary: packet.summary ?? "",
    phase: packet.phase ?? "init",
    phaseContextId: packet.phaseContextId ?? `phase-${packet.phase ?? "init"}`,
    status: packet.status ?? "pending",
    lifecycleStatus,
    lifecycleFamily: lifecycleFamilyForPacket(packet),
    doveDomain,
    active: packet.active ?? !GOVERNANCE_TERMINAL_LIFECYCLES.has(lifecycleStatus),
    assignedRole: ROLE_IDS.includes(packet.assignedRole) ? packet.assignedRole : ROLE_IDS.includes(packet.ownerRole) ? packet.ownerRole : "planner",
    parentPacketId: packet.parentPacketId ? slugify2(packet.parentPacketId) : null,
    childPacketIds: normalizeStringArray5(packet.childPacketIds),
    currentFocus: packet.currentFocus ?? packet.title ?? packet.id,
    nextAction: packet.nextAction ?? "Continue the durable workflow.",
    dependencies: normalizeStringArray5(packet.dependencies),
    evidenceLinks: normalizeStringArray5(packet.evidenceLinks),
    claimIds: normalizeStringArray5(packet.claimIds),
    noteIds: normalizeStringArray5(packet.noteIds),
    experimentIds: normalizeStringArray5(packet.experimentIds),
    rebuttalIssueIds: normalizeStringArray5(packet.rebuttalIssueIds),
    versionIds: normalizeStringArray5(packet.versionIds),
    outputPaths: normalizeStringArray5(packet.outputPaths),
    questions: Array.isArray(packet.questions) ? packet.questions : [],
    decisions: Array.isArray(packet.decisions) ? packet.decisions : [],
    lineage: packet.lineage ?? {},
    continuationState: packet.continuationState ?? { status: packet.active ? "in-progress" : "ready-to-resume", lastCheckpoint: packet.summary ?? packet.title ?? packet.id },
    autonomyEnvelope: workerRole && controllerRole ? {
      controllerRole,
      workerRole,
      scopeType: packet.autonomyEnvelope?.scopeType ?? "packet-local",
      explicitOnly: packet.autonomyEnvelope?.explicitOnly ?? true,
      requiredReadPaths: normalizeStringArray5(packet.autonomyEnvelope?.requiredReadPaths),
      localRules: normalizeStringArray5(packet.autonomyEnvelope?.localRules)
    } : null,
    updatedAt,
    packetPath: packet.packetPath ?? packetFilePath2(packet.id),
    packetContextPath: packet.packetContextPath ?? path8.join(ARTIFACT_PATHS.packetContextsDir, `${slugify2(packet.id)}.json`)
  };
}
function phaseContextIdForRole(roleId) {
  switch (roleId) {
    case "builder":
    case "researcher":
      return "research";
    case "reviewer":
      return "review";
    case "revision-lead":
    case "rebuttal-lead":
      return "rebuttal";
    case "experiment-planner":
      return "experiments";
    case "version-analyst":
      return "versions";
    default:
      return "plan";
  }
}
function roleContextPaths(roleId) {
  const shared = [
    ARTIFACT_PATHS.state,
    ARTIFACT_PATHS.orchestrationBoard,
    ARTIFACT_PATHS.orchestrationHandoffs,
    ARTIFACT_PATHS.taskPacketsIndex,
    ARTIFACT_PATHS.sessionSummary,
    ARTIFACT_PATHS.navigationReport,
    ARTIFACT_PATHS.workspaceIndex,
    path8.join(ARTIFACT_PATHS.phaseContextsDir, `${phaseContextIdForRole(roleId)}.json`),
    ARTIFACT_PATHS.wikiEntities,
    ARTIFACT_PATHS.wikiRelations
  ];
  switch (roleId) {
    case "builder":
      return [...shared, ARTIFACT_PATHS.researchBrief, ARTIFACT_PATHS.researchAgenda, ARTIFACT_PATHS.sources, ARTIFACT_PATHS.notes, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.queryPack, ARTIFACT_PATHS.plan, ARTIFACT_PATHS.outline, ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.experimentLog, ARTIFACT_PATHS.revisionPlan, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft, ARTIFACT_PATHS.checklist];
    case "researcher":
      return [...shared, ARTIFACT_PATHS.researchBrief, ARTIFACT_PATHS.researchAgenda, ARTIFACT_PATHS.sources, ARTIFACT_PATHS.notes, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.queryPack];
    case "reviewer":
      return [...shared, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.adversarialReviewState, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureSegments, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.revisionPlan, ARTIFACT_PATHS.checklist];
    case "revision-lead":
    case "rebuttal-lead":
      return [...shared, ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft];
    case "experiment-planner":
      return [...shared, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.experimentLog];
    case "version-analyst":
      return [...shared, ARTIFACT_PATHS.versionsIndex, ARTIFACT_PATHS.versionComparisons, ARTIFACT_PATHS.versionComparisonReport];
    default:
      return [...shared, ARTIFACT_PATHS.plan, ARTIFACT_PATHS.outline, ARTIFACT_PATHS.revisionPlan, ARTIFACT_PATHS.checklist];
  }
}
function artifactContextPath(relativePath) {
  const normalized = normalizeArtifactPath(relativePath);
  const slug = slugify2(normalized);
  const digest = crypto5.createHash("sha256").update(normalized).digest("hex").slice(0, 16);
  const readablePrefix = slug.slice(0, 120).replace(/-+$/g, "") || "artifact";
  return path8.join(
    ARTIFACT_PATHS.artifactContextsDir,
    `${readablePrefix}-${digest}.json`
  );
}
function actionContextPath(scopeId) {
  return path8.join(ARTIFACT_PATHS.actionContextsDir, `${slugify2(scopeId)}.json`);
}
function normalizeArtifactPath(relativePath) {
  return String(relativePath ?? "").replace(/^\.\//, "").replace(/\\/g, "/");
}
var PHASE_LIFECYCLE_FAMILIES = {
  init: "work-unit",
  sources: "knowledge",
  notes: "knowledge",
  research: "objective",
  plan: "structure",
  outline: "structure",
  draft: "structure",
  experiments: "audit",
  citations: "knowledge",
  review: "concern",
  rebuttal: "concern",
  versions: "structure",
  checklist: "structure"
};
function normalizeLifecycleFamilyId2(value, fallback = "work-unit") {
  const normalized = typeof value === "string" ? value.trim() : "";
  return PAPER_LIFECYCLE_FAMILY_IDS.includes(normalized) ? normalized : fallback;
}
function lifecycleFamilyForPhase(phase) {
  return normalizeLifecycleFamilyId2(PHASE_LIFECYCLE_FAMILIES[String(phase ?? "").trim()], "work-unit");
}
function lifecycleFamilyForArtifactPath(relativePath) {
  const normalized = normalizeArtifactPath(relativePath);
  const auditPaths = /* @__PURE__ */ new Set([
    ARTIFACT_PATHS.experimentAudits,
    ARTIFACT_PATHS.reviewLog,
    ARTIFACT_PATHS.figureQa,
    ARTIFACT_PATHS.versionComparisons,
    ARTIFACT_PATHS.versionComparisonReport,
    ARTIFACT_PATHS.metaEvents,
    ARTIFACT_PATHS.metaExecutionBridgeCandidates,
    ARTIFACT_PATHS.metaGovernanceCoverage,
    ARTIFACT_PATHS.metaGovernanceCoverageReport,
    ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown,
    ARTIFACT_PATHS.metaOperatorFollowThrough,
    ARTIFACT_PATHS.metaOperatorFollowThroughTransitions,
    ARTIFACT_PATHS.metaOperatorPlaybooks,
    ARTIFACT_PATHS.metaRemediationPacks,
    ARTIFACT_PATHS.metaRecommendations,
    ARTIFACT_PATHS.metaOptimizerState,
    ARTIFACT_PATHS.metaOptimizerReport
  ]);
  if (auditPaths.has(normalized) || normalized.startsWith(`${ARTIFACT_PATHS.metaDir}/`)) {
    return "audit";
  }
  const exact = /* @__PURE__ */ new Map([
    [ARTIFACT_PATHS.state, "objective"],
    [ARTIFACT_PATHS.project, "objective"],
    [ARTIFACT_PATHS.researchContract, "objective"],
    [ARTIFACT_PATHS.researchBrief, "objective"],
    [ARTIFACT_PATHS.researchAgenda, "objective"],
    [ARTIFACT_PATHS.plan, "structure"],
    [ARTIFACT_PATHS.outline, "structure"],
    [ARTIFACT_PATHS.checklist, "structure"],
    [ARTIFACT_PATHS.figuresIndex, "structure"],
    [ARTIFACT_PATHS.figureBriefs, "structure"],
    [ARTIFACT_PATHS.figureSegments, "structure"],
    [ARTIFACT_PATHS.figureTemplates, "structure"],
    [ARTIFACT_PATHS.figureEditableIndex, "structure"],
    [ARTIFACT_PATHS.figureFinalIndex, "structure"],
    [ARTIFACT_PATHS.figuresReadme, "structure"],
    [ARTIFACT_PATHS.versionsIndex, "structure"],
    [ARTIFACT_PATHS.programsIndex, "campaign"],
    [ARTIFACT_PATHS.programRuns, "campaign"],
    [ARTIFACT_PATHS.programApprovals, "campaign"],
    [ARTIFACT_PATHS.campaignsIndex, "campaign"],
    [ARTIFACT_PATHS.runtimeControllerState, "campaign"],
    [ARTIFACT_PATHS.runtimeContinuation, "campaign"],
    [ARTIFACT_PATHS.runtimeLeases, "campaign"],
    [ARTIFACT_PATHS.runtimeEvents, "campaign"],
    [ARTIFACT_PATHS.runtimeResults, "campaign"],
    [ARTIFACT_PATHS.orchestrationBoard, "work-unit"],
    [ARTIFACT_PATHS.orchestrationHandoffs, "work-unit"],
    [ARTIFACT_PATHS.taskPacketsIndex, "work-unit"],
    [ARTIFACT_PATHS.workspaceIndex, "work-unit"],
    [ARTIFACT_PATHS.sessionJournal, "work-unit"],
    [ARTIFACT_PATHS.sessionSummary, "work-unit"],
    [ARTIFACT_PATHS.navigationReport, "work-unit"],
    [ARTIFACT_PATHS.workflowBoundaries, "work-unit"],
    [ARTIFACT_PATHS.reviewState, "concern"],
    [ARTIFACT_PATHS.reviewConcerns, "concern"],
    [ARTIFACT_PATHS.reviewDebateLog, "concern"],
    [ARTIFACT_PATHS.adversarialReviewState, "concern"],
    [ARTIFACT_PATHS.revisionPlan, "concern"],
    [ARTIFACT_PATHS.rebuttalIssues, "concern"],
    [ARTIFACT_PATHS.rebuttalStrategy, "concern"],
    [ARTIFACT_PATHS.rebuttalResponseDraft, "concern"],
    [ARTIFACT_PATHS.sources, "knowledge"],
    [ARTIFACT_PATHS.notes, "knowledge"],
    [ARTIFACT_PATHS.evidence, "knowledge"],
    [ARTIFACT_PATHS.claims, "knowledge"],
    [ARTIFACT_PATHS.claimBridgeLog, "knowledge"],
    [ARTIFACT_PATHS.bibliography, "knowledge"],
    [ARTIFACT_PATHS.citationLog, "knowledge"],
    [ARTIFACT_PATHS.wiki, "knowledge"],
    [ARTIFACT_PATHS.queryPack, "knowledge"],
    [ARTIFACT_PATHS.wikiEntities, "knowledge"],
    [ARTIFACT_PATHS.wikiRelations, "knowledge"],
    [ARTIFACT_PATHS.experimentLog, "audit"],
    [ARTIFACT_PATHS.experimentPlans, "audit"],
    [ARTIFACT_PATHS.experimentResults, "audit"]
  ]);
  if (exact.has(normalized)) {
    return exact.get(normalized);
  }
  if (normalized.startsWith(`${ARTIFACT_PATHS.draftsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.versionSnapshotsDir}/`)) {
    return "structure";
  }
  if (normalized.startsWith(`${ARTIFACT_PATHS.programsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.runtimeDir}/`)) {
    return "campaign";
  }
  if (normalized.startsWith(`${ARTIFACT_PATHS.taskPacketsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.roleContextsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.phaseContextsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.packetContextsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.artifactContextsDir}/`) || normalized.startsWith(`${ARTIFACT_PATHS.actionContextsDir}/`)) {
    return "work-unit";
  }
  if (normalized.startsWith(`${ARTIFACT_PATHS.isolatedReviewsDir}/`)) {
    return "concern";
  }
  return "work-unit";
}
function lifecycleFamilyForPacket(packet = {}) {
  const explicit = normalizeLifecycleFamilyId2(packet.lifecycleFamily, null);
  if (explicit) {
    return explicit;
  }
  if (packet.sourceType === "experiment") {
    return "audit";
  }
  if (packet.sourceType === "rebuttal-issue") {
    return "concern";
  }
  if (packet.sourceType === "version") {
    return "structure";
  }
  const linkedPathFamily = [...packet.outputPaths ?? [], ...packet.evidenceLinks ?? []].map(lifecycleFamilyForArtifactPath).find((familyId) => familyId !== "work-unit");
  if (linkedPathFamily) {
    return linkedPathFamily;
  }
  if ((packet.rebuttalIssueIds ?? []).length > 0 || packet.assignedRole === "reviewer") {
    return "concern";
  }
  if ((packet.experimentIds ?? []).length > 0) {
    return "audit";
  }
  if ((packet.versionIds ?? []).length > 0) {
    return "structure";
  }
  if ((packet.claimIds ?? []).length > 0 || (packet.noteIds ?? []).length > 0) {
    return "knowledge";
  }
  return lifecycleFamilyForPhase(packet.phase);
}
function doveMissionStageForPhase(phase) {
  const normalized = typeof phase === "string" ? phase.trim() : "";
  const map = {
    init: "goal",
    sources: "goal",
    notes: "goal",
    research: "goal",
    plan: "design",
    outline: "design",
    checklist: "checklist",
    draft: "execution",
    experiments: "execution",
    citations: "execution",
    rebuttal: "execution",
    review: "audit",
    versions: "return"
  };
  return DOVE_MISSION_LIFECYCLE_STAGES.includes(map[normalized]) ? map[normalized] : "goal";
}
function doveDomainForPacket(packet = {}) {
  const explicitDomain = normalizeDoveDomainId(packet.doveDomain ?? packet.missionDomain ?? packet.domain, null);
  if (explicitDomain) {
    return explicitDomain;
  }
  const lifecycleFamily = packet.lifecycleFamily ?? lifecycleFamilyForPacket(packet);
  if ((packet.experimentIds ?? []).length > 0 || lifecycleFamily === "audit") {
    return "experiment";
  }
  if (packet.assignedRole === "reviewer" || lifecycleFamily === "concern") {
    return "review";
  }
  if (lifecycleFamily === "work-unit" || lifecycleFamily === "campaign") {
    return "general";
  }
  return "paper";
}
function buildDoveWorkspaceSummary(board, packets, lifecycle) {
  const base = createDoveWorkspaceKernel();
  const domainCounts = Object.fromEntries(DOVE_DOMAIN_IDS.map((domainId) => [domainId, 0]));
  for (const packet of packets) {
    const domainId = doveDomainForPacket(packet);
    domainCounts[domainId] = (domainCounts[domainId] ?? 0) + 1;
  }
  const activePackets2 = packets.filter((packet) => packet.active);
  const currentDomain = activePackets2.length > 0 ? doveDomainForPacket(activePackets2[0]) : "paper";
  return {
    ...base,
    currentDomain,
    missionLifecycle: {
      ...base.missionLifecycle,
      currentStage: doveMissionStageForPhase(board.currentPhase)
    },
    missionCount: packets.length,
    activeMissionCount: activePackets2.length,
    reviewNeededMissionCount: packets.filter((packet) => packet.lifecycleStatus === "review-needed").length,
    domainCounts,
    currentMissionFamily: lifecycle.boardFamily ?? lifecycleFamilyForPhase(board.currentPhase)
  };
}
function buildLifecycleWorkspaceSummary(board, packets) {
  const artifactCounts = Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0]));
  for (const relativePath of Object.values(ARTIFACT_PATHS)) {
    const familyId = lifecycleFamilyForArtifactPath(relativePath);
    artifactCounts[familyId] = (artifactCounts[familyId] ?? 0) + 1;
  }
  const packetCounts = Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0]));
  const activePacketCounts = Object.fromEntries(PAPER_LIFECYCLE_FAMILY_IDS.map((familyId) => [familyId, 0]));
  for (const packet of packets) {
    const familyId = lifecycleFamilyForPacket(packet);
    packetCounts[familyId] = (packetCounts[familyId] ?? 0) + 1;
    if (packet.active) {
      activePacketCounts[familyId] = (activePacketCounts[familyId] ?? 0) + 1;
    }
  }
  const topFamilies = PAPER_LIFECYCLE_FAMILY_IDS.filter((familyId) => activePacketCounts[familyId] > 0 || packetCounts[familyId] > 0).sort((left, right) => {
    const activeDelta = activePacketCounts[right] - activePacketCounts[left];
    if (activeDelta !== 0) {
      return activeDelta;
    }
    const packetDelta = packetCounts[right] - packetCounts[left];
    if (packetDelta !== 0) {
      return packetDelta;
    }
    return left.localeCompare(right);
  });
  return {
    taxonomyVersion: PAPER_LIFECYCLE_TAXONOMY_VERSION,
    familyIds: PAPER_LIFECYCLE_FAMILY_IDS,
    families: PAPER_LIFECYCLE_FAMILIES.map((family) => ({
      ...family,
      artifactCount: artifactCounts[family.id] ?? 0,
      activePacketCount: activePacketCounts[family.id] ?? 0,
      packetCount: packetCounts[family.id] ?? 0
    })),
    artifactCounts,
    activePacketCounts,
    packetCounts,
    boardFamily: lifecycleFamilyForPhase(board.currentPhase),
    boardPhaseFamily: lifecycleFamilyForPhase(board.currentPhase),
    topFamilies,
    protocol: {
      stages: PAPER_MAJOR_CHANGE_PROTOCOL_STAGES,
      majorChangeSignals: PAPER_MAJOR_CHANGE_SIGNALS,
      overview: "Major paper changes should close through design, checklist, implementation, and acceptance."
    }
  };
}
function artifactGuidance(relativePath) {
  const normalized = normalizeArtifactPath(relativePath);
  if ([ARTIFACT_PATHS.orchestrationBoard, ARTIFACT_PATHS.orchestrationHandoffs, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport].includes(normalized)) {
    return {
      category: "orchestration",
      summary: "Treat this artifact as workflow control state and refresh the nearest role/phase/packet context before mutating it.",
      localRules: [
        "Preserve explicit role ownership, currentFocus, nextAction, and continuation checkpoints.",
        "Prefer durable handoffs and packet updates over implicit intent changes.",
        "Refresh query/navigation surfaces after changes so downstream commands read the same file state."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.orchestrationBoard, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.navigationReport]
    };
  }
  if ([ARTIFACT_PATHS.researchBrief, ARTIFACT_PATHS.researchAgenda, ARTIFACT_PATHS.sources, ARTIFACT_PATHS.notes, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.queryPack, ARTIFACT_PATHS.claims].includes(normalized)) {
    return {
      category: "evidence",
      summary: "Research artifacts should stay provenance-aware and only promote evidence-backed claims.",
      localRules: [
        "Do not cite from memory or invent support that is not present in durable artifacts.",
        "Keep source, note, claim, and query surfaces aligned when evidence changes.",
        "Escalate uncertainty into open questions instead of hiding it in prose."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.researchBrief, ARTIFACT_PATHS.sources, ARTIFACT_PATHS.notes, ARTIFACT_PATHS.evidence]
    };
  }
  if ([ARTIFACT_PATHS.wiki, ARTIFACT_PATHS.wikiEntities, ARTIFACT_PATHS.wikiRelations, ARTIFACT_PATHS.queryPack].includes(normalized)) {
    return {
      category: "wiki",
      summary: "Wiki artifacts should keep typed entity and relation integrity explicit so repair work stays file-backed and review-visible.",
      localRules: [
        "Keep entity and relation ids stable enough for downstream navigation surfaces to reuse them.",
        "Treat dangling relation endpoints and invalid endpoint typing as explicit repair work, not hidden cleanup.",
        "Refresh workspace and navigation surfaces after wiki changes so the same repair frontier is visible everywhere."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.wikiEntities, ARTIFACT_PATHS.wikiRelations, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.navigationReport]
    };
  }
  if ([ARTIFACT_PATHS.plan, ARTIFACT_PATHS.outline, ARTIFACT_PATHS.checklist].includes(normalized) || normalized.startsWith(`${ARTIFACT_PATHS.draftsDir}/`)) {
    return {
      category: "writing",
      summary: "Writing artifacts should follow the current plan and preserve evidence/review gates before completion claims.",
      localRules: [
        "Use durable plan and outline state to scope edits before drafting.",
        "Leave explicit citation TODO markers instead of fabricating support.",
        "Treat review-before-finalize as a workflow gate, not a suggestion."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.plan, ARTIFACT_PATHS.outline, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.reviewState]
    };
  }
  if ([ARTIFACT_PATHS.experimentLog, ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog].includes(normalized)) {
    return {
      category: "experiments",
      summary: "Experiment artifacts should keep plans, results, audits, and claim bridges distinct and traceable.",
      localRules: [
        "Record results separately from audits and bridge events.",
        "Keep reviewed artifact refs and audit flags explicit.",
        "Update claim state only through durable result-to-claim linkage."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog]
    };
  }
  if ([ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.reviewDebateLog, ARTIFACT_PATHS.adversarialReviewState, ARTIFACT_PATHS.revisionPlan, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft].includes(normalized)) {
    return {
      category: "review",
      summary: "Review and rebuttal artifacts should preserve reviewer independence, blocker visibility, and response traceability.",
      localRules: [
        "Keep reviewer-raised concerns durable and assign responses to non-reviewer roles when needed.",
        "Turn findings into explicit blockers, revision items, or rebuttal issues.",
        "Do not collapse review state into a single prose summary when structured state exists."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.revisionPlan, ARTIFACT_PATHS.rebuttalIssues]
    };
  }
  if ([ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureSegments, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.figuresReadme].includes(normalized)) {
    return {
      category: "figures",
      summary: "Figure artifacts should preserve staged progression, claim linkage, and durable QA without inventing a render runtime.",
      localRules: [
        "Keep figure briefs, segment placeholders, templates, editable records, final contracts, and QA outputs in sync.",
        "Link figures to source sections, target claims, experiments, and review/rebuttal context when those relationships exist.",
        "Treat missing staged artifacts or broken figure linkage as review-visible issues, not informal notes."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.reviewState]
    };
  }
  if ([ARTIFACT_PATHS.versionsIndex, ARTIFACT_PATHS.versionComparisons, ARTIFACT_PATHS.versionComparisonReport].includes(normalized) || normalized.startsWith(`${ARTIFACT_PATHS.versionSnapshotsDir}/`)) {
    return {
      category: "versions",
      summary: "Version artifacts should preserve honest lineage and explicit comparison targets.",
      localRules: [
        "Snapshot before major revisions and keep parent lineage explicit.",
        "Record comparison deltas instead of relying on memory.",
        "Do not finalize around an uncleared review gate."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.versionsIndex, ARTIFACT_PATHS.versionComparisons]
    };
  }
  if ([ARTIFACT_PATHS.metaEvents, ARTIFACT_PATHS.metaExecutionBridgeCandidates, ARTIFACT_PATHS.metaRemediationPacks, ARTIFACT_PATHS.metaRecommendations, ARTIFACT_PATHS.metaOptimizerState, ARTIFACT_PATHS.metaOptimizerReport].includes(normalized)) {
    return {
      category: "meta-optimize",
      summary: "Meta-optimize artifacts are proposal-only summaries built from durable workflow signals.",
      localRules: [
        "Do not auto-apply recommendations from this layer.",
        "Treat execution bridge candidates as manual scaffolds, not real work items.",
        "Keep every recommendation tied to concrete evidence artifacts and ids.",
        "Use this layer to surface workflow/process/artifact health, not hidden self-modification."
      ],
      readBeforeMutating: [ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.versionComparisons]
    };
  }
  return {
    category: "general",
    summary: "Read the nearest local context surfaces before mutating this durable artifact.",
    localRules: [
      "Prefer explicit file-backed guidance over broad top-level instructions.",
      "Keep related artifacts in sync when this path changes.",
      "Avoid hidden state; make the next action legible in durable files."
    ],
    readBeforeMutating: [ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.navigationReport]
  };
}
function buildArtifactContextManifest(root, relativePath, board, packets, workspaceIndex) {
  const normalized = normalizeArtifactPath(relativePath);
  const guidance = artifactGuidance(normalized);
  const lifecycleFamily = lifecycleFamilyForArtifactPath(normalized);
  const relatedPackets = packets.filter((packet) => [packet.packetPath, packet.packetContextPath, ...packet.outputPaths ?? [], ...packet.evidenceLinks ?? []].includes(normalized));
  const relatedRoles = ROLE_IDS.filter((roleId) => roleContextPaths(roleId).includes(normalized));
  return {
    version: 1,
    artifactPath: normalized,
    exists: fs6.existsSync(resolvePath(root, normalized)),
    category: guidance.category,
    lifecycleFamily,
    doveLifecycle: {
      taxonomyVersion: PAPER_LIFECYCLE_TAXONOMY_VERSION,
      familyId: lifecycleFamily,
      familyLabel: PAPER_LIFECYCLE_FAMILIES.find((family) => family.id === lifecycleFamily)?.label ?? lifecycleFamily
    },
    summary: guidance.summary,
    boardPhase: board.currentPhase,
    boardAssignedRole: board.assignedRole,
    currentFocus: board.currentFocus,
    nextAction: board.nextAction,
    localRules: guidance.localRules,
    readBeforeMutating: uniqueSorted([
      ...guidance.readBeforeMutating,
      path8.join(ARTIFACT_PATHS.roleContextsDir, `${board.assignedRole}.json`),
      path8.join(ARTIFACT_PATHS.phaseContextsDir, `${board.currentPhase}.json`),
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.navigationReport
    ]),
    relatedPacketIds: relatedPackets.map((packet) => packet.id),
    relatedPacketContextPaths: relatedPackets.map((packet) => packet.packetContextPath),
    relatedRoleIds: relatedRoles,
    relatedRoleContextPaths: relatedRoles.map((roleId) => path8.join(ARTIFACT_PATHS.roleContextsDir, `${roleId}.json`)),
    workspaceCoupling: {
      workspaceIndexPath: ARTIFACT_PATHS.workspaceIndex,
      navigationReportPath: ARTIFACT_PATHS.navigationReport,
      currentActionContextPath: actionContextPath("current")
    },
    generatedAt: nowIso()
  };
}
function lifecycleContextForScope({ workspaceIndex, board, packet = null, phaseId = null, artifactPath = null }) {
  const familyId = packet?.lifecycleFamily ?? (artifactPath ? lifecycleFamilyForArtifactPath(artifactPath) : null) ?? (phaseId ? lifecycleFamilyForPhase(phaseId) : null) ?? workspaceIndex.lifecycle?.boardFamily ?? lifecycleFamilyForPhase(board.currentPhase);
  return {
    taxonomyVersion: workspaceIndex.lifecycle?.taxonomyVersion ?? PAPER_LIFECYCLE_TAXONOMY_VERSION,
    familyId,
    boardFamily: workspaceIndex.lifecycle?.boardFamily ?? lifecycleFamilyForPhase(board.currentPhase),
    topFamilies: workspaceIndex.lifecycle?.topFamilies ?? []
  };
}
function majorChangeProtocolForWorkspace(workspaceIndex) {
  return workspaceIndex.lifecycle?.protocol ?? {
    stages: PAPER_MAJOR_CHANGE_PROTOCOL_STAGES,
    majorChangeSignals: PAPER_MAJOR_CHANGE_SIGNALS,
    overview: "Major paper changes should close through design, checklist, implementation, and acceptance."
  };
}
function buildActionContextBundle({ scopeType, scopeId, summary, board, workspaceIndex, packet = null, roleId = null, phaseId = null, artifactPath = null, requiredReadPaths = [], localRules = [], nextAction = null, operatorGuidance = null }) {
  return {
    version: 1,
    scopeType,
    scopeId,
    summary,
    boardPhase: board.currentPhase,
    boardAssignedRole: board.assignedRole,
    currentFocus: packet?.currentFocus ?? workspaceIndex.currentFocus ?? board.currentFocus,
    nextAction: nextAction ?? packet?.nextAction ?? workspaceIndex.nextAction ?? board.nextAction,
    roleId,
    phaseId,
    packetId: packet?.id ?? null,
    autonomyEnvelope: packet?.autonomyEnvelope ?? null,
    programLinkage: packet?.lineage?.programId || packet?.materialization?.programId ? {
      programId: packet.lineage?.programId ?? packet.materialization?.programId ?? null,
      programRunId: packet.lineage?.programRunId ?? packet.materialization?.programRunId ?? null,
      approvalId: packet.lineage?.approvalId ?? packet.materialization?.approvalId ?? null
    } : null,
    artifactPath,
    lifecycle: lifecycleContextForScope({ workspaceIndex, board, packet, phaseId, artifactPath }),
    dove: {
      kernelVersion: workspaceIndex.dove?.kernelVersion ?? DOVE_WORKFLOW_KERNEL_VERSION,
      currentDomain: workspaceIndex.dove?.currentDomain ?? "paper",
      missionStage: workspaceIndex.dove?.missionLifecycle?.currentStage ?? doveMissionStageForPhase(board.currentPhase),
      primaryRoleIds: workspaceIndex.dove?.primaryRoleIds ?? DOVE_PRIMARY_ROLE_IDS,
      domainGuidance: workspaceIndex.dove?.domainGuidance ?? createDoveWorkspaceKernel().domainGuidance,
      authorityManifest: workspaceIndex.dove?.authorityManifest ?? createDoveWorkspaceKernel().authorityManifest
    },
    majorChangeProtocol: majorChangeProtocolForWorkspace(workspaceIndex),
    explicitOnly: true,
    noHiddenRuntime: true,
    requiredReadPaths: uniqueSorted(requiredReadPaths),
    localRules: uniqueSorted(localRules),
    operatorGuidance,
    generatedAt: nowIso()
  };
}
function isGovernanceRepairFrontierItem(item = {}) {
  return ["workflow-governance", "version-governance", "meta-optimize-drift"].includes(item.frontierType);
}
function remediationPackSpecificityScore(pack = {}, { roleId = null, packetId = null, packet = null } = {}) {
  let score = 0;
  const rankingBasis = [];
  const matchedPacketPointer = packetId ? (pack.packetPointers ?? []).find((pointer) => pointer.id === packetId) ?? null : null;
  if (matchedPacketPointer) {
    score += 80;
    rankingBasis.push(`packet=${packetId}`);
  }
  if (roleId && (pack.packetPointers ?? []).some((pointer) => pointer.assignedRole === roleId)) {
    score += 24;
    rankingBasis.push(`packetRole=${roleId}`);
  }
  if (roleId && (pack.reviewConcerns ?? []).some((concern) => concern.responseOwnerRole === roleId)) {
    score += 18;
    rankingBasis.push(`reviewOwner=${roleId}`);
  }
  const packetTextSource = matchedPacketPointer ?? packet ?? null;
  if (packetTextSource) {
    const packetText = [packetTextSource.title, packetTextSource.currentFocus, packetTextSource.nextAction, packetTextSource.summary].filter(Boolean).join(" ").toLowerCase();
    const taxonomyTerms = uniqueSorted([
      pack.clusterId,
      pack.clusterLabel,
      ...pack.taxonomyAnchors?.familyIds ?? [],
      ...pack.taxonomyAnchors?.familyLabels ?? [],
      ...pack.taxonomyAnchors?.groupIds ?? [],
      ...pack.taxonomyAnchors?.groupLabels ?? []
    ].map((value) => String(value).toLowerCase()));
    const matchedTerms = taxonomyTerms.filter((term) => term && packetText.includes(term));
    if (matchedTerms.length > 0) {
      score += 96 + matchedTerms.length * 4;
      rankingBasis.push(`packetText=${matchedTerms.join(",")}`);
    }
  }
  return { score, rankingBasis };
}
function selectTopRemediationPack(remediationPacks = {}, { roleId = null, packetId = null, packet = null } = {}) {
  const packs = remediationPacks?.packs ?? [];
  if (packetId || roleId) {
    const ranked = packs.map((pack) => ({ pack, ...remediationPackSpecificityScore(pack, { roleId, packetId, packet }) })).filter((candidate) => candidate.score > 0).sort((left, right) => {
      const scoreDelta = right.score - left.score;
      if (scoreDelta !== 0) {
        return scoreDelta;
      }
      return (left.pack.id ?? "").localeCompare(right.pack.id ?? "");
    });
    if (ranked[0]) {
      return ranked[0].pack;
    }
  }
  return packs[0] ?? null;
}
function conversionPathPriorityRank(value) {
  return value === "primary" ? 0 : value === "secondary" ? 1 : 2;
}
function buildRankedConversionCandidates(candidates = []) {
  return candidates.map((candidate, index) => ({
    ...candidate,
    priority: candidate.priority ?? "secondary",
    pathScore: Number.isFinite(candidate.pathScore) ? candidate.pathScore : 0,
    rankingBasis: normalizeStringArray5(candidate.rankingBasis ?? []),
    deterministicKey: candidate.deterministicKey ?? [
      String(9999 - (Number.isFinite(candidate.pathScore) ? candidate.pathScore : 0)).padStart(4, "0"),
      String(conversionPathPriorityRank(candidate.priority ?? "secondary")).padStart(2, "0"),
      candidate.targetType ?? "path",
      candidate.targetId ?? `path-${index + 1}`
    ].join(":"),
    originalIndex: index
  })).sort((left, right) => {
    const scoreDelta = (right.pathScore ?? 0) - (left.pathScore ?? 0);
    if (scoreDelta !== 0) {
      return scoreDelta;
    }
    const priorityDelta = conversionPathPriorityRank(left.priority) - conversionPathPriorityRank(right.priority);
    if (priorityDelta !== 0) {
      return priorityDelta;
    }
    return (left.deterministicKey ?? "").localeCompare(right.deterministicKey ?? "");
  }).map((candidate, index) => ({
    ...candidate,
    rank: index + 1
  }));
}
function operatorPlaybookSpecificityScore(playbook = {}, { roleId = null, packetId = null, packet = null, remediationPack = null, workspaceIndex = null } = {}) {
  let score = 0;
  const rankingBasis = [];
  const matchedPacketPointer = packetId ? (playbook.packetPointers ?? []).find((pointer) => pointer.id === packetId) ?? null : null;
  if (matchedPacketPointer) {
    score += 80;
    rankingBasis.push(`packet=${packetId}`);
  }
  if (packet?.id && packet.id === packetId) {
    score += 36;
    rankingBasis.push(`packetContext=${packet.id}`);
  }
  if (roleId && (playbook.responseOwnerRoles ?? []).includes(roleId)) {
    score += 40;
    rankingBasis.push(`ownerRole=${roleId}`);
  }
  if (roleId && (playbook.packetPointers ?? []).some((pointer) => pointer.assignedRole === roleId)) {
    score += 24;
    rankingBasis.push(`packetRole=${roleId}`);
  }
  if (roleId && (playbook.conversionHints ?? []).some((hint) => hint.assignedRole === roleId)) {
    score += 16;
    rankingBasis.push(`hintRole=${roleId}`);
  }
  const remediationFamilies = remediationPack?.taxonomyAnchors?.familyIds ?? [];
  const remediationGroups = remediationPack?.taxonomyAnchors?.groupIds ?? [];
  if (intersects(playbook.remediationPackIds ?? [], [remediationPack?.id].filter(Boolean))) {
    score += 36;
    rankingBasis.push(`pack=${remediationPack.id}`);
  }
  if (intersects(playbook.taxonomyFamilyId ? [playbook.taxonomyFamilyId] : [], remediationFamilies)) {
    score += 32;
    rankingBasis.push(`taxonomyFamily=${playbook.taxonomyFamilyId}`);
  }
  if (intersects(playbook.taxonomyGroupIds ?? [], remediationGroups)) {
    score += 18;
    rankingBasis.push(`taxonomyGroups=${(playbook.taxonomyGroupIds ?? []).filter((groupId) => remediationGroups.includes(groupId)).join(",")}`);
  }
  const activeFamilies = workspaceIndex?.metaOptimize?.topTaxonomyFamilyIds ?? [];
  if (intersects(playbook.taxonomyFamilyId ? [playbook.taxonomyFamilyId] : [], activeFamilies)) {
    score += 12;
    rankingBasis.push(`activeFamily=${playbook.taxonomyFamilyId}`);
  }
  const packetTextSource = matchedPacketPointer ?? packet ?? null;
  if (packetTextSource) {
    const packetText = [packetTextSource.title, packetTextSource.currentFocus, packetTextSource.nextAction].filter(Boolean).join(" ").toLowerCase();
    const familyTerms = uniqueSorted([
      playbook.taxonomyFamilyId,
      playbook.taxonomyFamilyLabel,
      ...playbook.taxonomyGroupIds ?? [],
      ...playbook.taxonomyGroupLabels ?? []
    ].map((value) => String(value).toLowerCase()));
    const matchedTerms = familyTerms.filter((term) => term && packetText.includes(term));
    if (matchedTerms.length > 0) {
      score += 48 + matchedTerms.length * 4;
      rankingBasis.push(`packetText=${matchedTerms.join(",")}`);
    }
  }
  score += Math.min((playbook.remediationPackIds ?? []).length, 5) * 2;
  score += Math.min((playbook.longHorizonFamilyIds ?? []).length, 5);
  rankingBasis.push(`remediationPackCount=${(playbook.remediationPackIds ?? []).length}`);
  rankingBasis.push(`longHorizonCount=${(playbook.longHorizonFamilyIds ?? []).length}`);
  return { score, rankingBasis: uniqueSorted(rankingBasis) };
}
function selectTopOperatorLessons(operatorLessons = {}, { roleId = null, packet = null } = {}) {
  const packetTerms = [packet?.title, packet?.summary, packet?.currentFocus, packet?.nextAction, packet?.doveDomain, packet?.missionStage].filter(Boolean).join(" ").toLowerCase();
  return (operatorLessons?.lessons ?? []).filter((lesson) => lesson.status === "active").map((lesson) => {
    let score = 0;
    if (roleId && (lesson.actorRole === roleId || roleCanActAs(roleId, lesson.actorRole))) {
      score += 24;
    }
    if (packet?.doveDomain && lesson.domain === packet.doveDomain) {
      score += 18;
    }
    if (packet?.missionStage && lesson.stage === packet.missionStage) {
      score += 12;
    }
    const matchedTags = (lesson.tags ?? []).filter((tag) => packetTerms.includes(String(tag).toLowerCase()));
    score += matchedTags.length * 8;
    return { ...lesson, selectionScore: score, matchedTags };
  }).sort((left, right) => (right.selectionScore ?? 0) - (left.selectionScore ?? 0) || String(right.updatedAt ?? right.createdAt ?? "").localeCompare(String(left.updatedAt ?? left.createdAt ?? "")) || left.id.localeCompare(right.id)).slice(0, 4);
}
function buildOperatorGuidance(workspaceIndex, remediationPacks = {}, operatorPlaybooks = {}, executionBridgeCandidatesIndex = {}, operatorFollowThrough = {}, operatorLessons = {}, { roleId = null, packetId = null, packet = null } = {}) {
  const topRepairItems = (workspaceIndex.repairFrontier?.prioritizedItems ?? []).slice(0, 3).map((item) => ({
    id: item.id,
    frontierType: item.frontierType,
    severity: item.severity ?? "medium",
    summary: item.summary,
    nextAction: item.nextAction
  }));
  const topRemediationPack = selectTopRemediationPack(remediationPacks, { roleId, packetId, packet });
  const rankedPlaybookCandidates = (operatorPlaybooks?.playbooks ?? []).map((playbook) => {
    const specificity = operatorPlaybookSpecificityScore(playbook, {
      roleId,
      packetId,
      packet,
      remediationPack: topRemediationPack,
      workspaceIndex
    });
    return {
      ...playbook,
      selectionScore: specificity.score,
      selectionRankingBasis: specificity.rankingBasis
    };
  }).sort((left, right) => {
    const scoreDelta = (right.selectionScore ?? 0) - (left.selectionScore ?? 0);
    if (scoreDelta !== 0) {
      return scoreDelta;
    }
    const packDelta = (right.remediationPackIds?.length ?? 0) - (left.remediationPackIds?.length ?? 0);
    if (packDelta !== 0) {
      return packDelta;
    }
    return left.taxonomyFamilyId.localeCompare(right.taxonomyFamilyId);
  });
  const topOperatorPlaybook = rankedPlaybookCandidates[0] ?? null;
  const executionBridgeCandidates = (executionBridgeCandidatesIndex?.candidates ?? []).filter((candidate) => {
    if (!candidate || typeof candidate !== "object") {
      return false;
    }
    if (topOperatorPlaybook && intersects(candidate.sourcePlaybookIds ?? [], [topOperatorPlaybook.id])) {
      return true;
    }
    if (topRemediationPack && intersects(candidate.sourceRemediationPackIds ?? [], [topRemediationPack.id])) {
      return true;
    }
    if (roleId && (candidate.sourceConversionPath?.assignedRole === roleId || (candidate.suggestedTitle ?? "").toLowerCase().includes(roleId.toLowerCase()))) {
      return true;
    }
    return false;
  }).slice(0, 4);
  const followThroughRecords = (operatorFollowThrough?.items ?? []).filter((record) => {
    if (topRemediationPack && record.sourceType === "remediation-pack" && record.sourceId === topRemediationPack.id) {
      return true;
    }
    if (topOperatorPlaybook && record.sourceType === "operator-playbook" && record.sourceId === topOperatorPlaybook.id) {
      return true;
    }
    if (executionBridgeCandidates.some((candidate) => record.sourceType === "execution-bridge" && record.sourceId === candidate.id)) {
      return true;
    }
    return false;
  }).slice(0, 4).map((record) => ({
    id: record.id,
    sourceType: record.sourceType,
    sourceId: record.sourceId,
    status: record.status,
    decisionSummary: record.decisionSummary,
    rationale: record.rationale,
    actorRole: record.actorRole,
    deferUntil: record.deferUntil,
    executeBy: record.executeBy,
    reviewAfter: record.reviewAfter,
    closureReason: record.closureReason,
    stale: record.stale,
    dueDeferred: record.dueDeferred,
    overdueExecution: record.overdueExecution,
    overdueExecutionSeverity: record.overdueExecutionSeverity,
    executionWindowState: record.executionWindowState,
    linkedTargetArtifact: record.linkedTargetArtifact,
    linkedTargetId: record.linkedTargetId
  }));
  const topOperatorLessons = selectTopOperatorLessons(operatorLessons, { roleId, packet }).map((lesson) => ({
    id: lesson.id,
    title: lesson.title,
    problem: lesson.problem,
    domain: lesson.domain,
    stage: lesson.stage,
    actorRole: lesson.actorRole,
    tags: lesson.tags,
    nextTime: (lesson.nextTime ?? []).slice(0, 4),
    pitfalls: (lesson.pitfalls ?? []).slice(0, 3),
    validation: (lesson.validation ?? []).slice(0, 3),
    selectionScore: lesson.selectionScore,
    matchedTags: lesson.matchedTags
  }));
  return {
    repairFrontier: {
      count: workspaceIndex.repairFrontier?.count ?? 0,
      governanceIssueCount: workspaceIndex.repairFrontier?.governanceIssueCount ?? 0,
      taxonomyOverview: workspaceIndex.repairFrontier?.taxonomyOverview ?? "No degraded typed wiki relation families are currently summarized.",
      topItems: topRepairItems
    },
    taxonomyPressure: {
      overview: workspaceIndex.metaOptimize?.taxonomyOverview ?? "No typed wiki taxonomy pressure is currently active in the optimizer frontier.",
      pressureAreas: workspaceIndex.metaOptimize?.pressureAreas ?? [],
      topTaxonomyFamilyIds: workspaceIndex.metaOptimize?.topTaxonomyFamilyIds ?? [],
      topTaxonomyGroupIds: workspaceIndex.metaOptimize?.topTaxonomyGroupIds ?? []
    },
    remediationPack: topRemediationPack ? {
      id: topRemediationPack.id,
      title: topRemediationPack.title,
      clusterId: topRemediationPack.clusterId,
      summary: topRemediationPack.summary,
      readiness: topRemediationPack.readiness,
      taxonomyOverview: topRemediationPack.taxonomyAnchors?.overview ?? "No typed wiki taxonomy pressure is active in this remediation pack.",
      acceptanceCriteria: (topRemediationPack.acceptanceCriteria ?? []).slice(0, 3),
      conversionHints: (topRemediationPack.conversionHints ?? []).slice(0, 3),
      rankedConversionPaths: (topRemediationPack.rankedConversionPaths ?? []).slice(0, 4),
      manualNextActions: (topRemediationPack.manualNextActions ?? []).slice(0, 3),
      workspacePointers: (topRemediationPack.workspacePointers ?? []).slice(0, 5)
    } : null,
    followThrough: {
      summary: operatorFollowThrough.summary ?? { itemCount: 0, overview: "No operator follow-through decisions have been recorded yet." },
      topRecords: followThroughRecords,
      actionRequiredItems: (operatorFollowThrough.actionRequiredItems ?? []).slice(0, 4),
      executionWindowSummary: {
        dueSoonCount: operatorFollowThrough.summary?.dueSoonExecutionCount ?? 0,
        dueReviewCount: operatorFollowThrough.summary?.dueReviewCount ?? 0,
        overdueCount: operatorFollowThrough.summary?.overdueExecutionCount ?? 0,
        criticalOverdueCount: operatorFollowThrough.summary?.criticalOverdueExecutionCount ?? 0
      }
    },
    operatorLessons: {
      summary: operatorLessons.summary ?? { lessonCount: 0, activeLessonCount: 0, overview: "No distilled operator lessons have been recorded yet." },
      topLessons: topOperatorLessons,
      lessonsPath: ARTIFACT_PATHS.metaOperatorLessons
    },
    familyPlaybook: topOperatorPlaybook ? {
      id: topOperatorPlaybook.id,
      title: topOperatorPlaybook.title,
      taxonomyFamilyId: topOperatorPlaybook.taxonomyFamilyId,
      taxonomyFamilyLabel: topOperatorPlaybook.taxonomyFamilyLabel,
      summary: topOperatorPlaybook.summary,
      readiness: topOperatorPlaybook.readiness,
      operatorGoal: topOperatorPlaybook.operatorGoal,
      longHorizonSummary: topOperatorPlaybook.longHorizonSummary,
      selectionScore: topOperatorPlaybook.selectionScore,
      selectionRankingBasis: topOperatorPlaybook.selectionRankingBasis,
      artifactUpdateOverview: topOperatorPlaybook.artifactUpdateMap?.overview ?? `No explicit artifact targets were derived for ${topOperatorPlaybook.taxonomyFamilyLabel}.`,
      artifactUpdateTargets: (topOperatorPlaybook.artifactUpdateMap?.targets ?? []).slice(0, 5),
      artifactUpdateOrder: (topOperatorPlaybook.artifactUpdateMap?.updateOrder ?? []).slice(0, 5),
      acceptanceCriteria: (topOperatorPlaybook.acceptanceCriteria ?? []).slice(0, 4),
      conversionHints: (topOperatorPlaybook.conversionHints ?? []).slice(0, 4),
      rankedConversionPaths: (topOperatorPlaybook.rankedConversionPaths ?? []).slice(0, 4),
      manualNextActions: (topOperatorPlaybook.manualNextActions ?? []).slice(0, 4),
      remediationPackIds: (topOperatorPlaybook.remediationPackIds ?? []).slice(0, 4),
      workspacePointers: (topOperatorPlaybook.workspacePointers ?? []).slice(0, 6)
    } : null,
    executionBridgeCandidates: executionBridgeCandidates.map((candidate) => ({
      id: candidate.id,
      candidateType: candidate.candidateType,
      targetArtifact: candidate.targetArtifact,
      targetId: candidate.targetId,
      suggestedTitle: candidate.suggestedTitle,
      suggestedSummary: candidate.suggestedSummary,
      suggestedNextStep: candidate.suggestedNextStep,
      sourceRemediationPackIds: (candidate.sourceRemediationPackIds ?? []).slice(0, 3),
      sourcePlaybookIds: (candidate.sourcePlaybookIds ?? []).slice(0, 3),
      linkedRepairItemIds: (candidate.linkedRepairItemIds ?? []).slice(0, 5),
      suggestedAcceptanceCriteria: (candidate.suggestedAcceptanceCriteria ?? []).slice(0, 4),
      context: {
        linkedPacketPointers: (candidate.context?.linkedPacketPointers ?? []).slice(0, 2),
        linkedWorkspacePointers: (candidate.context?.linkedWorkspacePointers ?? []).slice(0, 4),
        linkedRemediationPacks: (candidate.context?.linkedRemediationPacks ?? []).slice(0, 2),
        linkedPlaybooks: (candidate.context?.linkedPlaybooks ?? []).slice(0, 2),
        evidenceSummary: candidate.context?.evidenceSummary ?? { artifactPaths: [], ids: [] },
        repairSummary: candidate.context?.repairSummary ?? { repairItemIds: [], reviewConcernIds: [], figureIssueIds: [] },
        bridgeNotes: (candidate.context?.bridgeNotes ?? []).slice(0, 3)
      }
    }))
  };
}
function deriveBoardPackets(board, existingById) {
  const packets = [];
  for (const task of board.tasks ?? []) {
    const packetId = slugify2(task.packetId ?? `task-${task.id}`);
    const previous = existingById.get(packetId);
    packets.push(normalizePacket({
      ...previous ?? {},
      id: packetId,
      sourceType: "task",
      sourceId: task.id,
      title: task.title,
      summary: task.notes ?? previous?.summary ?? "",
      phase: board.currentPhase,
      phaseContextId: task.phaseContextId ?? `phase-${board.currentPhase}`,
      status: task.status,
      lifecycleStatus: task.lifecycleStatus ?? task.status,
      active: !["done", "cancelled"].includes(task.status),
      assignedRole: task.assignedRole,
      parentPacketId: task.parentPacketId,
      childPacketIds: task.childPacketIds,
      currentFocus: task.currentFocus ?? task.title,
      nextAction: task.nextAction,
      dependencies: task.blockedBy,
      boardAssignedRole: board.assignedRole,
      evidenceLinks: task.evidenceLinks,
      claimIds: task.claimIds,
      noteIds: task.noteIds,
      experimentIds: task.experimentIds,
      rebuttalIssueIds: task.rebuttalIssueIds,
      versionIds: task.versionIds,
      outputPaths: task.outputPaths,
      questions: mergeQuestions((task.questions ?? []).map((item, index) => normalizeQuestion(item, packetId, index)), previous?.questions),
      decisions: mergeDecisions((task.decisions ?? []).map((item, index) => normalizeDecision(item, packetId, index)), previous?.decisions),
      lineage: {
        ...previous?.lineage ?? {},
        boardPhase: board.currentPhase,
        intentType: board.intentType,
        currentVersionId: board.versionLineage?.currentVersionId ?? null,
        activeComparisonTargets: normalizeStringArray5(board.activeComparisonTargets)
      },
      continuationState: task.continuationState ?? previous?.continuationState,
      updatedAt: nowIso()
    }));
  }
  for (const blocker of board.blockers ?? []) {
    const packetId = slugify2(blocker.packetId ?? `blocker-${blocker.id}`);
    const previous = existingById.get(packetId);
    packets.push(normalizePacket({
      ...previous ?? {},
      id: packetId,
      sourceType: "blocker",
      sourceId: blocker.id,
      title: blocker.summary,
      summary: blocker.summary,
      phase: board.currentPhase,
      phaseContextId: blocker.phaseContextId ?? `phase-${board.currentPhase}`,
      status: blocker.status,
      lifecycleStatus: blocker.lifecycleStatus ?? blocker.status,
      active: blocker.status !== "resolved",
      assignedRole: blocker.assignedRole,
      parentPacketId: blocker.parentPacketId,
      childPacketIds: blocker.childPacketIds,
      currentFocus: blocker.currentFocus ?? blocker.summary,
      nextAction: blocker.nextAction,
      dependencies: blocker.blockedBy,
      boardAssignedRole: board.assignedRole,
      evidenceLinks: blocker.evidenceLinks,
      claimIds: blocker.claimIds,
      noteIds: blocker.noteIds,
      experimentIds: blocker.experimentIds,
      rebuttalIssueIds: blocker.rebuttalIssueIds,
      versionIds: blocker.versionIds,
      outputPaths: blocker.outputPaths,
      questions: mergeQuestions((blocker.questions ?? []).map((item, index) => normalizeQuestion(item, packetId, index)), previous?.questions),
      decisions: mergeDecisions((blocker.decisions ?? []).map((item, index) => normalizeDecision(item, packetId, index)), previous?.decisions),
      lineage: {
        ...previous?.lineage ?? {},
        boardPhase: board.currentPhase,
        intentType: board.intentType,
        currentVersionId: board.versionLineage?.currentVersionId ?? null
      },
      continuationState: blocker.continuationState ?? previous?.continuationState,
      updatedAt: nowIso()
    }));
  }
  return packets;
}
function deriveExperimentPackets(plansIndex) {
  return (plansIndex.items ?? []).map((plan) => normalizePacket({
    id: `experiment-${plan.id}`,
    sourceType: "experiment",
    sourceId: plan.id,
    title: plan.title,
    summary: plan.hypothesis ?? plan.methodology ?? "",
    phase: "experiments",
    phaseContextId: "phase-experiments",
    status: plan.status,
    lifecycleStatus: plan.status,
    active: plan.status !== "done",
    assignedRole: plan.owner ?? "experiment-planner",
    currentFocus: plan.title,
    nextAction: "Record or audit the experiment result.",
    dependencies: [],
    boardAssignedRole: "experiment-planner",
    evidenceLinks: [],
    claimIds: plan.claimId ? [plan.claimId] : [],
    noteIds: [],
    experimentIds: [plan.id],
    rebuttalIssueIds: [],
    versionIds: [],
    outputPaths: [ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentLog, ARTIFACT_PATHS.experimentAudits],
    questions: [],
    decisions: [],
    lineage: { comparisonTargets: normalizeStringArray5(plan.comparisonTargets) },
    updatedAt: plan.updatedAt ?? nowIso()
  }));
}
function deriveIssuePackets(issuesIndex) {
  return (issuesIndex.items ?? []).map((issue) => normalizePacket({
    id: deterministicBoundedTaskPacketId("rebuttal", issue.id),
    sourceType: "rebuttal-issue",
    sourceId: issue.id,
    title: issue.summary,
    summary: issue.summary,
    phase: "rebuttal",
    phaseContextId: "phase-rebuttal",
    status: issue.status,
    lifecycleStatus: issue.status,
    active: issue.status !== "resolved",
    assignedRole: "builder",
    currentFocus: issue.summary,
    nextAction: issue.responseDirection === "fix" ? "Revise the evidence or experiment record first." : "Clarify the response with the strongest durable evidence.",
    dependencies: [],
    boardAssignedRole: "builder",
    evidenceLinks: issue.evidenceLinks,
    claimIds: issue.claimIds,
    noteIds: [],
    experimentIds: issue.experimentIds,
    rebuttalIssueIds: [issue.id],
    versionIds: [],
    outputPaths: [ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.rebuttalStrategy],
    questions: [],
    decisions: [],
    lineage: { responseDirection: issue.responseDirection },
    updatedAt: issue.updatedAt ?? nowIso()
  }));
}
function deriveVersionPackets(versionsIndex) {
  return (versionsIndex.items ?? []).map((item) => normalizePacket({
    id: `version-${item.id}`,
    sourceType: "version",
    sourceId: item.id,
    title: item.label ?? item.id,
    summary: item.summary ?? "",
    phase: "versions",
    phaseContextId: "phase-versions",
    status: versionsIndex.currentVersionId === item.id ? "current" : "archived",
    lifecycleStatus: versionsIndex.currentVersionId === item.id ? "current" : "archived",
    active: versionsIndex.currentVersionId === item.id,
    assignedRole: "planner",
    currentFocus: item.label ?? item.id,
    nextAction: "Compare this version with the active target when claims move.",
    dependencies: item.parentVersionId ? [`version-${item.parentVersionId}`] : [],
    boardAssignedRole: "version-analyst",
    evidenceLinks: [],
    claimIds: [],
    noteIds: [],
    experimentIds: [],
    rebuttalIssueIds: [],
    versionIds: [item.id],
    outputPaths: [ARTIFACT_PATHS.versionsIndex, path8.join(ARTIFACT_PATHS.versionSnapshotsDir, `${item.id}.json`)],
    questions: [],
    decisions: [],
    lineage: { parentVersionId: item.parentVersionId ?? null },
    updatedAt: item.createdAt ?? nowIso()
  }));
}
function markInactiveLegacyPackets(items, activeIds) {
  return items.map((packet) => {
    if (!activeIds.has(packet.id) && ["task", "blocker"].includes(packet.sourceType) && !isFirstClassDoveTaskPacket(packet)) {
      const lifecycleStatus = GOVERNANCE_TERMINAL_LIFECYCLES.has(packet.lifecycleStatus) ? packet.lifecycleStatus : hasLineageLinks(packet) ? "archived-with-lineage" : "archived";
      return { ...packet, active: false, lifecycleStatus };
    }
    return packet;
  });
}
function buildOwnershipSummary(board, packets) {
  const packetByRole = /* @__PURE__ */ new Map();
  for (const roleId of PRIMARY_ROLE_IDS) {
    packetByRole.set(roleId, []);
  }
  for (const packet of packets.filter((item) => item.active)) {
    const assignedRole = ROLE_HIERARCHY[packet.assignedRole]?.parentRole ?? packet.assignedRole;
    packetByRole.get(assignedRole)?.push(packet);
  }
  return PRIMARY_ROLE_IDS.map((roleId) => {
    const owned = sortPacketsForQueue(packetByRole.get(roleId) ?? []);
    return {
      roleId,
      isBoardOwner: board.assignedRole === roleId,
      packetIds: owned.map((packet) => packet.id),
      activeCount: owned.length,
      waitingCount: owned.filter((packet) => ["waiting", "blocked"].includes(packet.lifecycleStatus)).length,
      reviewNeededCount: owned.filter((packet) => packet.lifecycleStatus === "review-needed").length,
      handoffReadyCount: owned.filter((packet) => packet.lifecycleStatus === "ready-for-handoff").length,
      staleCount: owned.filter((packet) => packet.lifecycleStatus === "stale").length
    };
  });
}
function buildPacketContextManifest(root, board, packet, packetById, workspaceIndex) {
  const dependencyHealth = packet.dependencyHealth ?? buildPacketDependencyHealth(packet, packetById);
  const preferredPhaseContextPath = path8.join(ARTIFACT_PATHS.phaseContextsDir, `${packet.phase}.json`);
  const fallbackPhaseContextPath = path8.join(ARTIFACT_PATHS.phaseContextsDir, `${board.currentPhase}.json`);
  const phaseContextPath = fs6.existsSync(resolvePath(root, preferredPhaseContextPath)) ? preferredPhaseContextPath : fallbackPhaseContextPath;
  const upstreamPackets = dependencyHealth.dependencyIds.map((dependencyId) => summarizePacket(packetById.get(dependencyId) ?? { id: dependencyId, title: dependencyId, status: "missing", lifecycleStatus: "waiting", assignedRole: "planner", phase: packet.phase, nextAction: "Repair missing dependency reference." }));
  const downstreamPackets = Array.from(packetById.values()).filter((candidate) => (candidate.dependencies ?? []).includes(packet.id)).map(summarizePacket);
  const artifactContextPaths = uniqueSorted([
    ...[packet.packetPath, ...packet.outputPaths ?? [], ...packet.evidenceLinks ?? []].filter(Boolean).map(artifactContextPath),
    artifactContextPath(ARTIFACT_PATHS.workspaceIndex),
    artifactContextPath(ARTIFACT_PATHS.navigationReport)
  ]);
  const actionBundlePath = actionContextPath(`packet-${packet.id}`);
  return {
    version: 1,
    packetId: packet.id,
    title: packet.title,
    sourceType: packet.sourceType,
    sourceId: packet.sourceId,
    phase: packet.phase,
    lifecycleStatus: packet.lifecycleStatus,
    lifecycleFamily: packet.lifecycleFamily ?? lifecycleFamilyForPacket(packet),
    assignedRole: packet.assignedRole,
    boardPhase: board.currentPhase,
    boardAssignedRole: board.assignedRole,
    currentFocus: packet.currentFocus,
    nextAction: packet.nextAction,
    continuationState: packet.continuationState,
    taskWorkspaceCoupling: {
      packetPath: packet.packetPath,
      packetContextPath: packet.packetContextPath,
      phaseContextPath,
      workspaceIndexPath: ARTIFACT_PATHS.workspaceIndex,
      sessionSummaryPath: ARTIFACT_PATHS.sessionSummary,
      navigationReportPath: ARTIFACT_PATHS.navigationReport
    },
    dependencyHealth,
    upstreamPackets,
    downstreamPackets,
    linkedArtifacts: uniqueSorted([
      packet.packetPath,
      packet.packetContextPath,
      phaseContextPath,
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.taskPacketsIndex,
      ARTIFACT_PATHS.sessionSummary,
      ARTIFACT_PATHS.navigationReport,
      packet.lineage?.programId ? ARTIFACT_PATHS.programsIndex : null,
      packet.lineage?.programRunId ? ARTIFACT_PATHS.programRuns : null,
      packet.lineage?.approvalId ? ARTIFACT_PATHS.programApprovals : null,
      packet.materialization?.sourceArtifactPath,
      ...packet.outputPaths ?? [],
      ...packet.evidenceLinks ?? []
    ]),
    artifactContextPaths,
    actionContextPath: actionBundlePath,
    preActionReadPaths: uniqueSorted([
      actionBundlePath,
      packet.packetContextPath,
      phaseContextPath,
      path8.join(ARTIFACT_PATHS.roleContextsDir, `${packet.assignedRole}.json`),
      ...artifactContextPaths
    ]),
    behaviorDiscipline: {
      summary: "Read the packet, phase, and linked artifact context before advancing this packet.",
      localRules: [
        "Preserve explicit dependency and handoff state.",
        "Use linked artifact paths as the source of truth for local guidance.",
        "Refresh durable surfaces after packet state changes."
      ]
    },
    linkedIds: {
      dependencies: dependencyHealth.dependencyIds,
      claims: uniqueSorted(packet.claimIds),
      notes: uniqueSorted(packet.noteIds),
      experiments: uniqueSorted(packet.experimentIds),
      rebuttalIssues: uniqueSorted(packet.rebuttalIssueIds),
      versions: uniqueSorted(packet.versionIds),
      children: uniqueSorted(packet.childPacketIds),
      parentPacketId: packet.parentPacketId ?? null
    },
    materialization: packet.materialization?.sourceType && packet.materialization?.sourceId ? {
      pathType: packet.materialization.pathType ?? "guidance-to-packet",
      sourceType: packet.materialization.sourceType,
      sourceId: packet.materialization.sourceId,
      sourceArtifactPath: packet.materialization.sourceArtifactPath ?? null,
      sourceTitle: packet.materialization.sourceTitle ?? null,
      sourceSummary: packet.materialization.sourceSummary ?? null,
      followThroughId: packet.materialization.followThroughId ?? null,
      selectedConversionPathKey: packet.materialization.selectedConversionPathKey ?? null,
      executionBridgeCandidateIds: uniqueSorted(packet.materialization.executionBridgeCandidateIds ?? []),
      remediationPackIds: uniqueSorted(packet.materialization.remediationPackIds ?? []),
      createdAt: packet.materialization.createdAt ?? null,
      createdByRole: packet.materialization.createdByRole ?? null
    } : null,
    autonomyEnvelope: packet.autonomyEnvelope ? {
      controllerRole: packet.autonomyEnvelope.controllerRole,
      workerRole: packet.autonomyEnvelope.workerRole,
      scopeType: packet.autonomyEnvelope.scopeType ?? "packet-local",
      explicitOnly: packet.autonomyEnvelope.explicitOnly ?? true,
      requiredReadPaths: uniqueSorted([
        path8.join(ARTIFACT_PATHS.roleContextsDir, `${packet.autonomyEnvelope.workerRole}.json`),
        ...normalizeStringArray5(packet.autonomyEnvelope.requiredReadPaths)
      ]),
      localRules: uniqueSorted(packet.autonomyEnvelope.localRules ?? [])
    } : null,
    programLinkage: packet.lineage?.programId || packet.materialization?.programId ? {
      programId: packet.lineage?.programId ?? packet.materialization?.programId ?? null,
      programRunId: packet.lineage?.programRunId ?? packet.materialization?.programRunId ?? null,
      approvalId: packet.lineage?.approvalId ?? packet.materialization?.approvalId ?? null,
      programsPath: ARTIFACT_PATHS.programsIndex,
      runsPath: ARTIFACT_PATHS.programRuns,
      approvalsPath: ARTIFACT_PATHS.programApprovals
    } : null,
    ownershipSummary: workspaceIndex.ownershipSummary?.find((entry) => entry.roleId === packet.assignedRole) ?? null,
    generatedAt: nowIso()
  };
}
function buildOpenQuestions(notesIndex, packets, reviewState, wikiEntities) {
  const questions = [];
  for (const note of notesIndex.items ?? []) {
    for (const [index, item] of (note.openQuestions ?? []).entries()) {
      const question = normalizeQuestion(item, note.id, index);
      questions.push({ ...question, packetId: null, sectionId: note.sectionId ?? null, sourcePath: ARTIFACT_PATHS.notes });
    }
  }
  for (const packet of packets) {
    for (const question of packet.questions ?? []) {
      questions.push({ ...question, packetId: packet.id, sectionId: null, sourcePath: packet.packetPath });
    }
  }
  for (const [index, item] of (reviewState.openItems ?? []).entries()) {
    questions.push({ id: `review-open-item-${index + 1}`, summary: item, status: "open", origin: "review-state", packetId: null, sectionId: null, sourcePath: ARTIFACT_PATHS.reviewState });
  }
  for (const entity of (wikiEntities.items ?? []).filter((item) => item.entityType === "question")) {
    questions.push({ id: entity.id, summary: entity.label, status: entity.status ?? "open", origin: "wiki-entity", packetId: entity.packetId ?? null, sectionId: entity.sectionId ?? null, sourcePath: ARTIFACT_PATHS.wikiEntities });
  }
  return questions;
}
function buildDecisions(board, versionsIndex, comparisons, packets, wikiEntities) {
  const decisions = [];
  decisions.push({
    id: "board-current-role",
    summary: `Current role owner is ${board.assignedRole}.`,
    rationale: `Board phase is ${board.currentPhase}. Current focus is ${board.currentFocus}.`,
    origin: ARTIFACT_PATHS.orchestrationBoard,
    recordedAt: board.updatedAt ?? null
  });
  decisions.push({
    id: "board-next-action",
    summary: `Next action: ${board.nextAction}`,
    rationale: `Intent type is ${board.intentType}.`,
    origin: ARTIFACT_PATHS.orchestrationBoard,
    recordedAt: board.updatedAt ?? null
  });
  if (versionsIndex.currentVersionId) {
    decisions.push({
      id: "current-version",
      summary: `Current paper version is ${versionsIndex.currentVersionId}.`,
      rationale: "The versions index marks this snapshot as the live version lineage head.",
      origin: ARTIFACT_PATHS.versionsIndex,
      recordedAt: versionsIndex.updatedAt ?? null
    });
  }
  for (const comparison of comparisons.items ?? []) {
    decisions.push({
      id: `comparison-${comparison.id}`,
      summary: `Compared ${comparison.fromVersionId} -> ${comparison.toVersionId}.`,
      rationale: comparison.objectiveChanged || comparison.thesisChanged ? "The paper objective or thesis changed across this lineage step." : "The comparison tracks evidence, citation, review, audit, and bridge deltas across versions.",
      origin: ARTIFACT_PATHS.versionComparisons,
      recordedAt: comparison.createdAt ?? null
    });
  }
  for (const packet of packets) {
    for (const decision of packet.decisions ?? []) {
      decisions.push({ ...decision, packetId: packet.id, origin: packet.packetPath, recordedAt: decision.recordedAt ?? packet.updatedAt ?? null });
    }
  }
  for (const entity of (wikiEntities.items ?? []).filter((item) => item.entityType === "decision")) {
    decisions.push({ id: entity.id, summary: entity.label, rationale: entity.summary ?? "", packetId: entity.packetId ?? null, origin: ARTIFACT_PATHS.wikiEntities, recordedAt: entity.updatedAt ?? null });
  }
  return decisions;
}
function buildTaskGraph(packets, autonomyLoops = null) {
  const nodeIds = new Set(packets.map((packet) => packet.id));
  const edges = [];
  for (const packet of packets) {
    for (const dependency of packet.dependencies) {
      if (nodeIds.has(dependency)) {
        edges.push({ from: packet.id, to: dependency, type: "depends-on" });
      }
    }
    if (packet.parentPacketId && nodeIds.has(packet.parentPacketId)) {
      edges.push({ from: packet.id, to: packet.parentPacketId, type: "child-of" });
    }
    for (const childId of packet.childPacketIds) {
      if (nodeIds.has(childId)) {
        edges.push({ from: packet.id, to: childId, type: "parent-of" });
      }
    }
    for (const versionId of packet.versionIds) {
      const target = `version-${versionId}`;
      if (packet.id !== target && nodeIds.has(target)) {
        edges.push({ from: packet.id, to: target, type: "version-link" });
      }
    }
    for (const issueId of packet.rebuttalIssueIds) {
      const target = deterministicBoundedTaskPacketId("rebuttal", issueId);
      if (packet.id !== target && nodeIds.has(target)) {
        edges.push({ from: packet.id, to: target, type: "rebuttal-link" });
      }
    }
    for (const experimentId of packet.experimentIds) {
      const target = `experiment-${experimentId}`;
      if (packet.id !== target && nodeIds.has(target)) {
        edges.push({ from: packet.id, to: target, type: "experiment-link" });
      }
    }
  }
  return { nodes: packets, edges, autonomyLoops };
}
function buildAutonomyLoopSummary({ board, packets, openQuestions = [], metaOptimize = null, runtime = null, programs = null, repairFrontier = null }) {
  const activePackets2 = packets.filter((packet) => packet.active);
  const waitingPackets = packets.filter((packet) => ["waiting", "blocked"].includes(packet.lifecycleStatus));
  const reviewPackets = packets.filter((packet) => packet.lifecycleStatus === "review-needed");
  const closedPackets = packets.filter((packet) => GOVERNANCE_TERMINAL_LIFECYCLES.has(packet.lifecycleStatus));
  const remediationSummary = metaOptimize?.remediationPacks ?? {};
  const followThroughSummary = metaOptimize?.followThrough ?? {};
  const executionBridgeSummary = metaOptimize?.executionBridgeCandidates ?? {};
  const runtimePointerIds = uniqueSorted([
    runtime?.lastRunId,
    runtime?.lastSelectedPacketId,
    runtime?.currentContinuationPacketId,
    runtime?.currentContinuationProgramRunId
  ]);
  const approvalPointerIds = uniqueSorted([
    programs?.currentApprovalId,
    ...programs?.topApprovalIds ?? []
  ]);
  const followThroughPointerIds = uniqueSorted([
    ...followThroughSummary.topSourceIds ?? [],
    ...followThroughSummary.dueSoonExecutionIds ?? [],
    ...followThroughSummary.dueReviewIds ?? [],
    ...followThroughSummary.overdueExecutionIds ?? []
  ]);
  const reviewCheckpointActive = (programs?.reviewCheckpointRunCount ?? 0) > 0 || (programs?.reviewNeededRunCount ?? 0) > 0;
  const packetRunStage = reviewCheckpointActive ? "review-checkpoint" : runtime?.lastStatus === "completed" ? "runtime-result" : approvalPointerIds.length > 0 ? "approval" : followThroughSummary.acceptedForExecutionCount > 0 ? "packet" : remediationSummary.packCount > 0 || executionBridgeSummary.candidateCount > 0 ? "proposal" : "scan";
  const packetApprovalState = reviewCheckpointActive ? "fresh-approval-required" : approvalPointerIds.length > 0 ? "approval-available" : "approval-missing";
  const packetRuntimeState = reviewCheckpointActive ? "review-checkpoint-recorded" : runtime?.lastStatus === "completed" ? "runtime-result-recorded" : runtime?.lastStatus && runtime.lastStatus !== "never-run" ? runtime.lastStatus : "runtime-not-started";
  const packetFollowThroughState = followThroughSummary.closedCount > 0 ? "follow-through-closed" : followThroughSummary.acceptedForExecutionCount > 0 ? "accepted-for-execution" : "follow-through-open";
  const packetLifecycleState = reviewCheckpointActive ? "review-checkpoint-awaiting-fresh-approval" : runtime?.lastStatus === "completed" ? packetFollowThroughState === "follow-through-closed" ? "runtime-result-and-follow-through-closed" : "runtime-result-awaiting-follow-through" : packetApprovalState === "approval-available" ? "approval-ready" : packetFollowThroughState === "accepted-for-execution" ? "packet-accepted-awaiting-approval-or-run" : packetRunStage;
  const blockerIds = uniqueSorted([
    ...waitingPackets.map((packet) => packet.id),
    ...reviewCheckpointActive ? [...reviewPackets.map((packet) => packet.id), programs?.currentReviewCheckpointRunId, programs?.currentReviewCheckpointPacketId] : [],
    ...(repairFrontier?.prioritizedItems ?? []).slice(0, 5).map((item) => item.id)
  ]);
  const loops = [
    {
      id: "packet-approval-run-follow-through",
      family: "Packet \u2192 Approval \u2192 Run \u2192 Runtime Result \u2192 Follow-through",
      sourceArtifact: ARTIFACT_PATHS.taskPacketsIndex,
      targetArtifact: ARTIFACT_PATHS.runtimeResults,
      currentStage: packetRunStage,
      lifecycleState: packetLifecycleState,
      approvalState: packetApprovalState,
      runtimeState: packetRuntimeState,
      followThroughState: packetFollowThroughState,
      nextSafeAction: reviewCheckpointActive ? `Review checkpoint ${programs?.currentReviewCheckpointRunId ?? "current-run"} and continue through project:dove.auto with explicit fresh approval.` : runtime?.currentContinuationCommand ?? "Use project:dove.mission to confirm and materialize the mission before any foreground autonomy run.",
      approvalPointers: approvalPointerIds,
      runtimePointers: runtimePointerIds,
      followThroughPointers: followThroughPointerIds,
      blockers: blockerIds,
      closureState: packetLifecycleState
    },
    {
      id: "question-evidence-claim",
      family: "Question \u2192 Evidence \u2192 Claim",
      sourceArtifact: ARTIFACT_PATHS.notes,
      targetArtifact: ARTIFACT_PATHS.evidence,
      currentStage: openQuestions.some((item) => item.status !== "answered") ? "question" : "evidence",
      lifecycleState: openQuestions.some((item) => item.status !== "answered") ? "question-open" : "evidence-ready-for-claim",
      nextSafeAction: openQuestions.find((item) => item.status !== "answered")?.summary ?? "Record evidence-backed claims through project:dove.experience.",
      approvalPointers: [],
      runtimePointers: [],
      followThroughPointers: [],
      blockers: openQuestions.filter((item) => item.status !== "answered").map((item) => item.id),
      closureState: openQuestions.some((item) => item.status !== "answered") ? "open" : "ready-for-claim"
    },
    {
      id: "debt-pack-closure",
      family: "Debt \u2192 Pack \u2192 Closure",
      sourceArtifact: ARTIFACT_PATHS.metaRecommendations,
      targetArtifact: ARTIFACT_PATHS.metaOperatorFollowThrough,
      currentStage: remediationSummary.packCount > 0 ? executionBridgeSummary.candidateCount > 0 ? "pack" : "debt" : "scan",
      lifecycleState: followThroughSummary.closedCount > 0 ? "closure-recorded" : executionBridgeSummary.candidateCount > 0 ? "pack-ready-for-follow-through" : remediationSummary.packCount > 0 ? "debt-packed" : "scan-ready",
      nextSafeAction: remediationSummary.topPackIds?.[0] ? `Review remediation pack ${remediationSummary.topPackIds[0]} and record explicit follow-through.` : "Run project:dove.status to inspect proposal-only debt.",
      approvalPointers: [],
      runtimePointers: [],
      followThroughPointers: followThroughPointerIds,
      blockers: (repairFrontier?.prioritizedItems ?? []).slice(0, 5).map((item) => item.id),
      closureState: followThroughSummary.closedCount > 0 ? "partially-closed" : "open"
    },
    {
      id: "board-role-artifact-handoff",
      family: "Board \u2192 Role \u2192 Artifact \u2192 Handoff",
      sourceArtifact: ARTIFACT_PATHS.orchestrationBoard,
      targetArtifact: ARTIFACT_PATHS.orchestrationHandoffs,
      currentStage: activePackets2.length > 0 ? "role" : "board",
      lifecycleState: activePackets2.length > 0 ? activePackets2.some((packet) => packet.assignedRole !== board.assignedRole) ? "handoff-needed" : "role-artifact-active" : "board-ready",
      nextSafeAction: board.nextAction,
      approvalPointers: [],
      runtimePointers: [],
      followThroughPointers: [],
      blockers: activePackets2.filter((packet) => packet.assignedRole !== board.assignedRole).map((packet) => packet.id),
      closureState: activePackets2.length > 0 ? "handoff-open" : "board-ready"
    }
  ];
  const blockedCount = loops.filter((loop) => loop.blockers.length > 0).length;
  const closedCount = loops.filter((loop) => ["runtime-result-and-follow-through-closed", "runtime-result-recorded", "ready-for-claim", "partially-closed", "board-ready"].includes(loop.closureState)).length;
  const readyCount = loops.length - blockedCount;
  const currentLoop = loops.find((loop) => loop.blockers.length > 0) ?? loops[0];
  return {
    contractVersion: "unified-autonomy-loop-v1",
    activeLifecycleState: currentLoop.lifecycleState ?? currentLoop.closureState,
    loopCount: loops.length,
    blockedCount,
    readyCount,
    closedCount,
    currentLoopId: currentLoop.id,
    nextSafeAction: currentLoop.nextSafeAction,
    explicitOnly: true,
    noHiddenRuntime: true,
    families: loops.map((loop) => loop.family),
    loops,
    approvalPointers: uniqueSorted(loops.flatMap((loop) => loop.approvalPointers)),
    runtimePointers: uniqueSorted(loops.flatMap((loop) => loop.runtimePointers)),
    followThroughPointers: uniqueSorted(loops.flatMap((loop) => loop.followThroughPointers)),
    blockerIds: uniqueSorted(loops.flatMap((loop) => loop.blockers)),
    closureStates: uniqueSorted(loops.map((loop) => loop.closureState)),
    lifecycleStates: uniqueSorted(loops.map((loop) => loop.lifecycleState).filter(Boolean)),
    safeExecutionPath: "project:dove.mission -> project:dove.auto",
    overview: `${loops.length} unified autonomy loop families are visible; ${blockedCount} blocked, ${readyCount} ready, ${closedCount} carrying closure evidence. Execution remains explicit foreground-only.`
  };
}
function renderAutonomyLoopOverviewLines(autonomyLoops = {}) {
  const summary = autonomyLoops && typeof autonomyLoops === "object" && !Array.isArray(autonomyLoops) ? autonomyLoops : {};
  return [
    `- Unified autonomy loops: ${summary.loopCount ?? 0} families (${summary.readyCount ?? 0} ready, ${summary.blockedCount ?? 0} blocked, ${summary.closedCount ?? 0} with closure evidence)`,
    `- Unified autonomy overview: ${summary.overview ?? "No unified autonomy loop summary has been generated yet."}`,
    `- Unified autonomy current loop: ${summary.currentLoopId ?? "none"}`,
    `- Unified autonomy lifecycle state: ${summary.activeLifecycleState ?? "unknown"}`,
    `- Unified autonomy next safe action: ${summary.nextSafeAction ?? "Refresh the board and choose the next explicit operator action."}`,
    `- Unified autonomy safe execution path: ${summary.safeExecutionPath ?? "project:dove.mission -> project:dove.auto"}`,
    `- Unified autonomy blockers: ${(summary.blockerIds ?? []).join(", ") || "none"}`,
    `- Unified autonomy approvals: ${(summary.approvalPointers ?? []).join(", ") || "none"}`,
    `- Unified autonomy runtime pointers: ${(summary.runtimePointers ?? []).join(", ") || "none"}`,
    `- Unified autonomy follow-through pointers: ${(summary.followThroughPointers ?? []).join(", ") || "none"}`,
    ...(summary.loops ?? []).map((loop) => `  - ${loop.id}: ${loop.currentStage} -> ${loop.targetArtifact} | lifecycle=${loop.lifecycleState ?? "unknown"} | closure=${loop.closureState} | next=${loop.nextSafeAction}`)
  ];
}
function renderSessionSummary(state, board, packets, openQuestions, decisions, primaryRoleRoster, workspaceIndex) {
  const activePackets2 = sortPacketsForQueue(packets.filter((packet) => packet.active));
  return [
    "# Latest session summary",
    "",
    `- Updated: ${nowIso()}`,
    `- Phase: ${board.currentPhase}`,
    `- Intent: ${board.intentType}`,
    `- Assigned role: ${board.assignedRole}`,
    `- Resume command: ${state.pipeline.resumeCommand}`,
    `- Current focus: ${board.currentFocus}`,
    `- Next action: ${board.nextAction}`,
    `- Continuation state: ${board.continuationState?.status ?? "unknown"}`,
    `- Resume guidance: ${workspaceIndex.resumeGuidance?.command ?? state.pipeline.resumeCommand ?? resolveResumeCommandForPhase(board.currentPhase)}`,
    `- Lifecycle family: ${workspaceIndex.lifecycle?.boardFamily ?? lifecycleFamilyForPhase(board.currentPhase)}`,
    `- Current version: ${board.versionLineage?.currentVersionId ?? "none"}`,
    "",
    "## Active task packets",
    "",
    ...activePackets2.length > 0 ? activePackets2.map((packet) => `- ${packet.id}: ${packet.title} [${packet.status} | ${packet.lifecycleStatus}] (${packet.assignedRole})${describePacketProvenance(packet) ? ` | ${describePacketProvenance(packet)}` : ""} \u2192 next: ${packet.nextAction}`) : ["- No active task packets."],
    "",
    "## Work queues",
    "",
    `- Ready: ${(workspaceIndex.workQueues?.ready ?? []).map((packet) => packet.id).join(", ") || "none"}`,
    `- Waiting: ${(workspaceIndex.workQueues?.waiting ?? []).map((packet) => packet.id).join(", ") || "none"}`,
    `- Review needed: ${(workspaceIndex.workQueues?.reviewNeeded ?? []).map((packet) => packet.id).join(", ") || "none"}`,
    `- Ready for handoff: ${(workspaceIndex.workQueues?.handoff ?? []).map((packet) => packet.id).join(", ") || "none"}`,
    `- Stale: ${(workspaceIndex.workQueues?.stale ?? []).map((packet) => packet.id).join(", ") || "none"}`,
    "",
    "## Open questions",
    "",
    ...openQuestions.length > 0 ? openQuestions.filter((item) => item.status !== "answered").map((question) => `- ${question.id}: ${question.summary}`) : ["- No open questions recorded."],
    "",
    "## Recent decisions",
    "",
    ...decisions.length > 0 ? decisions.slice(-10).reverse().map((decision) => `- ${decision.id}: ${decision.summary}`) : ["- No decisions recorded."],
    "",
    "## Workspace overview",
    "",
    `- Active roles: ${(workspaceIndex.activeRoles ?? []).join(", ") || "none"}`,
    `- Unresolved concerns: ${(workspaceIndex.unresolvedConcernIds ?? []).join(", ") || "none"}`,
    `- Dependency health: blocked=${workspaceIndex.dependencyHealth?.blockedPacketIds?.length ?? 0} waiting=${workspaceIndex.dependencyHealth?.waitingPacketIds?.length ?? 0} stale=${workspaceIndex.dependencyHealth?.stalePacketIds?.length ?? 0} missing=${workspaceIndex.dependencyHealth?.missingDependencyIds?.length ?? 0}`,
    `- Handoff obligations: ${(workspaceIndex.handoffObligations ?? []).map((item) => item.packetId).join(", ") || "none"}`,
    `- Repair frontier: ${workspaceIndex.repairFrontier?.count ?? 0} items (relations ${workspaceIndex.repairFrontier?.relationIssueCount ?? 0}, degraded families ${workspaceIndex.repairFrontier?.relationFamilyIssueCount ?? 0}, managed artifacts ${workspaceIndex.repairFrontier?.managedArtifactIssueCount ?? 0}, governance ${workspaceIndex.repairFrontier?.governanceIssueCount ?? 0})`,
    `- Paper lifecycle: ${workspaceIndex.lifecycle?.topFamilies?.join(", ") || workspaceIndex.lifecycle?.boardFamily || "none"}`,
    `- Relation taxonomy: ${workspaceIndex.repairFrontier?.taxonomyOverview ?? "No degraded typed wiki relation families are currently summarized."}`,
    ...(workspaceIndex.repairFrontier?.relationFamilySummaries ?? []).slice(0, 3).map((family) => `  - family ${family.id}: ${family.overview}`),
    ...(workspaceIndex.repairFrontier?.relationGroupSummaries ?? []).slice(0, 3).map((group) => `  - group ${group.id}: ${group.overview}`),
    ...(workspaceIndex.repairFrontier?.prioritizedItems ?? []).slice(0, 4).map((item) => `  - ${item.frontierType}: ${item.summary}`),
    ...renderRuntimeOverviewLines(workspaceIndex.runtime),
    ...renderProgramOverviewLines(workspaceIndex.programs),
    ...renderCampaignOverviewLines(workspaceIndex.campaigns),
    ...renderAutonomyLoopOverviewLines(workspaceIndex.autonomyLoops),
    ...renderMetaOptimizeOverviewLines(workspaceIndex.metaOptimize),
    "",
    "## Role context manifests",
    "",
    ...primaryRoleRoster.map((role) => `- ${role.id}: ${path8.join(ARTIFACT_PATHS.roleContextsDir, `${role.id}.json`)}`)
  ].join("\n");
}
function renderNavigationReport(board, taskGraph, openQuestions, decisions, versionsIndex, comparisons, workspaceIndex) {
  const readyForHandoff = sortPacketsForQueue(taskGraph.nodes.filter((packet) => packet.lifecycleStatus === "ready-for-handoff"));
  const stalePackets = sortPacketsForQueue(taskGraph.nodes.filter((packet) => packet.lifecycleStatus === "stale"));
  return [
    "# Navigation",
    "",
    `- Phase: ${board.currentPhase}`,
    `- Intent: ${board.intentType}`,
    `- Assigned role: ${board.assignedRole}`,
    `- Current focus: ${board.currentFocus}`,
    `- Next action: ${board.nextAction}`,
    "",
    "## Task graph",
    "",
    ...taskGraph.nodes.length > 0 ? taskGraph.nodes.map((packet) => `- ${packet.id}: ${packet.title} [${packet.status} | ${packet.lifecycleStatus}] parent=${packet.parentPacketId || "none"} depends on ${packet.dependencies.join(", ") || "none"} dependency-health=${packet.dependencyHealth?.state ?? "clear"}${describePacketProvenance(packet) ? ` | ${describePacketProvenance(packet)}` : ""} \u2192 next ${packet.nextAction}`) : ["- No task packets generated yet."],
    "",
    "## Operating queues",
    "",
    `- Ready for handoff: ${readyForHandoff.map((packet) => packet.id).join(", ") || "none"}`,
    `- Stale packets: ${stalePackets.map((packet) => packet.id).join(", ") || "none"}`,
    `- Repair frontier items: ${workspaceIndex.repairFrontier?.count ?? 0} (relations ${workspaceIndex.repairFrontier?.relationIssueCount ?? 0}, degraded families ${workspaceIndex.repairFrontier?.relationFamilyIssueCount ?? 0}, managed artifacts ${workspaceIndex.repairFrontier?.managedArtifactIssueCount ?? 0}, governance ${workspaceIndex.repairFrontier?.governanceIssueCount ?? 0})`,
    `- Paper lifecycle: ${workspaceIndex.lifecycle?.topFamilies?.join(", ") || workspaceIndex.lifecycle?.boardFamily || "none"}`,
    `- Relation taxonomy: ${workspaceIndex.repairFrontier?.taxonomyOverview ?? "No degraded typed wiki relation families are currently summarized."}`,
    ...(workspaceIndex.repairFrontier?.relationFamilySummaries ?? []).slice(0, 3).map((family) => `  - family ${family.id}: ${family.overview}`),
    ...(workspaceIndex.repairFrontier?.relationGroupSummaries ?? []).slice(0, 3).map((group) => `  - group ${group.id}: ${group.overview}`),
    ...(workspaceIndex.repairFrontier?.prioritizedItems ?? []).slice(0, 5).map((item) => `  - ${item.frontierType}: ${item.summary}`),
    ...renderRuntimeOverviewLines(workspaceIndex.runtime),
    ...renderProgramOverviewLines(workspaceIndex.programs),
    ...renderCampaignOverviewLines(workspaceIndex.campaigns),
    ...renderAutonomyLoopOverviewLines(workspaceIndex.autonomyLoops),
    ...renderMetaOptimizeOverviewLines(workspaceIndex.metaOptimize),
    "",
    "## Open questions",
    "",
    ...openQuestions.length > 0 ? openQuestions.filter((item) => item.status !== "answered").map((question) => `- ${question.id}: ${question.summary}`) : ["- No open questions recorded yet."],
    "",
    "## Decisions",
    "",
    ...decisions.length > 0 ? decisions.slice(-12).reverse().map((decision) => `- ${decision.id}: ${decision.summary}`) : ["- No durable decisions recorded yet."],
    "",
    "## Lineage",
    "",
    `- Current version: ${versionsIndex.currentVersionId ?? "none"}`,
    `- Snapshots: ${(versionsIndex.items ?? []).map((item) => item.id).join(", ") || "none"}`,
    `- Active comparison targets: ${(comparisons.activeTargets ?? []).join(", ") || "none"}`
  ].join("\n");
}
function buildRoleManifest(role, packets, openQuestions, decisions, workspaceIndex, remediationPacks = {}, operatorPlaybooks = {}, executionBridgeCandidates = {}, operatorFollowThrough = {}, operatorLessons = {}) {
  const roleMetadata = ROLE_HIERARCHY[role.id] ?? role;
  const ownedRoleIds = [role.id, ...roleMetadata.subagents ?? []].filter((roleId) => ROLE_IDS.includes(roleId));
  const rolePackets = packets.filter((packet) => ownedRoleIds.includes(packet.assignedRole) && packet.active);
  const roleEnvelopePackets = rolePackets.filter((packet) => ownedRoleIds.includes(packet.autonomyEnvelope?.workerRole));
  const roleQuestionIds = rolePackets.flatMap((packet) => (packet.questions ?? []).filter((item) => item.status !== "answered").map((item) => item.id));
  const roleDecisionIds = decisions.filter((item) => item.packetId ? rolePackets.some((packet) => packet.id === item.packetId) : ["board-current-role", "board-next-action"].includes(item.id)).map((item) => item.id);
  const localArtifactContextPaths = uniqueSorted(roleContextPaths(role.id).map(artifactContextPath));
  const actionBundlePath = actionContextPath(`role-${role.id}`);
  return {
    version: 1,
    roleId: role.id,
    label: role.label,
    kind: roleMetadata.kind ?? "primary",
    manuallySwitchable: roleMetadata.manuallySwitchable ?? true,
    parentRole: roleMetadata.parentRole ?? null,
    canonicalRole: roleMetadata.canonicalRole ?? roleMetadata.aliasOf ?? null,
    subagents: roleMetadata.subagents ?? [],
    charter: role.charter,
    contextPaths: roleContextPaths(role.id),
    localArtifactContextPaths,
    activeTaskPacketIds: rolePackets.map((packet) => packet.id),
    packetContextPaths: rolePackets.map((packet) => packet.packetContextPath),
    actionContextPath: actionBundlePath,
    preActionReadPaths: uniqueSorted([
      actionBundlePath,
      path8.join(ARTIFACT_PATHS.roleContextsDir, `${role.id}.json`),
      path8.join(ARTIFACT_PATHS.phaseContextsDir, `${workspaceIndex.boardPhase}.json`),
      ...rolePackets.map((packet) => packet.packetContextPath),
      ...localArtifactContextPaths
    ]),
    behaviorDiscipline: {
      summary: `Read the nearest role, packet, and artifact context before acting as ${role.id}.`,
      localRules: [
        "Prefer the narrowest durable guidance surface that matches the task.",
        "Do not treat board ownership as permission to skip packet or artifact-local guidance.",
        "Keep role decisions and handoffs explicit in durable artifacts."
      ]
    },
    openQuestionIds: Array.from(new Set(roleQuestionIds.concat(openQuestions.filter((item) => item.origin === "review-state" && role.id === "reviewer").map((item) => item.id)))),
    decisionIds: Array.from(new Set(roleDecisionIds)),
    workspaceIndexPath: ARTIFACT_PATHS.workspaceIndex,
    isCurrentBoardOwner: roleCanActAs(workspaceIndex.boardAssignedRole, role.id),
    boardPhase: workspaceIndex.boardPhase,
    boardAssignedRole: workspaceIndex.boardAssignedRole,
    boardIntentType: workspaceIndex.boardIntentType,
    lifecycle: {
      taxonomyVersion: workspaceIndex.lifecycle?.taxonomyVersion ?? PAPER_LIFECYCLE_TAXONOMY_VERSION,
      ownedFamilies: uniqueSorted(rolePackets.map((packet) => packet.lifecycleFamily ?? lifecycleFamilyForPacket(packet))),
      boardFamily: workspaceIndex.lifecycle?.boardFamily ?? lifecycleFamilyForPhase(workspaceIndex.boardPhase)
    },
    currentFocus: rolePackets[0]?.currentFocus ?? workspaceIndex.currentFocus ?? null,
    queueSummary: workspaceIndex.ownershipSummary?.find((entry) => entry.roleId === role.id) ?? null,
    autonomyEnvelopePacketIds: roleEnvelopePackets.map((packet) => packet.id),
    handoffCandidateIds: (workspaceIndex.handoffObligations ?? []).filter((item) => item.toRole === role.id || item.fromRole === role.id).map((item) => item.packetId),
    operatorGuidance: buildOperatorGuidance(workspaceIndex, remediationPacks, operatorPlaybooks, executionBridgeCandidates, operatorFollowThrough, operatorLessons, { roleId: role.id }),
    generatedAt: nowIso()
  };
}
function buildPhaseManifest(board, packets, workspaceIndex, remediationPacks = {}, operatorPlaybooks = {}, executionBridgeCandidates = {}, operatorFollowThrough = {}, operatorLessons = {}) {
  const phasePackets = sortPacketsForQueue(packets.filter((packet) => packet.phase === board.currentPhase && packet.active));
  const artifactContextPaths = uniqueSorted([
    artifactContextPath(ARTIFACT_PATHS.orchestrationBoard),
    artifactContextPath(ARTIFACT_PATHS.workspaceIndex),
    artifactContextPath(ARTIFACT_PATHS.navigationReport),
    ...phasePackets.flatMap((packet) => [packet.packetPath, ...packet.outputPaths ?? [], ...packet.evidenceLinks ?? []].filter(Boolean).map(artifactContextPath))
  ]);
  const actionBundlePath = actionContextPath(`phase-${board.currentPhase}`);
  return {
    version: 1,
    phaseId: board.currentPhase,
    lifecycleFamily: lifecycleFamilyForPhase(board.currentPhase),
    intentType: board.intentType,
    currentFocus: board.currentFocus,
    nextAction: board.nextAction,
    assignedRole: board.assignedRole,
    activePacketIds: phasePackets.map((packet) => packet.id),
    blockerIds: packets.filter((packet) => packet.sourceType === "blocker" && packet.active).map((packet) => packet.id),
    queueSummary: {
      ready: phasePackets.filter((packet) => ["active", "queued"].includes(packet.lifecycleStatus)).map((packet) => packet.id),
      waiting: phasePackets.filter((packet) => ["waiting", "blocked"].includes(packet.lifecycleStatus)).map((packet) => packet.id),
      reviewNeeded: phasePackets.filter((packet) => packet.lifecycleStatus === "review-needed").map((packet) => packet.id),
      handoff: phasePackets.filter((packet) => packet.lifecycleStatus === "ready-for-handoff").map((packet) => packet.id),
      stale: phasePackets.filter((packet) => packet.lifecycleStatus === "stale").map((packet) => packet.id)
    },
    contextPaths: uniqueSorted([
      ARTIFACT_PATHS.orchestrationBoard,
      ARTIFACT_PATHS.orchestrationHandoffs,
      ARTIFACT_PATHS.taskPacketsIndex,
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.sessionSummary,
      ARTIFACT_PATHS.navigationReport,
      ...phasePackets.map((packet) => packet.packetContextPath)
    ]),
    artifactContextPaths,
    actionContextPath: actionBundlePath,
    preActionReadPaths: uniqueSorted([
      actionBundlePath,
      path8.join(ARTIFACT_PATHS.roleContextsDir, `${board.assignedRole}.json`),
      path8.join(ARTIFACT_PATHS.phaseContextsDir, `${board.currentPhase}.json`),
      ...phasePackets.map((packet) => packet.packetContextPath),
      ...artifactContextPaths
    ]),
    behaviorDiscipline: {
      summary: "Use this phase manifest to narrow local guidance before phase-scoped work.",
      localRules: [
        "Start with the board owner and queue state, then drop into packet and artifact context.",
        "Treat stale, review-needed, and handoff packets as higher-priority than general queue work.",
        "Keep phase guidance explicit and file-backed."
      ]
    },
    operatorGuidance: buildOperatorGuidance(workspaceIndex, remediationPacks, operatorPlaybooks, executionBridgeCandidates, operatorFollowThrough, operatorLessons, { roleId: board.assignedRole }),
    resumeGuidance: workspaceIndex.resumeGuidance,
    generatedAt: nowIso()
  };
}
function severityRank(value) {
  return value === "high" ? 0 : value === "medium" ? 1 : 2;
}
function recommendationSortRank(value) {
  return value === "critical" ? 0 : value === "high" ? 1 : value === "medium" ? 2 : 3;
}
function recommendationPriorityWeight(value) {
  return value === "critical" ? 40 : value === "high" ? 24 : value === "medium" ? 12 : 4;
}
var TAXONOMY_OPERATOR_PRESSURE_BY_FAMILY = {
  "evidence-grounding": {
    clusterId: "evidence-grounding-pressure",
    clusterLabel: "Evidence grounding pressure",
    clusterSummary: "Typed wiki degradation shows evidence-grounding links are slipping, so claim and idea support may be less trustworthy than the workflow expects.",
    clusterOperatorGoal: "Restore source and idea grounding before promoting claims, summaries, or revisions.",
    longHorizonFamilyId: "taxonomy-evidence-grounding",
    longHorizonLabel: "Evidence grounding pressure",
    longHorizonSummary: "Evidence-grounding relation families keep degrading across optimizer snapshots.",
    pressureArea: "evidence-grounding"
  },
  "validation-loop": {
    clusterId: "validation-loop-pressure",
    clusterLabel: "Validation-loop pressure",
    clusterSummary: "Typed wiki degradation shows claim-to-experiment validation links are drifting, so experiment-backed closure remains unstable.",
    clusterOperatorGoal: "Repair validation-loop relations before treating experiments or claims as cleanly connected.",
    longHorizonFamilyId: "taxonomy-validation-loop",
    longHorizonLabel: "Validation-loop pressure",
    longHorizonSummary: "Validation-loop relation families keep degrading across optimizer snapshots.",
    pressureArea: "validation-loop"
  },
  "review-pressure": {
    clusterId: "review-pressure",
    clusterLabel: "Review pressure",
    clusterSummary: "Typed wiki degradation shows review-pressure links are accumulating, so reviewer concerns are staying active around important claims.",
    clusterOperatorGoal: "Reduce review-pressure before treating concerns as closed or rebuttal-ready.",
    longHorizonFamilyId: "taxonomy-review-pressure",
    longHorizonLabel: "Review pressure",
    longHorizonSummary: "Review-pressure relation families keep degrading across optimizer snapshots.",
    pressureArea: "review-pressure"
  }
};
function taxonomyPressureMetadata(familyId, fallbackLabel = null) {
  const normalizedFamilyId = typeof familyId === "string" && familyId.trim() ? familyId.trim() : "uncategorized";
  const configured = TAXONOMY_OPERATOR_PRESSURE_BY_FAMILY[normalizedFamilyId];
  if (configured) {
    return configured;
  }
  const readable = fallbackLabel ?? normalizedFamilyId.replace(/-/g, " ");
  return {
    clusterId: `taxonomy-${normalizedFamilyId}`,
    clusterLabel: `${readable[0]?.toUpperCase() ?? "T"}${readable.slice(1)} pressure`,
    clusterSummary: `${readable[0]?.toUpperCase() ?? "T"}${readable.slice(1)} relation degradation remains visible in the repair frontier and should stay operator-visible until repaired.`,
    clusterOperatorGoal: `Repair ${readable} relation degradation before it compounds into broader workflow debt.`,
    longHorizonFamilyId: `taxonomy-${normalizedFamilyId}`,
    longHorizonLabel: `${readable[0]?.toUpperCase() ?? "T"}${readable.slice(1)} pressure`,
    longHorizonSummary: `${readable[0]?.toUpperCase() ?? "T"}${readable.slice(1)} relation degradation keeps appearing across optimizer snapshots.`,
    pressureArea: normalizedFamilyId
  };
}
function toLabelMap(items = []) {
  return new Map(
    (items ?? []).filter((item) => item && typeof item === "object" && typeof item.id === "string" && item.id.trim()).map((item) => [item.id, item.label ?? item.id])
  );
}
function buildTaxonomyPressure(familyIds = [], groupIds = [], familyLabelMap = /* @__PURE__ */ new Map(), groupLabelMap = /* @__PURE__ */ new Map()) {
  const normalizedFamilyIds = uniqueSorted((familyIds ?? []).filter(Boolean));
  const normalizedGroupIds = uniqueSorted((groupIds ?? []).filter(Boolean));
  const pressureAreas = uniqueSorted(normalizedFamilyIds.map((familyId) => taxonomyPressureMetadata(familyId, familyLabelMap.get(familyId)).pressureArea));
  const familyLabels = normalizedFamilyIds.map((familyId) => familyLabelMap.get(familyId) ?? familyId);
  const groupLabels = normalizedGroupIds.map((groupId) => groupLabelMap.get(groupId) ?? groupId);
  const overview = normalizedFamilyIds.length > 0 ? `Taxonomy pressure is concentrated in ${familyLabels.join(", ")}${groupLabels.length > 0 ? ` via ${groupLabels.join(", ")}` : ""}.` : "No typed wiki taxonomy pressure is active in this optimizer surface.";
  return {
    familyIds: normalizedFamilyIds,
    groupIds: normalizedGroupIds,
    familyLabels,
    groupLabels,
    pressureAreas,
    overview
  };
}
function collectTaxonomyPressureSummary(items = []) {
  const familyCounts = {};
  const groupCounts = {};
  const familyLabelMap = /* @__PURE__ */ new Map();
  const groupLabelMap = /* @__PURE__ */ new Map();
  for (const item of items ?? []) {
    const taxonomyPressure = item?.taxonomyPressure ?? {};
    for (const [index, familyId] of (taxonomyPressure.familyIds ?? []).entries()) {
      incrementObjectCount(familyCounts, familyId);
      familyLabelMap.set(familyId, taxonomyPressure.familyLabels?.[index] ?? familyId);
    }
    for (const [index, groupId] of (taxonomyPressure.groupIds ?? []).entries()) {
      incrementObjectCount(groupCounts, groupId);
      groupLabelMap.set(groupId, taxonomyPressure.groupLabels?.[index] ?? groupId);
    }
  }
  const topTaxonomyFamilyIds = topFrequencyIds(Object.entries(familyCounts).flatMap(([familyId, count]) => Array(count).fill(familyId)));
  const topTaxonomyGroupIds = topFrequencyIds(Object.entries(groupCounts).flatMap(([groupId, count]) => Array(count).fill(groupId)));
  const pressureAreas = uniqueSorted(topTaxonomyFamilyIds.map((familyId) => taxonomyPressureMetadata(familyId, familyLabelMap.get(familyId)).pressureArea));
  const taxonomyOverview = topTaxonomyFamilyIds.length > 0 ? `Taxonomy-aware optimizer pressure is led by ${topTaxonomyFamilyIds.map((familyId) => familyLabelMap.get(familyId) ?? familyId).join(", ")}${topTaxonomyGroupIds.length > 0 ? ` across ${topTaxonomyGroupIds.map((groupId) => groupLabelMap.get(groupId) ?? groupId).join(", ")}` : ""}.` : "No typed wiki taxonomy pressure is currently active in the optimizer frontier.";
  return {
    familyCounts,
    groupCounts,
    topTaxonomyFamilyIds,
    topTaxonomyGroupIds,
    pressureAreas,
    taxonomyOverview
  };
}
function deriveRecommendationCluster(recommendation = {}) {
  const taxonomyPressure = recommendation.taxonomyPressure ?? {};
  if (recommendation.category === "artifact-health" && (taxonomyPressure.familyIds ?? []).length > 0) {
    const primaryFamilyId = taxonomyPressure.familyIds[0];
    const metadata = taxonomyPressureMetadata(primaryFamilyId, taxonomyPressure.familyLabels?.[0]);
    return {
      id: metadata.clusterId,
      label: metadata.clusterLabel,
      summary: metadata.clusterSummary,
      operatorGoal: metadata.clusterOperatorGoal
    };
  }
  switch (recommendation.category) {
    case "review-discipline":
    case "repair-pattern":
      return {
        id: "review-closure",
        label: "Review closure",
        summary: "Recurring or escalated review debt that needs an explicit closure checkpoint.",
        operatorGoal: "Close durable reviewer concerns instead of rediscovering them in later passes."
      };
    case "experiment-integrity":
    case "claim-bridge":
      return {
        id: "evidence-integrity",
        label: "Evidence integrity",
        summary: "Blocked audits and result-to-claim transitions that should stay visible as workflow gates.",
        operatorGoal: "Repair experiment evidence and bridge trails before stronger claim promotion."
      };
    case "artifact-health":
      return {
        id: "repair-frontier",
        label: "Repair frontier",
        summary: "Typed relation and managed-artifact repair items that should remain operator-visible until cleared.",
        operatorGoal: "Treat degraded artifacts as first-class repair work, not background maintenance."
      };
    case "workflow-queue":
    case "workflow-observability":
      return {
        id: "queue-discipline",
        label: "Queue discipline",
        summary: "Queue churn and repeated workflow activity that suggest coordination debt is accumulating.",
        operatorGoal: "Reduce stale work and coordination churn before expanding concurrent work."
      };
    case "version-evolution":
      return {
        id: "version-governance",
        label: "Version governance",
        summary: "Version-comparison regressions that need explicit explanation or repair linkage.",
        operatorGoal: "Keep version movement honest by pairing regressions with concrete repair intent."
      };
    default:
      return {
        id: "workflow-governance",
        label: "Workflow governance",
        summary: "General workflow optimization guidance derived from durable signals.",
        operatorGoal: "Keep the durable workflow legible and proposal-only."
      };
  }
}
function deriveCrossSessionRecurrence(recommendation = {}, journalSignalCounts = {}) {
  const patterns = recommendation.category === "review-discipline" || recommendation.category === "repair-pattern" ? [/review/, /revision/] : recommendation.category === "experiment-integrity" || recommendation.category === "claim-bridge" ? [/audit/, /bridge/] : recommendation.category === "artifact-health" ? [/figure/, /workspace-index/, /wiki/] : recommendation.category === "workflow-queue" || recommendation.category === "workflow-observability" ? [/workspace-index/, /meta-optimize/, /review/, /revision/] : recommendation.category === "version-evolution" ? [/version/] : [];
  const total = Object.entries(journalSignalCounts).reduce((sum, [eventType, count]) => {
    return patterns.some((pattern) => pattern.test(eventType)) ? sum + count : sum;
  }, 0);
  return Math.min(total, 6);
}
function buildRecommendationScorecard(recommendation = {}, journalSignalCounts = {}) {
  const taxonomyPressure = recommendation.taxonomyPressure ?? {};
  const evidenceDensity = Math.min((recommendation.evidenceArtifactPaths?.length ?? 0) + (recommendation.evidenceIds?.length ?? 0), 8);
  const recurrenceCount = Math.min(recommendation.recurrenceCount ?? 0, 6);
  const crossSessionRecurrence = deriveCrossSessionRecurrence(recommendation, journalSignalCounts);
  const repairFrontierOverlap = Math.min(recommendation.repairFrontierOverlap ?? 0, 3);
  const auditCriticality = Math.min(recommendation.auditCriticality ?? 0, 3);
  const bridgeCriticality = Math.min(recommendation.bridgeCriticality ?? 0, 3);
  const queueChurn = Math.min(recommendation.queueChurnCount ?? 0, 6);
  const taxonomyFamilyPressure = Math.min(taxonomyPressure.familyIds?.length ?? 0, 3);
  const taxonomyGroupPressure = Math.min(taxonomyPressure.groupIds?.length ?? 0, 4);
  const score = recommendationPriorityWeight(recommendation.priority) + recurrenceCount * 7 + evidenceDensity * 3 + crossSessionRecurrence * 2 + repairFrontierOverlap * 5 + auditCriticality * 8 + bridgeCriticality * 8 + queueChurn * 2 + taxonomyFamilyPressure * 6 + taxonomyGroupPressure * 4;
  return {
    score,
    evidenceDensity,
    recurrenceCount,
    crossSessionRecurrence,
    repairFrontierOverlap,
    auditCriticality,
    bridgeCriticality,
    queueChurn,
    taxonomyFamilyPressure,
    taxonomyGroupPressure
  };
}
function buildFrontierSummary(recommendationCount, clusterCount, criticalCount, frontierScore, topClusterIds = [], taxonomyPressure = {}) {
  if (recommendationCount === 0) {
    return "No proposal-only optimizer recommendations are active.";
  }
  const taxonomySuffix = (taxonomyPressure.topTaxonomyFamilyIds ?? []).length > 0 ? ` Dominant taxonomy pressure: ${(taxonomyPressure.topTaxonomyFamilyIds ?? []).join(", ")}${(taxonomyPressure.topTaxonomyGroupIds ?? []).length > 0 ? ` via ${(taxonomyPressure.topTaxonomyGroupIds ?? []).join(", ")}` : ""}.` : "";
  return `${recommendationCount} ranked recommendations across ${clusterCount} deterministic clusters (${criticalCount} critical, frontier score ${frontierScore}). Top clusters: ${topClusterIds.join(", ") || "none"}.${taxonomySuffix}`;
}
function longHorizonTrendRank(status) {
  const order = {
    rising: 0,
    stable: 1,
    cooling: 2,
    dormant: 3
  };
  return order[status] ?? 99;
}
function describeLongHorizonFamily(familyId) {
  if (typeof familyId === "string" && familyId.startsWith("taxonomy-")) {
    const taxonomyFamilyId = familyId.replace(/^taxonomy-/, "");
    const metadata = taxonomyPressureMetadata(taxonomyFamilyId);
    return {
      label: metadata.longHorizonLabel,
      summary: metadata.longHorizonSummary
    };
  }
  switch (familyId) {
    case "review-recurrence":
      return {
        label: "Review recurrence",
        summary: "Review and revision debt keeps coming back across optimizer refreshes."
      };
    case "audit-integrity":
      return {
        label: "Audit integrity",
        summary: "Experiment audits keep surfacing as workflow gates over time."
      };
    case "claim-bridge-regressions":
      return {
        label: "Claim bridge regressions",
        summary: "Result-to-claim transitions keep needing explicit re-review."
      };
    case "repair-frontier-persistence":
      return {
        label: "Repair frontier persistence",
        summary: "Artifact repairs remain visible across multiple optimizer snapshots."
      };
    case "workflow-churn":
      return {
        label: "Workflow churn",
        summary: "Queue and observability work keeps repeating instead of closing out cleanly."
      };
    case "version-regressions":
      return {
        label: "Version regressions",
        summary: "Version comparisons keep reintroducing unresolved concern debt."
      };
    default:
      return {
        label: "Workflow governance",
        summary: "Long-horizon workflow governance signals remain active."
      };
  }
}
function recommendationLongHorizonFamilyId(recommendation = {}) {
  if (recommendation.category === "artifact-health" && (recommendation.taxonomyPressure?.familyIds ?? []).length > 0) {
    return taxonomyPressureMetadata(recommendation.taxonomyPressure.familyIds[0], recommendation.taxonomyPressure.familyLabels?.[0]).longHorizonFamilyId;
  }
  switch (recommendation.category) {
    case "review-discipline":
    case "repair-pattern":
      return "review-recurrence";
    case "experiment-integrity":
      return "audit-integrity";
    case "claim-bridge":
      return "claim-bridge-regressions";
    case "artifact-health":
      return "repair-frontier-persistence";
    case "workflow-queue":
    case "workflow-observability":
      return "workflow-churn";
    case "version-evolution":
      return "version-regressions";
    default:
      return "workflow-governance";
  }
}
function incrementObjectCount(target, key, amount = 1) {
  if (!key) {
    return;
  }
  target[key] = (target[key] ?? 0) + amount;
}
function topFrequencyIds(items = [], limit = 3) {
  const counts = items.reduce((accumulator, item) => {
    if (item) {
      accumulator[item] = (accumulator[item] ?? 0) + 1;
    }
    return accumulator;
  }, {});
  return Object.entries(counts).sort((left, right) => {
    const countDelta = right[1] - left[1];
    if (countDelta !== 0) {
      return countDelta;
    }
    return left[0].localeCompare(right[0]);
  }).slice(0, limit).map(([id]) => id);
}
function normalizeLongHorizonFamilyCounts(record = {}) {
  return Object.fromEntries(
    Object.entries(record ?? {}).map(([key, value]) => [String(key).trim(), Number(value)]).filter(([key, value]) => key && Number.isFinite(value) && value > 0).sort((left, right) => left[0].localeCompare(right[0]))
  );
}
function normalizeLongHorizonStringArrayRecord(record = {}) {
  return Object.fromEntries(
    Object.entries(record ?? {}).map(([key, value]) => [String(key).trim(), uniqueSorted(Array.isArray(value) ? value.map((item) => String(item).trim()) : [])]).filter(([key, value]) => key && value.length > 0).sort((left, right) => left[0].localeCompare(right[0]))
  );
}
function buildLongHorizonHistoryEntry(rankedRecommendations, clusters, frontier, observedAt) {
  const taxonomyPressure = collectTaxonomyPressureSummary(rankedRecommendations);
  const recommendationsByFamily = rankedRecommendations.reduce((accumulator, recommendation) => {
    const familyId = recommendationLongHorizonFamilyId(recommendation);
    const existing = accumulator.get(familyId) ?? {
      recommendationIds: [],
      clusterIds: [],
      taxonomyFamilyIds: [],
      taxonomyGroupIds: [],
      operatorPressureAreas: [],
      evidenceArtifactPaths: [],
      signalTypes: []
    };
    existing.recommendationIds.push(recommendation.id);
    existing.clusterIds.push(recommendation.clusterId);
    existing.taxonomyFamilyIds.push(...recommendation.taxonomyPressure?.familyIds ?? []);
    existing.taxonomyGroupIds.push(...recommendation.taxonomyPressure?.groupIds ?? []);
    existing.operatorPressureAreas.push(...recommendation.taxonomyPressure?.pressureAreas ?? []);
    existing.evidenceArtifactPaths.push(...recommendation.evidenceArtifactPaths ?? []);
    existing.signalTypes.push(...recommendation.signalTypes ?? []);
    accumulator.set(familyId, existing);
    return accumulator;
  }, /* @__PURE__ */ new Map());
  const familyCounts = Object.fromEntries(Array.from(recommendationsByFamily.entries()).map(([familyId, value]) => [familyId, value.recommendationIds.length]));
  const familyTopRecommendationIds = Object.fromEntries(Array.from(recommendationsByFamily.entries()).map(([familyId, value]) => [familyId, value.recommendationIds.slice(0, 3)]));
  const familyTopClusterIds = Object.fromEntries(Array.from(recommendationsByFamily.entries()).map(([familyId, value]) => [familyId, uniqueSorted(value.clusterIds).slice(0, 3)]));
  return {
    observedAt,
    frontierScore: frontier.frontierScore ?? 0,
    recommendationCount: frontier.recommendationCount ?? rankedRecommendations.length,
    criticalCount: frontier.criticalCount ?? rankedRecommendations.filter((item) => item.priority === "critical").length,
    clusterCount: frontier.clusterCount ?? clusters.length,
    topClusterIds: frontier.topClusterIds ?? [],
    topRecommendationIds: frontier.topRecommendationIds ?? [],
    familyCounts,
    familyTopRecommendationIds,
    familyTopClusterIds,
    taxonomyFamilyCounts: taxonomyPressure.familyCounts,
    taxonomyGroupCounts: taxonomyPressure.groupCounts,
    topTaxonomyFamilyIds: taxonomyPressure.topTaxonomyFamilyIds,
    topTaxonomyGroupIds: taxonomyPressure.topTaxonomyGroupIds
  };
}
function comparableLongHorizonHistoryEntry(entry = {}) {
  return {
    frontierScore: Number(entry.frontierScore ?? 0),
    recommendationCount: Number(entry.recommendationCount ?? 0),
    criticalCount: Number(entry.criticalCount ?? 0),
    clusterCount: Number(entry.clusterCount ?? 0),
    topClusterIds: uniqueSorted(entry.topClusterIds ?? []),
    topRecommendationIds: uniqueSorted(entry.topRecommendationIds ?? []),
    familyCounts: normalizeLongHorizonFamilyCounts(entry.familyCounts),
    familyTopRecommendationIds: normalizeLongHorizonStringArrayRecord(entry.familyTopRecommendationIds),
    familyTopClusterIds: normalizeLongHorizonStringArrayRecord(entry.familyTopClusterIds),
    taxonomyFamilyCounts: normalizeLongHorizonFamilyCounts(entry.taxonomyFamilyCounts),
    taxonomyGroupCounts: normalizeLongHorizonFamilyCounts(entry.taxonomyGroupCounts),
    topTaxonomyFamilyIds: uniqueSorted(entry.topTaxonomyFamilyIds ?? []),
    topTaxonomyGroupIds: uniqueSorted(entry.topTaxonomyGroupIds ?? [])
  };
}
function resolveLongHorizonHistoryMutation(previousHistory, nextEntry, historyWindowSize) {
  const lastEntry = previousHistory.at(-1) ?? null;
  if (!lastEntry) {
    return {
      action: "append",
      reason: "Recorded the first long-horizon snapshot.",
      history: [...previousHistory.slice(-(historyWindowSize - 1)), nextEntry]
    };
  }
  const nextComparable = comparableLongHorizonHistoryEntry(nextEntry);
  const lastComparable = comparableLongHorizonHistoryEntry(lastEntry);
  if (JSON.stringify(lastComparable) !== JSON.stringify(nextComparable)) {
    return {
      action: "append",
      reason: "Frontier or family state changed in a meaningful way.",
      history: [...previousHistory.slice(-(historyWindowSize - 1)), nextEntry]
    };
  }
  const refreshedObservedAt = isIsoTimestamp(lastEntry.observedAt) ? lastEntry.observedAt : nextEntry.observedAt;
  const refreshedLastEntry = {
    ...lastEntry,
    ...nextComparable,
    observedAt: refreshedObservedAt
  };
  const normalizedLastEntry = {
    ...lastEntry,
    ...lastComparable,
    observedAt: isIsoTimestamp(lastEntry.observedAt) ? lastEntry.observedAt : null
  };
  if (JSON.stringify(normalizedLastEntry) !== JSON.stringify(refreshedLastEntry)) {
    return {
      action: "refresh",
      reason: "Canonical state stayed the same, so the current snapshot was normalized in place.",
      history: [...previousHistory.slice(0, -1), refreshedLastEntry]
    };
  }
  return {
    action: "unchanged",
    reason: "Canonical frontier and long-horizon state were unchanged, so no new snapshot was added.",
    history: previousHistory
  };
}
function buildLongHorizonMemory(existingMemory, rankedRecommendations, clusters, frontier, observedAt) {
  const base = createMetaLongHorizonMemory();
  const previousHistory = Array.isArray(existingMemory?.history) ? existingMemory.history : [];
  const currentTaxonomyPressure = collectTaxonomyPressureSummary(rankedRecommendations);
  const recommendationsByFamily = rankedRecommendations.reduce((accumulator, recommendation) => {
    const familyId = recommendationLongHorizonFamilyId(recommendation);
    const existing = accumulator.get(familyId) ?? {
      recommendationIds: [],
      clusterIds: [],
      taxonomyFamilyIds: [],
      taxonomyGroupIds: [],
      operatorPressureAreas: [],
      evidenceArtifactPaths: [],
      signalTypes: []
    };
    existing.recommendationIds.push(recommendation.id);
    existing.clusterIds.push(recommendation.clusterId);
    existing.taxonomyFamilyIds.push(...recommendation.taxonomyPressure?.familyIds ?? []);
    existing.taxonomyGroupIds.push(...recommendation.taxonomyPressure?.groupIds ?? []);
    existing.operatorPressureAreas.push(...recommendation.taxonomyPressure?.pressureAreas ?? []);
    existing.evidenceArtifactPaths.push(...recommendation.evidenceArtifactPaths ?? []);
    existing.signalTypes.push(...recommendation.signalTypes ?? []);
    accumulator.set(familyId, existing);
    return accumulator;
  }, /* @__PURE__ */ new Map());
  const historyWindowSize = Number.isFinite(existingMemory?.historyWindowSize) ? existingMemory.historyWindowSize : base.historyWindowSize;
  const historyEntry = buildLongHorizonHistoryEntry(rankedRecommendations, clusters, frontier, observedAt);
  const historyDecision = resolveLongHorizonHistoryMutation(previousHistory, historyEntry, historyWindowSize);
  const history = historyDecision.history;
  const recentWindow = history.slice(-5);
  const previousWindow = history.slice(-10, -5);
  const familyIds = uniqueSorted(history.flatMap((entry) => Object.keys(entry.familyCounts ?? {})));
  const families = familyIds.map((familyId) => {
    const metadata = describeLongHorizonFamily(familyId);
    const totalCount = history.reduce((sum, entry) => sum + (entry.familyCounts?.[familyId] ?? 0), 0);
    const recentCount = recentWindow.reduce((sum, entry) => sum + (entry.familyCounts?.[familyId] ?? 0), 0);
    const previousCount = previousWindow.reduce((sum, entry) => sum + (entry.familyCounts?.[familyId] ?? 0), 0);
    const activeSnapshots = history.filter((entry) => (entry.familyCounts?.[familyId] ?? 0) > 0);
    const current = recommendationsByFamily.get(familyId) ?? {
      recommendationIds: [],
      clusterIds: [],
      taxonomyFamilyIds: [],
      taxonomyGroupIds: [],
      operatorPressureAreas: [],
      evidenceArtifactPaths: [],
      signalTypes: []
    };
    const trendStatus = recentCount > previousCount ? "rising" : recentCount < previousCount ? "cooling" : recentCount > 0 ? "stable" : "dormant";
    return {
      id: familyId,
      label: metadata.label,
      summary: metadata.summary,
      currentCount: current.recommendationIds.length,
      totalCount,
      activeSnapshotCount: activeSnapshots.length,
      recurring: activeSnapshots.length >= 2,
      trend: {
        status: trendStatus,
        recentCount,
        previousCount
      },
      topRecommendationIds: topFrequencyIds(history.flatMap((entry) => entry.familyTopRecommendationIds?.[familyId] ?? [])),
      topClusterIds: topFrequencyIds(history.flatMap((entry) => entry.familyTopClusterIds?.[familyId] ?? [])),
      relatedRecommendationIds: current.recommendationIds.slice(0, 5),
      relatedTaxonomyFamilyIds: uniqueSorted(current.taxonomyFamilyIds ?? []),
      relatedTaxonomyGroupIds: uniqueSorted(current.taxonomyGroupIds ?? []),
      operatorPressureAreas: uniqueSorted(current.operatorPressureAreas ?? []),
      evidenceArtifactPaths: uniqueSorted(current.evidenceArtifactPaths),
      signalTypes: uniqueSorted(current.signalTypes),
      firstObservedAt: activeSnapshots[0]?.observedAt ?? null,
      lastObservedAt: activeSnapshots.at(-1)?.observedAt ?? null
    };
  }).sort((left, right) => {
    const recurringDelta = Number(right.recurring) - Number(left.recurring);
    if (recurringDelta !== 0) {
      return recurringDelta;
    }
    const trendDelta = longHorizonTrendRank(left.trend.status) - longHorizonTrendRank(right.trend.status);
    if (trendDelta !== 0) {
      return trendDelta;
    }
    const recentDelta = right.trend.recentCount - left.trend.recentCount;
    if (recentDelta !== 0) {
      return recentDelta;
    }
    const totalDelta = right.totalCount - left.totalCount;
    if (totalDelta !== 0) {
      return totalDelta;
    }
    return left.id.localeCompare(right.id);
  });
  const summary = {
    familyCount: families.length,
    recurringFamilyCount: families.filter((item) => item.recurring).length,
    risingFamilyCount: families.filter((item) => item.trend.status === "rising").length,
    stableFamilyCount: families.filter((item) => item.trend.status === "stable").length,
    coolingFamilyCount: families.filter((item) => item.trend.status === "cooling").length,
    snapshotCount: history.length,
    lastObservedAt: history.at(-1)?.observedAt ?? null,
    lastAction: historyDecision.action,
    topFamilyIds: families.slice(0, 3).map((item) => item.id),
    topTaxonomyFamilyIds: currentTaxonomyPressure.topTaxonomyFamilyIds,
    topTaxonomyGroupIds: currentTaxonomyPressure.topTaxonomyGroupIds,
    pressureAreas: currentTaxonomyPressure.pressureAreas,
    overview: families.length > 0 ? `${families.length} long-horizon workflow families across ${history.length} optimizer snapshots. Top families: ${families.slice(0, 3).map((item) => item.id).join(", ") || "none"}.${currentTaxonomyPressure.topTaxonomyFamilyIds.length > 0 ? ` Dominant taxonomy pressure: ${currentTaxonomyPressure.topTaxonomyFamilyIds.join(", ")}${currentTaxonomyPressure.topTaxonomyGroupIds.length > 0 ? ` via ${currentTaxonomyPressure.topTaxonomyGroupIds.join(", ")}` : ""}.` : ""}` : "No long-horizon workflow memory has been summarized yet."
  };
  return {
    ...base,
    historyWindowSize,
    horizon: {
      sessionEntriesAnalyzed: Math.min(history.length, historyWindowSize),
      reviewRoundsObserved: history.length,
      versionComparisonsAnalyzed: history.length,
      auditRecordsAnalyzed: history.reduce((sum, entry) => sum + (entry.familyCounts?.["audit-integrity"] ?? 0), 0),
      bridgeRecordsAnalyzed: history.reduce((sum, entry) => sum + (entry.familyCounts?.["claim-bridge-regressions"] ?? 0), 0)
    },
    summary,
    historyPolicy: {
      mode: "deterministic-noop-drift-guard-v1",
      lastAction: historyDecision.action,
      reason: historyDecision.reason,
      comparedAt: observedAt,
      lastMeaningfulChangeAt: historyDecision.action === "append" ? observedAt : existingMemory?.historyPolicy?.lastMeaningfulChangeAt ?? history.at(-1)?.observedAt ?? null
    },
    history,
    families,
    updatedAt: observedAt
  };
}
function buildMetaOptimizeMirror(metaRecommendations, longHorizonMemory, remediationPacks, operatorPlaybooks, executionBridgeCandidates, operatorFollowThrough, governanceCoverage, autonomyLoops = null, operatorLessons = createMetaOperatorLessonsIndex()) {
  return {
    proposalOnly: true,
    recommendationCount: metaRecommendations.items.length,
    criticalCount: metaRecommendations.summary.criticalCount,
    clusterCount: metaRecommendations.summary.clusterCount,
    frontierScore: metaRecommendations.summary.frontierScore,
    activeSignalTypes: metaRecommendations.summary.signalTypes,
    topClusterIds: metaRecommendations.summary.topClusterIds,
    topRecommendationIds: metaRecommendations.summary.topRecommendationIds,
    topClusters: metaRecommendations.summary.topClusters,
    topTaxonomyFamilyIds: metaRecommendations.summary.topTaxonomyFamilyIds ?? [],
    topTaxonomyGroupIds: metaRecommendations.summary.topTaxonomyGroupIds ?? [],
    pressureAreas: metaRecommendations.summary.pressureAreas ?? [],
    taxonomyOverview: metaRecommendations.summary.taxonomyOverview ?? "No typed wiki taxonomy pressure is currently active in the optimizer frontier.",
    frontierSummary: metaRecommendations.frontier.frontierSummary,
    rankingMethod: metaRecommendations.frontier.rankingMethod,
    tieBreakOrder: metaRecommendations.ranking.tieBreakOrder,
    reportPath: ARTIFACT_PATHS.metaOptimizerReport,
    recommendationsPath: ARTIFACT_PATHS.metaRecommendations,
    statePath: ARTIFACT_PATHS.metaOptimizerState,
    longHorizonPath: ARTIFACT_PATHS.metaLongHorizonMemory,
    remediationPacks: remediationPacks.summary,
    followThrough: operatorFollowThrough.summary,
    operatorLessons: operatorLessons.summary,
    governanceCoverage: governanceCoverage.summary,
    executionBridgeCandidates: executionBridgeCandidates.summary,
    operatorPlaybooks: operatorPlaybooks.summary,
    autonomyLoops,
    longHorizon: {
      ...longHorizonMemory.summary,
      memoryPath: ARTIFACT_PATHS.metaLongHorizonMemory
    }
  };
}
function renderMetaOptimizeOverviewLines(metaOptimize = {}) {
  return [
    `- Meta-optimize frontier: ${metaOptimize.recommendationCount ?? 0} recommendations across ${metaOptimize.clusterCount ?? 0} clusters (${metaOptimize.criticalCount ?? 0} critical, score ${metaOptimize.frontierScore ?? 0})`,
    `- Meta-optimize frontier summary: ${metaOptimize.frontierSummary ?? "No proposal-only optimizer recommendations have been generated yet."}`,
    `- Meta-optimize top clusters: ${(metaOptimize.topClusterIds ?? []).join(", ") || "none"}`,
    `- Meta-optimize taxonomy pressure: ${metaOptimize.taxonomyOverview ?? "No typed wiki taxonomy pressure is currently active in the optimizer frontier."}`,
    `- Meta-optimize top taxonomy families: ${(metaOptimize.topTaxonomyFamilyIds ?? []).join(", ") || "none"}`,
    `- Meta-optimize top taxonomy groups: ${(metaOptimize.topTaxonomyGroupIds ?? []).join(", ") || "none"}`,
    `- Meta-optimize pressure areas: ${(metaOptimize.pressureAreas ?? []).join(", ") || "none"}`,
    ...renderAutonomyLoopOverviewLines(metaOptimize.autonomyLoops),
    `- Remediation packs: ${metaOptimize.remediationPacks?.packCount ?? 0} proposal-only packs (${(metaOptimize.remediationPacks?.topPackIds ?? []).join(", ") || "none"})`,
    `- Remediation pack focus: ${metaOptimize.remediationPacks?.overview ?? "No proposal-only remediation packs have been generated yet."}`,
    `- Remediation pack readiness: ${metaOptimize.remediationPacks?.readinessOverview ?? "No proposal-only remediation packs have been generated yet."}`,
    `- Remediation packs path: ${metaOptimize.remediationPacks?.packsPath ?? ARTIFACT_PATHS.metaRemediationPacks}`,
    `- Operator follow-through: ${metaOptimize.followThrough?.itemCount ?? 0} records (${(metaOptimize.followThrough?.topSourceIds ?? []).join(", ") || "none"})`,
    `- Operator follow-through overview: ${metaOptimize.followThrough?.overview ?? "No operator follow-through decisions have been recorded yet."}`,
    `- Operator follow-through debt: overdue=${metaOptimize.followThrough?.overdueExecutionCount ?? 0}, deferred-due=${metaOptimize.followThrough?.dueDeferredCount ?? 0}, stale=${metaOptimize.followThrough?.staleCount ?? 0}, invalid=${metaOptimize.followThrough?.invalidStatusCount ?? 0}, action-required=${metaOptimize.followThrough?.actionRequiredCount ?? 0}`,
    `- Operator follow-through execution window: due-soon=${metaOptimize.followThrough?.dueSoonExecutionCount ?? 0}, due-review=${metaOptimize.followThrough?.dueReviewCount ?? 0}, critical-overdue=${metaOptimize.followThrough?.criticalOverdueExecutionCount ?? 0}`,
    `- Operator follow-through ids: due-soon=${(metaOptimize.followThrough?.dueSoonExecutionIds ?? []).join(", ") || "none"}, due-review=${(metaOptimize.followThrough?.dueReviewIds ?? []).join(", ") || "none"}, overdue=${(metaOptimize.followThrough?.overdueExecutionIds ?? []).join(", ") || "none"}, critical=${(metaOptimize.followThrough?.criticalOverdueExecutionIds ?? []).join(", ") || "none"}`,
    `- Operator follow-through path: ${metaOptimize.followThrough?.followThroughPath ?? ARTIFACT_PATHS.metaOperatorFollowThrough}`,
    `- Operator lessons: ${metaOptimize.operatorLessons?.activeLessonCount ?? 0} active / ${metaOptimize.operatorLessons?.lessonCount ?? 0} total (${(metaOptimize.operatorLessons?.topLessonIds ?? []).join(", ") || "none"})`,
    `- Operator lessons overview: ${metaOptimize.operatorLessons?.overview ?? "No distilled operator lessons have been recorded yet."}`,
    `- Operator lessons tags: ${(metaOptimize.operatorLessons?.topTags ?? []).join(", ") || "none"}`,
    `- Operator lessons domains: ${(metaOptimize.operatorLessons?.topDomains ?? []).join(", ") || "none"}`,
    `- Operator lessons path: ${metaOptimize.operatorLessons?.lessonsPath ?? ARTIFACT_PATHS.metaOperatorLessons}`,
    `- Governance coverage: ${metaOptimize.governanceCoverage?.guardedCount ?? 0} guarded / ${metaOptimize.governanceCoverage?.exemptCount ?? 0} exempt`,
    `- Governance coverage overview: ${metaOptimize.governanceCoverage?.overview ?? "No governance coverage matrix has been summarized yet."}`,
    `- Governance coverage path: ${metaOptimize.governanceCoverage?.coveragePath ?? ARTIFACT_PATHS.metaGovernanceCoverage}`,
    `- Execution bridge candidates: ${metaOptimize.executionBridgeCandidates?.candidateCount ?? 0} proposal-only candidates (${(metaOptimize.executionBridgeCandidates?.topCandidateIds ?? []).join(", ") || "none"})`,
    `- Execution bridge focus: ${metaOptimize.executionBridgeCandidates?.overview ?? "No proposal-only execution bridge candidates have been generated yet."}`,
    `- Execution bridge path: ${metaOptimize.executionBridgeCandidates?.candidatesPath ?? ARTIFACT_PATHS.metaExecutionBridgeCandidates}`,
    `- Family playbooks: ${metaOptimize.operatorPlaybooks?.playbookCount ?? 0} proposal-only playbooks (${(metaOptimize.operatorPlaybooks?.topTaxonomyFamilyIds ?? []).join(", ") || "none"})`,
    `- Family playbook focus: ${metaOptimize.operatorPlaybooks?.overview ?? "No proposal-only family-level operator playbooks have been generated yet."}`,
    `- Family playbook readiness: ${metaOptimize.operatorPlaybooks?.readinessOverview ?? "No proposal-only family-level operator playbooks have been generated yet."}`,
    `- Family playbooks path: ${metaOptimize.operatorPlaybooks?.playbooksPath ?? ARTIFACT_PATHS.metaOperatorPlaybooks}`,
    `- Long-horizon memory: ${metaOptimize.longHorizon?.overview ?? "No long-horizon workflow memory has been summarized yet."}`,
    `- Long-horizon snapshots: ${metaOptimize.longHorizon?.snapshotCount ?? 0}`,
    `- Long-horizon last action: ${metaOptimize.longHorizon?.lastAction ?? "unchanged"}`,
    `- Long-horizon last observed: ${metaOptimize.longHorizon?.lastObservedAt ?? "none"}`,
    `- Long-horizon top families: ${(metaOptimize.longHorizon?.topFamilyIds ?? []).join(", ") || "none"}`,
    `- Long-horizon taxonomy families: ${(metaOptimize.longHorizon?.topTaxonomyFamilyIds ?? []).join(", ") || "none"}`,
    `- Long-horizon taxonomy groups: ${(metaOptimize.longHorizon?.topTaxonomyGroupIds ?? []).join(", ") || "none"}`,
    `- Meta-optimize report: ${metaOptimize.reportPath ?? ARTIFACT_PATHS.metaOptimizerReport}`,
    `- Long-horizon memory path: ${metaOptimize.longHorizonPath ?? ARTIFACT_PATHS.metaLongHorizonMemory}`
  ];
}
function buildRuntimeWorkspaceMirror(controllerState, continuation, leases, events, results) {
  const normalizedController = normalizeRuntimeControllerState(controllerState);
  const normalizedContinuation = normalizeRuntimeContinuationIndex(continuation);
  const normalizedLeases = normalizeRuntimeLeasesIndex(leases);
  const normalizedEvents = normalizeRuntimeEventsIndex(events);
  const normalizedResults = normalizeRuntimeResultsIndex(results);
  return {
    explicitInvocationOnly: normalizedController.explicitInvocationOnly,
    noDaemon: normalizedController.noDaemon,
    selectionPolicy: normalizedController.selectionPolicy,
    boundedStepPolicy: normalizedController.boundedStepPolicy,
    controllerStatePath: normalizedController.summary.controllerStatePath,
    leasesPath: normalizedController.summary.leasesPath,
    eventsPath: normalizedController.summary.eventsPath,
    resultsPath: normalizedController.summary.resultsPath,
    lastRunId: normalizedController.summary.lastRunId,
    lastStatus: normalizedController.summary.lastStatus,
    lastOutcome: normalizedController.summary.lastOutcome,
    lastSelectedPacketId: normalizedController.summary.lastSelectedPacketId,
    lastEnvelopeWorkerRole: normalizedController.summary.lastEnvelopeWorkerRole,
    requestCount: normalizedController.summary.requestCount,
    acceptedRequestCount: normalizedController.summary.acceptedRequestCount,
    executingRequestCount: normalizedController.summary.executingRequestCount,
    staleRequestCount: normalizedController.summary.staleRequestCount,
    overdueExecutionCount: normalizedController.summary.overdueExecutionCount,
    dueReviewCount: normalizedController.summary.dueReviewCount,
    checkpointCount: normalizedResults.summary.checkpointCount,
    escalationCount: normalizedResults.summary.escalationCount,
    continuationCount: normalizedContinuation.summary.continuationCount,
    currentContinuationKind: normalizedContinuation.summary.currentKind,
    currentContinuationPacketId: normalizedContinuation.summary.currentPacketId,
    currentContinuationProgramRunId: normalizedContinuation.summary.currentProgramRunId,
    currentContinuationCommand: normalizedContinuation.summary.currentCommand,
    lastCheckpointPacketId: normalizedResults.summary.lastCheckpointPacketId,
    lastCheckpointSummary: normalizedResults.summary.lastCheckpointSummary,
    lastCheckpointAt: normalizedResults.summary.lastCheckpointAt,
    lastEscalationPacketId: normalizedResults.summary.lastEscalationPacketId,
    lastEscalationFollowThroughId: normalizedResults.summary.lastEscalationFollowThroughId,
    lastEscalationAt: normalizedResults.summary.lastEscalationAt,
    activeLeaseCount: normalizedLeases.summary.activeLeaseCount,
    activeLeasePacketIds: normalizedLeases.summary.activePacketIds,
    eventCount: normalizedEvents.summary.eventCount,
    resultCount: normalizedResults.summary.runCount,
    lastEventType: normalizedEvents.summary.lastEventType,
    overview: normalizedController.summary.overview
  };
}
function buildCampaignsWorkspaceMirror(campaignsIndex, programsIndex, programRuns) {
  const normalizedCampaigns = normalizeCampaignsIndex(campaignsIndex);
  const normalizedPrograms = normalizeProgramsIndex(programsIndex);
  const normalizedRuns = normalizeProgramRunsIndex(programRuns);
  const currentCampaign = normalizedCampaigns.items.find((item) => ["active", "review-needed"].includes(item.status)) ?? normalizedCampaigns.items[0] ?? null;
  const campaignProgramIds = normalizeStringArray5(currentCampaign?.programIds);
  const currentProgramIds = campaignProgramIds.length > 0 ? campaignProgramIds : normalizeStringArray5(currentCampaign?.steps?.map((step) => step.programId));
  const linkedRuns = currentProgramIds.length > 0 ? normalizedRuns.items.filter((item) => currentProgramIds.includes(item.programId)) : [];
  const currentSteps = Array.isArray(currentCampaign?.steps) ? currentCampaign.steps : [];
  const completedStepCount = currentSteps.filter((step) => ["completed", "achieved"].includes(step.status)).length;
  const reviewNeededStep = currentSteps.find((step) => step.status === "review-needed") ?? null;
  const nextStep = currentSteps.find((step) => ["planned", "approved", "active", "review-needed", "blocked"].includes(step.status)) ?? null;
  return {
    campaignCount: normalizedCampaigns.summary.campaignCount,
    plannedCount: normalizedCampaigns.summary.plannedCount,
    activeCount: normalizedCampaigns.summary.activeCount,
    reviewNeededCount: normalizedCampaigns.summary.reviewNeededCount,
    completedCount: normalizedCampaigns.summary.completedCount,
    blockedCount: normalizedCampaigns.summary.blockedCount,
    topCampaignIds: normalizedCampaigns.summary.topCampaignIds,
    currentCampaignId: currentCampaign?.id ?? null,
    currentCampaignStatus: currentCampaign?.status ?? null,
    currentCampaignStepCount: currentSteps.length,
    currentCampaignCompletedStepCount: completedStepCount,
    currentCampaignReviewNeededStepCount: currentSteps.filter((step) => step.status === "review-needed").length,
    currentCampaignNextStepId: nextStep?.id ?? null,
    currentCampaignNextAction: reviewNeededStep?.nextAction ?? nextStep?.nextAction ?? currentCampaign?.nextAction ?? null,
    linkedProgramCount: normalizedPrograms.items.filter((item) => currentProgramIds.includes(item.id)).length,
    linkedRunCount: linkedRuns.length,
    overview: normalizedCampaigns.summary.campaignCount > 0 ? `${normalizedCampaigns.summary.campaignCount} multi-cycle research campaigns are tracked (${normalizedCampaigns.summary.activeCount} active, ${normalizedCampaigns.summary.reviewNeededCount} awaiting review).` : "No multi-cycle research campaigns have been recorded yet.",
    campaignsPath: ARTIFACT_PATHS.campaignsIndex
  };
}
function buildProgramsWorkspaceMirror(programsIndex, programRuns, programApprovals, runtime = {}) {
  const normalizedPrograms = normalizeProgramsIndex(programsIndex);
  const normalizedRuns = normalizeProgramRunsIndex(programRuns);
  const normalizedApprovals = normalizeProgramApprovalsIndex(programApprovals);
  return {
    programCount: normalizedPrograms.summary.programCount,
    activeCount: normalizedPrograms.summary.activeCount,
    blockedCount: normalizedPrograms.summary.blockedCount,
    approvedRunCount: normalizedRuns.summary.approvedCount,
    reviewNeededRunCount: normalizedRuns.summary.reviewNeededCount,
    reviewCheckpointRunCount: normalizedRuns.summary.reviewCheckpointRunCount ?? 0,
    consumedApprovalCount: normalizedApprovals.summary.consumedCount ?? 0,
    topProgramIds: normalizedPrograms.summary.topProgramIds,
    topRunIds: normalizedRuns.summary.topRunIds,
    topApprovalIds: normalizedApprovals.summary.topApprovalIds,
    currentProgramId: normalizedPrograms.items.find((item) => item.id === runtime.lastProgramId)?.id ?? null,
    currentProgramRunId: normalizedRuns.items.find((item) => item.id === runtime.lastProgramRunId)?.id ?? null,
    currentApprovalId: normalizedApprovals.items.find((item) => item.id === runtime.lastApprovalId)?.id ?? null,
    currentReviewCheckpointRunId: normalizedRuns.items.find((item) => item.reviewCheckpointRequired)?.id ?? null,
    currentReviewCheckpointPacketId: normalizedRuns.items.find((item) => item.reviewCheckpointRequired)?.reviewCheckpointPacketId ?? null,
    currentReviewCheckpointSummary: normalizedRuns.items.find((item) => item.reviewCheckpointRequired)?.reviewCheckpointSummary ?? null,
    currentProgramClosureState: normalizedRuns.items.find((item) => item.id === runtime.lastProgramRunId)?.closureState ?? null,
    currentProgramClosureReason: normalizedRuns.items.find((item) => item.id === runtime.lastProgramRunId)?.closureReason ?? null,
    lastProgramOutcome: runtime.lastProgramOutcome ?? "not-started",
    overview: normalizedPrograms.summary.programCount > 0 ? `${normalizedPrograms.summary.programCount} research programs are tracked (${normalizedPrograms.summary.activeCount} active, ${normalizedRuns.summary.approvedCount} approved runs, ${normalizedRuns.summary.reviewCheckpointRunCount ?? 0} awaiting review checkpoints).` : "No program-level research operating surfaces are active yet.",
    programsPath: ARTIFACT_PATHS.programsIndex,
    runsPath: ARTIFACT_PATHS.programRuns,
    approvalsPath: ARTIFACT_PATHS.programApprovals
  };
}
function renderRuntimeOverviewLines(runtime = {}) {
  return [
    `- Runtime controller: ${runtime.overview ?? "No autonomous control-plane run has been executed yet."}`,
    `- Runtime status: ${runtime.lastStatus ?? "never-run"} / ${runtime.lastOutcome ?? "not-started"}`,
    `- Runtime last run: ${runtime.lastRunId ?? "none"}`,
    `- Runtime last packet: ${runtime.lastSelectedPacketId ?? "none"}`,
    `- Runtime envelope worker: ${runtime.lastEnvelopeWorkerRole ?? "none"}`,
    `- Runtime requests: total=${runtime.requestCount ?? 0} accepted=${runtime.acceptedRequestCount ?? 0} executing=${runtime.executingRequestCount ?? 0} stale=${runtime.staleRequestCount ?? 0} overdue=${runtime.overdueExecutionCount ?? 0} due-review=${runtime.dueReviewCount ?? 0}`,
    `- Runtime checkpoints/escalations: checkpoints=${runtime.checkpointCount ?? 0} escalations=${runtime.escalationCount ?? 0} last-checkpoint=${runtime.lastCheckpointPacketId ?? "none"} last-escalation=${runtime.lastEscalationPacketId ?? "none"}`,
    `- Runtime continuation: count=${runtime.continuationCount ?? 0} kind=${runtime.currentContinuationKind ?? "none"} packet=${runtime.currentContinuationPacketId ?? "none"} run=${runtime.currentContinuationProgramRunId ?? "none"} command=${runtime.currentContinuationCommand ?? "none"}`,
    `- Runtime active leases: ${runtime.activeLeaseCount ?? 0} (${(runtime.activeLeasePacketIds ?? []).join(", ") || "none"})`,
    `- Runtime events/results: events=${runtime.eventCount ?? 0} results=${runtime.resultCount ?? 0} last-event=${runtime.lastEventType ?? "none"}`,
    `- Runtime paths: controller=${runtime.controllerStatePath ?? ARTIFACT_PATHS.runtimeControllerState}, leases=${runtime.leasesPath ?? ARTIFACT_PATHS.runtimeLeases}, events=${runtime.eventsPath ?? ARTIFACT_PATHS.runtimeEvents}, results=${runtime.resultsPath ?? ARTIFACT_PATHS.runtimeResults}`
  ];
}
function renderCampaignOverviewLines(campaigns = {}) {
  return [
    `- Campaigns: ${campaigns.overview ?? "No multi-cycle research campaigns have been recorded yet."}`,
    `- Campaign counts: campaigns=${campaigns.campaignCount ?? 0} planned=${campaigns.plannedCount ?? 0} active=${campaigns.activeCount ?? 0} review-needed=${campaigns.reviewNeededCount ?? 0} completed=${campaigns.completedCount ?? 0} blocked=${campaigns.blockedCount ?? 0}`,
    `- Current campaign: ${campaigns.currentCampaignId ?? "none"} / status=${campaigns.currentCampaignStatus ?? "none"} / steps=${campaigns.currentCampaignCompletedStepCount ?? 0}/${campaigns.currentCampaignStepCount ?? 0} / review-needed-steps=${campaigns.currentCampaignReviewNeededStepCount ?? 0}`,
    `- Current campaign next step: ${campaigns.currentCampaignNextStepId ?? "none"} / action=${campaigns.currentCampaignNextAction ?? "none"}`,
    `- Campaign paths: campaigns=${campaigns.campaignsPath ?? ARTIFACT_PATHS.campaignsIndex}`
  ];
}
function renderProgramOverviewLines(programs = {}) {
  return [
    `- Programs: ${programs.overview ?? "No program-level research operating surfaces are active yet."}`,
    `- Program counts: programs=${programs.programCount ?? 0} active=${programs.activeCount ?? 0} blocked=${programs.blockedCount ?? 0} approved-runs=${programs.approvedRunCount ?? 0} review-needed-runs=${programs.reviewNeededRunCount ?? 0} review-checkpoints=${programs.reviewCheckpointRunCount ?? 0} consumed-approvals=${programs.consumedApprovalCount ?? 0}`,
    `- Current program: ${programs.currentProgramId ?? "none"} / run=${programs.currentProgramRunId ?? "none"} / approval=${programs.currentApprovalId ?? "none"} / last-outcome=${programs.lastProgramOutcome ?? "not-started"}`,
    `- Current program closure: ${programs.currentProgramClosureState ?? "none"} / reason=${programs.currentProgramClosureReason ?? "none"}`,
    `- Current review checkpoint: run=${programs.currentReviewCheckpointRunId ?? "none"} / packet=${programs.currentReviewCheckpointPacketId ?? "none"} / summary=${programs.currentReviewCheckpointSummary ?? "none"}`,
    `- Program paths: programs=${programs.programsPath ?? ARTIFACT_PATHS.programsIndex}, runs=${programs.runsPath ?? ARTIFACT_PATHS.programRuns}, approvals=${programs.approvalsPath ?? ARTIFACT_PATHS.programApprovals}`
  ];
}
function clusterSortKey(cluster = {}) {
  return [
    String(recommendationSortRank(cluster.priority)).padStart(2, "0"),
    String(999999 - (cluster.score ?? 0)).padStart(6, "0"),
    cluster.id ?? "cluster"
  ].join(":");
}
function summarizeLinkedEvidence(paths = [], ids = []) {
  return uniqueSorted([...paths ?? [], ...ids ?? []]);
}
function intersects(values = [], otherValues = []) {
  const target = new Set(otherValues);
  return values.some((value) => target.has(value));
}
function summarizeConcernForPack(concern = {}) {
  return {
    id: concern.id,
    summary: concern.summary,
    severity: concern.severity ?? "medium",
    status: concern.status ?? "open",
    responseOwnerRole: concern.responseOwnerRole ?? null,
    linkedArtifactPaths: uniqueSorted(concern.linkedArtifactPaths ?? []),
    linkedAuditIds: uniqueSorted(concern.linkedAuditIds ?? []),
    linkedBridgeIds: uniqueSorted(concern.linkedBridgeIds ?? [])
  };
}
function summarizeFigureIssueForPack(issue = {}) {
  return {
    id: issue.id,
    figureId: issue.figureId ?? null,
    severity: issue.severity ?? "medium",
    code: issue.code ?? null,
    summary: issue.summary ?? `Figure issue ${issue.id}`,
    artifactPaths: uniqueSorted(issue.artifactPaths ?? [])
  };
}
function buildFollowThroughSourceCatalog(remediationPacks, operatorPlaybooks, executionBridgeCandidates) {
  const catalog = /* @__PURE__ */ new Map();
  for (const pack of remediationPacks.packs ?? []) {
    catalog.set(`remediation-pack:${pack.id}`, {
      sourceType: "remediation-pack",
      sourceId: pack.id,
      sourceArtifactPath: ARTIFACT_PATHS.metaRemediationPacks,
      sourceFingerprint: followThroughSourceAuthorityFingerprint(
        "remediation-pack",
        pack
      ),
      allowedActorRoles: uniqueSorted([...(pack.packetPointers ?? []).map((item) => item.assignedRole), ...(pack.conversionHints ?? []).map((item) => item.assignedRole)].filter(Boolean)),
      title: pack.title,
      summary: pack.summary
    });
  }
  for (const playbook of operatorPlaybooks.playbooks ?? []) {
    catalog.set(`operator-playbook:${playbook.id}`, {
      sourceType: "operator-playbook",
      sourceId: playbook.id,
      sourceArtifactPath: ARTIFACT_PATHS.metaOperatorPlaybooks,
      sourceFingerprint: followThroughSourceAuthorityFingerprint(
        "operator-playbook",
        playbook
      ),
      allowedActorRoles: uniqueSorted(playbook.responseOwnerRoles ?? []),
      title: playbook.title,
      summary: playbook.summary
    });
  }
  for (const candidate of executionBridgeCandidates.candidates ?? []) {
    catalog.set(`execution-bridge:${candidate.id}`, {
      sourceType: "execution-bridge",
      sourceId: candidate.id,
      sourceArtifactPath: ARTIFACT_PATHS.metaExecutionBridgeCandidates,
      sourceFingerprint: followThroughSourceAuthorityFingerprint(
        "execution-bridge",
        candidate
      ),
      allowedActorRoles: uniqueSorted([candidate.sourceConversionPath?.assignedRole, ...(candidate.context?.linkedPacketPointers ?? []).map((item) => item.assignedRole)].filter(Boolean)),
      title: candidate.suggestedTitle,
      summary: candidate.suggestedSummary
    });
  }
  return catalog;
}
function normalizeFollowThroughRetryState(value = {}) {
  const raw = value && typeof value === "object" && !Array.isArray(value) ? value : {};
  const attemptCount = Number.isFinite(Number(raw.attemptCount)) ? Number(raw.attemptCount) : 0;
  const maxAttempts = Number.isFinite(Number(raw.maxAttempts)) ? Number(raw.maxAttempts) : 3;
  return {
    attemptCount: Math.max(0, attemptCount),
    maxAttempts: Math.max(1, maxAttempts),
    lastAttemptAt: raw.lastAttemptAt ?? null,
    lastError: raw.lastError ?? null,
    escalatedAt: raw.escalatedAt ?? null
  };
}
function buildOperatorFollowThrough(root, existingIndex, sourceCatalog, timestamp = nowIso(), targetPreview = null) {
  const operatingState = readProgramOperatingState(root);
  const items = (existingIndex.items ?? []).map((item, index) => {
    const sourceType = item.sourceType ?? "remediation-pack";
    const sourceId = item.sourceId ?? item.id ?? `unknown-${index + 1}`;
    const key = `${sourceType}:${sourceId}`;
    const source = sourceCatalog.get(key);
    const allowed = /* @__PURE__ */ new Set(["acknowledged", "accepted-for-execution", "executing", "deferred", "accepted-risk", "closed", "superseded"]);
    const authorityState = followThroughAuthorityState(item, operatingState);
    const invalidStatus = !allowed.has(item.status) || item.status === "accepted-for-execution" && !item.reviewAfter || !authorityState.trusted;
    const status = allowed.has(item.status) ? item.status : "acknowledged";
    const dueDeferred = status === "deferred" && item.deferUntil && String(item.deferUntil) <= timestamp;
    const dueReview = status === "accepted-for-execution" && item.reviewAfter && String(item.reviewAfter) <= timestamp;
    const dueSoonExecution = ["accepted-for-execution", "executing"].includes(status) && item.executeBy && String(item.executeBy) > timestamp && String(item.executeBy) <= new Date(Date.now() + 6 * 60 * 60 * 1e3).toISOString();
    const overdueExecution = ["accepted-for-execution", "executing"].includes(status) && item.executeBy && String(item.executeBy) <= timestamp;
    const overdueExecutionSeverity = overdueExecution ? String(item.executeBy) <= new Date(Date.now() - 24 * 60 * 60 * 1e3).toISOString() ? "critical" : "warning" : dueSoonExecution ? "at-risk" : null;
    const executionWindowState = !["accepted-for-execution", "executing"].includes(status) ? null : overdueExecution ? overdueExecutionSeverity === "critical" ? "critical-overdue" : "overdue" : dueSoonExecution ? "due-soon" : "on-track";
    const plannedTarget = Boolean(item.plannedTarget);
    const targetBound = plannedTarget ? false : followThroughTargetStillBound(root, {
      status,
      linkedTargetArtifact: item.linkedTargetArtifact ?? null,
      linkedTargetId: item.linkedTargetId ?? null
    }, targetPreview);
    return {
      id: item.id ?? `follow-through-${slugify2(`${sourceType}-${sourceId}`)}`,
      sourceType,
      sourceId,
      sourceArtifactPath: item.sourceArtifactPath ?? source?.sourceArtifactPath ?? null,
      sourceFingerprint: item.sourceFingerprint ?? source?.sourceFingerprint ?? null,
      sourceTitle: item.sourceTitle ?? source?.title ?? sourceId,
      sourceSummary: item.sourceSummary ?? source?.summary ?? "",
      status,
      decisionSummary: item.decisionSummary ?? "",
      rationale: item.rationale ?? "",
      selectedConversionPathKey: item.selectedConversionPathKey ?? null,
      linkedTargetArtifact: item.linkedTargetArtifact ?? null,
      linkedTargetId: item.linkedTargetId ?? null,
      programId: item.programId ?? null,
      programRunId: item.programRunId ?? null,
      approvalId: item.approvalId ?? null,
      authorityProvenance: item.authorityProvenance ?? null,
      authorityTrusted: authorityState.trusted,
      authorityFailureReason: authorityState.reason,
      plannedTarget,
      deferUntil: item.deferUntil ?? null,
      executeBy: item.executeBy ?? null,
      executionStartedAt: item.executionStartedAt ?? null,
      executionCompletedAt: item.executionCompletedAt ?? null,
      reviewAfter: item.reviewAfter ?? null,
      closureReason: item.closureReason ?? null,
      closureArtifactPaths: normalizeStringArray5(item.closureArtifactPaths),
      actorRole: item.actorRole ?? null,
      workerRole: ROLE_IDS.includes(item.workerRole) ? item.workerRole : null,
      retryState: normalizeFollowThroughRetryState(item.retryState),
      recordedAt: item.recordedAt ?? item.updatedAt ?? timestamp,
      updatedAt: item.updatedAt ?? timestamp,
      invalidStatus,
      stale: Boolean(source && item.sourceFingerprint && item.sourceFingerprint !== source.sourceFingerprint),
      dueDeferred,
      dueReview,
      overdueExecution,
      overdueExecutionSeverity,
      executionWindowState,
      targetBound
    };
  }).sort((left, right) => left.id.localeCompare(right.id));
  const summary = {
    itemCount: items.length,
    acknowledgedCount: items.filter((item) => item.status === "acknowledged").length,
    acceptedForExecutionCount: items.filter((item) => item.status === "accepted-for-execution").length,
    executingCount: items.filter((item) => item.status === "executing").length,
    overdueExecutionCount: items.filter((item) => item.overdueExecution).length,
    criticalOverdueExecutionCount: items.filter((item) => item.overdueExecutionSeverity === "critical").length,
    dueSoonExecutionCount: items.filter((item) => item.executionWindowState === "due-soon").length,
    deferredCount: items.filter((item) => item.status === "deferred").length,
    acceptedRiskCount: items.filter((item) => item.status === "accepted-risk").length,
    closedCount: items.filter((item) => item.status === "closed").length,
    supersededCount: items.filter((item) => item.status === "superseded").length,
    invalidStatusCount: items.filter((item) => item.invalidStatus).length,
    staleCount: items.filter((item) => item.stale).length,
    dueDeferredCount: items.filter((item) => item.dueDeferred).length,
    dueReviewCount: items.filter((item) => item.dueReview).length,
    unresolvedTargetCount: items.filter((item) => !item.targetBound && !item.plannedTarget).length,
    plannedTargetCount: items.filter((item) => item.plannedTarget).length,
    dueSoonExecutionIds: items.filter((item) => item.executionWindowState === "due-soon").map((item) => item.id),
    dueReviewIds: items.filter((item) => item.dueReview).map((item) => item.id),
    overdueExecutionIds: items.filter((item) => item.overdueExecution).map((item) => item.id),
    criticalOverdueExecutionIds: items.filter((item) => item.overdueExecutionSeverity === "critical").map((item) => item.id),
    topSourceIds: items.filter((item) => !["closed", "superseded"].includes(item.status)).slice(0, 5).map((item) => item.sourceId),
    overview: items.length > 0 ? `${items.length} operator follow-through records: ${items.filter((item) => item.status === "accepted-for-execution").length} accepted for execution, ${items.filter((item) => item.executionWindowState === "due-soon").length} due-soon, ${items.filter((item) => item.overdueExecution).length} overdue-execution (${items.filter((item) => item.overdueExecutionSeverity === "critical").length} critical), ${items.filter((item) => item.dueReview).length} due-review, ${items.filter((item) => item.status === "deferred").length} deferred, ${items.filter((item) => item.status === "accepted-risk").length} accepted-risk, ${items.filter((item) => item.stale).length} stale, ${items.filter((item) => item.invalidStatus).length} invalid-status, ${items.filter((item) => item.plannedTarget).length} planned-target, ${items.filter((item) => !item.targetBound && !item.plannedTarget).length} target-drift.` : "No operator follow-through decisions have been recorded yet.",
    followThroughPath: ARTIFACT_PATHS.metaOperatorFollowThrough,
    transitionsPath: ARTIFACT_PATHS.metaOperatorFollowThroughTransitions
  };
  const actionRequiredItems = items.filter((item) => item.invalidStatus || item.stale || item.dueDeferred || item.dueReview || item.overdueExecution || !item.targetBound && !item.plannedTarget || item.status === "accepted-for-execution" || item.status === "executing" || item.status === "accepted-risk").map((item) => ({
    id: item.id,
    sourceType: item.sourceType,
    sourceId: item.sourceId,
    status: item.status,
    stale: item.stale,
    dueDeferred: item.dueDeferred,
    invalidStatus: item.invalidStatus,
    authorityTrusted: item.authorityTrusted,
    authorityFailureReason: item.authorityFailureReason,
    dueReview: item.dueReview,
    overdueExecution: item.overdueExecution,
    overdueExecutionSeverity: item.overdueExecutionSeverity,
    executionWindowState: item.executionWindowState,
    linkedTargetArtifact: item.linkedTargetArtifact,
    linkedTargetId: item.linkedTargetId,
    plannedTarget: item.plannedTarget,
    nextAction: !item.targetBound ? item.plannedTarget ? "Materialize the planned target into a real packet before continuing." : "Re-bind this follow-through record to a live target artifact/id before continuing." : item.overdueExecution ? `Escalate this ${item.overdueExecutionSeverity ?? "warning"} accepted-for-execution record or update/close it immediately.` : item.dueReview ? "Review this accepted-for-execution record now or explicitly defer/close it before the review window drifts further." : item.executionWindowState === "due-soon" ? "Schedule execution or explicitly defer this accepted-for-execution record before it goes overdue." : item.status === "accepted-risk" ? "Resolve, defer, close, or supersede this risk record; accepted risk remains governance debt and cannot authorize execution." : item.status === "executing" ? "Advance this executing record to closure or explicitly defer it." : item.status === "accepted-for-execution" && (!item.linkedTargetArtifact || !item.linkedTargetId) ? "Add a durable target artifact/id before keeping this record accepted-for-execution." : item.status === "accepted-for-execution" ? "Carry this accepted-for-execution decision through to closure or explicitly defer it." : item.dueDeferred ? "Review or reschedule this deferred follow-through now." : item.stale ? "Re-open and reassess this stale follow-through against the updated source." : item.invalidStatus ? item.authorityFailureReason ? `Repair or retire this fail-closed follow-through authority record (${item.authorityFailureReason}).` : "Repair the invalid follow-through status in the durable ledger." : "Review this follow-through record."
  }));
  return {
    ...createMetaOperatorFollowThroughIndex(),
    items,
    summary: {
      ...summary,
      actionRequiredCount: actionRequiredItems.length,
      actionRequiredSourceIds: actionRequiredItems.map((item) => item.sourceId)
    },
    actionRequiredItems,
    updatedAt: timestamp
  };
}
function buildGovernanceCoverage(generatedAt = nowIso()) {
  const guardedMutations = GOVERNANCE_GUARDED_MUTATIONS;
  const exemptMutations = GOVERNANCE_EXEMPT_MUTATIONS;
  return {
    ...createMetaGovernanceCoverageIndex(),
    guardedMutations,
    exemptMutations,
    summary: {
      guardedCount: guardedMutations.length,
      exemptCount: exemptMutations.length,
      overview: `${guardedMutations.length} write paths currently require clear operator follow-through; ${exemptMutations.length} paths remain explicitly exempt.`,
      coveragePath: ARTIFACT_PATHS.metaGovernanceCoverage
    },
    updatedAt: generatedAt
  };
}
function buildGovernanceCoverageReport(governanceCoverage, generatedAt = nowIso()) {
  const now = Date.now();
  const cadenceWindowMs = (cadence) => {
    switch (cadence) {
      case "per-session":
        return 36 * 60 * 60 * 1e3;
      case "per-change":
        return 7 * 24 * 60 * 60 * 1e3;
      case "per-release":
        return 90 * 24 * 60 * 60 * 1e3;
      case "per-project":
        return 365 * 24 * 60 * 60 * 1e3;
      default:
        return 0;
    }
  };
  const staleReviewIds = governanceCoverage.exemptMutations.filter((item) => item.lastReviewedAt && cadenceWindowMs(item.reviewCadence) > 0 && Date.parse(item.lastReviewedAt) < now - cadenceWindowMs(item.reviewCadence)).map((item) => item.id);
  const expiringSoonIds = governanceCoverage.exemptMutations.filter((item) => item.sunsetAt && Date.parse(item.sunsetAt) <= now + 30 * 24 * 60 * 60 * 1e3).map((item) => item.id);
  return {
    ...createMetaGovernanceCoverageReport(),
    status: "ok",
    guardedIds: governanceCoverage.guardedMutations.map((item) => item.id),
    exemptIds: governanceCoverage.exemptMutations.map((item) => item.id),
    surfaceBindingAudit: {
      uncoveredTools: [],
      uncoveredCommands: [],
      uncoveredCoreFunctions: [],
      uncoveredNegativeCoverage: []
    },
    summary: {
      guardedCount: governanceCoverage.summary.guardedCount,
      exemptCount: governanceCoverage.summary.exemptCount,
      staleReviewCount: staleReviewIds.length,
      expiringSoonCount: expiringSoonIds.length,
      staleReviewIds,
      expiringSoonIds,
      overview: governanceCoverage.summary.overview,
      reportPath: ARTIFACT_PATHS.metaGovernanceCoverageReport,
      markdownPath: ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown
    },
    updatedAt: generatedAt
  };
}
function validateFollowThroughPayload(root, record = {}, targetPreview = null) {
  const status = record.status;
  const allowed = /* @__PURE__ */ new Set(["acknowledged", "accepted-for-execution", "executing", "deferred", "accepted-risk", "closed", "superseded"]);
  if (!allowed.has(status)) {
    throw new Error(`Invalid follow-through status: ${status}`);
  }
  if (status === "deferred" && !record.deferUntil && !record.reviewAfter) {
    throw new Error("Deferred follow-through records require deferUntil or reviewAfter.");
  }
  if (status === "accepted-risk" && !record.rationale) {
    throw new Error("Accepted-risk follow-through records require rationale.");
  }
  if (status === "accepted-for-execution" && (!record.linkedTargetArtifact || !record.linkedTargetId)) {
    throw new Error("Accepted-for-execution follow-through records require linkedTargetArtifact and linkedTargetId.");
  }
  if (status === "accepted-for-execution" && !record.plannedTarget && !targetArtifactContainsId2(root, record.linkedTargetArtifact, record.linkedTargetId, targetPreview)) {
    throw new Error(`Accepted-for-execution follow-through target ${record.linkedTargetId} was not found in ${record.linkedTargetArtifact}. Use plannedTarget when the target will be materialized later.`);
  }
  if (status === "accepted-for-execution" && !record.executeBy) {
    throw new Error("Accepted-for-execution follow-through records require executeBy.");
  }
  if (status === "accepted-for-execution" && !record.reviewAfter) {
    throw new Error("Accepted-for-execution follow-through records require reviewAfter.");
  }
  if (status === "executing" && (!record.linkedTargetArtifact || !record.linkedTargetId || !record.executionStartedAt)) {
    throw new Error("Executing follow-through records require linkedTargetArtifact, linkedTargetId, and executionStartedAt.");
  }
  if (status === "closed" && normalizeStringArray5(record.closureArtifactPaths).length === 0 && !record.closureReason) {
    throw new Error("Closed follow-through records require closureArtifactPaths or closureReason.");
  }
  if (status === "closed" && !record.executionCompletedAt) {
    throw new Error("Closed follow-through records require executionCompletedAt.");
  }
  if (!record.actorRole) {
    throw new Error("Follow-through records require actorRole.");
  }
  if (record.workerRole && !ROLE_IDS.includes(record.workerRole)) {
    throw new Error(`Unknown workerRole: ${record.workerRole}`);
  }
  return status;
}
function validateFollowThroughActor(root, record, source) {
  const allowedActorRoles = normalizeStringArray5(source?.allowedActorRoles ?? []);
  if (allowedActorRoles.length === 0) {
    return;
  }
  if (allowedActorRoles.includes(record.actorRole)) {
    return;
  }
  if (record.actorRole === "planner" && record.workerRole && allowedActorRoles.includes(record.workerRole)) {
    return;
  }
  throw new Error(`Actor role ${record.actorRole} is not allowed for ${record.sourceType}:${record.sourceId}. Allowed roles: ${allowedActorRoles.join(", ")}.`);
}
function targetArtifactContainsId2(root, artifactPath, targetId, targetPreview = null) {
  if (targetPreview && targetPreview.artifactPath === artifactPath && targetPreview.targetId === targetId) {
    return true;
  }
  const fullPath = resolvePath(root, artifactPath);
  if (!fs6.existsSync(fullPath)) {
    return false;
  }
  const extension = path8.extname(artifactPath).toLowerCase();
  if ([".json"].includes(extension)) {
    try {
      const value = JSON.parse(fs6.readFileSync(fullPath, "utf8"));
      const queue = [value];
      while (queue.length > 0) {
        const current = queue.shift();
        if (current === targetId) {
          return true;
        }
        if (Array.isArray(current)) {
          queue.push(...current);
          continue;
        }
        if (current && typeof current === "object") {
          queue.push(...Object.values(current));
        }
      }
      return false;
    } catch {
      return false;
    }
  }
  const text3 = fs6.readFileSync(fullPath, "utf8");
  return text3.includes(String(targetId));
}
function summarizeProgramsIndex(items = []) {
  return {
    programCount: items.length,
    activeCount: items.filter((item) => item.status === "active").length,
    blockedCount: items.filter((item) => item.status === "blocked").length,
    topProgramIds: items.slice(0, 8).map((item) => item.id),
    overview: items.length > 0 ? `${items.length} research programs are tracked (${items.filter((item) => item.status === "active").length} active).` : "No research programs have been recorded yet.",
    programsPath: ARTIFACT_PATHS.programsIndex
  };
}
function summarizeProgramRunsIndex(items = []) {
  return {
    runCount: items.length,
    approvedCount: items.filter((item) => item.status === "approved").length,
    activeCount: items.filter((item) => item.status === "active").length,
    reviewNeededCount: items.filter((item) => item.status === "review-needed").length,
    blockedCount: items.filter((item) => item.status === "blocked").length,
    reviewCheckpointRunCount: items.filter((item) => item.reviewCheckpointRequired).length,
    topRunIds: items.slice(0, 8).map((item) => item.id),
    overview: items.length > 0 ? `${items.length} program runs are tracked (${items.filter((item) => item.status === "approved").length} approved).` : "No approved program runs have been recorded yet.",
    runsPath: ARTIFACT_PATHS.programRuns
  };
}
function summarizeProgramApprovalsIndex(items = []) {
  return {
    approvalCount: items.length,
    approvedCount: items.filter((item) => item.status === "approved").length,
    revokedCount: items.filter((item) => item.status === "revoked").length,
    consumedCount: items.filter((item) => item.status === "consumed").length,
    topApprovalIds: items.slice(0, 8).map((item) => item.id),
    overview: items.length > 0 ? `${items.length} program approvals are tracked (${items.filter((item) => item.status === "approved").length} approved).` : "No program approvals have been recorded yet.",
    approvalsPath: ARTIFACT_PATHS.programApprovals
  };
}
function normalizeCampaignStep(step = {}, index = 0) {
  const allowedStepType = normalizeAutonomyAllowedStepType(step.allowedStepType, null);
  return {
    id: slugify2(step.id ?? `step-${index + 1}`),
    title: step.title ?? `Campaign step ${index + 1}`,
    status: ["planned", "approved", "active", "review-needed", "blocked", "completed", "achieved", "superseded"].includes(step.status) ? step.status : "planned",
    programId: step.programId ? slugify2(step.programId) : null,
    programRunId: step.programRunId ? slugify2(step.programRunId) : null,
    approvalId: step.approvalId ? slugify2(step.approvalId) : null,
    packetId: step.packetId ? slugify2(step.packetId) : null,
    allowedStepType,
    objective: step.objective ?? null,
    nextAction: step.nextAction ?? null,
    evidenceLinks: normalizeStringArray5(step.evidenceLinks),
    outputPaths: normalizeStringArray5(step.outputPaths),
    reviewAfter: step.reviewAfter ?? null,
    executeBy: step.executeBy ?? null
  };
}
function normalizeCampaignItem(campaign = {}, index = 0) {
  const steps = Array.isArray(campaign.steps) ? campaign.steps.map(normalizeCampaignStep) : [];
  const programIds = uniqueSorted([
    ...normalizeStringArray5(campaign.programIds).map(slugify2),
    ...steps.map((step) => step.programId).filter(Boolean)
  ]);
  const status = ["planned", "active", "review-needed", "blocked", "completed", "superseded"].includes(campaign.status) ? campaign.status : "planned";
  return {
    id: slugify2(campaign.id ?? `campaign-${index + 1}`),
    title: campaign.title ?? `Research campaign ${index + 1}`,
    objective: campaign.objective ?? "",
    status,
    phase: campaign.phase ?? "research",
    controllerRole: ROLE_IDS.includes(campaign.controllerRole) ? campaign.controllerRole : "planner",
    programIds,
    steps,
    nextAction: campaign.nextAction ?? steps.find((step) => ["planned", "approved", "active", "review-needed", "blocked"].includes(step.status))?.nextAction ?? null,
    reviewPolicy: campaign.reviewPolicy ?? "fresh-approval-after-review-checkpoint",
    explicitApprovalRequired: campaign.explicitApprovalRequired !== false,
    noHiddenRuntime: campaign.noHiddenRuntime !== false,
    createdAt: campaign.createdAt ?? campaign.updatedAt ?? nowIso(),
    updatedAt: campaign.updatedAt ?? nowIso()
  };
}
function summarizeCampaignsIndex(items = []) {
  return {
    campaignCount: items.length,
    plannedCount: items.filter((item) => item.status === "planned").length,
    activeCount: items.filter((item) => item.status === "active").length,
    reviewNeededCount: items.filter((item) => item.status === "review-needed" || item.steps.some((step) => step.status === "review-needed")).length,
    completedCount: items.filter((item) => item.status === "completed").length,
    blockedCount: items.filter((item) => item.status === "blocked").length,
    topCampaignIds: items.slice(0, 8).map((item) => item.id),
    overview: items.length > 0 ? `${items.length} multi-cycle research campaigns are tracked (${items.filter((item) => item.status === "active").length} active, ${items.filter((item) => item.status === "review-needed" || item.steps.some((step) => step.status === "review-needed")).length} awaiting review).` : "No multi-cycle research campaigns have been recorded yet.",
    campaignsPath: ARTIFACT_PATHS.campaignsIndex
  };
}
function writeCampaignsIndex(root, items) {
  writeJson(root, ARTIFACT_PATHS.campaignsIndex, {
    ...createCampaignsIndex(),
    items,
    summary: summarizeCampaignsIndex(items),
    updatedAt: nowIso()
  });
}
function writeProgramOperatingState(root, { programsIndex, programRuns, programApprovals }) {
  const timestamp = nowIso();
  writeJson(root, ARTIFACT_PATHS.programRuns, {
    ...createProgramRunsIndex(),
    items: programRuns,
    summary: summarizeProgramRunsIndex(programRuns),
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.programsIndex, {
    ...createProgramsIndex(),
    items: programsIndex,
    summary: summarizeProgramsIndex(programsIndex),
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.programApprovals, {
    ...createProgramApprovalsIndex(),
    items: programApprovals,
    summary: summarizeProgramApprovalsIndex(programApprovals),
    updatedAt: timestamp
  });
}
function queryCampaigns(root, args = {}) {
  const campaignId = args.campaignId ? slugify2(args.campaignId) : null;
  const status = args.status ? String(args.status).trim().toLowerCase() : null;
  const campaignsState = normalizeCampaignsIndex(readJson(root, ARTIFACT_PATHS.campaignsIndex, createCampaignsIndex));
  const operatingState = readProgramOperatingState(root);
  const programsState = operatingState.programs;
  const runsState = operatingState.programRuns;
  const items = (campaignsState.items ?? []).map(normalizeCampaignItem).filter((item) => !campaignId || item.id === campaignId).filter((item) => !status || item.status === status).map((item) => ({
    ...item,
    programs: (programsState.items ?? []).filter((program) => item.programIds.includes(program.id)),
    runs: (runsState.items ?? []).filter((run) => item.programIds.includes(run.programId))
  }));
  return {
    status: "ok",
    items,
    summary: {
      ...campaignsState.summary,
      filteredCount: items.length
    }
  };
}
function planCampaign(root, args = {}) {
  assertGovernanceMutationRegistered("plan-campaign", "exempt");
  ensureWorkspace(root);
  const actorRole = args.actorRole ?? "planner";
  if (actorRole !== "planner") {
    throw new Error("planCampaign currently requires actorRole 'planner'.");
  }
  const campaignId = slugify2(args.campaignId ?? args.id ?? "");
  if (!campaignId) {
    throw new Error("planCampaign requires campaignId.");
  }
  const campaignsState = normalizeCampaignsIndex(readJson(root, ARTIFACT_PATHS.campaignsIndex, createCampaignsIndex));
  const existing = (campaignsState.items ?? []).map(normalizeCampaignItem).find((item) => item.id === campaignId) ?? null;
  const timestamp = nowIso();
  const nextCampaign = normalizeCampaignItem({
    ...existing,
    id: campaignId,
    title: args.title ?? existing?.title,
    objective: args.objective ?? existing?.objective,
    status: args.status ?? existing?.status ?? "planned",
    phase: args.phase ?? existing?.phase ?? "research",
    controllerRole: actorRole,
    programIds: args.programIds ?? existing?.programIds ?? [],
    steps: args.steps ?? existing?.steps ?? [],
    nextAction: args.nextAction ?? existing?.nextAction ?? null,
    reviewPolicy: args.reviewPolicy ?? existing?.reviewPolicy ?? "fresh-approval-after-review-checkpoint",
    explicitApprovalRequired: args.explicitApprovalRequired ?? existing?.explicitApprovalRequired ?? true,
    noHiddenRuntime: args.noHiddenRuntime ?? existing?.noHiddenRuntime ?? true,
    createdAt: existing?.createdAt ?? timestamp,
    updatedAt: timestamp
  });
  const nextItems = [
    ...(campaignsState.items ?? []).map(normalizeCampaignItem).filter((item) => item.id !== campaignId),
    nextCampaign
  ].sort((left, right) => left.id.localeCompare(right.id));
  writeCampaignsIndex(root, nextItems);
  refreshDurableSurfaces(root, {
    type: "plan-campaign",
    summary: `Recorded campaign plan ${campaignId} without executing work.`,
    artifactPaths: [ARTIFACT_PATHS.campaignsIndex, ARTIFACT_PATHS.workspaceIndex]
  });
  return {
    status: existing ? "updated" : "planned",
    campaignId: nextCampaign.id,
    stepCount: nextCampaign.steps.length,
    explicitApprovalRequired: nextCampaign.explicitApprovalRequired,
    noHiddenRuntime: nextCampaign.noHiddenRuntime,
    campaignsPath: ARTIFACT_PATHS.campaignsIndex
  };
}
function queryProgramApprovals(root, args = {}) {
  const programId = args.programId ? slugify2(args.programId) : null;
  const programRunId = args.programRunId ? slugify2(args.programRunId) : null;
  const status = args.status ? String(args.status).trim().toLowerCase() : null;
  const operatingState = readProgramOperatingState(root);
  const approvalsState = operatingState.programApprovals;
  const programRunsState = operatingState.programRuns;
  const programsState = operatingState.programs;
  const items = (approvalsState.items ?? []).filter((item) => !programId || item.programId === programId).filter((item) => !programRunId || item.programRunId === programRunId).filter((item) => !status || item.status === status).map((item) => ({
    ...item,
    program: (programsState.items ?? []).find((entry) => entry.id === item.programId) ?? null,
    run: (programRunsState.items ?? []).find((entry) => entry.id === item.programRunId) ?? null
  }));
  const continuationIntents = (programRunsState.items ?? []).filter((item) => item.reviewCheckpointRequired && item.nextApprovalIntent).filter((item) => !programId || item.programId === programId).filter((item) => !programRunId || item.id === programRunId).map((item) => ({
    ...item.nextApprovalIntent,
    programId: item.programId,
    programRunId: item.id,
    approvalId: item.approvalId,
    reviewCheckpointSummary: item.reviewCheckpointSummary,
    reviewCheckpointAt: item.reviewCheckpointAt
  }));
  return {
    status: "ok",
    items,
    continuationIntents,
    summary: {
      ...approvalsState.summary,
      reviewCheckpointRunCount: programRunsState.summary.reviewCheckpointRunCount ?? 0,
      continuationIntentCount: continuationIntents.length,
      filteredCount: items.length
    }
  };
}
function revokeProgramApproval(root, args = {}) {
  assertGovernanceMutationRegistered("revoke-program-approval", "exempt");
  ensureWorkspace(root);
  const actorRole = args.actorRole ?? "planner";
  if (actorRole !== "planner") {
    throw new Error("revokeProgramApproval currently requires actorRole 'planner'.");
  }
  const approvalId = slugify2(args.approvalId ?? "");
  if (!approvalId) {
    throw new Error("revokeProgramApproval requires approvalId.");
  }
  const operatingState = readProgramOperatingState(root);
  const approvalsState = operatingState.programApprovals;
  const approval = (approvalsState.items ?? []).find((item) => item.id === approvalId) ?? null;
  if (!approval) {
    throw new Error(`Program approval ${approvalId} was not found.`);
  }
  const timestamp = nowIso();
  const nextApprovals = (approvalsState.items ?? []).map((item) => item.id === approvalId ? { ...item, status: "revoked", summary: args.summary ?? item.summary, revokedAt: timestamp, revokedByRole: actorRole, revokeReason: args.revokeReason ?? null, updatedAt: timestamp } : item);
  const runsState = operatingState.programRuns;
  const nextRuns = (runsState.items ?? []).map((item) => item.approvalId === approvalId && item.status === "approved" ? { ...item, status: "blocked", lastOutcome: "approval-revoked", updatedAt: timestamp } : item);
  const programsState = operatingState.programs;
  const nextPrograms = (programsState.items ?? []).map((item) => item.id === approval.programId && item.activeRunId === approval.programRunId ? { ...item, status: item.status === "active" ? "blocked" : item.status, lastOutcome: "approval-revoked", updatedAt: timestamp } : item);
  writeProgramOperatingState(root, {
    programsIndex: nextPrograms,
    programRuns: nextRuns,
    programApprovals: nextApprovals
  });
  refreshDurableSurfaces(root, {
    type: "revoke-program-approval",
    summary: `Revoked program approval ${approvalId}.`,
    artifactPaths: [ARTIFACT_PATHS.programsIndex, ARTIFACT_PATHS.programRuns, ARTIFACT_PATHS.programApprovals]
  });
  return {
    status: "revoked",
    approvalId,
    programId: approval.programId,
    programRunId: approval.programRunId
  };
}
function followThroughTargetStillBound(root, item, targetPreview = null) {
  if (!["accepted-for-execution", "closed"].includes(item.status)) {
    return true;
  }
  if (!item.linkedTargetArtifact || !item.linkedTargetId) {
    return false;
  }
  return targetArtifactContainsId2(root, item.linkedTargetArtifact, item.linkedTargetId, targetPreview);
}
function validateFollowThroughTargetBinding(root, record, targetPreview = null) {
  if (!["accepted-for-execution", "closed"].includes(record.status)) {
    return;
  }
  if (!record.linkedTargetArtifact || !record.linkedTargetId) {
    throw new Error(`${record.status} follow-through records require linkedTargetArtifact and linkedTargetId.`);
  }
  if (record.status === "accepted-for-execution" && record.plannedTarget) {
    return;
  }
  if (!targetArtifactContainsId2(root, record.linkedTargetArtifact, record.linkedTargetId, targetPreview)) {
    throw new Error(`Follow-through target ${record.linkedTargetId} was not found in ${record.linkedTargetArtifact}.`);
  }
  if (record.status === "closed") {
    for (const artifactPath of normalizeStringArray5(record.closureArtifactPaths)) {
      if (!fs6.existsSync(resolvePath(root, artifactPath))) {
        throw new Error(`Closed follow-through artifact ${artifactPath} does not exist.`);
      }
    }
  }
}
function buildFollowThroughTransitions(existingIndex, nextRecord, previousRecord, timestamp = nowIso()) {
  const transitions = normalizeMetaOperatorFollowThroughTransitionsIndex(existingIndex).transitions;
  const transition = {
    id: `follow-through-transition-${slugify2(`${nextRecord.id}-${timestamp}`)}`,
    followThroughId: nextRecord.id,
    sourceType: nextRecord.sourceType,
    sourceId: nextRecord.sourceId,
    fromStatus: previousRecord?.status ?? null,
    toStatus: nextRecord.status,
    actorRole: nextRecord.actorRole,
    decisionSummary: nextRecord.decisionSummary,
    rationale: nextRecord.rationale,
    recordedAt: timestamp,
    linkedTargetArtifact: nextRecord.linkedTargetArtifact,
    linkedTargetId: nextRecord.linkedTargetId,
    closureReason: nextRecord.closureReason ?? null,
    closureArtifactPaths: normalizeStringArray5(nextRecord.closureArtifactPaths)
  };
  const nextTransitions = [...transitions, transition];
  return {
    ...createMetaOperatorFollowThroughTransitionsIndex(),
    transitions: nextTransitions,
    summary: {
      transitionCount: nextTransitions.length,
      overview: `${nextTransitions.length} operator follow-through transitions recorded.`,
      transitionsPath: ARTIFACT_PATHS.metaOperatorFollowThroughTransitions
    },
    updatedAt: timestamp
  };
}
function validateFollowThroughRecordIdentity(previousRecord, nextRecord) {
  if (!previousRecord) {
    return;
  }
  if (previousRecord.sourceType !== nextRecord.sourceType || previousRecord.sourceId !== nextRecord.sourceId) {
    throw new Error(`Follow-through record ${nextRecord.id} cannot change source from ${previousRecord.sourceType}:${previousRecord.sourceId} to ${nextRecord.sourceType}:${nextRecord.sourceId}.`);
  }
  const previousTargetBound = previousRecord.linkedTargetArtifact && previousRecord.linkedTargetId;
  const nextTargetChanged = previousTargetBound && (previousRecord.linkedTargetArtifact !== nextRecord.linkedTargetArtifact || previousRecord.linkedTargetId !== nextRecord.linkedTargetId);
  if (nextTargetChanged) {
    throw new Error(`Follow-through record ${nextRecord.id} cannot change target from ${previousRecord.linkedTargetArtifact}:${previousRecord.linkedTargetId} to ${nextRecord.linkedTargetArtifact}:${nextRecord.linkedTargetId}.`);
  }
}
function validateFollowThroughTransition(previousRecord, nextRecord) {
  const previous = previousRecord?.status ?? null;
  const next = nextRecord.status;
  const allowed = {
    null: ["acknowledged", "accepted-for-execution", "deferred", "accepted-risk", "closed", "superseded"],
    "acknowledged": ["accepted-for-execution", "deferred", "accepted-risk", "closed", "superseded", "acknowledged"],
    "accepted-for-execution": ["executing", "deferred", "accepted-risk", "superseded", "accepted-for-execution"],
    "executing": ["closed", "deferred", "accepted-risk", "superseded", "executing"],
    "deferred": ["acknowledged", "accepted-for-execution", "accepted-risk", "closed", "superseded", "deferred"],
    "accepted-risk": ["acknowledged", "closed", "superseded", "accepted-risk"],
    "closed": ["closed"],
    "superseded": ["superseded"]
  };
  const validNext = allowed[previous] ?? [];
  if (!validNext.includes(next)) {
    throw new Error(`Invalid follow-through transition: ${previous ?? "none"} -> ${next}`);
  }
}
function coverageLevelFromCount(count, { fullAt = 3, partialAt = 2, minimalAt = 1 } = {}) {
  if (count >= fullAt) {
    return "full";
  }
  if (count >= partialAt) {
    return "partial";
  }
  if (count >= minimalAt) {
    return "minimal";
  }
  return "none";
}
function readinessRank(value) {
  return value === "actionable" ? 0 : value === "partially-actionable" ? 1 : 2;
}
function buildGuidanceReadiness({ acceptanceCriteria = [], conversionHints = [], rankedConversionPaths = [], packetPointers = [], evidenceArtifactPaths = [], longHorizonMemory = [], repairItems = [], reviewConcerns = [], figureQa = [], remediationPackIds = [] } = {}) {
  const conversionCoverageLevel = coverageLevelFromCount(rankedConversionPaths.length, { fullAt: 3, partialAt: 2, minimalAt: 1 });
  const acceptanceCoverageLevel = coverageLevelFromCount(acceptanceCriteria.length, { fullAt: 3, partialAt: 2, minimalAt: 1 });
  const evidenceCoverageLevel = coverageLevelFromCount(evidenceArtifactPaths.length, { fullAt: 3, partialAt: 1, minimalAt: 1 });
  const packetCoverageLevel = coverageLevelFromCount(packetPointers.length, { fullAt: 2, partialAt: 1, minimalAt: 1 });
  const missingIngredients = uniqueSorted([
    rankedConversionPaths.length === 0 ? "ranked-conversion-paths" : null,
    acceptanceCriteria.length === 0 ? "acceptance-criteria" : null,
    evidenceArtifactPaths.length === 0 ? "evidence-artifacts" : null,
    packetPointers.length === 0 ? "packet-pointers" : null,
    longHorizonMemory.length === 0 ? "long-horizon-memory" : null,
    repairItems.length === 0 ? "repair-frontier-links" : null,
    conversionHints.length === 0 ? "conversion-hints" : null
  ]);
  const operatorReadiness = rankedConversionPaths.length >= 2 && acceptanceCriteria.length >= 2 && evidenceArtifactPaths.length >= 1 ? "actionable" : rankedConversionPaths.length >= 1 && acceptanceCriteria.length >= 1 ? "partially-actionable" : "advisory-only";
  const overview = operatorReadiness === "actionable" ? `Actionable guidance with ${rankedConversionPaths.length} ranked conversion paths and ${acceptanceCriteria.length} acceptance criteria.` : operatorReadiness === "partially-actionable" ? `Partially actionable guidance: ${rankedConversionPaths.length} ranked conversion paths, ${acceptanceCriteria.length} acceptance criteria, missing ${missingIngredients.join(", ") || "none"}.` : `Advisory-only guidance: missing ${missingIngredients.join(", ") || "ranked execution context"}.`;
  return {
    operatorReadiness,
    conversionCoverageLevel,
    acceptanceCoverageLevel,
    evidenceCoverageLevel,
    packetCoverageLevel,
    missingIngredients,
    linkedSignalCounts: {
      remediationPackCount: remediationPackIds.length,
      repairItemCount: repairItems.length,
      reviewConcernCount: reviewConcerns.length,
      figureIssueCount: figureQa.length,
      longHorizonCount: longHorizonMemory.length,
      rankedConversionPathCount: rankedConversionPaths.length,
      acceptanceCriteriaCount: acceptanceCriteria.length,
      evidenceArtifactCount: evidenceArtifactPaths.length,
      packetPointerCount: packetPointers.length
    },
    overview
  };
}
function isArtifactPathLike(value) {
  return typeof value === "string" && (value.startsWith(".dove/") || value.startsWith("docs/") || value.startsWith("README") || /\.(json|md|svg|txt|mjs|yml|yaml|tex)$/i.test(value));
}
function buildPlaybookArtifactUpdateMap({ familyId, familyLabel, matchedPacks = [], repairItems = [], packetPointers = [], workspacePointers = [], taxonomyAnchors = {} }) {
  const targetMap = /* @__PURE__ */ new Map();
  const pushTarget = (artifactPath, details = {}) => {
    if (!isArtifactPathLike(artifactPath)) {
      return;
    }
    const existing = targetMap.get(artifactPath) ?? {
      artifactPath,
      sourceKinds: [],
      updateReasons: [],
      relatedRepairItemIds: [],
      relatedConversionTargetIds: [],
      relatedPacketIds: [],
      taxonomyFamilyIds: [],
      taxonomyGroupIds: [],
      priorityScore: 0
    };
    existing.sourceKinds.push(details.sourceKind ?? "workspace");
    existing.updateReasons.push(details.reason ?? `Review ${artifactPath} for ${familyLabel ?? familyId} pressure.`);
    existing.relatedRepairItemIds.push(...details.relatedRepairItemIds ?? []);
    existing.relatedConversionTargetIds.push(...details.relatedConversionTargetIds ?? []);
    existing.relatedPacketIds.push(...details.relatedPacketIds ?? []);
    existing.taxonomyFamilyIds.push(...details.taxonomyFamilyIds ?? []);
    existing.taxonomyGroupIds.push(...details.taxonomyGroupIds ?? []);
    existing.priorityScore = Math.max(existing.priorityScore, details.priorityScore ?? 0);
    targetMap.set(artifactPath, existing);
  };
  for (const repairItem of repairItems) {
    pushTarget(repairItem.artifactPath, {
      sourceKind: "repair-frontier",
      reason: repairItem.nextAction ?? repairItem.summary ?? `Repair ${repairItem.id}.`,
      relatedRepairItemIds: [repairItem.id],
      taxonomyFamilyIds: uniqueSorted([repairItem.taxonomyFamilyId, ...repairItem.taxonomyFamilyIds ?? [], familyId].filter(Boolean)),
      taxonomyGroupIds: uniqueSorted([repairItem.taxonomyGroupId, ...repairItem.taxonomyGroupIds ?? []].filter(Boolean)),
      priorityScore: 120
    });
    for (const relatedPath of repairItem.relatedArtifactPaths ?? []) {
      pushTarget(relatedPath, {
        sourceKind: "repair-related-artifact",
        reason: `Update alongside repair item ${repairItem.id} so related artifacts stay aligned.`,
        relatedRepairItemIds: [repairItem.id],
        taxonomyFamilyIds: [familyId].filter(Boolean),
        priorityScore: 108
      });
    }
  }
  for (const pack of matchedPacks) {
    for (const artifactPath of pack.evidence?.artifactPaths ?? []) {
      pushTarget(artifactPath, {
        sourceKind: "linked-evidence",
        reason: `Evidence linked to remediation pack ${pack.id} should stay aligned with ${familyLabel ?? familyId} updates.`,
        relatedConversionTargetIds: (pack.rankedConversionPaths ?? []).map((item) => item.targetId),
        taxonomyFamilyIds: uniqueSorted([familyId, ...pack.taxonomyAnchors?.familyIds ?? []].filter(Boolean)),
        taxonomyGroupIds: uniqueSorted(pack.taxonomyAnchors?.groupIds ?? []),
        priorityScore: 96
      });
    }
    for (const path28 of pack.workspacePointers ?? []) {
      pushTarget(path28, {
        sourceKind: "workspace-pointer",
        reason: `Workspace pointer from remediation pack ${pack.id} provides update context for ${familyLabel ?? familyId}.`,
        taxonomyFamilyIds: [familyId].filter(Boolean),
        priorityScore: 60
      });
    }
    for (const conversion of pack.rankedConversionPaths ?? []) {
      pushTarget(conversion.targetId, {
        sourceKind: "conversion-target",
        reason: conversion.rationale ?? `Conversion target ${conversion.targetId} is a ranked candidate update surface.`,
        relatedConversionTargetIds: [conversion.targetId],
        taxonomyFamilyIds: [familyId].filter(Boolean),
        priorityScore: 100 - (conversion.rank ?? 0)
      });
    }
  }
  for (const packet of packetPointers) {
    pushTarget(packet.packetContextPath, {
      sourceKind: "packet-context",
      reason: packet.nextAction ?? `Inspect packet ${packet.id} before mutating durable artifacts.`,
      relatedPacketIds: [packet.id],
      taxonomyFamilyIds: [familyId].filter(Boolean),
      priorityScore: 92
    });
  }
  pushTarget(ARTIFACT_PATHS.metaOperatorPlaybooks, {
    sourceKind: "playbook-artifact",
    reason: `Update the durable family playbook layer after reviewing ${familyLabel ?? familyId} targets.`,
    taxonomyFamilyIds: [familyId].filter(Boolean),
    taxonomyGroupIds: uniqueSorted(taxonomyAnchors.groupIds ?? []),
    priorityScore: 58
  });
  const targets = Array.from(targetMap.values()).map((target) => ({
    ...target,
    sourceKinds: uniqueSorted(target.sourceKinds),
    updateReasons: uniqueSorted(target.updateReasons),
    relatedRepairItemIds: uniqueSorted(target.relatedRepairItemIds),
    relatedConversionTargetIds: uniqueSorted(target.relatedConversionTargetIds),
    relatedPacketIds: uniqueSorted(target.relatedPacketIds),
    taxonomyFamilyIds: uniqueSorted(target.taxonomyFamilyIds),
    taxonomyGroupIds: uniqueSorted(target.taxonomyGroupIds)
  })).sort((left, right) => {
    const priorityDelta = (right.priorityScore ?? 0) - (left.priorityScore ?? 0);
    if (priorityDelta !== 0) {
      return priorityDelta;
    }
    return left.artifactPath.localeCompare(right.artifactPath);
  }).map((target, index) => ({
    ...target,
    rank: index + 1
  }));
  return {
    overview: targets.length > 0 ? `${targets.length} proposal-only artifact targets are ranked for ${familyLabel ?? familyId}; start with the highest-ranked repair and conversion-linked files before broader workspace pointers.` : `No explicit artifact targets were derived for ${familyLabel ?? familyId}; rely on the playbook summary and remediation pack pointers instead.`,
    targetCount: targets.length,
    targetList: targets.map((target) => target.artifactPath),
    updateOrder: targets.map((target) => target.artifactPath),
    targets
  };
}
function buildRemediationAcceptanceCriteria({ repairItems, reviewConcerns, figureQa, packetPointers, longHorizonMemory }) {
  const criteria = [];
  if (repairItems.length > 0) {
    criteria.push(`Clear or explicitly accept all ${repairItems.length} linked repair-frontier item(s) before treating this pack as complete.`);
  }
  if (reviewConcerns.length > 0) {
    criteria.push(`Resolve or explicitly retire linked review concerns (${reviewConcerns.map((item) => item.id).join(", ")}) with reviewer-visible artifact updates.`);
  }
  if (figureQa.length > 0) {
    criteria.push(`Clear linked figure QA issues (${figureQa.map((item) => item.id).join(", ")}) and rerun validate_figure_pipeline before closure.`);
  }
  if (packetPointers.length > 0) {
    criteria.push(`Advance linked packets (${packetPointers.map((item) => item.id).join(", ")}) out of stale/review-needed/handoff states, or record an explicit defer decision.`);
  }
  if (longHorizonMemory.length > 0) {
    criteria.push(`Confirm long-horizon memory pressure has stopped rising or is explicitly accepted as ongoing debt.`);
  }
  if (criteria.length === 0) {
    criteria.push("Confirm there is no remaining operator-visible debt before considering this remediation pack complete.");
  }
  return uniqueSorted(criteria);
}
function packetMatchesRemediationEvidence(packet = {}, { evidenceArtifactPaths = [], evidenceIds = [], repairItems = [], taxonomyAnchors = {} } = {}) {
  if (packet.sourceType === "materialized-guidance") {
    return false;
  }
  const packetArtifactPaths = uniqueSorted([
    packet.packetPath,
    packet.packetContextPath,
    ...packet.evidenceLinks ?? [],
    ...packet.outputPaths ?? []
  ]);
  const packetIds = uniqueSorted([
    packet.id,
    packet.sourceId,
    ...packet.claimIds ?? [],
    ...packet.noteIds ?? [],
    ...packet.experimentIds ?? [],
    ...packet.rebuttalIssueIds ?? [],
    ...packet.versionIds ?? []
  ]);
  const repairArtifactPaths = uniqueSorted(repairItems.flatMap((item) => [item.artifactPath, ...item.relatedArtifactPaths ?? []]));
  const repairIds = uniqueSorted(repairItems.map((item) => item.id));
  const taxonomyTerms = uniqueSorted([
    ...taxonomyAnchors.familyIds ?? [],
    ...taxonomyAnchors.familyLabels ?? [],
    ...taxonomyAnchors.groupIds ?? [],
    ...taxonomyAnchors.groupLabels ?? []
  ].map((value) => String(value).toLowerCase()));
  const packetText = [packet.title, packet.currentFocus, packet.nextAction, packet.summary].filter(Boolean).join(" ").toLowerCase();
  return intersects(packetArtifactPaths, evidenceArtifactPaths) || intersects(packetArtifactPaths, repairArtifactPaths) || intersects(packetIds, evidenceIds) || intersects(packetIds, repairIds) || taxonomyTerms.some((term) => term && packetText.includes(term));
}
function buildRemediationConversionHints({ cluster, packetPointers, repairItems, reviewConcerns, figureQa, manualNextActions, taxonomyAnchors }) {
  const responseOwnerRole = cluster.responseOwnerRoles?.[0] ?? cluster.recommendationOwnerRoles?.[0] ?? "planner";
  const candidates = [];
  if (packetPointers.length > 0) {
    const packet = packetPointers[0];
    candidates.push({
      targetType: "update-existing-packet",
      targetId: packet.id,
      assignedRole: packet.assignedRole,
      priority: "primary",
      pathScore: 120 + Math.min(packetPointers.length, 3) * 5,
      rankingBasis: ["packet-pointer", `packet=${packet.id}`, `role=${packet.assignedRole}`],
      suggestedTitle: `Update packet ${packet.id} for ${cluster.label}`,
      rationale: `This pack already maps to packet ${packet.id}, so updating the existing packet is the cleanest manual execution path.`,
      nextStep: packet.nextAction ?? manualNextActions[0] ?? `Inspect ${packet.packetContextPath ?? packet.id} and update it using this remediation pack.`
    });
  } else {
    candidates.push({
      targetType: "create-new-packet",
      targetId: `task-${cluster.id}`,
      assignedRole: responseOwnerRole,
      priority: "primary",
      pathScore: 88 + Math.min((taxonomyAnchors.familyIds ?? []).length, 3) * 4,
      rankingBasis: ["new-packet", `cluster=${cluster.id}`, `role=${responseOwnerRole}`],
      suggestedTitle: `${cluster.label} remediation work`,
      rationale: `No active packet currently owns this pack, so a new explicit packet is the cleanest manual execution path.`,
      nextStep: manualNextActions[0] ?? `Create a new packet for ${cluster.label} and attach the linked repair items and evidence.`
    });
  }
  if (repairItems.length > 0) {
    candidates.push({
      targetType: "add-checklist-entry",
      targetId: ARTIFACT_PATHS.checklist,
      assignedRole: responseOwnerRole,
      priority: packetPointers.length > 0 ? "secondary" : "fallback",
      pathScore: 72 + Math.min(repairItems.length, 4) * 4,
      rankingBasis: ["repair-frontier", `repairItems=${repairItems.length}`],
      suggestedTitle: `${cluster.label} repair checklist item`,
      rationale: `Checklist entries keep closure criteria visible for packs with explicit repair-frontier debt.`,
      nextStep: `Add a checklist item referencing ${repairItems[0].id} and taxonomy pressure (${taxonomyAnchors.familyLabels.join(", ") || "none"}).`
    });
  }
  if (reviewConcerns.length > 0 || figureQa.length > 0) {
    candidates.push({
      targetType: "add-revision-item",
      targetId: ARTIFACT_PATHS.revisionPlan,
      assignedRole: responseOwnerRole,
      priority: reviewConcerns.length > 0 ? "secondary" : "fallback",
      pathScore: 76 + Math.min(reviewConcerns.length + figureQa.length, 4) * 4,
      rankingBasis: [
        ...reviewConcerns.length > 0 ? [`reviewConcerns=${reviewConcerns.length}`] : [],
        ...figureQa.length > 0 ? [`figureQa=${figureQa.length}`] : []
      ],
      suggestedTitle: `${cluster.label} remediation follow-up`,
      rationale: `Review concerns or figure QA issues in this pack should remain durable in the revision plan until they close.`,
      nextStep: `Append a revision-plan item for ${reviewConcerns[0]?.id ?? figureQa[0]?.id ?? cluster.id} with the linked manual next action.`
    });
  }
  const rankedPaths = buildRankedConversionCandidates(candidates);
  return {
    rankedPaths,
    hints: rankedPaths.map(({ rank, priority, pathScore, rankingBasis, deterministicKey, originalIndex, ...hint }) => ({
      ...hint,
      rank,
      priority,
      pathScore,
      rankingBasis,
      deterministicKey
    }))
  };
}
function buildRemediationPacks({ clusters, recommendations, longHorizonMemory, reviewConcerns, figureQa, workspaceIndex }) {
  const generatedAt = nowIso();
  const packs = clusters.map((cluster) => {
    const clusterRecommendations = recommendations.filter((item) => item.clusterId === cluster.id);
    const recommendationIds = clusterRecommendations.map((item) => item.id);
    const evidenceArtifactPaths = uniqueSorted(clusterRecommendations.flatMap((item) => item.evidenceArtifactPaths ?? []));
    const evidenceIds = uniqueSorted(clusterRecommendations.flatMap((item) => item.evidenceIds ?? []));
    const taxonomyAnchors = {
      familyIds: uniqueSorted(cluster.taxonomyPressure?.familyIds ?? []),
      familyLabels: uniqueSorted(cluster.taxonomyPressure?.familyLabels ?? []),
      groupIds: uniqueSorted(cluster.taxonomyPressure?.groupIds ?? []),
      groupLabels: uniqueSorted(cluster.taxonomyPressure?.groupLabels ?? []),
      overview: cluster.taxonomyPressure?.overview ?? "No typed wiki taxonomy pressure is active in this remediation pack."
    };
    const repairItems = (workspaceIndex.repairFrontier?.prioritizedItems ?? []).filter((item) => {
      const repairFamilyIds = uniqueSorted([item.taxonomyFamilyId, ...item.taxonomyFamilyIds ?? []].filter(Boolean));
      const repairGroupIds = uniqueSorted([item.taxonomyGroupId, ...item.taxonomyGroupIds ?? []].filter(Boolean));
      return intersects(repairFamilyIds, taxonomyAnchors.familyIds) || intersects(repairGroupIds, taxonomyAnchors.groupIds) || intersects(uniqueSorted([item.artifactPath, ...item.relatedArtifactPaths ?? []]), evidenceArtifactPaths);
    });
    const packReviewConcerns = (reviewConcerns.items ?? []).filter((concern) => {
      const concernEvidenceIds = uniqueSorted([concern.id, ...concern.linkedAuditIds ?? [], ...concern.linkedBridgeIds ?? []]);
      return concernEvidenceIds.some((id) => evidenceIds.includes(id)) || intersects(concern.linkedArtifactPaths ?? [], evidenceArtifactPaths);
    }).map(summarizeConcernForPack);
    const packFigureIssues = (figureQa.issues ?? []).filter((issue) => {
      const issueIds = uniqueSorted([issue.id, issue.figureId].filter(Boolean));
      return issueIds.some((id) => evidenceIds.includes(id)) || intersects(issue.artifactPaths ?? [], evidenceArtifactPaths) || clusterRecommendations.some((item) => (item.signalTypes ?? []).includes("figure-qa") || (item.signalTypes ?? []).includes("figure-qa"));
    }).map(summarizeFigureIssueForPack);
    const memoryFamilies = (longHorizonMemory.families ?? []).filter((family) => intersects(family.topClusterIds ?? [], [cluster.id]) || intersects(family.topRecommendationIds ?? [], recommendationIds)).slice(0, 4).map((family) => ({
      id: family.id,
      label: family.label,
      summary: family.summary,
      trend: family.trend?.status ?? "stable",
      relatedTaxonomyFamilyIds: uniqueSorted(family.relatedTaxonomyFamilyIds ?? []),
      relatedTaxonomyGroupIds: uniqueSorted(family.relatedTaxonomyGroupIds ?? []),
      evidenceArtifactPaths: uniqueSorted(family.evidenceArtifactPaths ?? [])
    }));
    const packetPointers = (workspaceIndex.activePackets ?? []).filter((packet) => packetMatchesRemediationEvidence(packet, { evidenceArtifactPaths, evidenceIds, repairItems, taxonomyAnchors })).slice(0, 4).map((packet) => ({
      id: packet.id,
      title: packet.title,
      currentFocus: packet.currentFocus,
      assignedRole: packet.assignedRole,
      lifecycleStatus: packet.lifecycleStatus,
      nextAction: packet.nextAction,
      packetContextPath: packet.packetContextPath ?? null
    }));
    const workspacePointers = uniqueSorted([
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.navigationReport,
      ARTIFACT_PATHS.sessionSummary,
      ARTIFACT_PATHS.metaOptimizerReport,
      ARTIFACT_PATHS.metaRecommendations,
      ARTIFACT_PATHS.metaLongHorizonMemory,
      ARTIFACT_PATHS.metaRemediationPacks,
      workspaceIndex.contextSurfaces?.currentActionContextPath,
      workspaceIndex.contextSurfaces?.currentRoleContextPath,
      workspaceIndex.contextSurfaces?.currentPhaseContextPath,
      ...packetPointers.map((packet) => packet.packetContextPath),
      ...evidenceArtifactPaths
    ]);
    const manualNextActions = uniqueSorted([
      `Read ${ARTIFACT_PATHS.metaOptimizerReport} and inspect cluster ${cluster.id} before changing any workflow artifact.`,
      ...repairItems.slice(0, 2).map((item) => item.nextAction),
      ...clusterRecommendations.slice(0, 2).map((item) => item.nextAction),
      packReviewConcerns[0] ? `Review concern ${packReviewConcerns[0].id} and update its linked artifacts explicitly.` : null,
      packFigureIssues[0] ? `Inspect figure QA issue ${packFigureIssues[0].id} before treating the frontier as closed.` : null,
      packetPointers[0]?.nextAction ?? null
    ]);
    const acceptanceCriteria = buildRemediationAcceptanceCriteria({
      repairItems,
      reviewConcerns: packReviewConcerns,
      figureQa: packFigureIssues,
      packetPointers,
      longHorizonMemory: memoryFamilies
    });
    const conversionGuidance = buildRemediationConversionHints({
      cluster,
      packetPointers,
      repairItems,
      reviewConcerns: packReviewConcerns,
      figureQa: packFigureIssues,
      manualNextActions,
      taxonomyAnchors
    });
    const readiness = buildGuidanceReadiness({
      acceptanceCriteria,
      conversionHints: conversionGuidance.hints,
      rankedConversionPaths: conversionGuidance.rankedPaths,
      packetPointers,
      evidenceArtifactPaths,
      longHorizonMemory: memoryFamilies,
      repairItems,
      reviewConcerns: packReviewConcerns,
      figureQa: packFigureIssues
    });
    return {
      id: `remediation-pack-${cluster.id}`,
      title: `${cluster.label} remediation pack`,
      proposalOnly: true,
      explicitOnly: true,
      noAutoApply: true,
      rank: cluster.rank,
      clusterId: cluster.id,
      clusterLabel: cluster.label,
      priority: cluster.priority,
      score: cluster.score,
      summary: `${cluster.summary} This pack keeps the cluster's repair frontier, evidence links, taxonomy anchors, and manual next steps together for operator review.`,
      frontier: {
        clusterId: cluster.id,
        recommendationIds,
        repairItemIds: repairItems.map((item) => item.id),
        repairCount: repairItems.length,
        frontierSummary: workspaceIndex.metaOptimize?.frontierSummary ?? "No proposal-only optimizer recommendations have been generated yet."
      },
      taxonomyAnchors,
      evidence: {
        artifactPaths: evidenceArtifactPaths,
        ids: evidenceIds,
        longHorizonFamilyIds: memoryFamilies.map((family) => family.id),
        reviewConcernIds: packReviewConcerns.map((concern) => concern.id),
        figureIssueIds: packFigureIssues.map((issue) => issue.id)
      },
      reviewConcerns: packReviewConcerns,
      figureQa: packFigureIssues,
      longHorizonMemory: memoryFamilies,
      packetPointers,
      workspacePointers,
      readiness,
      acceptanceCriteria,
      conversionHints: conversionGuidance.hints,
      rankedConversionPaths: conversionGuidance.rankedPaths,
      manualNextActions,
      generatedAt
    };
  });
  const summary = {
    packCount: packs.length,
    topPackIds: packs.slice(0, 3).map((pack) => pack.id),
    topClusterIds: packs.slice(0, 3).map((pack) => pack.clusterId),
    actionableCount: packs.filter((pack) => pack.readiness?.operatorReadiness === "actionable").length,
    partiallyActionableCount: packs.filter((pack) => pack.readiness?.operatorReadiness === "partially-actionable").length,
    advisoryCount: packs.filter((pack) => pack.readiness?.operatorReadiness === "advisory-only").length,
    readinessOverview: packs.length > 0 ? `${packs.filter((pack) => pack.readiness?.operatorReadiness === "actionable").length} actionable, ${packs.filter((pack) => pack.readiness?.operatorReadiness === "partially-actionable").length} partially actionable, ${packs.filter((pack) => pack.readiness?.operatorReadiness === "advisory-only").length} advisory-only remediation packs.` : "No proposal-only remediation packs have been generated yet.",
    overview: packs.length > 0 ? `${packs.length} proposal-only remediation packs summarize the current repair frontier and optimizer clusters into grouped, evidence-backed operator bundles.` : "No proposal-only remediation packs have been generated yet.",
    packsPath: ARTIFACT_PATHS.metaRemediationPacks
  };
  return {
    ...createMetaRemediationPacksIndex(),
    packs,
    summary,
    updatedAt: generatedAt
  };
}
function buildFamilyOperatorPlaybooks({ workspaceIndex, remediationPacks, longHorizonMemory }) {
  const generatedAt = nowIso();
  const familySummaryMap = new Map((workspaceIndex.repairFrontier?.relationFamilySummaries ?? []).map((family) => [family.id, family]));
  const groupSummaryMap = new Map((workspaceIndex.repairFrontier?.relationGroupSummaries ?? []).map((group) => [group.id, group]));
  const remediationPackMap = new Map((remediationPacks.packs ?? []).map((pack) => [pack.id, pack]));
  const longHorizonByTaxonomyFamily = /* @__PURE__ */ new Map();
  for (const family of longHorizonMemory.families ?? []) {
    for (const taxonomyFamilyId of family.relatedTaxonomyFamilyIds ?? []) {
      const bucket = longHorizonByTaxonomyFamily.get(taxonomyFamilyId) ?? [];
      bucket.push(family);
      longHorizonByTaxonomyFamily.set(taxonomyFamilyId, bucket);
    }
  }
  const candidateFamilyIds = uniqueSorted([
    ...workspaceIndex.repairFrontier?.topDegradedFamilyIds ?? [],
    ...workspaceIndex.metaOptimize?.topTaxonomyFamilyIds ?? [],
    ...workspaceIndex.metaOptimize?.longHorizon?.topTaxonomyFamilyIds ?? [],
    ...(remediationPacks.packs ?? []).flatMap((pack) => pack.taxonomyAnchors?.familyIds ?? []),
    ...(longHorizonMemory.families ?? []).flatMap((family) => family.relatedTaxonomyFamilyIds ?? [])
  ]);
  const playbooks = candidateFamilyIds.map((familyId) => {
    const familySummary = familySummaryMap.get(familyId) ?? { id: familyId, label: familyId, overview: `${familyId} typed wiki pressure is active.`, topReasonCodes: [] };
    const matchedPacks = (remediationPacks.packs ?? []).filter((pack) => (pack.taxonomyAnchors?.familyIds ?? []).includes(familyId));
    const matchedMemoryFamilies = longHorizonByTaxonomyFamily.get(familyId) ?? [];
    const matchedRepairItems = (workspaceIndex.repairFrontier?.prioritizedItems ?? []).filter((item) => {
      const familyIds = uniqueSorted([item.taxonomyFamilyId, ...item.taxonomyFamilyIds ?? []].filter(Boolean));
      const groupIds2 = uniqueSorted([item.taxonomyGroupId, ...item.taxonomyGroupIds ?? []].filter(Boolean));
      return familyIds.includes(familyId) || intersects(groupIds2, [...familySummary.groupIds ?? [], ...matchedPacks.flatMap((pack) => pack.taxonomyAnchors?.groupIds ?? [])]);
    });
    const groupIds = uniqueSorted([
      ...familySummary.groupIds ?? [],
      ...matchedPacks.flatMap((pack) => pack.taxonomyAnchors?.groupIds ?? []),
      ...matchedMemoryFamilies.flatMap((family) => family.relatedTaxonomyGroupIds ?? [])
    ]);
    const groupLabels = groupIds.map((groupId) => groupSummaryMap.get(groupId)?.label ?? groupId);
    const acceptanceCriteria = uniqueSorted(matchedPacks.flatMap((pack) => pack.acceptanceCriteria ?? [])).slice(0, 6);
    const conversionHints = matchedPacks.flatMap((pack) => pack.conversionHints ?? []).reduce((accumulator, hint) => {
      const key = `${hint.targetType}:${hint.targetId}`;
      if (!accumulator.some((item) => `${item.targetType}:${item.targetId}` === key)) {
        accumulator.push(hint);
      }
      return accumulator;
    }, []).slice(0, 6);
    const rankedConversionPaths = buildRankedConversionCandidates(
      matchedPacks.flatMap((pack) => pack.rankedConversionPaths ?? pack.conversionHints ?? []).map((hint) => ({
        ...hint,
        pathScore: (hint.pathScore ?? 0) + 8,
        rankingBasis: [...hint.rankingBasis ?? [], `playbookFamily=${familyId}`]
      }))
    ).slice(0, 6);
    const manualNextActions = uniqueSorted([
      ...matchedPacks.flatMap((pack) => pack.manualNextActions ?? []),
      ...matchedPacks.flatMap((pack) => (pack.packetPointers ?? []).map((pointer) => pointer.nextAction)),
      ...matchedMemoryFamilies.map((family) => `Inspect long-horizon family ${family.id} (${family.trend?.status ?? "stable"}) before declaring ${familySummary.label} pressure resolved.`)
    ]).slice(0, 6);
    const responseOwnerRoles = uniqueSorted([
      ...matchedPacks.flatMap((pack) => (pack.packetPointers ?? []).map((pointer) => pointer.assignedRole)),
      ...matchedPacks.flatMap((pack) => (pack.conversionHints ?? []).map((hint) => hint.assignedRole))
    ]);
    const workspacePointers = uniqueSorted([
      ARTIFACT_PATHS.workspaceIndex,
      ARTIFACT_PATHS.navigationReport,
      ARTIFACT_PATHS.sessionSummary,
      ARTIFACT_PATHS.metaOptimizerReport,
      ARTIFACT_PATHS.metaOperatorPlaybooks,
      ARTIFACT_PATHS.metaRemediationPacks,
      ...matchedPacks.flatMap((pack) => pack.workspacePointers ?? []),
      ...matchedPacks.flatMap((pack) => pack.evidence?.artifactPaths ?? []),
      ...matchedMemoryFamilies.flatMap((family) => family.evidenceArtifactPaths ?? [])
    ]);
    const packetPointers = matchedPacks.flatMap((pack) => pack.packetPointers ?? []).slice(0, 6);
    const artifactUpdateMap = buildPlaybookArtifactUpdateMap({
      familyId,
      familyLabel: familySummary.label,
      matchedPacks,
      repairItems: matchedRepairItems,
      packetPointers,
      workspacePointers,
      taxonomyAnchors: {
        familyIds: [familyId],
        groupIds,
        groupLabels
      }
    });
    const operatorGoal = taxonomyPressureMetadata(familyId, familySummary.label).clusterOperatorGoal;
    const longHorizonSummary = matchedMemoryFamilies.length > 0 ? `${matchedMemoryFamilies.length} linked long-horizon family entries remain active (${matchedMemoryFamilies.map((family) => `${family.id}:${family.trend?.status ?? "stable"}`).join(", ")}).` : "No linked long-horizon family memory is currently active for this taxonomy family.";
    const readiness = buildGuidanceReadiness({
      acceptanceCriteria,
      conversionHints,
      rankedConversionPaths,
      packetPointers,
      evidenceArtifactPaths: uniqueSorted([
        ...matchedPacks.flatMap((pack) => pack.evidence?.artifactPaths ?? []),
        ...matchedMemoryFamilies.flatMap((family) => family.evidenceArtifactPaths ?? [])
      ]),
      longHorizonMemory: matchedMemoryFamilies,
      repairItems: matchedPacks.flatMap((pack) => (pack.frontier?.repairItemIds ?? []).map((id) => ({ id }))),
      reviewConcerns: matchedPacks.flatMap((pack) => pack.reviewConcerns ?? []),
      figureQa: matchedPacks.flatMap((pack) => pack.figureQa ?? []),
      remediationPackIds: matchedPacks.map((pack) => pack.id)
    });
    return {
      id: `family-playbook-${familyId}`,
      title: `${familySummary.label} operator playbook`,
      proposalOnly: true,
      explicitOnly: true,
      noAutoApply: true,
      taxonomyFamilyId: familyId,
      taxonomyFamilyLabel: familySummary.label,
      taxonomyOverview: familySummary.overview ?? `${familySummary.label} typed wiki pressure is active.`,
      taxonomyGroupIds: groupIds,
      taxonomyGroupLabels: groupLabels,
      readiness,
      artifactUpdateMap,
      operatorGoal,
      summary: `${familySummary.overview ?? `${familySummary.label} typed wiki pressure is active.`} This playbook aggregates the current remediation packs, acceptance criteria, conversion hints, and long-horizon memory for the ${familySummary.label} family without auto-applying any change.`,
      longHorizonSummary,
      topReasonCodes: uniqueSorted([
        ...familySummary.topReasonCodes ?? [],
        ...matchedPacks.flatMap((pack) => pack.frontier?.repairItemIds ?? []).slice(0, 3)
      ]).slice(0, 6),
      remediationPackIds: matchedPacks.map((pack) => pack.id),
      clusterIds: uniqueSorted(matchedPacks.map((pack) => pack.clusterId)),
      responseOwnerRoles,
      acceptanceCriteria,
      conversionHints,
      rankedConversionPaths,
      manualNextActions,
      longHorizonFamilyIds: matchedMemoryFamilies.map((family) => family.id),
      workspacePointers,
      packetPointers,
      generatedAt
    };
  }).sort((left, right) => {
    const readinessDelta = readinessRank(left.readiness?.operatorReadiness) - readinessRank(right.readiness?.operatorReadiness);
    if (readinessDelta !== 0) {
      return readinessDelta;
    }
    const packDelta = (right.remediationPackIds?.length ?? 0) - (left.remediationPackIds?.length ?? 0);
    if (packDelta !== 0) {
      return packDelta;
    }
    const longHorizonDelta = (right.longHorizonFamilyIds?.length ?? 0) - (left.longHorizonFamilyIds?.length ?? 0);
    if (longHorizonDelta !== 0) {
      return longHorizonDelta;
    }
    return left.taxonomyFamilyId.localeCompare(right.taxonomyFamilyId);
  });
  return {
    ...createMetaOperatorPlaybooksIndex(),
    playbooks,
    summary: {
      playbookCount: playbooks.length,
      topPlaybookIds: playbooks.slice(0, 3).map((playbook) => playbook.id),
      topTaxonomyFamilyIds: playbooks.slice(0, 3).map((playbook) => playbook.taxonomyFamilyId),
      actionableCount: playbooks.filter((playbook) => playbook.readiness?.operatorReadiness === "actionable").length,
      partiallyActionableCount: playbooks.filter((playbook) => playbook.readiness?.operatorReadiness === "partially-actionable").length,
      advisoryCount: playbooks.filter((playbook) => playbook.readiness?.operatorReadiness === "advisory-only").length,
      readinessOverview: playbooks.length > 0 ? `${playbooks.filter((playbook) => playbook.readiness?.operatorReadiness === "actionable").length} actionable, ${playbooks.filter((playbook) => playbook.readiness?.operatorReadiness === "partially-actionable").length} partially actionable, ${playbooks.filter((playbook) => playbook.readiness?.operatorReadiness === "advisory-only").length} advisory-only family playbooks.` : "No proposal-only family-level operator playbooks have been generated yet.",
      overview: playbooks.length > 0 ? `${playbooks.length} proposal-only family-level operator playbooks summarize taxonomy families using remediation packs, acceptance criteria, conversion hints, and long-horizon memory.` : "No proposal-only family-level operator playbooks have been generated yet.",
      playbooksPath: ARTIFACT_PATHS.metaOperatorPlaybooks
    },
    updatedAt: generatedAt
  };
}
function scaffoldCandidateType(targetType, pack = {}) {
  if (targetType === "update-existing-packet" || targetType === "create-new-packet") {
    return "packet-candidate";
  }
  if (targetType === "add-checklist-entry") {
    return "checklist-candidate";
  }
  if (targetType === "add-revision-item") {
    return (pack.reviewConcerns?.length ?? 0) > 0 ? "review-follow-up-candidate" : "revision-plan-candidate";
  }
  return "revision-plan-candidate";
}
function scaffoldTargetArtifact(targetType, targetId, pack = {}, path28 = {}) {
  if (targetType === "update-existing-packet") {
    const pointer = (pack.packetPointers ?? []).find((item) => item.id === targetId);
    return pointer?.packetContextPath ?? ARTIFACT_PATHS.taskPacketsIndex;
  }
  if (targetType === "create-new-packet") {
    return ARTIFACT_PATHS.taskPacketsIndex;
  }
  if (targetType === "add-checklist-entry") {
    return targetId ?? ARTIFACT_PATHS.checklist;
  }
  if (targetType === "add-revision-item") {
    return targetId ?? ARTIFACT_PATHS.revisionPlan;
  }
  return targetId ?? path28.targetId ?? ARTIFACT_PATHS.workspaceIndex;
}
function summarizeCandidateContext(pack = {}, operatorPlaybooks = {}, sourcePlaybookIds = [], path28 = null) {
  const playbooksById = new Map((operatorPlaybooks.playbooks ?? []).map((playbook) => [playbook.id, playbook]));
  const linkedPacketPointers = (pack.packetPointers ?? []).slice(0, 3).map((pointer) => ({
    id: pointer.id,
    assignedRole: pointer.assignedRole,
    lifecycleStatus: pointer.lifecycleStatus,
    packetContextPath: pointer.packetContextPath ?? null,
    nextAction: pointer.nextAction ?? null
  }));
  const linkedWorkspacePointers = uniqueSorted(pack.workspacePointers ?? []).slice(0, 6);
  const linkedRemediationPacks = [{
    id: pack.id,
    title: pack.title,
    clusterId: pack.clusterId,
    readiness: pack.readiness?.operatorReadiness ?? "advisory-only"
  }];
  const linkedPlaybooks = sourcePlaybookIds.slice(0, 3).map((playbookId) => {
    const playbook = playbooksById.get(playbookId) ?? {};
    return {
      id: playbookId,
      taxonomyFamilyId: playbook.taxonomyFamilyId ?? null,
      title: playbook.title ?? null,
      readiness: playbook.readiness?.operatorReadiness ?? null
    };
  });
  const evidenceSummary = {
    artifactPaths: uniqueSorted(pack.evidence?.artifactPaths ?? []).slice(0, 5),
    ids: uniqueSorted(pack.evidence?.ids ?? []).slice(0, 5)
  };
  const repairSummary = {
    repairItemIds: uniqueSorted(pack.frontier?.repairItemIds ?? []).slice(0, 5),
    reviewConcernIds: uniqueSorted((pack.reviewConcerns ?? []).map((item) => item.id)).slice(0, 4),
    figureIssueIds: uniqueSorted((pack.figureQa ?? []).map((item) => item.id)).slice(0, 4)
  };
  return {
    linkedPacketPointers,
    linkedWorkspacePointers,
    linkedRemediationPacks,
    linkedPlaybooks,
    evidenceSummary,
    repairSummary,
    bridgeNotes: uniqueSorted([
      path28?.rationale ?? null,
      pack.manualNextActions?.[0] ?? null,
      pack.readiness?.overview ?? null
    ]).slice(0, 3)
  };
}
function buildExecutionBridgeCandidates({ remediationPacks, operatorPlaybooks, existingExecutionBridgeCandidates = null }) {
  const generatedAt = nowIso();
  const playbooksById = new Map((operatorPlaybooks.playbooks ?? []).map((playbook) => [playbook.id, playbook]));
  const playbookIdsByFamily = new Map((operatorPlaybooks.playbooks ?? []).map((playbook) => [playbook.taxonomyFamilyId, playbook.id]));
  const preservedObjectiveCandidates = (existingExecutionBridgeCandidates?.candidates ?? []).filter((candidate) => candidate?.candidateOrigin === "operator-objective" || candidate?.objectiveDerived === true).map((candidate) => ({
    ...candidate,
    proposalOnly: true,
    noAutoApply: true,
    candidateOrigin: "operator-objective",
    objectiveDerived: true
  }));
  const candidates = [];
  for (const pack of remediationPacks.packs ?? []) {
    const sourcePlaybookIds = uniqueSorted((pack.taxonomyAnchors?.familyIds ?? []).map((familyId) => playbookIdsByFamily.get(familyId)).filter(Boolean));
    const commonFields = {
      proposalOnly: true,
      noAutoApply: true,
      sourceRemediationPackIds: [pack.id],
      sourcePlaybookIds,
      linkedEvidenceArtifactPaths: uniqueSorted(pack.evidence?.artifactPaths ?? []),
      linkedEvidenceIds: uniqueSorted(pack.evidence?.ids ?? []),
      linkedRepairItemIds: uniqueSorted(pack.frontier?.repairItemIds ?? []),
      suggestedAcceptanceCriteria: uniqueSorted(pack.acceptanceCriteria ?? []),
      taxonomyFamilyIds: uniqueSorted(pack.taxonomyAnchors?.familyIds ?? []),
      taxonomyGroupIds: uniqueSorted(pack.taxonomyAnchors?.groupIds ?? []),
      generatedAt
    };
    for (const path28 of pack.rankedConversionPaths ?? []) {
      const candidateType = scaffoldCandidateType(path28.targetType, pack);
      const targetArtifact = scaffoldTargetArtifact(path28.targetType, path28.targetId, pack, path28);
      const context = summarizeCandidateContext(pack, operatorPlaybooks, sourcePlaybookIds, path28);
      candidates.push({
        id: `candidate-${slugify2(pack.id)}-${slugify2(path28.targetType)}-${slugify2(path28.targetId)}`,
        candidateType,
        priority: path28.priority ?? "secondary",
        rank: path28.rank ?? 1,
        score: (pack.score ?? 0) + (path28.pathScore ?? 0),
        targetArtifact,
        targetId: path28.targetId,
        suggestedTitle: path28.suggestedTitle ?? `${pack.clusterLabel} candidate`,
        suggestedSummary: `${pack.summary} Proposed as a ${candidateType} from remediation pack ${pack.id}; review manually before creating any real work item.`,
        rationale: path28.rationale ?? `Use ${path28.targetType} as a manual execution bridge for ${pack.clusterLabel}.`,
        suggestedNextStep: path28.nextStep ?? pack.manualNextActions?.[0] ?? `Review ${targetArtifact} before acting.`,
        context,
        sourceConversionPath: {
          targetType: path28.targetType,
          targetId: path28.targetId,
          assignedRole: path28.assignedRole ?? "planner",
          rank: path28.rank ?? 1,
          pathScore: path28.pathScore ?? 0,
          rankingBasis: normalizeStringArray5(path28.rankingBasis ?? [])
        },
        ...commonFields
      });
    }
    if ((pack.reviewConcerns?.length ?? 0) > 0) {
      candidates.push({
        id: `candidate-${slugify2(pack.id)}-review-follow-up`,
        candidateType: "review-follow-up-candidate",
        priority: "secondary",
        rank: 99,
        score: (pack.score ?? 0) + 50,
        targetArtifact: ARTIFACT_PATHS.reviewConcerns,
        targetId: pack.reviewConcerns[0].id,
        suggestedTitle: `${pack.clusterLabel} review follow-up`,
        suggestedSummary: `Proposal-only review follow-up candidate for remediation pack ${pack.id}; use it to convert unresolved review pressure into an explicit manual item if needed.`,
        rationale: `Review concerns remain linked to ${pack.id}, so a review-focused follow-up candidate stays useful even if no task is created automatically.`,
        suggestedNextStep: `Inspect review concern ${pack.reviewConcerns[0].id} and decide whether to record a manual follow-up item.`,
        context: summarizeCandidateContext(pack, operatorPlaybooks, sourcePlaybookIds, null),
        sourceConversionPath: null,
        ...commonFields
      });
    }
    if ((pack.figureQa?.length ?? 0) > 0) {
      candidates.push({
        id: `candidate-${slugify2(pack.id)}-figure-follow-up`,
        candidateType: "figure-follow-up-candidate",
        priority: "secondary",
        rank: 100,
        score: (pack.score ?? 0) + 48,
        targetArtifact: ARTIFACT_PATHS.figureQa,
        targetId: pack.figureQa[0].id,
        suggestedTitle: `${pack.clusterLabel} figure follow-up`,
        suggestedSummary: `Proposal-only figure follow-up candidate for remediation pack ${pack.id}; use it to convert unresolved figure QA into an explicit manual item if needed.`,
        rationale: `Figure QA issues remain linked to ${pack.id}, so a figure-focused follow-up candidate keeps that work visible without auto-creating anything.`,
        suggestedNextStep: `Inspect figure QA issue ${pack.figureQa[0].id} and decide whether to record a manual follow-up item.`,
        context: summarizeCandidateContext(pack, operatorPlaybooks, sourcePlaybookIds, null),
        sourceConversionPath: null,
        ...commonFields
      });
    }
  }
  const generatedCandidateIds = new Set(candidates.map((candidate) => candidate.id));
  const mergedCandidates = [
    ...candidates,
    ...preservedObjectiveCandidates.filter((candidate) => !generatedCandidateIds.has(candidate.id))
  ];
  const sortedCandidates = mergedCandidates.sort((left, right) => {
    const scoreDelta = (right.score ?? 0) - (left.score ?? 0);
    if (scoreDelta !== 0) {
      return scoreDelta;
    }
    const rankDelta = (left.rank ?? 999) - (right.rank ?? 999);
    if (rankDelta !== 0) {
      return rankDelta;
    }
    return left.id.localeCompare(right.id);
  }).map((candidate, index) => ({
    ...candidate,
    globalRank: index + 1
  }));
  const candidateTypeCounts = sortedCandidates.reduce((accumulator, candidate) => {
    accumulator[candidate.candidateType] = (accumulator[candidate.candidateType] ?? 0) + 1;
    return accumulator;
  }, {});
  return {
    ...createMetaExecutionBridgeCandidatesIndex(),
    candidates: sortedCandidates,
    summary: {
      candidateCount: sortedCandidates.length,
      topCandidateIds: sortedCandidates.slice(0, 5).map((candidate) => candidate.id),
      candidateTypeCounts,
      overview: sortedCandidates.length > 0 ? `${sortedCandidates.length} proposal-only execution bridge candidates translate remediation packs and playbooks into likely manual work-item shapes without creating anything automatically.` : "No proposal-only execution bridge candidates have been generated yet.",
      candidatesPath: ARTIFACT_PATHS.metaExecutionBridgeCandidates
    },
    updatedAt: generatedAt
  };
}
function pushMetaEvent(collection, event = {}) {
  collection.push({
    id: event.id,
    signalType: event.signalType,
    severity: event.severity ?? "medium",
    summary: event.summary,
    evidenceArtifactPaths: uniqueSorted(event.evidenceArtifactPaths ?? []),
    evidenceIds: uniqueSorted(event.evidenceIds ?? []),
    recommendationIds: uniqueSorted(event.recommendationIds ?? []),
    observedAt: event.observedAt ?? nowIso()
  });
}
function pushRecommendation(collection, recommendation = {}) {
  collection.push({
    id: recommendation.id,
    category: recommendation.category,
    priority: recommendation.priority ?? "medium",
    proposalOnly: true,
    summary: recommendation.summary,
    rationale: recommendation.rationale,
    nextAction: recommendation.nextAction,
    scope: recommendation.scope ?? "workflow",
    responseOwnerRole: recommendation.responseOwnerRole ?? null,
    evidenceArtifactPaths: uniqueSorted(recommendation.evidenceArtifactPaths ?? []),
    evidenceIds: uniqueSorted(recommendation.evidenceIds ?? []),
    signalTypes: uniqueSorted(recommendation.signalTypes ?? []),
    relatedRecommendationIds: uniqueSorted(recommendation.relatedRecommendationIds ?? []),
    recurrenceCount: recommendation.recurrenceCount ?? 0,
    repairFrontierOverlap: recommendation.repairFrontierOverlap ?? 0,
    auditCriticality: recommendation.auditCriticality ?? 0,
    bridgeCriticality: recommendation.bridgeCriticality ?? 0,
    queueChurnCount: recommendation.queueChurnCount ?? 0,
    taxonomyPressure: recommendation.taxonomyPressure ?? buildTaxonomyPressure(),
    score: recommendation.score ?? 0,
    rankingBasis: normalizeStringArray5(recommendation.rankingBasis ?? []),
    tieBreakKey: recommendation.tieBreakKey ?? null,
    generatedAt: recommendation.generatedAt ?? nowIso()
  });
}
function buildRepairFrontier({ wikiRelations, figureQa, stalePackets = [], handoffObligations = [], latestComparison = null, board = null }) {
  const relationGroupSummaries = (wikiRelations.summary?.taxonomy?.groups ?? []).filter((group) => group.degradedCount > 0).map((group) => ({
    id: group.id,
    label: group.label,
    degradedCount: group.degradedCount,
    totalRelations: group.totalRelations,
    severity: group.severity ?? "medium",
    topReasonCodes: uniqueSorted(group.topReasonCodes ?? []),
    overview: group.overview ?? `${group.label} has degraded typed wiki relations.`
  }));
  const relationFamilySummaries = (wikiRelations.summary?.taxonomy?.families ?? []).filter((family) => family.degradedCount > 0).map((family) => ({
    id: family.id,
    label: family.label,
    degradedCount: family.degradedCount,
    totalRelations: family.totalRelations,
    severity: family.severity ?? "medium",
    topReasonCodes: uniqueSorted(family.topReasonCodes ?? []),
    overview: family.overview ?? `${family.label} has degraded typed wiki relations.`
  }));
  const relationFamilyItems = (wikiRelations.summary?.taxonomyRepairFrontier ?? []).map((item) => ({
    ...item,
    taxonomyFamilyId: item.taxonomyFamilyId ?? null,
    taxonomyGroupIds: uniqueSorted(item.taxonomyGroupIds ?? [])
  }));
  const relationItems = (wikiRelations.items ?? []).filter((relation) => relation.integrity?.status === "degraded").map((relation) => ({
    id: `repair-${relation.id}`,
    frontierType: "typed-wiki-relation",
    severity: relation.integrity?.severity ?? "medium",
    taxonomyFamilyId: relation.taxonomy?.familyId ?? null,
    taxonomyGroupId: relation.taxonomy?.groupId ?? null,
    summary: `Repair typed wiki relation ${relation.id} (${relation.relationType}, ${relation.taxonomy?.familyLabel ?? "uncategorized"} / ${relation.taxonomy?.groupLabel ?? "uncategorized"}).`,
    reasons: (relation.integrity?.reasons ?? []).map((reason) => reason.message).join(" "),
    reasonCodes: uniqueSorted((relation.integrity?.reasons ?? []).map((reason) => reason.code)),
    artifactPath: ARTIFACT_PATHS.wikiRelations,
    relatedArtifactPaths: uniqueSorted([ARTIFACT_PATHS.wikiEntities, ...relation.sourceArtifactPaths ?? []]),
    nextAction: `Repair the local artifacts for ${relation.id}, then rerun project:dove.status or refresh_wiki.`
  }));
  const figureItems = (figureQa.issues ?? []).map((issue) => ({
    id: `repair-${issue.id}`,
    frontierType: "figure-qa",
    severity: issue.severity ?? "medium",
    summary: `Repair figure artifact issue ${issue.id}.`,
    reasons: issue.summary ?? `Figure issue ${issue.code}.`,
    reasonCodes: uniqueSorted([issue.code]),
    artifactPath: ARTIFACT_PATHS.figureQa,
    relatedArtifactPaths: uniqueSorted([ARTIFACT_PATHS.figuresIndex, ...issue.artifactPaths ?? []]),
    nextAction: `Repair the staged figure artifacts for ${issue.figureId ?? issue.id}, then rerun validate_figure_pipeline.`
  }));
  const governanceItems = [];
  if (stalePackets.length > 0 || handoffObligations.length > 0) {
    governanceItems.push({
      id: "repair-workflow-governance",
      frontierType: "workflow-governance",
      severity: stalePackets.length > 0 ? "high" : "medium",
      summary: `Reduce ${stalePackets.length} stale packets and ${handoffObligations.length} handoff obligations before expanding concurrent work.`,
      reasons: `The workspace already carries ${stalePackets.length} stale packets and ${handoffObligations.length} cross-role handoff obligations, so coordination debt is staying operator-visible instead of closing cleanly.`,
      reasonCodes: uniqueSorted([
        ...stalePackets.length > 0 ? ["stale-packets"] : [],
        ...handoffObligations.length > 0 ? ["handoff-obligations"] : []
      ]),
      artifactPath: ARTIFACT_PATHS.workspaceIndex,
      relatedArtifactPaths: uniqueSorted([ARTIFACT_PATHS.orchestrationBoard, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.navigationReport]),
      nextAction: stalePackets[0]?.nextAction ?? handoffObligations[0]?.nextAction ?? board?.nextAction ?? "Repair the queue governance debt before widening the active frontier."
    });
  }
  if (latestComparison && (latestComparison.unresolvedConcernsAdded ?? []).length > 0) {
    governanceItems.push({
      id: `repair-version-governance-${latestComparison.id}`,
      frontierType: "version-governance",
      severity: "medium",
      summary: `Explain or repair unresolved concern debt introduced by version comparison ${latestComparison.id}.`,
      reasons: `Comparison ${latestComparison.id} added ${(latestComparison.unresolvedConcernsAdded ?? []).length} unresolved concerns, so version movement still needs an explicit repair or acceptance trail.`,
      reasonCodes: ["version-comparison-unresolved-concerns"],
      artifactPath: ARTIFACT_PATHS.versionComparisons,
      relatedArtifactPaths: [ARTIFACT_PATHS.versionComparisonReport],
      nextAction: `Review ${ARTIFACT_PATHS.versionComparisonReport} and connect the added unresolved concerns to explicit revision or repair work before treating the newer version as stable.`
    });
  }
  const prioritizedItems = [...relationFamilyItems, ...relationItems, ...figureItems, ...governanceItems].sort((left, right) => {
    const severityDelta = severityRank(left.severity) - severityRank(right.severity);
    if (severityDelta !== 0) {
      return severityDelta;
    }
    return left.id.localeCompare(right.id);
  }).slice(0, 12);
  return {
    count: relationFamilyItems.length + relationItems.length + figureItems.length + governanceItems.length,
    relationIssueCount: relationItems.length,
    relationFamilyIssueCount: relationFamilyItems.length,
    managedArtifactIssueCount: figureItems.length,
    governanceIssueCount: governanceItems.length,
    topDegradedFamilyIds: relationFamilySummaries.slice(0, 3).map((family) => family.id),
    topDegradedGroupIds: relationGroupSummaries.slice(0, 3).map((group) => group.id),
    taxonomyOverview: wikiRelations.summary?.taxonomy?.overview ?? "No degraded typed wiki relation families are currently summarized.",
    relationFamilySummaries,
    relationGroupSummaries,
    prioritizedItems
  };
}
function buildMetaOptimizeSurface({ root, board, workspaceIndex, journal, reviewConcerns, reviewState, adversarialState, experimentAudits, bridgeLog, figureQa, comparisons, existingLongHorizonMemory }) {
  const generatedAt = nowIso();
  const events = [];
  const recommendations = [];
  const recentEntries = [...journal.entries ?? []].slice(-40);
  const unresolvedConcerns = (reviewConcerns.items ?? []).filter((item) => !["resolved", "retired"].includes(item.status));
  const escalatedConcerns = unresolvedConcerns.filter((item) => ["escalated", "contested"].includes(item.status));
  const recurringConcerns = unresolvedConcerns.filter((item) => (item.recurrenceCount ?? 0) >= 2);
  const blockedAudits = (experimentAudits.items ?? []).filter((item) => item.auditVerdict === "blocked" || (item.integrityFlags ?? []).length > 0);
  const heldBridges = (bridgeLog.items ?? []).filter((item) => item.bridgeStatus === "held-for-review" || item.auditVerdict === "blocked" || (item.auditIds ?? []).length === 0);
  const figureIssues = figureQa.issues ?? [];
  const stalePackets = workspaceIndex.workQueues?.stale ?? [];
  const handoffObligations = workspaceIndex.handoffObligations ?? [];
  const latestComparison = (comparisons.items ?? []).at(-1) ?? null;
  const repairItems = workspaceIndex.repairFrontier?.prioritizedItems ?? [];
  const repairFamilyLabelMap = toLabelMap(workspaceIndex.repairFrontier?.relationFamilySummaries ?? []);
  const repairGroupLabelMap = toLabelMap(workspaceIndex.repairFrontier?.relationGroupSummaries ?? []);
  const repeatedRepairishEvents = recentEntries.reduce((accumulator, entry) => {
    if (entry.type === "query-meta-optimize") {
      return accumulator;
    }
    if (!/(review|revision|audit|bridge|figure|version|workspace-index|meta-optimize)/.test(entry.type ?? "")) {
      return accumulator;
    }
    accumulator[entry.type] = (accumulator[entry.type] ?? 0) + 1;
    return accumulator;
  }, {});
  for (const concern of escalatedConcerns) {
    const recommendationId = `meta-review-${concern.id}`;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "review-concern",
      severity: concern.severity === "high" ? "high" : "medium",
      summary: `Concern ${concern.id} remains ${concern.status} after ${concern.recurrenceCount} review rounds.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.adversarialReviewState, ARTIFACT_PATHS.reviewState],
      evidenceIds: [concern.id],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "review-discipline",
      priority: concern.severity === "high" ? "critical" : "high",
      summary: `Escalate durable workflow attention to review concern ${concern.id}.`,
      rationale: `The concern is still ${concern.status} with recurrence count ${concern.recurrenceCount}, so the workflow is repeatedly revisiting the same review debt without closure.`,
      nextAction: `Resolve concern ${concern.id}, update the linked artifacts, then rerun project:dove.review before finalization claims.`,
      scope: "review-artifact health",
      responseOwnerRole: concern.responseOwnerRole,
      evidenceArtifactPaths: summarizeLinkedEvidence([ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.adversarialReviewState, ARTIFACT_PATHS.reviewState], concern.linkedArtifactPaths),
      evidenceIds: [concern.id, ...concern.linkedAuditIds ?? [], ...concern.linkedBridgeIds ?? []],
      signalTypes: ["review-concern", "adversarial-review"],
      recurrenceCount: concern.recurrenceCount ?? 0,
      auditCriticality: (concern.linkedAuditIds ?? []).length > 0 ? 1 : 0,
      bridgeCriticality: (concern.linkedBridgeIds ?? []).length > 0 ? 1 : 0
    });
  }
  for (const concern of recurringConcerns.filter((item) => !escalatedConcerns.some((other) => other.id === item.id)).slice(0, 4)) {
    const recommendationId = `meta-recurring-review-${concern.id}`;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "recurring-review-concern",
      severity: "medium",
      summary: `Concern ${concern.id} has recurred across ${concern.recurrenceCount} review rounds.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.reviewState],
      evidenceIds: [concern.id],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "repair-pattern",
      priority: "high",
      summary: `Turn recurring concern ${concern.id} into a more explicit workflow checkpoint.`,
      rationale: `The same concern has been seen ${concern.recurrenceCount} times, which suggests the current workflow guidance is not forcing a durable close-out step.`,
      nextAction: `Add an explicit checklist or revision-plan item tied to ${concern.id} so future passes verify closure instead of rediscovering the same issue.`,
      scope: "workflow checkpointing",
      responseOwnerRole: concern.responseOwnerRole,
      evidenceArtifactPaths: summarizeLinkedEvidence([ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.reviewState], concern.linkedArtifactPaths),
      evidenceIds: [concern.id],
      signalTypes: ["review-concern"],
      recurrenceCount: concern.recurrenceCount ?? 0
    });
  }
  for (const item of repairItems.slice(0, 5)) {
    if (isGovernanceRepairFrontierItem(item)) {
      continue;
    }
    const recommendationId = `meta-repair-${slugify2(item.id)}`;
    const taxonomyPressure2 = buildTaxonomyPressure(
      [item.taxonomyFamilyId, ...item.taxonomyFamilyIds ?? []].filter(Boolean),
      [item.taxonomyGroupId, ...item.taxonomyGroupIds ?? []].filter(Boolean),
      repairFamilyLabelMap,
      repairGroupLabelMap
    );
    const primaryPressure = taxonomyPressure2.pressureAreas[0] ?? item.frontierType;
    const primaryFamilyLabel = taxonomyPressure2.familyLabels[0] ?? item.frontierType;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: item.frontierType,
      severity: item.severity ?? "medium",
      summary: item.summary,
      evidenceArtifactPaths: [item.artifactPath, ...item.relatedArtifactPaths ?? []],
      evidenceIds: [item.id],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "artifact-health",
      priority: item.severity === "high" ? "critical" : "high",
      summary: taxonomyPressure2.familyIds.length > 0 ? `Keep ${primaryPressure} visible as explicit repair work, not background maintenance.` : `Keep ${item.frontierType} visible as explicit repair work, not background maintenance.`,
      rationale: taxonomyPressure2.familyIds.length > 0 ? `${item.reasons || `The repair frontier still carries ${item.frontierType} debt.`} This is now explicit ${primaryPressure} pressure in the typed wiki taxonomy (${primaryFamilyLabel}${taxonomyPressure2.groupLabels.length > 0 ? ` via ${taxonomyPressure2.groupLabels.join(", ")}` : ""}), so operators can see exactly whether the weakness is in evidence-grounding, validation-loop, review-pressure, or another relation family.` : item.reasons || `The repair frontier still carries ${item.frontierType} debt, so the workflow should preserve an explicit repair surface until it clears.`,
      nextAction: item.nextAction,
      scope: taxonomyPressure2.familyIds.length > 0 ? `${primaryPressure} / artifact health` : "artifact health",
      evidenceArtifactPaths: [item.artifactPath, ...item.relatedArtifactPaths ?? []],
      evidenceIds: [item.id, ...item.reasonCodes ?? []],
      signalTypes: [item.frontierType],
      repairFrontierOverlap: 1,
      recurrenceCount: Math.min(item.degradedCount ?? 1, 6),
      taxonomyPressure: taxonomyPressure2
    });
  }
  for (const audit of blockedAudits.slice(0, 4)) {
    const recommendationId = `meta-audit-${audit.id}`;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "experiment-audit",
      severity: audit.auditVerdict === "blocked" ? "high" : "medium",
      summary: `Audit ${audit.id} is ${audit.auditVerdict} with integrity flags: ${(audit.integrityFlags ?? []).join(", ") || "none"}.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.experimentAudits],
      evidenceIds: [audit.id, ...audit.integrityFlags ?? []],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "experiment-integrity",
      priority: audit.auditVerdict === "blocked" ? "critical" : "high",
      summary: `Treat audit ${audit.id} as a workflow gate before more claim promotion.`,
      rationale: `This audit is not clean, so downstream claim updates or review closure would be relying on unstable experiment evidence.`,
      nextAction: `Repair the experiment artifacts referenced by ${audit.id}, rerun project:dove.experience, and only then bridge results into claims.`,
      scope: "experiment integrity",
      responseOwnerRole: "experiment-planner",
      evidenceArtifactPaths: [ARTIFACT_PATHS.experimentAudits, ...audit.reviewedArtifactRefs ?? []],
      evidenceIds: [audit.id, audit.resultId, audit.experimentId, ...audit.integrityFlags ?? []],
      signalTypes: ["experiment-audit"],
      auditCriticality: audit.auditVerdict === "blocked" ? 2 : 1
    });
  }
  for (const bridge of heldBridges.slice(0, 4)) {
    const recommendationId = `meta-bridge-${bridge.id}`;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "claim-bridge",
      severity: bridge.auditVerdict === "blocked" ? "high" : "medium",
      summary: `Bridge ${bridge.id} is ${bridge.bridgeStatus} with audit verdict ${bridge.auditVerdict ?? "missing"}.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.claimBridgeLog],
      evidenceIds: [bridge.id, bridge.resultId, bridge.experimentId],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "claim-bridge",
      priority: bridge.auditVerdict === "blocked" ? "critical" : "high",
      summary: `Keep claim bridge ${bridge.id} in proposal-only review until its audit trail is clean.`,
      rationale: bridge.reason ?? `The bridge is not safely applied, which means the workflow still needs an explicit review step before stronger claim status changes.`,
      nextAction: `Resolve the blocked or missing audits for ${bridge.id}, then rerun project:dove.experience with the repaired evidence trail.`,
      scope: "result-to-claim transition health",
      responseOwnerRole: "experiment-planner",
      evidenceArtifactPaths: [ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.experimentAudits],
      evidenceIds: [bridge.id, bridge.resultId, bridge.experimentId, ...bridge.auditIds ?? [], ...bridge.integrityFlags ?? []],
      signalTypes: ["claim-bridge", "experiment-audit"],
      auditCriticality: bridge.auditVerdict === "blocked" ? 1 : 0,
      bridgeCriticality: bridge.bridgeStatus === "held-for-review" || bridge.auditVerdict === "blocked" ? 2 : 1
    });
  }
  if (stalePackets.length > 0 || handoffObligations.length > 0) {
    const recommendationId = "meta-work-queue-discipline";
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "workspace-queue",
      severity: stalePackets.length > 0 ? "medium" : "low",
      summary: `Workspace has ${stalePackets.length} stale packets and ${handoffObligations.length} handoff obligations.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.orchestrationBoard],
      evidenceIds: [...stalePackets.map((packet) => packet.id), ...handoffObligations.map((item) => item.packetId)],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "workflow-queue",
      priority: stalePackets.length > 0 ? "high" : "medium",
      summary: "Reduce stale or cross-role queue churn before adding more concurrent work.",
      rationale: `The workspace index already shows stale packets or unresolved handoffs, so more work will likely amplify coordination debt instead of closing it.`,
      nextAction: stalePackets[0]?.nextAction ?? handoffObligations[0]?.nextAction ?? board.nextAction,
      scope: "orchestration discipline",
      responseOwnerRole: board.assignedRole,
      evidenceArtifactPaths: [ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.orchestrationBoard],
      evidenceIds: [...stalePackets.map((packet) => packet.id), ...handoffObligations.map((item) => item.packetId)],
      signalTypes: ["workspace-queue"],
      queueChurnCount: stalePackets.length + handoffObligations.length
    });
  }
  if (latestComparison && (latestComparison.unresolvedConcernsAdded ?? []).length > 0) {
    const recommendationId = `meta-version-${latestComparison.id}`;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "version-comparison",
      severity: "medium",
      summary: `Comparison ${latestComparison.id} introduced ${(latestComparison.unresolvedConcernsAdded ?? []).length} unresolved concerns.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.versionComparisons, ARTIFACT_PATHS.versionComparisonReport],
      evidenceIds: [latestComparison.id, ...latestComparison.unresolvedConcernsAdded ?? []],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "version-evolution",
      priority: "medium",
      summary: `Explain newly introduced concern debt in version comparison ${latestComparison.id}.`,
      rationale: `The latest comparison added unresolved concerns, so the version report should stay coupled to an explicit explanation of why those regressions were accepted or how they will be repaired.`,
      nextAction: `Review ${ARTIFACT_PATHS.versionComparisonReport} and connect the added unresolved concerns to concrete revision tasks before treating the newer version as stable.`,
      scope: "version comparison discipline",
      responseOwnerRole: "version-analyst",
      evidenceArtifactPaths: [ARTIFACT_PATHS.versionComparisons, ARTIFACT_PATHS.versionComparisonReport],
      evidenceIds: [latestComparison.id, ...latestComparison.unresolvedConcernsAdded ?? []],
      signalTypes: ["version-comparison"],
      recurrenceCount: (latestComparison.unresolvedConcernsAdded ?? []).length
    });
  }
  for (const [eventType, count] of Object.entries(repeatedRepairishEvents).filter(([, count2]) => count2 >= 4).slice(0, 3)) {
    const recommendationId = `meta-journal-${slugify2(eventType)}`;
    pushMetaEvent(events, {
      id: `event-${recommendationId}`,
      signalType: "session-journal",
      severity: "low",
      summary: `Recent session journal repeated ${eventType} ${count} times.`,
      evidenceArtifactPaths: [ARTIFACT_PATHS.sessionJournal],
      evidenceIds: [eventType],
      recommendationIds: [recommendationId]
    });
    pushRecommendation(recommendations, {
      id: recommendationId,
      category: "workflow-observability",
      priority: "low",
      summary: `Inspect whether repeated ${eventType} actions indicate workflow churn.`,
      rationale: `The journal shows ${count} recent ${eventType} events, which can be a signal that operators are repeatedly refreshing or repairing the same surface instead of closing a durable issue.`,
      nextAction: `Inspect the newest ${eventType} entries in ${ARTIFACT_PATHS.sessionJournal} and decide whether a narrower checklist, artifact rule, or review checkpoint should make the next step more explicit.`,
      scope: "session/workflow observability",
      responseOwnerRole: board.assignedRole,
      evidenceArtifactPaths: [ARTIFACT_PATHS.sessionJournal],
      evidenceIds: [eventType],
      signalTypes: ["session-journal"],
      recurrenceCount: count
    });
  }
  const rankedRecommendations = recommendations.map((recommendation) => {
    const cluster = deriveRecommendationCluster(recommendation);
    const scorecard = buildRecommendationScorecard(recommendation, repeatedRepairishEvents);
    const rankingBasis = [
      `priority=${recommendation.priority}`,
      `recurrence=${scorecard.recurrenceCount}`,
      `evidenceDensity=${scorecard.evidenceDensity}`,
      `crossSessionRecurrence=${scorecard.crossSessionRecurrence}`,
      `repairFrontierOverlap=${scorecard.repairFrontierOverlap}`,
      `auditCriticality=${scorecard.auditCriticality}`,
      `bridgeCriticality=${scorecard.bridgeCriticality}`,
      `queueChurn=${scorecard.queueChurn}`,
      `taxonomyFamilyPressure=${scorecard.taxonomyFamilyPressure}`,
      `taxonomyGroupPressure=${scorecard.taxonomyGroupPressure}`
    ];
    return {
      ...recommendation,
      clusterId: cluster.id,
      clusterLabel: cluster.label,
      clusterSummary: cluster.summary,
      clusterOperatorGoal: cluster.operatorGoal,
      score: scorecard.score,
      rankingBasis,
      signalStrength: {
        evidenceDensity: scorecard.evidenceDensity,
        recurrenceCount: scorecard.recurrenceCount,
        crossSessionRecurrence: scorecard.crossSessionRecurrence,
        repairFrontierOverlap: scorecard.repairFrontierOverlap,
        auditCriticality: scorecard.auditCriticality,
        bridgeCriticality: scorecard.bridgeCriticality,
        queueChurn: scorecard.queueChurn,
        taxonomyFamilyPressure: scorecard.taxonomyFamilyPressure,
        taxonomyGroupPressure: scorecard.taxonomyGroupPressure
      }
    };
  }).sort((left, right) => {
    const scoreDelta = right.score - left.score;
    if (scoreDelta !== 0) {
      return scoreDelta;
    }
    const priorityDelta = recommendationSortRank(left.priority) - recommendationSortRank(right.priority);
    if (priorityDelta !== 0) {
      return priorityDelta;
    }
    const clusterDelta = left.clusterId.localeCompare(right.clusterId);
    if (clusterDelta !== 0) {
      return clusterDelta;
    }
    return left.id.localeCompare(right.id);
  }).slice(0, 12).map((recommendation, index) => ({
    ...recommendation,
    rank: index + 1,
    sortKey: [
      String(9999 - recommendation.score).padStart(4, "0"),
      String(recommendationSortRank(recommendation.priority)).padStart(2, "0"),
      String(index + 1).padStart(2, "0"),
      recommendation.clusterId,
      recommendation.category,
      recommendation.id
    ].join(":"),
    tieBreakKey: [
      String(9999 - recommendation.score).padStart(4, "0"),
      String(recommendationSortRank(recommendation.priority)).padStart(2, "0"),
      recommendation.clusterId,
      recommendation.category,
      recommendation.id
    ].join(":")
  }));
  const clusterMap = /* @__PURE__ */ new Map();
  for (const recommendation of rankedRecommendations) {
    const existing = clusterMap.get(recommendation.clusterId) ?? {
      id: recommendation.clusterId,
      label: recommendation.clusterLabel,
      summary: recommendation.clusterSummary,
      operatorGoal: recommendation.clusterOperatorGoal,
      priority: recommendation.priority,
      score: 0,
      recommendationCount: 0,
      criticalCount: 0,
      topRecommendationId: recommendation.id,
      recommendationIds: [],
      categories: {},
      responseOwnerRoles: [],
      evidenceArtifactPaths: [],
      evidenceIds: [],
      signalTypes: [],
      maxRecommendationScore: 0,
      maxRecurrenceCount: 0,
      queueChurnCount: 0,
      repairFrontierOverlap: 0,
      auditCriticality: 0,
      bridgeCriticality: 0,
      taxonomyPressure: buildTaxonomyPressure()
    };
    existing.priority = recommendationSortRank(recommendation.priority) < recommendationSortRank(existing.priority) ? recommendation.priority : existing.priority;
    existing.score += recommendation.score;
    existing.recommendationCount += 1;
    existing.criticalCount += recommendation.priority === "critical" ? 1 : 0;
    existing.topRecommendationId = existing.topRecommendationId === recommendation.id || recommendation.score > existing.maxRecommendationScore ? recommendation.id : existing.topRecommendationId;
    existing.recommendationIds.push(recommendation.id);
    existing.categories[recommendation.category] = (existing.categories[recommendation.category] ?? 0) + 1;
    if (recommendation.responseOwnerRole) {
      existing.responseOwnerRoles.push(recommendation.responseOwnerRole);
    }
    existing.evidenceArtifactPaths.push(...recommendation.evidenceArtifactPaths);
    existing.evidenceIds.push(...recommendation.evidenceIds);
    existing.signalTypes.push(...recommendation.signalTypes);
    existing.maxRecommendationScore = Math.max(existing.maxRecommendationScore, recommendation.score);
    existing.maxRecurrenceCount = Math.max(existing.maxRecurrenceCount, recommendation.signalStrength.recurrenceCount);
    existing.queueChurnCount += recommendation.signalStrength.queueChurn;
    existing.repairFrontierOverlap += recommendation.signalStrength.repairFrontierOverlap;
    existing.auditCriticality += recommendation.signalStrength.auditCriticality;
    existing.bridgeCriticality += recommendation.signalStrength.bridgeCriticality;
    existing.taxonomyPressure = buildTaxonomyPressure(
      [...existing.taxonomyPressure.familyIds ?? [], ...recommendation.taxonomyPressure?.familyIds ?? []],
      [...existing.taxonomyPressure.groupIds ?? [], ...recommendation.taxonomyPressure?.groupIds ?? []],
      new Map([
        ...(existing.taxonomyPressure.familyIds ?? []).map((familyId, index) => [familyId, existing.taxonomyPressure.familyLabels?.[index] ?? familyId]),
        ...(recommendation.taxonomyPressure?.familyIds ?? []).map((familyId, index) => [familyId, recommendation.taxonomyPressure.familyLabels?.[index] ?? familyId])
      ]),
      new Map([
        ...(existing.taxonomyPressure.groupIds ?? []).map((groupId, index) => [groupId, existing.taxonomyPressure.groupLabels?.[index] ?? groupId]),
        ...(recommendation.taxonomyPressure?.groupIds ?? []).map((groupId, index) => [groupId, recommendation.taxonomyPressure.groupLabels?.[index] ?? groupId])
      ])
    );
    clusterMap.set(recommendation.clusterId, existing);
  }
  const taxonomyPressure = collectTaxonomyPressureSummary(rankedRecommendations);
  const clusters = Array.from(clusterMap.values()).map((cluster) => ({
    ...cluster,
    recommendationIds: uniqueSorted(cluster.recommendationIds),
    responseOwnerRoles: uniqueSorted(cluster.responseOwnerRoles),
    evidenceArtifactPaths: uniqueSorted(cluster.evidenceArtifactPaths),
    evidenceIds: uniqueSorted(cluster.evidenceIds),
    signalTypes: uniqueSorted(cluster.signalTypes),
    taxonomyPressure: cluster.taxonomyPressure,
    summaryLine: `${cluster.recommendationCount} recommendations, ${cluster.criticalCount} critical, score ${cluster.score}`,
    membershipPreview: rankedRecommendations.filter((item) => item.clusterId === cluster.id).slice(0, 3).map((item) => ({
      id: item.id,
      rank: item.rank,
      priority: item.priority,
      score: item.score,
      summary: item.summary,
      tieBreakKey: item.tieBreakKey
    }))
  })).sort((left, right) => {
    const priorityDelta = recommendationSortRank(left.priority) - recommendationSortRank(right.priority);
    if (priorityDelta !== 0) {
      return priorityDelta;
    }
    const scoreDelta = right.score - left.score;
    if (scoreDelta !== 0) {
      return scoreDelta;
    }
    return left.id.localeCompare(right.id);
  }).map((cluster, index) => ({ ...cluster, rank: index + 1, sortKey: clusterSortKey(cluster) }));
  const clusterMembership = Object.fromEntries(clusters.map((cluster) => [cluster.id, cluster.recommendationIds]));
  const keptRecommendationIds = new Set(rankedRecommendations.map((item) => item.id));
  const sortedEvents = events.filter((item) => item.recommendationIds.some((id) => keptRecommendationIds.has(id))).sort((left, right) => {
    const severityDelta = severityRank(left.severity) - severityRank(right.severity);
    if (severityDelta !== 0) {
      return severityDelta;
    }
    return left.id.localeCompare(right.id);
  });
  const categoryCounts = rankedRecommendations.reduce((accumulator, item) => {
    accumulator[item.category] = (accumulator[item.category] ?? 0) + 1;
    return accumulator;
  }, {});
  const signalTypes = uniqueSorted(rankedRecommendations.flatMap((item) => item.signalTypes));
  const criticalCount = rankedRecommendations.filter((item) => item.priority === "critical").length;
  const clusterCount = clusters.length;
  const frontierScore = rankedRecommendations.reduce((sum, item) => sum + item.score, 0);
  const topClusterIds = clusters.slice(0, 3).map((cluster) => cluster.id);
  const topRecommendationIds = rankedRecommendations.slice(0, 5).map((item) => item.id);
  const frontierSummary = buildFrontierSummary(rankedRecommendations.length, clusterCount, criticalCount, frontierScore, topClusterIds, taxonomyPressure);
  const topClusters = clusters.slice(0, 3).map((cluster) => ({
    id: cluster.id,
    label: cluster.label,
    rank: cluster.rank,
    priority: cluster.priority,
    score: cluster.score,
    recommendationCount: cluster.recommendationCount,
    criticalCount: cluster.criticalCount,
    summary: cluster.summary,
    operatorGoal: cluster.operatorGoal,
    topRecommendationId: cluster.topRecommendationId,
    taxonomyPressure: cluster.taxonomyPressure,
    sortKey: cluster.sortKey
  }));
  const metaEvents = {
    ...createMetaEventsIndex(),
    items: sortedEvents,
    updatedAt: generatedAt
  };
  const metaRecommendations = {
    ...createMetaRecommendationsIndex(),
    items: rankedRecommendations,
    clusters,
    ranking: {
      method: "durable-signal-frontier-v1",
      signals: [
        "priority",
        "recurrenceCount",
        "evidenceDensity",
        "crossSessionRecurrence",
        "repairFrontierOverlap",
        "auditCriticality",
        "bridgeCriticality",
        "queueChurn",
        "taxonomyFamilyPressure",
        "taxonomyGroupPressure"
      ],
      tieBreakOrder: ["score-desc", "priority-rank", "cluster-rank", "cluster-id", "category", "id"]
    },
    frontier: {
      recommendationCount: rankedRecommendations.length,
      criticalCount,
      clusterCount,
      frontierScore,
      topClusterIds,
      topRecommendationIds,
      activeSignalTypes: signalTypes,
      topTaxonomyFamilyIds: taxonomyPressure.topTaxonomyFamilyIds,
      topTaxonomyGroupIds: taxonomyPressure.topTaxonomyGroupIds,
      pressureAreas: taxonomyPressure.pressureAreas,
      taxonomyOverview: taxonomyPressure.taxonomyOverview,
      frontierSummary,
      rankingMethod: "durable-signal-frontier-v1"
    },
    summary: {
      recommendationCount: rankedRecommendations.length,
      criticalCount,
      clusterCount,
      frontierScore,
      categories: categoryCounts,
      signalTypes,
      topClusterIds,
      topRecommendationIds,
      topTaxonomyFamilyIds: taxonomyPressure.topTaxonomyFamilyIds,
      topTaxonomyGroupIds: taxonomyPressure.topTaxonomyGroupIds,
      pressureAreas: taxonomyPressure.pressureAreas,
      taxonomyOverview: taxonomyPressure.taxonomyOverview,
      clusterMembership,
      topClusters
    },
    updatedAt: generatedAt
  };
  const longHorizonMemory = buildLongHorizonMemory(existingLongHorizonMemory, rankedRecommendations, clusters, metaRecommendations.frontier, generatedAt);
  const governanceCoverage = buildGovernanceCoverage(generatedAt);
  const governanceCoverageReport = buildGovernanceCoverageReport(governanceCoverage, generatedAt);
  const metaOptimizeMirror = buildMetaOptimizeMirror(metaRecommendations, longHorizonMemory, createMetaRemediationPacksIndex(), createMetaOperatorPlaybooksIndex(), createMetaExecutionBridgeCandidatesIndex(), createMetaOperatorFollowThroughIndex(), governanceCoverage);
  const remediationPacks = buildRemediationPacks({
    clusters,
    recommendations: rankedRecommendations,
    longHorizonMemory,
    reviewConcerns,
    figureQa,
    workspaceIndex: {
      ...workspaceIndex,
      metaOptimize: metaOptimizeMirror
    }
  });
  const operatorPlaybooks = buildFamilyOperatorPlaybooks({
    workspaceIndex: {
      ...workspaceIndex,
      metaOptimize: {
        ...metaOptimizeMirror,
        remediationPacks: remediationPacks.summary
      }
    },
    remediationPacks,
    longHorizonMemory
  });
  const existingExecutionBridgeCandidates = normalizeMetaExecutionBridgeCandidatesIndex(readJson(root, ARTIFACT_PATHS.metaExecutionBridgeCandidates, createMetaExecutionBridgeCandidatesIndex));
  const executionBridgeCandidates = buildExecutionBridgeCandidates({ remediationPacks, operatorPlaybooks, existingExecutionBridgeCandidates });
  const sourceCatalog = buildFollowThroughSourceCatalog(remediationPacks, operatorPlaybooks, executionBridgeCandidates);
  const existingFollowThrough = normalizeMetaOperatorFollowThroughIndex(readJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex));
  const operatorFollowThrough = buildOperatorFollowThrough(root, existingFollowThrough, sourceCatalog, generatedAt);
  const operatorLessons = normalizeMetaOperatorLessonsIndex(readJson(root, ARTIFACT_PATHS.metaOperatorLessons, createMetaOperatorLessonsIndex));
  const finalMetaOptimizeMirror = buildMetaOptimizeMirror(metaRecommendations, longHorizonMemory, remediationPacks, operatorPlaybooks, executionBridgeCandidates, operatorFollowThrough, governanceCoverage, workspaceIndex.autonomyLoops, operatorLessons);
  const metaOptimizerState = {
    ...createMetaOptimizerState(),
    frontier: {
      recommendationCount: finalMetaOptimizeMirror.recommendationCount,
      criticalCount: finalMetaOptimizeMirror.criticalCount,
      clusterCount: finalMetaOptimizeMirror.clusterCount,
      frontierScore: finalMetaOptimizeMirror.frontierScore,
      activeSignalTypes: finalMetaOptimizeMirror.activeSignalTypes,
      topClusterIds: finalMetaOptimizeMirror.topClusterIds,
      topRecommendationIds: finalMetaOptimizeMirror.topRecommendationIds,
      topClusters: finalMetaOptimizeMirror.topClusters,
      frontierSummary: finalMetaOptimizeMirror.frontierSummary,
      rankingMethod: finalMetaOptimizeMirror.rankingMethod,
      tieBreakOrder: finalMetaOptimizeMirror.tieBreakOrder,
      reportPath: finalMetaOptimizeMirror.reportPath,
      recommendationsPath: finalMetaOptimizeMirror.recommendationsPath,
      statePath: finalMetaOptimizeMirror.statePath,
      longHorizonPath: finalMetaOptimizeMirror.longHorizonPath,
      remediationPacksPath: ARTIFACT_PATHS.metaRemediationPacks,
      topTaxonomyFamilyIds: finalMetaOptimizeMirror.topTaxonomyFamilyIds,
      topTaxonomyGroupIds: finalMetaOptimizeMirror.topTaxonomyGroupIds,
      pressureAreas: finalMetaOptimizeMirror.pressureAreas,
      taxonomyOverview: finalMetaOptimizeMirror.taxonomyOverview
    },
    clusters: finalMetaOptimizeMirror.topClusters,
    executionBridgeCandidates: executionBridgeCandidates.summary,
    followThrough: operatorFollowThrough.summary,
    operatorLessons: operatorLessons.summary,
    governanceCoverage: governanceCoverage.summary,
    operatorPlaybooks: operatorPlaybooks.summary,
    remediationPacks: remediationPacks.summary,
    longHorizon: finalMetaOptimizeMirror.longHorizon,
    lastRefreshedAt: generatedAt,
    updatedAt: generatedAt
  };
  const reportLines = [
    "# Latest optimizer report",
    "",
    "- Proposal only: true",
    `- Generated: ${generatedAt}`,
    ...renderMetaOptimizeOverviewLines(finalMetaOptimizeMirror),
    `- Active signal types: ${signalTypes.join(", ") || "none"}`,
    `- Ranking method: ${finalMetaOptimizeMirror.rankingMethod}`,
    `- Stable tie-break order: ${finalMetaOptimizeMirror.tieBreakOrder.join(", ")}`,
    `- Long-horizon history policy: ${longHorizonMemory.historyPolicy.mode} (${longHorizonMemory.historyPolicy.lastAction}: ${longHorizonMemory.historyPolicy.reason})`,
    `- Board phase: ${board.currentPhase}`,
    `- Board role: ${board.assignedRole}`,
    "",
    "## Optimization frontier",
    "",
    ...clusters.length > 0 ? clusters.flatMap((cluster) => [
      `### ${cluster.rank}. ${cluster.label} [${cluster.priority}]`,
      `- Cluster id: ${cluster.id}`,
      `- Summary: ${cluster.summary}`,
      `- Operator goal: ${cluster.operatorGoal}`,
      `- Recommendation count: ${cluster.recommendationCount}`,
      `- Cluster score: ${cluster.score}`,
      `- Cluster sort key: ${cluster.sortKey}`,
      `- Top recommendation: ${cluster.topRecommendationId}`,
      `- Response owners: ${cluster.responseOwnerRoles.join(", ") || "none"}`,
      `- Signal types: ${cluster.signalTypes.join(", ") || "none"}`,
      `- Taxonomy pressure: ${cluster.taxonomyPressure?.overview ?? "No typed wiki taxonomy pressure is active in this cluster."}`,
      `- Evidence artifacts: ${cluster.evidenceArtifactPaths.join(", ") || "none"}`,
      `- Membership: ${cluster.recommendationIds.join(", ") || "none"}`,
      ""
    ]) : ["- No clusters generated from the current durable signals."],
    "## Evidence-backed recommendations",
    "",
    ...clusters.length > 0 ? clusters.flatMap((cluster) => [
      `### Cluster ${cluster.rank}: ${cluster.label}`,
      ...rankedRecommendations.filter((item) => item.clusterId === cluster.id).flatMap((item) => [
        `#### ${item.rank}. ${item.id} [${item.priority}]`,
        `- Cluster: ${item.clusterLabel}`,
        `- Category: ${item.category}`,
        `- Scope: ${item.scope}`,
        `- Summary: ${item.summary}`,
        `- Why: ${item.rationale}`,
        `- Next action: ${item.nextAction}`,
        `- Score: ${item.score}`,
        `- Ranking basis: ${item.rankingBasis.join(", ")}`,
        `- Signal strength: recurrence=${item.signalStrength.recurrenceCount} evidence=${item.signalStrength.evidenceDensity} cross-session=${item.signalStrength.crossSessionRecurrence} repair=${item.signalStrength.repairFrontierOverlap} audit=${item.signalStrength.auditCriticality} bridge=${item.signalStrength.bridgeCriticality} queue=${item.signalStrength.queueChurn} taxonomy-family=${item.signalStrength.taxonomyFamilyPressure} taxonomy-group=${item.signalStrength.taxonomyGroupPressure}`,
        `- Taxonomy pressure: ${item.taxonomyPressure?.overview ?? "No typed wiki taxonomy pressure is active in this recommendation."}`,
        `- Evidence artifacts: ${item.evidenceArtifactPaths.join(", ") || "none"}`,
        `- Evidence ids: ${item.evidenceIds.join(", ") || "none"}`,
        `- Response owner: ${item.responseOwnerRole ?? "none"}`,
        `- Stable sort key: ${item.sortKey}`,
        `- Stable tie-break key: ${item.tieBreakKey}`,
        ""
      ])
    ]) : ["- No recommendations generated from the current durable signals."],
    "## Remediation packs",
    "",
    ...remediationPacks.packs.length > 0 ? remediationPacks.packs.flatMap((pack) => [
      `### ${pack.rank}. ${pack.title} [${pack.priority}]`,
      `- Pack id: ${pack.id}`,
      `- Cluster: ${pack.clusterId}`,
      `- Summary: ${pack.summary}`,
      `- Taxonomy anchors: ${pack.taxonomyAnchors.overview}`,
      `- Linked review concerns: ${pack.reviewConcerns.map((item) => item.id).join(", ") || "none"}`,
      `- Linked figure QA: ${pack.figureQa.map((item) => item.id).join(", ") || "none"}`,
      `- Long-horizon memory: ${pack.longHorizonMemory.map((item) => item.id).join(", ") || "none"}`,
      `- Packet pointers: ${pack.packetPointers.map((item) => item.id).join(", ") || "none"}`,
      `- Workspace pointers: ${pack.workspacePointers.join(", ") || "none"}`,
      `- Readiness: ${pack.readiness?.operatorReadiness ?? "advisory-only"} (${pack.readiness?.overview ?? "No readiness diagnostics computed."})`,
      `- Missing ingredients: ${(pack.readiness?.missingIngredients ?? []).join(", ") || "none"}`,
      `- Acceptance criteria: ${pack.acceptanceCriteria.join(" | ") || "none"}`,
      `- Conversion hints: ${pack.conversionHints.map((item) => `${item.targetType}:${item.targetId}`).join(" | ") || "none"}`,
      `- Ranked conversion paths: ${(pack.rankedConversionPaths ?? []).map((item) => `${item.rank}:${item.priority}:${item.targetType}:${item.targetId}`).join(" | ") || "none"}`,
      `- Manual next actions: ${pack.manualNextActions.join(" | ") || "none"}`,
      ""
    ]) : ["- No remediation packs generated from the current durable signals."],
    "## Family-level operator playbooks",
    "",
    ...operatorPlaybooks.playbooks.length > 0 ? operatorPlaybooks.playbooks.flatMap((playbook, index) => [
      `### ${index + 1}. ${playbook.title}`,
      `- Playbook id: ${playbook.id}`,
      `- Taxonomy family: ${playbook.taxonomyFamilyId} (${playbook.taxonomyFamilyLabel})`,
      `- Summary: ${playbook.summary}`,
      `- Operator goal: ${playbook.operatorGoal}`,
      `- Selection basis: ${(playbook.selectionRankingBasis ?? []).join(" | ") || "none"}`,
      `- Readiness: ${playbook.readiness?.operatorReadiness ?? "advisory-only"} (${playbook.readiness?.overview ?? "No readiness diagnostics computed."})`,
      `- Missing ingredients: ${(playbook.readiness?.missingIngredients ?? []).join(", ") || "none"}`,
      `- Artifact update overview: ${playbook.artifactUpdateMap?.overview ?? "No explicit artifact targets derived."}`,
      `- Artifact update order: ${(playbook.artifactUpdateMap?.updateOrder ?? []).join(" -> ") || "none"}`,
      `- Artifact targets: ${(playbook.artifactUpdateMap?.targets ?? []).slice(0, 5).map((item) => `${item.rank}:${item.artifactPath}`).join(" | ") || "none"}`,
      `- Long-horizon summary: ${playbook.longHorizonSummary}`,
      `- Linked remediation packs: ${playbook.remediationPackIds.join(", ") || "none"}`,
      `- Acceptance criteria: ${playbook.acceptanceCriteria.join(" | ") || "none"}`,
      `- Conversion hints: ${playbook.conversionHints.map((item) => `${item.targetType}:${item.targetId}`).join(" | ") || "none"}`,
      `- Ranked conversion paths: ${(playbook.rankedConversionPaths ?? []).map((item) => `${item.rank}:${item.priority}:${item.targetType}:${item.targetId}`).join(" | ") || "none"}`,
      `- Manual next actions: ${playbook.manualNextActions.join(" | ") || "none"}`,
      ""
    ]) : ["- No family-level operator playbooks generated from the current durable signals."],
    "## Operator lessons",
    "",
    ...operatorLessons.lessons.length > 0 ? operatorLessons.lessons.slice(0, 12).flatMap((lesson, index) => [
      `### ${index + 1}. ${lesson.title}`,
      `- Lesson id: ${lesson.id}`,
      `- Domain/stage/role: ${lesson.domain} / ${lesson.stage} / ${lesson.actorRole}`,
      `- Problem: ${lesson.problem}`,
      `- Decisions: ${lesson.decisions.join(" | ") || "none"}`,
      `- Pitfalls: ${lesson.pitfalls.join(" | ") || "none"}`,
      `- Validation: ${lesson.validation.join(" | ") || "none"}`,
      `- Next time: ${lesson.nextTime.join(" | ") || "none"}`,
      `- Tags: ${lesson.tags.join(", ") || "none"}`,
      ""
    ]) : ["- No distilled operator lessons have been recorded yet."],
    "## Execution bridge candidate scaffolds",
    "",
    ...executionBridgeCandidates.candidates.length > 0 ? executionBridgeCandidates.candidates.slice(0, 10).flatMap((candidate, index) => [
      `### ${index + 1}. ${candidate.suggestedTitle}`,
      `- Candidate id: ${candidate.id}`,
      `- Candidate type: ${candidate.candidateType}`,
      `- Target artifact: ${candidate.targetArtifact}`,
      `- Summary: ${candidate.suggestedSummary}`,
      `- Suggested next step: ${candidate.suggestedNextStep}`,
      `- Source remediation packs: ${candidate.sourceRemediationPackIds.join(", ") || "none"}`,
      `- Source playbooks: ${candidate.sourcePlaybookIds.join(", ") || "none"}`,
      `- Linked packet pointers: ${(candidate.context?.linkedPacketPointers ?? []).map((item) => item.id).join(", ") || "none"}`,
      `- Workspace pointers: ${(candidate.context?.linkedWorkspacePointers ?? []).join(", ") || "none"}`,
      `- Evidence summary: artifacts=${(candidate.context?.evidenceSummary?.artifactPaths ?? []).join(", ") || "none"} ids=${(candidate.context?.evidenceSummary?.ids ?? []).join(", ") || "none"}`,
      `- Repair summary: repairs=${(candidate.context?.repairSummary?.repairItemIds ?? []).join(", ") || "none"} reviews=${(candidate.context?.repairSummary?.reviewConcernIds ?? []).join(", ") || "none"} figures=${(candidate.context?.repairSummary?.figureIssueIds ?? []).join(", ") || "none"}`,
      `- Linked repair items: ${candidate.linkedRepairItemIds.join(", ") || "none"}`,
      `- Acceptance criteria: ${candidate.suggestedAcceptanceCriteria.join(" | ") || "none"}`,
      ""
    ]) : ["- No execution bridge candidates generated from the current durable signals."],
    "## Governance coverage matrix",
    "",
    `- Overview: ${governanceCoverage.summary.overview}`,
    `- Guarded mutations: ${governanceCoverage.summary.guardedCount}`,
    `- Exempt mutations: ${governanceCoverage.summary.exemptCount}`,
    ...governanceCoverage.guardedMutations.slice(0, 12).map((item) => `  - guarded ${item.id}: ${item.action} -> ${item.artifactPath}`),
    ...governanceCoverage.exemptMutations.slice(0, 6).map((item) => `  - exempt ${item.id}: ${item.action} -> ${item.artifactPath}`),
    "",
    "## Long-horizon workflow memory",
    "",
    ...longHorizonMemory.families.length > 0 ? longHorizonMemory.families.flatMap((family) => [
      `### ${family.label} [${family.trend.status}]`,
      `- Family id: ${family.id}`,
      `- Summary: ${family.summary}`,
      `- Current recommendation count: ${family.currentCount}`,
      `- Total observations: ${family.totalCount}`,
      `- Active snapshots: ${family.activeSnapshotCount}`,
      `- Trend: recent=${family.trend.recentCount} previous=${family.trend.previousCount}`,
      `- Taxonomy families: ${family.relatedTaxonomyFamilyIds.join(", ") || "none"}`,
      `- Taxonomy groups: ${family.relatedTaxonomyGroupIds.join(", ") || "none"}`,
      `- Pressure areas: ${family.operatorPressureAreas.join(", ") || "none"}`,
      `- Top recommendations: ${family.topRecommendationIds.join(", ") || "none"}`,
      `- Top clusters: ${family.topClusterIds.join(", ") || "none"}`,
      `- Evidence artifacts: ${family.evidenceArtifactPaths.join(", ") || "none"}`,
      ""
    ]) : ["- No long-horizon memory families have been observed yet."],
    "## Signal observations",
    "",
    ...sortedEvents.length > 0 ? sortedEvents.map((item) => `- [${item.severity}] ${item.signalType}: ${item.summary}`) : ["- No signal observations captured."],
    "",
    "## Explicit non-goals",
    "",
    "- This layer does not auto-apply workflow, prompt, code, or config changes.",
    "- This layer only summarizes durable signals and recommends explicit next steps.",
    "- Operators must choose whether to act on any recommendation."
  ];
  return {
    metaEvents,
    executionBridgeCandidates,
    governanceCoverage,
    governanceCoverageReport,
    operatorFollowThrough,
    operatorLessons,
    operatorPlaybooks,
    remediationPacks,
    longHorizonMemory,
    metaRecommendations,
    metaOptimizerState,
    metaOptimizerReport: reportLines.join("\n"),
    metaGovernanceCoverageReport: {
      ...governanceCoverageReport,
      markdown: [
        "# Governance coverage report",
        "",
        `- Generated: ${generatedAt}`,
        `- Status: ${governanceCoverageReport.status}`,
        `- Guarded mutations: ${governanceCoverageReport.summary.guardedCount}`,
        `- Exempt mutations: ${governanceCoverageReport.summary.exemptCount}`,
        `- Exempt review freshness: stale-review=${governanceCoverageReport.summary.staleReviewCount}, expiring-soon=${governanceCoverageReport.summary.expiringSoonCount}`,
        `- Overview: ${governanceCoverageReport.summary.overview}`,
        "",
        "## Guarded surfaces",
        "",
        ...governanceCoverage.guardedMutations.map((item) => `- ${item.id}: ${item.surfaceBindings.coreFunction} / ${item.surfaceBindings.mcpTool} / ${item.surfaceBindings.commandIds.join(", ")}`),
        "",
        "## Exempt surfaces",
        "",
        ...governanceCoverage.exemptMutations.map((item) => `- ${item.id}: ${item.surfaceBindings.coreFunction} / ${item.surfaceBindings.mcpTool} / ${item.surfaceBindings.commandIds.join(", ")} | owner=${item.ownerRole} | approvedBy=${item.approvedByRole} | cadence=${item.reviewCadence} | sunset=${item.sunsetAt}`),
        "",
        "## Exempt review metadata",
        "",
        ...governanceCoverage.exemptMutations.map((item) => `- ${item.id}: approvedAt=${item.approvedAt} | lastReviewedAt=${item.lastReviewedAt} | reviewCadence=${item.reviewCadence}`),
        "",
        "## Uncovered bindings",
        "",
        `- Tools: ${(governanceCoverageReport.surfaceBindingAudit.uncoveredTools ?? []).join(", ") || "none"}`,
        `- Commands: ${(governanceCoverageReport.surfaceBindingAudit.uncoveredCommands ?? []).join(", ") || "none"}`,
        `- Core functions: ${(governanceCoverageReport.surfaceBindingAudit.uncoveredCoreFunctions ?? []).join(", ") || "none"}`,
        `- Negative coverage gaps: ${(governanceCoverageReport.surfaceBindingAudit.uncoveredNegativeCoverage ?? []).join(", ") || "none"}`
      ].join("\n")
    }
  };
}
function buildWorkspaceIndex(state, board, packets, reviewState, journal, versionsIndex, comparisons, wikiRelations, figureQa, metaOptimize = null, runtime = null, programs = null, campaigns = null, openQuestions = []) {
  const base = createWorkspaceIndex();
  const packetById = new Map(packets.map((packet) => [packet.id, packet]));
  const enrichedPackets = sortPacketsForQueue(packets.map((packet) => ({
    ...packet,
    dependencyHealth: buildPacketDependencyHealth(packet, packetById)
  })));
  const lifecycleCounts = enrichedPackets.reduce((accumulator, packet) => {
    accumulator[packet.lifecycleStatus] = (accumulator[packet.lifecycleStatus] ?? 0) + 1;
    return accumulator;
  }, {});
  const readyPackets = enrichedPackets.filter((packet) => packet.active && ["queued", "active"].includes(packet.lifecycleStatus));
  const waitingPackets = enrichedPackets.filter((packet) => packet.lifecycleStatus === "waiting" || packet.lifecycleStatus === "blocked");
  const reviewNeededPackets = enrichedPackets.filter((packet) => packet.lifecycleStatus === "review-needed");
  const handoffPackets = enrichedPackets.filter((packet) => packet.lifecycleStatus === "ready-for-handoff");
  const stalePackets = enrichedPackets.filter((packet) => packet.lifecycleStatus === "stale");
  const archivedPackets = enrichedPackets.filter((packet) => GOVERNANCE_TERMINAL_LIFECYCLES.has(packet.lifecycleStatus));
  const ownershipSummary = buildOwnershipSummary(board, enrichedPackets);
  const dependencyHealth = {
    blockedPacketIds: uniqueSorted(enrichedPackets.filter((packet) => packet.dependencyHealth.state === "blocked-by-dependencies").map((packet) => packet.id)),
    healthyPacketIds: uniqueSorted(enrichedPackets.filter((packet) => packet.dependencyHealth.state === "clear").map((packet) => packet.id)),
    waitingPacketIds: uniqueSorted(waitingPackets.map((packet) => packet.id)),
    stalePacketIds: uniqueSorted(stalePackets.map((packet) => packet.id)),
    missingDependencyIds: uniqueSorted(enrichedPackets.flatMap((packet) => packet.dependencyHealth.missingDependencyIds)),
    orphanPacketIds: uniqueSorted(enrichedPackets.filter((packet) => packet.parentPacketId && !packetById.has(packet.parentPacketId)).map((packet) => packet.id))
  };
  const handoffObligations = handoffPackets.map((packet) => ({
    packetId: packet.id,
    fromRole: board.assignedRole,
    toRole: packet.assignedRole,
    summary: `${packet.id} is active under ${packet.assignedRole} while the board owner is ${board.assignedRole}.`,
    nextAction: packet.nextAction,
    packetContextPath: packet.packetContextPath
  }));
  const lifecycle = buildLifecycleWorkspaceSummary(board, enrichedPackets);
  const latestComparison = (comparisons.items ?? []).at(-1) ?? null;
  const repairFrontier = buildRepairFrontier({
    wikiRelations,
    figureQa,
    stalePackets,
    handoffObligations,
    latestComparison,
    board
  });
  const prioritizedPackets = [
    ...stalePackets,
    ...reviewNeededPackets,
    ...handoffPackets,
    ...waitingPackets,
    ...readyPackets
  ];
  const prioritizedArtifactContextPaths = uniqueSorted([
    artifactContextPath(ARTIFACT_PATHS.orchestrationBoard),
    artifactContextPath(ARTIFACT_PATHS.workspaceIndex),
    artifactContextPath(ARTIFACT_PATHS.navigationReport),
    ...repairFrontier.prioritizedItems.flatMap((item) => [item.artifactPath, ...item.relatedArtifactPaths ?? []].filter(Boolean).map(artifactContextPath)),
    ...prioritizedPackets.flatMap((packet) => [packet.packetPath, ...packet.outputPaths ?? [], ...packet.evidenceLinks ?? []].filter(Boolean).map(artifactContextPath))
  ]);
  const autonomyLoops = buildAutonomyLoopSummary({
    board,
    packets: enrichedPackets,
    openQuestions,
    metaOptimize: metaOptimize ?? base.metaOptimize,
    runtime: runtime ?? base.runtime,
    programs: programs ?? base.programs,
    repairFrontier
  });
  const dove = buildDoveWorkspaceSummary(board, enrichedPackets, lifecycle);
  return {
    ...base,
    currentFocus: board.currentFocus,
    nextAction: board.nextAction,
    boardPhase: board.currentPhase,
    boardAssignedRole: board.assignedRole,
    boardIntentType: board.intentType,
    continuationState: board.continuationState,
    activePackets: enrichedPackets.filter((packet) => packet.active).map(summarizePacket),
    workQueues: {
      ready: readyPackets.map(summarizePacket),
      waiting: waitingPackets.map(summarizePacket),
      reviewNeeded: reviewNeededPackets.map(summarizePacket),
      handoff: handoffPackets.map(summarizePacket),
      stale: stalePackets.map(summarizePacket),
      archived: archivedPackets.map(summarizePacket)
    },
    ownershipSummary,
    packetLifecycleCounts: lifecycleCounts,
    handoffObligations,
    resumeGuidance: {
      command: state.pipeline.resumeCommand ?? resolveResumeCommandForPhase(board.currentPhase),
      summary: repairFrontier.prioritizedItems[0]?.nextAction ?? prioritizedPackets[0]?.nextAction ?? board.nextAction,
      prioritizedPacketIds: prioritizedPackets.map((packet) => packet.id),
      packetContextPaths: prioritizedPackets.slice(0, 8).map((packet) => packet.packetContextPath),
      handoffCandidateIds: handoffPackets.map((packet) => packet.id)
    },
    contextSurfaces: {
      currentRoleContextPath: path8.join(ARTIFACT_PATHS.roleContextsDir, `${board.assignedRole}.json`),
      currentPhaseContextPath: path8.join(ARTIFACT_PATHS.phaseContextsDir, `${board.currentPhase}.json`),
      currentActionContextPath: actionContextPath("current"),
      prioritizedArtifactContextPaths,
      prioritizedPacketActionContextPaths: prioritizedPackets.slice(0, 8).map((packet) => actionContextPath(`packet-${packet.id}`))
    },
    behaviorDiscipline: {
      summary: "Read the current action bundle, then the role/phase/packet/artifact context surfaces before mutating durable state.",
      explicitOnly: true,
      noHiddenRuntime: true,
      requiredReadOrder: uniqueSorted([
        actionContextPath("current"),
        path8.join(ARTIFACT_PATHS.roleContextsDir, `${board.assignedRole}.json`),
        path8.join(ARTIFACT_PATHS.phaseContextsDir, `${board.currentPhase}.json`),
        ...prioritizedPackets.slice(0, 3).map((packet) => packet.packetContextPath),
        ...prioritizedArtifactContextPaths.slice(0, 6)
      ])
    },
    dependencyHealth,
    repairFrontier,
    metaOptimize: metaOptimize ?? base.metaOptimize,
    runtime: runtime ?? base.runtime,
    programs: programs ?? base.programs,
    campaigns: campaigns ?? base.campaigns,
    autonomyLoops,
    lifecycle,
    dove,
    activeRoles: Array.from(/* @__PURE__ */ new Set([board.assignedRole, ...enrichedPackets.filter((packet) => packet.active).map((packet) => packet.assignedRole)])),
    unresolvedConcernIds: reviewState.unresolvedConcernIds ?? [],
    mostRecentSessions: [...journal.entries ?? []].slice(-10).reverse().map((entry) => ({
      id: entry.id,
      type: entry.type,
      phase: entry.phase,
      summary: entry.summary,
      timestamp: entry.timestamp
    })),
    latestVersions: {
      currentVersionId: versionsIndex.currentVersionId,
      activeTargets: comparisons.activeTargets ?? [],
      latestSnapshotIds: (versionsIndex.items ?? []).slice(-5).map((item) => item.id)
    },
    updatedAt: nowIso()
  };
}
function refreshDurableSurfaces(root, event = {}) {
  assertGovernanceMutationRegistered("refresh-durable-surfaces", "exempt");
  ensureWorkspace(root);
  const state = loadState(root);
  const board = readJson(root, ARTIFACT_PATHS.orchestrationBoard, () => createDefaultBoard(state));
  const notesIndex = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, { version: 2, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  const reviewConcerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const adversarialState = readJson(root, ARTIFACT_PATHS.adversarialReviewState, { version: 2, unresolvedConcernIds: [], escalatedConcernIds: [], pendingAuthorResponseIds: [], pendingReviewerRulingIds: [], concernStatusCounts: {}, updatedAt: null });
  const plansIndex = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const experimentAudits = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
  const issuesIndex = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const versionsIndex = readJson(root, ARTIFACT_PATHS.versionsIndex, createVersionsIndex);
  const comparisons = readJson(root, ARTIFACT_PATHS.versionComparisons, createVersionComparisonsIndex);
  const existingLongHorizonMemory = normalizeMetaLongHorizonMemory(readJson(root, ARTIFACT_PATHS.metaLongHorizonMemory, createMetaLongHorizonMemory));
  const taskPacketIndex = readJson(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex);
  const wikiEntities = readJson(root, ARTIFACT_PATHS.wikiEntities, createWikiEntitiesIndex);
  const wikiRelations = readJson(root, ARTIFACT_PATHS.wikiRelations, createWikiRelationsIndex);
  const figureQa = readJson(root, ARTIFACT_PATHS.figureQa, { version: 1, items: [], issues: [], updatedAt: null });
  const campaignsIndex = normalizeCampaignsIndex(readJson(root, ARTIFACT_PATHS.campaignsIndex, createCampaignsIndex));
  const operatingState = readProgramOperatingState(root);
  const programsIndex = operatingState.programs;
  const programRuns = operatingState.programRuns;
  const programApprovals = operatingState.programApprovals;
  const runtimeControllerState = normalizeRuntimeControllerState(readJson(root, ARTIFACT_PATHS.runtimeControllerState, createRuntimeControllerState));
  const runtimeLeases = normalizeRuntimeLeasesIndex(readJson(root, ARTIFACT_PATHS.runtimeLeases, createRuntimeLeasesIndex));
  const runtimeEvents = normalizeRuntimeEventsIndex(readJson(root, ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex));
  const runtimeResults = normalizeRuntimeResultsIndex(readJson(root, ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex));
  const runtimeContinuation = normalizeRuntimeContinuationIndex(readJson(root, ARTIFACT_PATHS.runtimeContinuation, createRuntimeContinuationIndex));
  const runtimeMirror = buildRuntimeWorkspaceMirror(runtimeControllerState, runtimeContinuation, runtimeLeases, runtimeEvents, runtimeResults);
  const programsMirror = buildProgramsWorkspaceMirror(programsIndex, programRuns, programApprovals, runtimeControllerState.summary ?? {});
  const campaignsMirror = buildCampaignsWorkspaceMirror(campaignsIndex, programsIndex, programRuns);
  const existingById = loadExistingPacketMap(root, taskPacketIndex.items ?? []);
  const refreshed = [
    ...deriveBoardPackets(board, existingById),
    ...deriveExperimentPackets(plansIndex),
    ...deriveIssuePackets(issuesIndex),
    ...deriveVersionPackets(versionsIndex)
  ].map((packet) => ({
    ...existingById.get(packet.id) ?? {},
    ...packet,
    questions: packet.questions,
    decisions: packet.decisions,
    lineage: packet.lineage,
    continuationState: packet.continuationState,
    updatedAt: packet.updatedAt
  }));
  const activeIds = new Set(refreshed.map((packet) => packet.id));
  const preserved = (taskPacketIndex.items ?? []).filter((packet) => !activeIds.has(packet.id) && (isFirstClassDoveTaskPacket(packet) || !["task", "blocker", "experiment", "rebuttal-issue", "version"].includes(packet.sourceType)));
  const packets = markInactiveLegacyPackets([...refreshed, ...preserved].map(normalizePacket), activeIds).sort((left, right) => left.id.localeCompare(right.id));
  const packetById = new Map(packets.map((packet) => [packet.id, packet]));
  const packetsWithHealth = packets.map((packet) => ({ ...packet, dependencyHealth: buildPacketDependencyHealth(packet, packetById) }));
  const lifecycleCounts = packetsWithHealth.reduce((accumulator, packet) => {
    accumulator[packet.lifecycleStatus] = (accumulator[packet.lifecycleStatus] ?? 0) + 1;
    return accumulator;
  }, {});
  const lifecycleFamilyCounts = packetsWithHealth.reduce((accumulator, packet) => {
    const familyId = packet.lifecycleFamily ?? lifecycleFamilyForPacket(packet);
    accumulator[familyId] = (accumulator[familyId] ?? 0) + 1;
    return accumulator;
  }, {});
  const packetIndex = {
    version: 3,
    items: packetsWithHealth,
    lifecycleCounts,
    lifecycleFamilyCounts,
    dependencyHealth: {
      blockedPacketIds: uniqueSorted(packetsWithHealth.filter((packet) => packet.dependencyHealth.state === "blocked-by-dependencies").map((packet) => packet.id)),
      readyPacketIds: uniqueSorted(packetsWithHealth.filter((packet) => packet.active && GOVERNANCE_ACTIVE_LIFECYCLES.has(packet.lifecycleStatus)).map((packet) => packet.id)),
      stalePacketIds: uniqueSorted(packetsWithHealth.filter((packet) => packet.lifecycleStatus === "stale").map((packet) => packet.id)),
      missingDependencyIds: uniqueSorted(packetsWithHealth.flatMap((packet) => packet.dependencyHealth.missingDependencyIds))
    },
    updatedAt: nowIso()
  };
  writeJson(root, ARTIFACT_PATHS.taskPacketsIndex, packetIndex);
  for (const packet of packetsWithHealth) {
    writeJson(root, packetFilePath2(packet.id), packet);
  }
  const openQuestions = buildOpenQuestions(notesIndex, packetsWithHealth, reviewState, wikiEntities).sort((left, right) => left.id.localeCompare(right.id));
  const decisions = buildDecisions(board, versionsIndex, comparisons, packetsWithHealth, wikiEntities);
  const taskGraph = buildTaskGraph(packetsWithHealth);
  const primaryRoleRoster = Array.isArray(board.roleRoster) ? board.roleRoster : [];
  const manifestRoster = [
    ...primaryRoleRoster,
    ...ROLE_IDS.filter((roleId) => !primaryRoleRoster.some((role) => role.id === roleId)).map((roleId) => ROLE_HIERARCHY[roleId]).filter(Boolean)
  ];
  const journal = readJson(root, ARTIFACT_PATHS.sessionJournal, createSessionJournal);
  const entry = {
    id: `${nowIso()}-${slugify2(event.type ?? event.summary ?? "workspace-refresh")}`,
    timestamp: nowIso(),
    type: event.type ?? "workspace-refresh",
    summary: event.summary ?? `Refreshed durable surfaces during ${board.currentPhase}.`,
    phase: board.currentPhase,
    assignedRole: board.assignedRole,
    taskPacketIds: packetsWithHealth.filter((packet) => packet.active).map((packet) => packet.id),
    artifactPaths: normalizeStringArray5(event.artifactPaths ?? [ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.workspaceIndex])
  };
  const entries = [...(journal.entries ?? []).slice(-199), entry];
  writeJson(root, ARTIFACT_PATHS.sessionJournal, { version: 1, entries, updatedAt: nowIso() });
  const preliminaryWorkspaceIndex = buildWorkspaceIndex(state, board, packetsWithHealth, reviewState, { entries }, versionsIndex, comparisons, wikiRelations, figureQa, null, runtimeMirror, programsMirror, campaignsMirror, openQuestions);
  const metaOptimize = buildMetaOptimizeSurface({
    root,
    board,
    workspaceIndex: preliminaryWorkspaceIndex,
    journal: { entries },
    reviewConcerns,
    reviewState,
    adversarialState,
    experimentAudits,
    bridgeLog,
    figureQa,
    comparisons,
    existingLongHorizonMemory
  });
  const workspaceIndex = buildWorkspaceIndex(
    state,
    board,
    packetsWithHealth,
    reviewState,
    { entries },
    versionsIndex,
    comparisons,
    wikiRelations,
    figureQa,
    buildMetaOptimizeMirror(metaOptimize.metaRecommendations, metaOptimize.longHorizonMemory, metaOptimize.remediationPacks, metaOptimize.operatorPlaybooks, metaOptimize.executionBridgeCandidates, metaOptimize.operatorFollowThrough, metaOptimize.governanceCoverage, null, metaOptimize.operatorLessons),
    runtimeMirror,
    programsMirror,
    campaignsMirror,
    openQuestions
  );
  const taskGraphWithLoops = buildTaskGraph(packetsWithHealth, workspaceIndex.autonomyLoops);
  writeJson(root, ARTIFACT_PATHS.workspaceIndex, workspaceIndex);
  writeJson(root, ARTIFACT_PATHS.metaEvents, metaOptimize.metaEvents);
  writeJson(root, ARTIFACT_PATHS.metaExecutionBridgeCandidates, metaOptimize.executionBridgeCandidates);
  writeJson(root, ARTIFACT_PATHS.metaGovernanceCoverage, metaOptimize.governanceCoverage);
  writeJson(root, ARTIFACT_PATHS.metaGovernanceCoverageReport, metaOptimize.metaGovernanceCoverageReport);
  writeJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, metaOptimize.operatorFollowThrough);
  writeJson(root, ARTIFACT_PATHS.metaOperatorLessons, metaOptimize.operatorLessons);
  writeJson(root, ARTIFACT_PATHS.metaLongHorizonMemory, metaOptimize.longHorizonMemory);
  writeJson(root, ARTIFACT_PATHS.metaOperatorPlaybooks, metaOptimize.operatorPlaybooks);
  writeJson(root, ARTIFACT_PATHS.metaRemediationPacks, metaOptimize.remediationPacks);
  writeJson(root, ARTIFACT_PATHS.metaRecommendations, metaOptimize.metaRecommendations);
  writeJson(root, ARTIFACT_PATHS.campaignsIndex, campaignsIndex);
  writeJson(root, ARTIFACT_PATHS.metaOptimizerState, metaOptimize.metaOptimizerState);
  writeText(root, ARTIFACT_PATHS.metaOptimizerReport, metaOptimize.metaOptimizerReport);
  writeText(root, ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown, metaOptimize.metaGovernanceCoverageReport.markdown);
  const packetByIdForManifest = new Map(packetsWithHealth.map((packet) => [packet.id, packet]));
  const artifactPaths = uniqueSorted([
    ARTIFACT_PATHS.orchestrationBoard,
    ARTIFACT_PATHS.orchestrationHandoffs,
    ARTIFACT_PATHS.taskPacketsIndex,
    ARTIFACT_PATHS.workspaceIndex,
    ARTIFACT_PATHS.metaEvents,
    ARTIFACT_PATHS.metaExecutionBridgeCandidates,
    ARTIFACT_PATHS.metaGovernanceCoverage,
    ARTIFACT_PATHS.metaGovernanceCoverageReport,
    ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown,
    ARTIFACT_PATHS.metaOperatorFollowThrough,
    ARTIFACT_PATHS.metaOperatorLessons,
    ARTIFACT_PATHS.metaLongHorizonMemory,
    ARTIFACT_PATHS.metaOperatorPlaybooks,
    ARTIFACT_PATHS.metaRemediationPacks,
    ARTIFACT_PATHS.metaRecommendations,
    ARTIFACT_PATHS.metaOptimizerState,
    ARTIFACT_PATHS.metaOptimizerReport,
    ARTIFACT_PATHS.runtimeControllerState,
    ARTIFACT_PATHS.runtimeLeases,
    ARTIFACT_PATHS.runtimeEvents,
    ARTIFACT_PATHS.runtimeResults,
    ARTIFACT_PATHS.programsIndex,
    ARTIFACT_PATHS.programRuns,
    ARTIFACT_PATHS.programApprovals,
    ARTIFACT_PATHS.campaignsIndex,
    ARTIFACT_PATHS.sessionSummary,
    ARTIFACT_PATHS.navigationReport,
    ...packetsWithHealth.flatMap((packet) => [packet.packetPath, ...packet.outputPaths ?? [], ...packet.evidenceLinks ?? []].filter(Boolean))
  ]);
  for (const relativePath of artifactPaths) {
    writeJson(root, artifactContextPath(relativePath), buildArtifactContextManifest(root, relativePath, board, packetsWithHealth, workspaceIndex));
  }
  for (const packet of packetsWithHealth) {
    const manifest = buildPacketContextManifest(root, board, packet, packetByIdForManifest, workspaceIndex);
    writeJson(root, packet.packetContextPath, manifest);
    writeJson(root, actionContextPath(`packet-${packet.id}`), buildActionContextBundle({
      scopeType: "packet",
      scopeId: packet.id,
      summary: `Packet-scoped pre-action bundle for ${packet.id}.`,
      board,
      workspaceIndex,
      packet,
      roleId: packet.assignedRole,
      phaseId: packet.phase,
      requiredReadPaths: [
        packet.packetContextPath,
        path8.join(ARTIFACT_PATHS.roleContextsDir, `${packet.assignedRole}.json`),
        path8.join(ARTIFACT_PATHS.phaseContextsDir, `${packet.phase}.json`),
        ...manifest.programLinkage ? [ARTIFACT_PATHS.programsIndex, ARTIFACT_PATHS.programRuns, ARTIFACT_PATHS.programApprovals] : [],
        ...manifest.autonomyEnvelope?.requiredReadPaths ?? [],
        ...manifest.artifactContextPaths
      ],
      localRules: uniqueSorted([...manifest.behaviorDiscipline.localRules ?? [], ...manifest.autonomyEnvelope?.localRules ?? []]),
      operatorGuidance: buildOperatorGuidance(workspaceIndex, metaOptimize.remediationPacks, metaOptimize.operatorPlaybooks, metaOptimize.executionBridgeCandidates, metaOptimize.operatorFollowThrough, metaOptimize.operatorLessons, { roleId: packet.assignedRole, packetId: packet.id, packet })
    }));
  }
  for (const role of manifestRoster) {
    const manifest = buildRoleManifest(role, packetsWithHealth, openQuestions, decisions, workspaceIndex, metaOptimize.remediationPacks, metaOptimize.operatorPlaybooks, metaOptimize.executionBridgeCandidates, metaOptimize.operatorFollowThrough, metaOptimize.operatorLessons);
    writeJson(root, path8.join(ARTIFACT_PATHS.roleContextsDir, `${role.id}.json`), manifest);
    writeJson(root, actionContextPath(`role-${role.id}`), buildActionContextBundle({
      scopeType: "role",
      scopeId: role.id,
      summary: `Role-scoped pre-action bundle for ${role.id}.`,
      board,
      workspaceIndex,
      roleId: role.id,
      phaseId: workspaceIndex.boardPhase,
      requiredReadPaths: manifest.preActionReadPaths,
      localRules: manifest.behaviorDiscipline.localRules,
      operatorGuidance: manifest.operatorGuidance
    }));
  }
  const phaseManifestPath = path8.join(ARTIFACT_PATHS.phaseContextsDir, `${board.currentPhase}.json`);
  const phaseManifest = buildPhaseManifest(board, packetsWithHealth, workspaceIndex, metaOptimize.remediationPacks, metaOptimize.operatorPlaybooks, metaOptimize.executionBridgeCandidates, metaOptimize.operatorFollowThrough, metaOptimize.operatorLessons);
  writeJson(root, phaseManifestPath, phaseManifest);
  writeJson(root, actionContextPath(`phase-${board.currentPhase}`), buildActionContextBundle({
    scopeType: "phase",
    scopeId: board.currentPhase,
    summary: `Phase-scoped pre-action bundle for ${board.currentPhase}.`,
    board,
    workspaceIndex,
    roleId: board.assignedRole,
    phaseId: board.currentPhase,
    requiredReadPaths: phaseManifest.preActionReadPaths,
    localRules: phaseManifest.behaviorDiscipline.localRules,
    operatorGuidance: phaseManifest.operatorGuidance
  }));
  writeJson(root, actionContextPath("current"), buildActionContextBundle({
    scopeType: "current",
    scopeId: "current",
    summary: "Current workspace pre-action bundle. Read this before mutating durable workflow state.",
    board,
    workspaceIndex,
    roleId: board.assignedRole,
    phaseId: board.currentPhase,
    artifactPath: workspaceIndex.contextSurfaces?.prioritizedArtifactContextPaths?.[0] ?? null,
    requiredReadPaths: workspaceIndex.behaviorDiscipline.requiredReadOrder,
    localRules: [
      "Start from the current action bundle, then follow the required read order.",
      "Use packet and artifact-local guidance instead of broad top-level rules when available.",
      "Do not assume hidden rule loading; read the surfaced files explicitly before acting."
    ],
    operatorGuidance: buildOperatorGuidance(workspaceIndex, metaOptimize.remediationPacks, metaOptimize.operatorPlaybooks, metaOptimize.executionBridgeCandidates, metaOptimize.operatorFollowThrough, metaOptimize.operatorLessons, { roleId: board.assignedRole })
  }));
  writeText(root, ARTIFACT_PATHS.sessionSummary, renderSessionSummary(state, board, packetsWithHealth, openQuestions, decisions, primaryRoleRoster, workspaceIndex));
  writeText(root, ARTIFACT_PATHS.navigationReport, renderNavigationReport(board, taskGraphWithLoops, openQuestions, decisions, versionsIndex, comparisons, workspaceIndex));
  return { packetIndex, openQuestions, decisions, taskGraph: taskGraphWithLoops, workspaceIndex, metaOptimize: metaOptimize.metaOptimizerState };
}
function deriveNavigationQuerySnapshot(root) {
  const state = loadState(root);
  const board = readJson(root, ARTIFACT_PATHS.orchestrationBoard, () => createDefaultBoard(state));
  const notesIndex = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, { version: 2, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  const reviewConcerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const adversarialState = readJson(root, ARTIFACT_PATHS.adversarialReviewState, { version: 2, unresolvedConcernIds: [], escalatedConcernIds: [], pendingAuthorResponseIds: [], pendingReviewerRulingIds: [], concernStatusCounts: {}, updatedAt: null });
  const experimentAudits = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
  const existingLongHorizonMemory = normalizeMetaLongHorizonMemory(readJson(root, ARTIFACT_PATHS.metaLongHorizonMemory, createMetaLongHorizonMemory));
  const versionsIndex = readJson(root, ARTIFACT_PATHS.versionsIndex, createVersionsIndex);
  const comparisons = readJson(root, ARTIFACT_PATHS.versionComparisons, createVersionComparisonsIndex);
  const plansIndex = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const issuesIndex = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const taskPacketIndex = readJson(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex);
  const wikiEntities = readJson(root, ARTIFACT_PATHS.wikiEntities, createWikiEntitiesIndex);
  const existingById = loadExistingPacketMap(root, taskPacketIndex.items ?? []);
  const derivedPackets = [
    ...deriveBoardPackets(board, existingById),
    ...deriveExperimentPackets(plansIndex),
    ...deriveIssuePackets(issuesIndex),
    ...deriveVersionPackets(versionsIndex)
  ].map((packet) => ({
    ...existingById.get(packet.id) ?? {},
    ...packet,
    questions: packet.questions,
    decisions: packet.decisions,
    lineage: packet.lineage,
    continuationState: packet.continuationState,
    updatedAt: packet.updatedAt
  }));
  const activeIds = new Set(derivedPackets.map((packet) => packet.id));
  const preserved = (taskPacketIndex.items ?? []).filter((packet) => !activeIds.has(packet.id) && (isFirstClassDoveTaskPacket(packet) || !["task", "blocker", "experiment", "rebuttal-issue", "version"].includes(packet.sourceType)));
  const packets = markInactiveLegacyPackets([...derivedPackets, ...preserved].map(normalizePacket), activeIds).sort((left, right) => left.id.localeCompare(right.id));
  const packetById = new Map(packets.map((packet) => [packet.id, packet]));
  const packetsWithHealth = packets.map((packet) => ({ ...packet, dependencyHealth: buildPacketDependencyHealth(packet, packetById) }));
  const wikiRelations = readJson(root, ARTIFACT_PATHS.wikiRelations, createWikiRelationsIndex);
  const figureQa = readJson(root, ARTIFACT_PATHS.figureQa, { version: 1, items: [], issues: [], updatedAt: null });
  const journal = readJson(root, ARTIFACT_PATHS.sessionJournal, createSessionJournal);
  const campaignsIndex = normalizeCampaignsIndex(readJson(root, ARTIFACT_PATHS.campaignsIndex, createCampaignsIndex));
  const operatingState = readProgramOperatingState(root);
  const runtimeControllerState = normalizeRuntimeControllerState(readJson(root, ARTIFACT_PATHS.runtimeControllerState, createRuntimeControllerState));
  const runtimeLeases = normalizeRuntimeLeasesIndex(readJson(root, ARTIFACT_PATHS.runtimeLeases, createRuntimeLeasesIndex));
  const runtimeEvents = normalizeRuntimeEventsIndex(readJson(root, ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex));
  const runtimeResults = normalizeRuntimeResultsIndex(readJson(root, ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex));
  const runtimeContinuation = normalizeRuntimeContinuationIndex(readJson(root, ARTIFACT_PATHS.runtimeContinuation, createRuntimeContinuationIndex));
  const runtimeMirror = buildRuntimeWorkspaceMirror(runtimeControllerState, runtimeContinuation, runtimeLeases, runtimeEvents, runtimeResults);
  const programsMirror = buildProgramsWorkspaceMirror(operatingState.programs, operatingState.programRuns, operatingState.programApprovals, runtimeControllerState.summary ?? {});
  const campaignsMirror = buildCampaignsWorkspaceMirror(campaignsIndex, operatingState.programs, operatingState.programRuns);
  const openQuestions = buildOpenQuestions(notesIndex, packetsWithHealth, reviewState, wikiEntities).sort((left, right) => left.id.localeCompare(right.id));
  const decisions = buildDecisions(board, versionsIndex, comparisons, packetsWithHealth, wikiEntities);
  const preliminaryWorkspaceIndex = buildWorkspaceIndex(state, board, packetsWithHealth, reviewState, journal, versionsIndex, comparisons, wikiRelations, figureQa, null, runtimeMirror, programsMirror, campaignsMirror, openQuestions);
  const metaOptimize = buildMetaOptimizeSurface({
    root,
    board,
    workspaceIndex: preliminaryWorkspaceIndex,
    journal,
    reviewConcerns,
    reviewState,
    adversarialState,
    experimentAudits,
    bridgeLog,
    figureQa,
    comparisons,
    existingLongHorizonMemory
  });
  const workspaceIndex = buildWorkspaceIndex(
    state,
    board,
    packetsWithHealth,
    reviewState,
    journal,
    versionsIndex,
    comparisons,
    wikiRelations,
    figureQa,
    buildMetaOptimizeMirror(metaOptimize.metaRecommendations, metaOptimize.longHorizonMemory, metaOptimize.remediationPacks, metaOptimize.operatorPlaybooks, metaOptimize.executionBridgeCandidates, metaOptimize.operatorFollowThrough, metaOptimize.governanceCoverage, null, metaOptimize.operatorLessons),
    runtimeMirror,
    programsMirror,
    campaignsMirror,
    openQuestions
  );
  return {
    state,
    board,
    packets: packetsWithHealth,
    openQuestions,
    decisions,
    taskGraph: buildTaskGraph(packetsWithHealth, workspaceIndex.autonomyLoops),
    workspaceIndex,
    metaOptimize,
    versionsIndex,
    comparisons
  };
}
function queryTaskGraph(root) {
  return deriveNavigationQuerySnapshot(root).taskGraph;
}
function queryOpenQuestions(root) {
  const { openQuestions } = deriveNavigationQuerySnapshot(root);
  return { items: openQuestions, count: openQuestions.length, reportPath: ARTIFACT_PATHS.navigationReport };
}
function queryDecisions(root) {
  const { decisions } = deriveNavigationQuerySnapshot(root);
  return { items: decisions, count: decisions.length, reportPath: ARTIFACT_PATHS.navigationReport };
}
function queryLineage(root) {
  const versions = readJson(root, ARTIFACT_PATHS.versionsIndex, createVersionsIndex);
  const comparisons = readJson(root, ARTIFACT_PATHS.versionComparisons, createVersionComparisonsIndex);
  return {
    currentVersionId: versions.currentVersionId,
    lineage: versions.lineage ?? [],
    comparisons: comparisons.items ?? [],
    activeTargets: comparisons.activeTargets ?? [],
    reportPath: ARTIFACT_PATHS.navigationReport
  };
}
function queryWorkspaceIndex(root) {
  return deriveNavigationQuerySnapshot(root).workspaceIndex;
}
function queryMetaOptimize(root) {
  const navigation = deriveNavigationQuerySnapshot(root);
  const derived = navigation.metaOptimize;
  const executionBridgeCandidates = derived.executionBridgeCandidates;
  const governanceCoverage = derived.governanceCoverage;
  const operatorFollowThrough = derived.operatorFollowThrough;
  const operatorLessons = derived.operatorLessons;
  const longHorizonMemory = derived.longHorizonMemory;
  const operatorPlaybooks = derived.operatorPlaybooks;
  const remediationPacks = derived.remediationPacks;
  const recommendations = derived.metaRecommendations;
  const state = derived.metaOptimizerState;
  const workspaceIndex = navigation.workspaceIndex;
  return {
    proposalOnly: true,
    events: derived.metaEvents.items ?? [],
    executionBridgeCandidates,
    governanceCoverage,
    governanceCoverageReport: normalizeMetaGovernanceCoverageReport(derived.metaGovernanceCoverageReport),
    operatorFollowThrough,
    operatorLessons,
    operatorPlaybooks,
    remediationPacks,
    longHorizon: longHorizonMemory,
    recommendations: recommendations.items ?? [],
    clusters: recommendations.clusters ?? [],
    groupedFrontier: {
      ranking: recommendations.ranking ?? state.frontier?.ranking ?? null,
      frontier: recommendations.frontier ?? state.frontier,
      topClusters: recommendations.summary?.topClusters ?? state.frontier?.topClusters ?? [],
      clusterMembership: recommendations.summary?.clusterMembership ?? {}
    },
    frontier: recommendations.frontier ?? state.frontier,
    summary: recommendations.summary ?? state.frontier,
    autonomyLoops: workspaceIndex.autonomyLoops,
    state,
    reportPath: ARTIFACT_PATHS.metaOptimizerReport,
    recommendationsPath: ARTIFACT_PATHS.metaRecommendations,
    eventsPath: ARTIFACT_PATHS.metaEvents,
    executionBridgeCandidatesPath: ARTIFACT_PATHS.metaExecutionBridgeCandidates,
    governanceCoveragePath: ARTIFACT_PATHS.metaGovernanceCoverage,
    governanceCoverageReportPath: ARTIFACT_PATHS.metaGovernanceCoverageReport,
    governanceCoverageReportMarkdownPath: ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown,
    operatorFollowThroughPath: ARTIFACT_PATHS.metaOperatorFollowThrough,
    operatorLessonsPath: ARTIFACT_PATHS.metaOperatorLessons,
    operatorPlaybooksPath: ARTIFACT_PATHS.metaOperatorPlaybooks,
    remediationPacksPath: ARTIFACT_PATHS.metaRemediationPacks,
    longHorizonPath: ARTIFACT_PATHS.metaLongHorizonMemory
  };
}
function queryOperatorLessons(root, args = {}) {
  const index = normalizeMetaOperatorLessonsIndex(readJson(root, ARTIFACT_PATHS.metaOperatorLessons, createMetaOperatorLessonsIndex));
  const domain = normalizeOptionalString(args.domain, null);
  const status = normalizeOptionalString(args.status, null);
  const tag = normalizeOptionalString(args.tag, null);
  const actorRole = normalizeOptionalString(args.actorRole, null);
  const limit = Number.isFinite(args.limit) && args.limit > 0 ? Math.floor(args.limit) : null;
  let lessons = index.lessons ?? [];
  if (domain) {
    lessons = lessons.filter((lesson) => lesson.domain === domain);
  }
  if (status) {
    lessons = lessons.filter((lesson) => lesson.status === status);
  }
  if (tag) {
    lessons = lessons.filter((lesson) => (lesson.tags ?? []).includes(tag));
  }
  if (actorRole) {
    lessons = lessons.filter((lesson) => lesson.actorRole === actorRole);
  }
  lessons = lessons.sort((left, right) => String(right.updatedAt ?? right.createdAt ?? "").localeCompare(String(left.updatedAt ?? left.createdAt ?? "")) || left.id.localeCompare(right.id));
  if (limit) {
    lessons = lessons.slice(0, limit);
  }
  return {
    ...index,
    lessons,
    filters: { domain, status, tag, actorRole, limit },
    resultCount: lessons.length,
    lessonsPath: ARTIFACT_PATHS.metaOperatorLessons
  };
}
function requireLessonArrayField(args, fieldName) {
  const values = normalizeStringArray5(args[fieldName]);
  if (values.length === 0) {
    throw new Error(`recordOperatorLesson requires at least one ${fieldName} entry.`);
  }
  return values;
}
function lessonTaskTargetAliasArgs(args = {}) {
  return {
    packetId: args.packetId,
    taskPacketId: args.taskPacketId,
    missionPacketId: args.missionPacketId,
    taskId: args.taskId,
    target: args.target,
    packetTarget: args.packetTarget,
    taskName: args.taskName
  };
}
function hasLessonTaskTargetAlias(args = {}) {
  return Object.values(lessonTaskTargetAliasArgs(args)).some((value) => typeof value === "string" && value.trim());
}
function resolveOperatorLessonPacketIds(root, args = {}) {
  const packetIds = uniqueSorted([
    ...normalizeStringArray5(args.packetIds),
    ...normalizeStringArray5(args.relatedPacketIds),
    ...normalizeStringArray5(args.taskPacketIds),
    ...normalizeStringArray5(args.missionPacketIds),
    ...normalizeStringArray5(args.taskIds)
  ]);
  if (!hasLessonTaskTargetAlias(args)) {
    return packetIds;
  }
  const resolved = resolveDurableTaskPacket(root, lessonTaskTargetAliasArgs(args), {
    targetFields: ["target", "packetTarget", "taskName"]
  });
  return uniqueSorted([...packetIds, resolved.packetId]);
}
function recordOperatorLesson(root, args = {}) {
  assertGovernanceMutationRegistered("record-operator-lesson", "exempt");
  ensureWorkspace(root);
  const title = normalizeOptionalString(args.title, null);
  const problem = normalizeOptionalString(args.problem, null);
  if (!title) {
    throw new Error("recordOperatorLesson requires title.");
  }
  if (!problem) {
    throw new Error("recordOperatorLesson requires problem.");
  }
  const decisions = requireLessonArrayField(args, "decisions");
  const pitfalls = requireLessonArrayField(args, "pitfalls");
  const validation = requireLessonArrayField(args, "validation");
  const nextTime = requireLessonArrayField(args, "nextTime");
  const sourceArtifacts = uniqueSorted([
    ...normalizeStringArray5(args.sourceArtifacts),
    ...normalizeStringArray5(args.artifactPaths),
    normalizeOptionalString(args.sourceArtifactPath, null)
  ].filter(Boolean));
  const rawTraceArtifacts = sourceArtifacts.filter(isTrellisTaskSourceArtifact2);
  if (rawTraceArtifacts.length > 0) {
    throw new Error(`Operator lessons must be distilled manually and cannot cite raw .trellis/tasks runtime traces as source artifacts: ${rawTraceArtifacts.join(", ")}.`);
  }
  const packetIds = resolveOperatorLessonPacketIds(root, args);
  const timestamp = nowIso();
  const existing = normalizeMetaOperatorLessonsIndex(readJson(root, ARTIFACT_PATHS.metaOperatorLessons, createMetaOperatorLessonsIndex));
  const recordId = normalizeOptionalString(args.id, null) ? slugify2(args.id) : `lesson-${slugify2(title)}-${slugify2(timestamp)}`;
  const previous = existing.lessons.find((lesson) => lesson.id === recordId) ?? null;
  const nextRecord = {
    ...previous ?? {},
    id: recordId,
    title,
    problem,
    decisions,
    pitfalls,
    validation,
    nextTime,
    domain: normalizeDoveDomainId(args.doveDomain ?? args.missionDomain ?? args.domain, "engineering"),
    stage: normalizeDoveMissionLifecycleStage(args.missionStage ?? args.stage, "return"),
    actorRole: ROLE_IDS.includes(args.actorRole) ? args.actorRole : previous?.actorRole ?? "planner",
    tags: uniqueSorted(normalizeStringArray5(args.tags)),
    sourceType: normalizeOptionalString(args.sourceType, previous?.sourceType ?? "manual-retrospective"),
    sourceId: normalizeOptionalString(args.sourceId, previous?.sourceId ?? null),
    sourceArtifacts,
    packetIds,
    recommendationIds: uniqueSorted([...normalizeStringArray5(args.recommendationIds), ...normalizeStringArray5(args.relatedRecommendationIds)]),
    playbookIds: uniqueSorted([...normalizeStringArray5(args.playbookIds), ...normalizeStringArray5(args.relatedPlaybookIds)]),
    remediationPackIds: uniqueSorted([...normalizeStringArray5(args.remediationPackIds), ...normalizeStringArray5(args.relatedRemediationPackIds)]),
    status: normalizeOptionalString(args.status, previous?.status ?? "active"),
    createdAt: normalizeOptionalString(args.createdAt, previous?.createdAt ?? timestamp),
    updatedAt: timestamp
  };
  const lessons = [...existing.lessons.filter((lesson) => lesson.id !== recordId), nextRecord];
  const next = normalizeMetaOperatorLessonsIndex({ ...existing, lessons, updatedAt: timestamp });
  writeJson(root, ARTIFACT_PATHS.metaOperatorLessons, next);
  refreshDurableSurfaces(root, {
    type: "record-operator-lesson",
    summary: `Recorded distilled operator lesson ${recordId}.`,
    artifactPaths: [ARTIFACT_PATHS.metaOperatorLessons, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.metaOptimizerReport]
  });
  return {
    ...next,
    recordedLesson: next.lessons.find((lesson) => lesson.id === recordId) ?? nextRecord,
    lessonsPath: ARTIFACT_PATHS.metaOperatorLessons
  };
}
function queryOperatorFollowThrough(root) {
  assertGovernanceMutationRegistered("record-operator-follow-through", "exempt");
  const meta = queryMetaOptimize(root);
  return {
    ...meta.operatorFollowThrough,
    executionWindowSummary: {
      dueSoonCount: meta.operatorFollowThrough.summary?.dueSoonExecutionCount ?? 0,
      dueSoonIds: meta.operatorFollowThrough.summary?.dueSoonExecutionIds ?? [],
      dueReviewCount: meta.operatorFollowThrough.summary?.dueReviewCount ?? 0,
      dueReviewIds: meta.operatorFollowThrough.summary?.dueReviewIds ?? [],
      overdueCount: meta.operatorFollowThrough.summary?.overdueExecutionCount ?? 0,
      overdueIds: meta.operatorFollowThrough.summary?.overdueExecutionIds ?? [],
      criticalOverdueCount: meta.operatorFollowThrough.summary?.criticalOverdueExecutionCount ?? 0,
      criticalOverdueIds: meta.operatorFollowThrough.summary?.criticalOverdueExecutionIds ?? []
    },
    reportPath: ARTIFACT_PATHS.metaOptimizerReport
  };
}
function queryGovernanceCoverageReport(root) {
  const meta = queryMetaOptimize(root);
  return {
    ...meta.governanceCoverageReport,
    reportPath: ARTIFACT_PATHS.metaGovernanceCoverageReport,
    markdownPath: ARTIFACT_PATHS.metaGovernanceCoverageReportMarkdown
  };
}
var MATERIALIZATION_PACKET_FIELDS = /* @__PURE__ */ new Set([
  "sourceType",
  "sourceId",
  "selectedConversionPathKey",
  "packetId",
  "followThroughId",
  "title",
  "summary",
  "status",
  "currentFocus",
  "nextAction",
  "dependencies",
  "evidenceLinks",
  "outputPaths",
  "decisionSummary",
  "rationale",
  "executeBy",
  "reviewAfter",
  "domain",
  "doveDomain",
  "missionDomain",
  "stage",
  "missionStage",
  "goal",
  "missionGoal",
  "objective",
  "targetArtifacts",
  "artifacts",
  "artifactPaths",
  "acceptanceCriteria",
  "acceptanceChecks",
  "returnProtocol"
]);
function assertPacketOnlyMaterializationArgs(args) {
  if (!args || typeof args !== "object" || Array.isArray(args)) {
    throw new Error("materializeGuidancePacket requires a packet-only argument object.");
  }
  const unknownFields = Object.keys(args).filter(
    (field) => !MATERIALIZATION_PACKET_FIELDS.has(field)
  );
  if (unknownFields.length > 0) {
    throw new Error(
      `materializeGuidancePacket rejects non-packet fields: ${unknownFields.join(", ")}.`
    );
  }
}
function materializeGuidancePacket(root, args = {}) {
  assertGovernanceMutationRegistered("materialize-guidance-packet", "guarded");
  assertNoPolicyOverrideArgs(args, "materialize_guidance_packet");
  assertPacketOnlyMaterializationArgs(args);
  throw new Error(
    "Public materialize_guidance_packet is disabled: packet materialization requires a pre-existing, dynamically valid system-owned program approval, program run, runtime execution claim, and trusted authority provenance. No public caller field grants that authority."
  );
}
var PUBLIC_FOLLOW_THROUGH_STATUSES = /* @__PURE__ */ new Set([
  "acknowledged",
  "deferred"
]);
var PUBLIC_FOLLOW_THROUGH_FIELDS = /* @__PURE__ */ new Set([
  "id",
  "sourceType",
  "sourceId",
  "status",
  "decisionSummary",
  "rationale",
  "selectedConversionPathKey",
  "deferUntil",
  "reviewAfter"
]);
function assertPublicFollowThroughInput(root, args = {}) {
  assertNoPolicyOverrideArgs(args, "record_operator_follow_through");
  const forbidden = Object.keys(args ?? {}).filter(
    (key) => !PUBLIC_FOLLOW_THROUGH_FIELDS.has(key)
  );
  if (forbidden.length > 0) {
    throw new Error(
      `Public follow-through mutations do not accept system-owned or unknown fields: ${forbidden.join(", ")}.`
    );
  }
  if (!PUBLIC_FOLLOW_THROUGH_STATUSES.has(args.status)) {
    throw new Error(
      `Public follow-through status must be one of: ${Array.from(PUBLIC_FOLLOW_THROUGH_STATUSES).join(", ")}.`
    );
  }
  const recordId = args.id ?? `follow-through-${slugify2(`${args.sourceType}-${args.sourceId}`)}`;
  const existing = normalizeMetaOperatorFollowThroughIndex(
    readJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex)
  );
  const previous = (existing.items ?? []).find((item) => item.id === recordId) ?? null;
  if (previous && (previous.programId || previous.programRunId || previous.approvalId || previous.executionStartedAt || previous.executionCompletedAt || Number(previous.retryState?.attemptCount ?? 0) > 0 || previous.retryState?.lastAttemptAt || previous.retryState?.escalatedAt || ["executing", "closed", "superseded"].includes(previous.status))) {
    throw new Error(
      `Public follow-through mutations cannot alter runtime- or program-owned record ${recordId}.`
    );
  }
}
function readFollowThroughSourceCatalog(root) {
  const meta = queryMetaOptimize(root);
  return buildFollowThroughSourceCatalog(meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates);
}
function prepareOperatorFollowThrough(root, args = {}, options = {}) {
  const sourceType = args.sourceType;
  const sourceId = args.sourceId;
  const existing = normalizeMetaOperatorFollowThroughIndex(
    readJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex)
  );
  const recordId = args.id ?? `follow-through-${slugify2(`${sourceType}-${sourceId}`)}`;
  const previousRecord = existing.items.find((item) => item.id === recordId) ?? null;
  const sourceCatalog = options.sourceCatalog ?? readFollowThroughSourceCatalog(root);
  const source = sourceCatalog.get(`${sourceType}:${sourceId}`) ?? null;
  if (!source) {
    throw new Error(`Unknown follow-through source: ${sourceType}:${sourceId}`);
  }
  const targetPreview = options.targetPreview ?? null;
  const status = validateFollowThroughPayload(root, args, targetPreview);
  validateFollowThroughActor(root, args, source);
  const existingTransitions = normalizeMetaOperatorFollowThroughTransitionsIndex(
    readJson(root, ARTIFACT_PATHS.metaOperatorFollowThroughTransitions, createMetaOperatorFollowThroughTransitionsIndex)
  );
  const timestamp = options.timestamp ?? nowIso();
  const items = existing.items.filter((item) => item.id !== recordId);
  const nextRecord = {
    id: recordId,
    sourceType,
    sourceId,
    sourceArtifactPath: source.sourceArtifactPath,
    sourceFingerprint: source.sourceFingerprint,
    sourceTitle: source.title,
    sourceSummary: source.summary,
    status,
    decisionSummary: args.decisionSummary ?? "",
    rationale: args.rationale ?? "",
    selectedConversionPathKey: args.selectedConversionPathKey ?? null,
    linkedTargetArtifact: args.linkedTargetArtifact ?? null,
    linkedTargetId: args.linkedTargetId ?? null,
    programId: args.programId ?? previousRecord?.programId ?? null,
    programRunId: args.programRunId ?? previousRecord?.programRunId ?? null,
    approvalId: args.approvalId ?? previousRecord?.approvalId ?? null,
    plannedTarget: Boolean(args.plannedTarget),
    deferUntil: args.deferUntil ?? null,
    executeBy: args.executeBy ?? null,
    executionStartedAt: args.executionStartedAt ?? previousRecord?.executionStartedAt ?? null,
    executionCompletedAt: args.executionCompletedAt ?? previousRecord?.executionCompletedAt ?? null,
    reviewAfter: args.reviewAfter ?? null,
    closureReason: args.closureReason ?? null,
    closureArtifactPaths: normalizeStringArray5(args.closureArtifactPaths),
    actorRole: args.actorRole,
    workerRole: ROLE_IDS.includes(args.workerRole) ? args.workerRole : previousRecord?.workerRole ?? null,
    retryState: normalizeFollowThroughRetryState(args.retryState ?? previousRecord?.retryState),
    recordedAt: args.recordedAt ?? previousRecord?.recordedAt ?? timestamp,
    updatedAt: timestamp
  };
  validateFollowThroughRecordIdentity(previousRecord, nextRecord);
  validateFollowThroughTargetBinding(root, nextRecord, targetPreview);
  validateFollowThroughTransition(previousRecord, nextRecord);
  items.push(nextRecord);
  const next = buildOperatorFollowThrough(root, { ...existing, items }, sourceCatalog, timestamp, targetPreview);
  const nextTransitions = buildFollowThroughTransitions(existingTransitions, nextRecord, previousRecord, timestamp);
  return { next, nextTransitions, nextRecord, sourceCatalog };
}
function commitOperatorFollowThrough(root, prepared) {
  writeJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, prepared.next);
  writeJson(root, ARTIFACT_PATHS.metaOperatorFollowThroughTransitions, prepared.nextTransitions);
  refreshDurableSurfaces(root, {
    type: "record-operator-follow-through",
    summary: `Recorded operator follow-through for ${prepared.nextRecord.sourceType}:${prepared.nextRecord.sourceId}.`,
    artifactPaths: [ARTIFACT_PATHS.metaOperatorFollowThrough, ARTIFACT_PATHS.metaOperatorFollowThroughTransitions, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.metaOptimizerReport]
  });
  return prepared.next;
}
function recordOperatorFollowThrough(root, args = {}) {
  assertGovernanceMutationRegistered("record-operator-follow-through", "exempt");
  assertPublicFollowThroughInput(root, args);
  const sourceCatalog = readFollowThroughSourceCatalog(root);
  const source = sourceCatalog.get(`${args.sourceType}:${args.sourceId}`) ?? null;
  if (!source) {
    throw new Error(`Unknown follow-through source: ${args.sourceType}:${args.sourceId}`);
  }
  const actorRole = normalizeStringArray5(source.allowedActorRoles)[0] ?? "planner";
  const publicRecord = {
    ...args,
    actorRole,
    linkedTargetArtifact: null,
    linkedTargetId: null,
    plannedTarget: false,
    executeBy: null,
    workerRole: null
  };
  return commitOperatorFollowThrough(root, prepareOperatorFollowThrough(root, publicRecord, { sourceCatalog }));
}
function deriveContextQuerySnapshot(root) {
  const navigation = deriveNavigationQuerySnapshot(root);
  const derived = navigation.metaOptimize;
  const meta = {
    executionBridgeCandidates: derived.executionBridgeCandidates,
    governanceCoverage: derived.governanceCoverage,
    operatorFollowThrough: derived.operatorFollowThrough,
    operatorLessons: derived.operatorLessons,
    operatorPlaybooks: derived.operatorPlaybooks,
    remediationPacks: derived.remediationPacks,
    longHorizon: derived.longHorizonMemory,
    recommendations: derived.metaRecommendations.items ?? [],
    state: derived.metaOptimizerState
  };
  const packetById = new Map(navigation.packets.map((packet) => [packet.id, packet]));
  return { navigation, meta, packetById };
}
function readPhaseContextManifest(root, phaseId = null) {
  const { navigation, meta } = deriveContextQuerySnapshot(root);
  const resolvedPhaseId = phaseId ?? navigation.board.currentPhase;
  const board = resolvedPhaseId === navigation.board.currentPhase ? navigation.board : { ...navigation.board, currentPhase: resolvedPhaseId };
  return buildPhaseManifest(board, navigation.packets, navigation.workspaceIndex, meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates, meta.operatorFollowThrough, meta.operatorLessons);
}
function readRoleContextManifest(root, roleId) {
  if (!ROLE_IDS.includes(roleId)) {
    throw new Error(`Unknown roleId: ${roleId}`);
  }
  const { navigation, meta } = deriveContextQuerySnapshot(root);
  const role = navigation.board.roleRoster?.find((item) => item.id === roleId) ?? ROLE_HIERARCHY[roleId];
  return buildRoleManifest(role, navigation.packets, navigation.openQuestions, navigation.decisions, navigation.workspaceIndex, meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates, meta.operatorFollowThrough, meta.operatorLessons);
}
function readPacketContextManifest(root, packetId) {
  const normalizedPacketId = slugify2(packetId);
  const { navigation, packetById } = deriveContextQuerySnapshot(root);
  const packet = packetById.get(normalizedPacketId) ?? null;
  return packet ? buildPacketContextManifest(root, navigation.board, packet, packetById, navigation.workspaceIndex) : null;
}
function readArtifactContextManifest(root, artifactPath) {
  const normalizedArtifactPath = normalizeArtifactPath(artifactPath);
  const navigation = deriveNavigationQuerySnapshot(root);
  return buildArtifactContextManifest(root, normalizedArtifactPath, navigation.board, navigation.packets, navigation.workspaceIndex);
}
function readActionContextBundle(root, args = {}) {
  const scopeType = args.scopeType ?? "current";
  const { navigation, meta, packetById } = deriveContextQuerySnapshot(root);
  if (scopeType === "role") {
    if (!ROLE_IDS.includes(args.roleId)) {
      throw new Error(`Unknown roleId: ${args.roleId}`);
    }
    const role = navigation.board.roleRoster?.find((item) => item.id === args.roleId) ?? ROLE_HIERARCHY[args.roleId];
    const roleManifest = buildRoleManifest(role, navigation.packets, navigation.openQuestions, navigation.decisions, navigation.workspaceIndex, meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates, meta.operatorFollowThrough, meta.operatorLessons);
    return buildActionContextBundle({
      scopeType: "role",
      scopeId: args.roleId,
      summary: `Role-scoped pre-action bundle for ${args.roleId}.`,
      board: navigation.board,
      workspaceIndex: navigation.workspaceIndex,
      roleId: args.roleId,
      phaseId: navigation.workspaceIndex.boardPhase,
      requiredReadPaths: roleManifest.preActionReadPaths,
      localRules: roleManifest.behaviorDiscipline.localRules,
      operatorGuidance: roleManifest.operatorGuidance
    });
  }
  if (scopeType === "packet") {
    const packetId = slugify2(args.packetId);
    const packet = packetById.get(packetId) ?? null;
    if (!packet) {
      return null;
    }
    const manifest = buildPacketContextManifest(root, navigation.board, packet, packetById, navigation.workspaceIndex);
    return buildActionContextBundle({
      scopeType: "packet",
      scopeId: packetId,
      summary: `Packet-scoped pre-action bundle for ${packetId}.`,
      board: navigation.board,
      workspaceIndex: navigation.workspaceIndex,
      packet,
      roleId: packet.assignedRole,
      phaseId: packet.phase,
      requiredReadPaths: manifest.preActionReadPaths,
      localRules: manifest.behaviorDiscipline.localRules,
      operatorGuidance: buildOperatorGuidance(navigation.workspaceIndex, meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates, meta.operatorFollowThrough, meta.operatorLessons, { roleId: packet.assignedRole, packetId, packet })
    });
  }
  if (scopeType === "phase") {
    const phaseId = args.phaseId ?? navigation.board.currentPhase;
    const phaseBoard = phaseId === navigation.board.currentPhase ? navigation.board : { ...navigation.board, currentPhase: phaseId };
    const phaseManifest = buildPhaseManifest(phaseBoard, navigation.packets, navigation.workspaceIndex, meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates, meta.operatorFollowThrough, meta.operatorLessons);
    return buildActionContextBundle({
      scopeType: "phase",
      scopeId: phaseManifest.phaseId,
      summary: `Phase-scoped pre-action bundle for ${phaseManifest.phaseId}.`,
      board: { ...navigation.board, currentPhase: phaseManifest.phaseId },
      workspaceIndex: navigation.workspaceIndex,
      roleId: phaseManifest.assignedRole,
      phaseId: phaseManifest.phaseId,
      requiredReadPaths: phaseManifest.preActionReadPaths,
      localRules: phaseManifest.behaviorDiscipline.localRules,
      operatorGuidance: phaseManifest.operatorGuidance
    });
  }
  if (scopeType === "artifact") {
    const normalizedArtifactPath = normalizeArtifactPath(args.artifactPath);
    const artifactManifest = buildArtifactContextManifest(root, normalizedArtifactPath, navigation.board, navigation.packets, navigation.workspaceIndex);
    return buildActionContextBundle({
      scopeType: "artifact",
      scopeId: `artifact-${normalizedArtifactPath}`,
      summary: `Artifact-scoped pre-action bundle for ${normalizedArtifactPath}.`,
      board: navigation.board,
      workspaceIndex: navigation.workspaceIndex,
      artifactPath: normalizedArtifactPath,
      requiredReadPaths: [artifactContextPath(normalizedArtifactPath), ...artifactManifest.readBeforeMutating],
      localRules: artifactManifest.localRules
    });
  }
  return buildActionContextBundle({
    scopeType: "current",
    scopeId: "current",
    summary: "Current workspace pre-action bundle. Read this before mutating durable workflow state.",
    board: navigation.board,
    workspaceIndex: navigation.workspaceIndex,
    roleId: navigation.board.assignedRole,
    phaseId: navigation.board.currentPhase,
    artifactPath: navigation.workspaceIndex.contextSurfaces?.prioritizedArtifactContextPaths?.[0] ?? null,
    requiredReadPaths: navigation.workspaceIndex.behaviorDiscipline.requiredReadOrder,
    localRules: [
      "Start from the current action bundle, then follow the required read order.",
      "Use packet and artifact-local guidance instead of broad top-level rules when available.",
      "Do not assume hidden rule loading; read the surfaced files explicitly before acting."
    ],
    operatorGuidance: buildOperatorGuidance(navigation.workspaceIndex, meta.remediationPacks, meta.operatorPlaybooks, meta.executionBridgeCandidates, meta.operatorFollowThrough, meta.operatorLessons, { roleId: navigation.board.assignedRole })
  });
}
function summarizeSessionJournal(root) {
  assertGovernanceMutationRegistered("summarize-session-journal", "exempt");
  refreshDurableSurfaces(root, {
    type: "summarize-session-journal",
    summary: "Refreshed session summary surface.",
    artifactPaths: [ARTIFACT_PATHS.sessionJournal, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.workspaceIndex]
  });
  const journal = readJson(root, ARTIFACT_PATHS.sessionJournal, createSessionJournal);
  return {
    entryCount: journal.entries?.length ?? 0,
    latestEntry: journal.entries?.at(-1) ?? null,
    summaryPath: ARTIFACT_PATHS.sessionSummary
  };
}
function readBoundaryReport(root) {
  const boundaryPath = resolvePath(root, ARTIFACT_PATHS.workflowBoundaries);
  if (!fs6.existsSync(boundaryPath)) {
    return {
      status: "missing",
      error: "Boundary policy file is missing.",
      boundaries: null,
      missingBootstrapArtifacts: [],
      userOwnedExistingPaths: []
    };
  }
  let boundaries;
  try {
    boundaries = JSON.parse(fs6.readFileSync(boundaryPath, "utf8"));
  } catch (error) {
    return {
      status: "invalid",
      error: error instanceof Error ? error.message : String(error),
      boundaries: null,
      missingBootstrapArtifacts: [],
      userOwnedExistingPaths: []
    };
  }
  const missingBootstrapArtifacts = (boundaries.doveBootstrapOnlyPaths ?? []).filter((relativePath) => !fs6.existsSync(resolvePath(root, relativePath)));
  return {
    status: "ok",
    boundaries,
    missingBootstrapArtifacts,
    userOwnedExistingPaths: (boundaries.userOwnedPaths ?? []).filter((relativePath) => fs6.existsSync(resolvePath(root, relativePath)))
  };
}

// src/core/orchestration.mjs
import fs10 from "node:fs";
import path12 from "node:path";
import crypto7 from "node:crypto";

// src/core/mutation-guard.mjs
var GOVERNANCE_MUTATION_BY_ID = new Map([
  ...GOVERNANCE_GUARDED_MUTATIONS,
  ...GOVERNANCE_EXEMPT_MUTATIONS
].map((entry) => [entry.id, entry]));
function getGovernanceMutationEntry(actionId) {
  return GOVERNANCE_MUTATION_BY_ID.get(actionId) ?? null;
}
function assertTaskScopedMutationTarget(root, actionId, args = {}, options = {}) {
  const entry = getGovernanceMutationEntry(actionId);
  if (!entry) {
    throw new Error(`Governance registry missing mutation entry: ${actionId}`);
  }
  if (!entry.requiresPacketTarget) {
    return {
      packet: null,
      packetId: null,
      resolution: {
        mode: "not-packet-scoped",
        mutationScope: entry.mutationScope ?? "unspecified"
      },
      artifactIds: []
    };
  }
  return assertResolvedTaskPacket(root, args, {
    ...options,
    targetFields: options.targetFields ?? entry.targetFields,
    artifactFields: options.artifactFields ?? entry.artifactFields,
    mutationScope: entry.mutationScope
  });
}

// src/core/pre-action-guidance.mjs
var FULL_LESSON_LIMIT = 5;
var DEFAULT_LESSON_LIMIT = 3;
function text(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function normalizeString3(value, fallback = null) {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray6(value) {
  if (Array.isArray(value)) {
    return Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean)));
  }
  if (typeof value === "string" && value.trim()) {
    return [value.trim()];
  }
  return [];
}
function normalizeObjectArray2(value) {
  return Array.isArray(value) ? value.filter((item) => item && typeof item === "object" && !Array.isArray(item)) : [];
}
function normalizeObject3(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function normalizeSurface(surface) {
  const normalized = normalizeString3(surface, "dove.status").replace(/^project:/, "");
  return normalized.startsWith("dove.") ? normalized : `dove.${normalized}`;
}
function normalizeRoleId(roleId) {
  const normalized = normalizeString3(roleId, null);
  if (!normalized || !ROLE_HIERARCHY[normalized]) {
    return null;
  }
  const role = ROLE_HIERARCHY[normalized];
  return role.kind === "primary" ? role.id : role.parentRole ?? null;
}
function canonicalSubagentId(roleId) {
  const normalized = normalizeString3(roleId, null);
  if (!normalized || !ROLE_HIERARCHY[normalized]) {
    return null;
  }
  const role = ROLE_HIERARCHY[normalized];
  if (role.kind === "primary") {
    return null;
  }
  return role.canonicalRole ?? role.aliasOf ?? role.id;
}
function roleLabel(roleId) {
  return DOVE_PRIMARY_ROLES.find((role) => role.id === roleId)?.label ?? ROLE_HIERARCHY[roleId]?.label ?? roleId;
}
function roleSummary(roleId) {
  return DOVE_PRIMARY_ROLES.find((role) => role.id === roleId)?.summary ?? ROLE_HIERARCHY[roleId]?.charter ?? null;
}
function roleBoundaryCard(roleId, responseLanguage = "zh") {
  const role = ROLE_HIERARCHY[roleId];
  return {
    role: roleId,
    label: roleLabel(roleId),
    summary: roleSummary(roleId),
    boundary: role?.charter ?? roleSummary(roleId),
    manuallySwitchable: role?.manuallySwitchable === true,
    subagents: normalizeStringArray6(role?.subagents)
  };
}
function normalizeWorkflowStage(stage, surface = "dove.status") {
  const normalized = normalizeString3(stage, null)?.toLowerCase().replace(/_/g, "-") ?? null;
  if (["plan", "goal", "design", "checklist"].includes(normalized)) {
    return "plan";
  }
  if (["execute", "execution", "draft", "experiments", "source", "sources", "notes", "figure", "figures", "claim", "claims"].includes(normalized)) {
    return "execute";
  }
  if (["audit", "review"].includes(normalized)) {
    return "audit";
  }
  if (normalized === "return") {
    return "return";
  }
  if (["dove.auto", "dove.figure", "dove.experience", "dove.draft", "dove.source", "dove.note", "dove.document", "dove.documents", "dove.claim"].includes(surface)) {
    return "execute";
  }
  if (["dove.review", "dove.audit", "dove.paper-audit", "dove.audio-review", "dove.isolated-review"].includes(surface)) {
    return "audit";
  }
  return ["dove.status", "dove.mission", "dove.operator"].includes(surface) ? "plan" : null;
}
function surfaceIn(surface, needles) {
  return needles.some((needle) => surface === needle || surface.includes(needle));
}
function inferPrimaryRoleForSurface(surface, context = {}) {
  const normalizedSurface = normalizeSurface(surface);
  const explicitRole = normalizeRoleId(context.roleId ?? context.primaryRole ?? context.currentContext?.primaryRole ?? context.packet?.ownerRole ?? context.packet?.nextRole);
  if (explicitRole && PRIMARY_ROLE_IDS.includes(explicitRole)) {
    return explicitRole;
  }
  const stage = normalizeWorkflowStage(context.stage ?? context.currentContext?.stage ?? context.packet?.stage, normalizedSurface);
  if (stage === "audit") {
    return "reviewer";
  }
  if (stage === "execute") {
    return "builder";
  }
  if (surfaceIn(normalizedSurface, ["review", "audit"])) {
    return "reviewer";
  }
  if (surfaceIn(normalizedSurface, ["auto", "figure", "experience", "draft", "source", "note", "document", "claim", "experiment", "rebuttal"])) {
    return "builder";
  }
  return "planner";
}
function inferSubagentSpecialty(surface, context = {}) {
  const explicit = canonicalSubagentId(context.subagentSpecialty ?? context.specialty ?? context.roleId ?? context.packet?.ownerRole ?? context.packet?.nextRole);
  if (explicit) {
    return explicit;
  }
  const normalizedSurface = normalizeSurface(surface);
  if (surfaceIn(normalizedSurface, ["source", "note", "claim", "draft"])) {
    return "researcher";
  }
  if (surfaceIn(normalizedSurface, ["experience", "experiment"])) {
    return context.stage === "audit" ? null : "experiment-planner";
  }
  if (surfaceIn(normalizedSurface, ["rebuttal", "revision"])) {
    return "revision-lead";
  }
  if (surfaceIn(normalizedSurface, ["version"])) {
    return "version-analyst";
  }
  return null;
}
function interpretedIntentForSurface(surface, context = {}, responseLanguage = "zh") {
  const request = normalizeString3(context.request, null);
  if (request) {
    return text(responseLanguage, `\u628A\u666E\u901A prompt \u201C${request}\u201D \u5148\u89E3\u91CA\u6210 Dove \u5F53\u524D\u9879\u76EE\u72B6\u6001\u3001\u89D2\u8272\u8FB9\u754C\u548C\u4E0B\u4E00\u6B65\u5DE5\u4F5C\u6D41\u3002`, `Interpret the ordinary prompt \u201C${request}\u201D as Dove project state, role boundary, and next workflow route first.`);
  }
  if (surface === "dove.status") {
    return text(responseLanguage, "\u5148\u8BFB\u53D6\u9879\u76EE\u72B6\u51B5\u4E3B\u9875\uFF0C\u518D\u51B3\u5B9A planner/builder/reviewer \u7684\u4E0B\u4E00\u6B65\u3002", "Read the project situation home first, then decide the planner/builder/reviewer next step.");
  }
  if (surface === "dove.mission") {
    return text(responseLanguage, "\u628A\u7528\u6237\u9700\u6C42\u8F6C\u6210 durable work contract\uFF0C\u800C\u4E0D\u662F\u628A mission \u5F53\u4F5C\u5217\u8868\u6216\u4E3B\u754C\u9762\u3002", "Convert the user demand into a durable work contract, not a mission list or primary UI.");
  }
  if (surface === "dove.auto") {
    return text(responseLanguage, "\u5728\u663E\u5F0F\u786E\u8BA4\u540E\u8FD0\u884C\u6709\u754C\u524D\u53F0\u6267\u884C\uFF0C\u5E76\u5728\u8FB9\u754C\u5904\u505C\u6B62\u8FD4\u56DE\u7ED3\u679C\u3002", "After explicit confirmation, run bounded foreground work and stop at boundaries with a returned result.");
  }
  if (surface === "dove.operator") {
    return text(responseLanguage, "\u7531 planner \u534F\u8C03 ready/blocked work\uFF0C\u5E76\u53EA\u6267\u884C\u663E\u5F0F\u786E\u8BA4\u7684\u4E00\u6B21\u524D\u53F0 pass\u3002", "Let the planner coordinate ready/blocked work and run only one explicitly confirmed foreground pass.");
  }
  if (surface === "dove.source") {
    return text(responseLanguage, "\u628A\u5DF2\u9A8C\u8BC1\u5916\u90E8\u94FE\u63A5\u3001\u6A21\u677F\u3001\u6307\u5357\u3001venue/ranking \u8BC1\u636E\u5148\u6CE8\u518C\u4E3A packet-bound sources\uFF1B\u672A\u6293\u53D6\u6216\u672A\u6CE8\u518C\u7684\u5019\u9009\u94FE\u63A5\u53EA\u80FD\u5217\u4E3A candidate links\uFF0C\u518D\u8FDB\u5165 note \u6216 document evidence \u7EFC\u5408\u6C89\u6DC0\u3002", "Register verified external links, templates, guidelines, and venue/ranking evidence as packet-bound sources first; unfetched or unregistered URLs stay as candidate links before synthesis through note or document evidence.");
  }
  if (surface === "dove.note") {
    return text(responseLanguage, "\u5DF2\u6709 summary\u3001quote\u3001claim \u6216 open question \u65F6\uFF0C\u76F4\u63A5\u628A\u5DF2\u6CE8\u518C sources \u7EFC\u5408\u6210\u7ED1\u5B9A\u4E3B\u4EFB\u52A1\u7684\u7ED3\u6784\u5316 note\uFF1B\u5185\u90E8\u538B\u529B\u6D4B\u8BD5\u603B\u7ED3\u548C\u5199\u4F5C\u504F\u597D\u4E0D\u8981\u4F2A\u88C5\u6210 external source\u3002", "When a summary, quote, claim, or open question is present, directly synthesize registered sources into a packet-bound structured note; internal pressure-test summaries and writing preferences must not be disguised as external sources.");
  }
  if (surface === "dove.draft") {
    return text(responseLanguage, "\u5DF2\u6709\u6B63\u6587\u65F6\uFF0C\u76F4\u63A5\u8BB0\u5F55 packet-bound draft body\uFF1B\u7F3A\u6B63\u6587\u65F6\u505C\u4E0B\u8981\u6C42 draft content\uFF0C\u4E0D\u521B\u5EFA\u5360\u4F4D\u8349\u7A3F\u3002", "When body content is present, directly record a packet-bound draft body; when body content is missing, stop for draft content instead of creating a placeholder draft.");
  }
  if (surface === "dove.document" || surface === "dove.documents") {
    return text(responseLanguage, "\u5DF2\u6709\u62A5\u544A\u6216\u4EA7\u7269\u6458\u8981/\u8DEF\u5F84\u65F6\uFF0C\u76F4\u63A5\u4F5C\u4E3A document evidence \u7ED1\u5B9A\u5230 durable packet\uFF0C\u5E76\u4FDD\u7559 source/artifact provenance\u3002", "When a report or output summary/path is present, directly bind it as document evidence to a durable packet while preserving source/artifact provenance.");
  }
  return text(responseLanguage, "\u5148\u7528\u4E09\u89D2\u8272\u548C lesson guardrail \u6846\u4F4F\u884C\u52A8\uFF0C\u518D\u8FDB\u5165\u5177\u4F53 Dove workflow\u3002", "Frame the action with the three roles and lesson guardrails before entering the concrete Dove workflow.");
}
function buildIntentFrame(surface, context = {}, responseLanguage = "zh") {
  const normalizedSurface = normalizeSurface(surface);
  return {
    ordinaryPromptFirst: true,
    interpretedIntent: interpretedIntentForSurface(normalizedSurface, context, responseLanguage),
    primaryEntry: "statusHome",
    missionAsWorkContract: true,
    noDedicatedMissionListCommand: true,
    statusAsCommandCenter: true,
    surface: normalizedSurface
  };
}
function commandFromNextAction(nextAction) {
  if (!nextAction) {
    return null;
  }
  if (typeof nextAction === "string") {
    return nextAction;
  }
  return normalizeString3(nextAction.command ?? nextAction.firstAction ?? nextAction.copyableCommand ?? nextAction.nextAction, null);
}
function titleFromNextAction(nextAction) {
  if (!nextAction) {
    return null;
  }
  if (typeof nextAction === "string") {
    return nextAction;
  }
  return normalizeString3(nextAction.title ?? nextAction.why ?? nextAction.command ?? nextAction.firstAction, null);
}
function workflowRouteForSurface(surface, command, responseLanguage = "zh") {
  if (command) {
    return command;
  }
  const route = {
    "dove.status": "query_dove_status",
    "dove.mission": "create_dove_task proposal -> materialized contract -> recommended handoff",
    "dove.auto": "run_dove_auto preview -> confirmed bounded foreground pass",
    "dove.operator": "run_dove_operator preview -> confirmed queue pass",
    "dove.review": "review/audit workflow with independent reviewer boundary",
    "dove.source": "register_source quick path for verified external provenance, keep candidate links separate, optionally batch -> upsert_note or record_document_evidence synthesis",
    "dove.note": "upsert_note quick path for packet-bound synthesis from registered sources -> claims or document evidence",
    "dove.draft": "upsert_draft quick path for an existing packet-bound draft body -> run_review_loop independent review",
    "dove.document": "record_document_evidence quick path for packet-bound report/archive ledger with source and artifact provenance",
    "dove.documents": "record_document_evidence quick path for packet-bound report/archive ledger with source and artifact provenance",
    "dove.figure": "figure materials -> generation/import -> caption/provenance -> QA",
    "dove.experience": "experiment plan/result -> audit -> claim bridge",
    "dove.rebuttal": "review issue board -> builder revision strategy -> response draft"
  }[surface];
  return route ?? text(responseLanguage, "\u5148 statusHome \u5206\u8BCA\uFF0C\u518D\u8FDB\u5165\u5BF9\u5E94 Dove workflow\u3002", "Triage through statusHome first, then enter the matching Dove workflow.");
}
function foregroundFlowForSurface(surface, responseLanguage = "zh") {
  if (surface === "dove.status") {
    return text(responseLanguage, "\u53EA\u8BFB command-center \u67E5\u8BE2\uFF1B\u4E0D\u5199\u5165\u3001\u4E0D\u5237\u65B0\u3001\u4E0D\u6267\u884C\u547D\u4EE4\u3002", "Read-only command-center query; no writes, refresh, or command execution.");
  }
  if (surface === "dove.mission") {
    return text(responseLanguage, "\u5148 proposal card\uFF0C\u786E\u8BA4\u540E\u53EA\u7269\u5316\u4EFB\u52A1\u5408\u540C\u5E76\u4EA4\u63A5\u63A8\u8350\u8DEF\u7EBF\uFF1Bmission \u672C\u8EAB\u4E0D\u6267\u884C pass\u3002", "Show a proposal card first; after confirmation only materialize the task contract and hand off recommended routes; mission itself does not run a pass.");
  }
  if (surface === "dove.auto") {
    return text(responseLanguage, "\u663E\u5F0F\u786E\u8BA4\u540E\u7684 bounded foreground loop\uFF1B\u5230\u8BC1\u636E\u3001\u8FB9\u754C\u6216\u9884\u7B97\u5373\u505C\u3002", "Explicitly confirmed bounded foreground loop; stop at evidence, boundary, or budget.");
  }
  if (surface === "dove.operator") {
    return text(responseLanguage, "\u663E\u5F0F\u786E\u8BA4\u7684\u4E00\u6B21\u961F\u5217\u534F\u8C03 pass\uFF1B\u4E0D\u542F\u52A8 scheduler \u6216 daemon\u3002", "One explicitly confirmed queue coordination pass; no scheduler or daemon.");
  }
  return text(responseLanguage, "\u663E\u5F0F\u5DE5\u5177\u8C03\u7528\u5185\u5B8C\u6210\u5F53\u524D workflow \u9636\u6BB5\uFF1B\u5199\u5165\u5FC5\u987B\u7ECF\u8FC7\u8BE5\u5DE5\u5177\u7684\u786E\u8BA4\u5951\u7EA6\u3002", "Complete the current workflow stage inside an explicit tool call; writes must follow that tool's confirmation contract.");
}
function gatesForSurface(surface, primaryRole, responseLanguage = "zh") {
  const common = [
    text(responseLanguage, "\u5148\u8BFB\u53D6\u81EA\u52A8\u53EC\u56DE\u7684 lessons\uFF0C\u53EA\u4F5C\u4E3A guardrail\u3002", "Read automatically recalled lessons first as guardrails only."),
    text(responseLanguage, "\u5199\u5165\u3001\u6267\u884C\u3001\u72B6\u6001\u8C03\u6574\u90FD\u5FC5\u987B\u663E\u5F0F\u786E\u8BA4\u3002", "Writes, execution, and status changes require explicit confirmation."),
    text(responseLanguage, "\u4E0D\u80FD\u542F\u52A8\u9690\u85CF\u540E\u53F0 runtime\u3002", "Do not start hidden background runtime.")
  ];
  if (primaryRole === "reviewer") {
    return [
      text(responseLanguage, "Reviewer \u5FC5\u987B\u72EC\u7ACB\u5BA1\u67E5\u8BC1\u636E\uFF0C\u4E0D\u80FD\u66FF builder \u81EA\u8BC1\u5B8C\u6574\u6027\u3002", "Reviewer must independently audit evidence and cannot self-certify builder completeness."),
      ...common
    ];
  }
  if (primaryRole === "builder") {
    return [
      text(responseLanguage, "Builder \u4EA7\u51FA\u5FC5\u987B\u5E26 evidence/provenance\uFF0C\u8FD4\u56DE\u540E\u4EA4 reviewer \u6216 status gate\u3002", "Builder output must carry evidence/provenance and return through reviewer or status gates."),
      ...common
    ];
  }
  return [
    text(responseLanguage, "Planner \u5148\u5B9A\u8303\u56F4\u3001\u4F18\u5148\u7EA7\u3001\u4E0B\u4E00\u6761 workflow route\uFF0C\u4E0D\u76F4\u63A5\u4F2A\u88C5\u6210\u6267\u884C\u7ED3\u679C\u3002", "Planner sets scope, priority, and the next workflow route first, without pretending execution happened."),
    ...common
  ];
}
function stopConditionsForSurface(surface, responseLanguage = "zh") {
  if (surface === "dove.status") {
    return [text(responseLanguage, "\u8FD4\u56DE\u9879\u76EE\u72B6\u51B5\u3001\u89D2\u8272\u8DEF\u7531\u3001lesson guardrails \u548C\u4E0B\u4E00\u6B65\uFF1B\u4E0D\u6267\u884C\u4E0B\u4E00\u6B65\u3002", "Return project state, role route, lesson guardrails, and next step; do not execute it.")];
  }
  return [
    text(responseLanguage, "\u9047\u5230\u7F3A\u8F93\u5165\u3001\u7F3A\u8BC1\u636E\u3001\u6743\u9650\u8FB9\u754C\u3001review gate \u6216\u9884\u7B97\u8017\u5C3D\u65F6\u505C\u6B62\u3002", "Stop on missing input, missing evidence, authority boundary, review gate, or budget exhaustion."),
    text(responseLanguage, "\u8FD4\u56DE result card / handoff / next action\uFF0C\u800C\u4E0D\u662F\u7EE7\u7EED\u9690\u85CF\u8FD0\u884C\u3002", "Return a result card, handoff, or next action instead of continuing hidden work.")
  ];
}
function executionGuidanceStopCondition({ readiness, coverage, statusExecutionGaps }, responseLanguage = "zh") {
  if (readiness && !readiness.ready) {
    return text(responseLanguage, "\u7F3A\u53EF\u6267\u884C\u5408\u540C\u65F6\u505C\u6B62\u5728 Planner\uFF0C\u4E0D\u8FDB\u5165 Builder \u6216 completion\u3002", "Stop at Planner when the executable contract is missing; do not enter Builder or completion.");
  }
  if (normalizeStringArray6(statusExecutionGaps?.missingMaterialTaskIds).length > 0) {
    return text(responseLanguage, "\u7F3A source/material/read-first \u8F93\u5165\u65F6\u505C\u6B62\u5E76\u8981\u6C42\u8865\u6750\u6599\u3002", "Stop and require materials when source/material/read-first inputs are missing.");
  }
  if (coverage && coverage.complete === false) {
    return text(responseLanguage, "convergence.criteria \u672A\u8986\u76D6\u65F6\u505C\u6B62\u5728 verification/reviewer gate\u3002", "Stop at the verification/reviewer gate when convergence.criteria are not covered.");
  }
  if (normalizeStringArray6(statusExecutionGaps?.verificationGapTaskIds).length > 0) {
    return text(responseLanguage, "\u5DF2\u6709\u7ED3\u679C\u4F46\u7F3A verifiedCriteria \u65F6\u505C\u6B62\u5728 Reviewer \u9A8C\u8BC1\u3002", "Stop at Reviewer verification when results exist but verifiedCriteria is missing.");
  }
  return text(responseLanguage, "\u4E0B\u4E00\u6B65\u53EA\u80FD\u6267\u884C\u5F53\u524D\u663E\u5F0F\u524D\u53F0 pass\uFF0C\u5E76\u8FD4\u56DE\u8BC1\u636E\u6216\u8FB9\u754C\u3002", "Run only the current explicit foreground pass next, returning evidence or a boundary.");
}
function buildExecutionGuidance(context = {}, primaryRole = "planner", responseLanguage = "zh") {
  const packet = normalizeObject3(context.packet);
  const statusExecutionGaps = normalizeObject3(context.statusSummary?.executionGaps);
  const hasPacket = Boolean(packet.id ?? packet.packetId ?? packet.taskPacketId);
  const contract = hasPacket ? normalizeDoveExecutionContract(packet.executionContract, null) : null;
  const readiness = hasPacket ? doveExecutionContractReadiness(contract) : null;
  const verifiedCriteria = hasPacket ? normalizeDoveVerifiedCriteria(packet.verifiedCriteria) : [];
  const coverage = hasPacket ? doveExecutionCriteriaCoverage(contract, verifiedCriteria) : null;
  const missingContractTaskIds = normalizeStringArray6(statusExecutionGaps.missingContractTaskIds);
  const missingMaterialTaskIds = normalizeStringArray6(statusExecutionGaps.missingMaterialTaskIds);
  const verificationGapTaskIds = normalizeStringArray6(statusExecutionGaps.verificationGapTaskIds);
  const readyBuilderTaskIds = normalizeStringArray6(statusExecutionGaps.readyBuilderTaskIds);
  const nextRole = !hasPacket && missingContractTaskIds.length > 0 ? "planner" : readiness && !readiness.ready ? "planner" : missingMaterialTaskIds.length > 0 ? "planner" : coverage?.complete === false || verificationGapTaskIds.length > 0 ? "reviewer" : readyBuilderTaskIds.length > 0 ? "builder" : primaryRole;
  return {
    executableContractPresent: hasPacket ? Boolean(contract) : missingContractTaskIds.length === 0 ? null : false,
    executableContractReady: hasPacket ? readiness?.ready === true : null,
    executionContractStatus: hasPacket ? readiness?.status ?? null : null,
    missingContractFields: hasPacket ? normalizeStringArray6(readiness?.missing) : [],
    missingContractTaskIds,
    missingMaterialTaskIds,
    verificationGapTaskIds,
    readyBuilderTaskIds,
    requiredMaterials: hasPacket ? normalizeStringArray6(readiness?.requiredMaterials) : normalizeStringArray6(statusExecutionGaps.requiredMaterials),
    evidenceRequired: hasPacket ? normalizeStringArray6(readiness?.evidenceRequired) : normalizeStringArray6(statusExecutionGaps.evidenceRequired),
    criteriaCoverage: coverage ? {
      complete: Boolean(coverage.complete),
      missing: normalizeStringArray6(coverage.missing),
      requiredCount: normalizeStringArray6(coverage.required).length
    } : null,
    nextRole,
    stopCondition: executionGuidanceStopCondition({ readiness, coverage, statusExecutionGaps }, responseLanguage)
  };
}
function buildWorkflowFrame(surface, context = {}, responseLanguage = "zh") {
  const normalizedSurface = normalizeSurface(surface);
  const currentStage = normalizeWorkflowStage(context.stage ?? context.currentContext?.stage ?? context.packet?.stage, normalizedSurface);
  const command = commandFromNextAction(context.nextAction ?? context.routeHint);
  const nextHumanAction = normalizeString3(context.nextHumanAction, null) ?? titleFromNextAction(context.nextAction) ?? command ?? workflowRouteForSurface(normalizedSurface, null, responseLanguage);
  const primaryRole = inferPrimaryRoleForSurface(normalizedSurface, context);
  const executionGuidance = buildExecutionGuidance(context, primaryRole, responseLanguage);
  return {
    currentStage,
    recommendedRoute: workflowRouteForSurface(normalizedSurface, command, responseLanguage),
    nextHumanAction,
    allowedForegroundFlow: foregroundFlowForSurface(normalizedSurface, responseLanguage),
    executionGuidance,
    gates: gatesForSurface(normalizedSurface, primaryRole, responseLanguage),
    stopConditions: [executionGuidance.stopCondition, ...stopConditionsForSurface(normalizedSurface, responseLanguage)]
  };
}
function lessonUpdatedAt(lesson = {}) {
  return String(lesson.updatedAt ?? lesson.createdAt ?? "");
}
function lessonRoleMatches(lessonRole, primaryRole) {
  const normalized = normalizeString3(lessonRole, null);
  if (!normalized || !primaryRole) {
    return false;
  }
  if (normalized === primaryRole) {
    return true;
  }
  return ROLE_HIERARCHY[normalized]?.parentRole === primaryRole;
}
function scoreLesson(lesson = {}, context = {}) {
  const matchedBecause = [];
  let score = 0;
  const packet = context.packet && typeof context.packet === "object" ? context.packet : {};
  const packetIds = normalizeStringArray6(lesson.packetIds);
  const linkedPacketIds = normalizeStringArray6([context.packetId, context.taskPacketId, packet.id, packet.packetId, packet.taskPacketId]);
  const linkedLessonIds = new Set(normalizeStringArray6(packet.lessonIds));
  const primaryRole = inferPrimaryRoleForSurface(context.surface ?? "dove.status", context);
  if (packetIds.some((packetId) => linkedPacketIds.includes(packetId))) {
    score += 100;
    matchedBecause.push("packet");
  }
  if (linkedLessonIds.has(lesson.id)) {
    score += 90;
    matchedBecause.push("packet-lesson-id");
  }
  if (lessonRoleMatches(lesson.actorRole ?? lesson.roleId, primaryRole)) {
    score += 40;
    matchedBecause.push("role");
  }
  const domain = normalizeString3(context.domain ?? context.currentContext?.domain ?? packet.domain ?? packet.doveDomain, null);
  if (domain && lesson.domain === domain) {
    score += 25;
    matchedBecause.push("domain");
  }
  const stage = normalizeWorkflowStage(context.stage ?? context.currentContext?.stage ?? packet.stage, context.surface ?? "dove.status");
  if (stage && normalizeWorkflowStage(lesson.stage, context.surface ?? "dove.status") === stage) {
    score += 20;
    matchedBecause.push("stage");
  }
  const contextTags = new Set(normalizeStringArray6([context.workflowKind, context.surface, context.domain, context.stage, ...Array.isArray(context.tags) ? context.tags : []]));
  const tagMatches = normalizeStringArray6(lesson.tags).filter((tag) => contextTags.has(tag));
  if (tagMatches.length > 0) {
    score += tagMatches.length * 8;
    matchedBecause.push(...tagMatches.map((tag) => `tag:${tag}`));
  }
  if (packetIds.length === 0) {
    score += 5;
    matchedBecause.push("global");
  }
  if (score === 0) {
    score = 1;
    matchedBecause.push("active");
  }
  return { score, matchedBecause: Array.from(new Set(matchedBecause)) };
}
function compactPreActionLesson(lesson = {}, matchInfo = {}) {
  const nextTimeCount = normalizeStringArray6(lesson.nextTime).length;
  return {
    id: lesson.id,
    title: lesson.title,
    matchedBecause: normalizeStringArray6(matchInfo.matchedBecause),
    mustObey: lesson.mustObey ?? true,
    nextTimeCount,
    hasNextTimeGuidance: nextTimeCount > 0,
    role: lesson.actorRole ?? null,
    domain: lesson.domain ?? null,
    stage: lesson.stage ?? null,
    tags: normalizeStringArray6(lesson.tags).slice(0, 5)
  };
}
function selectPreActionLessons(operatorLessons = {}, context = {}) {
  const lessons = Array.isArray(operatorLessons) ? operatorLessons : normalizeObjectArray2(operatorLessons.lessons);
  const limit = Math.min(FULL_LESSON_LIMIT, Math.max(0, Number.isFinite(context.lessonLimit) ? Math.floor(context.lessonLimit) : DEFAULT_LESSON_LIMIT));
  return lessons.filter((lesson) => String(lesson.status ?? "active").trim().toLowerCase() === "active").map((lesson) => ({ lesson, matchInfo: scoreLesson(lesson, context) })).sort((left, right) => right.matchInfo.score - left.matchInfo.score || lessonUpdatedAt(right.lesson).localeCompare(lessonUpdatedAt(left.lesson)) || String(left.lesson.id ?? "").localeCompare(String(right.lesson.id ?? ""))).slice(0, limit).map(({ lesson, matchInfo }) => compactPreActionLesson(lesson, matchInfo));
}
function buildRoleFrame(surface, context = {}, responseLanguage = "zh") {
  const primaryRole = inferPrimaryRoleForSurface(surface, context);
  return {
    primaryRole,
    primaryRoleLabel: roleLabel(primaryRole),
    roleReason: text(responseLanguage, `${roleLabel(primaryRole)} \u662F\u5F53\u524D\u5165\u53E3\u7684\u4E3B\u89D2\u8272\uFF1Bspecialty \u53EA\u4F5C\u4E3A\u8BE5\u4E3B\u89D2\u8272\u4E0B\u7684\u81EA\u52A8\u5B50\u80FD\u529B\u3002`, `${roleLabel(primaryRole)} is the primary role for this surface; specialties stay as automatic sub-capabilities under it.`),
    subagentSpecialty: inferSubagentSpecialty(surface, context),
    roleBoundaries: PRIMARY_ROLE_IDS.map((roleId) => roleBoundaryCard(roleId, responseLanguage))
  };
}
function buildPreActionGuidance({
  surface = "dove.status",
  responseLanguage = "zh",
  request = null,
  roleId = null,
  packet = null,
  currentContext = {},
  operatorLessons = {},
  nextAction = null,
  routeHint = null,
  workflowKind = null,
  domain = null,
  stage = null,
  statusSummary = null,
  lessonLimit = DEFAULT_LESSON_LIMIT,
  subagentSpecialty = null,
  nextHumanAction = null,
  tags = []
} = {}) {
  const normalizedSurface = normalizeSurface(surface);
  const context = {
    surface: normalizedSurface,
    responseLanguage,
    request,
    roleId: roleId ?? currentContext?.primaryRole ?? packet?.ownerRole ?? packet?.nextRole,
    packet,
    currentContext,
    nextAction,
    routeHint,
    workflowKind,
    domain: domain ?? currentContext?.domain ?? packet?.domain ?? packet?.doveDomain,
    stage: stage ?? currentContext?.stage ?? packet?.stage,
    statusSummary,
    lessonLimit,
    subagentSpecialty,
    nextHumanAction,
    tags
  };
  const roleFrame = buildRoleFrame(normalizedSurface, context, responseLanguage);
  const workflowFrame = buildWorkflowFrame(normalizedSurface, context, responseLanguage);
  return {
    version: 1,
    presentation: "dove-pre-action-guidance",
    surface: normalizedSurface,
    mode: "read-only-guidance",
    intentFrame: buildIntentFrame(normalizedSurface, context, responseLanguage),
    roleFrame,
    workflowFrame,
    lessonRecall: {
      automatic: true,
      readOnly: true,
      recordingExplicitOnly: true,
      lessonsPath: ARTIFACT_PATHS.metaOperatorLessons,
      topLessons: selectPreActionLessons(operatorLessons, { ...context, roleId: roleFrame.primaryRole })
    },
    referencePatterns: {
      learnedFrom: ["ARIS", "Trellis", "CodeStable", "oh-my-openagent", "AutoFigure-edit"],
      appliedPatterns: ["intent-gate", "stage-gates", "knowledge-compounding", "primary-role-routing", "artifact-provenance"]
    },
    guardrails: {
      explicitOnly: true,
      noHiddenRuntime: true,
      noAutoApply: true,
      requiresConfirmationForWrites: true,
      boundedForegroundOnly: true
    }
  };
}
function summarizePreActionGuidance(guidance = {}) {
  const topLessons = normalizeObjectArray2(guidance.lessonRecall?.topLessons);
  return {
    presentation: "dove-pre-action-guidance-summary",
    surface: guidance.surface ?? null,
    primaryRole: guidance.roleFrame?.primaryRole ?? null,
    primaryRoleLabel: guidance.roleFrame?.primaryRoleLabel ?? null,
    subagentSpecialty: guidance.roleFrame?.subagentSpecialty ?? null,
    nextHumanAction: guidance.workflowFrame?.nextHumanAction ?? null,
    executionNextRole: guidance.workflowFrame?.executionGuidance?.nextRole ?? null,
    executableContractReady: guidance.workflowFrame?.executionGuidance?.executableContractReady ?? null,
    stopCondition: guidance.workflowFrame?.executionGuidance?.stopCondition ?? null,
    lessonIds: topLessons.map((lesson) => lesson.id).filter(Boolean),
    noHiddenRuntime: guidance.guardrails?.noHiddenRuntime === true,
    requiresConfirmationForWrites: guidance.guardrails?.requiresConfirmationForWrites === true,
    recordingExplicitOnly: guidance.lessonRecall?.recordingExplicitOnly === true
  };
}

// src/core/result-cards.mjs
function normalizeString4(value, fallback = null) {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray7(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return Array.from(new Set(value.map((item) => typeof item === "string" ? item.trim() : "").filter(Boolean)));
}
function compactResultList(values = [], fallback, internalFallback = fallback) {
  const normalized = normalizeStringArray7(values);
  const publicValues = compactPublicStringArray(normalized);
  if (publicValues.length > 0) {
    return publicValues;
  }
  return normalized.length > 0 ? [internalFallback] : [fallback];
}
function normalizePlainObject(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function compactPlainObject(fields) {
  return Object.fromEntries(Object.entries(fields).filter(([, value]) => {
    if (value === null || value === void 0) {
      return false;
    }
    if (Array.isArray(value)) {
      return value.length > 0;
    }
    if (value && typeof value === "object") {
      return Object.keys(value).length > 0;
    }
    return true;
  }));
}
var COMPACT_INTERNAL_FIELD_PATTERN = /^(?:id|packetId|packetIds|taskPacketId|missionPacketId|runId|receiptId|boundaryId|boundaryType|implementationBoundaryType|implementationReason|ownerRole|nextRole|handoff|handoffSuggestion|providerId|providerStatus|providerError|apiKeyEnv|sourceSvgPath|targetFinalSvgPath|finalSvgPath|outputManifestPath|svgContent|qaPath|resultPath|mutationMode|operatorRoute|queueSummary|queuePreview|preActionGuidance|preActionGuidanceSummary|executionReceipt|fullResult|diagnostics)$/u;
var COMPACT_INTERNAL_STRING_PATTERN = /\.dove\/|\bproject:dove\.[a-z0-9.-]+|--packet-id\b|\b(?:packetId|taskPacketId|missionPacketId|runId|receiptId|boundaryId|boundaryType|mutationMode|patch-plan|direct-process|ownerRole|nextRole|handoff|providerId|sourceSvgPath|targetFinalSvgPath|finalSvgPath|outputManifestPath|svgContent|queueSummary|queuePreview|preActionGuidance|resultCard)\b|\b(?:query_dove_status|run_dove_auto|record_dove_mission_pass|run_figure_workflow|run_dove_operator|record_document_evidence|upsert_note|upsert_draft|register_source)\b/u;
function isCompactInternalFieldName(value) {
  const key = String(value ?? "");
  return COMPACT_INTERNAL_FIELD_PATTERN.test(key) || /(?:^|[A-Za-z])Ids?$/u.test(key);
}
function isCompactInternalReference(value) {
  const text3 = normalizeString4(value, "") ?? "";
  return COMPACT_INTERNAL_STRING_PATTERN.test(text3) || /source-svg|output-manifest|figure-qa/u.test(text3);
}
function compactPublicValue(value) {
  if (Array.isArray(value)) {
    return Array.from(new Set(value.map((item) => compactPublicValue(item)).filter((item) => item !== null && item !== void 0 && item !== "")));
  }
  if (value && typeof value === "object") {
    const entries = Object.entries(value).filter(([key]) => !isCompactInternalFieldName(key)).map(([key, item]) => [key, compactPublicValue(item)]).filter(([, item]) => {
      if (item === null || item === void 0 || item === "") {
        return false;
      }
      if (Array.isArray(item)) {
        return item.length > 0;
      }
      if (item && typeof item === "object") {
        return Object.keys(item).length > 0;
      }
      return true;
    });
    return compactPlainObject(Object.fromEntries(entries));
  }
  if (typeof value === "string") {
    const text3 = normalizeString4(value, null);
    return text3 && !isCompactInternalReference(text3) ? text3 : null;
  }
  return value === null || value === void 0 ? null : value;
}
function compactPublicObject(value) {
  const object = normalizePlainObject(value);
  if (!object) {
    return null;
  }
  return compactPublicValue(object);
}
function compactPublicStringArray(value) {
  return normalizeStringArray7(value).filter((item) => !isCompactInternalReference(item));
}
function publicBoundaryType(value) {
  const boundaryType = normalizeString4(value, null);
  if (!boundaryType) {
    return null;
  }
  if (["awaiting-audio-review-output"].includes(boundaryType)) {
    return "awaiting-review-output";
  }
  if (["missing-secret-env", "provider-failed"].includes(boundaryType)) {
    return "awaiting-provider-output";
  }
  if (boundaryType.startsWith("audio-review-") || boundaryType === "needs-review") {
    return "verification-failed";
  }
  return boundaryType;
}
function publicOutcome(value) {
  const outcome = normalizeString4(value, null);
  if (!outcome) {
    return null;
  }
  return publicBoundaryType(outcome) ?? outcome;
}
function publicBoundaryDetail(value) {
  return compactPublicObject(value?.detail) ?? null;
}
function sanitizeBoundary(value) {
  const boundary = normalizePlainObject(value);
  if (!boundary) {
    return null;
  }
  const detail = publicBoundaryDetail(boundary);
  return compactPlainObject({
    type: publicBoundaryType(boundary.type ?? boundary.boundaryType),
    title: compactPublicValue(boundary.title ?? boundary.label),
    summary: compactPublicValue(boundary.summary ?? boundary.message),
    why: compactPublicValue(boundary.why),
    nextAction: compactPublicValue(boundary.nextAction ?? boundary.action),
    requiredInputs: compactPublicStringArray(boundary.requiredInputs),
    requiredActions: compactPublicStringArray(boundary.requiredActions),
    requires: compactPublicStringArray(boundary.requires),
    confirmationRequired: boundary.confirmationRequired === true ? true : void 0,
    detail
  });
}
function compactExecutionReceipt(value) {
  const receipt = normalizeDoveExecutionReceipt(value, null);
  if (!receipt) {
    return null;
  }
  return compactPlainObject({
    status: normalizeString4(receipt.status, null),
    outcome: publicOutcome(receipt.outcome),
    resultSummary: compactPublicValue(receipt.publicSafeSummary ?? receipt.resultSummary),
    evidenceCount: normalizeStringArray7([
      ...normalizeStringArray7(receipt.evidenceLinks),
      ...normalizeStringArray7(receipt.evidencePaths),
      ...normalizeStringArray7(receipt.artifactRefs),
      ...normalizeStringArray7(receipt.artifactPaths),
      ...normalizeStringArray7(receipt.validationEvidencePaths),
      ...normalizeStringArray7(receipt.verificationEvidencePaths)
    ]).length,
    criteriaCoverage: compactPublicObject(receipt.criteriaCoverage)
  });
}
function publicActionTitle(value, responseLanguage) {
  const text3 = normalizeString4(value, null);
  if (!text3 || isCompactInternalReference(text3)) {
    return doveText(responseLanguage, "resultCardNextStatus");
  }
  return text3;
}
function resultAction(action, responseLanguage = "zh") {
  if (typeof action === "string" && action.trim()) {
    return {
      title: publicActionTitle(action, responseLanguage),
      proposalOnly: true,
      noAutoApply: true
    };
  }
  if (!action || typeof action !== "object" || Array.isArray(action)) {
    return null;
  }
  const command = normalizeString4(action.command ?? action.nextAction, null);
  const title = normalizeString4(action.title ?? action.label, null) ?? publicActionTitle(command, responseLanguage) ?? doveText(responseLanguage, "compactCardFirstActionFallback");
  return compactPlainObject({
    title,
    why: compactPublicValue(action.why),
    requiredInputs: compactPublicStringArray(action.requiredInputs),
    requiredActions: compactPublicStringArray(action.requiredActions),
    proposalOnly: true,
    noAutoApply: true,
    confirmationRequired: action.confirmationRequired === true ? true : void 0
  });
}
function resultActions(actions = [], nextAction = null, responseLanguage = "zh") {
  const sourceActions = Array.isArray(actions) && actions.length > 0 ? actions : nextAction ? [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: nextAction }] : [];
  return sourceActions.map((action) => resultAction(action, responseLanguage)).filter(Boolean);
}
function requiresAction(status, boundary, stopReason) {
  const statusText = normalizeString4(status, "") ?? "";
  return Boolean(boundary) || ["blocked", "blocked-boundary", "awaiting-host-pass", "awaiting-host-results", "needs-host-results", "needs-completion-evidence", "needs-explicit-progress-step", "needs-review", "prepared-awaiting-audio", "prepared-awaiting-output", "step-budget-exhausted", "max-iterations-exhausted", "stopped-killed"].includes(statusText) || /awaiting|missing|required|blocked|failed|exhausted|needs-/u.test(String(stopReason ?? ""));
}
function buildCommandResultCard(details = {}, responseLanguage = "zh") {
  const receipt = normalizeDoveExecutionReceipt(details.executionReceipt, null);
  const executionReceipt = compactExecutionReceipt(receipt);
  const evidence = [
    ...normalizeStringArray7(details.evidenceLinks),
    ...normalizeStringArray7(details.evidencePaths),
    ...normalizeStringArray7(details.artifactRefs),
    ...normalizeStringArray7(details.artifactPaths),
    ...normalizeStringArray7(details.evidence),
    ...normalizeStringArray7(details.artifacts),
    ...normalizeStringArray7(receipt?.evidenceLinks),
    ...normalizeStringArray7(receipt?.evidencePaths),
    ...normalizeStringArray7(receipt?.artifactRefs),
    ...normalizeStringArray7(receipt?.artifactPaths)
  ];
  const validation = [
    ...normalizeStringArray7(details.validationEvidence),
    ...normalizeStringArray7(details.validationEvidencePaths),
    ...normalizeStringArray7(details.validation),
    ...normalizeStringArray7(details.validationOutputPaths),
    ...normalizeStringArray7(receipt?.validationEvidencePaths),
    ...normalizeStringArray7(receipt?.verificationEvidencePaths)
  ];
  const status = normalizeString4(details.status, null);
  const boundary = sanitizeBoundary(details.boundary);
  const stopReason = normalizeString4(details.stopReason, null);
  const happened = compactPublicValue(details.happened ?? details.summary) ?? doveText(responseLanguage, "resultCardHappenedFallback");
  return compactPlainObject({
    presentation: "compact-result-summary-card",
    surface: normalizeString4(details.surface, null),
    command: normalizeString4(details.command, null),
    executionReceipt,
    title: compactPublicValue(details.title),
    status,
    outcome: publicOutcome(details.outcome),
    happened,
    durableWrites: compactResultList(details.durableWrites, doveText(responseLanguage, "resultCardNoDurableWrites"), doveText(responseLanguage, "resultCardDurableWritesHidden")),
    evidence: compactResultList(evidence, doveText(responseLanguage, "resultCardNoEvidence"), doveText(responseLanguage, "resultCardEvidenceHidden")),
    evidenceExplanation: compactPublicObject(details.evidenceExplanation),
    validation: compactResultList(validation, doveText(responseLanguage, "resultCardNoValidation"), doveText(responseLanguage, "resultCardValidationHidden")),
    codeChanges: compactResultList(details.codeChanges, doveText(responseLanguage, "resultCardCodeNotInspected"), doveText(responseLanguage, "resultCardDetailsAvailable")),
    boundary,
    scope: compactPublicObject(details.scope),
    taskStatusBefore: normalizeString4(details.taskStatusBefore, null),
    taskStatusAfter: normalizeString4(details.taskStatusAfter, null),
    nextActions: resultActions(details.nextActions, details.nextAction, responseLanguage),
    completed: status === "completed",
    stopped: Boolean(stopReason || boundary || ["blocked", "blocked-boundary", "awaiting-host-pass", "awaiting-host-results", "needs-host-results", "needs-completion-evidence", "needs-explicit-progress-step", "needs-review", "prepared-awaiting-output", "step-budget-exhausted", "max-iterations-exhausted", "stopped-killed"].includes(status ?? "")),
    requiresAction: requiresAction(status, boundary, stopReason),
    detailsAvailable: true,
    showMore: {
      text: doveText(responseLanguage, "resultCardDetailsAvailable")
    }
  });
}

// src/core/review-proof.mjs
import fs9 from "node:fs";
import path11 from "node:path";

// src/core/artifact-integrity.mjs
import fs7 from "node:fs";
import path9 from "node:path";
import { inflateSync } from "node:zlib";
var DEFAULT_READ_LIMIT_BYTES = 24 * 1024;
var ARTIFACT_EVIDENCE_ROLES = /* @__PURE__ */ new Map([
  [ARTIFACT_PATHS.researchBrief, "conditional"],
  [ARTIFACT_PATHS.researchAgenda, "conditional"],
  [ARTIFACT_PATHS.plan, "conditional"],
  [ARTIFACT_PATHS.outline, "conditional"],
  [ARTIFACT_PATHS.findings, "conditional"],
  [ARTIFACT_PATHS.experimentLog, "conditional"],
  [ARTIFACT_PATHS.experimentPlans, "conditional"],
  [ARTIFACT_PATHS.experimentResults, "conditional"],
  [ARTIFACT_PATHS.experimentAudits, "conditional"],
  [ARTIFACT_PATHS.notes, "conditional"],
  [ARTIFACT_PATHS.evidence, "conditional"],
  [ARTIFACT_PATHS.claims, "conditional"],
  [ARTIFACT_PATHS.claimBridgeLog, "conditional"],
  [ARTIFACT_PATHS.reviewLog, "conditional"],
  [ARTIFACT_PATHS.reviewReport, "conditional"],
  [ARTIFACT_PATHS.reviewConcerns, "conditional"],
  [ARTIFACT_PATHS.reviewDebateLog, "conditional"],
  [ARTIFACT_PATHS.revisionPlan, "conditional"],
  [ARTIFACT_PATHS.wiki, "conditional"],
  [ARTIFACT_PATHS.bibliography, "conditional"],
  [ARTIFACT_PATHS.citationLog, "conditional"],
  [ARTIFACT_PATHS.figureBriefs, "conditional"],
  [ARTIFACT_PATHS.figureSegments, "conditional"],
  [ARTIFACT_PATHS.figureTemplates, "conditional"],
  [ARTIFACT_PATHS.figureEditableIndex, "conditional"],
  [ARTIFACT_PATHS.figureFinalIndex, "conditional"],
  [ARTIFACT_PATHS.figureMaterials, "conditional"],
  [ARTIFACT_PATHS.figureGenerations, "conditional"],
  [ARTIFACT_PATHS.figureCaptions, "conditional"],
  [ARTIFACT_PATHS.figureQa, "conditional"],
  [ARTIFACT_PATHS.rebuttalIssues, "conditional"],
  [ARTIFACT_PATHS.rebuttalStrategy, "conditional"],
  [ARTIFACT_PATHS.rebuttalResponseDraft, "conditional"],
  [ARTIFACT_PATHS.versionComparisons, "conditional"],
  [ARTIFACT_PATHS.versionComparisonReport, "conditional"]
]);
var BOOKKEEPING_ARTIFACT_PATHS = new Set(
  Object.values(ARTIFACT_PATHS).filter((artifactPath) => typeof artifactPath === "string" && artifactPath.startsWith(`${ARTIFACT_PATHS.doveRoot}/`) && path9.posix.extname(artifactPath).length > 0 && !ARTIFACT_EVIDENCE_ROLES.has(artifactPath))
);
var DYNAMIC_EVIDENCE_PATTERNS = [
  { pattern: /^\.dove\/drafts\/(?!README\.md$)[^/]+\.(?:md|txt|tex)$/u, role: "substantive" },
  { pattern: /^\.dove\/evidence\/(?!index\.json$).+$/u, role: "validation" },
  { pattern: /^\.dove\/(?:reviews\/isolated|audio\/reviews)\/[^/]+\/report\.md$/u, role: "conditional" },
  { pattern: /^\.dove\/figures\/(?!runs\/).+\.(?:final\.svg|png|jpe?g|pdf)$/iu, role: "conditional" }
];
var WORKFLOW_GOAL_COMPLETION_EVIDENCE_ROLES = /* @__PURE__ */ new Map([
  [".dove/evidence/workflow-goal-verification.log", "validation"],
  [".dove/evidence/workflow-goal-result.md", "substantive"]
]);
var DYNAMIC_COMPLETION_EVIDENCE_PATTERN = /^\.dove\/evidence\/(?!index\.json$).+$/u;
var COMPLETION_MEDIA_EXTENSIONS = /* @__PURE__ */ new Map([
  [".svg", "svg"],
  [".png", "png"],
  [".jpg", "jpeg"],
  [".jpeg", "jpeg"],
  [".pdf", "pdf"]
]);
var NEGATIVE_VALIDATION_STATUSES = /* @__PURE__ */ new Set([
  "author-response-submitted",
  "awaiting-author-response",
  "blocked",
  "challenged",
  "concern",
  "contested",
  "escalated",
  "failed",
  "failure",
  "held",
  "held-audit-blocked",
  "held-for-review",
  "held-missing-claim",
  "incomplete",
  "inconclusive",
  "invalid",
  "needs-evidence",
  "needs-review",
  "needs-revision",
  "negative",
  "needs-operator",
  "needs-rebuttal",
  "needs-source-verification",
  "not-ready",
  "not-reviewed",
  "open",
  "pending",
  "rejected",
  "unresolved"
]);
var POSITIVE_VALIDATION_STATUSES = /* @__PURE__ */ new Set([
  "applied",
  "approved",
  "clean",
  "coherent",
  "complete",
  "completed",
  "passed",
  "ready",
  "resolved",
  "verified"
]);
var REQUIREMENT_PURPOSE_PATTERNS = /* @__PURE__ */ new Map([
  ["audit", /\b(?:audit|audited|integrity)\b|审计|完整性/iu],
  ["bridge", /\b(?:bridge|bridged|claim mapping)\b|桥接|论点映射/iu],
  ["comparison", /\b(?:compare|comparison|diff|version delta)\b|比较|对比|版本差异/iu],
  ["qa", /\b(?:qa|quality assurance|quality check)\b|质量检查|质检/iu],
  ["review", /\b(?:review|reviewer|verdict|concern)\b|审查|评审|问题项/iu],
  ["validation", /\b(?:check|lint|test|typecheck|validat(?:e|ed|ion)|verif(?:y|ied|ication))\b|测试|校验|验证/iu],
  ["artifact", /\b(?:artifact|deliverable|draft|figure|implementation|output|report)\b|产物|交付物|草稿|图|实现|输出|报告/iu],
  ["evidence", /\b(?:evidence|log|result|source|citation)\b|证据|日志|结果|来源|引文/iu]
]);
var BOOKKEEPING_EVIDENCE_PATTERNS = [
  /^\.dove\/(?:public|orchestration|task-packets|context|sessions|workspace|programs|workflow-pack|runtime|mutations|meta)(?:\/|$)/u,
  /^\.dove\/wiki\/(?:query_pack|navigation)\.md$/u,
  /^\.dove\/wiki\/(?:entities|relations)\.json$/u,
  /^\.dove\/(?:reviews\/isolated|audio\/reviews)\/[^/]+\/(?!report\.md$).+/u,
  /^\.dove\/versions\/snapshots(?:\/|$)/u,
  /^\.dove\/figures\/runs(?:\/|$)/u
];
var CONDITIONAL_JSON_COLLECTION_FIELDS = /* @__PURE__ */ new Map([
  [ARTIFACT_PATHS.researchAgenda, ["agenda", "evidenceBacklog"]],
  [ARTIFACT_PATHS.experimentAudits, ["items"]],
  [ARTIFACT_PATHS.sources, ["items"]],
  [ARTIFACT_PATHS.notes, ["items"]],
  [ARTIFACT_PATHS.evidence, ["claims"]],
  [ARTIFACT_PATHS.claimBridgeLog, ["items"]],
  [ARTIFACT_PATHS.reviewConcerns, ["items"]],
  [ARTIFACT_PATHS.figureBriefs, ["items"]],
  [ARTIFACT_PATHS.figureSegments, ["items"]],
  [ARTIFACT_PATHS.figureTemplates, ["items"]],
  [ARTIFACT_PATHS.figureEditableIndex, ["items"]],
  [ARTIFACT_PATHS.figureFinalIndex, ["items"]],
  [ARTIFACT_PATHS.figureMaterials, ["items"]],
  [ARTIFACT_PATHS.figureGenerations, ["items"]],
  [ARTIFACT_PATHS.figureCaptions, ["items"]],
  [ARTIFACT_PATHS.figureQa, ["items", "issues"]],
  [ARTIFACT_PATHS.rebuttalIssues, ["items"]],
  [ARTIFACT_PATHS.versionComparisons, ["items"]]
]);
var BOOTSTRAP_MARKDOWN_PATTERNS = /* @__PURE__ */ new Map([
  [ARTIFACT_PATHS.researchBrief, [/Clarify the paper objective and contribution\./iu, /澄清论文目标与贡献。/u]],
  [ARTIFACT_PATHS.plan, [/Run `project:dove\.status`/iu, /运行 `project:dove\.status`/u]],
  [ARTIFACT_PATHS.outline, [/(?:Goal|Evidence): TBD/iu, /(?:目标|证据): 待定/u]],
  [ARTIFACT_PATHS.findings, [/Capture key empirical or analytical takeaways here/iu, /在转化为 claims 前，在此记录/u]],
  [ARTIFACT_PATHS.experimentLog, [/Document planned runs, settings, outcomes/iu, /在此记录计划运行、设置、结果/u]],
  [ARTIFACT_PATHS.claims, [/List only claims that can be traced/iu, /只列出可追溯到/u]],
  [ARTIFACT_PATHS.reviewLog, [/Starter review log created\./iu, /已创建 starter 审查日志。/u]],
  [ARTIFACT_PATHS.reviewDebateLog, [/Use this file to capture adversarial review rounds/iu, /用此文件以持久文本记录/u]],
  [ARTIFACT_PATHS.revisionPlan, [/No review loop has generated/iu, /尚未由 review loop 生成/u]],
  [ARTIFACT_PATHS.wiki, [/- TBD/iu, /- 待定/u]],
  [ARTIFACT_PATHS.citationLog, [/Track registration and verification status/iu, /在此跟踪 sources 的注册和验证状态/u]],
  [ARTIFACT_PATHS.rebuttalStrategy, [/No issue strategy has been generated/iu, /尚未生成 issue strategy/u]],
  [ARTIFACT_PATHS.rebuttalResponseDraft, [/Draft concise, evidence-backed responses here/iu, /在此起草简洁且有证据支撑的回应/u]],
  [ARTIFACT_PATHS.versionComparisonReport, [/No comparison has been generated/iu, /尚未生成 comparison/u]]
]);
function normalizeStringArray8(value) {
  const values = Array.isArray(value) ? value : typeof value === "string" ? [value] : [];
  return Array.from(new Set(values.map((item) => String(item).trim()).filter(Boolean)));
}
function plainObject(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function normalizeLocalPath(value) {
  const normalized = normalizeProjectRelativePath(value);
  return normalized.ok ? normalized.normalizedPath : null;
}
function normalizeLocalPathArray(values) {
  return Array.from(new Set(normalizeStringArray8(values).map(normalizeLocalPath).filter(Boolean)));
}
function flattenPathFields(value, fields) {
  if (Array.isArray(value)) {
    return value.flatMap((item) => flattenPathFields(item, fields));
  }
  const source = plainObject(value);
  return fields.flatMap((field) => normalizeStringArray8(source[field]));
}
function explicitTaskLinkedPaths(task = {}) {
  const source = plainObject(task);
  const context = plainObject(source.context);
  const pathFields = [
    "artifactRefs",
    "artifactPaths",
    "artifacts",
    "outputPaths",
    "evidenceLinks",
    "evidencePaths",
    "validationEvidencePaths",
    "verificationEvidencePaths"
  ];
  return normalizeLocalPathArray([
    ...flattenPathFields([source, context], pathFields),
    ...flattenPathFields([source.verifiedCriteria, context.verifiedCriteria], ["evidencePaths"])
  ]);
}
function evidenceRequirementKind(value) {
  const requirement = String(value ?? "").trim();
  if (!requirement) {
    return "invalid";
  }
  if (isExternalArtifactReference(requirement)) {
    return "reference";
  }
  const normalized = normalizeProjectRelativePath(requirement);
  if (!normalized.ok) {
    return "invalid";
  }
  const looksNarrative = /\s|[，。；！？：]/u.test(normalized.normalizedPath);
  const pathLike = normalized.normalizedPath.startsWith(".") || !looksNarrative && normalized.normalizedPath.includes("/") || !looksNarrative && path9.posix.extname(normalized.normalizedPath).length > 0;
  return pathLike ? "reference" : "description";
}
function explicitContractLinkedPaths(executionContract = {}) {
  const contract = plainObject(executionContract);
  const materials = plainObject(contract.materials);
  const convergence = plainObject(contract.convergence);
  return normalizeLocalPathArray([
    ...flattenPathFields(contract.files, ["path", "target"]),
    ...normalizeStringArray8(materials.requiredArtifacts).filter((item) => evidenceRequirementKind(item) === "reference"),
    ...normalizeStringArray8(materials.artifactRefs).filter((item) => evidenceRequirementKind(item) === "reference"),
    ...normalizeStringArray8(convergence.evidenceRequired).filter((item) => evidenceRequirementKind(item) === "reference")
  ]);
}
function normalizeCompletionRequirements(value, source = "caller") {
  const values = Array.isArray(value) ? value : value === void 0 || value === null ? [] : [value];
  return values.map((item, index) => {
    if (typeof item === "string" && item.trim()) {
      const requirement2 = item.trim();
      return {
        id: `${source}-${index + 1}`,
        requirement: requirement2,
        purpose: null,
        evidencePaths: evidenceRequirementKind(requirement2) === "reference" ? normalizeLocalPathArray([requirement2]) : [],
        source
      };
    }
    const requirement = plainObject(item);
    const text3 = String(requirement.requirement ?? requirement.description ?? requirement.text ?? requirement.title ?? "").trim();
    if (!text3) {
      return null;
    }
    return {
      id: String(requirement.id ?? `${source}-${index + 1}`).trim(),
      requirement: text3,
      purpose: String(requirement.purpose ?? requirement.kind ?? "").trim() || null,
      evidencePaths: normalizeLocalPathArray(requirement.evidencePaths ?? requirement.paths),
      source
    };
  }).filter(Boolean);
}
function descriptiveContractRequirements(executionContract = {}) {
  return normalizeStringArray8(plainObject(executionContract).convergence?.evidenceRequired).filter((requirement) => evidenceRequirementKind(requirement) === "description");
}
function completionPolicyContext(options = {}) {
  const context = plainObject(options.context);
  const task = plainObject(context.task);
  const executionContract = plainObject(context.executionContract ?? task.executionContract);
  const requirements = [
    ...normalizeCompletionRequirements(context.requirements, "caller"),
    ...normalizeCompletionRequirements(context.evidenceRequirements, "caller-evidence"),
    ...normalizeCompletionRequirements(descriptiveContractRequirements(executionContract), "execution-contract")
  ];
  const taskLinkedPaths = explicitTaskLinkedPaths(task);
  const contractLinkedPaths = explicitContractLinkedPaths(executionContract);
  const requirementLinkedPaths = normalizeLocalPathArray(requirements.flatMap((requirement) => requirement.evidencePaths));
  const criterionLinkedPaths = normalizeLocalPathArray(
    flattenPathFields(context.verifiedCriteria, ["evidencePaths"])
  );
  return {
    task,
    executionContract,
    requirements,
    taskLinkedPaths,
    contractLinkedPaths,
    requirementLinkedPaths,
    criterionLinkedPaths,
    linkedPaths: /* @__PURE__ */ new Set([...taskLinkedPaths, ...contractLinkedPaths, ...requirementLinkedPaths, ...criterionLinkedPaths])
  };
}
function isExternalArtifactReference(value) {
  const text3 = String(value ?? "").trim();
  return /^https?:\/\/[^\s]+$/iu.test(text3) || /^(?:doi|arxiv|source|note):[^\s]+$/iu.test(text3) || /^10\.\d{4,9}\/[^\s]+$/u.test(text3);
}
function normalizeProjectRelativePath(rawPath) {
  const original = typeof rawPath === "string" ? rawPath.trim() : String(rawPath ?? "").trim();
  if (!original) {
    return { ok: false, path: original, reason: "empty path" };
  }
  if (original.includes("\0")) {
    return { ok: false, path: original, reason: "path contains a null byte" };
  }
  if (path9.isAbsolute(original) || /^[A-Za-z]:[\\/]/.test(original)) {
    return { ok: false, path: original, reason: "absolute paths are not inspected" };
  }
  if (/^[a-z][a-z0-9+.-]*:/iu.test(original)) {
    return { ok: false, path: original, reason: "unsupported or malformed external reference scheme" };
  }
  const normalizedPath = path9.posix.normalize(original.replace(/\\/g, "/"));
  if (normalizedPath === "." || normalizedPath === ".." || normalizedPath.startsWith("../")) {
    return { ok: false, path: original, normalizedPath, reason: "path escapes the project root" };
  }
  return { ok: true, path: original, normalizedPath };
}
function readBoundedText(fullPath, maxBytes = DEFAULT_READ_LIMIT_BYTES) {
  const descriptor = fs7.openSync(fullPath, "r");
  try {
    const buffer = Buffer.alloc(maxBytes);
    const bytesRead = fs7.readSync(descriptor, buffer, 0, maxBytes, 0);
    return {
      text: buffer.subarray(0, bytesRead).toString("utf8"),
      bytesRead
    };
  } finally {
    fs7.closeSync(descriptor);
  }
}
var EXPERIMENT_RESULT_OUTCOMES = /* @__PURE__ */ new Set(["supports", "refutes", "inconclusive", "failed", "pending"]);
var CONDITIONAL_JSON_VALIDATORS = /* @__PURE__ */ new Map([
  [ARTIFACT_PATHS.experimentPlans, (parsed) => Array.isArray(parsed?.items) && parsed.items.length > 0 && parsed.items.some((item) => typeof item?.methodology === "string" && item.methodology.trim().length > 0 && typeof item?.successMetric === "string" && item.successMetric.trim().length > 0)],
  [ARTIFACT_PATHS.experimentResults, (parsed) => Array.isArray(parsed?.items) && parsed.items.length > 0 && parsed.items.some((item) => {
    const outcome = String(item?.outcome ?? "").trim().toLowerCase();
    return EXPERIMENT_RESULT_OUTCOMES.has(outcome) && outcome !== "pending" && typeof item?.summary === "string" && item.summary.trim().length > 0;
  })]
]);
function conditionalJsonHasEvidence(relativePath, text3) {
  const fields = CONDITIONAL_JSON_COLLECTION_FIELDS.get(relativePath);
  const validator = CONDITIONAL_JSON_VALIDATORS.get(relativePath);
  if (!fields && !validator) {
    return null;
  }
  try {
    const parsed = JSON.parse(text3);
    return validator ? validator(parsed) : fields.some((field) => Array.isArray(parsed?.[field]) && parsed[field].length > 0);
  } catch {
    return false;
  }
}
function conditionalMarkdownHasEvidence(relativePath, text3) {
  const patterns = BOOTSTRAP_MARKDOWN_PATTERNS.get(relativePath);
  if (!patterns) {
    return null;
  }
  const normalizedText = String(text3 ?? "").trim();
  return normalizedText.length > 0 && !patterns.some((pattern) => pattern.test(normalizedText));
}
function inspectSemanticEvidence(relativePath, text3) {
  const role = artifactEvidenceRole(relativePath);
  if (["bookkeeping", "unsupported"].includes(role)) {
    return {
      role,
      satisfied: false,
      status: role,
      reason: role === "unsupported" ? "path is not an approved Dove completion-evidence artifact" : "path is a navigation, status, runtime, task, or ledger record rather than substantive work evidence"
    };
  }
  if (role !== "conditional") {
    return { role, satisfied: true, status: "existing", reason: null };
  }
  const jsonEvidence = conditionalJsonHasEvidence(relativePath, text3);
  const markdownEvidence = conditionalMarkdownHasEvidence(relativePath, text3);
  const satisfied = jsonEvidence ?? markdownEvidence ?? String(text3 ?? "").trim().length > 0;
  return satisfied ? { role, satisfied: true, status: "existing", reason: null } : {
    role,
    satisfied: false,
    status: "placeholder",
    reason: "path contains only bootstrap, placeholder, or semantically empty content"
  };
}
function readBoundedBuffer(fullPath, maxBytes = DEFAULT_READ_LIMIT_BYTES) {
  const descriptor = fs7.openSync(fullPath, "r");
  try {
    const buffer = Buffer.alloc(maxBytes);
    const bytesRead = fs7.readSync(descriptor, buffer, 0, maxBytes, 0);
    return buffer.subarray(0, bytesRead);
  } finally {
    fs7.closeSync(descriptor);
  }
}
function completionMediaFormat(relativePath) {
  return COMPLETION_MEDIA_EXTENSIONS.get(path9.posix.extname(String(relativePath ?? "")).toLowerCase()) ?? null;
}
function validSvgBuffer(buffer) {
  const text3 = buffer.toString("utf8").replace(/^﻿/u, "").trim();
  const documentText = text3.replace(/^<\?xml[^>]*>\s*/iu, "");
  if (!/^<svg\b/iu.test(documentText) || !/<\/svg\s*>\s*$/iu.test(documentText)) {
    return false;
  }
  if (/<!DOCTYPE|<!ENTITY|<script\b|\bon\w+\s*=|\b(?:href|xlink:href)\s*=\s*["']\s*(?:https?:|data:|javascript:)/iu.test(documentText)) {
    return false;
  }
  const stack = [];
  const tagPattern = /<\/?([A-Za-z_][\w:.-]*)\b[^>]*>/gu;
  for (const match of documentText.matchAll(tagPattern)) {
    const token = match[0];
    const tag = match[1].toLowerCase();
    if (token.startsWith("</")) {
      if (stack.pop() !== tag) {
        return false;
      }
    } else if (!token.endsWith("/>")) {
      stack.push(tag);
    }
  }
  return stack.length === 0;
}
function crc32(buffer) {
  let crc = 4294967295;
  for (const byte of buffer) {
    crc ^= byte;
    for (let bit = 0; bit < 8; bit += 1) {
      crc = crc >>> 1 ^ (crc & 1 ? 3988292384 : 0);
    }
  }
  return (crc ^ 4294967295) >>> 0;
}
function pngPassDimensions(width, height, interlace) {
  if (interlace === 0) {
    return [{ width, height }];
  }
  const starts = [
    [0, 0, 8, 8],
    [4, 0, 8, 8],
    [0, 4, 4, 8],
    [2, 0, 4, 4],
    [0, 2, 2, 4],
    [1, 0, 2, 2],
    [0, 1, 1, 2]
  ];
  return starts.map(([startX, startY, stepX, stepY]) => ({
    width: width <= startX ? 0 : Math.ceil((width - startX) / stepX),
    height: height <= startY ? 0 : Math.ceil((height - startY) / stepY)
  }));
}
function validPngScanlines(buffer, width, height, bitsPerPixel, interlace) {
  const passes = pngPassDimensions(width, height, interlace).filter((pass) => pass.width > 0 && pass.height > 0);
  const expectedBytes = passes.reduce((total, pass) => total + pass.height * (Math.ceil(pass.width * bitsPerPixel / 8) + 1), 0);
  if (!Number.isSafeInteger(expectedBytes) || expectedBytes <= 0 || expectedBytes > 64 * 1024 * 1024) {
    return false;
  }
  let inflated;
  try {
    inflated = inflateSync(buffer, { maxOutputLength: expectedBytes + 1 });
  } catch {
    return false;
  }
  if (inflated.length !== expectedBytes) {
    return false;
  }
  let offset = 0;
  for (const pass of passes) {
    const rowBytes = Math.ceil(pass.width * bitsPerPixel / 8);
    for (let row = 0; row < pass.height; row += 1) {
      if (inflated[offset] > 4) {
        return false;
      }
      offset += rowBytes + 1;
    }
  }
  return offset === inflated.length;
}
function validPngBuffer(buffer) {
  const signature = Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]);
  if (buffer.length < 57 || !buffer.subarray(0, 8).equals(signature)) {
    return false;
  }
  const allowedBitDepths = /* @__PURE__ */ new Map([
    [0, /* @__PURE__ */ new Set([1, 2, 4, 8, 16])],
    [2, /* @__PURE__ */ new Set([8, 16])],
    [3, /* @__PURE__ */ new Set([1, 2, 4, 8])],
    [4, /* @__PURE__ */ new Set([8, 16])],
    [6, /* @__PURE__ */ new Set([8, 16])]
  ]);
  const channelCounts = /* @__PURE__ */ new Map([[0, 1], [2, 3], [3, 1], [4, 2], [6, 4]]);
  const criticalChunkTypes = /* @__PURE__ */ new Set(["IHDR", "PLTE", "IDAT", "IEND"]);
  let offset = 8;
  let width = 0;
  let height = 0;
  let bitDepth = 0;
  let colorType = -1;
  let interlace = -1;
  let sawIhdr = false;
  let sawPlte = false;
  let sawIdat = false;
  let closedIdatSequence = false;
  const idatChunks = [];
  while (offset + 12 <= buffer.length) {
    const chunkLength = buffer.readUInt32BE(offset);
    const dataStart = offset + 8;
    const dataEnd = dataStart + chunkLength;
    const chunkEnd = dataEnd + 4;
    if (chunkEnd > buffer.length) {
      return false;
    }
    const typeBuffer = buffer.subarray(offset + 4, offset + 8);
    const chunkType = typeBuffer.toString("ascii");
    if (!/^[A-Za-z]{4}$/u.test(chunkType)) {
      return false;
    }
    const storedCrc = buffer.readUInt32BE(dataEnd);
    if (crc32(Buffer.concat([typeBuffer, buffer.subarray(dataStart, dataEnd)])) !== storedCrc) {
      return false;
    }
    if (chunkType[0] === chunkType[0].toUpperCase() && !criticalChunkTypes.has(chunkType)) {
      return false;
    }
    if (!sawIhdr) {
      if (chunkType !== "IHDR" || chunkLength !== 13) {
        return false;
      }
      width = buffer.readUInt32BE(dataStart);
      height = buffer.readUInt32BE(dataStart + 4);
      bitDepth = buffer[dataStart + 8];
      colorType = buffer[dataStart + 9];
      const compression = buffer[dataStart + 10];
      const filter = buffer[dataStart + 11];
      interlace = buffer[dataStart + 12];
      if (width <= 0 || height <= 0 || !allowedBitDepths.get(colorType)?.has(bitDepth) || compression !== 0 || filter !== 0 || ![0, 1].includes(interlace)) {
        return false;
      }
      sawIhdr = true;
    } else if (chunkType === "IHDR") {
      return false;
    } else if (chunkType === "PLTE") {
      if (sawPlte || sawIdat || chunkLength === 0 || chunkLength % 3 !== 0 || chunkLength > 768) {
        return false;
      }
      sawPlte = true;
    } else if (chunkType === "IDAT") {
      if (closedIdatSequence || chunkLength === 0) {
        return false;
      }
      sawIdat = true;
      idatChunks.push(buffer.subarray(dataStart, dataEnd));
    } else {
      if (sawIdat && chunkType !== "IEND") {
        closedIdatSequence = true;
      }
      if (chunkType === "IEND") {
        if (chunkLength !== 0 || chunkEnd !== buffer.length || !sawIdat || colorType === 3 && !sawPlte) {
          return false;
        }
        const bitsPerPixel = channelCounts.get(colorType) * bitDepth;
        return validPngScanlines(Buffer.concat(idatChunks), width, height, bitsPerPixel, interlace);
      }
    }
    offset = chunkEnd;
  }
  return false;
}
var JPEG_START_OF_FRAME_MARKERS = /* @__PURE__ */ new Set([
  192,
  193,
  194,
  195,
  197,
  198,
  199,
  201,
  202,
  203,
  205,
  206,
  207
]);
function validJpegBuffer(buffer) {
  if (buffer.length < 16 || buffer[0] !== 255 || buffer[1] !== 216) {
    return false;
  }
  let offset = 2;
  let sawSof = false;
  let sawSos = false;
  let sawEntropy = false;
  let sawDac = false;
  const quantizationTableIds = /* @__PURE__ */ new Set();
  const huffmanTableIds = /* @__PURE__ */ new Set();
  const requiredQuantizationTableIds = /* @__PURE__ */ new Set();
  const requiredHuffmanTableIds = /* @__PURE__ */ new Set();
  let frameComponents = /* @__PURE__ */ new Set();
  while (offset < buffer.length) {
    if (buffer[offset] !== 255) {
      return false;
    }
    while (offset < buffer.length && buffer[offset] === 255) {
      offset += 1;
    }
    if (offset >= buffer.length) {
      return false;
    }
    const marker = buffer[offset];
    offset += 1;
    if (marker === 0 || marker === 216 || marker >= 208 && marker <= 215) {
      return false;
    }
    if (marker === 217) {
      return sawSof && sawSos && sawEntropy && offset === buffer.length;
    }
    if (marker === 1) {
      continue;
    }
    if (offset + 2 > buffer.length) {
      return false;
    }
    const segmentLength = buffer.readUInt16BE(offset);
    const segmentEnd = offset + segmentLength;
    if (segmentLength < 2 || segmentEnd > buffer.length) {
      return false;
    }
    if (marker === 219) {
      let tableOffset = offset + 2;
      while (tableOffset < segmentEnd) {
        const tableInfo = buffer[tableOffset];
        const precision = tableInfo >> 4;
        const tableId = tableInfo & 15;
        const tableBytes = precision === 0 ? 64 : precision === 1 ? 128 : 0;
        if (tableId > 3 || tableBytes === 0 || tableOffset + 1 + tableBytes > segmentEnd) {
          return false;
        }
        quantizationTableIds.add(tableId);
        tableOffset += 1 + tableBytes;
      }
      if (tableOffset !== segmentEnd) {
        return false;
      }
    }
    if (marker === 196) {
      let tableOffset = offset + 2;
      while (tableOffset < segmentEnd) {
        const tableInfo = buffer[tableOffset];
        const tableClass = tableInfo >> 4;
        const tableId = tableInfo & 15;
        if (tableClass > 1 || tableId > 3 || tableOffset + 17 > segmentEnd) {
          return false;
        }
        let symbolCount = 0;
        for (let index = 1; index <= 16; index += 1) {
          symbolCount += buffer[tableOffset + index];
        }
        if (symbolCount === 0 || tableOffset + 17 + symbolCount > segmentEnd) {
          return false;
        }
        huffmanTableIds.add(`${tableClass}:${tableId}`);
        tableOffset += 17 + symbolCount;
      }
      if (tableOffset !== segmentEnd) {
        return false;
      }
    }
    if (marker === 204) {
      sawDac = true;
    }
    if (JPEG_START_OF_FRAME_MARKERS.has(marker)) {
      if (sawSof || segmentLength < 11) {
        return false;
      }
      const precision = buffer[offset + 2];
      const height = buffer.readUInt16BE(offset + 3);
      const width = buffer.readUInt16BE(offset + 5);
      const componentCount = buffer[offset + 7];
      if (precision === 0 || width === 0 || height === 0 || componentCount === 0 || segmentLength !== 8 + 3 * componentCount) {
        return false;
      }
      frameComponents = /* @__PURE__ */ new Set();
      for (let index = 0; index < componentCount; index += 1) {
        const componentId = buffer[offset + 8 + 3 * index];
        const sampling = buffer[offset + 9 + 3 * index];
        const horizontalSampling = sampling >> 4;
        const verticalSampling = sampling & 15;
        const quantizationTableId = buffer[offset + 10 + 3 * index];
        if (frameComponents.has(componentId) || horizontalSampling === 0 || verticalSampling === 0 || quantizationTableId > 3) {
          return false;
        }
        frameComponents.add(componentId);
        requiredQuantizationTableIds.add(quantizationTableId);
      }
      sawSof = true;
    }
    if (marker !== 218) {
      offset = segmentEnd;
      continue;
    }
    if (!sawSof || segmentLength < 8) {
      return false;
    }
    const scanComponentCount = buffer[offset + 2];
    if (scanComponentCount === 0 || segmentLength !== 6 + 2 * scanComponentCount) {
      return false;
    }
    const scanComponents = /* @__PURE__ */ new Set();
    for (let index = 0; index < scanComponentCount; index += 1) {
      const componentId = buffer[offset + 3 + 2 * index];
      const tableSelectors = buffer[offset + 4 + 2 * index];
      const dcTableId = tableSelectors >> 4;
      const acTableId = tableSelectors & 15;
      if (!frameComponents.has(componentId) || scanComponents.has(componentId) || dcTableId > 3 || acTableId > 3) {
        return false;
      }
      scanComponents.add(componentId);
      requiredHuffmanTableIds.add(`0:${dcTableId}`);
      requiredHuffmanTableIds.add(`1:${acTableId}`);
    }
    if ([...requiredQuantizationTableIds].some((tableId) => !quantizationTableIds.has(tableId)) || !sawDac && [...requiredHuffmanTableIds].some((tableId) => !huffmanTableIds.has(tableId))) {
      return false;
    }
    sawSos = true;
    offset = segmentEnd;
    let scanBytes = 0;
    while (offset < buffer.length) {
      if (buffer[offset] !== 255) {
        scanBytes += 1;
        offset += 1;
        continue;
      }
      let markerOffset = offset + 1;
      while (markerOffset < buffer.length && buffer[markerOffset] === 255) {
        markerOffset += 1;
      }
      if (markerOffset >= buffer.length) {
        return false;
      }
      const scanMarker = buffer[markerOffset];
      if (scanMarker === 0) {
        scanBytes += 1;
        offset = markerOffset + 1;
        continue;
      }
      if (scanMarker >= 208 && scanMarker <= 215) {
        offset = markerOffset + 1;
        continue;
      }
      if (scanBytes > 0) {
        sawEntropy = true;
      }
      offset = markerOffset - 1;
      break;
    }
  }
  return false;
}
function validPdfBuffer(buffer) {
  const text3 = buffer.toString("latin1");
  if (!text3.startsWith("%PDF-") || !/\n%%EOF\s*$/u.test(text3)) {
    return false;
  }
  const startXrefMatch = /startxref\s+(\d+)\s+%%EOF\s*$/u.exec(text3);
  if (!startXrefMatch) {
    return false;
  }
  const xrefOffset = Number(startXrefMatch[1]);
  return Number.isSafeInteger(xrefOffset) && xrefOffset >= 0 && xrefOffset < buffer.length && text3.startsWith("xref", xrefOffset);
}
function completionMediaValidation(relativePath, fullPath, sizeBytes, maxBytes = DEFAULT_READ_LIMIT_BYTES) {
  const format = completionMediaFormat(relativePath);
  if (!format) {
    return null;
  }
  const requiredBytes = Math.max(maxBytes, Math.min(sizeBytes, 4 * 1024 * 1024));
  if (sizeBytes > requiredBytes) {
    return {
      satisfied: false,
      status: "malformed",
      reason: `${format.toUpperCase()} completion evidence exceeds the bounded structural validation limit`
    };
  }
  const buffer = readBoundedBuffer(fullPath, requiredBytes);
  const satisfied = format === "svg" ? validSvgBuffer(buffer) : format === "png" ? validPngBuffer(buffer) : format === "jpeg" ? validJpegBuffer(buffer) : validPdfBuffer(buffer);
  return satisfied ? { satisfied: true, status: "existing", reason: null } : {
    satisfied: false,
    status: "malformed",
    reason: `${format.toUpperCase()} completion evidence has a malformed or incomplete file structure`
  };
}
function artifactEvidenceRole(relativePath) {
  const normalized = normalizeProjectRelativePath(relativePath);
  if (!normalized.ok) {
    return "unsupported";
  }
  const normalizedPath = normalized.normalizedPath;
  if (!normalizedPath.startsWith(`${ARTIFACT_PATHS.doveRoot}/`)) {
    return "external-project";
  }
  const declaredRole = ARTIFACT_EVIDENCE_ROLES.get(normalizedPath);
  if (declaredRole) {
    return declaredRole;
  }
  const dynamicRole = DYNAMIC_EVIDENCE_PATTERNS.find(({ pattern }) => pattern.test(normalizedPath))?.role;
  if (dynamicRole) {
    return dynamicRole;
  }
  if (BOOKKEEPING_ARTIFACT_PATHS.has(normalizedPath) || BOOKKEEPING_EVIDENCE_PATTERNS.some((pattern) => pattern.test(normalizedPath))) {
    return "bookkeeping";
  }
  return "unsupported";
}
function isBookkeepingArtifactPath(relativePath) {
  return artifactEvidenceRole(relativePath) === "bookkeeping";
}
function inspectDeclaredPath(root, rawPath, options = {}) {
  const normalized = normalizeProjectRelativePath(rawPath);
  if (!normalized.ok) {
    return {
      path: normalized.path,
      normalizedPath: normalized.normalizedPath ?? null,
      status: "unsafe",
      exists: false,
      file: false,
      reason: normalized.reason
    };
  }
  const rootPath = path9.resolve(root);
  const fullPath = path9.resolve(rootPath, normalized.normalizedPath);
  const relativeToRoot = path9.relative(rootPath, fullPath);
  if (relativeToRoot.startsWith("..") || path9.isAbsolute(relativeToRoot)) {
    return {
      path: normalized.path,
      normalizedPath: normalized.normalizedPath,
      status: "unsafe",
      exists: false,
      file: false,
      reason: "resolved path escapes the project root"
    };
  }
  let realRootPath;
  let realFullPath;
  let canonicalRelativePath;
  let stat;
  try {
    realRootPath = fs7.realpathSync(rootPath);
    realFullPath = fs7.realpathSync(fullPath);
    const relativeToRealRoot = path9.relative(realRootPath, realFullPath);
    if (relativeToRealRoot === ".." || relativeToRealRoot.startsWith(`..${path9.sep}`) || path9.isAbsolute(relativeToRealRoot)) {
      return {
        path: normalized.path,
        normalizedPath: normalized.normalizedPath,
        status: "unsafe",
        exists: true,
        file: false,
        reason: "real path escapes the project root"
      };
    }
    canonicalRelativePath = relativeToRealRoot.split(path9.sep).join("/");
    stat = fs7.statSync(realFullPath);
  } catch (error) {
    if (error?.code === "ENOENT") {
      return {
        path: normalized.path,
        normalizedPath: normalized.normalizedPath,
        status: "missing",
        exists: false,
        file: false,
        reason: "path does not exist"
      };
    }
    return {
      path: normalized.path,
      normalizedPath: normalized.normalizedPath,
      status: "unreadable",
      exists: false,
      file: false,
      reason: error instanceof Error ? error.message : String(error)
    };
  }
  if (stat.isDirectory()) {
    return {
      path: normalized.path,
      normalizedPath: normalized.normalizedPath,
      status: "directory",
      exists: true,
      file: false,
      sizeBytes: stat.size,
      reason: "path is a directory"
    };
  }
  if (!stat.isFile()) {
    return {
      path: normalized.path,
      normalizedPath: normalized.normalizedPath,
      status: "unsupported",
      exists: true,
      file: false,
      sizeBytes: stat.size,
      reason: "path is not a regular file"
    };
  }
  const declaredRole = artifactEvidenceRole(normalized.normalizedPath);
  const canonicalRole = artifactEvidenceRole(canonicalRelativePath);
  const baseItem = {
    path: normalized.path,
    normalizedPath: normalized.normalizedPath,
    canonicalRelativePath,
    evidenceRole: declaredRole,
    canonicalEvidenceRole: canonicalRole,
    status: "existing",
    exists: true,
    file: true,
    sizeBytes: stat.size
  };
  if (options.rejectBookkeeping) {
    const rejectedRole = [declaredRole, canonicalRole].find((role) => role === "bookkeeping" || options.requireSemanticEvidence === true && role === "unsupported");
    if (rejectedRole) {
      return {
        ...baseItem,
        status: rejectedRole,
        reason: rejectedRole === "unsupported" ? "path is not an approved Dove completion-evidence artifact" : "path is a navigation, status, runtime, task, or ledger record rather than substantive work evidence"
      };
    }
  }
  if (options.requireNonEmpty && stat.size <= 0) {
    return {
      ...baseItem,
      status: "empty",
      reason: "path is an empty file"
    };
  }
  const needsSemanticRead = options.requireSemanticEvidence === true && [declaredRole, canonicalRole].includes("conditional");
  const mediaRelativePath = options.mediaRelativePath ?? normalized.normalizedPath;
  const needsMediaValidation = options.requireValidMedia === true && Boolean(completionMediaFormat(mediaRelativePath) ?? completionMediaFormat(canonicalRelativePath));
  if (!options.readText && !needsSemanticRead && !needsMediaValidation) {
    return baseItem;
  }
  try {
    const mediaValidation = needsMediaValidation ? completionMediaValidation(
      completionMediaFormat(mediaRelativePath) ? mediaRelativePath : canonicalRelativePath,
      realFullPath,
      stat.size,
      options.maxBytes
    ) : null;
    if (mediaValidation && !mediaValidation.satisfied) {
      return {
        ...baseItem,
        status: mediaValidation.status,
        reason: mediaValidation.reason
      };
    }
    const read = options.readText || needsSemanticRead ? readBoundedText(realFullPath, options.maxBytes ?? DEFAULT_READ_LIMIT_BYTES) : null;
    const semanticInspection = needsSemanticRead ? inspectSemanticEvidence(
      canonicalRole === "conditional" ? canonicalRelativePath : normalized.normalizedPath,
      read?.text ?? ""
    ) : null;
    if (semanticInspection && !semanticInspection.satisfied) {
      return {
        ...baseItem,
        status: semanticInspection.status,
        reason: semanticInspection.reason,
        bytesRead: read?.bytesRead ?? 0,
        truncated: stat.size > (read?.bytesRead ?? 0)
      };
    }
    return {
      ...baseItem,
      ...options.readText ? { text: read?.text ?? "" } : {},
      ...read ? { bytesRead: read.bytesRead, truncated: stat.size > read.bytesRead } : {}
    };
  } catch (error) {
    return {
      path: normalized.path,
      normalizedPath: normalized.normalizedPath,
      status: "unreadable",
      exists: true,
      file: true,
      sizeBytes: stat.size,
      reason: error instanceof Error ? error.message : String(error)
    };
  }
}
function publicPathInspection(item) {
  const { text: text3, ...publicItem } = item;
  return publicItem;
}
function summarizePathInspections(inspections) {
  const pathsWithStatus = (status) => inspections.filter((item) => item.status === status).map((item) => item.normalizedPath ?? item.path).filter(Boolean);
  const problemStatuses = /* @__PURE__ */ new Set(["missing", "unsafe", "unreadable", "directory", "unsupported", "unlinked", "empty", "malformed", "placeholder", "bookkeeping"]);
  return {
    declaredPaths: Array.from(new Set(inspections.map((item) => item.path).filter(Boolean))),
    inspectedPaths: Array.from(new Set(inspections.filter((item) => item.status !== "unsafe").map((item) => item.normalizedPath).filter(Boolean))),
    existingPaths: pathsWithStatus("existing"),
    missingPaths: pathsWithStatus("missing"),
    unsafePaths: pathsWithStatus("unsafe"),
    unreadablePaths: pathsWithStatus("unreadable"),
    directoryPaths: pathsWithStatus("directory"),
    unsupportedPaths: pathsWithStatus("unsupported"),
    unlinkedPaths: pathsWithStatus("unlinked"),
    emptyPaths: pathsWithStatus("empty"),
    malformedPaths: pathsWithStatus("malformed"),
    placeholderPaths: pathsWithStatus("placeholder"),
    bookkeepingPaths: pathsWithStatus("bookkeeping"),
    satisfied: inspections.some((item) => item.status === "existing"),
    problemCount: inspections.filter((item) => problemStatuses.has(item.status)).length,
    items: inspections.map(publicPathInspection)
  };
}
function inspectPathEvidence(root, paths, options = {}) {
  return summarizePathInspections(normalizeStringArray8(paths).map((item) => inspectDeclaredPath(root, item, options)));
}
function evidencePathProblemFlags(root, paths, options = {}) {
  const evidencePaths = normalizeStringArray8(paths);
  const localEvidencePaths = evidencePaths.filter((item) => !isExternalArtifactReference(item));
  const externalEvidenceRefs = evidencePaths.filter(isExternalArtifactReference);
  const pathEvidence = inspectPathEvidence(root, localEvidencePaths, {
    requireNonEmpty: true,
    rejectBookkeeping: true,
    requireSemanticEvidence: true,
    ...options
  });
  const flags = [];
  if (evidencePaths.length === 0) {
    flags.push("missing-evidence-links");
  } else if (localEvidencePaths.length === 0) {
    flags.push("missing-evidence-file");
  }
  if (pathEvidence.missingPaths.length > 0) flags.push("missing-evidence-file");
  if (pathEvidence.unsafePaths.length > 0) flags.push("unsafe-evidence-path");
  if (pathEvidence.unreadablePaths.length > 0) flags.push("unreadable-evidence-file");
  if (pathEvidence.directoryPaths.length > 0) flags.push("directory-evidence-file");
  if (pathEvidence.unsupportedPaths.length > 0) flags.push("unsupported-evidence-file");
  if (pathEvidence.emptyPaths.length > 0) flags.push("empty-evidence-file");
  if (pathEvidence.malformedPaths.length > 0) flags.push("malformed-evidence-file");
  if (pathEvidence.placeholderPaths.length > 0) flags.push("placeholder-evidence-file");
  if (pathEvidence.bookkeepingPaths.length > 0) flags.push("bookkeeping-evidence-file");
  return {
    evidencePaths,
    localEvidencePaths,
    externalEvidenceRefs,
    pathEvidence,
    flags: Array.from(new Set(flags)),
    satisfied: pathEvidence.satisfied && pathEvidence.problemCount === 0
  };
}
function pathProblems(summary) {
  return [
    ...summary.missingPaths,
    ...summary.unsafePaths,
    ...summary.unreadablePaths,
    ...summary.directoryPaths,
    ...summary.unsupportedPaths,
    ...summary.unlinkedPaths ?? [],
    ...summary.emptyPaths,
    ...summary.malformedPaths,
    ...summary.placeholderPaths,
    ...summary.bookkeepingPaths
  ];
}
function completionPathApproval(relativePath, canonicalRelativePath, policy) {
  const normalizedPath = normalizeLocalPath(relativePath);
  const canonicalPath = normalizeLocalPath(canonicalRelativePath) ?? normalizedPath;
  const workflowGoalRole = WORKFLOW_GOAL_COMPLETION_EVIDENCE_ROLES.get(normalizedPath) ?? null;
  if (workflowGoalRole && canonicalPath === normalizedPath) {
    return { approved: true, role: workflowGoalRole, linkage: "workflow-goal-fixed" };
  }
  if (policy.linkedPaths.has(normalizedPath)) {
    return {
      approved: true,
      role: artifactEvidenceRole(canonicalPath),
      linkage: policy.contractLinkedPaths.includes(normalizedPath) ? "execution-contract" : policy.requirementLinkedPaths.includes(normalizedPath) ? "requirement" : policy.criterionLinkedPaths.includes(normalizedPath) ? "verified-criterion" : "task"
    };
  }
  const role = artifactEvidenceRole(canonicalPath);
  if (DYNAMIC_COMPLETION_EVIDENCE_PATTERN.test(normalizedPath ?? "")) {
    return {
      approved: false,
      role,
      linkage: null,
      reason: "dynamic .dove/evidence paths require an exact task, execution-contract, or requirement link"
    };
  }
  if (role === "external-project") {
    return {
      approved: false,
      role,
      linkage: null,
      reason: "repository files outside .dove require an exact task, execution-contract, or requirement link"
    };
  }
  return { approved: true, role, linkage: "approved-dove-role" };
}
function applyCompletionPathPolicy(root, inspection, policy, inspectOptions) {
  if (inspection.status !== "existing") {
    return inspection;
  }
  const rejectedRole = [inspection.evidenceRole, inspection.canonicalEvidenceRole].find((role) => role === "bookkeeping") ?? ([inspection.evidenceRole, inspection.canonicalEvidenceRole].every((role) => role === "unsupported") ? "unsupported" : null);
  if (rejectedRole) {
    return {
      ...inspection,
      status: rejectedRole,
      reason: rejectedRole === "unsupported" ? "path is not an approved Dove completion-evidence artifact" : "path is a navigation, status, runtime, task, or ledger record rather than substantive work evidence"
    };
  }
  const approval = completionPathApproval(
    inspection.normalizedPath,
    inspection.canonicalRelativePath,
    policy
  );
  if (!approval.approved) {
    return {
      ...inspection,
      status: "unlinked",
      completionEvidenceRole: approval.role,
      completionLinkage: null,
      reason: approval.reason
    };
  }
  const mediaFormat = completionMediaFormat(inspection.normalizedPath) ?? completionMediaFormat(inspection.canonicalRelativePath);
  if (mediaFormat) {
    const mediaInspection = inspectDeclaredPath(root, inspection.normalizedPath, {
      ...inspectOptions,
      requireValidMedia: true,
      mediaRelativePath: inspection.normalizedPath
    });
    if (mediaInspection.status !== "existing") {
      return {
        ...mediaInspection,
        completionEvidenceRole: approval.role,
        completionLinkage: approval.linkage
      };
    }
  }
  return {
    ...inspection,
    completionEvidenceRole: approval.role,
    completionLinkage: approval.linkage
  };
}
function completionPathEvidence(root, paths, policy, inspectOptions) {
  const inspections = normalizeStringArray8(paths).map((item) => {
    const inspection = inspectDeclaredPath(root, item, {
      ...inspectOptions,
      rejectBookkeeping: false
    });
    return applyCompletionPathPolicy(root, inspection, policy, inspectOptions);
  });
  const summary = summarizePathInspections(inspections);
  return {
    ...summary,
    items: inspections.map(publicPathInspection)
  };
}
function completionPathProblems(summary) {
  return pathProblems(summary);
}
function criterionExpectsNegativeOutcome(value) {
  const criterion = String(value ?? "").trim();
  return /^(?:blocked|failed|held|negative|rejected|unresolved|incomplete)\b/iu.test(criterion) || /\b(?:must|should|is|remains?|returns?|records?|reports?|shows?|surfaces?)\s+(?:be\s+)?(?:blocked|failed|held|negative|rejected|unresolved|incomplete)\b/iu.test(criterion) || /\b(?:reject|block|hold|surface|report|record)\w*\b[^.]{0,80}\b(?:failure|blocked|failed|held|negative|rejected|unresolved|incomplete)\b/iu.test(criterion);
}
function positiveCriterion(criterion) {
  const status = String(criterion?.status ?? "").trim().toLowerCase();
  return ["met", "passed", "verified"].includes(status) && !criterionExpectsNegativeOutcome(criterion?.criterion);
}
function normalizedStatus(value) {
  return String(value ?? "").trim().toLowerCase();
}
function collectValidationSignals(value, pointer = "$", depth = 0, seen = /* @__PURE__ */ new Set()) {
  if (depth > 8 || value === null || value === void 0) {
    return [];
  }
  if (typeof value !== "object") {
    return [];
  }
  if (seen.has(value)) {
    return [];
  }
  seen.add(value);
  if (Array.isArray(value)) {
    return value.flatMap((item, index) => collectValidationSignals(item, `${pointer}[${index}]`, depth + 1, seen));
  }
  const signals = [];
  for (const [key, item] of Object.entries(value)) {
    const itemPointer = `${pointer}.${key}`;
    const normalizedKey = key.toLowerCase();
    if (typeof item === "string" && (/(?:status|verdict|readiness)$/u.test(normalizedKey) || /(?:validation|verification|qa|review|comparison)(?:outcome|mapping)$/u.test(normalizedKey))) {
      const status = normalizedStatus(item);
      if (NEGATIVE_VALIDATION_STATUSES.has(status) || POSITIVE_VALIDATION_STATUSES.has(status)) {
        signals.push({ pointer: itemPointer, field: key, status });
      }
    }
    if (typeof item === "boolean" && /(?:complete|completed|passed|ready|resolved|satisfied|valid)$/u.test(normalizedKey)) {
      signals.push({ pointer: itemPointer, field: key, status: item ? "positive" : "incomplete" });
    }
    if (Array.isArray(item) && item.length > 0 && /(?:issues|openissues|openissueids|unresolved|unresolvedconcerns|unresolvedconcernids|integrityflags|missing|missingartifactrefs|missingrequirementids)$/u.test(normalizedKey.replace(/[^a-z]/gu, ""))) {
      signals.push({ pointer: itemPointer, field: key, status: "unresolved", count: item.length });
    }
    if (item && typeof item === "object") {
      signals.push(...collectValidationSignals(item, itemPointer, depth + 1, seen));
    }
  }
  return signals;
}
function parseValidationArtifact(item) {
  if (item.status !== "existing") {
    return null;
  }
  const canonicalPath = item.canonicalRelativePath ?? item.normalizedPath;
  if (path9.posix.extname(canonicalPath ?? "").toLowerCase() !== ".json") {
    return null;
  }
  try {
    return JSON.parse(item.text ?? "");
  } catch {
    return null;
  }
}
function structuredTextValidationSignals(text3) {
  const signals = [];
  const lines = String(text3 ?? "").split(/\r?\n/u);
  for (let index = 0; index < lines.length; index += 1) {
    const line = lines[index].trim();
    const fieldMatch = /^[-*]?\s*(?:qa\s+)?(?:status|verdict|outcome|readiness|mapping|review verdict|comparison status|audit verdict|bridge status)\s*:\s*([^\s,;]+)/iu.exec(line);
    if (fieldMatch) {
      const status = normalizedStatus(fieldMatch[1]);
      if (NEGATIVE_VALIDATION_STATUSES.has(status) || POSITIVE_VALIDATION_STATUSES.has(status)) {
        signals.push({ pointer: `$text[${index + 1}]`, field: "structured-text", status });
      }
    }
    const unresolvedMatch = /^[-*]?\s*(?:unresolved concerns?|open issues?)\s*:\s*(.+)$/iu.exec(line);
    if (unresolvedMatch && !/^(?:none|no(?:ne)?|0|\[\])\.?$/iu.test(unresolvedMatch[1].trim())) {
      signals.push({ pointer: `$text[${index + 1}]`, field: "structured-text", status: "unresolved" });
    }
  }
  return signals;
}
function negativeOutcomeInspection(root, criterion, pathEvidence, inspectOptions) {
  if (!positiveCriterion(criterion)) {
    return { contradictory: false, paths: [], signals: [] };
  }
  const negativePaths = [];
  const signals = [];
  for (const item of pathEvidence.items ?? []) {
    if (item.status !== "existing") {
      continue;
    }
    const canonicalPath = item.canonicalRelativePath ?? item.normalizedPath;
    const role = item.completionEvidenceRole ?? artifactEvidenceRole(canonicalPath);
    if (!["conditional", "validation"].includes(role)) {
      continue;
    }
    const withText = inspectDeclaredPath(root, item.normalizedPath, { ...inspectOptions, readText: true });
    const parsed = parseValidationArtifact(withText);
    const itemSignals = parsed ? collectValidationSignals(parsed) : structuredTextValidationSignals(withText.text);
    const negativeSignals = itemSignals.filter((signal) => NEGATIVE_VALIDATION_STATUSES.has(signal.status));
    if (negativeSignals.length > 0) {
      negativePaths.push(item.normalizedPath);
      signals.push(...negativeSignals.map((signal) => ({ ...signal, path: item.normalizedPath })));
    }
  }
  return {
    contradictory: negativePaths.length > 0,
    paths: Array.from(new Set(negativePaths)),
    signals
  };
}
function requirementPurpose(requirement) {
  if (requirement.purpose) {
    return requirement.purpose.toLowerCase();
  }
  return Array.from(REQUIREMENT_PURPOSE_PATTERNS.entries()).find(([, pattern]) => pattern.test(requirement.requirement))?.[0] ?? "unspecified";
}
function evidencePurpose(item) {
  const relativePath = String(item.canonicalRelativePath ?? item.normalizedPath ?? "");
  if (relativePath === ARTIFACT_PATHS.experimentAudits) return "audit";
  if (relativePath === ARTIFACT_PATHS.claimBridgeLog) return "bridge";
  if (relativePath === ARTIFACT_PATHS.figureQa) return "qa";
  if ([ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewReport, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.reviewDebateLog].includes(relativePath)) return "review";
  if ([ARTIFACT_PATHS.versionComparisons, ARTIFACT_PATHS.versionComparisonReport].includes(relativePath)) return "comparison";
  const role = item.completionEvidenceRole ?? item.evidenceRole;
  if (role === "validation") return "validation";
  if (role === "substantive" || role === "external-project") return "artifact";
  return "evidence";
}
function requirementCoverage(policy, pathEvidence) {
  const available = (pathEvidence.items ?? []).filter((item) => item.status === "existing");
  const usedEvidenceFiles = /* @__PURE__ */ new Set();
  const coverage = policy.requirements.map((requirement) => {
    const requiredPurpose = requirementPurpose(requirement);
    const exactPaths = new Set(requirement.evidencePaths);
    const matched = available.find((item) => {
      const evidenceFile = item.canonicalRelativePath ?? item.normalizedPath;
      if (usedEvidenceFiles.has(evidenceFile)) {
        return false;
      }
      if (exactPaths.size > 0) {
        return exactPaths.has(item.normalizedPath);
      }
      const itemPurpose = evidencePurpose(item);
      return requiredPurpose === "unspecified" ? false : requiredPurpose === itemPurpose || requiredPurpose === "evidence" && ["artifact", "validation"].includes(itemPurpose);
    });
    if (matched) {
      usedEvidenceFiles.add(matched.canonicalRelativePath ?? matched.normalizedPath);
    }
    return {
      id: requirement.id,
      requirement: requirement.requirement,
      purpose: requiredPurpose,
      source: requirement.source,
      requiredEvidencePaths: requirement.evidencePaths,
      covered: Boolean(matched),
      evidencePath: matched?.normalizedPath ?? null,
      evidencePurpose: matched ? evidencePurpose(matched) : null
    };
  });
  return {
    coverage,
    uncoveredRequirements: coverage.filter((item) => !item.covered),
    coveredRequirements: coverage.filter((item) => item.covered)
  };
}
function completionEvidenceIntegrity(root, evidence = {}, options = {}) {
  const inspectOptions = {
    requireNonEmpty: true,
    rejectBookkeeping: true,
    requireSemanticEvidence: true,
    ...options.inspectOptions ?? {}
  };
  const policy = completionPolicyContext(options);
  const eligibleSourceReferences = new Set(normalizeStringArray8(options.context?.eligibleSourceReferences));
  const evidencePaths = normalizeStringArray8(evidence.evidencePaths);
  const localEvidencePaths = evidencePaths.filter((item) => !isExternalArtifactReference(item));
  const externalEvidenceRefs = evidencePaths.filter(isExternalArtifactReference);
  const sourceEvidenceRefs = externalEvidenceRefs.filter((item) => item.startsWith("source:") || item.startsWith("note:"));
  const eligibleSourceEvidenceRefs = sourceEvidenceRefs.filter((item) => eligibleSourceReferences.has(item));
  const pathEvidence = completionPathEvidence(root, localEvidencePaths, policy, inspectOptions);
  const criteria = (Array.isArray(evidence.verifiedCriteria) ? evidence.verifiedCriteria : []).map((criterion) => {
    const criterionEvidencePaths = normalizeStringArray8(criterion?.evidencePaths);
    const criterionLocalEvidencePaths = criterionEvidencePaths.filter((item) => !isExternalArtifactReference(item));
    const criterionExternalEvidenceRefs = criterionEvidencePaths.filter(isExternalArtifactReference);
    const criterionEligibleSourceEvidenceRefs = criterionExternalEvidenceRefs.filter((item) => item.startsWith("source:") || item.startsWith("note:")).filter((item) => eligibleSourceReferences.has(item));
    const criterionPathEvidence = completionPathEvidence(root, criterionLocalEvidencePaths, policy, inspectOptions);
    const negativeOutcome = negativeOutcomeInspection(root, criterion, criterionPathEvidence, inspectOptions);
    return {
      criterion: criterion?.criterion ?? null,
      status: criterion?.status ?? null,
      evidencePaths: criterionEvidencePaths,
      localEvidencePaths: criterionLocalEvidencePaths,
      externalEvidenceRefs: criterionExternalEvidenceRefs,
      eligibleSourceEvidenceRefs: criterionEligibleSourceEvidenceRefs,
      pathEvidence: criterionPathEvidence,
      negativeOutcome,
      satisfied: (criterionPathEvidence.satisfied || criterionEligibleSourceEvidenceRefs.length > 0) && criterionPathEvidence.problemCount === 0 && negativeOutcome.contradictory === false,
      problemPaths: completionPathProblems(criterionPathEvidence)
    };
  });
  const missingCriteriaEvidence = criteria.filter((item) => !item.satisfied);
  const problemPaths = completionPathProblems(pathEvidence);
  const requirementIntegrity = requirementCoverage(policy, pathEvidence);
  const uncoveredRequirements = requirementIntegrity.uncoveredRequirements;
  const existingEvidencePaths = pathEvidence.existingPaths.filter((item) => !(pathEvidence.unlinkedPaths ?? []).includes(item));
  const substantiveEvidencePaths = [...existingEvidencePaths, ...eligibleSourceEvidenceRefs];
  return {
    declaredPaths: evidencePaths,
    localEvidencePaths,
    externalEvidenceRefs,
    eligibleSourceEvidenceRefs,
    pathEvidence,
    existingEvidencePaths,
    substantiveEvidencePaths,
    problemPaths,
    criteria,
    missingCriteriaEvidence,
    requirementCoverage: requirementIntegrity.coverage,
    coveredRequirements: requirementIntegrity.coveredRequirements,
    uncoveredRequirements,
    semanticContext: {
      taskId: policy.task.id ?? policy.task.packetId ?? null,
      taskLinkedPaths: policy.taskLinkedPaths,
      contractLinkedPaths: policy.contractLinkedPaths,
      requirementLinkedPaths: policy.requirementLinkedPaths
    },
    satisfied: (pathEvidence.satisfied || eligibleSourceEvidenceRefs.length > 0) && missingCriteriaEvidence.length === 0 && problemPaths.length === 0 && uncoveredRequirements.length === 0,
    hasSubstantiveEvidence: substantiveEvidencePaths.length > 0 && problemPaths.length === 0
  };
}

// src/core/review-artifact-snapshot.mjs
import crypto6 from "node:crypto";
import fs8 from "node:fs";
import path10 from "node:path";
function sha256Buffer(value) {
  return crypto6.createHash("sha256").update(value).digest("hex");
}
function sha256File(fullPath) {
  return sha256Buffer(fs8.readFileSync(fullPath));
}
function stableSnapshotSetHash(snapshots = []) {
  const canonical = [...snapshots].map(({ path: artifactPath, sizeBytes, sha256: sha2563 }) => ({ path: artifactPath, sizeBytes, sha256: sha2563 })).sort((left, right) => left.path.localeCompare(right.path));
  return sha256Buffer(`${JSON.stringify(canonical)}
`);
}
function canonicalReviewArtifactPath(root, relativePath, label = "review artifact") {
  const normalized = normalizeProjectRelativePath(relativePath);
  if (!normalized.ok) {
    throw new Error(`${label} has an unsafe path ${relativePath}: ${normalized.reason}.`);
  }
  const inspection = inspectDeclaredPath(root, normalized.normalizedPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  if (inspection.status !== "existing") {
    throw new Error(`${label} is not a usable file at ${normalized.normalizedPath}: ${inspection.reason ?? inspection.status}.`);
  }
  return inspection.canonicalRelativePath ?? inspection.normalizedPath;
}
function snapshotReviewedArtifacts(root, relativePaths, label = "reviewed artifacts") {
  const snapshots = [];
  const seen = /* @__PURE__ */ new Set();
  for (const relativePath of relativePaths ?? []) {
    const canonicalPath = canonicalReviewArtifactPath(root, relativePath, label);
    if (seen.has(canonicalPath)) {
      continue;
    }
    seen.add(canonicalPath);
    const inspection = inspectDeclaredPath(root, canonicalPath, {
      requireNonEmpty: true,
      rejectBookkeeping: true
    });
    snapshots.push({
      path: canonicalPath,
      sizeBytes: inspection.sizeBytes,
      sha256: sha256File(path10.resolve(root, canonicalPath))
    });
  }
  if (snapshots.length === 0) {
    throw new Error(`${label} requires at least one existing non-empty non-bookkeeping reviewed artifact.`);
  }
  snapshots.sort((left, right) => left.path.localeCompare(right.path));
  return {
    reviewedArtifacts: snapshots,
    reviewedArtifactSetSha256: stableSnapshotSetHash(snapshots)
  };
}
function normalizedSnapshot(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return null;
  }
  const normalized = normalizeProjectRelativePath(value.path);
  if (!normalized.ok || !Number.isSafeInteger(value.sizeBytes) || value.sizeBytes <= 0 || typeof value.sha256 !== "string" || !/^[a-f0-9]{64}$/u.test(value.sha256)) {
    return null;
  }
  return {
    path: normalized.normalizedPath,
    sizeBytes: value.sizeBytes,
    sha256: value.sha256
  };
}
function normalizeSnapshotArray(value) {
  if (!Array.isArray(value)) {
    return { ok: false, snapshots: [], reason: "reviewedArtifacts must be an array" };
  }
  const snapshots = value.map(normalizedSnapshot);
  if (snapshots.some((item) => item === null) || snapshots.length === 0) {
    return { ok: false, snapshots: [], reason: "reviewedArtifacts must contain valid non-empty artifact snapshots" };
  }
  const paths = snapshots.map((item) => item.path);
  if (new Set(paths).size !== paths.length) {
    return { ok: false, snapshots: [], reason: "reviewedArtifacts contains duplicate canonical paths" };
  }
  snapshots.sort((left, right) => left.path.localeCompare(right.path));
  return { ok: true, snapshots, reason: null };
}
function snapshotsEqual(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function verifyPreparedReviewSnapshot(root, {
  manifest,
  input,
  inputPath,
  actualInputSha256,
  handoffInputPath,
  handoffInputSha256,
  handoffReviewedArtifactPaths
}) {
  const failures = [];
  if (manifest.inputPath !== inputPath || handoffInputPath !== inputPath) {
    failures.push("input-path-mismatch");
  }
  if (manifest.inputSha256 !== actualInputSha256 || handoffInputSha256 !== actualInputSha256) {
    failures.push("input-hash-mismatch");
  }
  const manifestSnapshots = normalizeSnapshotArray(manifest.reviewedArtifacts);
  const inputSnapshots = normalizeSnapshotArray(input.reviewedArtifacts);
  if (!manifestSnapshots.ok) failures.push(`manifest-${manifestSnapshots.reason}`);
  if (!inputSnapshots.ok) failures.push(`input-${inputSnapshots.reason}`);
  if (manifestSnapshots.ok && inputSnapshots.ok && !snapshotsEqual(manifestSnapshots.snapshots, inputSnapshots.snapshots)) {
    failures.push("manifest-input-snapshot-mismatch");
  }
  const preparedSnapshots = manifestSnapshots.ok ? manifestSnapshots.snapshots : [];
  const expectedSetHash = preparedSnapshots.length > 0 ? stableSnapshotSetHash(preparedSnapshots) : null;
  if (!expectedSetHash || manifest.reviewedArtifactSetSha256 !== expectedSetHash || input.reviewedArtifactSetSha256 !== expectedSetHash) {
    failures.push("reviewed-artifact-set-hash-mismatch");
  }
  const handoffPaths = [];
  for (const rawPath of Array.isArray(handoffReviewedArtifactPaths) ? handoffReviewedArtifactPaths : []) {
    const normalized = normalizeProjectRelativePath(rawPath);
    if (!normalized.ok) {
      failures.push("handoff-unsafe-reviewed-artifact-path");
      continue;
    }
    let canonicalPath = normalized.normalizedPath;
    try {
      canonicalPath = canonicalReviewArtifactPath(root, normalized.normalizedPath, "review handoff artifact");
    } catch {
    }
    handoffPaths.push(canonicalPath);
  }
  if (handoffPaths.length === 0) {
    failures.push("handoff-reviewed-artifacts-missing");
  }
  if (new Set(handoffPaths).size !== handoffPaths.length) {
    failures.push("handoff-duplicate-reviewed-artifact-path");
  }
  const preparedPaths = preparedSnapshots.map((item) => item.path).sort();
  const returnedPaths = [...handoffPaths].sort();
  if (JSON.stringify(preparedPaths) !== JSON.stringify(returnedPaths)) {
    failures.push("handoff-reviewed-artifact-set-mismatch");
  }
  for (const prepared of preparedSnapshots) {
    const inspection = inspectDeclaredPath(root, prepared.path, {
      requireNonEmpty: true,
      rejectBookkeeping: true
    });
    if (inspection.status !== "existing") {
      failures.push(`reviewed-artifact-${inspection.status}:${prepared.path}`);
      continue;
    }
    const canonicalPath = inspection.canonicalRelativePath ?? inspection.normalizedPath;
    const current = {
      path: canonicalPath,
      sizeBytes: inspection.sizeBytes,
      sha256: sha256File(path10.resolve(root, canonicalPath))
    };
    if (!snapshotsEqual([prepared], [current])) {
      failures.push(`reviewed-artifact-changed:${prepared.path}`);
    }
  }
  return {
    ok: failures.length === 0,
    failures: Array.from(new Set(failures)),
    reviewedArtifacts: preparedSnapshots,
    reviewedArtifactSetSha256: expectedSetHash
  };
}

// src/core/review-proof.mjs
var UNRESOLVED_CONCERN_STATUSES = /* @__PURE__ */ new Set([
  "open",
  "awaiting-author-response",
  "author-response-submitted",
  "escalated",
  "contested"
]);
function readJsonFile(fullPath) {
  try {
    return JSON.parse(fs9.readFileSync(fullPath, "utf8"));
  } catch {
    return null;
  }
}
function normalizeExpectedPaths(relativePaths = []) {
  const paths = [];
  for (const relativePath of relativePaths) {
    const normalized = normalizeProjectRelativePath(relativePath);
    if (!normalized.ok) return { ok: false, paths: [], reason: `unsafe-expected-artifact:${relativePath}` };
    paths.push(normalized.normalizedPath);
  }
  return { ok: true, paths: Array.from(new Set(paths)).sort(), reason: null };
}
function normalizeStringArray9(value) {
  return Array.isArray(value) ? Array.from(new Set(value.filter((item) => typeof item === "string").map((item) => item.trim()).filter(Boolean))) : [];
}
function sameStringSet(left, right) {
  return JSON.stringify(normalizeStringArray9(left).sort()) === JSON.stringify(normalizeStringArray9(right).sort());
}
function proofFailure(manifestPath, failures, details = {}) {
  return {
    ok: false,
    authoritative: false,
    proof: null,
    manifestPath,
    failures: Array.from(new Set(failures)),
    ...details
  };
}
function workspaceReviewVerdict(perPacket = {}) {
  const packetStates = Object.values(perPacket);
  if (packetStates.some((item) => item?.verdict === "blocked")) return "blocked";
  if (packetStates.some((item) => item?.verdict === "needs-evidence")) return "needs-evidence";
  if (packetStates.some((item) => item?.verdict === "needs-revision")) return "needs-revision";
  return packetStates.length > 0 ? "coherent" : "not-reviewed";
}
function isOpenReviewProofConcern(item) {
  if (!item || typeof item !== "object" || ["resolved", "retired"].includes(item.status)) {
    return false;
  }
  return String(item.id ?? "").endsWith("-review-proof-required") || item.responseOwnerRole === "reviewer" && /Reviewer runtime execution claim|Reviewer proof/u.test(String(item.summary ?? ""));
}
function openReviewProofRequiredBoundaries(root) {
  const concerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  return (concerns.items ?? []).filter(isOpenReviewProofConcern);
}
function assertReviewProofBoundaryTransition(root, currentBoard, nextPhase = currentBoard?.currentPhase, nextAssignedRole = currentBoard?.assignedRole, actionLabel = "Updating workflow ownership") {
  const boundaries = openReviewProofRequiredBoundaries(root);
  if (boundaries.length > 0 && (nextPhase !== "review" || nextAssignedRole !== "reviewer")) {
    const boundaryIds = boundaries.map((item) => item.id).filter(Boolean).join(", ");
    throw new Error(`${actionLabel} cannot leave review/reviewer while review-proof-required remains open (${boundaryIds || "open Reviewer proof boundary"}); only authorized independent Reviewer proof can resolve this boundary.`);
  }
  return boundaries;
}
function verifyImportedReviewProofIntegrity(root, manifestPath, options = {}) {
  const manifest = readJsonFile(manifestPath);
  if (!manifest || typeof manifest !== "object") return proofFailure(manifestPath, ["manifest-unreadable"]);
  const failures = [];
  if (manifest.status !== "imported") failures.push("manifest-not-imported");
  if (manifest.verdict !== "coherent") failures.push("manifest-verdict-not-coherent");
  if (options.packetId && manifest.packetId !== options.packetId) failures.push("packet-mismatch");
  if (!options.isolationModel) failures.push("expected-isolation-model-missing");
  if (!options.handoffSha256Field) failures.push("handoff-hash-field-missing");
  const inputPath = normalizeProjectRelativePath(manifest.inputPath);
  const handoffPath = normalizeProjectRelativePath(manifest.handoffPath);
  const reportPath = normalizeProjectRelativePath(manifest.reportPath);
  if (!inputPath.ok) failures.push("unsafe-input-path");
  if (!handoffPath.ok) failures.push("unsafe-handoff-path");
  if (!reportPath.ok) failures.push("unsafe-report-path");
  if (failures.length > 0) return proofFailure(manifestPath, failures, { runId: manifest.runId ?? null });
  const inputFullPath = path11.resolve(root, inputPath.normalizedPath);
  const handoffFullPath = path11.resolve(root, handoffPath.normalizedPath);
  const reportFullPath = path11.resolve(root, reportPath.normalizedPath);
  const input = readJsonFile(inputFullPath);
  const handoff = readJsonFile(handoffFullPath);
  if (!input) failures.push("input-unreadable");
  if (!handoff) failures.push("handoff-unreadable");
  const reportInspection = inspectDeclaredPath(root, reportPath.normalizedPath, { requireNonEmpty: true, rejectBookkeeping: true });
  if (reportInspection.status !== "existing") failures.push(`report-${reportInspection.status}`);
  if (failures.length > 0) return proofFailure(manifestPath, failures, { runId: manifest.runId ?? null });
  const actualInputSha256 = sha256File(inputFullPath);
  const verification = verifyPreparedReviewSnapshot(root, {
    manifest,
    input,
    inputPath: inputPath.normalizedPath,
    actualInputSha256,
    handoffInputPath: handoff.inputPath,
    handoffInputSha256: handoff.inputSha256,
    handoffReviewedArtifactPaths: handoff.reviewedArtifactPaths
  });
  failures.push(...verification.failures);
  if (input.runId !== manifest.runId) failures.push("input-run-id-mismatch");
  if (input.packetId !== manifest.packetId) failures.push("input-packet-mismatch");
  if (!sameStringSet(input.includedPacketIds, manifest.includedPacketIds)) failures.push("included-packet-set-mismatch");
  if (handoff.runId !== manifest.runId) failures.push("handoff-run-id-mismatch");
  if (handoff.status !== "completed") failures.push("handoff-not-completed");
  if (handoff.verdict !== "coherent") failures.push("handoff-verdict-not-coherent");
  if (input.outputContract?.handoffPath !== handoffPath.normalizedPath || manifest.handoffPath !== input.outputContract?.handoffPath) failures.push("output-contract-handoff-path-mismatch");
  if (input.outputContract?.reportPath !== reportPath.normalizedPath || handoff.reportPath !== reportPath.normalizedPath || manifest.reportPath !== input.outputContract?.reportPath) failures.push("output-contract-report-path-mismatch");
  if (!sameStringSet(manifest.reviewedArtifactPaths, input.reviewedArtifactPaths)) failures.push("manifest-input-reviewed-path-set-mismatch");
  if (!sameStringSet(input.reviewedArtifactPaths, verification.reviewedArtifacts.map((item) => item.path))) failures.push("input-snapshot-reviewed-path-set-mismatch");
  if (input.isolationModel !== options.isolationModel) failures.push("isolation-model-mismatch");
  if (input.privacyBoundary?.writerPrivateTranscriptShared !== false || input.privacyBoundary?.reviewerPrivateTranscriptShouldReturn !== false) failures.push("privacy-boundary-mismatch");
  if (options.isolationModel === "audio-final-plan-results-explicit-artifacts") {
    if (input.contextPolicy !== DOVE_AUDIO_CONTEXT_POLICY) failures.push("audio-context-policy-mismatch");
    if (input.privacyBoundary?.projectContextShared !== false || input.privacyBoundary?.orchestrationBoardShared !== false) failures.push("audio-context-boundary-mismatch");
  }
  const actualHandoffSha256 = sha256File(handoffFullPath);
  const actualReportSha256 = sha256File(reportFullPath);
  if (manifest[options.handoffSha256Field] !== actualHandoffSha256) failures.push("handoff-hash-mismatch");
  if (manifest.reportSha256 !== actualReportSha256) failures.push("report-hash-mismatch");
  const expected = normalizeExpectedPaths(options.reviewedArtifactPaths ?? []);
  if (!expected.ok) failures.push(expected.reason);
  const reviewedPaths = verification.reviewedArtifacts.map((item) => item.path).sort();
  if (expected.ok && expected.paths.length > 0 && JSON.stringify(expected.paths) !== JSON.stringify(reviewedPaths)) failures.push("expected-artifact-set-mismatch");
  if (failures.length > 0) return proofFailure(manifestPath, failures, { runId: manifest.runId ?? null });
  return {
    ok: false,
    authoritative: false,
    proof: null,
    failures: ["reviewer-runtime-authorization-required"],
    manifestPath: path11.relative(root, manifestPath).split(path11.sep).join("/"),
    runId: manifest.runId,
    packetId: manifest.packetId ?? null,
    snapshotIntegrityVerified: true,
    inputSha256: actualInputSha256,
    handoffSha256: actualHandoffSha256,
    reportSha256: actualReportSha256,
    reviewedArtifacts: verification.reviewedArtifacts,
    reviewedArtifactSetSha256: verification.reviewedArtifactSetSha256
  };
}
function verifyImportedIsolatedReviewProof(root, manifestPath, options = {}) {
  return verifyImportedReviewProofIntegrity(root, manifestPath, {
    ...options,
    isolationModel: "parallel-session-file-handoff",
    handoffSha256Field: "outputSha256"
  });
}
function recordReviewProofRequiredBoundary(root, {
  reviewKind,
  runId,
  packetId,
  includedPacketIds = [packetId],
  timestamp,
  handoffPath,
  reportPath,
  reviewedArtifactPaths = []
}) {
  const normalizedKind = String(reviewKind ?? "review").trim().toLowerCase().replace(/[^a-z0-9]+/g, "-") || "review";
  const label = normalizedKind === "audio" ? "audio review" : "isolated review";
  const recordedAt = timestamp ?? nowIso();
  const summary = `Imported coherent ${label} material is non-authoritative until an approved Reviewer runtime execution claim submits proof through the private capability.`;
  const proofBoundary = {
    id: `${normalizedKind}-${runId}-review-proof-required`,
    packetId,
    summary,
    severity: "high",
    status: "open",
    raisedByRole: "reviewer",
    responseOwnerRole: "reviewer",
    reviewerRationale: "Public prepare/import, caller-supplied reviewerId, and caller-written report or manifest cannot authenticate Reviewer authority.",
    rulingOutcome: "pending",
    reviewerDisposition: "pending",
    recurrenceCount: 1,
    firstSeenAt: recordedAt,
    lastSeenAt: recordedAt,
    linkedArtifactPaths: Array.from(new Set([handoffPath, reportPath, ...reviewedArtifactPaths].filter(Boolean))),
    claimIds: [],
    experimentIds: [],
    updatedAt: nowIso()
  };
  const concerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const concernItems = [...(concerns.items ?? []).filter((item) => item.id !== proofBoundary.id), proofBoundary];
  writeJson(root, ARTIFACT_PATHS.reviewConcerns, {
    ...concerns,
    version: 2,
    items: concernItems,
    updatedAt: nowIso()
  });
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, { version: 4, history: [], perPacket: {}, lastVerdict: "not-reviewed", unresolvedConcernIds: [] });
  const reviewEntry = {
    id: `${normalizedKind}-${runId}`,
    packetId,
    includedPacketIds,
    timestamp: recordedAt,
    stage: `${normalizedKind}-review-import`,
    scope: summary,
    verdict: "needs-evidence",
    importedVerdict: "coherent",
    authoritative: false,
    summary,
    reviewRequiredBeforeFinalize: true,
    reportPath,
    reviewedArtifactPaths,
    resolvedConcernIds: [],
    findings: [{ severity: "high", summary, responseOwnerRole: "reviewer", methodologicalCategory: "review-proof" }],
    actionItems: ["Submit proof from an approved Reviewer runtime execution claim through the private capability."],
    independentReviewProof: null
  };
  const history = [...(reviewState.history ?? []).filter((item) => item.id !== reviewEntry.id), reviewEntry];
  const unresolvedConcernIds = concernItems.filter((item) => UNRESOLVED_CONCERN_STATUSES.has(item.status)).map((item) => item.id);
  const packetUnresolvedConcernIds = concernItems.filter((item) => item.packetId === packetId && UNRESOLVED_CONCERN_STATUSES.has(item.status)).map((item) => item.id);
  const perPacket = {
    ...reviewState.perPacket ?? {},
    [packetId]: {
      packetId,
      includedPacketIds,
      verdict: "needs-evidence",
      reviewedAt: recordedAt,
      reviewId: reviewEntry.id,
      reviewedArtifactPaths,
      unresolvedConcernIds: packetUnresolvedConcernIds
    }
  };
  const workspaceVerdict = workspaceReviewVerdict(perPacket);
  const openItems = Array.from(/* @__PURE__ */ new Set([
    ...normalizeStringArray9(reviewState.openItems),
    ...reviewEntry.actionItems
  ]));
  writeJson(root, ARTIFACT_PATHS.reviewState, {
    ...reviewState,
    version: 4,
    history,
    perPacket,
    lastVerdict: workspaceVerdict,
    lastReviewedAt: recordedAt,
    openItems,
    unresolvedConcernIds
  });
  return { proofBoundary, reviewEntry };
}
function findCurrentIndependentReviewProof() {
  return {
    ok: false,
    authoritative: false,
    proof: null,
    failures: ["current-authorized-hash-bound-isolated-review-proof-missing"]
  };
}

// src/core/orchestration.mjs
var ALLOWED_TRANSITIONS = {
  init: ["init", "sources", "research"],
  sources: ["sources", "notes", "research"],
  notes: ["notes", "research", "plan"],
  research: ["research", "plan", "experiments", "review"],
  plan: ["plan", "outline", "research"],
  outline: ["outline", "draft", "research"],
  draft: ["draft", "experiments", "citations", "review"],
  experiments: ["experiments", "draft", "review"],
  citations: ["citations", "review", "draft"],
  review: ["review", "rebuttal", "plan", "research", "draft"],
  rebuttal: ["rebuttal", "versions", "draft"],
  versions: ["versions", "review", "checklist"],
  checklist: ["checklist", "research", "plan", "draft"]
};
var PHASE_ROLE_OWNERS = {
  init: "planner",
  sources: "builder",
  notes: "builder",
  research: "builder",
  plan: "planner",
  outline: "planner",
  draft: "builder",
  experiments: "builder",
  citations: "builder",
  review: "reviewer",
  rebuttal: "builder",
  versions: "planner",
  checklist: "planner"
};
var GOVERNANCE_LIFECYCLE_STATUSES = /* @__PURE__ */ new Set([
  "queued",
  "active",
  "waiting",
  "blocked",
  "review-needed",
  "ready-for-handoff",
  "stale",
  "archived",
  "archived-with-lineage"
]);
var ACTIVE_WORK_STATUSES = /* @__PURE__ */ new Set(["in-progress", "active", "current", "working"]);
var WAITING_WORK_STATUSES = /* @__PURE__ */ new Set(["pending", "planned", "queued", "open", "paused", "blocked"]);
var REVIEW_WORK_STATUSES = /* @__PURE__ */ new Set(["review-needed", "needs-review", "awaiting-review", "review"]);
var TERMINAL_TASK_STATUSES = /* @__PURE__ */ new Set(["done", "cancelled"]);
var TERMINAL_BLOCKER_STATUSES = /* @__PURE__ */ new Set(["resolved", "retired"]);
function slugify3(value) {
  return String(value).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function normalizeStringArray10(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean))) : [];
}
function localizedText(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function workflowGuidanceSummary(root, args = {}, details = {}) {
  const packet = details.packet ?? null;
  const stage = details.stage ?? packet?.stage ?? null;
  const domain = details.domain ?? packet?.domain ?? null;
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: details.surface,
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: details.request ?? args.goal ?? args.objective ?? args.title ?? args.summary ?? args.scope ?? null,
    roleId: details.roleId,
    subagentSpecialty: details.subagentSpecialty,
    packet,
    currentContext: {
      domain,
      stage,
      primaryRole: details.roleId ?? null
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: details.nextAction ?? null,
    routeHint: details.routeHint ?? details.nextAction ?? null,
    workflowKind: details.workflowKind,
    domain,
    stage,
    tags: details.tags ?? [],
    statusSummary: details.statusSummary
  }));
}
function openRebuttalIssueCount(items = []) {
  return items.filter((issue) => issue.status !== "resolved").length;
}
function rebuttalIssuesResultCard(root, args = {}, items = []) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const openIssues = openRebuttalIssueCount(items);
  return buildCommandResultCard({
    surface: "dove.review",
    command: "normalize_rebuttal_issues",
    title: localizedText(responseLanguage, "\u5BA1\u7A3F\u95EE\u9898\u5DF2\u6574\u7406", "Review issues organized"),
    status: openIssues > 0 ? "needs-rebuttal" : "coherent",
    happened: items.length > 0 ? localizedText(responseLanguage, `\u5DF2\u628A ${items.length} \u6761\u5BA1\u7A3F\u95EE\u9898\u6574\u7406\u6210\u56DE\u5E94\u6E05\u5355\u3002`, `Organized ${items.length} review issues into a response list.`) : localizedText(responseLanguage, "\u5F53\u524D\u6CA1\u6709\u53EF\u6574\u7406\u7684\u5BA1\u7A3F\u95EE\u9898\u3002", "No review issues were available to organize."),
    durableWrites: [localizedText(responseLanguage, "\u5BA1\u7A3F\u95EE\u9898\u6E05\u5355\u548C\u4E0B\u4E00\u6B65\u5DE5\u4F5C\u72B6\u6001\u5DF2\u66F4\u65B0\u3002", "Review issue list and next-step work state were updated.")],
    evidence: items.length > 0 ? [localizedText(responseLanguage, `\u5176\u4E2D ${openIssues} \u6761\u8FD8\u9700\u8981\u56DE\u5E94\u6216\u4FEE\u8BA2\u3002`, `${openIssues} still need a response or revision.`)] : [],
    validation: [openIssues > 0 ? localizedText(responseLanguage, "\u56DE\u5E94\u524D\u8FD8\u8981\u9010\u6761\u7ED1\u5B9A\u8BC1\u636E\u6216\u660E\u786E\u7F3A\u53E3\u3002", "Each response still needs evidence or an explicit gap before finalizing.") : localizedText(responseLanguage, "\u6CA1\u6709\u65B0\u7684\u5F85\u56DE\u5E94\u95EE\u9898\u3002", "There are no new response items.")],
    scope: { issueCount: items.length, openIssues },
    nextActions: [{
      title: openIssues > 0 ? localizedText(responseLanguage, "\u6574\u7406\u56DE\u5E94\u7B56\u7565", "Build the response strategy") : localizedText(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u9009\u62E9\u4E0B\u4E00\u6B65", "Return to status for the next step"),
      why: openIssues > 0 ? localizedText(responseLanguage, "\u95EE\u9898\u5DF2\u7ECF\u5F52\u5E76\uFF0C\u4E0B\u4E00\u6B65\u8981\u51B3\u5B9A\u54EA\u4E9B\u6539\u6B63\u6587\u3001\u54EA\u4E9B\u8865\u5B9E\u9A8C\u3001\u54EA\u4E9B\u53EA\u6F84\u6E05\u3002", "The issues are grouped; next decide what needs text changes, experiments, or clarification.") : localizedText(responseLanguage, "\u6CA1\u6709\u5F85\u56DE\u5E94\u95EE\u9898\u65F6\uFF0C\u5E94\u56DE\u5230\u6574\u4F53\u72B6\u6001\u51B3\u5B9A\u7EE7\u7EED\u5199\u4F5C\u3001review \u6216\u6536\u5C3E\u3002", "With no response items, return to the overall status and choose drafting, review, or closure.")
    }]
  }, responseLanguage);
}
function rebuttalStrategyResultCard(root, args = {}, issueCount = 0) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  return buildCommandResultCard({
    surface: "dove.rebuttal",
    command: "build_rebuttal_strategy",
    title: localizedText(responseLanguage, "\u56DE\u5E94\u7B56\u7565\u5DF2\u5F62\u6210", "Response strategy drafted"),
    status: "drafted",
    happened: localizedText(responseLanguage, `\u5DF2\u56F4\u7ED5 ${issueCount} \u6761\u95EE\u9898\u5F62\u6210\u56DE\u5E94\u7B56\u7565\u548C\u56DE\u590D\u8349\u7A3F\u3002`, `Drafted a response strategy and reply text for ${issueCount} issues.`),
    durableWrites: [localizedText(responseLanguage, "\u56DE\u5E94\u7B56\u7565\u548C\u56DE\u590D\u8349\u7A3F\u5DF2\u66F4\u65B0\u3002", "Response strategy and reply draft were updated.")],
    evidence: [localizedText(responseLanguage, "\u6BCF\u6761\u56DE\u5E94\u4ECD\u9700\u8981\u6700\u7EC8\u6838\u5BF9\u8BC1\u636E\u548C\u63AA\u8F9E\u3002", "Each response still needs final evidence and wording checks.")],
    validation: [localizedText(responseLanguage, "\u6700\u7EC8\u53D1\u9001\u524D\u8FD8\u9700\u8981\u72EC\u7ACB review\u3002", "An independent review is still needed before finalizing.")],
    scope: { issueCount },
    nextActions: [{
      title: localizedText(responseLanguage, "\u9001\u53BB review \u68C0\u67E5\u56DE\u5E94\u662F\u5426\u7AD9\u5F97\u4F4F", "Review whether the responses hold up"),
      why: localizedText(responseLanguage, "\u56DE\u5E94\u8349\u7A3F\u5DF2\u7ECF\u6709\u4E86\uFF0C\u4E0B\u4E00\u6B65\u8981\u786E\u8BA4\u5B83\u6CA1\u6709\u8FC7\u5EA6\u627F\u8BFA\uFF0C\u4E5F\u6CA1\u6709\u7F3A\u8BC1\u636E\u3002", "The response draft exists; next confirm it does not over-promise or lack evidence.")
    }]
  }, responseLanguage);
}
function missingRebuttalIssuesResult(root, args = {}, command = "build_rebuttal_strategy") {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const requiredActions = ["review-or-import-rebuttal-issues", "normalize-rebuttal-issues"];
  const boundary = {
    id: "rebuttal-normalized-issues-required",
    type: "missing-required-materials",
    reason: "At least one real issue in the canonical normalized rebuttal issues index is required before rebuttal artifacts can be written.",
    requiredInputs: [ARTIFACT_PATHS.rebuttalIssues],
    requiredActions,
    artifactRefs: [],
    nextAction: "project:dove.review",
    ownerRole: "reviewer",
    nextRole: "reviewer"
  };
  return {
    status: "missing-required-materials",
    issueCount: 0,
    boundary,
    boundaryType: boundary.type,
    requiredActions,
    artifactRefs: [],
    nextAction: boundary.nextAction,
    artifacts: [],
    resultCard: buildCommandResultCard({
      surface: "dove.rebuttal",
      command,
      title: localizedText(responseLanguage, "\u7F3A\u5C11\u89C4\u8303\u5316\u5BA1\u7A3F\u95EE\u9898", "Normalized review issues required"),
      status: "missing-required-materials",
      happened: localizedText(responseLanguage, "\u672A\u5199\u5165\u56DE\u5E94\u7B56\u7565\u3001\u56DE\u590D\u8349\u7A3F\u6216 rebuttal draft\u3002", "No response strategy, response draft, or rebuttal draft was written."),
      durableWrites: [],
      boundary,
      nextActions: [{
        title: localizedText(responseLanguage, "\u5148 review \u5E76\u89C4\u8303\u5316\u95EE\u9898", "Review and normalize issues first"),
        why: localizedText(responseLanguage, "\u56DE\u5E94\u5FC5\u987B\u4EE5 canonical normalized issues index \u4E2D\u7684\u771F\u5B9E\u95EE\u9898\u4E3A\u524D\u7F6E\u6750\u6599\u3002", "A rebuttal requires real issues from the canonical normalized issues index as prerequisite material.")
      }]
    }, responseLanguage)
  };
}
function versionSnapshotResultCard(root, args = {}, snapshot = {}, { sectionCount = 0, claimCount = 0, reviewVerdict = "not-reviewed" } = {}) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  return buildCommandResultCard({
    surface: "dove.version",
    command: "create_version_snapshot",
    title: localizedText(responseLanguage, "\u7248\u672C\u5FEB\u7167\u5DF2\u521B\u5EFA", "Version snapshot created"),
    status: "snapshot-created",
    happened: localizedText(responseLanguage, "\u5DF2\u4FDD\u5B58\u5F53\u524D\u8BBA\u6587\u548C\u4EFB\u52A1\u72B6\u6001\uFF0C\u4F5C\u4E3A\u540E\u7EED\u6BD4\u8F83\u57FA\u7EBF\u3002", "Saved the current paper and task state as a comparison baseline."),
    durableWrites: [localizedText(responseLanguage, "\u7248\u672C\u8BB0\u5F55\u548C\u9879\u76EE\u5BFC\u822A\u5DF2\u66F4\u65B0\u3002", "Version records and project navigation were updated.")],
    evidence: [localizedText(responseLanguage, `\u5F53\u524D\u7EB3\u5165 ${sectionCount} \u4E2A\u7AE0\u8282\u548C ${claimCount} \u6761\u8BBA\u70B9\u3002`, `Captured ${sectionCount} sections and ${claimCount} claims.`)],
    validation: [reviewVerdict === "coherent" ? localizedText(responseLanguage, "\u6700\u8FD1 review \u72B6\u6001\u663E\u793A\u6750\u6599\u57FA\u672C\u81EA\u6D3D\u3002", "The latest review state says the material is coherent.") : localizedText(responseLanguage, "\u5FEB\u7167\u53EA\u662F\u8BB0\u5F55\u5F53\u524D\u72B6\u6001\uFF0C\u4E0D\u4EE3\u8868 review \u5DF2\u901A\u8FC7\u3002", "The snapshot records current state; it does not mean review has passed.")],
    scope: { label: snapshot.label ?? null, sectionCount, claimCount, review: reviewVerdict },
    nextActions: [{
      title: localizedText(responseLanguage, "\u9700\u8981\u65F6\u518D\u6BD4\u8F83\u4E24\u4E2A\u7248\u672C", "Compare versions when needed"),
      why: localizedText(responseLanguage, "\u5FEB\u7167\u5DF2\u7ECF\u6210\u4E3A\u57FA\u7EBF\uFF1B\u53EA\u6709\u65B9\u5411\u6216\u6750\u6599\u6709\u53D8\u5316\u65F6\uFF0C\u6BD4\u8F83\u624D\u6709\u4EF7\u503C\u3002", "The snapshot is now a baseline; comparison matters when direction or material changes.")
    }]
  }, responseLanguage);
}
function versionComparisonResultCard(root, args = {}, comparison = {}) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const changedSections = comparison.changedDraftSections?.length ?? 0;
  const addedClaims = comparison.addedClaimIds?.length ?? 0;
  const addedEvidence = comparison.addedEvidenceLinks?.length ?? 0;
  const addedCitations = comparison.addedCitationKeys?.length ?? 0;
  const concernDelta = (comparison.unresolvedConcernsAdded?.length ?? 0) + (comparison.unresolvedConcernsRemoved?.length ?? 0);
  const hasMaterialChanges = Boolean(comparison.objectiveChanged || comparison.thesisChanged || comparison.verdictChanged || changedSections > 0 || addedClaims > 0 || addedEvidence > 0 || addedCitations > 0 || concernDelta > 0);
  return buildCommandResultCard({
    surface: "dove.version",
    command: "compare_versions",
    title: localizedText(responseLanguage, "\u7248\u672C\u5DEE\u5F02\u5DF2\u6574\u7406", "Version differences summarized"),
    status: hasMaterialChanges ? "changed" : "no-material-change",
    happened: hasMaterialChanges ? localizedText(responseLanguage, "\u5DF2\u6574\u7406\u4E24\u4E2A\u7248\u672C\u4E4B\u95F4\u7684\u5173\u952E\u53D8\u5316\u3002", "Summarized the key differences between the two versions.") : localizedText(responseLanguage, "\u4E24\u4E2A\u7248\u672C\u4E4B\u95F4\u6CA1\u6709\u53D1\u73B0\u5173\u952E\u53D8\u5316\u3002", "No material difference was found between the two versions."),
    durableWrites: [localizedText(responseLanguage, "\u7248\u672C\u6BD4\u8F83\u6458\u8981\u5DF2\u66F4\u65B0\u3002", "Version comparison summary was updated.")],
    evidence: [localizedText(responseLanguage, `\u53D8\u5316\u5305\u62EC ${changedSections} \u4E2A\u8349\u7A3F\u6BB5\u843D\u3001${addedClaims} \u6761\u65B0\u589E\u8BBA\u70B9\u3001${addedEvidence} \u6761\u65B0\u589E\u8BC1\u636E\u3002`, `Changes include ${changedSections} draft sections, ${addedClaims} new claims, and ${addedEvidence} new evidence items.`)],
    validation: [comparison.objectiveChanged || comparison.thesisChanged ? localizedText(responseLanguage, "\u76EE\u6807\u6216\u6838\u5FC3\u8BBA\u70B9\u53D1\u751F\u53D8\u5316\uFF0C\u9700\u8981\u4EBA\u5DE5\u786E\u8BA4\u65B9\u5411\u3002", "The objective or thesis changed, so the direction needs operator confirmation.") : localizedText(responseLanguage, "\u76EE\u6807\u548C\u6838\u5FC3\u8BBA\u70B9\u6CA1\u6709\u53D8\u5316\u3002", "The objective and thesis did not change.")],
    scope: { changedSections, addedClaims, addedEvidence, addedCitations, concernDelta, reviewChanged: Boolean(comparison.verdictChanged) },
    nextActions: [{
      title: hasMaterialChanges ? localizedText(responseLanguage, "\u628A\u5DEE\u5F02\u8F6C\u6210\u4FEE\u8BA2\u8BF4\u660E", "Turn the differences into a revision note") : localizedText(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u9009\u62E9\u4E0B\u4E00\u6B65", "Return to status for the next step"),
      why: hasMaterialChanges ? localizedText(responseLanguage, "\u6BD4\u8F83\u5DF2\u7ECF\u8BF4\u660E\u54EA\u91CC\u53D8\u4E86\uFF0C\u4E0B\u4E00\u6B65\u8981\u89E3\u91CA\u8FD9\u4E9B\u53D8\u5316\u662F\u5426\u6539\u5584\u4E86\u8BBA\u6587\u3002", "The comparison shows what changed; next explain whether those changes improved the paper.") : localizedText(responseLanguage, "\u6CA1\u6709\u5173\u952E\u5DEE\u5F02\u65F6\uFF0C\u4E0D\u9700\u8981\u4E3A\u7248\u672C\u6BD4\u8F83\u5236\u9020\u989D\u5916\u5DE5\u4F5C\u3002", "With no material difference, do not create extra versioning work.")
    }]
  }, responseLanguage);
}
function isIsoTimestamp2(value) {
  return typeof value === "string" && !Number.isNaN(Date.parse(value));
}
function normalizeLifecycleStatus(value) {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return GOVERNANCE_LIFECYCLE_STATUSES.has(normalized) ? normalized : null;
}
function normalizeUpdatedAt2(value) {
  return isIsoTimestamp2(value) ? value : null;
}
function packetHasLineageLinks(item = {}) {
  return [
    item.parentPacketId,
    ...item.childPacketIds ?? [],
    ...item.claimIds ?? [],
    ...item.noteIds ?? [],
    ...item.experimentIds ?? [],
    ...item.rebuttalIssueIds ?? [],
    ...item.versionIds ?? [],
    ...item.outputPaths ?? [],
    ...item.evidenceLinks ?? []
  ].filter(Boolean).length > 0;
}
function deriveGovernanceLifecycle({
  kind,
  status,
  explicitLifecycleStatus,
  blockedBy = [],
  continuationState = {},
  updatedAt,
  assignedRole,
  boardAssignedRole,
  hasLineage = false
} = {}) {
  const explicit = normalizeLifecycleStatus(explicitLifecycleStatus);
  if (explicit) {
    return explicit;
  }
  const normalizedStatus2 = typeof status === "string" ? status.trim().toLowerCase() : "pending";
  const continuationStatus = typeof continuationState?.status === "string" ? continuationState.status.trim().toLowerCase() : "";
  const normalizedUpdatedAt = normalizeUpdatedAt2(updatedAt) ?? normalizeUpdatedAt2(continuationState?.updatedAt);
  const ageMs = normalizedUpdatedAt ? Date.now() - Date.parse(normalizedUpdatedAt) : 0;
  const isStale = ageMs > 1e3 * 60 * 60 * 24 * 7;
  const terminalStatuses = kind === "blocker" ? TERMINAL_BLOCKER_STATUSES : TERMINAL_TASK_STATUSES;
  if (terminalStatuses.has(normalizedStatus2) || continuationStatus === "completed") {
    return hasLineage ? "archived-with-lineage" : "archived";
  }
  if (REVIEW_WORK_STATUSES.has(normalizedStatus2) || continuationStatus === "review-needed") {
    return "review-needed";
  }
  if (blockedBy.length > 0 || continuationStatus === "blocked") {
    return normalizedStatus2 === "blocked" ? "blocked" : "waiting";
  }
  if (assignedRole && boardAssignedRole && assignedRole !== boardAssignedRole && !WAITING_WORK_STATUSES.has(normalizedStatus2)) {
    return "ready-for-handoff";
  }
  if (isStale) {
    return "stale";
  }
  if (ACTIVE_WORK_STATUSES.has(normalizedStatus2) || continuationStatus === "in-progress") {
    return "active";
  }
  if (WAITING_WORK_STATUSES.has(normalizedStatus2) || continuationStatus === "ready-to-resume") {
    return "waiting";
  }
  return "queued";
}
function hashText(value) {
  return crypto7.createHash("sha256").update(value).digest("hex");
}
function transitionAllowed(fromPhase, toPhase) {
  const allowed = ALLOWED_TRANSITIONS[fromPhase] ?? [fromPhase];
  return allowed.includes(toPhase);
}
function expectedRoleForPhase(phase) {
  return PHASE_ROLE_OWNERS[phase] ?? "planner";
}
function targetArtifactContainsId3(root, artifactPath, targetId) {
  if (!artifactPath || !targetId) {
    return false;
  }
  const fullPath = path12.join(root, artifactPath);
  if (!fs10.existsSync(fullPath)) {
    return false;
  }
  const extension = path12.extname(artifactPath).toLowerCase();
  if (extension === ".json") {
    try {
      const value = JSON.parse(fs10.readFileSync(fullPath, "utf8"));
      const queue = [value];
      while (queue.length > 0) {
        const current = queue.shift();
        if (current === targetId) {
          return true;
        }
        if (Array.isArray(current)) {
          queue.push(...current);
          continue;
        }
        if (current && typeof current === "object") {
          queue.push(...Object.values(current));
        }
      }
      return false;
    } catch {
      return false;
    }
  }
  return fs10.readFileSync(fullPath, "utf8").includes(String(targetId));
}
function assertNoBlockingFollowThrough(root, currentPhase, nextPhase, currentAssignedRole, nextAssignedRole) {
  const changingGovernance = currentPhase !== nextPhase || currentAssignedRole !== nextAssignedRole;
  if (!changingGovernance) {
    return;
  }
  const followThrough = normalizeMetaOperatorFollowThroughIndex(readJson(root, ARTIFACT_PATHS.metaOperatorFollowThrough, createMetaOperatorFollowThroughIndex));
  const operatingState = readProgramOperatingState(root);
  const items = Array.isArray(followThrough.items) ? followThrough.items : [];
  const actionRequired = items.filter((item) => {
    const status = item.status;
    const authorityState = followThroughAuthorityState(item, operatingState);
    const invalidStatus = Boolean(item.invalidStatus) || !["acknowledged", "accepted-for-execution", "executing", "deferred", "accepted-risk", "closed", "superseded"].includes(status) || !authorityState.trusted;
    const dueDeferred = status === "deferred" && item.deferUntil && String(item.deferUntil) <= nowIso();
    const targetBound = !["accepted-for-execution", "executing", "closed"].includes(status) ? true : targetArtifactContainsId3(root, item.linkedTargetArtifact, item.linkedTargetId);
    const acceptedExecutionOpen = status === "accepted-for-execution" || status === "executing";
    return invalidStatus || Boolean(item.stale) || dueDeferred || !targetBound || acceptedExecutionOpen || status === "accepted-risk";
  }).map((item) => ({
    id: item.id,
    status: item.status,
    linkedTargetArtifact: item.linkedTargetArtifact,
    linkedTargetId: item.linkedTargetId
  }));
  if (actionRequired.length > 0) {
    throw new Error(`Cannot advance orchestration from ${currentPhase} to ${nextPhase} while operator follow-through still requires action: ${actionRequired.map((item) => item.id).join(", ")}.`);
  }
}
function formatExpectedRoleMessage(actionLabel, phase, role) {
  return `${actionLabel} requires routing role ${role} for phase ${phase}. Use appendHandoff or upsertOrchestrationBoard to record a valid workflow state transition.`;
}
function assertPhaseRoleOwnership(phase, role, actionLabel) {
  const expectedRole = expectedRoleForPhase(phase);
  if (!roleCanActAs(role, expectedRole)) {
    throw new Error(formatExpectedRoleMessage(actionLabel, phase, expectedRole));
  }
}
function assertLegalTransition(currentPhase, nextPhase, strictMode) {
  if (!strictMode || !currentPhase || !nextPhase) {
    return;
  }
  if (!transitionAllowed(currentPhase, nextPhase)) {
    throw new Error(`Illegal orchestration phase transition: ${currentPhase} -> ${nextPhase}`);
  }
}
function validateBoardMutation(currentBoard, nextPhase, nextAssignedRole, strictMode, actionLabel) {
  assertLegalTransition(currentBoard.currentPhase, nextPhase, strictMode);
  assertPhaseRoleOwnership(nextPhase, nextAssignedRole, actionLabel);
}
function classifyWorkflowIntent({ phase, tasks = [], blockers = [] } = {}) {
  const hasOpenBlockers = blockers.some((blocker) => blocker.status !== "resolved" && blocker.status !== "retired");
  if (hasOpenBlockers) {
    return "repair";
  }
  switch (phase) {
    case "sources":
    case "notes":
    case "research":
      return "research";
    case "plan":
    case "outline":
      return "plan";
    case "draft":
      return tasks.some((task) => task.status === "done") ? "review" : "write";
    case "experiments":
      return "experiment";
    case "review":
      return "review";
    case "rebuttal":
      return "respond";
    case "versions":
      return "version";
    case "checklist":
      return "finalize";
    default:
      return "plan";
  }
}
function defaultFocusForPhase(phase, board = {}) {
  const firstActiveTask = (board.tasks ?? []).find((task) => !["done", "cancelled"].includes(task.status));
  const firstOpenBlocker = (board.blockers ?? []).find((blocker) => blocker.status !== "resolved");
  if (firstOpenBlocker) {
    return `Resolve blocker: ${firstOpenBlocker.summary}`;
  }
  if (firstActiveTask) {
    return firstActiveTask.title;
  }
  switch (phase) {
    case "research":
      return "Build the evidence base before stronger claims.";
    case "plan":
      return "Convert evidence into a concrete paper plan.";
    case "outline":
      return "Turn the plan into section-level structure.";
    case "draft":
      return "Draft the next section without inventing support.";
    case "experiments":
      return "Run or audit claim-linked experiments.";
    case "review":
      return "Stress-test claims, citations, and draft integrity.";
    case "rebuttal":
      return "Address reviewer concerns with evidence-backed responses.";
    case "versions":
      return "Snapshot and compare the paper honestly.";
    default:
      return "Align the durable workflow state.";
  }
}
function defaultNextActionForPhase(phase) {
  switch (phase) {
    case "sources":
      return "Register or update the next durable source.";
    case "notes":
      return "Capture a structured note tied to sources.";
    case "research":
      return "Refresh the research brief and evidence backlog.";
    case "plan":
      return "Update the plan with evidence gaps and milestones.";
    case "outline":
      return "Update the outline before drafting.";
    case "draft":
      return "Draft the next section and leave explicit citation TODOs where support is missing.";
    case "experiments":
      return "Record the next experiment result, then audit it.";
    case "review":
      return "Run or refresh the review loop before finalizing claims.";
    case "rebuttal":
      return "Normalize reviewer concerns and draft the response plan.";
    case "versions":
      return "Create or compare a version snapshot.";
    case "checklist":
      return "Run verification and refresh the checklist.";
    default:
      return "Refresh the board and choose the next role-owned step.";
  }
}
function normalizeCheckpoint(checkpoint, index = 0) {
  if (typeof checkpoint === "string") {
    return {
      id: `checkpoint-${index + 1}`,
      summary: checkpoint,
      recordedAt: nowIso()
    };
  }
  return {
    id: slugify3(checkpoint?.id ?? checkpoint?.summary ?? `checkpoint-${index + 1}`),
    summary: checkpoint?.summary ?? `Checkpoint ${index + 1}`,
    recordedAt: checkpoint?.recordedAt ?? nowIso()
  };
}
function mergeContinuationState(current, incoming, defaults = {}) {
  const base = createContinuationState(current);
  if (!incoming || typeof incoming !== "object") {
    return {
      ...base,
      status: defaults.status ?? base.status,
      lastCheckpoint: defaults.lastCheckpoint ?? base.lastCheckpoint,
      updatedAt: nowIso()
    };
  }
  const history = Array.isArray(incoming.checkpointHistory) ? incoming.checkpointHistory.map(normalizeCheckpoint) : base.checkpointHistory;
  return {
    ...base,
    ...incoming,
    checkpointHistory: history,
    status: incoming.status ?? defaults.status ?? base.status,
    lastCheckpoint: incoming.lastCheckpoint ?? defaults.lastCheckpoint ?? base.lastCheckpoint,
    updatedAt: nowIso()
  };
}
function normalizeTask(task = {}, index = 0, boardAssignedRole = null) {
  const id = slugify3(task.id ?? task.title ?? `task-${index + 1}`);
  const updatedAt = normalizeUpdatedAt2(task.updatedAt) ?? nowIso();
  const hasLineage = packetHasLineageLinks(task);
  return {
    id,
    packetId: task.packetId ? slugify3(task.packetId) : void 0,
    parentPacketId: task.parentPacketId ? slugify3(task.parentPacketId) : null,
    childPacketIds: normalizeStringArray10(task.childPacketIds),
    phaseContextId: task.phaseContextId ?? null,
    title: task.title ?? `Task ${index + 1}`,
    status: task.status ?? "pending",
    lifecycleStatus: deriveGovernanceLifecycle({
      kind: "task",
      status: task.status,
      explicitLifecycleStatus: task.lifecycleStatus,
      blockedBy: normalizeStringArray10(task.blockedBy),
      continuationState: task.continuationState,
      updatedAt: task.updatedAt,
      assignedRole: task.assignedRole,
      boardAssignedRole,
      hasLineage
    }),
    assignedRole: ROLE_IDS.includes(task.assignedRole) ? task.assignedRole : "planner",
    currentFocus: task.currentFocus ?? task.title ?? `Task ${index + 1}`,
    nextAction: task.nextAction ?? "Continue the assigned task and refresh durable state.",
    continuationState: mergeContinuationState(task.continuationState, task.continuationState, {
      status: task.status === "done" ? "completed" : task.status === "blocked" ? "blocked" : "in-progress",
      lastCheckpoint: task.notes || task.title || `Task ${index + 1}`
    }),
    evidenceLinks: normalizeStringArray10(task.evidenceLinks),
    claimIds: normalizeStringArray10(task.claimIds),
    noteIds: normalizeStringArray10(task.noteIds),
    experimentIds: normalizeStringArray10(task.experimentIds),
    rebuttalIssueIds: normalizeStringArray10(task.rebuttalIssueIds),
    versionIds: normalizeStringArray10(task.versionIds),
    blockedBy: normalizeStringArray10(task.blockedBy),
    outputPaths: normalizeStringArray10(task.outputPaths),
    questions: Array.isArray(task.questions) ? task.questions : [],
    decisions: Array.isArray(task.decisions) ? task.decisions : [],
    notes: task.notes ?? "",
    updatedAt
  };
}
function normalizeBlocker(blocker = {}, index = 0, boardAssignedRole = null) {
  const id = slugify3(blocker.id ?? blocker.summary ?? `blocker-${index + 1}`);
  const updatedAt = normalizeUpdatedAt2(blocker.updatedAt) ?? nowIso();
  const hasLineage = packetHasLineageLinks(blocker);
  return {
    id,
    packetId: blocker.packetId ? slugify3(blocker.packetId) : void 0,
    parentPacketId: blocker.parentPacketId ? slugify3(blocker.parentPacketId) : null,
    childPacketIds: normalizeStringArray10(blocker.childPacketIds),
    phaseContextId: blocker.phaseContextId ?? null,
    summary: blocker.summary ?? `Blocker ${index + 1}`,
    status: blocker.status ?? "open",
    lifecycleStatus: deriveGovernanceLifecycle({
      kind: "blocker",
      status: blocker.status,
      explicitLifecycleStatus: blocker.lifecycleStatus,
      blockedBy: normalizeStringArray10(blocker.blockedBy),
      continuationState: blocker.continuationState,
      updatedAt: blocker.updatedAt,
      assignedRole: blocker.assignedRole,
      boardAssignedRole,
      hasLineage
    }),
    assignedRole: ROLE_IDS.includes(blocker.assignedRole) ? blocker.assignedRole : "planner",
    currentFocus: blocker.currentFocus ?? blocker.summary ?? `Blocker ${index + 1}`,
    nextAction: blocker.nextAction ?? "Resolve the blocker before moving downstream.",
    continuationState: mergeContinuationState(blocker.continuationState, blocker.continuationState, {
      status: blocker.status === "resolved" ? "completed" : "blocked",
      lastCheckpoint: blocker.summary ?? `Blocker ${index + 1}`
    }),
    evidenceLinks: normalizeStringArray10(blocker.evidenceLinks),
    claimIds: normalizeStringArray10(blocker.claimIds),
    noteIds: normalizeStringArray10(blocker.noteIds),
    experimentIds: normalizeStringArray10(blocker.experimentIds),
    rebuttalIssueIds: normalizeStringArray10(blocker.rebuttalIssueIds),
    versionIds: normalizeStringArray10(blocker.versionIds),
    blockedBy: normalizeStringArray10(blocker.blockedBy),
    outputPaths: normalizeStringArray10(blocker.outputPaths),
    questions: Array.isArray(blocker.questions) ? blocker.questions : [],
    decisions: Array.isArray(blocker.decisions) ? blocker.decisions : [],
    updatedAt
  };
}
function computeUnresolvedBlockersByRole(blockers = []) {
  const grouped = {};
  for (const blocker of blockers.filter((item) => item.status !== "resolved" && item.status !== "retired")) {
    grouped[blocker.assignedRole] ??= [];
    grouped[blocker.assignedRole].push(blocker.id);
  }
  return grouped;
}
function renderHandoffEntry({ timestamp, fromRole, toRole, phase, intentType, summary, currentFocus, nextAction, nextActions, evidenceLinks, blockerIds }) {
  return [
    `
## ${timestamp} \u2014 ${fromRole} -> ${toRole}`,
    "",
    `- Phase: ${phase}`,
    `- Intent: ${intentType}`,
    `- Summary: ${summary}`,
    `- Current focus: ${currentFocus}`,
    `- Next action: ${nextAction}`,
    "- Next actions:",
    ...nextActions.length > 0 ? nextActions.map((item) => `  - ${item}`) : ["  - None recorded"],
    `- Evidence links: ${evidenceLinks.join(", ") || "none"}`,
    `- Blockers: ${blockerIds.join(", ") || "none"}`,
    ""
  ].filter(Boolean).join("\n");
}
function appendHandoffEntry(root, payload) {
  const existing = readText(root, ARTIFACT_PATHS.orchestrationHandoffs, "");
  writeText(root, ARTIFACT_PATHS.orchestrationHandoffs, `${existing}${renderHandoffEntry(payload)}`.replace(/^\n+/, ""));
}
function collectDraftSnapshot(root, sections) {
  return Object.fromEntries(
    Object.entries(sections).map(([sectionId, section]) => {
      const relativePath = section.draftPath ?? `${ARTIFACT_PATHS.draftsDir}/${sectionId}.md`;
      const content = readText(root, relativePath, "");
      return [sectionId, {
        draftPath: relativePath,
        contentHash: content ? hashText(content) : null,
        citedKeys: Array.from(new Set(content.match(/\[cite:[^\]]+\]/g)?.map((item) => item.slice(6, -1).trim()) ?? []))
      }];
    })
  );
}
function ensureBlocker(blockers, blocker) {
  const normalized = normalizeBlocker(blocker);
  const existingIndex = blockers.findIndex((item) => item.id === normalized.id);
  if (existingIndex >= 0) {
    const next = [...blockers];
    next[existingIndex] = { ...next[existingIndex], ...normalized };
    return next;
  }
  return [...blockers, normalized];
}
function renderClaimsMarkdown(claims) {
  return [
    "# Claims from results",
    "",
    ...claims.flatMap((claim) => [
      `## ${claim.id}`,
      "",
      `- Text: ${claim.text}`,
      `- Section: ${claim.sectionId}`,
      `- Source IDs: ${claim.sourceIds.join(", ") || "none"}`,
      `- Note IDs: ${claim.noteIds.join(", ") || "none"}`,
      `- Experiment IDs: ${claim.experimentIds.join(", ") || "none"}`,
      `- Evidence links: ${claim.evidenceLinks.join(", ") || "none"}`,
      `- Status: ${claim.status}`,
      `- Confidence: ${claim.confidence}`,
      claim.latestAuditId ? `- Latest audit: ${claim.latestAuditId}` : null,
      claim.latestAuditVerdict ? `- Latest audit verdict: ${claim.latestAuditVerdict}` : null,
      claim.latestBridgeId ? `- Latest bridge event: ${claim.latestBridgeId}` : null,
      claim.bridgeStatus ? `- Bridge status: ${claim.bridgeStatus}` : null,
      claim.gap ? `- Gap: ${claim.gap}` : null,
      ""
    ].filter(Boolean))
  ].join("\n");
}
function collectFinalizeBlockersFromClaims(root) {
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
  const bridgeById = new Map((bridgeLog.items ?? []).map((item) => [item.id, item]));
  const heldBridgeClaimIds = new Set((bridgeLog.items ?? []).filter((bridge) => bridge.bridgeStatus === "held-for-review" || bridge.auditVerdict === "blocked").map((bridge) => bridge.claimId).filter(Boolean));
  const blockedClaimIds = /* @__PURE__ */ new Set();
  for (const claim of evidence.claims ?? []) {
    if (!claim || typeof claim !== "object") {
      continue;
    }
    if (claim.bridgeStatus === "held-for-review" || claim.latestAuditVerdict === "blocked" || heldBridgeClaimIds.has(claim.id)) {
      blockedClaimIds.add(claim.id);
      continue;
    }
    const bridge = claim.latestBridgeId ? bridgeById.get(claim.latestBridgeId) : null;
    if ((claim.experimentIds ?? []).length > 0 && !bridge) {
      blockedClaimIds.add(claim.id);
      continue;
    }
    if (!bridge) {
      continue;
    }
    if (bridge.bridgeStatus === "held-for-review" || bridge.auditVerdict === "blocked") {
      blockedClaimIds.add(claim.id);
    }
  }
  return Array.from(blockedClaimIds);
}
var COMPLETION_LIKE_FINAL_SECTION_STATUSES = /* @__PURE__ */ new Set([
  "ready",
  "review-ready",
  "done",
  "complete",
  "completed",
  "final",
  "approved"
]);
function isCompletionLikeFinalSectionStatus(value) {
  return COMPLETION_LIKE_FINAL_SECTION_STATUSES.has(
    String(value ?? "").trim().toLowerCase()
  );
}
function inspectFinalizationArtifact(root, artifactPath) {
  if (typeof artifactPath !== "string" || !artifactPath.trim()) {
    return null;
  }
  const inspection = inspectDeclaredPath(root, artifactPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  return {
    inspection,
    artifactPath: inspection.canonicalRelativePath ?? inspection.normalizedPath ?? artifactPath.trim()
  };
}
function currentFinalizationArtifactPaths(root) {
  const state = loadState(root);
  const artifactPaths = [];
  for (const [sectionId, section] of Object.entries(state.sections ?? {})) {
    const candidate = inspectFinalizationArtifact(root, section?.draftPath);
    if (!candidate) {
      continue;
    }
    if (candidate.inspection.status === "existing") {
      artifactPaths.push(candidate.artifactPath);
      continue;
    }
    if (isCompletionLikeFinalSectionStatus(section?.status)) {
      throw new Error(
        `Current final section ${sectionId} has no usable draft artifact at ${candidate.artifactPath}: ${candidate.inspection.reason ?? candidate.inspection.status}`
      );
    }
  }
  const figureFinals = readJson(root, ARTIFACT_PATHS.figureFinalIndex, {
    items: []
  });
  const figureGenerations = readJson(root, ARTIFACT_PATHS.figureGenerations, {
    items: []
  });
  const importedFinalFigurePaths = new Set(
    (figureGenerations.items ?? []).filter((item) => item?.status === "imported").map((item) => item?.finalSvgPath).filter((artifactPath) => typeof artifactPath === "string" && artifactPath.trim())
  );
  for (const item of figureFinals.items ?? []) {
    const candidate = inspectFinalizationArtifact(root, item?.finalSvgPath);
    if (!candidate) {
      continue;
    }
    if (candidate.inspection.status === "existing") {
      artifactPaths.push(candidate.artifactPath);
      continue;
    }
    const importedArtifactMissing = importedFinalFigurePaths.has(
      item?.finalSvgPath
    );
    if (item?.readinessStatus === "ready-for-finalization" || importedArtifactMissing) {
      throw new Error(
        `Current final figure ${item?.figureId ?? item?.id ?? "unknown"} has no usable final artifact at ${candidate.artifactPath}: ${candidate.inspection.reason ?? candidate.inspection.status}`
      );
    }
  }
  return Array.from(new Set(artifactPaths)).sort();
}
function assertFinalizeReviewGate(root, actionLabel) {
  const board = loadBoard(root);
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, { version: 3, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  const concerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const unresolvedConcerns = (concerns.items ?? []).filter(
    (item) => !["resolved", "retired"].includes(item.status)
  );
  const unresolvedConcernIds = new Set(
    (reviewState.unresolvedConcernIds ?? []).concat(
      unresolvedConcerns.map((item) => item.id)
    )
  );
  const canonicalReviewGateRequired = board.reviewRequiredBeforeFinalize || ["needs-revision", "needs-evidence", "blocked"].includes(
    reviewState.lastVerdict
  ) || unresolvedConcerns.some(
    (item) => String(item.id ?? "").endsWith("-review-proof-required") || item.responseOwnerRole === "reviewer" && /Reviewer runtime execution claim|Reviewer proof/u.test(
      String(item.summary ?? "")
    )
  );
  if (!canonicalReviewGateRequired) return;
  const finalArtifactPaths = currentFinalizationArtifactPaths(root);
  if (finalArtifactPaths.length === 0) {
    throw new Error(`${actionLabel} requires at least one current final draft or final figure artifact before finalization.`);
  }
  const currentSnapshot = snapshotReviewedArtifacts(root, finalArtifactPaths, `${actionLabel} final artifact scope`);
  const independentProof = findCurrentIndependentReviewProof(root, {
    reviewedArtifactPaths: currentSnapshot.reviewedArtifacts.map((artifact) => artifact.path)
  });
  const reviewIsClear = reviewState.lastVerdict === "coherent" && unresolvedConcernIds.size === 0 && independentProof.ok;
  const blockedClaimIds = collectFinalizeBlockersFromClaims(root);
  if (!reviewIsClear) {
    throw new Error(`${actionLabel} requires a coherent review with no unresolved concerns and authorized independent Reviewer proof covering the exact current final artifact set and hashes while the canonical finalization review gate is active.`);
  }
  if (independentProof.proof.reviewedArtifactSetSha256 !== currentSnapshot.reviewedArtifactSetSha256) {
    throw new Error(`${actionLabel} rejected stale or incomplete Reviewer proof because the current final artifact exact-set hash changed after review.`);
  }
  if (blockedClaimIds.length > 0) {
    throw new Error(`${actionLabel} is blocked by experiment integrity: claim(s) ${blockedClaimIds.join(", ")} are held for review. Resolve bridge/audit issues before finalization.`);
  }
}
function loadBoard(root) {
  return readJson(root, ARTIFACT_PATHS.orchestrationBoard, () => createDefaultBoard(loadState(root)));
}
function syncStateWithBoard(root, board, state = loadState(root)) {
  const nextState = {
    ...state,
    pipeline: {
      ...state.pipeline,
      currentStage: board.currentPhase,
      lastCompletedStage: board.currentPhase,
      updatedAt: board.updatedAt ?? nowIso(),
      resumeCommand: resolveResumeCommandForPhase(board.currentPhase)
    },
    orchestration: {
      ...state.orchestration,
      phase: board.currentPhase,
      intentType: board.intentType,
      assignedRole: board.assignedRole,
      currentFocus: board.currentFocus,
      nextAction: board.nextAction,
      continuationState: board.continuationState,
      reviewRequiredBeforeFinalize: Boolean(board.reviewRequiredBeforeFinalize),
      activeTaskIds: board.tasks.filter((task) => !["done", "cancelled"].includes(task.status)).map((task) => task.id),
      blockerIds: board.blockers.filter((blocker) => blocker.status !== "resolved").map((blocker) => blocker.id),
      evidenceLinks: normalizeStringArray10(board.evidenceLinks),
      experimentIds: normalizeStringArray10(board.experimentIds),
      rebuttalIssueIds: normalizeStringArray10(board.rebuttalIssueIds),
      currentVersionId: board.versionLineage?.currentVersionId ?? null,
      activeComparisonTargets: normalizeStringArray10(board.activeComparisonTargets)
    }
  };
  saveState(root, nextState);
  return nextState;
}
function persistBoard(root, board) {
  const state = loadState(root);
  const withDefaults = { ...createDefaultBoard(state), ...board };
  const tasks = Array.isArray(withDefaults.tasks) ? withDefaults.tasks.map((task, index) => normalizeTask(task, index, withDefaults.assignedRole)) : [];
  const blockers = Array.isArray(withDefaults.blockers) ? withDefaults.blockers.map((blocker, index) => normalizeBlocker(blocker, index, withDefaults.assignedRole)) : [];
  const intentType = withDefaults.intentType ?? classifyWorkflowIntent({ phase: withDefaults.currentPhase, tasks, blockers });
  const currentFocus = withDefaults.currentFocus ?? defaultFocusForPhase(withDefaults.currentPhase, { ...withDefaults, tasks, blockers });
  const nextAction = withDefaults.nextAction ?? defaultNextActionForPhase(withDefaults.currentPhase);
  const continuationState = mergeContinuationState(withDefaults.continuationState, withDefaults.continuationState, {
    status: blockers.some((item) => item.status !== "resolved") ? "blocked" : "ready-to-resume",
    lastCheckpoint: nextAction
  });
  const normalized = {
    ...withDefaults,
    version: 2,
    assignedRole: ROLE_IDS.includes(withDefaults.assignedRole) ? withDefaults.assignedRole : "planner",
    intentType,
    currentFocus,
    nextAction,
    continuationState,
    reviewRequiredBeforeFinalize: Boolean(withDefaults.reviewRequiredBeforeFinalize),
    tasks,
    blockers,
    evidenceLinks: normalizeStringArray10(withDefaults.evidenceLinks),
    experimentIds: normalizeStringArray10(withDefaults.experimentIds),
    rebuttalIssueIds: normalizeStringArray10(withDefaults.rebuttalIssueIds),
    activeComparisonTargets: normalizeStringArray10(withDefaults.activeComparisonTargets),
    unresolvedBlockersByRole: computeUnresolvedBlockersByRole(blockers),
    versionLineage: {
      currentVersionId: withDefaults.versionLineage?.currentVersionId ?? null,
      parentVersionId: withDefaults.versionLineage?.parentVersionId ?? null,
      snapshotIds: normalizeStringArray10(withDefaults.versionLineage?.snapshotIds)
    },
    updatedAt: nowIso()
  };
  writeJson(root, ARTIFACT_PATHS.orchestrationBoard, normalized);
  syncStateWithBoard(root, normalized);
  refreshDurableSurfaces(root, {
    type: "persist-board",
    summary: `Updated board phase ${normalized.currentPhase} for ${normalized.assignedRole}.`,
    artifactPaths: [ARTIFACT_PATHS.orchestrationBoard, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.workspaceIndex]
  });
  return normalized;
}
var SYSTEM_BOARD_AUTHORITY = /* @__PURE__ */ Symbol("system-board-authority");
function persistOrchestrationBoardUpdate(root, args = {}, { authority = null } = {}) {
  const systemOwned = authority === SYSTEM_BOARD_AUTHORITY;
  assertNoPolicyOverrideArgs(args, "Updating the orchestration board");
  if (!systemOwned && Object.hasOwn(args, "reviewRequiredBeforeFinalize")) {
    throw new Error("Updating the orchestration board does not accept system-owned field reviewRequiredBeforeFinalize.");
  }
  const state = loadState(root);
  const current = loadBoard(root);
  const nextPhase = args.phase ?? current.currentPhase;
  const nextAssignedRole = args.assignedRole ?? current.assignedRole;
  assertReviewProofBoundaryTransition(
    root,
    current,
    nextPhase,
    nextAssignedRole,
    "Updating the orchestration board"
  );
  validateBoardMutation(current, nextPhase, nextAssignedRole, state.settings?.strictMode, "Updating the orchestration board");
  assertNoBlockingFollowThrough(root, current.currentPhase, nextPhase, current.assignedRole, nextAssignedRole);
  const tasks = Array.isArray(args.tasks) ? args.tasks.map((task, index) => normalizeTask(task, index, nextAssignedRole)) : current.tasks;
  const blockers = Array.isArray(args.blockers) ? args.blockers.map((blocker, index) => normalizeBlocker(blocker, index, nextAssignedRole)) : current.blockers;
  const intentType = args.intentType ?? classifyWorkflowIntent({ phase: nextPhase, tasks, blockers });
  const currentFocus = args.currentFocus ?? current.currentFocus ?? defaultFocusForPhase(nextPhase, { tasks, blockers });
  const nextAction = args.nextAction ?? current.nextAction ?? defaultNextActionForPhase(nextPhase);
  const continuationState = mergeContinuationState(current.continuationState, args.continuationState, {
    status: blockers.some((item) => item.status !== "resolved") ? "blocked" : "ready-to-resume",
    lastCheckpoint: nextAction
  });
  if (nextAssignedRole !== current.assignedRole && !args.skipAutoHandoff) {
    appendHandoffEntry(root, {
      timestamp: args.timestamp ?? nowIso(),
      fromRole: current.assignedRole,
      toRole: nextAssignedRole,
      phase: nextPhase,
      intentType,
      summary: args.handoffSummary ?? `Workflow routing moved from ${current.assignedRole} to ${nextAssignedRole}.`,
      currentFocus,
      nextAction,
      nextActions: normalizeStringArray10(args.nextActions ?? [nextAction]),
      evidenceLinks: normalizeStringArray10(args.evidenceLinks ?? current.evidenceLinks),
      blockerIds: normalizeStringArray10(blockers.filter((item) => item.status !== "resolved").map((item) => item.id))
    });
  }
  return persistBoard(root, {
    ...current,
    paperObjective: args.doveObjective ?? args.objective ?? current.doveObjective ?? state.dove.objective,
    currentPhase: nextPhase,
    assignedRole: nextAssignedRole,
    intentType,
    currentFocus,
    nextAction,
    continuationState,
    reviewRequiredBeforeFinalize: Object.hasOwn(args, "reviewRequiredBeforeFinalize") ? Boolean(args.reviewRequiredBeforeFinalize) : current.reviewRequiredBeforeFinalize,
    tasks,
    blockers,
    evidenceLinks: args.evidenceLinks ? normalizeStringArray10(args.evidenceLinks) : current.evidenceLinks,
    experimentIds: args.experimentIds ? normalizeStringArray10(args.experimentIds) : current.experimentIds,
    rebuttalIssueIds: args.rebuttalIssueIds ? normalizeStringArray10(args.rebuttalIssueIds) : current.rebuttalIssueIds,
    activeComparisonTargets: args.activeComparisonTargets ? normalizeStringArray10(args.activeComparisonTargets) : current.activeComparisonTargets,
    versionLineage: {
      currentVersionId: args.versionLineage?.currentVersionId ?? current.versionLineage?.currentVersionId ?? null,
      parentVersionId: args.versionLineage?.parentVersionId ?? current.versionLineage?.parentVersionId ?? null,
      snapshotIds: args.versionLineage?.snapshotIds ? normalizeStringArray10(args.versionLineage.snapshotIds) : current.versionLineage?.snapshotIds ?? []
    }
  });
}
function upsertOrchestrationBoard(root, args = {}) {
  const board = loadBoard(root);
  if (Object.hasOwn(args, "assignedRole") && args.assignedRole !== board.assignedRole) {
    throw new Error("Updating the orchestration board cannot transfer board ownership through public assignedRole input.");
  }
  return persistOrchestrationBoardUpdate(root, {
    ...args,
    assignedRole: board.assignedRole
  });
}
function assertSystemOrchestrationBoardUpdate(root, args = {}) {
  assertNoPolicyOverrideArgs(args, "Updating the orchestration board");
  const state = loadState(root);
  const current = loadBoard(root);
  const nextPhase = args.phase ?? current.currentPhase;
  const nextAssignedRole = args.assignedRole ?? current.assignedRole;
  assertReviewProofBoundaryTransition(
    root,
    current,
    nextPhase,
    nextAssignedRole,
    "Updating the orchestration board"
  );
  validateBoardMutation(
    current,
    nextPhase,
    nextAssignedRole,
    state.settings?.strictMode,
    "Updating the orchestration board"
  );
  assertNoBlockingFollowThrough(
    root,
    current.currentPhase,
    nextPhase,
    current.assignedRole,
    nextAssignedRole
  );
  return {
    current,
    nextPhase,
    nextAssignedRole
  };
}
function upsertSystemOrchestrationBoard(root, args = {}) {
  return persistOrchestrationBoardUpdate(root, args, { authority: SYSTEM_BOARD_AUTHORITY });
}
function persistHandoff(root, args = {}, { authority = null } = {}) {
  const systemOwned = authority === SYSTEM_BOARD_AUTHORITY;
  assertNoPolicyOverrideArgs(args, "Appending a handoff");
  const board = loadBoard(root);
  const state = loadState(root);
  const timestamp = args.timestamp ?? nowIso();
  if (!systemOwned && (Object.hasOwn(args, "fromRole") || Object.hasOwn(args, "toRole"))) {
    throw new Error("Appending a handoff cannot claim or transfer board ownership through public role input.");
  }
  const fromRole = systemOwned ? args.fromRole ?? board.assignedRole : board.assignedRole;
  const toRole = systemOwned ? args.toRole ?? board.assignedRole : board.assignedRole;
  const phase = args.phase ?? board.currentPhase;
  assertReviewProofBoundaryTransition(
    root,
    board,
    phase,
    toRole,
    "Appending a handoff"
  );
  validateBoardMutation(board, phase, toRole, state.settings?.strictMode, "Appending a handoff");
  if (systemOwned && !roleCanActAs(fromRole, board.assignedRole)) {
    throw new Error(`Appending a handoff requires fromRole ${board.assignedRole}, but received ${fromRole}.`);
  }
  const intentType = args.intentType ?? board.intentType ?? classifyWorkflowIntent({ phase, tasks: board.tasks, blockers: board.blockers });
  const currentFocus = args.currentFocus ?? board.currentFocus;
  const nextAction = args.nextAction ?? board.nextAction;
  appendHandoffEntry(root, {
    timestamp,
    fromRole,
    toRole,
    phase,
    intentType,
    summary: args.summary ?? "No summary provided.",
    currentFocus,
    nextAction,
    nextActions: Array.isArray(args.nextActions) ? args.nextActions : [nextAction],
    evidenceLinks: normalizeStringArray10(args.evidenceLinks ?? board.evidenceLinks),
    blockerIds: normalizeStringArray10(args.blockerIds ?? board.blockers.filter((item) => item.status !== "resolved").map((item) => item.id))
  });
  return upsertSystemOrchestrationBoard(root, {
    phase,
    assignedRole: toRole,
    intentType,
    currentFocus,
    nextAction,
    evidenceLinks: args.evidenceLinks ?? board.evidenceLinks,
    tasks: board.tasks,
    blockers: board.blockers,
    skipAutoHandoff: true
  });
}
function appendHandoff(root, args = {}) {
  return persistHandoff(root, args);
}
function appendSystemHandoff(root, args = {}) {
  return persistHandoff(root, args, { authority: SYSTEM_BOARD_AUTHORITY });
}
function renderResearchBrief(agenda) {
  return [
    "# Research brief",
    "",
    `## Objective

${agenda.objective}`,
    "",
    "## Agenda",
    "",
    ...agenda.agenda.length > 0 ? agenda.agenda.map((item) => `- ${item}`) : ["- No research agenda recorded."],
    "",
    "## Evidence backlog",
    "",
    ...agenda.evidenceBacklog.length > 0 ? agenda.evidenceBacklog.map((item) => `- ${item}`) : ["- No evidence backlog recorded."]
  ].join("\n");
}
function updateResearchBrief(root, args = {}) {
  assertGovernanceMutationRegistered("update-research-brief", "guarded");
  assertTaskScopedMutationTarget(root, "update-research-brief", args);
  assertFollowThroughReady(root, "Updating the research brief", args);
  const forbiddenAuthorityFields = ["phase", "assignedRole", "ownerRole", "nextRole"].filter((field) => Object.hasOwn(args, field));
  if (forbiddenAuthorityFields.length > 0) {
    throw new Error(`update_research_brief does not accept workflow authority fields: ${forbiddenAuthorityFields.join(", ")}.`);
  }
  const board = loadBoard(root);
  assertReviewProofBoundaryTransition(
    root,
    board,
    "research",
    "researcher",
    "Updating the research brief"
  );
  const state = loadState(root);
  const current = readJson(root, ARTIFACT_PATHS.researchAgenda, { version: 1, objective: state.dove.objective, agenda: [], evidenceBacklog: [], updatedAt: null });
  const next = {
    version: 1,
    objective: args.objective ?? current.objective ?? state.dove.objective,
    agenda: args.agenda ? normalizeStringArray10(args.agenda) : current.agenda,
    evidenceBacklog: args.evidenceBacklog ? normalizeStringArray10(args.evidenceBacklog) : current.evidenceBacklog,
    updatedAt: nowIso()
  };
  writeJson(root, ARTIFACT_PATHS.researchAgenda, next);
  writeText(root, ARTIFACT_PATHS.researchBrief, renderResearchBrief(next));
  upsertSystemOrchestrationBoard(root, {
    objective: next.objective,
    phase: board.currentPhase,
    assignedRole: board.assignedRole,
    intentType: "research",
    currentFocus: args.currentFocus ?? "Tighten the research agenda and evidence backlog.",
    nextAction: args.nextAction ?? "Turn backlog items into sources, notes, or experiments.",
    continuationState: {
      status: "in-progress",
      lastCheckpoint: "Research brief refreshed.",
      checkpointHistory: [{ summary: "Research brief refreshed.", recordedAt: nowIso() }]
    }
  });
  refreshDurableSurfaces(root, {
    type: "update-research-brief",
    summary: "Updated research brief and agenda.",
    artifactPaths: [ARTIFACT_PATHS.researchBrief, ARTIFACT_PATHS.researchAgenda, ARTIFACT_PATHS.queryPack]
  });
  return next;
}
function normalizeExperimentPlan(plan = {}, index = 0) {
  return {
    id: slugify3(plan.id ?? plan.title ?? `experiment-${index + 1}`),
    title: plan.title ?? `Experiment ${index + 1}`,
    claimId: plan.claimId ?? "",
    hypothesis: plan.hypothesis ?? "",
    methodology: plan.methodology ?? "",
    successMetric: plan.successMetric ?? "",
    comparisonTargets: normalizeStringArray10(plan.comparisonTargets),
    status: plan.status ?? "planned",
    owner: ROLE_IDS.includes(plan.owner) ? plan.owner : "builder",
    updatedAt: nowIso()
  };
}
function renderExperimentLog(plans, results, audits = []) {
  return [
    "# Experiment log",
    "",
    "## Planned experiments",
    "",
    ...plans.length > 0 ? plans.flatMap((plan) => [
      `### ${plan.id} \u2014 ${plan.title}`,
      "",
      `- Claim ID: ${plan.claimId || "none"}`,
      `- Hypothesis: ${plan.hypothesis || "TBD"}`,
      `- Methodology: ${plan.methodology || "TBD"}`,
      `- Success metric: ${plan.successMetric || "TBD"}`,
      `- Comparison targets: ${plan.comparisonTargets.join(", ") || "none"}`,
      `- Status: ${plan.status}`,
      ""
    ]) : ["No experiment plans recorded.", ""],
    "## Recorded results",
    "",
    ...results.length > 0 ? results.flatMap((result) => [
      `### ${result.id}`,
      "",
      `- Experiment ID: ${result.experimentId}`,
      `- Claim ID: ${result.claimId || "none"}`,
      `- Outcome: ${result.outcome}`,
      `- Summary: ${result.summary || "No summary provided."}`,
      `- Evidence links: ${result.evidenceLinks.join(", ") || "none"}`,
      result.latestAuditId ? `- Latest audit: ${result.latestAuditId}` : null,
      result.latestBridgeId ? `- Latest bridge: ${result.latestBridgeId}` : null,
      ""
    ].filter(Boolean)) : ["No experiment results recorded."],
    "",
    "## Experiment audits",
    "",
    ...audits.length > 0 ? audits.slice(-10).reverse().flatMap((audit) => [
      `- ${audit.id}: ${audit.experimentId} [verdict=${audit.auditVerdict ?? "concern"} confidence=${audit.confidence}] flags=${audit.integrityFlags.join(", ") || "none"}`
    ]) : ["- No audits recorded."]
  ].join("\n");
}
function normalizeExperimentResult(result = {}, index = 0) {
  return {
    id: slugify3(result.id ?? `${result.experimentId ?? "experiment"}-result-${index + 1}`),
    experimentId: result.experimentId ?? "",
    claimId: result.claimId ?? "",
    outcome: result.outcome ?? "pending",
    summary: result.summary ?? "",
    evidenceLinks: normalizeStringArray10(result.evidenceLinks),
    comparisonTargets: normalizeStringArray10(result.comparisonTargets),
    latestAuditId: result.latestAuditId ?? null,
    latestBridgeId: result.latestBridgeId ?? null,
    updatedAt: nowIso()
  };
}
function normalizeExperimentAudit(audit = {}, index = 0) {
  return {
    id: slugify3(audit.id ?? `${audit.experimentId ?? "experiment"}-audit-${index + 1}`),
    experimentId: audit.experimentId ?? "",
    resultId: audit.resultId ?? "",
    claimId: audit.claimId ?? "",
    reviewedArtifactRefs: normalizeStringArray10(audit.reviewedArtifactRefs),
    requiredArtifactRefs: normalizeStringArray10(audit.requiredArtifactRefs),
    missingArtifactRefs: normalizeStringArray10(audit.missingArtifactRefs),
    auditFindings: Array.isArray(audit.auditFindings) ? audit.auditFindings : [],
    integrityFlags: normalizeStringArray10(audit.integrityFlags),
    evidencePathIntegrity: audit.evidencePathIntegrity ?? null,
    confidence: audit.confidence ?? "medium",
    outcomeMapping: audit.outcomeMapping ?? "inconclusive",
    auditVerdict: audit.auditVerdict ?? "concern",
    bridgeReadiness: audit.bridgeReadiness ?? "blocked",
    resultOutcome: audit.resultOutcome ?? "pending",
    evidenceLinkCount: Number.isInteger(audit.evidenceLinkCount) ? audit.evidenceLinkCount : 0,
    comparisonTargetCount: Number.isInteger(audit.comparisonTargetCount) ? audit.comparisonTargetCount : 0,
    claimStateBefore: audit.claimStateBefore ?? null,
    updatedAt: nowIso()
  };
}
function confidenceAfterSupport(current) {
  if (current === "low") return "medium";
  if (current === "medium") return "high";
  return "high";
}
var SYSTEM_OWNED_EXPERIMENT_PROVENANCE_FIELDS = /* @__PURE__ */ new Set([
  "authorizationProvenance",
  "authorizationProvenanceHistory",
  "authorizationFingerprint",
  "authorityStepId",
  "authorityStepIndex",
  "executionClaimId",
  "runtimeRunId",
  "leaseId"
]);
function collectForbiddenExperimentProvenancePaths(value, path28 = "", seen = /* @__PURE__ */ new WeakSet()) {
  if (!value || typeof value !== "object") {
    return [];
  }
  if (seen.has(value)) {
    return [];
  }
  seen.add(value);
  if (Array.isArray(value)) {
    return value.flatMap(
      (item, index) => collectForbiddenExperimentProvenancePaths(item, `${path28}[${index}]`, seen)
    );
  }
  return Object.entries(value).flatMap(([key, nestedValue]) => {
    const fieldPath = path28 ? `${path28}.${key}` : key;
    return [
      ...SYSTEM_OWNED_EXPERIMENT_PROVENANCE_FIELDS.has(key) ? [fieldPath] : [],
      ...collectForbiddenExperimentProvenancePaths(nestedValue, fieldPath, seen)
    ];
  });
}
function assertNoPublicExperimentProvenance(args = {}, actionLabel) {
  const forbidden = collectForbiddenExperimentProvenancePaths(args);
  if (forbidden.length > 0) {
    throw new Error(
      `${actionLabel} does not accept system-owned runtime provenance fields: ${forbidden.join(", ")}.`
    );
  }
}
function resolveDurableExperimentResult(resultsIndex, args = {}, actionLabel = "Experiment result lookup") {
  if (args.result) {
    const resultId = args.result.id ?? args.resultId;
    if (!resultId) {
      throw new Error(`${actionLabel} requires args.result to include a durable id.`);
    }
    const durable = resultsIndex.items.find((item) => item.id === resultId);
    if (!durable) {
      throw new Error(`${actionLabel} received non-durable result ${resultId}.`);
    }
    for (const field of ["experimentId", "claimId", "outcome"]) {
      if (args.result[field] && durable[field] !== args.result[field]) {
        throw new Error(`${actionLabel} result ${resultId} ${field} does not match durable state.`);
      }
    }
    return durable;
  }
  if (args.resultId) {
    const durable = resultsIndex.items.find((item) => item.id === args.resultId);
    if (!durable) {
      throw new Error(`${actionLabel} could not find result ${args.resultId}.`);
    }
    return durable;
  }
  if (args.experimentId) {
    const matches = resultsIndex.items.filter((item) => item.experimentId === args.experimentId);
    if (matches.length === 1) {
      return matches[0];
    }
    if (matches.length > 1) {
      throw new Error(`${actionLabel} found multiple results for experiment ${args.experimentId}; provide resultId.`);
    }
  }
  throw new Error(`${actionLabel} requires an existing durable resultId.`);
}
function runExperimentAudit(root, args = {}) {
  assertGovernanceMutationRegistered("run-experiment-audit", "guarded");
  assertNoPublicExperimentProvenance(args, "Public experiment audit mutations");
  const target = assertTaskScopedMutationTarget(root, "run-experiment-audit", args);
  assertFollowThroughReady(root, "Running an experiment audit", args);
  const audit = persistExperimentAudit(root, args);
  return {
    ...audit,
    preActionGuidanceSummary: workflowGuidanceSummary(root, args, {
      surface: "dove.experience",
      roleId: "reviewer",
      packet: target.packet,
      stage: target.packet?.stage ?? "audit",
      workflowKind: "experiment-audit",
      nextAction: audit.bridgeReadiness === "ready" ? "project:dove.experience" : "project:dove.review",
      tags: ["experiment", "audit", "review"],
      statusSummary: { auditId: audit.id, auditVerdict: audit.auditVerdict, bridgeReadiness: audit.bridgeReadiness }
    })
  };
}
function persistExperimentAudit(root, args = {}, runtimeContext = {}) {
  const resultsIndex = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const plansIndex = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const auditsIndex = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  const rawResult = resolveDurableExperimentResult(resultsIndex, args, "Experiment audit");
  const linkedPlan = plansIndex.items.find((item) => item.id === rawResult.experimentId);
  const linkedClaim = rawResult.claimId ? evidence.claims.find((item) => item.id === rawResult.claimId) : null;
  const integrityFlags = [];
  const auditFindings = [];
  const requiredArtifactRefs = [ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentLog, ...rawResult.evidenceLinks ?? []];
  if (linkedPlan) {
    requiredArtifactRefs.unshift(ARTIFACT_PATHS.experimentPlans);
  }
  const missingArtifactRefs = [];
  if (!linkedPlan) {
    integrityFlags.push("missing-plan");
    auditFindings.push("No linked experiment plan was found.");
  }
  if (!rawResult.claimId) {
    integrityFlags.push("missing-claim-link");
    auditFindings.push("Result is missing an explicit claim link.");
  }
  const evidenceIntegrity = evidencePathProblemFlags(root, rawResult.evidenceLinks ?? []);
  for (const flag of evidenceIntegrity.flags) {
    integrityFlags.push(flag);
  }
  if (evidenceIntegrity.flags.includes("missing-evidence-links")) {
    auditFindings.push("Result has no durable evidence links.");
  }
  if (evidenceIntegrity.flags.includes("missing-evidence-file")) {
    auditFindings.push("Result evidence links do not resolve to an existing local evidence file.");
  }
  if (evidenceIntegrity.flags.includes("empty-evidence-file")) {
    auditFindings.push("Result evidence links include an empty evidence file.");
  }
  if (evidenceIntegrity.flags.includes("directory-evidence-file")) {
    auditFindings.push("Result evidence links include a directory instead of a file.");
  }
  if (evidenceIntegrity.flags.includes("unsafe-evidence-path")) {
    auditFindings.push("Result evidence links include an unsafe path.");
  }
  if (evidenceIntegrity.flags.includes("bookkeeping-evidence-file")) {
    auditFindings.push("Result evidence links cite status, navigation, runtime, task, or ledger bookkeeping rather than experiment output.");
  }
  if (!(rawResult.summary ?? "").trim()) {
    integrityFlags.push("missing-result-summary");
    auditFindings.push("Result summary is empty, making downstream review harder.");
  }
  if (!linkedPlan?.methodology?.trim()) {
    integrityFlags.push("missing-methodology");
    auditFindings.push("Experiment plan is missing methodology details.");
  }
  if (!linkedPlan?.successMetric?.trim()) {
    integrityFlags.push("missing-success-metric");
    auditFindings.push("Experiment plan is missing a success metric.");
  }
  if ((linkedPlan?.comparisonTargets ?? []).length > 0 && (rawResult.comparisonTargets ?? []).length === 0) {
    integrityFlags.push("missing-comparison-context");
    auditFindings.push("Result omitted comparison targets declared in the plan.");
  }
  if (rawResult.outcome === "pending") {
    integrityFlags.push("pending-outcome");
    auditFindings.push("Result is still pending and cannot strongly support a claim yet.");
  }
  const reviewedArtifactRefs = args.reviewedArtifactRefs ?? [
    ...linkedPlan ? [ARTIFACT_PATHS.experimentPlans] : [],
    ARTIFACT_PATHS.experimentResults,
    ARTIFACT_PATHS.experimentLog,
    ...rawResult.evidenceLinks
  ];
  for (const ref of requiredArtifactRefs) {
    if (!reviewedArtifactRefs.includes(ref)) {
      missingArtifactRefs.push(ref);
    }
  }
  if (missingArtifactRefs.length > 0) {
    integrityFlags.push("missing-reviewed-artifact-refs");
    auditFindings.push(`Audit omitted required artifact refs: ${missingArtifactRefs.join(", ")}.`);
  }
  const confidence = integrityFlags.length > 0 ? "low" : rawResult.outcome === "supports" ? "high" : "medium";
  const outcomeMapping = rawResult.outcome === "supports" ? "supports" : rawResult.outcome === "refutes" || rawResult.outcome === "failed" ? "refutes" : "inconclusive";
  const auditVerdict = integrityFlags.length === 0 ? "clean" : integrityFlags.some((flag) => ["missing-plan", "missing-claim-link", "missing-evidence-links", "missing-evidence-file", "empty-evidence-file", "directory-evidence-file", "unsafe-evidence-path", "bookkeeping-evidence-file", "unsupported-evidence-file", "unreadable-evidence-file", "missing-methodology", "missing-success-metric", "missing-reviewed-artifact-refs", "pending-outcome"].includes(flag)) ? "blocked" : "concern";
  const audit = normalizeExperimentAudit({
    ...args,
    experimentId: rawResult.experimentId,
    resultId: rawResult.id,
    claimId: rawResult.claimId,
    reviewedArtifactRefs,
    requiredArtifactRefs,
    missingArtifactRefs,
    auditFindings,
    integrityFlags,
    evidencePathIntegrity: evidenceIntegrity.pathEvidence,
    confidence,
    outcomeMapping,
    auditVerdict,
    bridgeReadiness: auditVerdict === "clean" ? "ready" : "blocked",
    resultOutcome: rawResult.outcome,
    evidenceLinkCount: (rawResult.evidenceLinks ?? []).length,
    comparisonTargetCount: (rawResult.comparisonTargets ?? []).length,
    claimStateBefore: linkedClaim ? { status: linkedClaim.status, confidence: linkedClaim.confidence } : null,
    ...runtimeContext.authorizationProvenance ? { authorizationProvenance: runtimeContext.authorizationProvenance } : {}
  }, auditsIndex.items.length);
  if (runtimeContext.authorizationProvenance) {
    audit.authorizationProvenance = runtimeContext.authorizationProvenance;
  }
  const existingIndex = auditsIndex.items.findIndex((item) => item.id === audit.id);
  if (existingIndex >= 0) {
    const existingAudit = auditsIndex.items[existingIndex];
    const existingProvenance = existingAudit.authorizationProvenance ?? null;
    const nextProvenance = runtimeContext.authorizationProvenance ?? null;
    if ((existingProvenance || nextProvenance) && JSON.stringify(existingProvenance) !== JSON.stringify(nextProvenance)) {
      throw new Error(`Experiment audit ${audit.id} authorization provenance conflicts with durable state.`);
    }
    auditsIndex.items[existingIndex] = audit;
  } else {
    auditsIndex.items.push(audit);
  }
  auditsIndex.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.experimentAudits, auditsIndex);
  return audit;
}
function bridgeExperimentResultToClaim(root, args = {}) {
  assertGovernanceMutationRegistered("bridge-experiment-result-to-claim", "guarded");
  assertNoPublicExperimentProvenance(args, "Public experiment bridge mutations");
  const target = assertTaskScopedMutationTarget(root, "bridge-experiment-result-to-claim", args);
  assertFollowThroughReady(root, "Bridging an experiment result to a claim", args);
  const bridge = persistExperimentResultClaimBridge(root, args);
  return {
    ...bridge,
    preActionGuidanceSummary: workflowGuidanceSummary(root, args, {
      surface: "dove.experience",
      roleId: "builder",
      subagentSpecialty: "experiment-planner",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "claim-bridge",
      nextAction: bridge.bridgeStatus === "applied" ? "project:dove.review" : "project:dove.experience",
      tags: ["experiment", "claim-bridge", "evidence"],
      statusSummary: { bridgeId: bridge.id, bridgeStatus: bridge.bridgeStatus, claimId: bridge.claimId }
    })
  };
}
function persistExperimentResultClaimBridge(root, args = {}, runtimeContext = {}) {
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const resultsIndex = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const auditsIndex = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
  const result = resolveDurableExperimentResult(resultsIndex, args, "Result bridge");
  if (!result.claimId) {
    throw new Error(`Claim bridge requires result ${result.id} to include claimId.`);
  }
  if (!(result.summary ?? "").trim()) {
    throw new Error(`Claim bridge requires result ${result.id} to include a durable result summary.`);
  }
  if ((result.evidenceLinks ?? []).length === 0) {
    throw new Error(`Claim bridge requires result ${result.id} to include durable evidence links.`);
  }
  const evidenceIntegrity = evidencePathProblemFlags(root, result.evidenceLinks ?? []);
  if (!evidenceIntegrity.satisfied) {
    throw new Error(`Claim bridge requires substantive local experiment evidence for result ${result.id}; evidence problems: ${evidenceIntegrity.flags.join(", ") || "missing-substantive-evidence"}.`);
  }
  const claimIndex = evidence.claims.findIndex((item) => item.id === result.claimId);
  if (claimIndex === -1) {
    throw new Error(`Claim bridge could not find claim ${result.claimId}`);
  }
  const currentClaim = evidence.claims[claimIndex];
  const currentClaimState = { status: currentClaim.status, confidence: currentClaim.confidence };
  const requestedAuditIds = normalizeStringArray10(args.auditIds);
  const auditById = new Map((auditsIndex.items ?? []).map((item) => [item.id, item]));
  const missingAuditIds = requestedAuditIds.filter((id) => !auditById.has(id));
  if (missingAuditIds.length > 0) {
    throw new Error(`Claim bridge requires durable experiment audit records; missing auditIds: ${missingAuditIds.join(", ")}.`);
  }
  const candidateAudits = requestedAuditIds.length > 0 ? requestedAuditIds.map((id) => auditById.get(id)) : (auditsIndex.items ?? []).filter((item) => item.resultId === result.id);
  if (candidateAudits.length === 0) {
    throw new Error(`Claim bridge requires at least one durable experiment audit for result ${result.id}.`);
  }
  const mismatchedAudits = candidateAudits.filter((audit) => audit.resultId !== result.id || audit.claimId !== result.claimId);
  if (mismatchedAudits.length > 0) {
    throw new Error(`Claim bridge audit records do not match result ${result.id}: ${mismatchedAudits.map((audit) => audit.id).join(", ")}.`);
  }
  const candidateAuditVerdict = candidateAudits.some((audit) => audit.auditVerdict === "blocked") ? "blocked" : candidateAudits.some((audit) => audit.auditVerdict === "concern") ? "concern" : "clean";
  const candidateMapping = candidateAuditVerdict !== "clean" ? "integrity-hold" : result.outcome === "supports" ? "supports" : result.outcome === "refutes" || result.outcome === "failed" ? "refutes" : "inconclusive";
  const candidateBridgeId = slugify3(args.id ?? `${result.id}-${candidateMapping}-bridge`);
  const candidateBridge = (bridgeLog.items ?? []).find((item) => item.id === candidateBridgeId) ?? null;
  const resultLinkedBridge = !args.id && result.latestBridgeId ? (bridgeLog.items ?? []).find(
    (item) => item.id === result.latestBridgeId && item.resultId === result.id
  ) ?? null : null;
  if (!args.id && result.latestBridgeId && !resultLinkedBridge) {
    throw new Error(`Claim bridge retry for result ${result.id} cannot find linked bridge ${result.latestBridgeId}.`);
  }
  const existingBridge = candidateBridge ?? resultLinkedBridge;
  const bridgeId = existingBridge?.id ?? candidateBridgeId;
  const implicitResultRetry = Boolean(
    existingBridge && !args.id && resultLinkedBridge?.id === existingBridge.id
  );
  const existingAuditIds = existingBridge ? normalizeStringArray10(existingBridge.auditIds) : [];
  const audits = implicitResultRetry || existingBridge && !Object.hasOwn(args, "auditIds") ? existingAuditIds.map((id) => auditById.get(id)).filter(Boolean) : candidateAudits;
  if ((implicitResultRetry || existingBridge && !Object.hasOwn(args, "auditIds")) && audits.length !== existingAuditIds.length) {
    throw new Error(`Claim bridge ${existingBridge.id} references missing durable experiment audit records.`);
  }
  const auditIds = audits.map((audit) => audit.id);
  const auditVerdict = audits.some((audit) => audit.auditVerdict === "blocked") ? "blocked" : audits.some((audit) => audit.auditVerdict === "concern") ? "concern" : "clean";
  const aggregatedIntegrityFlags = Array.from(new Set(audits.flatMap((audit) => audit.integrityFlags ?? [])));
  const mapping = auditVerdict !== "clean" ? "integrity-hold" : result.outcome === "supports" ? "supports" : result.outcome === "refutes" || result.outcome === "failed" ? "refutes" : "inconclusive";
  const before = existingBridge?.claimStateBefore ?? currentClaimState;
  const bridgeStatus = auditVerdict === "clean" ? "applied" : "held-for-review";
  let after = { ...before };
  let stateChange = "hold";
  if (auditVerdict !== "clean") {
    stateChange = "hold";
  } else if (result.outcome === "supports") {
    after = { status: "supported", confidence: confidenceAfterSupport(before.confidence) };
    stateChange = "promote";
  } else if (result.outcome === "refutes") {
    after = { status: "refuted", confidence: "low" };
    stateChange = "downgrade";
  } else if (result.outcome === "failed") {
    after = { status: "challenged", confidence: "low" };
    stateChange = "downgrade";
  } else {
    after = { status: "inconclusive", confidence: "low" };
    stateChange = "downgrade";
  }
  const bridgeEvent = {
    id: bridgeId,
    experimentId: result.experimentId,
    resultId: result.id,
    claimId: result.claimId,
    mapping,
    confidenceBefore: before.confidence,
    confidenceAfter: after.confidence,
    statusBefore: before.status,
    statusAfter: after.status,
    claimStateBefore: before,
    claimStateAfter: after,
    auditIds,
    auditVerdict,
    integrityFlags: aggregatedIntegrityFlags,
    bridgeStatus,
    stateChange,
    reviewRequiredBeforeFinalize: auditVerdict !== "clean" || mapping !== "supports",
    reason: implicitResultRetry ? existingBridge.reason : args.reason ?? (auditVerdict !== "clean" ? `Experiment ${result.experimentId} cannot update claim ${result.claimId} cleanly because audit verdict is ${auditVerdict}.` : result.summary ?? `Experiment ${result.experimentId} returned ${result.outcome}.`),
    ...runtimeContext.authorizationProvenance ? { authorizationProvenance: runtimeContext.authorizationProvenance } : {},
    updatedAt: nowIso()
  };
  if (existingBridge) {
    const stableExisting = {
      ...existingBridge,
      updatedAt: null
    };
    const stableNext = {
      ...bridgeEvent,
      updatedAt: null
    };
    if (JSON.stringify(stableExisting) !== JSON.stringify(stableNext)) {
      throw new Error(
        `Claim bridge ${bridgeEvent.id} already exists with different durable content.`
      );
    }
    const claimAtBeforeState = currentClaimState.status === existingBridge.claimStateBefore?.status && currentClaimState.confidence === existingBridge.claimStateBefore?.confidence && (currentClaim.latestBridgeId ?? null) !== existingBridge.id;
    if (existingBridge.bridgeStatus !== "applied") {
      if (!claimAtBeforeState) {
        throw new Error(
          `Claim bridge ${existingBridge.id} durable claim state conflicts with its recorded held state.`
        );
      }
      return existingBridge;
    }
    const expectedLatestAuditId = audits[0]?.id ?? null;
    const claimAtAppliedState = currentClaimState.status === existingBridge.claimStateAfter?.status && currentClaimState.confidence === existingBridge.claimStateAfter?.confidence && currentClaim.latestBridgeId === existingBridge.id && currentClaim.bridgeStatus === existingBridge.bridgeStatus && currentClaim.latestAuditVerdict === existingBridge.auditVerdict && (currentClaim.latestAuditId ?? null) === expectedLatestAuditId && (currentClaim.experimentIds ?? []).includes(existingBridge.experimentId);
    if (!claimAtBeforeState && !claimAtAppliedState) {
      throw new Error(
        `Claim bridge ${existingBridge.id} durable claim state conflicts with both its recorded before and applied states.`
      );
    }
    if (claimAtAppliedState) {
      writeText(root, ARTIFACT_PATHS.claims, renderClaimsMarkdown(evidence.claims));
      return existingBridge;
    }
  } else {
    bridgeLog.items.push(bridgeEvent);
    bridgeLog.updatedAt = nowIso();
    writeJson(root, ARTIFACT_PATHS.claimBridgeLog, bridgeLog);
  }
  if (bridgeStatus !== "applied") {
    return bridgeEvent;
  }
  const durableBridge = existingBridge ?? bridgeEvent;
  evidence.claims[claimIndex] = {
    ...currentClaim,
    status: durableBridge.claimStateAfter.status,
    confidence: durableBridge.claimStateAfter.confidence,
    latestAuditId: audits[0]?.id ?? currentClaim.latestAuditId ?? null,
    latestAuditVerdict: durableBridge.auditVerdict,
    latestBridgeId: durableBridge.id,
    bridgeStatus: durableBridge.bridgeStatus,
    experimentIds: Array.from(/* @__PURE__ */ new Set([...currentClaim.experimentIds ?? [], durableBridge.experimentId]))
  };
  evidence.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.evidence, evidence);
  writeText(root, ARTIFACT_PATHS.claims, renderClaimsMarkdown(evidence.claims));
  return durableBridge;
}
function upsertExperimentPlan(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-experiment-plan", "guarded");
  const target = assertTaskScopedMutationTarget(root, "upsert-experiment-plan", args);
  assertFollowThroughReady(root, "Updating an experiment plan", args);
  assertReviewProofBoundaryTransition(root, loadBoard(root), "experiments", "builder", "Updating an experiment plan");
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const plansIndex = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const rawPlan = args.plan ?? args;
  const plan = normalizeExperimentPlan(rawPlan, plansIndex.items.length);
  const existingIndex = plansIndex.items.findIndex((item) => item.id === plan.id);
  const existingPlan = existingIndex >= 0 ? plansIndex.items[existingIndex] : null;
  const nextPlan = {
    ...existingPlan ?? {},
    ...plan,
    claimId: Object.hasOwn(rawPlan, "claimId") ? plan.claimId : existingPlan?.claimId ?? plan.claimId,
    updatedAt: nowIso()
  };
  if (nextPlan.claimId && !evidence.claims.some((claim) => claim.id === nextPlan.claimId)) {
    throw new Error(`Experiment plan ${nextPlan.id} references unknown claim ${nextPlan.claimId}`);
  }
  if (existingIndex >= 0) {
    plansIndex.items[existingIndex] = nextPlan;
  } else {
    plansIndex.items.push(nextPlan);
  }
  plansIndex.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.experimentPlans, plansIndex);
  const resultsIndex = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const audits = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  writeText(root, ARTIFACT_PATHS.experimentLog, renderExperimentLog(plansIndex.items, resultsIndex.items, audits.items));
  const board = loadBoard(root);
  upsertSystemOrchestrationBoard(root, {
    phase: "experiments",
    assignedRole: "builder",
    intentType: "experiment",
    currentFocus: `Advance experiment ${nextPlan.id}.`,
    nextAction: `Record results for ${nextPlan.id}, then audit the outcome.`,
    experimentIds: Array.from(/* @__PURE__ */ new Set([...board.experimentIds, nextPlan.id])),
    activeComparisonTargets: Array.from(/* @__PURE__ */ new Set([...board.activeComparisonTargets, ...nextPlan.comparisonTargets])),
    continuationState: {
      status: "in-progress",
      lastCheckpoint: `Experiment plan ${nextPlan.id} updated.`,
      checkpointHistory: [{ summary: `Experiment plan ${nextPlan.id} updated.`, recordedAt: nowIso() }]
    },
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "upsert-experiment-plan",
    summary: `Updated experiment plan ${nextPlan.id}.`,
    artifactPaths: [ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentLog, ARTIFACT_PATHS.taskPacketsIndex]
  });
  return {
    ...nextPlan,
    preActionGuidanceSummary: workflowGuidanceSummary(root, args, {
      surface: "dove.experience",
      roleId: "builder",
      subagentSpecialty: "experiment-planner",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "experience",
      nextAction: "project:dove.experience",
      tags: ["experiment", "plan"],
      statusSummary: { experimentId: nextPlan.id, status: nextPlan.status }
    })
  };
}
function upsertExperimentResult(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-experiment-result", "guarded");
  assertReviewProofBoundaryTransition(root, loadBoard(root), "experiments", "builder", "Updating an experiment result");
  const taskTarget = assertTaskScopedMutationTarget(root, "upsert-experiment-result", args);
  assertFollowThroughReady(root, "Updating an experiment result", args);
  const plansIndex = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const resultsIndex = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const result = normalizeExperimentResult(args.result ?? args, resultsIndex.items.length);
  const allowedOutcomes = /* @__PURE__ */ new Set(["supports", "refutes", "inconclusive", "failed", "pending"]);
  if (!allowedOutcomes.has(result.outcome)) {
    throw new Error(`Experiment result ${result.id} has invalid outcome ${result.outcome}`);
  }
  const linkedPlan = plansIndex.items.find((item) => item.id === result.experimentId);
  if (!linkedPlan) {
    throw new Error(`Experiment result ${result.id} references unknown experiment ${result.experimentId}`);
  }
  if (linkedPlan.claimId && !result.claimId) {
    throw new Error(`Experiment result ${result.id} must include claimId ${linkedPlan.claimId} from its plan`);
  }
  if (result.claimId && !evidence.claims.some((claim) => claim.id === result.claimId)) {
    throw new Error(`Experiment result ${result.id} references unknown claim ${result.claimId}`);
  }
  if (linkedPlan.claimId && result.claimId && linkedPlan.claimId !== result.claimId) {
    throw new Error(`Experiment result ${result.id} claim ${result.claimId} does not match plan claim ${linkedPlan.claimId}`);
  }
  const existingIndex = resultsIndex.items.findIndex((item) => item.id === result.id);
  if (existingIndex >= 0) {
    resultsIndex.items[existingIndex] = { ...resultsIndex.items[existingIndex], ...result, updatedAt: nowIso() };
  } else {
    resultsIndex.items.push(result);
  }
  resultsIndex.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.experimentResults, resultsIndex);
  const audit = runExperimentAudit(root, {
    resultId: result.id,
    packetId: taskTarget.packetId ?? args.packetId,
    actorRole: args.actorRole
  });
  let bridgeEvent;
  if (audit.auditVerdict === "clean" && result.claimId) {
    bridgeEvent = bridgeExperimentResultToClaim(root, {
      resultId: result.id,
      packetId: taskTarget.packetId ?? args.packetId,
      auditIds: [audit.id],
      actorRole: args.actorRole
    });
  } else {
    const claim = result.claimId ? evidence.claims.find((item) => item.id === result.claimId) : null;
    const before = { status: claim?.status ?? null, confidence: claim?.confidence ?? null };
    bridgeEvent = {
      id: slugify3(`${result.id}-integrity-hold-bridge`),
      experimentId: result.experimentId,
      resultId: result.id,
      claimId: result.claimId,
      mapping: "integrity-hold",
      confidenceBefore: before.confidence,
      confidenceAfter: before.confidence,
      statusBefore: before.status,
      statusAfter: before.status,
      claimStateBefore: before,
      claimStateAfter: before,
      auditIds: [audit.id],
      auditVerdict: audit.auditVerdict,
      integrityFlags: audit.integrityFlags ?? [],
      bridgeStatus: "held-for-review",
      stateChange: "hold",
      reviewRequiredBeforeFinalize: true,
      reason: `Experiment ${result.experimentId} cannot update claim ${result.claimId ?? "unlinked"} until audit integrity gaps are resolved: ${(audit.integrityFlags ?? []).join(", ") || audit.auditVerdict}.`,
      updatedAt: nowIso()
    };
    const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
    bridgeLog.items = [...(bridgeLog.items ?? []).filter((item) => item.id !== bridgeEvent.id), bridgeEvent];
    bridgeLog.updatedAt = nowIso();
    writeJson(root, ARTIFACT_PATHS.claimBridgeLog, bridgeLog);
  }
  const resultIndex = resultsIndex.items.findIndex((item) => item.id === result.id);
  resultsIndex.items[resultIndex] = {
    ...resultsIndex.items[resultIndex],
    latestAuditId: audit.id,
    latestBridgeId: bridgeEvent.id,
    updatedAt: nowIso()
  };
  writeJson(root, ARTIFACT_PATHS.experimentResults, resultsIndex);
  const audits = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  writeText(root, ARTIFACT_PATHS.experimentLog, renderExperimentLog(plansIndex.items, resultsIndex.items, audits.items));
  const board = loadBoard(root);
  const blockers = result.outcome === "failed" || result.outcome === "refutes" || bridgeEvent.mapping !== "supports" ? ensureBlocker(board.blockers, {
    id: `${result.experimentId}-needs-followup`,
    summary: bridgeEvent.mapping === "integrity-hold" ? `Experiment ${result.experimentId} cannot cleanly update claim ${result.claimId} until audit integrity gaps are resolved.` : `Experiment ${result.experimentId} produced ${result.outcome}; reconcile the linked claim before finalization.`,
    status: "open",
    assignedRole: "reviewer",
    evidenceLinks: result.evidenceLinks,
    experimentIds: [result.experimentId],
    currentFocus: `Review the claim impact of ${result.experimentId}.`,
    nextAction: "Use the audit and bridge logs to decide whether the claim should be strengthened, weakened, or rewritten."
  }) : board.blockers;
  upsertSystemOrchestrationBoard(root, {
    phase: "experiments",
    assignedRole: "builder",
    intentType: bridgeEvent.mapping === "supports" ? "experiment" : "repair",
    currentFocus: bridgeEvent.mapping === "supports" ? `Experiment ${result.experimentId} now supports ${result.claimId}.` : bridgeEvent.mapping === "integrity-hold" ? `Experiment ${result.experimentId} is held for review until audit gaps are closed.` : `Experiment ${result.experimentId} needs claim reconciliation.`,
    nextAction: bridgeEvent.mapping === "supports" ? "Refresh the review surfaces before making stronger claims." : bridgeEvent.mapping === "integrity-hold" ? "Repair the experiment audit provenance, then re-run review before finalization." : "Run the review loop and resolve the concern before finalization.",
    blockers,
    evidenceLinks: Array.from(/* @__PURE__ */ new Set([...board.evidenceLinks, ...result.evidenceLinks, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog])),
    activeComparisonTargets: Array.from(/* @__PURE__ */ new Set([...board.activeComparisonTargets, ...result.comparisonTargets])),
    continuationState: {
      status: bridgeEvent.mapping === "supports" ? "ready-to-resume" : "blocked",
      lastCheckpoint: `Experiment result ${result.id} recorded, audited, and bridged to claim ${result.claimId}.`,
      checkpointHistory: [{ summary: `Experiment result ${result.id} recorded.`, recordedAt: nowIso() }]
    },
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "upsert-experiment-result",
    summary: `Updated experiment result ${result.id}.`,
    artifactPaths: [ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.experimentLog, ARTIFACT_PATHS.taskPacketsIndex]
  });
  return {
    ...resultsIndex.items[resultIndex],
    preActionGuidanceSummary: workflowGuidanceSummary(root, args, {
      surface: "dove.experience",
      roleId: "builder",
      subagentSpecialty: "experiment-planner",
      packet: taskTarget.packet,
      stage: taskTarget.packet?.stage ?? "execute",
      workflowKind: "experience-result",
      nextAction: "project:dove.review",
      tags: ["experiment", "result", "claim-bridge", "audit"],
      statusSummary: {
        experimentId: result.experimentId,
        resultId: result.id,
        outcome: result.outcome,
        auditId: audit.id,
        bridgeStatus: bridgeEvent.bridgeStatus
      }
    })
  };
}
function normalizeIssue(issue = {}, index = 0) {
  return {
    id: slugify3(issue.id ?? issue.summary ?? `issue-${index + 1}`),
    reviewer: issue.reviewer ?? `reviewer-${index + 1}`,
    summary: issue.summary ?? `Issue ${index + 1}`,
    severity: issue.severity ?? "medium",
    status: issue.status ?? "open",
    evidenceLinks: normalizeStringArray10(issue.evidenceLinks),
    claimIds: normalizeStringArray10(issue.claimIds),
    experimentIds: normalizeStringArray10(issue.experimentIds),
    responseDirection: issue.responseDirection ?? "clarify",
    ...issue.authorizationProvenance ? { authorizationProvenance: issue.authorizationProvenance } : {},
    authorizationProvenanceHistory: Array.isArray(
      issue.authorizationProvenanceHistory
    ) ? issue.authorizationProvenanceHistory : [],
    sourceReviewIds: normalizeStringArray10(
      issue.sourceReviewIds
    ),
    sourceReviewExecutionClaimId: issue.sourceReviewExecutionClaimId ?? null,
    updatedAt: nowIso()
  };
}
function assertNoReservedRebuttalFields(args = {}) {
  const reservedFields = /* @__PURE__ */ new Set([
    "authorizationProvenance",
    "authorizationProvenanceHistory",
    "sourceReviewId",
    "sourceReviewIds",
    "sourceReviewExecutionClaimId",
    "executionClaimId",
    "updateBoard",
    "refreshDurableSurfaces"
  ]);
  const invalid = [
    ...Object.keys(args ?? {}).filter((key) => reservedFields.has(key)),
    ...(args.issues ?? []).flatMap(
      (issue, index) => Object.keys(issue ?? {}).filter(
        (key) => reservedFields.has(key)
      ).map(
        (key) => `issues[${index}].${key}`
      )
    )
  ];
  if (invalid.length > 0) {
    throw new Error(`Public rebuttal mutations do not accept runtime provenance fields: ${invalid.join(", ")}.`);
  }
}
function normalizeRebuttalIssues(root, args = {}) {
  assertGovernanceMutationRegistered("normalize-rebuttal-issues", "guarded");
  assertNoReservedRebuttalFields(args);
  assertReviewProofBoundaryTransition(root, loadBoard(root), "rebuttal", "builder", "Normalizing rebuttal issues");
  const target = assertTaskScopedMutationTarget(root, "normalize-rebuttal-issues", args);
  assertFollowThroughReady(root, "Normalizing rebuttal issues", args);
  const next = persistRebuttalIssues(root, args);
  return {
    ...next,
    resultCard: rebuttalIssuesResultCard(root, args, next.items ?? []),
    preActionGuidanceSummary: workflowGuidanceSummary(root, args, {
      surface: "dove.review",
      roleId: "reviewer",
      packet: target.packet,
      stage: args.stage ?? target.packet?.stage ?? "audit",
      workflowKind: "review",
      nextAction: "project:dove.rebuttal",
      tags: ["review", "rebuttal", "issue-normalization"],
      statusSummary: { issueCount: next.items?.length ?? 0 }
    })
  };
}
function persistRebuttalIssues(root, args = {}) {
  const issuesIndex = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const provided = Array.isArray(args.issues) ? args.issues.map((issue, index) => {
    const existing = (issuesIndex.items ?? []).find(
      (item) => item.id === issue.id
    ) ?? null;
    return normalizeIssue({
      ...existing,
      ...issue
    }, index);
  }) : [];
  const merged = new Map((issuesIndex.items ?? []).map((issue, index) => {
    const normalized = normalizeIssue(issue, index);
    return [normalized.id, normalized];
  }));
  for (const issue of provided) {
    merged.set(issue.id, issue);
  }
  const items = Array.from(merged.values()).sort((left, right) => left.id.localeCompare(right.id));
  const next = { version: 1, items, updatedAt: nowIso() };
  writeJson(root, ARTIFACT_PATHS.rebuttalIssues, next);
  if (args.updateBoard !== false) {
    upsertSystemOrchestrationBoard(root, {
      phase: "rebuttal",
      assignedRole: "builder",
      intentType: "respond",
      currentFocus: items.length > 0 ? items[0].summary : "Prepare the rebuttal strategy.",
      nextAction: "Turn issues into strategy and response drafts without over-claiming.",
      rebuttalIssueIds: items.map((issue) => issue.id),
      evidenceLinks: Array.from(new Set(items.flatMap((issue) => issue.evidenceLinks))),
      continuationState: {
        status: items.some((issue) => issue.status !== "resolved") ? "in-progress" : "ready-to-resume",
        lastCheckpoint: `Normalized ${items.length} rebuttal issues.`,
        checkpointHistory: [{ summary: `Normalized ${items.length} rebuttal issues.`, recordedAt: nowIso() }]
      },
      reviewRequiredBeforeFinalize: true
    });
  }
  if (args.refreshDurableSurfaces !== false) {
    refreshDurableSurfaces(root, {
      type: "normalize-rebuttal-issues",
      summary: `Normalized ${items.length} rebuttal issues.`,
      artifactPaths: [ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.navigationReport]
    });
  }
  return next;
}
function buildRebuttalStrategy(root, args = {}) {
  assertGovernanceMutationRegistered("build-rebuttal-strategy", "guarded");
  const target = assertTaskScopedMutationTarget(root, "build-rebuttal-strategy", args);
  assertFollowThroughReady(root, "Building the rebuttal strategy", args);
  assertReviewProofBoundaryTransition(root, loadBoard(root), "rebuttal", "builder", "Building the rebuttal strategy");
  const issues = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  if (!Array.isArray(issues.items) || issues.items.length === 0) {
    return missingRebuttalIssuesResult(root, args);
  }
  const board = loadBoard(root);
  const strategy = [
    "# Rebuttal strategy",
    "",
    `- Phase: ${board.currentPhase}`,
    `- Assigned role: ${board.assignedRole}`,
    "",
    "## Issue board",
    "",
    ...issues.items.length > 0 ? issues.items.flatMap((issue) => [
      `### ${issue.id}`,
      "",
      `- Reviewer: ${issue.reviewer}`,
      `- Severity: ${issue.severity}`,
      `- Status: ${issue.status}`,
      `- Response direction: ${issue.responseDirection}`,
      `- Evidence links: ${issue.evidenceLinks.join(", ") || "none"}`,
      `- Claim IDs: ${issue.claimIds.join(", ") || "none"}`,
      `- Experiment IDs: ${issue.experimentIds.join(", ") || "none"}`,
      `- Recommended owner: ${issue.responseDirection === "fix" ? "planner + builder/researcher" : "builder/revision-lead"}`,
      `- Required action: ${issue.responseDirection === "fix" ? "Update evidence or experiment coverage before final response." : "Clarify scope and cite the strongest existing evidence."}`,
      ""
    ]) : ["No rebuttal issues recorded."]
  ].join("\n");
  const responseDraft = [
    "# Rebuttal response draft",
    "",
    ...issues.items.length > 0 ? issues.items.flatMap((issue) => [
      `## ${issue.id}`,
      "",
      `Reviewer concern: ${issue.summary}`,
      "",
      `Planned response: ${issue.responseDirection === "fix" ? "Describe the concrete revision and point to the updated evidence." : "Clarify the evidence and scope without over-claiming."}`,
      "",
      `Evidence links: ${issue.evidenceLinks.join(", ") || "none"}`,
      ""
    ]) : ["No issues available for rebuttal drafting."]
  ].join("\n");
  writeText(root, ARTIFACT_PATHS.rebuttalStrategy, strategy);
  writeText(root, ARTIFACT_PATHS.rebuttalResponseDraft, responseDraft);
  upsertSystemOrchestrationBoard(root, {
    phase: "rebuttal",
    assignedRole: "builder",
    intentType: "respond",
    currentFocus: issues.items.length > 0 ? issues.items[0].summary : "Prepare the rebuttal.",
    nextAction: "Draft concise evidence-backed responses.",
    rebuttalIssueIds: issues.items.map((issue) => issue.id),
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "build-rebuttal-strategy",
    summary: `Built rebuttal strategy for ${issues.items.length} issues.`,
    artifactPaths: [ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft, ARTIFACT_PATHS.navigationReport]
  });
  return {
    strategyPath: ARTIFACT_PATHS.rebuttalStrategy,
    responseDraftPath: ARTIFACT_PATHS.rebuttalResponseDraft,
    issueCount: issues.items.length,
    resultCard: rebuttalStrategyResultCard(root, args, issues.items.length),
    preActionGuidanceSummary: workflowGuidanceSummary(root, args, {
      surface: "dove.rebuttal",
      roleId: "builder",
      subagentSpecialty: "revision-lead",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "rebuttal",
      nextAction: "project:dove.review",
      tags: ["rebuttal", "revision", "evidence"],
      statusSummary: { issueCount: issues.items.length }
    })
  };
}
function readSnapshot(root, snapshotId) {
  const snapshotPath = path12.join(ARTIFACT_PATHS.versionSnapshotsDir, `${snapshotId}.json`);
  const raw = readJson(root, snapshotPath, null);
  if (!raw) {
    return null;
  }
  return {
    id: raw.id ?? snapshotId,
    label: raw.label ?? snapshotId,
    parentVersionId: raw.parentVersionId ?? null,
    summary: raw.summary ?? "",
    createdAt: raw.createdAt ?? null,
    dove: {
      title: raw.dove?.title ?? raw.paper?.title ?? "",
      venue: raw.dove?.venue ?? raw.paper?.venue ?? "",
      objective: raw.dove?.objective ?? raw.paper?.objective ?? "",
      thesis: raw.dove?.thesis ?? raw.paper?.thesis ?? "",
      audience: raw.dove?.audience ?? raw.paper?.audience ?? ""
    },
    board: {
      currentPhase: raw.board?.currentPhase ?? "versions",
      assignedRole: raw.board?.assignedRole ?? "planner",
      intentType: raw.board?.intentType ?? "version",
      currentFocus: raw.board?.currentFocus ?? "",
      nextAction: raw.board?.nextAction ?? "",
      continuationState: raw.board?.continuationState ?? createContinuationState(),
      experimentIds: Array.isArray(raw.board?.experimentIds) ? raw.board.experimentIds : [],
      rebuttalIssueIds: Array.isArray(raw.board?.rebuttalIssueIds) ? raw.board.rebuttalIssueIds : [],
      activeComparisonTargets: Array.isArray(raw.board?.activeComparisonTargets) ? raw.board.activeComparisonTargets : []
    },
    sections: raw.sections && typeof raw.sections === "object" ? raw.sections : {},
    draftSnapshot: raw.draftSnapshot && typeof raw.draftSnapshot === "object" ? raw.draftSnapshot : {},
    claimIds: Array.isArray(raw.claimIds) ? raw.claimIds : [],
    evidenceLinks: Array.isArray(raw.evidenceLinks) ? raw.evidenceLinks : [],
    reviewVerdict: raw.reviewVerdict ?? "not-reviewed",
    openReviewItems: Array.isArray(raw.openReviewItems) ? raw.openReviewItems : [],
    unresolvedConcernIds: Array.isArray(raw.unresolvedConcernIds) ? raw.unresolvedConcernIds : [],
    experimentPlanIds: Array.isArray(raw.experimentPlanIds) ? raw.experimentPlanIds : [],
    experimentResultIds: Array.isArray(raw.experimentResultIds) ? raw.experimentResultIds : [],
    experimentAuditIds: Array.isArray(raw.experimentAuditIds) ? raw.experimentAuditIds : [],
    claimBridgeIds: Array.isArray(raw.claimBridgeIds) ? raw.claimBridgeIds : []
  };
}
function createVersionSnapshot(root, args = {}) {
  assertGovernanceMutationRegistered("create-version-snapshot", "guarded");
  assertTaskScopedMutationTarget(root, "create-version-snapshot", args);
  assertFollowThroughReady(root, "Creating a version snapshot", args);
  assertFinalizeReviewGate(root, "Creating a version snapshot");
  const state = loadState(root);
  const board = loadBoard(root);
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const reviews = readJson(root, ARTIFACT_PATHS.reviewState, { version: 2, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  const plans = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const results = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const audits = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
  const versions = readJson(root, ARTIFACT_PATHS.versionsIndex, { version: 1, currentVersionId: null, items: [], lineage: [], updatedAt: null });
  const versionId = slugify3(args.versionId ?? args.label ?? `${state.dove.title}-${versions.items.length + 1}`);
  const snapshot = {
    id: versionId,
    label: args.label ?? versionId,
    parentVersionId: args.parentVersionId ?? versions.currentVersionId ?? null,
    summary: args.summary ?? "Manual paper snapshot.",
    createdAt: nowIso(),
    dove: state.dove,
    board: {
      currentPhase: board.currentPhase,
      assignedRole: board.assignedRole,
      intentType: board.intentType,
      currentFocus: board.currentFocus,
      nextAction: board.nextAction,
      continuationState: board.continuationState,
      experimentIds: board.experimentIds,
      rebuttalIssueIds: board.rebuttalIssueIds,
      activeComparisonTargets: board.activeComparisonTargets
    },
    sections: state.sections,
    draftSnapshot: collectDraftSnapshot(root, state.sections),
    claimIds: evidence.claims.map((claim) => claim.id),
    evidenceLinks: Array.from(new Set(evidence.claims.flatMap((claim) => claim.evidenceLinks ?? []))),
    reviewVerdict: reviews.lastVerdict,
    openReviewItems: reviews.openItems,
    unresolvedConcernIds: reviews.unresolvedConcernIds ?? [],
    experimentPlanIds: plans.items.map((item) => item.id),
    experimentResultIds: results.items.map((item) => item.id),
    experimentAuditIds: audits.items.map((item) => item.id),
    claimBridgeIds: bridgeLog.items.map((item) => item.id)
  };
  writeJson(root, path12.join(ARTIFACT_PATHS.versionSnapshotsDir, `${versionId}.json`), snapshot);
  const existingIndex = versions.items.findIndex((item) => item.id === versionId);
  const summaryEntry = {
    id: versionId,
    label: snapshot.label,
    parentVersionId: snapshot.parentVersionId,
    summary: snapshot.summary,
    createdAt: snapshot.createdAt
  };
  if (existingIndex >= 0) {
    versions.items[existingIndex] = summaryEntry;
  } else {
    versions.items.push(summaryEntry);
  }
  versions.currentVersionId = versionId;
  versions.lineage = versions.items.map((item) => ({ id: item.id, parentVersionId: item.parentVersionId ?? null }));
  versions.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.versionsIndex, versions);
  const snapshotIds = Array.from(/* @__PURE__ */ new Set([...board.versionLineage?.snapshotIds ?? [], versionId]));
  upsertSystemOrchestrationBoard(root, {
    phase: "versions",
    assignedRole: "planner",
    intentType: "version",
    currentFocus: `Snapshot ${versionId} recorded.`,
    nextAction: "Compare it to the previous version if the paper changed materially.",
    versionLineage: {
      currentVersionId: versionId,
      parentVersionId: snapshot.parentVersionId,
      snapshotIds
    },
    continuationState: {
      status: "ready-to-resume",
      lastCheckpoint: `Version snapshot ${versionId} created.`,
      checkpointHistory: [{ summary: `Version snapshot ${versionId} created.`, recordedAt: nowIso() }]
    }
  });
  refreshDurableSurfaces(root, {
    type: "create-version-snapshot",
    summary: `Created version snapshot ${versionId}.`,
    artifactPaths: [ARTIFACT_PATHS.versionsIndex, path12.join(ARTIFACT_PATHS.versionSnapshotsDir, `${versionId}.json`), ARTIFACT_PATHS.taskPacketsIndex]
  });
  return {
    ...snapshot,
    resultCard: versionSnapshotResultCard(root, args, snapshot, {
      sectionCount: Object.keys(state.sections).length,
      claimCount: evidence.claims.length,
      reviewVerdict: reviews.lastVerdict
    })
  };
}
function compareVersions(root, args = {}) {
  assertGovernanceMutationRegistered("compare-versions", "guarded");
  assertTaskScopedMutationTarget(root, "compare-versions", args);
  assertFollowThroughReady(root, "Comparing versions", args);
  assertFinalizeReviewGate(root, "Comparing versions");
  const fromId = args.fromVersionId;
  const toId = args.toVersionId;
  const fromSnapshot = readSnapshot(root, fromId);
  const toSnapshot = readSnapshot(root, toId);
  if (!fromSnapshot || !toSnapshot) {
    throw new Error(`Both snapshots must exist before comparison. Missing: ${!fromSnapshot ? fromId : toId}`);
  }
  const comparison = {
    id: slugify3(`${fromId}-vs-${toId}`),
    fromVersionId: fromId,
    toVersionId: toId,
    createdAt: nowIso(),
    objectiveChanged: fromSnapshot.dove.objective !== toSnapshot.dove.objective,
    thesisChanged: fromSnapshot.dove.thesis !== toSnapshot.dove.thesis,
    addedClaimIds: toSnapshot.claimIds.filter((id) => !fromSnapshot.claimIds.includes(id)),
    removedClaimIds: fromSnapshot.claimIds.filter((id) => !toSnapshot.claimIds.includes(id)),
    addedExperimentResultIds: toSnapshot.experimentResultIds.filter((id) => !fromSnapshot.experimentResultIds.includes(id)),
    addedAuditIds: (toSnapshot.experimentAuditIds ?? []).filter((id) => !(fromSnapshot.experimentAuditIds ?? []).includes(id)),
    addedBridgeIds: (toSnapshot.claimBridgeIds ?? []).filter((id) => !(fromSnapshot.claimBridgeIds ?? []).includes(id)),
    openReviewItemsAdded: toSnapshot.openReviewItems.filter((item) => !fromSnapshot.openReviewItems.includes(item)),
    openReviewItemsRemoved: fromSnapshot.openReviewItems.filter((item) => !toSnapshot.openReviewItems.includes(item)),
    unresolvedConcernsAdded: (toSnapshot.unresolvedConcernIds ?? []).filter((id) => !(fromSnapshot.unresolvedConcernIds ?? []).includes(id)),
    unresolvedConcernsRemoved: (fromSnapshot.unresolvedConcernIds ?? []).filter((id) => !(toSnapshot.unresolvedConcernIds ?? []).includes(id)),
    addedEvidenceLinks: toSnapshot.evidenceLinks.filter((item) => !fromSnapshot.evidenceLinks.includes(item)),
    removedEvidenceLinks: fromSnapshot.evidenceLinks.filter((item) => !toSnapshot.evidenceLinks.includes(item)),
    addedCitationKeys: Array.from(new Set(Object.values(toSnapshot.draftSnapshot ?? {}).flatMap((item) => item.citedKeys ?? []))).filter((item) => !Array.from(new Set(Object.values(fromSnapshot.draftSnapshot ?? {}).flatMap((entry) => entry.citedKeys ?? []))).includes(item)),
    removedCitationKeys: Array.from(new Set(Object.values(fromSnapshot.draftSnapshot ?? {}).flatMap((item) => item.citedKeys ?? []))).filter((item) => !Array.from(new Set(Object.values(toSnapshot.draftSnapshot ?? {}).flatMap((entry) => entry.citedKeys ?? []))).includes(item)),
    verdictChanged: fromSnapshot.reviewVerdict !== toSnapshot.reviewVerdict,
    changedDraftSections: Object.keys({ ...fromSnapshot.draftSnapshot ?? {}, ...toSnapshot.draftSnapshot ?? {} }).flatMap((sectionId) => {
      const before = fromSnapshot.draftSnapshot?.[sectionId]?.contentHash ?? null;
      const after = toSnapshot.draftSnapshot?.[sectionId]?.contentHash ?? null;
      return before === after ? [] : [{ sectionId, from: before, to: after }];
    }),
    sectionStatusChanges: Object.keys({ ...fromSnapshot.sections, ...toSnapshot.sections }).flatMap((sectionId) => {
      const before = fromSnapshot.sections[sectionId]?.status ?? null;
      const after = toSnapshot.sections[sectionId]?.status ?? null;
      return before === after ? [] : [{ sectionId, from: before, to: after }];
    })
  };
  const comparisons = readJson(root, ARTIFACT_PATHS.versionComparisons, { version: 1, items: [], activeTargets: [], updatedAt: null });
  const existingIndex = comparisons.items.findIndex((item) => item.id === comparison.id);
  if (existingIndex >= 0) {
    comparisons.items[existingIndex] = comparison;
  } else {
    comparisons.items.push(comparison);
  }
  comparisons.activeTargets = [fromId, toId];
  comparisons.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.versionComparisons, comparisons);
  writeText(root, ARTIFACT_PATHS.versionComparisonReport, [
    "# Latest version comparison",
    "",
    `- From: ${fromId}`,
    `- To: ${toId}`,
    `- Objective changed: ${comparison.objectiveChanged}`,
    `- Thesis changed: ${comparison.thesisChanged}`,
    `- Verdict changed: ${comparison.verdictChanged}`,
    "",
    "## Added audits",
    "",
    ...comparison.addedAuditIds.length > 0 ? comparison.addedAuditIds.map((id) => `- ${id}`) : ["- None"],
    "",
    "## Added claim-bridge events",
    "",
    ...comparison.addedBridgeIds.length > 0 ? comparison.addedBridgeIds.map((id) => `- ${id}`) : ["- None"],
    "",
    "## Added evidence links",
    "",
    ...comparison.addedEvidenceLinks.length > 0 ? comparison.addedEvidenceLinks.map((id) => `- ${id}`) : ["- None"],
    "",
    "## Removed evidence links",
    "",
    ...comparison.removedEvidenceLinks.length > 0 ? comparison.removedEvidenceLinks.map((id) => `- ${id}`) : ["- None"],
    "",
    "## Added citation keys",
    "",
    ...comparison.addedCitationKeys.length > 0 ? comparison.addedCitationKeys.map((id) => `- ${id}`) : ["- None"],
    "",
    "## Added claims",
    "",
    ...comparison.addedClaimIds.length > 0 ? comparison.addedClaimIds.map((id) => `- ${id}`) : ["- None"],
    "",
    "## Unresolved concern delta",
    "",
    ...comparison.unresolvedConcernsAdded.length > 0 ? comparison.unresolvedConcernsAdded.map((id) => `- Added: ${id}`) : ["- No newly added unresolved concerns."],
    ...comparison.unresolvedConcernsRemoved.length > 0 ? comparison.unresolvedConcernsRemoved.map((id) => `- Removed: ${id}`) : ["- No resolved concerns removed from the set."],
    "",
    "## Section status changes",
    "",
    ...comparison.sectionStatusChanges.length > 0 ? comparison.sectionStatusChanges.map((change) => `- ${change.sectionId}: ${change.from} -> ${change.to}`) : ["- None"],
    "",
    "## Changed draft sections",
    "",
    ...comparison.changedDraftSections.length > 0 ? comparison.changedDraftSections.map((change) => `- ${change.sectionId}: content hash changed`) : ["- None"]
  ].join("\n"));
  upsertSystemOrchestrationBoard(root, {
    phase: "versions",
    assignedRole: "planner",
    intentType: "version",
    currentFocus: `Compare ${fromId} to ${toId}.`,
    nextAction: "Use the comparison report to explain what changed and why.",
    activeComparisonTargets: [fromId, toId]
  });
  refreshDurableSurfaces(root, {
    type: "compare-versions",
    summary: `Compared versions ${fromId} and ${toId}.`,
    artifactPaths: [ARTIFACT_PATHS.versionComparisons, ARTIFACT_PATHS.versionComparisonReport, ARTIFACT_PATHS.navigationReport]
  });
  return {
    ...comparison,
    resultCard: versionComparisonResultCard(root, args, comparison)
  };
}

// src/core/source-trust.mjs
import crypto8 from "node:crypto";
import fs11 from "node:fs";
import path13 from "node:path";
var SOURCE_LIFECYCLE_STATES = Object.freeze(["candidate", "verified", "rejected"]);
var TRUSTED_SOURCE_VERIFICATION_ISSUERS = /* @__PURE__ */ new Map([
  ["dove-reviewer", "reviewer"],
  ["dove-system", "system"]
]);
function normalizeText(value) {
  return typeof value === "string" ? value.trim().replace(/\s+/gu, " ") : "";
}
function normalizeIdentityText(value) {
  return normalizeText(value).normalize("NFKC").toLowerCase();
}
function normalizeDoi(value) {
  const text3 = normalizeIdentityText(value).replace(/^https?:\/\/(?:dx\.)?doi\.org\//u, "");
  return text3.startsWith("10.") ? text3 : "";
}
function normalizeUrl(value) {
  const text3 = normalizeText(value);
  if (!text3) return "";
  try {
    const parsed = new URL(text3);
    if (!["http:", "https:"].includes(parsed.protocol)) return "";
    parsed.hash = "";
    parsed.hostname = parsed.hostname.toLowerCase();
    if (parsed.protocol === "https:" && parsed.port === "443" || parsed.protocol === "http:" && parsed.port === "80") parsed.port = "";
    return parsed.toString();
  } catch {
    return "";
  }
}
function sourceDoi(source = {}) {
  return normalizeDoi(source.doi) || normalizeDoi(source.url) || normalizeDoi(source.locator);
}
function sourceUrl(source = {}) {
  return normalizeUrl(source.url) || normalizeUrl(source.locator);
}
function canonicalSourceIdentity(source = {}) {
  return {
    doi: sourceDoi(source),
    url: sourceUrl(source),
    locator: normalizeIdentityText(source.locator),
    title: normalizeIdentityText(source.title),
    authors: (Array.isArray(source.authors) ? source.authors : []).map(normalizeIdentityText).filter(Boolean).sort()
  };
}
function sourceIdentityFingerprint(source = {}) {
  return crypto8.createHash("sha256").update(JSON.stringify(canonicalSourceIdentity(source))).digest("hex");
}
function verificationMaterialState(root, materialPath) {
  const normalizedPath = normalizeText(materialPath);
  if (!normalizedPath) return { valid: false, reason: "source-verification-material-missing" };
  const inspection = inspectDeclaredPath(root, normalizedPath, { requireNonEmpty: true });
  if (inspection.status !== "existing") {
    return { valid: false, reason: `source-verification-material-${inspection.status}` };
  }
  const canonicalPath = inspection.canonicalRelativePath ?? inspection.normalizedPath;
  const fullPath = path13.resolve(root, canonicalPath);
  const materialHash = crypto8.createHash("sha256").update(fs11.readFileSync(fullPath)).digest("hex");
  return { valid: true, materialPath: canonicalPath, materialHash };
}
function trustedVerificationProvenance(verification = {}) {
  const issuer = normalizeText(verification.issuer);
  const issuerRole = normalizeText(verification.issuerRole).toLowerCase();
  const expectedRole = TRUSTED_SOURCE_VERIFICATION_ISSUERS.get(issuer);
  if (!expectedRole || issuerRole !== expectedRole) return false;
  return normalizeText(verification.provenance) === "trusted-internal-transition";
}
function readSourceTrustState(root) {
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 2, items: [], updatedAt: null });
  const verifications = readJson(root, ARTIFACT_PATHS.sourceVerifications, { version: 1, items: [], updatedAt: null });
  return { sources, verifications };
}
function sourceReferenceMap(sources = []) {
  return new Map(sources.flatMap((source) => [
    source.id ? [source.id, source] : null,
    source.citationKey ? [source.citationKey, source] : null,
    source.locator ? [source.locator, source] : null,
    source.url ? [source.url, source] : null,
    source.doi ? [source.doi, source] : null
  ].filter(Boolean)));
}
function sourceEligibility(source, verifications = [], options = {}) {
  if (!source) return { eligible: false, reason: "unknown-source", source: null, verification: null };
  const verification = [...verifications].reverse().find((item) => item.sourceId === source.id) ?? null;
  if (!verification) return { eligible: false, reason: `source-${source.lifecycle ?? "candidate"}`, source, verification: null };
  if (verification.decision !== "verified") {
    return { eligible: false, reason: `source-${verification.decision ?? source.lifecycle ?? "candidate"}`, source, verification };
  }
  if (!trustedVerificationProvenance(verification)) {
    return { eligible: false, reason: "source-verification-untrusted-provenance", source, verification };
  }
  const fingerprint = sourceIdentityFingerprint(source);
  if (verification.fingerprint !== fingerprint) {
    return { eligible: false, reason: "source-identity-changed", source, verification };
  }
  const sourcePacketIds = new Set(Array.isArray(source.packetIds) ? source.packetIds : []);
  if (!verification.packetId || !sourcePacketIds.has(verification.packetId)) {
    return { eligible: false, reason: "source-packet-binding-mismatch", source, verification };
  }
  if (options.root) {
    const material = verificationMaterialState(options.root, verification.materialPath);
    if (!material.valid) {
      return { eligible: false, reason: material.reason, source, verification };
    }
    if (!verification.materialHash || verification.materialHash !== material.materialHash) {
      return { eligible: false, reason: "source-verification-material-changed", source, verification };
    }
  } else if (!verification.materialPath || !verification.materialHash) {
    return { eligible: false, reason: "source-verification-material-unavailable", source, verification };
  }
  return { eligible: true, reason: "verified-source", source, verification };
}
function evaluateSourceReferences(root, references = []) {
  const { sources, verifications } = readSourceTrustState(root);
  const byReference = sourceReferenceMap(sources.items ?? []);
  return references.map((reference) => {
    const source = byReference.get(reference) ?? null;
    return { reference, ...sourceEligibility(source, verifications.items ?? [], { root }) };
  });
}
function evaluateNoteReferences(root, references = [], packetId = null) {
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const { sources, verifications } = readSourceTrustState(root);
  const bySource = sourceReferenceMap(sources.items ?? []);
  const normalizedPacketId = normalizeText(packetId);
  return references.map((reference) => {
    const note = (notes.items ?? []).find((item) => item.id === reference) ?? null;
    if (!note) return { reference, eligible: false, reason: "unknown-note", note: null, sources: [] };
    if (!normalizedPacketId || !(note.packetIds ?? []).includes(normalizedPacketId)) {
      return { reference, eligible: false, reason: "note-packet-binding-mismatch", note, sources: [] };
    }
    const sourceIds = Array.isArray(note.sourceIds) ? note.sourceIds : [];
    if (sourceIds.length === 0) {
      return { reference, eligible: false, reason: "note-source-missing", note, sources: [] };
    }
    const sourceEvaluations = sourceIds.map((sourceId) => {
      const source = bySource.get(sourceId) ?? null;
      const eligibility = sourceEligibility(source, verifications.items ?? [], { root });
      if (eligibility.eligible && eligibility.verification?.packetId !== normalizedPacketId) {
        return { reference: sourceId, ...eligibility, eligible: false, reason: "source-verification-packet-mismatch" };
      }
      return { reference: sourceId, ...eligibility };
    });
    const failure = sourceEvaluations.find((item) => !item.eligible);
    return {
      reference,
      eligible: !failure,
      reason: failure ? failure.reason : "verified-note",
      note,
      sources: sourceEvaluations
    };
  });
}
function querySources(root, args = {}) {
  const { sources, verifications } = readSourceTrustState(root);
  const sourceId = normalizeText(args.sourceId ?? args.id);
  const packetId = normalizeText(args.packetId ?? args.taskPacketId ?? args.missionPacketId);
  const lifecycle = normalizeText(args.lifecycle).toLowerCase();
  const limit = Math.min(200, Math.max(1, Number.isFinite(Number(args.limit)) ? Math.trunc(Number(args.limit)) : 50));
  const items = (sources.items ?? []).filter((source) => !sourceId || [source.id, source.citationKey, source.locator, source.url, source.doi].includes(sourceId)).filter((source) => !packetId || (source.packetIds ?? []).includes(packetId)).map((source) => {
    const eligibility = sourceEligibility(source, verifications.items ?? [], { root });
    return {
      ...source,
      eligibility: {
        eligible: eligibility.eligible,
        reason: eligibility.reason,
        verificationId: eligibility.verification?.id ?? null,
        decision: eligibility.verification?.decision ?? null,
        packetId: eligibility.verification?.packetId ?? null,
        checkedAt: eligibility.verification?.checkedAt ?? null
      }
    };
  }).filter((source) => !lifecycle || source.eligibility.decision === lifecycle || source.lifecycle === lifecycle).slice(0, limit);
  return {
    status: items.length > 0 ? "ok" : "empty",
    sourceCount: items.length,
    items,
    bookkeeping: [ARTIFACT_PATHS.sources, ARTIFACT_PATHS.sourceVerifications]
  };
}
function assertEligibleSourceReferences(root, references = [], label = "Evidence") {
  const evaluations = evaluateSourceReferences(root, references);
  const failures = evaluations.filter((item) => !item.eligible);
  if (failures.length > 0) {
    throw new Error(`${label} requires verified sources with matching identity fingerprints: ${failures.map((item) => `${item.reference} (${item.reason})`).join(", ")}.`);
  }
  return evaluations;
}
function normalizedAuditEvidence(root, value) {
  if (!Array.isArray(value)) return [];
  return value.map((item, index) => {
    if (!item || typeof item !== "object" || Array.isArray(item)) {
      throw new Error(`verify_source auditEvidence[${index}] must be an object with reference, kind, and observation.`);
    }
    const keys = Object.keys(item);
    const unknown = keys.filter((key) => !["reference", "kind", "observation"].includes(key));
    if (unknown.length > 0) {
      throw new Error(`verify_source auditEvidence[${index}] does not accept unknown fields: ${unknown.join(", ")}.`);
    }
    const reference = normalizeText(item.reference);
    const kind = normalizeText(item.kind).toLowerCase();
    const observation = normalizeText(item.observation);
    if (!reference || !observation || !["source", "capture"].includes(kind)) {
      throw new Error(`verify_source auditEvidence[${index}] requires non-empty reference and observation, with kind source or capture.`);
    }
    if (kind === "source") {
      const validReference = /^https:\/\/[^\s]+$/iu.test(reference) || /^(?:doi:)?10\.\d{4,9}\/[^\s]+$/iu.test(reference) || /^arxiv:(?:\d{4}\.\d{4,5}|[a-z-]+\/\d{7})(?:v\d+)?$/iu.test(reference);
      if (!validReference) {
        throw new Error(`verify_source auditEvidence[${index}].reference must be HTTPS, DOI, or arXiv when kind is source.`);
      }
    } else {
      const inspection = inspectDeclaredPath(root, reference, { requireNonEmpty: true });
      if (inspection.status !== "existing") {
        throw new Error(`verify_source auditEvidence[${index}] capture must be a safe existing non-empty local file (${inspection.reason ?? inspection.status}).`);
      }
    }
    return { reference, kind, observation };
  });
}
function auditEvidenceMatchesSource(source, evidence) {
  const identity = canonicalSourceIdentity(source);
  const references = new Set([
    identity.url,
    identity.doi,
    identity.doi ? `doi:${identity.doi}` : "",
    identity.locator
  ].filter(Boolean));
  return evidence.some((item) => {
    if (item.kind !== "source") return false;
    const reference = normalizeIdentityText(item.reference);
    const referenceDoi = normalizeDoi(item.reference);
    const referenceUrl = normalizeUrl(item.reference);
    return references.has(reference) || referenceDoi && referenceDoi === identity.doi || referenceUrl && referenceUrl === identity.url;
  });
}
function prepareSourceVerification(root, source, args = {}, packetId = null) {
  const method = normalizeText(args.method);
  const checkedMaterial = normalizeText(args.checkedMaterial);
  const auditEvidence = normalizedAuditEvidence(root, args.auditEvidence);
  const decision = normalizeText(args.decision).toLowerCase();
  if (!source) throw new Error("verify_source references an unknown source.");
  if (!method) throw new Error("verify_source requires a verification method.");
  if (!checkedMaterial) throw new Error("verify_source requires checkedMaterial describing the material actually inspected.");
  if (auditEvidence.length === 0) throw new Error("verify_source requires at least one structured auditEvidence item.");
  if (!auditEvidenceMatchesSource(source, auditEvidence)) {
    throw new Error("verify_source requires at least one auditEvidence source reference matching the registered source identity.");
  }
  if (decision !== "rejected") {
    throw new Error("Public verify_source only records rejection. Positive verification requires a trusted internal Reviewer/system transition that is not exposed through this API.");
  }
  const normalizedPacketId = normalizeText(packetId);
  if (!normalizedPacketId || !(Array.isArray(source.packetIds) ? source.packetIds : []).includes(normalizedPacketId)) {
    throw new Error("verify_source requires the resolved packet to be bound to the registered source.");
  }
  const timestamp = nowIso();
  const record = {
    id: `source-verification-${source.id}-${Date.now().toString(36)}`,
    sourceId: source.id,
    packetId: normalizedPacketId,
    fingerprint: sourceIdentityFingerprint(source),
    decision,
    method,
    checkedMaterial,
    auditEvidence,
    checkedAt: timestamp
  };
  const index = readJson(root, ARTIFACT_PATHS.sourceVerifications, { version: 1, items: [], updatedAt: null });
  index.items.push(record);
  index.updatedAt = timestamp;
  return { record, index };
}

// src/core/evidence.mjs
function slugify4(value) {
  return String(value).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function claimsGuidanceSummary(root, args = {}, target = {}, statusSummary = {}) {
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: "dove.draft",
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: args.summary ?? args.title ?? args.claims?.[0]?.text ?? null,
    roleId: "builder",
    subagentSpecialty: "researcher",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "execute",
      primaryRole: "builder"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: "project:dove.review",
    routeHint: "project:dove.draft",
    workflowKind: "claims",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "execute",
    tags: ["claims", "evidence", "citations"],
    statusSummary
  }));
}
function normalizeClaim(claim, index) {
  return {
    id: slugify4(claim.id ?? `claim-${index + 1}`),
    text: claim.text ?? "",
    sectionId: slugify4(claim.sectionId ?? "introduction"),
    sourceIds: Array.isArray(claim.sourceIds) ? claim.sourceIds : [],
    noteIds: Array.isArray(claim.noteIds) ? claim.noteIds : [],
    experimentIds: Array.isArray(claim.experimentIds) ? claim.experimentIds : [],
    evidenceLinks: Array.isArray(claim.evidenceLinks) ? claim.evidenceLinks : [],
    status: claim.status ?? "draft",
    confidence: claim.confidence ?? "medium",
    latestAuditId: claim.latestAuditId ?? null,
    latestAuditVerdict: claim.latestAuditVerdict ?? null,
    latestBridgeId: claim.latestBridgeId ?? null,
    bridgeStatus: claim.bridgeStatus ?? null,
    gap: claim.gap ?? "",
    packetIds: Array.isArray(claim.packetIds) ? Array.from(new Set(claim.packetIds.filter(Boolean))) : []
  };
}
function renderClaimsMarkdown2(claims) {
  return [
    "# Claims from results",
    "",
    ...claims.flatMap((claim) => [
      `## ${claim.id}`,
      "",
      `- Text: ${claim.text}`,
      `- Section: ${claim.sectionId}`,
      `- Source IDs: ${claim.sourceIds.join(", ") || "none"}`,
      `- Note IDs: ${claim.noteIds.join(", ") || "none"}`,
      `- Experiment IDs: ${claim.experimentIds.join(", ") || "none"}`,
      `- Evidence links: ${claim.evidenceLinks.join(", ") || "none"}`,
      `- Status: ${claim.status}`,
      `- Confidence: ${claim.confidence}`,
      claim.latestAuditId ? `- Latest audit: ${claim.latestAuditId}` : null,
      claim.latestAuditVerdict ? `- Latest audit verdict: ${claim.latestAuditVerdict}` : null,
      claim.latestBridgeId ? `- Latest bridge event: ${claim.latestBridgeId}` : null,
      claim.bridgeStatus ? `- Bridge status: ${claim.bridgeStatus}` : null,
      claim.gap ? `- Gap: ${claim.gap}` : null,
      ""
    ].filter(Boolean))
  ].join("\n");
}
function upsertClaims(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-claims", "guarded");
  const target = assertTaskScopedMutationTarget(root, "upsert-claims", args);
  assertFollowThroughReady(root, "Updating evidence-backed claims", args);
  assertReviewProofBoundaryTransition(root, loadBoard(root), "plan", "planner", "Updating evidence-backed claims");
  const current = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const sourceIds = new Set(sources.items.flatMap((item) => [item.id, item.citationKey].filter(Boolean)));
  const noteIds = new Set(notes.items.map((item) => item.id));
  const inputClaims = Array.isArray(args.claims) ? args.claims.map(normalizeClaim) : [];
  const merged = new Map((Array.isArray(current.claims) ? current.claims : []).map((claim, index) => {
    const normalized = normalizeClaim(claim, index);
    return [normalized.id, normalized];
  }));
  for (const claim of inputClaims) {
    claim.packetIds = Array.from(/* @__PURE__ */ new Set([...claim.packetIds, target.packetId]));
    if (!claim.text.trim()) {
      throw new Error(`Claim ${claim.id} must include text.`);
    }
    if (claim.sourceIds.length === 0) {
      throw new Error(`Claim ${claim.id} must reference at least one source.`);
    }
    const unknownSources = claim.sourceIds.filter((id) => !sourceIds.has(id));
    if (unknownSources.length > 0) {
      throw new Error(`Claim ${claim.id} references unknown sources: ${unknownSources.join(", ")}`);
    }
    assertEligibleSourceReferences(root, claim.sourceIds, `Claim ${claim.id}`);
    const unknownNotes = claim.noteIds.filter((id) => !noteIds.has(id));
    if (unknownNotes.length > 0) {
      throw new Error(`Claim ${claim.id} references unknown notes: ${unknownNotes.join(", ")}`);
    }
    merged.set(claim.id, claim);
  }
  const claims = Array.from(merged.values()).sort((left, right) => left.id.localeCompare(right.id));
  const next = {
    version: 3,
    claims,
    updatedAt: nowIso()
  };
  writeJson(root, ARTIFACT_PATHS.evidence, next);
  writeText(root, ARTIFACT_PATHS.claims, renderClaimsMarkdown2(claims));
  const board = loadBoard(root);
  upsertSystemOrchestrationBoard(root, {
    phase: "plan",
    assignedRole: "planner",
    intentType: "plan",
    currentFocus: `Convert ${claims.length} evidence-backed claims into an executable plan.`,
    nextAction: "Update the plan or outline before drafting stronger conclusions.",
    evidenceLinks: Array.from(/* @__PURE__ */ new Set([...board.evidenceLinks, ARTIFACT_PATHS.claims, ARTIFACT_PATHS.evidence])),
    continuationState: {
      status: "ready-to-resume",
      lastCheckpoint: `Claims index refreshed with ${claims.length} claims.`,
      checkpointHistory: [{ summary: `Claims index refreshed with ${claims.length} claims.`, recordedAt: nowIso() }]
    }
  });
  refreshDurableSurfaces(root, {
    type: "upsert-claims",
    summary: `Upserted ${claims.length} durable claims.`,
    artifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.claims, ARTIFACT_PATHS.navigationReport]
  });
  return {
    ...next,
    preActionGuidanceSummary: claimsGuidanceSummary(root, args, target, {
      claimCount: claims.length,
      inputClaimCount: inputClaims.length
    })
  };
}
function evaluateEvidence(root) {
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const experimentResults = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const audits = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
  const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
  const sourceById = sourceReferenceMap(sources.items ?? []);
  const noteIds = new Set(notes.items.map((item) => item.id));
  const auditsById = new Map((audits.items ?? []).map((item) => [item.id, item]));
  const resultsById = new Map((experimentResults.items ?? []).map((item) => [item.id, item]));
  const unsupportedClaims = [];
  const weakClaims = [];
  const missingSourceRefs = [];
  const missingNoteRefs = [];
  const draftClaimMismatches = [];
  const claimBridgeProblems = [];
  const auditIntegrityFlags = [];
  for (const claim of evidence.claims) {
    const heldBridgeForClaim = (bridgeLog.items ?? []).find((item) => item.claimId === claim.id && (item.bridgeStatus === "held-for-review" || item.auditVerdict === "blocked"));
    if (heldBridgeForClaim && claim.latestBridgeId !== heldBridgeForClaim.id) {
      claimBridgeProblems.push({ claim, reason: "bridge-held-for-review", bridgeId: heldBridgeForClaim.id, auditIds: heldBridgeForClaim.auditIds ?? [] });
    }
    if (!Array.isArray(claim.sourceIds) || claim.sourceIds.length === 0) {
      unsupportedClaims.push(claim);
      continue;
    }
    const sourceEvaluations = evaluateSourceReferences(root, claim.sourceIds);
    const unknownSources = sourceEvaluations.filter((item) => item.reason === "unknown-source").map((item) => item.reference);
    if (unknownSources.length > 0) {
      missingSourceRefs.push({ claim, missing: unknownSources });
      unsupportedClaims.push(claim);
      continue;
    }
    const ineligibleSources = sourceEvaluations.filter((item) => !item.eligible);
    if (ineligibleSources.length > 0) {
      missingSourceRefs.push({ claim, missing: [], ineligible: ineligibleSources.map((item) => ({ sourceId: item.reference, reason: item.reason })) });
      unsupportedClaims.push(claim);
      continue;
    }
    const unknownNotes = (Array.isArray(claim.noteIds) ? claim.noteIds : []).filter((id) => !noteIds.has(id));
    if (unknownNotes.length > 0) {
      missingNoteRefs.push({ claim, missing: unknownNotes });
    }
    if (claim.sourceIds.length === 1 || claim.status === "weak" || claim.confidence === "low") {
      weakClaims.push(claim);
    }
    const latestBridge = claim.latestBridgeId ? bridgeLog.items.find((item) => item.id === claim.latestBridgeId) : null;
    if ((claim.experimentIds ?? []).length > 0 && !latestBridge) {
      claimBridgeProblems.push({ claim, reason: "missing-bridge-event" });
    }
    if (latestBridge && latestBridge.statusAfter !== claim.status) {
      claimBridgeProblems.push({ claim, reason: "stale-bridge-state", bridgeId: latestBridge.id });
    }
    if (latestBridge && (!Array.isArray(latestBridge.auditIds) || latestBridge.auditIds.length === 0)) {
      claimBridgeProblems.push({ claim, reason: "missing-audit-link", bridgeId: latestBridge.id });
    }
    if (latestBridge) {
      const unknownAuditIds = (latestBridge.auditIds ?? []).filter((id) => !auditsById.has(id));
      if (unknownAuditIds.length > 0) {
        claimBridgeProblems.push({ claim, reason: "unknown-audit-link", bridgeId: latestBridge.id, auditIds: unknownAuditIds });
      }
      if (latestBridge.bridgeStatus === "held-for-review" || latestBridge.auditVerdict === "blocked") {
        claimBridgeProblems.push({ claim, reason: "bridge-held-for-review", bridgeId: latestBridge.id, auditIds: latestBridge.auditIds ?? [] });
      }
    }
    const draftPath = `${ARTIFACT_PATHS.draftsDir}/${claim.sectionId}.md`;
    const draftContent = readText(root, draftPath, "");
    if (!draftContent) {
      draftClaimMismatches.push({ claim, reason: "missing-draft" });
      continue;
    }
    const citedKeys = new Set(extractCitationKeysFromText(draftContent));
    const expectedKeys = claim.sourceIds.flatMap((id) => {
      const source = sourceById.get(id);
      return source ? [source.id, source.citationKey].filter(Boolean) : [];
    });
    if (expectedKeys.length > 0 && !expectedKeys.some((key) => citedKeys.has(key))) {
      draftClaimMismatches.push({ claim, reason: "draft-missing-claim-citation", expectedKeys });
    }
  }
  for (const audit of audits.items ?? []) {
    const result = audit.resultId ? resultsById.get(audit.resultId) : null;
    if ((audit.integrityFlags ?? []).length > 0 || audit.auditVerdict === "blocked") {
      auditIntegrityFlags.push(audit);
    }
    if ((audit.reviewedArtifactRefs ?? []).length === 0) {
      auditIntegrityFlags.push({ ...audit, integrityFlags: [...audit.integrityFlags ?? [], "missing-reviewed-artifact-refs"] });
    }
    if (result?.latestAuditId && result.latestAuditId !== audit.id && result.id === audit.resultId) {
      auditIntegrityFlags.push({ ...audit, integrityFlags: [...audit.integrityFlags ?? [], "stale-result-audit-pointer"] });
    }
  }
  const citationTodos = [];
  const missingCitationRefs = [];
  for (const draftFile of listDraftFiles(root)) {
    const content = readText(root, `${ARTIFACT_PATHS.draftsDir}/${draftFile}`, "");
    const lines = content.split(/\r?\n/);
    lines.forEach((line, index) => {
      if (/TODO\[citation\]|CITATION NEEDED|TODO: citation/i.test(line)) {
        citationTodos.push({ draftFile, line: index + 1, text: line.trim() });
      }
    });
    for (const key of extractCitationKeysFromText(content)) {
      if (!sourceById.has(key)) {
        missingCitationRefs.push({ draftFile, key });
      }
    }
  }
  return {
    unsupportedClaims,
    weakClaims,
    missingSourceRefs,
    missingNoteRefs,
    missingCitationRefs,
    draftClaimMismatches,
    citationTodos,
    claimBridgeProblems,
    auditIntegrityFlags,
    claimCount: evidence.claims.length
  };
}

// src/core/onboarding.mjs
import fs12 from "node:fs";
import path14 from "node:path";
var DEFAULT_EXCLUDED_DIRS = /* @__PURE__ */ new Set([
  ".git",
  ".dove",
  ".agents",
  ".cache",
  ".claude",
  ".codex",
  ".cursor",
  ".opencode",
  ".trellis",
  ".next",
  ".pytest_cache",
  ".venv",
  "__pycache__",
  "build",
  "coverage",
  "dist",
  "node_modules",
  "docs",
  "mcp",
  "reference_repos",
  "scripts",
  "src",
  "tmp"
]);
var IMAGE_EXTENSIONS = /* @__PURE__ */ new Set([".eps", ".jpeg", ".jpg", ".pdf", ".png", ".svg", ".tif", ".tiff"]);
var TABLE_EXTENSIONS = /* @__PURE__ */ new Set([".csv", ".tsv", ".xlsx"]);
var RESULT_EXTENSIONS = /* @__PURE__ */ new Set([".json", ".jsonl", ".npy", ".npz", ".pkl", ".parquet"]);
var NOTE_EXTENSIONS = /* @__PURE__ */ new Set([".md", ".txt"]);
function normalizeRelativePath2(relativePath) {
  return relativePath.split(path14.sep).join("/");
}
function safeInteger(value, fallback) {
  const parsed = Number.parseInt(value, 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : fallback;
}
function classifyByName(relativePath) {
  const baseName = path14.basename(relativePath).toLowerCase();
  const stem = baseName.replace(/\.[^.]+$/, "");
  const extension = path14.extname(baseName);
  const pathParts = relativePath.toLowerCase().split("/");
  const parentHints = new Set(pathParts.slice(0, -1));
  if (extension === ".bib") {
    return {
      artifactType: "bibliography",
      lifecycleFamily: "knowledge",
      suggestedTarget: ARTIFACT_PATHS.bibliography,
      confidence: "high",
      reason: "BibTeX bibliography file."
    };
  }
  if (["main", "paper", "manuscript", "ms", "article"].includes(stem) && [".tex", ".md"].includes(extension)) {
    return {
      artifactType: "manuscript",
      lifecycleFamily: "structure",
      suggestedTarget: extension === ".md" ? `${ARTIFACT_PATHS.draftsDir}/${stem}.md` : ARTIFACT_PATHS.draftsDir,
      confidence: ["main", "paper", "manuscript"].includes(stem) ? "high" : "medium",
      reason: "Likely primary manuscript entrypoint."
    };
  }
  if (IMAGE_EXTENSIONS.has(extension) && [...parentHints].some((part) => ["fig", "figs", "figure", "figures", "images", "plots"].includes(part))) {
    return {
      artifactType: "figure",
      lifecycleFamily: "structure",
      suggestedTarget: ARTIFACT_PATHS.figuresIndex,
      confidence: "medium",
      reason: "Image or vector asset under a figure-like directory."
    };
  }
  if (TABLE_EXTENSIONS.has(extension) && [...parentHints].some((part) => ["table", "tables", "data"].includes(part)) || /^table[-_\d]/.test(stem)) {
    return {
      artifactType: "table",
      lifecycleFamily: "structure",
      suggestedTarget: ARTIFACT_PATHS.figuresIndex,
      confidence: "medium",
      reason: "Likely table or tabular artifact."
    };
  }
  if ((RESULT_EXTENSIONS.has(extension) || TABLE_EXTENSIONS.has(extension)) && [...parentHints].some((part) => ["result", "results", "experiment", "experiments", "eval", "evaluation", "outputs"].includes(part))) {
    return {
      artifactType: "result",
      lifecycleFamily: "audit",
      suggestedTarget: ARTIFACT_PATHS.experimentResults,
      confidence: "medium",
      reason: "Likely experiment result or evaluation output."
    };
  }
  if (NOTE_EXTENSIONS.has(extension) && /note|notes|reading|literature|survey|annot/i.test(relativePath)) {
    return {
      artifactType: "note",
      lifecycleFamily: "knowledge",
      suggestedTarget: ARTIFACT_PATHS.notes,
      confidence: "medium",
      reason: "Likely research note or literature annotation."
    };
  }
  if (NOTE_EXTENSIONS.has(extension) && /review|reviewer|critique|decision|meta-review/i.test(relativePath)) {
    return {
      artifactType: "review",
      lifecycleFamily: "concern",
      suggestedTarget: ARTIFACT_PATHS.reviewConcerns,
      confidence: "medium",
      reason: "Likely reviewer feedback or decision artifact."
    };
  }
  if (/supplement|appendix|camera-ready|submission|cover[-_ ]?letter|rebuttal|response/i.test(relativePath) && [".tex", ".md", ".pdf", ".txt"].includes(extension)) {
    return {
      artifactType: "submission",
      lifecycleFamily: "structure",
      suggestedTarget: ARTIFACT_PATHS.versionsIndex,
      confidence: "low",
      reason: "Likely submission, appendix, rebuttal, or versioned paper artifact."
    };
  }
  return null;
}
function walkFiles(root, options = {}) {
  const maxDepth = safeInteger(options.maxDepth, 6);
  const maxFiles = safeInteger(options.maxFiles, 3e3);
  const excludedDirs = /* @__PURE__ */ new Set([...DEFAULT_EXCLUDED_DIRS, ...Array.isArray(options.excludeDirs) ? options.excludeDirs : []]);
  const files = [];
  const warnings = [];
  function walk(currentDir, depth) {
    if (files.length >= maxFiles) {
      return;
    }
    let entries = [];
    try {
      entries = fs12.readdirSync(currentDir, { withFileTypes: true });
    } catch (error) {
      warnings.push(`Could not read ${normalizeRelativePath2(path14.relative(root, currentDir)) || "."}: ${error instanceof Error ? error.message : String(error)}`);
      return;
    }
    for (const entry of entries) {
      if (files.length >= maxFiles) {
        warnings.push(`Scan stopped after ${maxFiles} files.`);
        return;
      }
      const fullPath = path14.join(currentDir, entry.name);
      const relativePath = normalizeRelativePath2(path14.relative(root, fullPath));
      if (entry.isDirectory()) {
        if (excludedDirs.has(entry.name) || depth >= maxDepth) {
          continue;
        }
        walk(fullPath, depth + 1);
        continue;
      }
      if (entry.isFile()) {
        files.push(relativePath);
      }
    }
  }
  walk(root, 0);
  return { files: files.sort(), warnings };
}
function buildConflicts(mappings) {
  const conflicts = [];
  const manuscriptMappings = mappings.filter((item) => item.artifactType === "manuscript" && item.confidence === "high");
  if (manuscriptMappings.length > 1) {
    conflicts.push({
      type: "multiple-primary-manuscripts",
      sourcePaths: manuscriptMappings.map((item) => item.sourcePath),
      suggestedResolution: "Choose one primary manuscript entrypoint before writing or importing drafts."
    });
  }
  const bibliographyMappings = mappings.filter((item) => item.artifactType === "bibliography");
  if (bibliographyMappings.length > 1) {
    conflicts.push({
      type: "multiple-bibliographies",
      sourcePaths: bibliographyMappings.map((item) => item.sourcePath),
      suggestedResolution: "Select the canonical bibliography or merge entries before syncing citations."
    });
  }
  return conflicts;
}
function buildRecommendedNextActions(mappings, conflicts, writeMap) {
  const actions = [];
  if (!writeMap) {
    actions.push("Run `dove onboard . --write-map` to persist this proposal under .dove/workspace/artifact-map.json.");
  }
  if (conflicts.length > 0) {
    actions.push("Resolve mapping conflicts before importing or rewriting paper artifacts.");
  }
  if (mappings.some((item) => item.artifactType === "manuscript")) {
    actions.push("Use `project:dove.mission` before converting a legacy manuscript into the design/checklist/implementation/acceptance flow.");
  }
  if (mappings.some((item) => item.artifactType === "bibliography")) {
    actions.push("Use `project:dove.source` after selecting the canonical bibliography.");
  }
  if (mappings.some((item) => item.artifactType === "review")) {
    actions.push("Use `project:dove.review` or `project:dove.rebuttal` after mapping reviewer feedback artifacts.");
  }
  return actions;
}
function buildProposal(root, options = {}) {
  const { files, warnings } = walkFiles(root, options);
  const mappings = [];
  const unmapped = [];
  for (const sourcePath of files) {
    const classified = classifyByName(sourcePath);
    if (!classified) {
      const extension = path14.extname(sourcePath).toLowerCase();
      if ([".tex", ".md", ".bib", ".pdf", ".png", ".jpg", ".jpeg", ".svg", ".csv", ".tsv", ".json"].includes(extension)) {
        unmapped.push({ sourcePath, reason: "Recognized paper-adjacent extension but no confident lifecycle mapping." });
      }
      continue;
    }
    mappings.push({
      sourcePath,
      artifactType: classified.artifactType,
      lifecycleFamily: normalizeLifecycleFamilyId(classified.lifecycleFamily, "knowledge"),
      suggestedTarget: classified.suggestedTarget,
      confidence: classified.confidence,
      reason: classified.reason,
      action: "map-reference-only"
    });
  }
  const conflicts = buildConflicts(mappings);
  const writeMap = Boolean(options.writeMap);
  return {
    version: 1,
    mode: "onboarding-artifact-map",
    proposalOnly: !writeMap,
    noAutoApply: true,
    writeMap,
    artifactMapPath: ARTIFACT_PATHS.workspaceArtifactMap,
    summary: {
      scannedFileCount: files.length,
      mappingCount: mappings.length,
      conflictCount: conflicts.length,
      unmappedCount: unmapped.length,
      manuscriptCount: mappings.filter((item) => item.artifactType === "manuscript").length,
      bibliographyCount: mappings.filter((item) => item.artifactType === "bibliography").length
    },
    mappings,
    conflicts,
    unmapped,
    warnings,
    recommendedNextActions: buildRecommendedNextActions(mappings, conflicts, writeMap)
  };
}
function discoverPaperArtifacts(root, args = {}) {
  const proposal = buildProposal(root, args);
  if (args.writeMap) {
    writeJson(root, ARTIFACT_PATHS.workspaceArtifactMap, {
      ...proposal,
      proposalOnly: true,
      writeMap: true,
      writtenAt: (/* @__PURE__ */ new Date()).toISOString()
    });
    return {
      ...proposal,
      proposalOnly: true,
      written: [ARTIFACT_PATHS.workspaceArtifactMap]
    };
  }
  return {
    ...proposal,
    written: []
  };
}
function queryDoveOnboarding(root, args = {}) {
  const proposal = discoverPaperArtifacts(root, { ...args, writeMap: false });
  return {
    ...proposal,
    mode: "dove-onboarding-query",
    proposalOnly: true,
    noAutoApply: true,
    writeMap: false,
    written: [],
    writes: [],
    diagnostics: {
      noCommandExecution: true,
      noGitInspection: true,
      noSourceMutation: true,
      writeMapForcedFalse: true
    }
  };
}

// src/core/dove.mjs
import fs14 from "node:fs";
import path16 from "node:path";

// src/core/paper-audit.mjs
import fs13 from "node:fs";
import path15 from "node:path";
function cloneFallback2(fallback) {
  return typeof fallback === "function" ? fallback() : structuredClone(fallback);
}
function safeReadJson(root, relativePath, fallback, readErrors) {
  const fullPath = resolvePath(root, relativePath);
  if (!fs13.existsSync(fullPath)) {
    return cloneFallback2(fallback);
  }
  try {
    return JSON.parse(fs13.readFileSync(fullPath, "utf8"));
  } catch (error) {
    readErrors.push({
      path: relativePath,
      message: error instanceof Error ? error.message : String(error)
    });
    return cloneFallback2(fallback);
  }
}
function safeLoadState(root, readErrors) {
  return normalizeState(safeReadJson(root, ARTIFACT_PATHS.state, createDefaultState, readErrors));
}
function safeListDraftFiles(root) {
  const draftsDir = resolvePath(root, ARTIFACT_PATHS.draftsDir);
  if (!fs13.existsSync(draftsDir)) {
    return [];
  }
  return fs13.readdirSync(draftsDir, { withFileTypes: true }).filter((entry) => entry.isFile() && entry.name.endsWith(".md") && entry.name !== "README.md").map((entry) => entry.name);
}
function normalizeStringArray11(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return value.filter((item) => typeof item === "string").map((item) => item.trim()).filter(Boolean);
}
function normalizeFigureArtifactPath(value) {
  if (typeof value !== "string") {
    return null;
  }
  const normalized = value.trim();
  return normalized.length > 0 ? normalized : null;
}
function isSafeProjectRelativePath(relativePath) {
  if (!relativePath || path15.isAbsolute(relativePath)) {
    return false;
  }
  const normalized = path15.normalize(relativePath);
  return normalized !== "." && !normalized.startsWith("..") && !normalized.includes(`${path15.sep}..${path15.sep}`);
}
function figurePathLooksPortable(relativePath) {
  if (!relativePath) {
    return true;
  }
  return isSafeProjectRelativePath(relativePath) && !relativePath.startsWith(".dove/") && !relativePath.includes("node_modules/");
}
function figureIssueId(figureId, code) {
  return `figure-${figureId}-${code}`.replace(/[^a-zA-Z0-9_-]+/g, "-").toLowerCase();
}
function responseOwnerForFigureIssue(issue) {
  if (issue.stage === "brief") {
    return "builder";
  }
  if (issue.stage === "editable" || issue.stage === "final-contract") {
    return "builder";
  }
  return "planner";
}
function buildFigureIssue({ figure, code, severity, stage, summary, artifactPaths, timestamp, claimIds, experimentIds, reviewConcernIds, rebuttalIssueIds }) {
  return {
    id: figureIssueId(figure.id, code),
    figureId: figure.id,
    severity,
    code,
    stage,
    summary,
    claimIds: claimIds ?? figure.targetClaimIds,
    experimentIds: experimentIds ?? figure.relatedExperimentIds,
    reviewConcernIds: reviewConcernIds ?? figure.reviewConcernIds,
    rebuttalIssueIds: rebuttalIssueIds ?? figure.rebuttalIssueIds,
    artifactPaths,
    updatedAt: timestamp
  };
}
function safeEvaluateEvidence(root, readErrors) {
  const evidence = safeReadJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null }, readErrors);
  const sources = safeReadJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null }, readErrors);
  const notes = safeReadJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null }, readErrors);
  const experimentResults = safeReadJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null }, readErrors);
  const audits = safeReadJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null }, readErrors);
  const bridgeLog = safeReadJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null }, readErrors);
  const sourceById = new Map((sources.items ?? []).flatMap((item) => [[item.id, item], item.citationKey ? [item.citationKey, item] : null].filter(Boolean)));
  const noteIds = new Set((notes.items ?? []).map((item) => item.id));
  const auditsById = new Map((audits.items ?? []).map((item) => [item.id, item]));
  const resultsById = new Map((experimentResults.items ?? []).map((item) => [item.id, item]));
  const unsupportedClaims = [];
  const weakClaims = [];
  const missingSourceRefs = [];
  const missingNoteRefs = [];
  const draftClaimMismatches = [];
  const claimBridgeProblems = [];
  const auditIntegrityFlags = [];
  for (const claim of evidence.claims ?? []) {
    const heldBridgeForClaim = (bridgeLog.items ?? []).find((item) => item.claimId === claim.id && (item.bridgeStatus === "held-for-review" || item.auditVerdict === "blocked"));
    if (heldBridgeForClaim && claim.latestBridgeId !== heldBridgeForClaim.id) {
      claimBridgeProblems.push({ claim, reason: "bridge-held-for-review", bridgeId: heldBridgeForClaim.id, auditIds: heldBridgeForClaim.auditIds ?? [] });
    }
    if (!Array.isArray(claim.sourceIds) || claim.sourceIds.length === 0) {
      unsupportedClaims.push(claim);
      continue;
    }
    const unknownSources = claim.sourceIds.filter((id) => !sourceById.has(id));
    if (unknownSources.length > 0) {
      missingSourceRefs.push({ claim, missing: unknownSources });
      unsupportedClaims.push(claim);
      continue;
    }
    const unknownNotes = (Array.isArray(claim.noteIds) ? claim.noteIds : []).filter((id) => !noteIds.has(id));
    if (unknownNotes.length > 0) {
      missingNoteRefs.push({ claim, missing: unknownNotes });
    }
    if (claim.sourceIds.length === 1 || claim.status === "weak" || claim.confidence === "low") {
      weakClaims.push(claim);
    }
    const latestBridge = claim.latestBridgeId ? (bridgeLog.items ?? []).find((item) => item.id === claim.latestBridgeId) : null;
    if ((claim.experimentIds ?? []).length > 0 && !latestBridge) {
      claimBridgeProblems.push({ claim, reason: "missing-bridge-event" });
    }
    if (latestBridge && latestBridge.statusAfter !== claim.status) {
      claimBridgeProblems.push({ claim, reason: "stale-bridge-state", bridgeId: latestBridge.id });
    }
    if (latestBridge && (!Array.isArray(latestBridge.auditIds) || latestBridge.auditIds.length === 0)) {
      claimBridgeProblems.push({ claim, reason: "missing-audit-link", bridgeId: latestBridge.id });
    }
    if (latestBridge) {
      const unknownAuditIds = (latestBridge.auditIds ?? []).filter((id) => !auditsById.has(id));
      if (unknownAuditIds.length > 0) {
        claimBridgeProblems.push({ claim, reason: "unknown-audit-link", bridgeId: latestBridge.id, auditIds: unknownAuditIds });
      }
      if (latestBridge.bridgeStatus === "held-for-review" || latestBridge.auditVerdict === "blocked") {
        claimBridgeProblems.push({ claim, reason: "bridge-held-for-review", bridgeId: latestBridge.id, auditIds: latestBridge.auditIds ?? [] });
      }
    }
    const draftPath = `${ARTIFACT_PATHS.draftsDir}/${claim.sectionId}.md`;
    const draftContent = readText(root, draftPath, "");
    if (!draftContent) {
      draftClaimMismatches.push({ claim, reason: "missing-draft" });
      continue;
    }
    const citedKeys = new Set(extractCitationKeysFromText(draftContent));
    const expectedKeys = claim.sourceIds.flatMap((id) => {
      const source = sourceById.get(id);
      return source ? [source.id, source.citationKey].filter(Boolean) : [];
    });
    if (expectedKeys.length > 0 && !expectedKeys.some((key) => citedKeys.has(key))) {
      draftClaimMismatches.push({ claim, reason: "draft-missing-claim-citation", expectedKeys });
    }
  }
  for (const audit of audits.items ?? []) {
    const result = audit.resultId ? resultsById.get(audit.resultId) : null;
    if ((audit.integrityFlags ?? []).length > 0 || audit.auditVerdict === "blocked") {
      auditIntegrityFlags.push(audit);
    }
    if ((audit.reviewedArtifactRefs ?? []).length === 0) {
      auditIntegrityFlags.push({ ...audit, integrityFlags: [...audit.integrityFlags ?? [], "missing-reviewed-artifact-refs"] });
    }
    if (result?.latestAuditId && result.latestAuditId !== audit.id && result.id === audit.resultId) {
      auditIntegrityFlags.push({ ...audit, integrityFlags: [...audit.integrityFlags ?? [], "stale-result-audit-pointer"] });
    }
  }
  const citationTodos = [];
  const missingCitationRefs = [];
  for (const draftFile of safeListDraftFiles(root)) {
    const content = readText(root, `${ARTIFACT_PATHS.draftsDir}/${draftFile}`, "");
    const lines = content.split(/\r?\n/);
    lines.forEach((line, index) => {
      if (/TODO\[citation\]|CITATION NEEDED|TODO: citation/i.test(line)) {
        citationTodos.push({ draftFile, line: index + 1, text: line.trim() });
      }
    });
    for (const key of extractCitationKeysFromText(content)) {
      if (!sourceById.has(key)) {
        missingCitationRefs.push({ draftFile, key });
      }
    }
  }
  return {
    unsupportedClaims,
    weakClaims,
    missingSourceRefs,
    missingNoteRefs,
    missingCitationRefs,
    draftClaimMismatches,
    citationTodos,
    claimBridgeProblems,
    auditIntegrityFlags,
    claimCount: (evidence.claims ?? []).length
  };
}
function safeEvaluateFigurePipeline(root, state, readErrors) {
  const evidence = safeReadJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null }, readErrors);
  const experimentPlans = safeReadJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null }, readErrors);
  const experimentResults = safeReadJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null }, readErrors);
  const reviewConcerns = safeReadJson(root, ARTIFACT_PATHS.reviewConcerns, createReviewConcernsIndex, readErrors);
  const rebuttalIssues = safeReadJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null }, readErrors);
  const figures = safeReadJson(root, ARTIFACT_PATHS.figuresIndex, { version: 1, items: [], updatedAt: null }, readErrors);
  const briefs = safeReadJson(root, ARTIFACT_PATHS.figureBriefs, { version: 1, items: [], updatedAt: null }, readErrors);
  const segments = safeReadJson(root, ARTIFACT_PATHS.figureSegments, { version: 1, items: [], updatedAt: null }, readErrors);
  const templates = safeReadJson(root, ARTIFACT_PATHS.figureTemplates, { version: 1, items: [], updatedAt: null }, readErrors);
  const editable = safeReadJson(root, ARTIFACT_PATHS.figureEditableIndex, { version: 1, items: [], updatedAt: null }, readErrors);
  const finals = safeReadJson(root, ARTIFACT_PATHS.figureFinalIndex, { version: 1, items: [], updatedAt: null }, readErrors);
  const claimIds = new Set((evidence.claims ?? []).map((claim) => claim.id));
  const sectionIds = new Set(Object.keys(state.sections ?? {}));
  const experimentIds = /* @__PURE__ */ new Set([
    ...(experimentPlans.items ?? []).map((item) => item.id),
    ...(experimentResults.items ?? []).map((item) => item.experimentId)
  ]);
  const reviewConcernIds = new Set((reviewConcerns.items ?? []).map((item) => item.id));
  const rebuttalIssueIds = new Set((rebuttalIssues.items ?? []).map((item) => item.id));
  const briefByFigure = new Map((briefs.items ?? []).map((item) => [item.figureId, item]));
  const segmentByFigure = new Map((segments.items ?? []).map((item) => [item.figureId, item]));
  const templateByFigure = new Map((templates.items ?? []).map((item) => [item.figureId, item]));
  const editableByFigure = new Map((editable.items ?? []).map((item) => [item.figureId, item]));
  const finalByFigure = new Map((finals.items ?? []).map((item) => [item.figureId, item]));
  const stagePathUsage = /* @__PURE__ */ new Map();
  const timestamp = (/* @__PURE__ */ new Date(0)).toISOString();
  const items = [];
  const issues = [];
  function normalizeFigureListField(figure, field, stage, artifactPaths) {
    const rawValue = figure[field];
    const normalized = normalizeStringArray11(rawValue);
    if (rawValue !== void 0 && rawValue !== null && !Array.isArray(rawValue)) {
      issues.push(buildFigureIssue({
        figure,
        code: `malformed-${field}`,
        severity: "medium",
        stage,
        summary: `Figure ${figure.id} has a malformed ${field} field and it was ignored during QA validation.`,
        artifactPaths,
        timestamp
      }));
    }
    return normalized;
  }
  for (const figure of figures.items ?? []) {
    for (const [field, stage] of [["templateSvgPath", "template"], ["editableSvgPath", "editable"], ["finalSvgPath", "final-contract"]]) {
      const normalizedPath = normalizeFigureArtifactPath(figure[field]);
      if (!normalizedPath || !isSafeProjectRelativePath(normalizedPath)) {
        continue;
      }
      const current = stagePathUsage.get(normalizedPath) ?? [];
      current.push({ figureId: figure.id, field, stage });
      stagePathUsage.set(normalizedPath, current);
    }
  }
  for (const figure of figures.items ?? []) {
    const figureIssues = [];
    const sourceSections = normalizeFigureListField(figure, "sourceSections", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const sourceArtifactPaths = normalizeFigureListField(figure, "sourceArtifactPaths", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const targetClaimIds = normalizeFigureListField(figure, "targetClaimIds", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const relatedExperimentIds = normalizeFigureListField(figure, "relatedExperimentIds", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const reviewConcernIdsForFigure = normalizeFigureListField(figure, "reviewConcernIds", "editable", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureQa]);
    const rebuttalIssueIdsForFigure = normalizeFigureListField(figure, "rebuttalIssueIds", "final-contract", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa]);
    const brief = briefByFigure.get(figure.id);
    const segment = segmentByFigure.get(figure.id);
    const template = templateByFigure.get(figure.id);
    const editableArtifact = editableByFigure.get(figure.id);
    const finalArtifact = finalByFigure.get(figure.id);
    const stageArtifacts = {
      brief: Boolean(brief),
      segments: Boolean(segment),
      template: Boolean(template),
      editable: Boolean(editableArtifact),
      finalContract: Boolean(finalArtifact)
    };
    const stageArtifactPaths = [
      ARTIFACT_PATHS.figuresIndex,
      ARTIFACT_PATHS.figureBriefs,
      ARTIFACT_PATHS.figureSegments,
      ARTIFACT_PATHS.figureTemplates,
      ARTIFACT_PATHS.figureEditableIndex,
      ARTIFACT_PATHS.figureFinalIndex,
      ARTIFACT_PATHS.figureQa
    ];
    const fileChecks = { stagedArtifacts: {}, sourceArtifacts: [] };
    for (const [stageName, present] of Object.entries(stageArtifacts)) {
      if (!present) {
        figureIssues.push(buildFigureIssue({
          figure,
          code: "missing-stage-artifact",
          severity: "high",
          stage: stageName,
          summary: `Figure ${figure.id} is missing the ${stageName} stage artifact.`,
          artifactPaths: stageArtifactPaths,
          timestamp
        }));
      }
    }
    if (targetClaimIds.length === 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "missing-claim-linkage",
        severity: "high",
        stage: "brief",
        summary: `Figure ${figure.id} has no linked target claims.`,
        claimIds: [],
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const missingSections = sourceSections.filter((sectionId) => !sectionIds.has(sectionId));
    if (missingSections.length > 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "missing-source-sections",
        severity: "medium",
        stage: "brief",
        summary: `Figure ${figure.id} references unknown source sections: ${missingSections.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const missingClaims = targetClaimIds.filter((claimId) => !claimIds.has(claimId));
    if (missingClaims.length > 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "unknown-claims",
        severity: "high",
        stage: "brief",
        summary: `Figure ${figure.id} references unknown target claims: ${missingClaims.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const missingExperiments = relatedExperimentIds.filter((experimentId) => !experimentIds.has(experimentId));
    if (missingExperiments.length > 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "unknown-experiments",
        severity: "medium",
        stage: "brief",
        summary: `Figure ${figure.id} references unknown related experiments: ${missingExperiments.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const missingReviewConcerns = reviewConcernIdsForFigure.filter((id) => !reviewConcernIds.has(id));
    if (missingReviewConcerns.length > 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "unknown-review-concerns",
        severity: "medium",
        stage: "editable",
        summary: `Figure ${figure.id} references unknown review concerns: ${missingReviewConcerns.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const missingRebuttalIssues = rebuttalIssueIdsForFigure.filter((id) => !rebuttalIssueIds.has(id));
    if (missingRebuttalIssues.length > 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "unknown-rebuttal-issues",
        severity: "medium",
        stage: "final-contract",
        summary: `Figure ${figure.id} references unknown rebuttal issues: ${missingRebuttalIssues.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const portablePathProblems = [figure.templateSvgPath, figure.editableSvgPath, figure.finalSvgPath].filter((relativePath) => !figurePathLooksPortable(relativePath));
    if (portablePathProblems.length > 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "non-portable-paths",
        severity: "high",
        stage: "template",
        summary: `Figure ${figure.id} uses non-portable artifact paths: ${portablePathProblems.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const stagePathChecks = [
      { field: "templateSvgPath", stage: "template", label: "template SVG", value: figure.templateSvgPath, artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureQa] },
      { field: "editableSvgPath", stage: "editable", label: "editable SVG", value: figure.editableSvgPath, artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureQa] },
      { field: "finalSvgPath", stage: "final-contract", label: "final SVG", value: figure.finalSvgPath, artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa] }
    ];
    for (const pathCheck of stagePathChecks) {
      const normalizedPath = normalizeFigureArtifactPath(pathCheck.value);
      const exists = normalizedPath && isSafeProjectRelativePath(normalizedPath) ? fs13.existsSync(resolvePath(root, normalizedPath)) : false;
      fileChecks.stagedArtifacts[pathCheck.field] = { path: normalizedPath, exists };
      if (!normalizedPath || !isSafeProjectRelativePath(normalizedPath)) {
        figureIssues.push(buildFigureIssue({
          figure,
          code: `malformed-${pathCheck.field}`,
          severity: "high",
          stage: pathCheck.stage,
          summary: `Figure ${figure.id} has a malformed ${pathCheck.label.toLowerCase()} path.`,
          artifactPaths: pathCheck.artifactPaths,
          timestamp
        }));
        continue;
      }
      if (!exists) {
        figureIssues.push(buildFigureIssue({
          figure,
          code: `missing-${pathCheck.field}-file`,
          severity: "high",
          stage: pathCheck.stage,
          summary: `Figure ${figure.id} is missing the ${pathCheck.label.toLowerCase()} file at ${normalizedPath}.`,
          artifactPaths: pathCheck.artifactPaths,
          timestamp
        }));
      }
    }
    const distinctStagePaths = stagePathChecks.map((item) => normalizeFigureArtifactPath(item.value)).filter((item) => item && isSafeProjectRelativePath(item));
    if (new Set(distinctStagePaths).size !== distinctStagePaths.length) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "colliding-stage-paths",
        severity: "high",
        stage: "template",
        summary: `Figure ${figure.id} reuses the same file path for multiple staged SVG artifacts.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    for (const pathCheck of stagePathChecks) {
      const normalizedPath = normalizeFigureArtifactPath(pathCheck.value);
      const collisions = normalizedPath ? stagePathUsage.get(normalizedPath) ?? [] : [];
      if (collisions.some((entry) => entry.figureId !== figure.id)) {
        const otherFigureIds = Array.from(new Set(collisions.filter((entry) => entry.figureId !== figure.id).map((entry) => entry.figureId))).sort();
        figureIssues.push(buildFigureIssue({
          figure,
          code: `shared-${pathCheck.field}`,
          severity: "high",
          stage: pathCheck.stage,
          summary: `Figure ${figure.id} shares ${pathCheck.label.toLowerCase()} path ${normalizedPath} with ${otherFigureIds.join(", ")}.`,
          artifactPaths: pathCheck.artifactPaths,
          timestamp
        }));
      }
    }
    const normalizedSourceArtifactPaths = sourceArtifactPaths.map(normalizeFigureArtifactPath);
    for (let index = 0; index < normalizedSourceArtifactPaths.length; index += 1) {
      const sourcePath = normalizedSourceArtifactPaths[index];
      const exists = sourcePath && isSafeProjectRelativePath(sourcePath) ? fs13.existsSync(resolvePath(root, sourcePath)) : false;
      fileChecks.sourceArtifacts.push({ path: sourcePath, exists });
      if (!sourcePath || !isSafeProjectRelativePath(sourcePath)) {
        figureIssues.push(buildFigureIssue({
          figure,
          code: `malformed-source-artifact-${index + 1}`,
          severity: "medium",
          stage: "brief",
          summary: `Figure ${figure.id} has a malformed source artifact path entry.`,
          artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
          timestamp
        }));
        continue;
      }
      if (!exists) {
        figureIssues.push(buildFigureIssue({
          figure,
          code: `missing-source-artifact-${index + 1}`,
          severity: "medium",
          stage: "brief",
          summary: `Figure ${figure.id} references a missing source artifact at ${sourcePath}.`,
          artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
          timestamp
        }));
      }
    }
    const templatePathMismatch = template && [template.templateSvgPath !== figure.templateSvgPath, template.editableSvgPath !== figure.editableSvgPath, template.finalSvgPath !== figure.finalSvgPath].some(Boolean);
    if (templatePathMismatch) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "inconsistent-template-stage-paths",
        severity: "high",
        stage: "template",
        summary: `Figure ${figure.id} has inconsistent staged SVG paths between the figure index and template artifact.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const editablePathMismatch = editableArtifact && [editableArtifact.templateSvgPath !== figure.templateSvgPath, editableArtifact.editableSvgPath !== figure.editableSvgPath, editableArtifact.finalSvgPath !== figure.finalSvgPath].some(Boolean);
    if (editablePathMismatch) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "inconsistent-editable-stage-paths",
        severity: "high",
        stage: "editable",
        summary: `Figure ${figure.id} has inconsistent staged SVG paths between the figure index and editable artifact.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (finalArtifact && finalArtifact.finalSvgPath !== figure.finalSvgPath) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "inconsistent-final-contract-path",
        severity: "high",
        stage: "final-contract",
        summary: `Figure ${figure.id} has inconsistent final SVG paths between the figure index and final contract.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (brief && JSON.stringify(brief.sourceArtifactPaths ?? []) !== JSON.stringify(figure.sourceArtifactPaths ?? [])) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "inconsistent-source-artifact-paths",
        severity: "medium",
        stage: "brief",
        summary: `Figure ${figure.id} has inconsistent source artifact paths between the figure index and brief artifact.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (editableArtifact && editableArtifact.finalSvgPath !== figure.finalSvgPath) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "inconsistent-final-path",
        severity: "high",
        stage: "editable",
        summary: `Figure ${figure.id} has inconsistent final SVG paths across staged artifacts.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (((figure.reviewConcernIds ?? []).length > 0 || (figure.rebuttalIssueIds ?? []).length > 0) && (figure.reviewNotes ?? []).length === 0) {
      figureIssues.push(buildFigureIssue({
        figure,
        code: "missing-review-notes",
        severity: "medium",
        stage: "editable",
        summary: `Figure ${figure.id} links to review or rebuttal context but has no durable review notes.`,
        artifactPaths: [ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    issues.push(...figureIssues.map((issue) => ({
      ...issue,
      responseOwnerRole: responseOwnerForFigureIssue(issue)
    })));
    items.push({
      figureId: figure.id,
      qaStatus: figureIssues.some((issue) => issue.severity === "high") ? "blocked" : figureIssues.length > 0 ? "needs-review" : "ready",
      issueCount: figureIssues.length,
      openIssueIds: figureIssues.map((issue) => issue.id),
      stageArtifacts,
      targetClaimIds: figure.targetClaimIds,
      relatedExperimentIds: figure.relatedExperimentIds,
      reviewConcernIds: figure.reviewConcernIds,
      rebuttalIssueIds: figure.rebuttalIssueIds,
      fileChecks,
      updatedAt: timestamp
    });
  }
  return { version: 1, items, issues, updatedAt: timestamp };
}
function findingId(category, code, seed) {
  return `audit-${category}-${code}-${String(seed).replace(/[^a-zA-Z0-9_-]+/g, "-").toLowerCase()}`;
}
function addFinding(findings, { id, severity, category, confidence = "high", summary, artifactPaths = [], suggestedNextCommand, claimIds = [], experimentIds = [], reviewConcernIds = [], rebuttalIssueIds = [], proposalOnly = true }) {
  findings.push({
    id,
    severity,
    category,
    confidence,
    summary,
    artifactPaths: Array.from(new Set(artifactPaths.filter(Boolean))),
    claimIds,
    experimentIds,
    reviewConcernIds,
    rebuttalIssueIds,
    suggestedNextCommand,
    proposalOnly,
    noAutoApply: true
  });
}
function countBy(items, field) {
  const counts = {};
  for (const item of items) {
    const key = item[field] ?? "unknown";
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return counts;
}
function suggestedCommandsFromFindings(findings) {
  return Array.from(new Set(findings.map((finding) => finding.suggestedNextCommand).filter(Boolean)));
}
function artifactPathsRead() {
  return [
    ARTIFACT_PATHS.state,
    ARTIFACT_PATHS.orchestrationBoard,
    ARTIFACT_PATHS.workspaceIndex,
    ARTIFACT_PATHS.checklist,
    ARTIFACT_PATHS.sources,
    ARTIFACT_PATHS.notes,
    ARTIFACT_PATHS.evidence,
    ARTIFACT_PATHS.experimentResults,
    ARTIFACT_PATHS.experimentAudits,
    ARTIFACT_PATHS.claimBridgeLog,
    ARTIFACT_PATHS.reviewState,
    ARTIFACT_PATHS.reviewConcerns,
    ARTIFACT_PATHS.versionComparisons,
    ARTIFACT_PATHS.figuresIndex,
    ARTIFACT_PATHS.figureBriefs,
    ARTIFACT_PATHS.figureSegments,
    ARTIFACT_PATHS.figureTemplates,
    ARTIFACT_PATHS.figureEditableIndex,
    ARTIFACT_PATHS.figureFinalIndex,
    ARTIFACT_PATHS.figureQa,
    ARTIFACT_PATHS.draftsDir
  ];
}
function queryPaperAudit(root, args = {}) {
  const readErrors = [];
  const state = safeLoadState(root, readErrors);
  const board = safeReadJson(root, ARTIFACT_PATHS.orchestrationBoard, {}, readErrors);
  const reviewState = safeReadJson(root, ARTIFACT_PATHS.reviewState, createReviewState, readErrors);
  const reviewConcerns = safeReadJson(root, ARTIFACT_PATHS.reviewConcerns, createReviewConcernsIndex, readErrors);
  const versionComparisons = safeReadJson(root, ARTIFACT_PATHS.versionComparisons, createVersionComparisonsIndex, readErrors);
  const workspaceIndex = normalizeWorkspaceIndex(safeReadJson(root, ARTIFACT_PATHS.workspaceIndex, createWorkspaceIndex, readErrors));
  const checklistText = readText(root, ARTIFACT_PATHS.checklist, "");
  const evidence = safeEvaluateEvidence(root, readErrors);
  const figureQa = safeEvaluateFigurePipeline(root, state, readErrors);
  const findings = [];
  for (const error of readErrors) {
    addFinding(findings, {
      id: findingId("artifact", "malformed-json", error.path),
      severity: "high",
      category: "artifact",
      confidence: "high",
      summary: `${error.path} could not be parsed as JSON: ${error.message}`,
      artifactPaths: [error.path],
      suggestedNextCommand: "project:dove.status"
    });
  }
  for (const claim of evidence.unsupportedClaims) {
    addFinding(findings, {
      id: findingId("evidence", "unsupported-claim", claim.id),
      severity: "high",
      category: "evidence",
      summary: `Claim ${claim.id} has no valid source support.`,
      artifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.sources],
      claimIds: [claim.id],
      suggestedNextCommand: "project:dove.experience"
    });
  }
  for (const claim of evidence.weakClaims) {
    addFinding(findings, {
      id: findingId("evidence", "weak-claim", claim.id),
      severity: "medium",
      category: "evidence",
      summary: `Claim ${claim.id} is weakly supported and should be strengthened before acceptance.`,
      artifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.sources, ARTIFACT_PATHS.notes],
      claimIds: [claim.id],
      suggestedNextCommand: "project:dove.source"
    });
  }
  for (const item of evidence.missingSourceRefs) {
    addFinding(findings, {
      id: findingId("evidence", "missing-source-ref", item.claim.id),
      severity: "high",
      category: "evidence",
      summary: `Claim ${item.claim.id} references missing sources: ${item.missing.join(", ")}.`,
      artifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.sources],
      claimIds: [item.claim.id],
      suggestedNextCommand: "project:dove.source"
    });
  }
  for (const item of evidence.missingNoteRefs) {
    addFinding(findings, {
      id: findingId("evidence", "missing-note-ref", item.claim.id),
      severity: "medium",
      category: "evidence",
      summary: `Claim ${item.claim.id} references missing notes: ${item.missing.join(", ")}.`,
      artifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.notes],
      claimIds: [item.claim.id],
      suggestedNextCommand: "project:dove.note"
    });
  }
  for (const item of evidence.missingCitationRefs) {
    addFinding(findings, {
      id: findingId("citation", "missing-citation-ref", `${item.draftFile}-${item.key}`),
      severity: "high",
      category: "citation",
      summary: `${item.draftFile} cites unknown source key ${item.key}.`,
      artifactPaths: [ARTIFACT_PATHS.draftsDir, ARTIFACT_PATHS.sources, ARTIFACT_PATHS.bibliography],
      suggestedNextCommand: "project:dove.source"
    });
  }
  for (const todo of evidence.citationTodos) {
    addFinding(findings, {
      id: findingId("citation", "citation-todo", `${todo.draftFile}-${todo.line}`),
      severity: "medium",
      category: "citation",
      summary: `${todo.draftFile}:${todo.line} still has a citation TODO.`,
      artifactPaths: [ARTIFACT_PATHS.draftsDir, ARTIFACT_PATHS.bibliography],
      suggestedNextCommand: "project:dove.source"
    });
  }
  for (const item of evidence.draftClaimMismatches) {
    addFinding(findings, {
      id: findingId("draft", item.reason, item.claim.id),
      severity: "medium",
      category: "draft",
      summary: item.reason === "missing-draft" ? `Claim ${item.claim.id} targets section ${item.claim.sectionId} but no draft exists for that section.` : `Draft for ${item.claim.sectionId} does not cite any expected source for claim ${item.claim.id}.`,
      artifactPaths: [ARTIFACT_PATHS.draftsDir, ARTIFACT_PATHS.evidence],
      claimIds: [item.claim.id],
      suggestedNextCommand: "project:dove.draft"
    });
  }
  for (const audit of evidence.auditIntegrityFlags) {
    addFinding(findings, {
      id: findingId("experiment", "audit-integrity", audit.id),
      severity: "high",
      category: "experiment",
      summary: `Experiment audit ${audit.id} raised integrity flags: ${(audit.integrityFlags ?? []).join(", ")}.`,
      artifactPaths: [ARTIFACT_PATHS.experimentAudits],
      claimIds: audit.claimId ? [audit.claimId] : [],
      experimentIds: audit.experimentId ? [audit.experimentId] : [],
      suggestedNextCommand: "project:dove.experience"
    });
  }
  for (const bridgeProblem of evidence.claimBridgeProblems) {
    addFinding(findings, {
      id: findingId("claim-bridge", bridgeProblem.reason, bridgeProblem.claim.id),
      severity: "high",
      category: "claim-bridge",
      summary: `Claim ${bridgeProblem.claim.id} has a result-to-claim bridge problem (${bridgeProblem.reason}).`,
      artifactPaths: [ARTIFACT_PATHS.claimBridgeLog, ARTIFACT_PATHS.evidence],
      claimIds: [bridgeProblem.claim.id],
      experimentIds: bridgeProblem.claim.experimentIds ?? [],
      suggestedNextCommand: "project:dove.experience"
    });
  }
  for (const issue of figureQa.issues ?? []) {
    addFinding(findings, {
      id: findingId("figure", issue.code, issue.id),
      severity: issue.severity,
      category: "figure",
      summary: issue.summary,
      artifactPaths: issue.artifactPaths ?? [ARTIFACT_PATHS.figureQa],
      claimIds: issue.claimIds ?? [],
      experimentIds: issue.experimentIds ?? [],
      reviewConcernIds: issue.reviewConcernIds ?? [],
      rebuttalIssueIds: issue.rebuttalIssueIds ?? [],
      suggestedNextCommand: "project:dove.figure"
    });
  }
  const openConcerns = (reviewConcerns.items ?? []).filter((item) => item.status !== "resolved");
  for (const concern of openConcerns) {
    addFinding(findings, {
      id: findingId("review", "open-concern", concern.id),
      severity: concern.severity ?? "medium",
      category: "review",
      summary: `Review concern ${concern.id} remains open: ${concern.summary ?? "No summary."}`,
      artifactPaths: [ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.reviewState],
      reviewConcernIds: [concern.id],
      suggestedNextCommand: "project:dove.review"
    });
  }
  if ((reviewState.lastVerdict === "needs-work" || reviewState.lastVerdict === "needs-evidence") && openConcerns.length === 0) {
    addFinding(findings, {
      id: findingId("review", "verdict-without-open-concerns", reviewState.lastVerdict),
      severity: "medium",
      category: "review",
      summary: `Review state verdict is ${reviewState.lastVerdict}, but no open concern items were found.`,
      artifactPaths: [ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns],
      suggestedNextCommand: "project:dove.review"
    });
  }
  const activeComparisonTargets = versionComparisons.activeTargets ?? [];
  if (activeComparisonTargets.length > 0 && (versionComparisons.items ?? []).length === 0) {
    addFinding(findings, {
      id: findingId("version", "active-targets-without-comparison", activeComparisonTargets.join("-")),
      severity: "medium",
      category: "version",
      summary: `Version comparison has active targets (${activeComparisonTargets.join(", ")}) but no comparison records.`,
      artifactPaths: [ARTIFACT_PATHS.versionComparisons],
      suggestedNextCommand: "project:dove.version"
    });
  }
  if (/\[ \]|TODO|needs-review|blocked/i.test(checklistText) && PAPER_MAJOR_CHANGE_PROTOCOL_STAGES.includes("acceptance")) {
    addFinding(findings, {
      id: findingId("process", "open-checklist-items", ARTIFACT_PATHS.checklist),
      severity: "medium",
      category: "process",
      summary: "The checklist still appears to contain open, TODO, blocked, or needs-review items.",
      artifactPaths: [ARTIFACT_PATHS.checklist],
      suggestedNextCommand: "project:dove.status"
    });
  }
  const boardPhase = board.currentPhase ?? board.phase ?? state.pipeline?.currentStage ?? "init";
  if (workspaceIndex.lifecycle?.boardFamily && workspaceIndex.lifecycle.boardFamily !== "work-unit" && !workspaceIndex.lifecycle.familyIds.includes(workspaceIndex.lifecycle.boardFamily)) {
    addFinding(findings, {
      id: findingId("workspace", "invalid-board-family", workspaceIndex.lifecycle.boardFamily),
      severity: "medium",
      category: "workspace",
      summary: `Workspace lifecycle board family ${workspaceIndex.lifecycle.boardFamily} is not part of the current taxonomy.`,
      artifactPaths: [ARTIFACT_PATHS.workspaceIndex],
      suggestedNextCommand: "project:dove.status"
    });
  }
  const severityCounts = countBy(findings, "severity");
  const categoryCounts = countBy(findings, "category");
  const suggestedNextCommands = suggestedCommandsFromFindings(findings);
  const highestSeverity = findings.some((finding) => finding.severity === "high") ? "high" : findings.some((finding) => finding.severity === "medium") ? "medium" : findings.some((finding) => finding.severity === "low") ? "low" : "none";
  return {
    mode: "audit-only",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    summary: {
      highestSeverity,
      findingCount: findings.length,
      claimCount: evidence.claimCount,
      figureCount: (figureQa.items ?? []).length,
      openConcernCount: openConcerns.length,
      boardPhase,
      lifecycleTaxonomyVersion: PAPER_LIFECYCLE_TAXONOMY_VERSION,
      majorChangeProtocolStages: PAPER_MAJOR_CHANGE_PROTOCOL_STAGES
    },
    findings,
    severityCounts,
    categoryCounts,
    artifactPathsRead: artifactPathsRead(),
    suggestedNextCommands,
    diagnostics: {
      readErrors,
      workspaceLifecycle: workspaceIndex.lifecycle ?? null,
      requestedScope: typeof args.scope === "string" && args.scope.trim().length > 0 ? args.scope.trim() : null
    }
  };
}

// src/core/operator-ux.mjs
function isPlainObject3(value) {
  return value && typeof value === "object" && !Array.isArray(value);
}
function normalizeString5(value, fallback = null) {
  if (typeof value !== "string") {
    return fallback;
  }
  const trimmed = value.trim();
  return trimmed || fallback;
}
function normalizeStringArray12(values) {
  if (!Array.isArray(values)) {
    return [];
  }
  return values.map((value) => normalizeString5(value)).filter(Boolean);
}
function uniqueStrings2(values) {
  return [...new Set(normalizeStringArray12(values))];
}
function compactObject(fields) {
  return Object.fromEntries(Object.entries(fields).filter(([, value]) => {
    if (value === null || value === void 0) {
      return false;
    }
    if (Array.isArray(value)) {
      return value.length > 0;
    }
    if (isPlainObject3(value)) {
      return Object.keys(value).length > 0;
    }
    return true;
  }));
}
function text2(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function firstString(...values) {
  for (const value of values) {
    const normalized = normalizeString5(value);
    if (normalized) {
      return normalized;
    }
  }
  return null;
}
function responseLanguageFrom(data, fallback = "zh") {
  return normalizeString5(data?.responseLanguage ?? data?.statusHome?.responseLanguage, fallback) === "en" ? "en" : "zh";
}
var codeUx = {
  "source-requires-host-provenance": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u4F60\u63D0\u4F9B\u771F\u5B9E\u6765\u6E90 URL \u6216\u53EF\u9A8C\u8BC1\u6765\u6E90\u4FE1\u606F\u3002", "Blocked: provide a real source URL or verifiable source information."],
    cannotContinueBecause: ["\u6CA1\u6709\u53EF\u9A8C\u8BC1 source\uFF0CDove \u4E0D\u80FD\u628A\u5019\u9009\u94FE\u63A5\u5F53\u6210 provenance\u3002", "There is no verifiable source, so Dove cannot treat candidate links as provenance."],
    nextOperatorAction: ["\u63D0\u4F9B URL\u3001\u6807\u9898\u548C locator\uFF0C\u6216\u5148\u8FD0\u884C\u6388\u6743\u68C0\u7D22\u540E\u518D\u767B\u8BB0 source\u3002", "Provide a URL, title, and locator, or run authorized retrieval before registering the source."],
    requiredEvidence: [["\u771F\u5B9E\u6765\u6E90 URL", "\u6765\u6E90\u6807\u9898", "locator \u6216\u53EF\u8BBF\u95EE\u51FA\u5904"], ["real source URL", "source title", "locator or accessible origin"]]
  },
  "source-provenance-unverified": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u53EF\u9A8C\u8BC1 source\u3002", "Blocked: a verifiable source is required."],
    cannotContinueBecause: ["\u68C0\u7D22\u5931\u8D25\u30010 \u7ED3\u679C\u6216\u88AB\u5B89\u5168\u7B56\u7565\u963B\u65AD\u4E0D\u80FD\u767B\u8BB0\u4E3A source provenance\u3002", "Failed retrieval, zero results, or safety blocks cannot be registered as source provenance."],
    nextOperatorAction: ["\u8865\u5145\u5DF2\u8BBF\u95EE\u8FC7\u7684\u6765\u6E90\u6750\u6599\uFF0C\u6216\u6388\u6743\u4E3B\u673A\u4FA7\u91CD\u65B0\u68C0\u7D22\u3002", "Provide already-accessed source material, or authorize host-side retrieval again."],
    requiredEvidence: [["\u53EF\u8BBF\u95EE URL", "\u6765\u6E90\u6807\u9898", "\u68C0\u7D22/\u8BBF\u95EE\u8BC1\u636E"], ["accessible URL", "source title", "retrieval/access evidence"]]
  },
  "collect-source-provenance": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u6536\u96C6\u771F\u5B9E\u6765\u6E90\u8BC1\u636E\u3002", "Blocked: collect real source evidence."],
    cannotContinueBecause: ["\u7F3A\u5C11 source provenance\u3002", "Source provenance is missing."],
    nextOperatorAction: ["\u8865 URL\u3001\u6807\u9898\u3001\u4F5C\u8005/\u5E74\u4EFD\u6216 locator\uFF0C\u7136\u540E\u518D\u8C03\u7528 source \u767B\u8BB0\u3002", "Add URL, title, authors/year, or locator, then register the source."],
    requiredEvidence: [["source provenance"], ["source provenance"]]
  },
  "provide-explicit-auto-step": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u660E\u786E\u7684\u4E0B\u4E00\u6B65\u6267\u884C\u5185\u5BB9\u3002", "Blocked: an explicit next execution step is required."],
    cannotContinueBecause: ["\u53EA\u8BFB\u67E5\u8BE2\u6216\u7A7A\u6B65\u9AA4\u4E0D\u80FD\u63A8\u8FDB durable task\u3002", "Read-only queries or empty steps cannot advance a durable task."],
    nextOperatorAction: ["\u63D0\u4F9B\u5177\u4F53\u5199\u5165\u3001\u751F\u6210\u3001review \u6216 evidence step\u3002", "Provide a concrete write, generation, review, or evidence step."],
    requiredEvidence: [["\u660E\u786E auto step", "\u6267\u884C\u8BC1\u636E"], ["explicit auto step", "execution evidence"]]
  },
  "note-requires-host-synthesis": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u4F60\u63D0\u4F9B note \u7684\u771F\u5B9E\u7EFC\u5408\u5185\u5BB9\u3002", "Blocked: provide real note synthesis content."],
    cannotContinueBecause: ["\u7A7A note \u4E0D\u80FD\u4F5C\u4E3A\u7814\u7A76\u8FDB\u5C55\u5199\u5165\u3002", "An empty note cannot be written as research progress."],
    nextOperatorAction: ["\u63D0\u4F9B summary\u3001quote\u3001claim \u6216 open question\u3002", "Provide a summary, quote, claim, or open question."],
    requiredEvidence: [["note summary/claim/quote"], ["note summary/claim/quote"]]
  },
  "draft-requires-host-content": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u771F\u5B9E draft body\u3002", "Blocked: a real draft body is required."],
    cannotContinueBecause: ["\u6CA1\u6709\u6B63\u6587\u5185\u5BB9\u65F6\u4E0D\u80FD\u5199 draft\u3002", "A draft cannot be written without body content."],
    nextOperatorAction: ["\u63D0\u4F9B sectionId \u548C draft body\uFF0C\u6216\u53EA\u66F4\u65B0\u72B6\u6001\u65F6\u6539\u7528 section status\u3002", "Provide sectionId and draft body, or use section status for metadata-only updates."],
    requiredEvidence: [["draft body", "sectionId"], ["draft body", "sectionId"]]
  },
  "experience-requires-host-objective": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u5B9E\u9A8C\u76EE\u6807\u6216\u5DF2\u6709 experimentId\u3002", "Blocked: an experiment goal or existing experimentId is required."],
    cannotContinueBecause: ["\u6CA1\u6709\u5B9E\u9A8C\u76EE\u6807\u65F6\u4E0D\u80FD\u89C4\u5212\u6216\u8BB0\u5F55 experiment\u3002", "An experiment cannot be planned or recorded without an objective."],
    nextOperatorAction: ["\u63D0\u4F9B goal/title/idea/experimentId \u548C\u6210\u529F\u6307\u6807\u3002", "Provide a goal/title/idea/experimentId and success metric."],
    requiredEvidence: [["\u5B9E\u9A8C\u76EE\u6807", "\u6210\u529F\u6307\u6807"], ["experiment objective", "success metric"]]
  },
  "review-loop-requires-host-material": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981 review-loop \u6750\u6599\u3002", "Blocked: review-loop material is required."],
    cannotContinueBecause: ["\u7F3A\u5C11 draft \u5185\u5BB9\u6216 experiment \u76EE\u6807\u3002", "Draft content or an experiment objective is missing."],
    nextOperatorAction: ["\u63D0\u4F9B draft body \u6216 experience goal \u540E\u518D\u8FD0\u884C review-loop\u3002", "Provide a draft body or experience goal before running the review loop."],
    requiredEvidence: [["draft body \u6216 experience goal"], ["draft body or experience goal"]]
  },
  "awaiting-host-pass-result": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9700\u8981\u771F\u5B9E\u8BC1\u636E\u6216\u6267\u884C\u7ED3\u679C\u3002", "Blocked: real evidence or execution results are required."],
    cannotContinueBecause: ["Dove \u4E0D\u4F1A\u5047\u88C5\u5916\u90E8\u68C0\u7D22\u3001\u751F\u6210\u6216\u9A8C\u8BC1\u5DF2\u7ECF\u5B8C\u6210\u3002", "Dove will not pretend external retrieval, generation, or verification has completed."],
    nextOperatorAction: ["\u8865\u5145\u771F\u5B9E\u6267\u884C\u7ED3\u679C\u3001\u8BC1\u636E\u51FA\u5904\u6216\u5931\u8D25\u539F\u56E0\u3002", "Provide real execution results, evidence references, or the failure reason."],
    requiredEvidence: [["\u771F\u5B9E\u6267\u884C\u7ED3\u679C", "\u8BC1\u636E\u51FA\u5904"], ["real execution result", "evidence reference"]]
  },
  "host-tool-blocked": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u5DE5\u5177\u6216\u6743\u9650\u963B\u6B62\u7EE7\u7EED\u3002", "Blocked: a tool or permission issue prevents progress."],
    cannotContinueBecause: ["\u9700\u8981\u8865\u6267\u884C\u7ED3\u679C\uFF0C\u6216\u5148\u8BA9\u76F8\u5173\u5DE5\u5177\u53EF\u7528\u3002", "Provide execution results, or make the needed tool available first."],
    nextOperatorAction: ["\u8865\u771F\u5B9E\u7ED3\u679C/\u8BC1\u636E\uFF0C\u6216\u4FEE\u590D\u5DE5\u5177\u6743\u9650\u540E\u91CD\u8BD5\u3002", "Provide real results/evidence, or fix tool permissions and retry."],
    requiredEvidence: [["\u771F\u5B9E\u6267\u884C\u7ED3\u679C", "\u5931\u8D25\u65E5\u5FD7\u6216\u8BC1\u636E\u51FA\u5904"], ["real execution result", "failure log or evidence reference"]]
  },
  "awaiting-provider-output": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u7B49\u5F85\u751F\u6210\u7ED3\u679C\u3002", "Blocked: generation output is still missing."],
    cannotContinueBecause: ["\u7F3A\u5C11\u751F\u6210\u7ED3\u679C\uFF0C\u6216\u751F\u6210\u6240\u9700\u914D\u7F6E\u8FD8\u4E0D\u53EF\u7528\u3002", "Generation output is missing, or the required generation setup is unavailable."],
    nextOperatorAction: ["\u63D0\u4F9B\u751F\u6210\u7ED3\u679C\uFF0C\u6216\u914D\u7F6E\u73AF\u5883\u53D8\u91CF\u540E\u663E\u5F0F\u91CD\u8BD5\u3002", "Provide the generation output, or configure environment variables and retry explicitly."],
    requiredEvidence: [["\u751F\u6210\u7ED3\u679C", "\u751F\u6210\u8BB0\u5F55"], ["generation output", "generation record"]]
  },
  "awaiting-review-output": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u7B49\u5F85\u72EC\u7ACB review \u7ED3\u679C\u3002", "Blocked: waiting for independent review results."],
    cannotContinueBecause: ["\u7F3A\u5C11 reviewer \u8F93\u51FA\u6216\u62A5\u544A\u3002", "Reviewer output or report is missing."],
    nextOperatorAction: ["\u5BFC\u5165 reviewer \u8F93\u51FA/\u62A5\u544A\uFF0C\u6216\u91CD\u65B0\u51C6\u5907 review \u6750\u6599\u3002", "Import reviewer output/report, or prepare the review materials again."],
    requiredEvidence: [["reviewer \u8F93\u51FA", "review report"], ["reviewer output", "review report"]]
  },
  "missing-required-materials": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u7F3A\u5C11\u5FC5\u9700\u6750\u6599\u3002", "Blocked: required materials are missing."],
    cannotContinueBecause: ["\u6750\u6599\u4E0D\u8DB3\u65F6\u7EE7\u7EED\u4F1A\u4EA7\u751F\u4E0D\u53EF\u9A8C\u8BC1\u8F93\u51FA\u3002", "Continuing without materials would produce unverifiable output."],
    nextOperatorAction: ["\u8865\u9F50 requiredInputs/requiredMaterials \u540E\u518D\u7EE7\u7EED\u3002", "Provide requiredInputs/requiredMaterials before continuing."],
    requiredEvidence: [["\u5FC5\u9700\u8F93\u5165\u6750\u6599"], ["required input materials"]]
  },
  "verification-failed": {
    blockedSummary: ["\u73B0\u5728\u5361\u5728\uFF1A\u9A8C\u8BC1\u672A\u901A\u8FC7\u3002", "Blocked: verification failed."],
    cannotContinueBecause: ["\u5B8C\u6210\u6807\u51C6\u8FD8\u6CA1\u6709\u88AB\u8BC1\u636E\u8986\u76D6\u3002", "Done criteria are not covered by evidence yet."],
    nextOperatorAction: ["\u8865\u9A8C\u8BC1\u8BC1\u636E\u6216\u4FEE\u590D\u5931\u8D25\u9879\u540E\u91CD\u65B0 review\u3002", "Provide verification evidence or fix failed items before review."],
    requiredEvidence: [["\u9A8C\u8BC1\u8BC1\u636E", "\u5931\u8D25\u9879\u4FEE\u590D\u8BB0\u5F55"], ["verification evidence", "fix record"]]
  }
};
function localizedEntry(entry, responseLanguage, key) {
  if (!entry?.[key]) {
    return null;
  }
  const value = entry[key];
  if (Array.isArray(value) && Array.isArray(value[0])) {
    return responseLanguage === "en" ? value[1] : value[0];
  }
  if (Array.isArray(value)) {
    return responseLanguage === "en" ? value[1] : value[0];
  }
  return value;
}
function collectOperatorCodes(data) {
  if (!isPlainObject3(data)) {
    return [];
  }
  const resultCardAction = Array.isArray(data.resultCard?.nextActions) ? data.resultCard.nextActions[0] : null;
  return uniqueStrings2([
    data.status,
    data.kind,
    data.outcome,
    data.stopReason,
    data.reason,
    data.boundaryType,
    data.boundary?.type,
    data.boundary?.reason,
    data.resultCard?.kind,
    data.resultCard?.outcome,
    data.resultCard?.stopReason,
    data.resultCard?.boundaryType,
    data.resultCard?.boundary?.type,
    data.resultCard?.boundary?.reason,
    resultCardAction?.kind,
    resultCardAction?.boundaryType,
    resultCardAction?.reason,
    ...normalizeStringArray12(data.requiredActions),
    ...normalizeStringArray12(data.boundary?.requiredActions),
    ...normalizeStringArray12(data.resultCard?.requiredActions),
    ...normalizeStringArray12(data.resultCard?.boundary?.requiredActions),
    ...normalizeStringArray12(resultCardAction?.requiredActions)
  ]);
}
function buildOperatorUnblock(data, responseLanguage = responseLanguageFrom(data)) {
  const codes = collectOperatorCodes(data);
  const matchedCode = codes.find((code) => codeUx[code]);
  const entry = matchedCode ? codeUx[matchedCode] : null;
  const boundaryType = firstString(data?.boundaryType, data?.boundary?.type, data?.resultCard?.boundaryType, data?.resultCard?.boundary?.type);
  if (!entry && !boundaryType) {
    return null;
  }
  const blockedSummary = localizedEntry(entry, responseLanguage, "blockedSummary") ?? text2(responseLanguage, "\u5F53\u524D\u6CA1\u6709\u660E\u786E\u7684\u4EBA\u4E3A\u963B\u585E\uFF1B\u5982\u9700\u7EC6\u8282\u8BF7\u5C55\u5F00 full/debug\u3002", "There is no explicit operator block; expand full/debug for details.");
  const cannotContinueBecause = localizedEntry(entry, responseLanguage, "cannotContinueBecause");
  const nextOperatorAction = localizedEntry(entry, responseLanguage, "nextOperatorAction") ?? firstString(data?.nextAction, data?.nextCommand, data?.boundary?.command);
  const requiredEvidence = uniqueStrings2([
    ...localizedEntry(entry, responseLanguage, "requiredEvidence") ?? [],
    ...normalizeStringArray12(data?.requiredEvidence),
    ...normalizeStringArray12(data?.boundary?.requiredInputs)
  ]).slice(0, 8);
  return compactObject({
    summary: blockedSummary,
    why: cannotContinueBecause,
    operatorAction: nextOperatorAction,
    needs: requiredEvidence,
    blockedSummary,
    cannotContinueBecause,
    nextOperatorAction,
    requiredEvidence,
    boundaryType
  });
}
function normalizeStatusIntent(value) {
  const normalized = normalizeString5(value, "project-status").toLowerCase();
  return ["project-status", "health-check", "contract-test"].includes(normalized) ? normalized : "project-status";
}

// src/core/dove.mjs
function cloneFallback3(fallback) {
  return typeof fallback === "function" ? fallback() : structuredClone(fallback);
}
function resolveProjectPath(root, relativePath) {
  return path16.join(root, relativePath);
}
function safeReadJson2(root, relativePath, fallback, readErrors) {
  const fullPath = resolveProjectPath(root, relativePath);
  if (!fs14.existsSync(fullPath)) {
    return cloneFallback3(fallback);
  }
  try {
    return JSON.parse(fs14.readFileSync(fullPath, "utf8"));
  } catch (error) {
    readErrors.push({
      path: relativePath,
      message: error instanceof Error ? error.message : String(error)
    });
    return cloneFallback3(fallback);
  }
}
function safeReadText(root, relativePath, readErrors) {
  const fullPath = resolveProjectPath(root, relativePath);
  if (!fs14.existsSync(fullPath)) {
    return null;
  }
  try {
    return fs14.readFileSync(fullPath, "utf8");
  } catch (error) {
    readErrors.push({
      path: relativePath,
      message: error instanceof Error ? error.message : String(error)
    });
    return null;
  }
}
function objectOrFallback(value, fallback) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : cloneFallback3(fallback);
}
var VALIDATION_OUTPUT_READ_LIMIT_BYTES = 24 * 1024;
var ARCHIVED_PACKET_STATUSES = new Set(DOVE_ARCHIVED_TASK_STATUSES);
function archivedPacketStatus(value) {
  return ARCHIVED_PACKET_STATUSES.has(String(value ?? "").trim().toLowerCase());
}
function isArchivedStatusTask(task) {
  return archivedPacketStatus(task?.status) || archivedPacketStatus(task?.lifecycleStatus);
}
function normalizeStringArray13(value) {
  const values = Array.isArray(value) ? value : typeof value === "string" ? [value] : [];
  return Array.from(new Set(values.map((item) => String(item).trim()).filter(Boolean)));
}
function mergeStringArrays(...values) {
  return Array.from(new Set(values.flatMap((value) => normalizeStringArray13(value))));
}
function activePackets(packets = []) {
  return packets.filter((packet) => !archivedPacketStatus(packet.status) && !archivedPacketStatus(packet.lifecycleStatus));
}
function classifyValidationOutputText(text3) {
  const normalized = String(text3 ?? "").toLowerCase();
  if (!normalized.trim()) {
    return "unknown";
  }
  const failurePatterns = [
    /\bnot ok\b/,
    /\btraceback\b/,
    /\bexception\b/,
    /\berror\b/,
    /\b[1-9]\d*\s+(?:failing|failures?|failed)\b/,
    /\bfailed\b/,
    /\bnon[- ]?zero\b/,
    /\bexit\s+[1-9]\d*\b/
  ];
  if (failurePatterns.some((pattern) => pattern.test(normalized))) {
    return "failed";
  }
  const passPatterns = [
    /\bpass(?:ed|es)?\b/,
    /\bsuccess(?:ful)?\b/,
    /\bok\b/,
    /\b0\s+(?:failing|failures?|failed)\b/,
    /\bexit\s+0\b/
  ];
  if (passPatterns.some((pattern) => pattern.test(normalized))) {
    return "passed";
  }
  return "unknown";
}
function summarizeValidationOutputText(text3) {
  return String(text3 ?? "").replace(/\s+/g, " ").trim().slice(0, 240);
}
function aggregateValidationOutputStatus(signals, pathEvidence) {
  if (signals.some((signal) => signal.status === "failed")) {
    return "failed";
  }
  if (signals.some((signal) => signal.status === "passed")) {
    return "passed";
  }
  if (signals.length > 0) {
    return "unknown";
  }
  return pathEvidence.declaredPaths.length > 0 ? "missing" : "missing";
}
function buildValidationOutputEvidence(root, pathValues, textValues) {
  const paths = mergeStringArrays(...pathValues);
  const inspections = paths.map((item) => inspectDeclaredPath(root, item, { readText: true, maxBytes: VALIDATION_OUTPUT_READ_LIMIT_BYTES }));
  const pathEvidence = summarizePathInspections(inspections);
  const pathSignals = inspections.filter((item) => typeof item.text === "string").map((item) => ({
    source: "path",
    path: item.normalizedPath,
    status: classifyValidationOutputText(item.text),
    bytesRead: item.bytesRead ?? 0,
    truncated: Boolean(item.truncated),
    sample: summarizeValidationOutputText(item.text)
  }));
  const textSignals = mergeStringArrays(...textValues).map((text3, index) => ({
    source: "text",
    index,
    status: classifyValidationOutputText(text3),
    sample: summarizeValidationOutputText(text3)
  }));
  const signals = [...pathSignals, ...textSignals];
  const status = aggregateValidationOutputStatus(signals, pathEvidence);
  return {
    status,
    satisfied: status === "passed",
    declaredTextCount: textSignals.length,
    paths: pathEvidence,
    signals
  };
}
function acceptanceChecksInclude(mission, terms) {
  return mission.acceptanceChecks.some((check) => {
    const normalized = check.toLowerCase();
    return terms.some((term) => normalized.includes(term));
  });
}
function addPathProblemEvidence(missingEvidence, category, requirement, evidence) {
  const problemPaths = [
    ...evidence.missingPaths,
    ...evidence.unsafePaths,
    ...evidence.unreadablePaths,
    ...evidence.directoryPaths,
    ...evidence.unsupportedPaths
  ];
  if (problemPaths.length > 0) {
    missingEvidence.push({
      category,
      requirement,
      reason: "One or more declared evidence paths could not be safely inspected.",
      paths: problemPaths,
      suggestedInput: `Provide project-local existing file paths for ${requirement}.`
    });
  }
}
function buildEngineeringEvidence(root, args, inputs, mission, completedChecks, uncheckedChecks) {
  const missionPackets = activePackets(inputs.packets).filter((packet) => packet.doveDomain === "engineering");
  const changedFiles = inspectPathEvidence(root, mergeStringArrays(
    args.changedFilePaths,
    args.changedFiles,
    args.changedPaths,
    missionPackets.flatMap((packet) => packet.outputPaths)
  ));
  const validationEvidence = inspectPathEvidence(root, mergeStringArrays(
    args.validationEvidencePaths,
    args.validationEvidence,
    args.evidencePaths,
    args.testEvidencePaths,
    args.testPaths,
    missionPackets.flatMap((packet) => packet.evidenceLinks)
  ));
  const validationOutput = buildValidationOutputEvidence(root, [
    args.validationOutputPaths,
    args.validationLogPaths,
    args.testOutputPaths
  ], [
    args.validationOutputs,
    args.validationOutput
  ]);
  const reviewEvidence = inspectPathEvidence(root, mergeStringArrays(args.reviewEvidencePaths, args.reviewEvidence));
  const requiresValidationOutput = acceptanceChecksInclude(mission, ["test", "validation", "output"]);
  const requiresReview = acceptanceChecksInclude(mission, ["review"]);
  const requiresChecklist = acceptanceChecksInclude(mission, ["checklist"]);
  const missingEvidence = [];
  if (changedFiles.declaredPaths.length === 0) {
    missingEvidence.push({
      category: "changed-files",
      requirement: "changed files",
      reason: "No changed source, test, or documentation files were declared for the engineering return.",
      paths: [],
      suggestedInput: "Pass --changed-file or provide packet outputPaths."
    });
  } else if (!changedFiles.satisfied) {
    missingEvidence.push({
      category: "changed-files",
      requirement: "changed files",
      reason: "Changed-file evidence was declared but no existing project-local file could be inspected.",
      paths: changedFiles.declaredPaths,
      suggestedInput: "Pass project-local changed-file paths that exist in the workspace."
    });
  }
  addPathProblemEvidence(missingEvidence, "changed-files", "changed files", changedFiles);
  if (validationEvidence.declaredPaths.length === 0) {
    missingEvidence.push({
      category: "validation-evidence",
      requirement: "tests or validation evidence",
      reason: "No test, validation, or evidence file was declared for the engineering return.",
      paths: [],
      suggestedInput: "Pass --test-evidence, --validation-evidence, or provide packet evidenceLinks."
    });
  } else if (!validationEvidence.satisfied) {
    missingEvidence.push({
      category: "validation-evidence",
      requirement: "tests or validation evidence",
      reason: "Validation evidence was declared but no existing project-local evidence file could be inspected.",
      paths: validationEvidence.declaredPaths,
      suggestedInput: "Pass project-local test or validation evidence files that exist in the workspace."
    });
  }
  addPathProblemEvidence(missingEvidence, "validation-evidence", "tests or validation evidence", validationEvidence);
  if (requiresValidationOutput && validationOutput.status !== "passed") {
    missingEvidence.push({
      category: "validation-output",
      requirement: "validation output",
      reason: validationOutput.status === "failed" ? "Declared validation output contains failure signals." : validationOutput.status === "unknown" ? "Declared validation output is inconclusive." : "No passing validation output was declared.",
      paths: validationOutput.paths.declaredPaths,
      suggestedInput: "Pass a project-local validation output file or explicit validation output text showing a passing run."
    });
  }
  addPathProblemEvidence(missingEvidence, "validation-output", "validation output", validationOutput.paths);
  const reviewMissing = requiresReview && inputs.reviewState.lastVerdict === "not-reviewed" && !reviewEvidence.satisfied;
  if (reviewMissing) {
    missingEvidence.push({
      category: "review-evidence",
      requirement: "review evidence",
      reason: "The mission asks for review evidence, but no review verdict or review evidence path is available.",
      paths: reviewEvidence.declaredPaths,
      suggestedInput: "Run a review surface or pass project-local review evidence paths."
    });
  }
  addPathProblemEvidence(missingEvidence, "review-evidence", "review evidence", reviewEvidence);
  const checklistMissing = requiresChecklist && (!inputs.checklist || uncheckedChecks > 0);
  if (checklistMissing) {
    missingEvidence.push({
      category: "acceptance-checklist",
      requirement: "acceptance checklist",
      reason: !inputs.checklist ? "No checklist artifact is available." : "The checklist still has unchecked items.",
      paths: [ARTIFACT_PATHS.checklist],
      suggestedInput: "Complete or update the acceptance checklist before return."
    });
  }
  const pathProblemCount = changedFiles.problemCount + validationEvidence.problemCount + validationOutput.paths.problemCount + reviewEvidence.problemCount;
  const hasFailedValidationOutput = validationOutput.status === "failed";
  const hasReviewGap = missingEvidence.some((item) => item.category === "review-evidence");
  const hasChecklistGap = missingEvidence.some((item) => item.category === "acceptance-checklist");
  const hasEvidenceGaps = missingEvidence.some((item) => ["changed-files", "validation-evidence", "validation-output"].includes(item.category));
  return {
    declaredInputsOnly: true,
    noCommandExecution: true,
    noGitInspection: true,
    packetEvidence: {
      packetIds: missionPackets.map((packet) => packet.packetId ?? packet.id).filter(Boolean),
      missionPacketIds: missionPackets.map((packet) => packet.missionPacketId ?? packet.packetId ?? packet.id).filter(Boolean),
      missionPacketStorePath: ARTIFACT_PATHS.taskPacketsIndex,
      missionPacketPaths: Array.from(new Set(missionPackets.map((packet) => packet.missionPacketPath).filter(Boolean))),
      outputPaths: Array.from(new Set(missionPackets.flatMap((packet) => packet.outputPaths))),
      evidenceLinks: Array.from(new Set(missionPackets.flatMap((packet) => packet.evidenceLinks)))
    },
    changedFiles,
    validationEvidence,
    validationOutput,
    reviewEvidence,
    checklist: {
      completedCount: completedChecks,
      uncheckedCount: uncheckedChecks,
      path: ARTIFACT_PATHS.checklist
    },
    missingEvidence,
    readinessSignals: [
      { category: "changed-files", status: changedFiles.satisfied ? "present" : "missing" },
      { category: "validation-evidence", status: validationEvidence.satisfied ? "present" : "missing" },
      { category: "validation-output", status: validationOutput.status },
      { category: "review-evidence", status: hasReviewGap ? "missing" : "not-required-or-present" },
      { category: "acceptance-checklist", status: hasChecklistGap ? "incomplete" : "not-required-or-complete" }
    ],
    readiness: {
      ready: missingEvidence.length === 0,
      pathProblemCount,
      hasEvidenceGaps,
      hasFailedValidationOutput,
      hasReviewGap,
      hasChecklistGap,
      validationOutputStatus: validationOutput.status
    },
    evidenceReadPaths: Array.from(/* @__PURE__ */ new Set([
      ...changedFiles.existingPaths,
      ...validationEvidence.existingPaths,
      ...validationOutput.paths.existingPaths,
      ...reviewEvidence.existingPaths
    ]))
  };
}
function missionStageForPhase(phase) {
  const map = {
    init: "goal",
    sources: "goal",
    notes: "goal",
    research: "goal",
    plan: "design",
    outline: "design",
    checklist: "checklist",
    draft: "execution",
    experiments: "execution",
    citations: "execution",
    rebuttal: "execution",
    review: "audit",
    versions: "return"
  };
  return normalizeDoveMissionLifecycleStage(map[String(phase ?? "").trim()], "goal");
}
function domainForPacket(packet = {}) {
  const explicit = normalizeDoveDomainId(packet.doveDomain ?? packet.missionDomain ?? packet.domain, null);
  if (explicit) {
    return explicit;
  }
  if ((packet.experimentIds ?? []).length > 0 || packet.lifecycleFamily === "audit") {
    return "experiment";
  }
  if (packet.assignedRole === "reviewer" || packet.lifecycleFamily === "concern") {
    return "review";
  }
  if (packet.lifecycleFamily === "work-unit" || packet.lifecycleFamily === "campaign") {
    return "general";
  }
  return "paper";
}
function primaryRoleForStage(stage) {
  if (stage === "audit" || stage === "return") {
    return DOVE_PRIMARY_ROLES.find((role) => role.id === "reviewer");
  }
  if (stage === "execution") {
    return DOVE_PRIMARY_ROLES.find((role) => role.id === "builder");
  }
  return DOVE_PRIMARY_ROLES.find((role) => role.id === "planner");
}
function selectDomainGuidance(workspaceIndex, domain) {
  const guidance = workspaceIndex.dove?.domainGuidance ?? createDoveWorkspaceKernel().domainGuidance;
  return guidance.find((item) => item.id === domain) ?? createDoveWorkspaceKernel().domainGuidance.find((item) => item.id === "paper");
}
function missionPacketAliases(packet) {
  const packetId = String(packet.id ?? packet.packetId ?? packet.missionPacketId ?? "").trim();
  const packetPath = packet.packetPath ?? (packetId ? path16.join(ARTIFACT_PATHS.taskPacketsPacketsDir, `${packetId}.json`) : null);
  const packetContextPath2 = packet.packetContextPath ?? (packetId ? path16.join(ARTIFACT_PATHS.packetContextsDir, `${packetId}.json`) : null);
  return {
    packetId,
    packetPath,
    packetContextPath: packetContextPath2,
    missionPacketId: packetId,
    missionPacketPath: packetPath,
    missionPacketContextPath: packetContextPath2,
    missionPacketStorePath: ARTIFACT_PATHS.taskPacketsIndex,
    missionPacketStoreRoot: ARTIFACT_PATHS.taskPacketsDir,
    source: "mission-packet"
  };
}
function summarizePacket2(packet) {
  return {
    id: packet.id,
    ...missionPacketAliases(packet),
    title: packet.title ?? packet.id,
    status: packet.status ?? "pending",
    lifecycleStatus: packet.lifecycleStatus ?? "active",
    lifecycleFamily: packet.lifecycleFamily ?? null,
    doveDomain: domainForPacket(packet),
    assignedRole: packet.assignedRole ?? null,
    phase: packet.phase ?? null,
    nextAction: packet.nextAction ?? null,
    outputPaths: normalizeStringArray13(packet.outputPaths),
    evidenceLinks: normalizeStringArray13(packet.evidenceLinks)
  };
}
function safeReadTaskPacketCatalog(root, taskPackets, readErrors) {
  try {
    return readTaskPacketCatalog(root);
  } catch (error) {
    readErrors.push({
      path: ARTIFACT_PATHS.taskPacketsDir,
      message: error instanceof Error ? error.message : String(error)
    });
    const packets = Array.isArray(taskPackets.items) ? taskPackets.items : [];
    return {
      index: taskPackets,
      packets,
      byId: new Map(packets.map((packet) => [packet.id, packet]).filter(([id]) => id))
    };
  }
}
function readDoveInputs(root) {
  const readErrors = [];
  const state = normalizeState(objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.state, createDefaultState, readErrors), createDefaultState));
  const board = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.orchestrationBoard, () => createDefaultBoard(state), readErrors), () => createDefaultBoard(state));
  const workspaceIndex = normalizeWorkspaceIndex(objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.workspaceIndex, createWorkspaceIndex, readErrors), createWorkspaceIndex));
  const doveAuthorityManifest = normalizeDoveAuthorityManifest(objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.doveRootManifest, createDoveAuthorityManifest, readErrors), createDoveAuthorityManifest));
  const taskPackets = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex, readErrors), createTaskPacketsIndex);
  const taskCatalog = safeReadTaskPacketCatalog(root, taskPackets, readErrors);
  const reviewState = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.reviewState, createReviewState, readErrors), createReviewState);
  const reviewConcerns = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.reviewConcerns, () => ({ version: 2, items: [], updatedAt: null }), readErrors), () => ({ version: 2, items: [], updatedAt: null }));
  const versions = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.versionsIndex, createVersionsIndex, readErrors), createVersionsIndex);
  const comparisons = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.versionComparisons, createVersionComparisonsIndex, readErrors), createVersionComparisonsIndex);
  const experimentPlans = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.experimentPlans, () => ({ version: 1, items: [], updatedAt: null }), readErrors), () => ({ version: 1, items: [], updatedAt: null }));
  const experimentResults = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.experimentResults, () => ({ version: 1, items: [], updatedAt: null }), readErrors), () => ({ version: 1, items: [], updatedAt: null }));
  const experimentAudits = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.experimentAudits, () => ({ version: 1, items: [], updatedAt: null }), readErrors), () => ({ version: 1, items: [], updatedAt: null }));
  const operatorLessons = objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.metaOperatorLessons, () => ({ version: 1, lessons: [], summary: { lessonCount: 0, activeLessonCount: 0, topLessonIds: [], lessonsPath: ARTIFACT_PATHS.metaOperatorLessons } }), readErrors), () => ({ version: 1, lessons: [], summary: { lessonCount: 0, activeLessonCount: 0, topLessonIds: [], lessonsPath: ARTIFACT_PATHS.metaOperatorLessons } }));
  const runtimeContinuation = normalizeRuntimeContinuationIndex(objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.runtimeContinuation, createRuntimeContinuationIndex, readErrors), createRuntimeContinuationIndex));
  const runtimeEvents = normalizeRuntimeEventsIndex(objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex, readErrors), createRuntimeEventsIndex));
  const runtimeResults = normalizeRuntimeResultsIndex(objectOrFallback(safeReadJson2(root, ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex, readErrors), createRuntimeResultsIndex));
  const checklist = safeReadText(root, ARTIFACT_PATHS.checklist, readErrors);
  const packets = taskCatalog.packets.map(summarizePacket2);
  return { state, board, workspaceIndex, doveAuthorityManifest, taskPackets, taskCatalog, packets, reviewState, reviewConcerns, versions, comparisons, experimentPlans, experimentResults, experimentAudits, operatorLessons, runtimeContinuation, runtimeEvents, runtimeResults, checklist, readErrors };
}
function normalizeDoveStatusTaskStatus(packet) {
  if ([packet.status, packet.lifecycleStatus].some(archivedPacketStatus)) {
    return "archived";
  }
  const raw = String(packet.status ?? packet.lifecycleStatus ?? "pending").trim().toLowerCase();
  if (DOVE_TASK_STATUSES.includes(raw)) {
    return raw;
  }
  if (raw === "active") {
    return "in-progress";
  }
  if (raw === "done") {
    return "completed";
  }
  return "pending";
}
function normalizeDoveStatusTaskStage(packet) {
  const raw = String(packet.stage ?? packet.taskStage ?? "").trim().toLowerCase();
  if (DOVE_TASK_STAGES.includes(raw)) {
    return raw;
  }
  const legacyStage = String(packet.missionStage ?? missionStageForPhase(packet.phase)).trim();
  if (["audit", "return"].includes(legacyStage)) {
    return "audit";
  }
  if (legacyStage === "execution") {
    return "execute";
  }
  return "plan";
}
function normalizeDoveStatusTaskDomain(packet) {
  const raw = String(packet.domain ?? packet.doveDomain ?? packet.missionDomain ?? "").trim().toLowerCase();
  if (DOVE_TASK_DOMAINS.includes(raw)) {
    return raw;
  }
  const legacyDomain = domainForPacket(packet);
  return DOVE_TASK_DOMAINS.includes(legacyDomain) ? legacyDomain : "engineering";
}
function normalizeDoveStatusCreatorKind(packet) {
  const raw = String(packet.creatorKind ?? "user").trim().toLowerCase();
  return DOVE_TASK_CREATOR_KINDS.includes(raw) ? raw : "user";
}
function normalizeDoveStatusLevel(packet) {
  const level = Number(packet.level);
  return Number.isFinite(level) ? level : 3;
}
function normalizeStatusContractStringArray(value) {
  if (Array.isArray(value)) {
    return normalizeStringArray13(value);
  }
  return typeof value === "string" && value.trim() ? [value.trim()] : [];
}
function firstStatusContractStringArray(...values) {
  for (const value of values) {
    const normalized = normalizeStatusContractStringArray(value);
    if (normalized.length > 0) {
      return normalized;
    }
  }
  return [];
}
function statusCopyableCommand(command, packetId) {
  const publicCommand = toPublicDoveCommand(command, "project:dove.auto");
  return packetId ? `${publicCommand} --packet-id ${packetId}` : publicCommand;
}
function normalizeStatusContractRoutes(routes) {
  return (Array.isArray(routes) ? routes : []).map((route, index) => {
    const source = typeof route === "string" ? { command: route } : objectOrFallback(route, {});
    const command = typeof source.command === "string" && source.command.trim() ? toPublicDoveCommand(source.command, source.command) : null;
    if (!command) {
      return null;
    }
    return {
      label: source.label ?? source.title ?? null,
      command,
      copyableCommand: source.copyableCommand ?? source.copyCommand ?? null,
      packetId: source.packetId ?? source.taskPacketId ?? source.missionPacketId ?? null,
      when: source.when ?? source.reason ?? null,
      role: source.role ?? source.ownerRole ?? null,
      evidenceRequired: normalizeStatusContractStringArray(source.evidenceRequired ?? source.evidenceContract ?? source.evidenceExpectations),
      doneCriteria: normalizeStatusContractStringArray(source.doneCriteria),
      rank: Number.isFinite(source.rank) ? source.rank : index + 1
    };
  }).filter(Boolean);
}
function normalizeDoveStatusWorkContract(contract) {
  const explicit = objectOrFallback(contract, null);
  if (!explicit) {
    return null;
  }
  return {
    purpose: explicit.purpose ?? null,
    deliverables: normalizeStatusContractStringArray(explicit.deliverables),
    outOfScope: firstStatusContractStringArray(explicit.outOfScope, explicit.outOfScopeItems),
    evidenceContract: normalizeStatusContractStringArray(explicit.evidenceContract),
    doneCriteria: normalizeStatusContractStringArray(explicit.doneCriteria),
    recommendedRoutes: normalizeStatusContractRoutes(explicit.recommendedRoutes),
    practicalImpact: explicit.practicalImpact ?? null
  };
}
function summarizeDoveStatusTask(packet, responseLanguage = "zh") {
  const id = String(packet.id ?? packet.packetId ?? packet.missionPacketId ?? "").trim();
  const aliases = missionPacketAliases({ ...packet, id });
  const title = packet.title ?? id;
  const summary = packet.summary ?? packet.currentFocus ?? null;
  const stage = normalizeDoveStatusTaskStage(packet);
  const domain = normalizeDoveStatusTaskDomain(packet);
  const evidenceExpectations = normalizeStringArray13(packet.evidenceExpectations ?? packet.acceptanceChecks);
  const ownerRole = packet.ownerRole ?? null;
  const nextRole = packet.nextRole ?? null;
  const nextAction = packet.nextAction ?? null;
  const workContract = normalizeDoveStatusWorkContract(packet.workContract);
  const executionContract = normalizeDoveExecutionContract(packet.executionContract, null);
  const executionReadiness = doveExecutionContractReadiness(executionContract);
  const verifiedCriteria = normalizeDoveVerifiedCriteria(packet.verifiedCriteria);
  const criteriaCoverage = doveExecutionCriteriaCoverage(executionContract, verifiedCriteria);
  const validationEvidencePaths = normalizeStringArray13(packet.validationEvidencePaths);
  const verificationEvidencePaths = normalizeStringArray13(packet.verificationEvidencePaths);
  const criteriaEvidencePaths = normalizeStringArray13(verifiedCriteria.flatMap((item) => item.evidencePaths ?? []));
  const status = normalizeDoveStatusTaskStatus(packet);
  return {
    id,
    ...aliases,
    title,
    summary,
    parentId: packet.parentId ?? null,
    rootId: packet.rootId ?? null,
    level: normalizeDoveStatusLevel(packet),
    creatorKind: normalizeDoveStatusCreatorKind(packet),
    stage,
    domain,
    status,
    displayStatus: status === "completed" || status === "killed" ? "done" : status,
    lifecycleStatus: packet.lifecycleStatus ?? packet.status ?? null,
    dependencies: normalizeStringArray13(packet.dependencies ?? packet.dependencyIds),
    blockedBy: normalizeStringArray13(packet.blockedBy ?? packet.blockerIds),
    blockedReason: packet.blockedReason ?? packet.blockerReason ?? packet.blockingReason ?? null,
    lessonIds: normalizeStringArray13(packet.lessonIds),
    sourcePlanTaskId: packet.sourcePlanTaskId ?? null,
    derivedFrom: packet.derivedFrom ?? null,
    evidenceExpectations,
    workContract,
    executionContract,
    executionReadiness,
    verifiedCriteria,
    criteriaCoverage,
    validationEvidencePaths,
    verificationEvidencePaths,
    artifactRefs: mergeStringArrays(packet.artifactRefs, packet.artifactPaths, packet.outputPaths, packet.evidenceLinks, validationEvidencePaths, verificationEvidencePaths, criteriaEvidencePaths),
    outputPaths: normalizeStringArray13(packet.outputPaths),
    evidenceLinks: normalizeStringArray13(packet.evidenceLinks),
    contextPolicy: packet.contextPolicy ?? null,
    currentFocus: packet.currentFocus ?? null,
    nextAction,
    createdAt: packet.createdAt ?? null,
    updatedAt: packet.updatedAt ?? null,
    completedAt: packet.completedAt ?? null,
    killedAt: packet.killedAt ?? null,
    killReason: packet.killReason ?? null,
    archivedAt: packet.archivedAt ?? null,
    archiveReason: packet.archiveReason ?? null,
    ownerRole,
    nextRole,
    boundary: normalizeDoveBoundary(packet.boundary, null),
    boundaryHistory: Array.isArray(packet.boundaryHistory) ? packet.boundaryHistory.map((item) => normalizeDoveBoundary(item, null)).filter(Boolean) : [],
    handoff: normalizeDoveHandoff(packet.handoff, null),
    lastTransition: packet.lastTransition && typeof packet.lastTransition === "object" && !Array.isArray(packet.lastTransition) ? packet.lastTransition : null
  };
}
function sortStatusTasks(tasks) {
  return [...tasks].sort((left, right) => left.level - right.level || String(left.createdAt ?? "").localeCompare(String(right.createdAt ?? "")) || left.id.localeCompare(right.id));
}
function sortRecentStatusTasks(tasks) {
  return [...tasks].sort((left, right) => String(right.updatedAt ?? right.completedAt ?? right.killedAt ?? right.createdAt ?? "").localeCompare(String(left.updatedAt ?? left.completedAt ?? left.killedAt ?? left.createdAt ?? "")) || left.id.localeCompare(right.id));
}
function runtimeResultEntries(inputs) {
  const entries = [
    ...Array.isArray(inputs.runtimeResults.items) ? inputs.runtimeResults.items : [],
    ...Array.isArray(inputs.runtimeResults.entries) ? inputs.runtimeResults.entries : []
  ].filter((entry) => entry && typeof entry === "object" && !Array.isArray(entry));
  const byKey = /* @__PURE__ */ new Map();
  for (const entry of entries) {
    const key = entry.id ?? entry.runId ?? `${entry.surface ?? "runtime"}:${entry.packetId ?? "unknown"}:${entry.updatedAt ?? entry.recordedAt ?? entry.createdAt ?? byKey.size}`;
    byKey.set(key, entry);
  }
  return Array.from(byKey.values());
}
function runtimeIterationForPacket(entry, packetId) {
  const iterations = Array.isArray(entry.iterations) ? entry.iterations : [];
  return [...iterations].reverse().find((iteration) => [iteration.packetId, iteration.taskId, iteration.missionPacketId].includes(packetId)) ?? null;
}
function runtimeEntryTouchesPacket(entry, packetId) {
  if ([entry.packetId, entry.taskId, entry.missionPacketId, entry.selectedPacketId].includes(packetId)) {
    return true;
  }
  if (runtimeIterationForPacket(entry, packetId)) {
    return true;
  }
  return ["updatedTaskIds", "awaitingResultTaskIds", "autoRunnableTaskIds", "hostPassRequiredTaskIds", "runnableTaskIds", "blockedTaskIds", "pendingTaskIds"].some((field) => normalizeStringArray13(entry[field]).includes(packetId));
}
function runtimeTimestamp(entry, iteration = null) {
  return iteration?.completedAt ?? iteration?.startedAt ?? entry.updatedAt ?? entry.recordedAt ?? entry.completedAt ?? entry.createdAt ?? entry.startedAt ?? "";
}
function compactRuntimeExecutionReceipt(value) {
  const receipt = normalizeDoveExecutionReceipt(value, null);
  if (!receipt) {
    return null;
  }
  return {
    receiptId: receipt.receiptId ?? null,
    runId: receipt.runId ?? null,
    packetId: receipt.packetId ?? null,
    surface: receipt.surface ?? null,
    command: receipt.command ?? null,
    actionType: receipt.actionType ?? null,
    status: receipt.status ?? null,
    outcome: receipt.outcome ?? null,
    resultSummary: receipt.publicSafeSummary ?? receipt.resultSummary ?? null,
    lifecycleTransition: receipt.lifecycleTransition ?? null,
    evidenceCount: mergeStringArrays(
      receipt.evidenceLinks,
      receipt.evidencePaths,
      receipt.artifactRefs,
      receipt.artifactPaths,
      receipt.validationEvidencePaths,
      receipt.verificationEvidencePaths
    ).length,
    criteriaCoverage: receipt.criteriaCoverage ?? null,
    nextAction: receipt.nextAction ?? null
  };
}
function summarizeRuntimeRun(entry, packetId) {
  const iteration = runtimeIterationForPacket(entry, packetId);
  const executionReceipt = compactRuntimeExecutionReceipt(iteration?.output?.executionReceipt ?? iteration?.executionReceipt ?? entry.executionReceipt);
  return {
    id: entry.id ?? entry.runId ?? null,
    surface: entry.surface ?? null,
    packetId,
    status: iteration?.status ?? entry.status ?? null,
    outcome: iteration?.outcome ?? entry.outcome ?? null,
    stopReason: iteration?.stopReason ?? entry.stopReason ?? null,
    command: iteration?.command ?? entry.command ?? null,
    executionReceipt,
    startedAt: iteration?.startedAt ?? entry.startedAt ?? entry.createdAt ?? null,
    completedAt: iteration?.completedAt ?? entry.completedAt ?? entry.updatedAt ?? entry.recordedAt ?? null,
    updatedAt: entry.updatedAt ?? entry.recordedAt ?? null
  };
}
function lastRuntimeRunForTask(inputs, packetId) {
  return runtimeResultEntries(inputs).filter((entry) => runtimeEntryTouchesPacket(entry, packetId)).sort((left, right) => String(runtimeTimestamp(right, runtimeIterationForPacket(right, packetId))).localeCompare(String(runtimeTimestamp(left, runtimeIterationForPacket(left, packetId))))).map((entry) => summarizeRuntimeRun(entry, packetId))[0] ?? null;
}
function runtimeEventEntries(inputs) {
  return [
    ...Array.isArray(inputs.runtimeEvents?.items) ? inputs.runtimeEvents.items : [],
    ...Array.isArray(inputs.runtimeEvents?.entries) ? inputs.runtimeEvents.entries : []
  ].filter((entry) => entry && typeof entry === "object" && !Array.isArray(entry));
}
function runtimeEventTimestamp(entry) {
  return entry.timestamp ?? entry.recordedAt ?? entry.completedAt ?? entry.createdAt ?? entry.startedAt ?? "";
}
function runtimeEventTouchesPacket(entry, packetId) {
  return [entry.packetId, entry.taskId, entry.missionPacketId, entry.boundary?.packetId].includes(packetId);
}
function summarizeRuntimeEvent(entry) {
  return {
    id: entry.id ?? entry.eventId ?? null,
    type: entry.type ?? entry.eventType ?? null,
    packetId: entry.packetId ?? entry.taskId ?? entry.missionPacketId ?? null,
    runId: entry.runId ?? null,
    surface: entry.surface ?? entry.sourceSurface ?? null,
    command: entry.command ?? null,
    fromStatus: entry.fromStatus ?? null,
    toStatus: entry.toStatus ?? null,
    boundaryId: entry.boundaryId ?? entry.boundary?.id ?? null,
    handoffId: entry.handoffId ?? entry.handoff?.id ?? null,
    summary: entry.summary ?? null,
    timestamp: runtimeEventTimestamp(entry)
  };
}
function lastRuntimeEventForTask(inputs, packetId) {
  return runtimeEventEntries(inputs).filter((entry) => runtimeEventTouchesPacket(entry, packetId)).sort((left, right) => String(runtimeEventTimestamp(right)).localeCompare(String(runtimeEventTimestamp(left)))).map(summarizeRuntimeEvent)[0] ?? null;
}
function continuationForTask(inputs, packetId) {
  const item = (Array.isArray(inputs.runtimeContinuation.items) ? inputs.runtimeContinuation.items : []).find((candidate) => candidate?.packetId === packetId) ?? null;
  if (!item) {
    return null;
  }
  return {
    kind: item.kind ?? null,
    command: item.command ?? null,
    packetId,
    programRunId: item.programRunId ?? null,
    followThroughId: item.followThroughId ?? null,
    summary: item.summary ?? null,
    requiredReadPaths: normalizeStringArray13(item.requiredReadPaths),
    readyAt: item.readyAt ?? null
  };
}
function openBoundaryForTask(task) {
  const boundary = normalizeDoveBoundary(task.boundary, null);
  return boundary?.status === "open" ? boundary : null;
}
function summarizeActionableBoundary(task) {
  const boundary = task.currentBoundary ?? openBoundaryForTask(task);
  if (!boundary || task.level === 0 || ["completed", "killed"].includes(task.status)) {
    return null;
  }
  return {
    id: boundary.id,
    type: boundary.type,
    status: boundary.status,
    packetId: task.id,
    title: task.title,
    taskStatus: task.status,
    reason: boundary.reason,
    summary: boundary.summary,
    requiredInputs: normalizeStringArray13(boundary.requiredInputs),
    requiredActions: normalizeStringArray13(boundary.requiredActions),
    ownerRole: boundary.ownerRole ?? task.ownerRole ?? null,
    nextRole: boundary.nextRole ?? task.nextRole ?? null,
    createdAt: boundary.createdAt ?? null,
    command: boundary.command ?? task.nextAction ?? null,
    runId: boundary.runId ?? null,
    handoff: normalizeDoveHandoff(task.handoff, null)
  };
}
function boundaryRecommendsBlocked(task) {
  const boundary = task.currentBoundary ?? openBoundaryForTask(task);
  return ["blocked-boundary", "missing-executable-contract", "plan-output-not-executable", "missing-required-materials", "missing-secret-env", "provider-failed", "workflow-error-boundary", "verification-failed", "debug-retry-required", "fix-required", "needs-review", "awaiting-provider-output", "awaiting-review-output"].includes(boundary?.type);
}
function statusActionBase(fields = {}) {
  return {
    proposalOnly: true,
    noAutoApply: true,
    confirmationRequired: true,
    ...fields
  };
}
function boundaryActionKind(boundary) {
  if (["needs-review", "awaiting-review-output"].includes(boundary?.type)) {
    return "send-to-review";
  }
  if (["awaiting-host-pass", "awaiting-host-pass-result", "awaiting-host-results", "host-tool-blocked", "awaiting-provider-output", "missing-executable-contract", "plan-output-not-executable", "missing-required-materials", "missing-secret-env", "verification-failed", "debug-retry-required", "fix-required"].includes(boundary?.type)) {
    return "provide-evidence-or-result";
  }
  return "adjust-status";
}
function toPublicDoveCommand(command, fallback = "project:dove.status") {
  const normalized = typeof command === "string" ? command.trim() : "";
  if (!normalized) {
    return fallback;
  }
  if (normalized.startsWith("project:dove.")) {
    return normalized;
  }
  const toolRoutes = {
    run_dove_auto: "project:dove.auto",
    run_dove_operator: "project:dove.operator",
    create_dove_task: "project:dove.mission",
    record_dove_mission_pass: "project:dove.mission",
    apply_dove_status_adjustments: "project:dove.status",
    run_audio_review: "project:dove.review",
    run_dove_review_loop: "project:dove.review-loop",
    run_experience_workflow: "project:dove.experience",
    run_figure_workflow: "project:dove.figure",
    register_source: "project:dove.source",
    upsert_note: "project:dove.note",
    upsert_draft: "project:dove.draft",
    build_rebuttal: "project:dove.rebuttal"
  };
  return toolRoutes[normalized] ?? (normalized.startsWith("dove.") ? `project:${normalized}` : fallback);
}
function statusPublicCommandText(command, fallback = null) {
  const raw = typeof command === "string" ? command.trim() : "";
  if (!raw) {
    return fallback;
  }
  const firstToken = raw.split(/\s+/u)[0];
  const publicCommand = toPublicDoveCommand(firstToken, fallback);
  const projectMatch = publicCommand?.match(/^project:dove\.([a-z0-9.-]+)$/u);
  if (projectMatch) {
    return `dove.${projectMatch[1]}`;
  }
  return /^dove\.[a-z0-9.-]+$/u.test(publicCommand ?? "") ? publicCommand : fallback;
}
function boundaryActionCommand(task, boundary, kind) {
  if (kind === "send-to-review") {
    return "project:dove.review";
  }
  if (kind === "adjust-status") {
    return null;
  }
  const command = toPublicDoveCommand(boundary?.command ?? task.continuationState?.command ?? task.nextAction, null);
  return command === "project:dove.status" ? null : command;
}
function boundaryActionLabel(kind, responseLanguage) {
  if (kind === "send-to-review") {
    return doveText(responseLanguage, "boundaryActionReviewLabel");
  }
  if (kind === "provide-evidence-or-result") {
    return doveText(responseLanguage, "boundaryActionEvidenceLabel");
  }
  if (kind === "kill-through-status") {
    return doveText(responseLanguage, "boundaryActionKillLabel");
  }
  if (kind === "continue-resume") {
    return doveText(responseLanguage, "boundaryActionContinueLabel");
  }
  return doveText(responseLanguage, "boundaryActionStatusLabel");
}
function boundaryActionOption(kind, task, boundary, responseLanguage) {
  const command = boundaryActionCommand(task, boundary, kind);
  return statusActionBase({
    id: `${boundary.id}-${kind}`,
    label: boundaryActionLabel(kind, responseLanguage),
    kind,
    command,
    tool: ["adjust-status", "kill-through-status"].includes(kind) ? "apply_dove_status_adjustments" : null,
    packetId: task.id,
    boundaryId: boundary.id,
    boundaryType: boundary.type,
    requires: mergeStringArrays(boundary.requiredInputs, boundary.requiredActions),
    requiredInputs: normalizeStringArray13(boundary.requiredInputs),
    requiredActions: normalizeStringArray13(boundary.requiredActions)
  });
}
function buildBoundaryActionCard(task, responseLanguage = "zh") {
  const boundary = task.actionableBoundary ?? summarizeActionableBoundary(task);
  if (!boundary) {
    return null;
  }
  const primaryKind = boundaryActionKind(boundary);
  const optionKinds = Array.from(new Set([
    task.continuationState || task.nextAction ? "continue-resume" : null,
    primaryKind,
    "adjust-status",
    "kill-through-status"
  ].filter(Boolean)));
  const options = optionKinds.map((kind) => boundaryActionOption(kind, task, boundary, responseLanguage));
  const primaryOption = options.find((option) => option.kind === primaryKind) ?? options[0];
  return statusActionBase({
    id: `boundary-action-${boundary.id}`,
    label: primaryOption.label,
    kind: primaryKind,
    command: primaryOption.command,
    tool: primaryOption.tool,
    packetId: task.id,
    title: task.title,
    boundaryId: boundary.id,
    boundaryType: boundary.type,
    reason: boundary.reason,
    summary: boundary.summary,
    requiredInputs: normalizeStringArray13(boundary.requiredInputs),
    requiredActions: normalizeStringArray13(boundary.requiredActions),
    requires: mergeStringArrays(boundary.requiredInputs, boundary.requiredActions),
    ownerRole: boundary.ownerRole ?? task.ownerRole ?? null,
    nextRole: boundary.nextRole ?? task.nextRole ?? null,
    handoff: boundary.handoff ?? task.handoff ?? null,
    runId: boundary.runId ?? null,
    options
  });
}
function rankStatusActionCards(cards) {
  return cards.filter(Boolean).sort((left, right) => (left.priority ?? 100) - (right.priority ?? 100) || String(left.title ?? "").localeCompare(String(right.title ?? ""))).slice(0, 3).map((card, index) => {
    const { priority, ...rest } = card;
    return { ...rest, rank: index + 1 };
  });
}
var STATUS_RECOVERY_CARD_KINDS = /* @__PURE__ */ new Set([
  "reconcile-completion-consistency",
  "boundary-resume",
  "provide-evidence",
  "blocked-unblock",
  "missing-executable-contract",
  "missing-required-materials",
  "verification-failed",
  "review-needed"
]);
function statusCardNeedsRecovery(card) {
  return STATUS_RECOVERY_CARD_KINDS.has(card?.kind);
}
function recoveryCandidateDetail(card) {
  if (!card) {
    return null;
  }
  return compactStatusObject({
    kind: card.kind,
    title: card.title,
    command: card.command,
    copyableCommand: card.copyableCommand,
    packetId: card.packetId ?? null,
    nextRole: card.nextRole ?? null,
    boundaryType: card.boundaryType ?? card.boundary?.type ?? null,
    evidenceRequired: normalizeStringArray13(card.evidenceRequired).slice(0, 8),
    doneCriteria: normalizeStringArray13(card.doneCriteria).slice(0, 8),
    requiredMaterials: normalizeStringArray13(card.requiredMaterials).slice(0, 8),
    criteriaCoverage: card.criteriaCoverage ? {
      complete: Boolean(card.criteriaCoverage.complete),
      missing: normalizeStringArray13(card.criteriaCoverage.missing).slice(0, 8)
    } : null
  });
}
function statusCardIsStatusTriage(card, command) {
  return statusCardNeedsRecovery(card) && command === "project:dove.status";
}
function statusCardPublicCommand(card, fallback = null) {
  const command = typeof card?.command === "string" && card.command.trim() ? card.command.trim() : null;
  if (!command) {
    return fallback;
  }
  const publicCommand = toPublicDoveCommand(command, fallback);
  if (!publicCommand || statusCardIsStatusTriage(card, publicCommand)) {
    return fallback;
  }
  return publicCommand;
}
function statusCardCopyableCommand(card, fallback = null) {
  const explicit = typeof card?.copyableCommand === "string" && card.copyableCommand.trim() ? card.copyableCommand.trim() : null;
  if (explicit && !statusCardIsStatusTriage(card, explicit)) {
    return explicit;
  }
  const command = statusCardPublicCommand(card, null);
  if (command) {
    if (card?.packetId && /^project:dove\.(mission|auto|operator|source|note|figure|experience|draft|review|review-loop|rebuttal)$/.test(command)) {
      return statusCopyableCommand(command, card.packetId);
    }
    return command;
  }
  const firstAction = typeof card?.firstAction === "string" && card.firstAction.trim() ? card.firstAction.trim() : null;
  if (firstAction && (firstAction.startsWith("project:dove.") || firstAction.startsWith("dove."))) {
    const publicFirstAction = toPublicDoveCommand(firstAction, null);
    return publicFirstAction && !statusCardIsStatusTriage(card, publicFirstAction) ? publicFirstAction : fallback;
  }
  return fallback;
}
function statusShortText(value, maxLength = 90) {
  const text3 = String(value ?? "").replace(/\s+/g, " ").trim();
  if (!text3) {
    return "";
  }
  return text3.length > maxLength ? `${text3.slice(0, maxLength - 1)}\u2026` : text3;
}
var STATUS_PUBLIC_FORBIDDEN_TEXT = /\.dove\/|\bproject:dove\.[a-z0-9.-]+|--packet-id\b|\b(?:packetId|taskPacketId|missionPacketId|runId|receiptId|boundaryId|boundaryType|mutationMode|patch-plan|direct-process|ownerRole|nextRole|handoff|providerId|sourceSvgPath|targetFinalSvgPath|finalSvgPath|outputManifestPath|svgContent|queueSummary|queuePreview|preActionGuidance|resultCard)\b|\b(?:query_dove_status|run_dove_auto|record_dove_mission_pass|run_figure_workflow|run_dove_operator|record_document_evidence|upsert_note|upsert_draft|register_source)\b/u;
var STATUS_INTERNAL_ACTION_CODE = /^(?:[a-z]+-){2,}[a-z]+$/u;
function statusPublicText(value, responseLanguage = "zh", maxLength = 90) {
  let text3 = statusShortText(value, maxLength);
  if (!text3) {
    return "";
  }
  text3 = text3.replace(/\btask-[a-z0-9][a-z0-9-]*\b/giu, responseLanguage === "en" ? "the current task" : "\u5F53\u524D\u4EFB\u52A1");
  if (STATUS_PUBLIC_FORBIDDEN_TEXT.test(text3) || STATUS_INTERNAL_ACTION_CODE.test(text3)) {
    return "";
  }
  return text3;
}
function statusFirstPublicText(responseLanguage, maxLength, ...values) {
  for (const value of values) {
    const text3 = statusPublicText(value, responseLanguage, maxLength);
    if (text3) {
      return text3;
    }
  }
  return "";
}
function statusRecoverySubject(card, responseLanguage = "zh") {
  if (card?.packetId) {
    return responseLanguage === "en" ? "the current task" : "\u5F53\u524D\u4EFB\u52A1";
  }
  return statusFirstPublicText(responseLanguage, 72, card?.title, card?.label, card?.kind);
}
function statusRecoveryRequirement(card, responseLanguage = "zh") {
  const items = mergeStringArrays(
    card?.requiredMaterials,
    card?.evidenceRequired,
    card?.requiredInputs,
    card?.requires,
    card?.operatorUnblock?.needs,
    card?.operatorUnblock?.requiredEvidence
  ).map((item) => statusPublicText(item, responseLanguage, 52)).filter(Boolean).slice(0, 3);
  if (items.length === 0) {
    return "";
  }
  return items.join(responseLanguage === "en" ? ", " : "\u3001");
}
function statusRecoveryLabel(responseLanguage, zhAction, enAction, subject, requirement) {
  if (responseLanguage === "en") {
    const base2 = subject ? `First ${enAction} for ${subject}` : `First ${enAction}`;
    return requirement ? `${base2}: ${requirement}` : base2;
  }
  const base = subject ? `\u5148\u4E3A${subject}${zhAction}` : `\u5148${zhAction}`;
  return requirement ? `${base}\uFF1A${requirement}` : base;
}
function recoveryPathTitle(primaryCard, responseLanguage = "zh") {
  const kind = primaryCard?.kind;
  const subject = statusRecoverySubject(primaryCard, responseLanguage);
  const requirement = statusRecoveryRequirement(primaryCard, responseLanguage);
  if (kind === "missing-executable-contract") {
    return statusRecoveryLabel(responseLanguage, "\u8865\u53EF\u6267\u884C\u5408\u540C", "add the executable contract", subject, requirement);
  }
  if (kind === "missing-required-materials") {
    return statusRecoveryLabel(responseLanguage, "\u8865\u6750\u6599", "provide the missing materials", subject, requirement);
  }
  if (kind === "verification-failed") {
    return statusRecoveryLabel(responseLanguage, "\u8865\u9A8C\u8BC1\u8BC1\u636E", "provide verification evidence", subject, requirement);
  }
  if (kind === "review-needed") {
    return statusRecoveryLabel(responseLanguage, "\u9001\u5BA1\u6216\u5BFC\u5165\u5BA1\u67E5\u7ED3\u679C", "send for review or import the review result", subject, requirement);
  }
  if (kind === "reconcile-completion-consistency") {
    return statusRecoveryLabel(responseLanguage, "\u6838\u5BF9\u7236\u4EFB\u52A1\u548C checklist \u5B50\u4EFB\u52A1\u72B6\u6001", "reconcile parent and checklist child status", subject, requirement);
  }
  if (kind === "provide-evidence" || kind === "boundary-resume" || kind === "blocked-unblock") {
    return statusRecoveryLabel(responseLanguage, "\u8865\u771F\u5B9E\u7ED3\u679C", "provide the real result", subject, requirement);
  }
  return statusRecoveryLabel(responseLanguage, "\u5904\u7406\u5F53\u524D\u963B\u585E", "handle the current blocker", subject, requirement);
}
function buildRecoveryPathCard(primaryCard, recoveryCards = [], responseLanguage = "zh") {
  if (!primaryCard) {
    return null;
  }
  const command = statusCardPublicCommand(primaryCard);
  const action = statusCardCopyableCommand(primaryCard);
  const relatedCards = recoveryCards.filter((card) => card && card !== primaryCard);
  return statusActionBase({
    ...primaryCard,
    priority: Math.min(primaryCard.priority ?? 10, 9),
    kind: "recover-current-work",
    title: recoveryPathTitle(primaryCard, responseLanguage),
    why: primaryCard.operatorUnblock?.blockedSummary ?? primaryCard.why ?? primaryCard.operatorUnblock?.nextOperatorAction,
    command,
    firstAction: action,
    copyableCommand: action,
    recoveryPrimaryKind: primaryCard.kind,
    relatedActionCount: recoveryCards.length,
    relatedActionKinds: Array.from(new Set(recoveryCards.map((card) => card?.kind).filter(Boolean))).slice(0, 8),
    detail: compactStatusObject({
      primary: recoveryCandidateDetail(primaryCard),
      related: relatedCards.slice(0, 8).map(recoveryCandidateDetail),
      candidates: recoveryCards.slice(0, 12).map(recoveryCandidateDetail)
    })
  });
}
function collapseStatusRecoveryCards(cards, responseLanguage = "zh") {
  const recoveryCards = cards.filter(statusCardNeedsRecovery);
  if (recoveryCards.length === 0) {
    return cards;
  }
  const primaryRecovery = recoveryCards.slice().sort((left, right) => (left.priority ?? 100) - (right.priority ?? 100) || String(left.title ?? "").localeCompare(String(right.title ?? "")))[0];
  const collapsed = buildRecoveryPathCard(primaryRecovery, recoveryCards, responseLanguage);
  return [
    ...cards.filter((card) => !statusCardNeedsRecovery(card)),
    collapsed
  ].filter(Boolean);
}
function completionConsistencyFindings(completionConsistency = {}) {
  return Array.isArray(completionConsistency.findings) ? completionConsistency.findings : [];
}
function completionConsistencyOpenChecklistChildIds(completionConsistency = {}) {
  return new Set(completionConsistencyFindings(completionConsistency).flatMap((finding) => normalizeStringArray13(finding.openChecklistChildIds)));
}
function buildCompletionReconciliationCard(completionConsistency = {}, responseLanguage = "zh") {
  const findings = completionConsistencyFindings(completionConsistency);
  if (findings.length === 0) {
    return null;
  }
  const openChecklistChildIds = Array.from(completionConsistencyOpenChecklistChildIds(completionConsistency));
  return statusActionBase({
    priority: 70,
    kind: "reconcile-completion-consistency",
    title: doveText(responseLanguage, "statusHomeReconcileTitle", { count: findings.length }),
    why: doveText(responseLanguage, "statusHomeReconcileWhy"),
    command: "project:dove.status",
    firstAction: "project:dove.status",
    affectedParentIds: normalizeStringArray13(findings.map((finding) => finding.parentId)),
    findingCount: findings.length,
    openChecklistChildCount: openChecklistChildIds.length,
    openChecklistChildIds,
    evidenceRequired: openChecklistChildIds,
    doneCriteria: [doveText(responseLanguage, "statusHomeReconcileDoneCriteria")],
    findings
  });
}
function primaryStatusRoute(task = {}) {
  const routes = Array.isArray(task.workContract?.recommendedRoutes) ? task.workContract.recommendedRoutes : [];
  return routes.find((route) => typeof route?.command === "string" && route.command.trim()) ?? null;
}
function statusRouteEvidence(task = {}, route = null) {
  return firstStatusContractStringArray(route?.evidenceRequired, task.executionReadiness?.evidenceRequired, task.executionReadiness?.requiredMaterials, task.workContract?.evidenceContract, task.evidenceExpectations);
}
function statusRouteDoneCriteria(task = {}, route = null) {
  return firstStatusContractStringArray(route?.doneCriteria, task.executionContract?.convergence?.criteria, task.executionContract?.convergence?.definitionOfDone, task.workContract?.doneCriteria);
}
function statusInlineText(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function statusTaskEvidenceBundle(task = {}) {
  return mergeStringArrays(
    task.artifactRefs,
    task.outputPaths,
    task.evidenceLinks,
    task.validationEvidencePaths,
    task.verificationEvidencePaths,
    task.verifiedCriteria?.flatMap((item) => item.evidencePaths ?? [])
  );
}
function missingExecutionMaterials(task = {}) {
  const required = normalizeStringArray13(task.executionReadiness?.requiredMaterials);
  if (required.length === 0) {
    return [];
  }
  const available = new Set(statusTaskEvidenceBundle(task).map((item) => item.toLowerCase()));
  return required.filter((item) => !available.has(item.toLowerCase()));
}
function hasExecutableContract(task = {}) {
  return task.executionReadiness?.ready === true;
}
function buildMissingExecutionContractCard(task, responseLanguage = "zh") {
  if (hasExecutableContract(task)) {
    return null;
  }
  return statusActionBase({
    priority: 20,
    kind: "missing-executable-contract",
    title: statusInlineText(responseLanguage, `\u8865\u9F50\u53EF\u6267\u884C\u5408\u540C\uFF1A${task.title}`, `Add executable contract: ${task.title}`),
    why: statusInlineText(responseLanguage, "Planner \u5FC5\u987B\u5148\u4EA7\u51FA action\u3001implementation\u3001convergence.criteria \u548C failureRoutes\uFF0C\u4E0D\u80FD\u8BA9\u4EFB\u52A1\u53EA\u505C\u7559\u5728 mission \u6587\u6848\u3002", "Planner must provide action, implementation, convergence.criteria, and failureRoutes before the task can execute."),
    packetId: task.id,
    command: "project:dove.mission",
    firstAction: statusCopyableCommand("project:dove.mission", task.id),
    evidenceRequired: normalizeStringArray13(task.executionReadiness?.missing).map((item) => item === "executionContract" ? item : `executionContract.${item}`),
    doneCriteria: [statusInlineText(responseLanguage, "\u4EFB\u52A1\u5305\u542B\u53EF\u6267\u884C\u5408\u540C\uFF0C\u5E76\u660E\u786E Builder \u8BC1\u636E\u4E0E Reviewer \u9A8C\u8BC1\u6807\u51C6\u3002", "Task has an executable contract with Builder evidence and Reviewer verification criteria.")],
    executionReadiness: task.executionReadiness,
    nextRole: "planner"
  });
}
function buildMissingExecutionMaterialsCard(task, responseLanguage = "zh") {
  if (!hasExecutableContract(task)) {
    return null;
  }
  const missingMaterials = missingExecutionMaterials(task);
  if (missingMaterials.length === 0) {
    return null;
  }
  const route = primaryStatusRoute(task);
  const command = toPublicDoveCommand(route?.command ?? task.nextAction, null);
  const actionableCommand = command === "project:dove.status" ? null : command;
  return statusActionBase({
    priority: 30,
    kind: "missing-required-materials",
    title: statusInlineText(responseLanguage, `\u8865\u6750\u6599/\u8F93\u5165\uFF1A${task.title}`, `Provide materials/inputs: ${task.title}`),
    why: statusInlineText(responseLanguage, "\u6267\u884C\u5408\u540C\u58F0\u660E\u4E86\u5FC5\u9700\u8F93\u5165\u6216 artifact\uFF0CBuilder \u4E0D\u80FD\u5728\u6750\u6599\u7F3A\u5931\u65F6\u5047\u88C5\u63A8\u8FDB\u3002", "The execution contract declares required inputs or artifacts; Builder cannot claim progress while materials are missing."),
    packetId: task.id,
    command: actionableCommand,
    firstAction: actionableCommand ? route?.copyableCommand ?? statusCopyableCommand(actionableCommand, task.id) : null,
    copyableCommand: actionableCommand ? route?.copyableCommand ?? statusCopyableCommand(actionableCommand, task.id) : null,
    evidenceRequired: missingMaterials,
    doneCriteria: [statusInlineText(responseLanguage, "\u7F3A\u5931\u6750\u6599\u88AB\u6CE8\u518C\u4E3A source\u3001artifact \u6216 verification evidence\u3002", "Missing materials are registered as source, artifact, or verification evidence.")],
    requiredMaterials: missingMaterials,
    executionContract: task.executionContract,
    nextRole: "planner"
  });
}
function taskNeedsBuilderExecution(task = {}) {
  const hasEvidence = statusTaskEvidenceBundle(task).length > 0;
  return hasExecutableContract(task) && missingExecutionMaterials(task).length === 0 && !(task.criteriaCoverage?.complete === false && hasEvidence);
}
function buildReadyBuilderExecutionCard(task, responseLanguage = "zh") {
  if (!taskNeedsBuilderExecution(task)) {
    return null;
  }
  const route = primaryStatusRoute(task);
  const command = toPublicDoveCommand(route?.command ?? task.nextAction, "project:dove.auto");
  return statusActionBase({
    priority: 40,
    kind: "ready-builder-execution",
    title: statusInlineText(responseLanguage, `\u6267\u884C\u5408\u540C\uFF1A${task.title}`, `Execute contract: ${task.title}`),
    why: statusInlineText(responseLanguage, "\u5408\u540C\u5DF2\u53EF\u6267\u884C\uFF0C\u4E0B\u4E00\u6B65\u5E94\u7531 Builder \u4EA7\u51FA\u771F\u5B9E\u8BC1\u636E\uFF0C\u800C\u4E0D\u662F\u7EE7\u7EED\u6574\u7406\u72B6\u6001\u3002", "The contract is executable; Builder should now produce real evidence instead of more bookkeeping."),
    packetId: task.id,
    command,
    firstAction: route?.copyableCommand ?? statusCopyableCommand(command, task.id),
    copyableCommand: route?.copyableCommand ?? statusCopyableCommand(command, task.id),
    evidenceRequired: statusRouteEvidence(task, route),
    doneCriteria: statusRouteDoneCriteria(task, route),
    executionContract: task.executionContract,
    nextRole: "builder"
  });
}
function buildVerificationGapCard(task, responseLanguage = "zh") {
  if (!hasExecutableContract(task) || task.criteriaCoverage?.complete !== false || statusTaskEvidenceBundle(task).length === 0) {
    return null;
  }
  const route = primaryStatusRoute(task);
  const command = toPublicDoveCommand(route?.command ?? task.nextAction, "project:dove.review");
  const actionableCommand = command === "project:dove.status" ? "project:dove.review" : command;
  return statusActionBase({
    priority: 45,
    kind: "verification-failed",
    title: statusInlineText(responseLanguage, `\u8865\u9A8C\u8BC1\u8986\u76D6\uFF1A${task.title}`, `Complete verification coverage: ${task.title}`),
    why: statusInlineText(responseLanguage, "\u5DF2\u6709\u6267\u884C\u8BC1\u636E\uFF0C\u4F46 verifiedCriteria \u5C1A\u672A\u8986\u76D6\u5168\u90E8 convergence.criteria\u3002", "Execution evidence exists, but verifiedCriteria does not cover all convergence.criteria."),
    packetId: task.id,
    command: actionableCommand,
    firstAction: route?.copyableCommand ?? statusCopyableCommand(actionableCommand, task.id),
    copyableCommand: route?.copyableCommand ?? statusCopyableCommand(actionableCommand, task.id),
    evidenceRequired: normalizeStringArray13(task.criteriaCoverage?.missing),
    doneCriteria: normalizeStringArray13(task.criteriaCoverage?.required),
    criteriaCoverage: task.criteriaCoverage,
    nextRole: "reviewer"
  });
}
function taskLooksLikeCleanupNoise(task = {}) {
  if (task.derivedFrom === "blocked-mission-investigation") {
    return true;
  }
  const searchable = normalizeStringArray13([task.id, task.title, task.summary, task.sourceId, task.sourceType, task.creatorKind]).join(" ").toLowerCase();
  return /\bdogfood\b|visible[-_ ]?test|operator[-_ ]?investigation|blocked[-_ ]?mission[-_ ]?investigation/.test(searchable);
}
function cleanupArchiveCandidates(tasks = []) {
  return sortStatusTasks(tasks.filter((task) => task.level !== 0 && !["completed", "killed", "archived"].includes(task.status) && !isArchivedStatusTask(task) && taskLooksLikeCleanupNoise(task))).slice(0, 5);
}
function buildCleanupArchiveCard(tasks = [], responseLanguage = "zh") {
  const candidates = cleanupArchiveCandidates(tasks);
  if (candidates.length === 0) {
    return null;
  }
  const candidateIds = candidates.map((task) => task.id);
  return statusActionBase({
    priority: 80,
    kind: "cleanup-archive",
    title: statusInlineText(responseLanguage, "\u5F52\u6863\u8FC7\u671F\u6D4B\u8BD5/\u8C03\u67E5\u566A\u97F3", "Archive stale test/investigation noise"),
    why: statusInlineText(responseLanguage, "\u53D1\u73B0\u7591\u4F3C dogfood \u6216 operator blocker-investigation \u566A\u97F3\uFF1B\u53EA\u5EFA\u8BAE\u901A\u8FC7\u663E\u5F0F status adjustment \u5F52\u6863\uFF0C\u4E0D\u5220\u9664\u8BC1\u636E\u3002", "Likely dogfood or operator blocker-investigation noise was found; archive it only through explicit status adjustment and preserve evidence."),
    command: "project:dove.status",
    firstAction: "project:dove.status --request-status-adjustment",
    copyableCommand: "project:dove.status --request-status-adjustment",
    evidenceRequired: candidateIds,
    doneCriteria: [statusInlineText(responseLanguage, "\u786E\u8BA4\u8FD9\u4E9B packet \u5DF2\u8FC7\u671F\u540E\uFF0C\u5C06\u5B83\u4EEC\u8C03\u6574\u4E3A archived\uFF0C\u4FDD\u7559 durable evidence\u3002", "After confirming these packets are stale, adjust them to archived while preserving durable evidence.")],
    requires: candidateIds,
    options: [{ kind: "preview-status-adjustments", label: statusInlineText(responseLanguage, "\u9884\u89C8\u72B6\u6001\u8C03\u6574", "Preview status adjustments"), command: "project:dove.status --request-status-adjustment" }]
  });
}
function buildStatusExecutionGaps(activeTasks = []) {
  const missingContractTasks = [];
  const missingMaterialTasks = [];
  const verificationGapTasks = [];
  const readyBuilderTasks = [];
  const requiredMaterials = [];
  const evidenceRequired = [];
  for (const task of activeTasks) {
    if (task.actionableBoundary) {
      continue;
    }
    if (!hasExecutableContract(task)) {
      missingContractTasks.push(task);
      evidenceRequired.push(...normalizeStringArray13(task.executionReadiness?.missing).map((item) => item === "executionContract" ? item : `executionContract.${item}`));
      continue;
    }
    const missingMaterials = missingExecutionMaterials(task);
    if (missingMaterials.length > 0) {
      missingMaterialTasks.push(task);
      requiredMaterials.push(...missingMaterials);
      evidenceRequired.push(...missingMaterials);
      continue;
    }
    if (task.criteriaCoverage?.complete === false && statusTaskEvidenceBundle(task).length > 0) {
      verificationGapTasks.push(task);
      evidenceRequired.push(...normalizeStringArray13(task.criteriaCoverage.missing));
      continue;
    }
    if (taskNeedsBuilderExecution(task)) {
      readyBuilderTasks.push(task);
      evidenceRequired.push(...statusRouteEvidence(task, primaryStatusRoute(task)));
    }
  }
  return {
    missingContractTaskIds: missingContractTasks.map((task) => task.id),
    missingMaterialTaskIds: missingMaterialTasks.map((task) => task.id),
    verificationGapTaskIds: verificationGapTasks.map((task) => task.id),
    readyBuilderTaskIds: readyBuilderTasks.map((task) => task.id),
    requiredMaterials: mergeStringArrays(requiredMaterials),
    evidenceRequired: mergeStringArrays(evidenceRequired),
    counts: {
      missingContract: missingContractTasks.length,
      missingMaterials: missingMaterialTasks.length,
      verificationGaps: verificationGapTasks.length,
      readyBuilder: readyBuilderTasks.length,
      blocking: missingContractTasks.length + missingMaterialTasks.length + verificationGapTasks.length
    }
  };
}
function buildStatusNextActionCards({ initTask, activeTasks, blockedTasks, visibleTasks, review, boundaryActionCards, completionConsistency, responseLanguage = "zh" }) {
  const cards = [];
  const seen = /* @__PURE__ */ new Set();
  const reconciliationChildIds = completionConsistencyOpenChecklistChildIds(completionConsistency);
  const pushCard = (key, card) => {
    if (!key || seen.has(key) || !card) {
      return;
    }
    seen.add(key);
    cards.push(statusActionBase(card));
  };
  if (!initTask) {
    pushCard("init", {
      priority: 0,
      kind: "init",
      title: doveText(responseLanguage, "statusHomeInitTitle"),
      why: doveText(responseLanguage, "statusHomeInitWhy"),
      command: "project:dove.init",
      firstAction: "project:dove.init",
      evidenceRequired: []
    });
  }
  pushCard("completion-consistency", buildCompletionReconciliationCard(completionConsistency, responseLanguage));
  for (const action of boundaryActionCards) {
    if (reconciliationChildIds.has(action.packetId)) {
      continue;
    }
    const kind = action.kind === "send-to-review" ? "review-needed" : action.kind === "provide-evidence-or-result" ? "provide-evidence" : "boundary-resume";
    const operatorUnblock = buildStatusOperatorUnblock({
      kind,
      reason: action.reason,
      boundaryType: action.boundaryType,
      boundary: {
        id: action.boundaryId,
        type: action.boundaryType,
        reason: action.reason,
        requiredInputs: action.requiredInputs,
        requiredActions: action.requiredActions,
        command: action.command
      },
      requiredActions: action.requiredActions,
      requiredEvidence: action.requires,
      nextAction: action.command
    }, responseLanguage);
    pushCard(`boundary:${action.boundaryId}`, {
      priority: 10,
      kind,
      title: doveText(responseLanguage, "statusHomeBoundaryTitle", { title: action.title ?? action.packetId }),
      why: operatorUnblock?.blockedSummary ?? doveText(responseLanguage, "statusHomeBoundaryWhy", { reason: action.reason }),
      packetId: action.packetId,
      command: action.command,
      firstAction: operatorUnblock?.nextOperatorAction ?? action.label,
      evidenceRequired: operatorUnblock?.requiredEvidence?.length ? operatorUnblock.requiredEvidence : action.requires,
      operatorUnblock,
      boundary: {
        id: action.boundaryId,
        type: action.boundaryType,
        reason: action.reason,
        requiredInputs: action.requiredInputs,
        requiredActions: action.requiredActions
      },
      handoff: action.handoff,
      boundaryActionCard: action
    });
  }
  for (const task of activeTasks) {
    if (reconciliationChildIds.has(task.id) || task.actionableBoundary) {
      continue;
    }
    pushCard(`execution-contract:${task.id}`, buildMissingExecutionContractCard(task, responseLanguage));
    pushCard(`execution-materials:${task.id}`, buildMissingExecutionMaterialsCard(task, responseLanguage));
    pushCard(`execution-builder:${task.id}`, buildReadyBuilderExecutionCard(task, responseLanguage));
    pushCard(`execution-verification:${task.id}`, buildVerificationGapCard(task, responseLanguage));
  }
  for (const task of activeTasks) {
    if (reconciliationChildIds.has(task.id) || !task.continuationState) {
      continue;
    }
    const route = primaryStatusRoute(task);
    const command = toPublicDoveCommand(task.continuationState.command ?? route?.command ?? task.nextAction, "project:dove.auto");
    pushCard(`continuation:${task.id}`, {
      priority: 20,
      kind: "continue-task",
      title: doveText(responseLanguage, "statusHomeContinuationTitle", { title: task.title }),
      why: route?.when ?? task.workContract?.practicalImpact ?? doveText(responseLanguage, "statusHomeContinuationWhy"),
      packetId: task.id,
      command,
      firstAction: route?.copyableCommand ?? statusCopyableCommand(command, task.id),
      copyableCommand: route?.copyableCommand ?? statusCopyableCommand(command, task.id),
      evidenceRequired: statusRouteEvidence(task, route),
      doneCriteria: statusRouteDoneCriteria(task, route),
      workContract: task.workContract,
      route,
      continuation: task.continuationState,
      handoff: task.handoff ?? null
    });
  }
  for (const task of blockedTasks) {
    const route = primaryStatusRoute(task);
    const command = toPublicDoveCommand(task.currentBoundary?.command ?? route?.command ?? task.nextAction, null);
    const actionableCommand = command === "project:dove.status" ? null : command;
    pushCard(`blocked:${task.id}`, {
      priority: 30,
      kind: "blocked-unblock",
      title: doveText(responseLanguage, "statusHomeBlockedTitle", { title: task.title }),
      why: doveText(responseLanguage, "statusHomeBlockedWhy", { reason: task.blockedReason ?? task.lastStopReason }),
      packetId: task.id,
      command: actionableCommand,
      firstAction: actionableCommand ? route?.copyableCommand ?? statusCopyableCommand(actionableCommand, task.id) : null,
      copyableCommand: actionableCommand ? route?.copyableCommand ?? statusCopyableCommand(actionableCommand, task.id) : null,
      evidenceRequired: normalizeStringArray13(task.evidenceExpectations),
      boundary: task.currentBoundary ?? null,
      handoff: task.handoff ?? null
    });
  }
  if ((review.unresolvedConcernCount ?? 0) > 0) {
    pushCard("review", {
      priority: 40,
      kind: "review-needed",
      title: doveText(responseLanguage, "statusHomeReviewTitle"),
      why: doveText(responseLanguage, "statusHomeReviewWhy"),
      command: "project:dove.review",
      firstAction: "project:dove.review",
      evidenceRequired: normalizeStringArray13(review.unresolvedConcernIds)
    });
  }
  for (const task of activeTasks) {
    if (reconciliationChildIds.has(task.id)) {
      continue;
    }
    const route = primaryStatusRoute(task);
    if (!task.nextAction && !route) {
      continue;
    }
    const command = toPublicDoveCommand(route?.command ?? task.nextAction, "project:dove.auto");
    pushCard(`next:${task.id}`, {
      priority: 50,
      kind: "continue-task",
      title: doveText(responseLanguage, "statusHomeContinueTitle", { title: task.title }),
      why: route?.when ?? task.workContract?.practicalImpact ?? doveText(responseLanguage, "statusHomeContinueWhy"),
      packetId: task.id,
      command,
      firstAction: route?.copyableCommand ?? statusCopyableCommand(command, task.id),
      copyableCommand: route?.copyableCommand ?? statusCopyableCommand(command, task.id),
      evidenceRequired: statusRouteEvidence(task, route),
      doneCriteria: statusRouteDoneCriteria(task, route),
      workContract: task.workContract,
      route,
      boundary: task.currentBoundary ?? null,
      handoff: task.handoff ?? null
    });
  }
  if (initTask && activeTasks.length === 0) {
    pushCard("mission", {
      priority: 60,
      kind: "create-mission",
      title: doveText(responseLanguage, "statusHomeCreateMissionTitle"),
      why: doveText(responseLanguage, "statusHomeCreateMissionWhy"),
      command: "project:dove.mission",
      firstAction: "project:dove.mission",
      evidenceRequired: []
    });
  }
  pushCard("cleanup-archive", buildCleanupArchiveCard(visibleTasks, responseLanguage));
  return rankStatusActionCards(collapseStatusRecoveryCards(cards, responseLanguage));
}
function buildStatusAdjustmentCard(task, recommendedStatus, responseLanguage = "zh") {
  return statusActionBase({
    presentation: "compact-status-adjustment-card",
    packetId: task.id,
    title: task.title,
    currentStatus: task.status,
    recommendedStatus,
    scope: doveText(responseLanguage, "compactCardScope", { stage: task.stage, domain: task.domain, status: task.status }),
    why: task.blockedReason ?? task.lastStopReason ?? doveText(responseLanguage, "compactCardFirstActionFallback"),
    firstAction: "apply_dove_status_adjustments",
    evidenceRequired: normalizeStringArray13(task.evidenceExpectations),
    boundaryOrResume: task.actionableBoundary ?? task.currentBoundary ?? null,
    confirmation: doveText(responseLanguage, "compactCardNoAutomaticExecution")
  });
}
function buildRecentExecutionReceipts(tasks = []) {
  const seen = /* @__PURE__ */ new Set();
  return sortRecentStatusTasks(tasks).map((task) => {
    const receipt = task.lastRun?.executionReceipt ?? null;
    if (!receipt) {
      return null;
    }
    return {
      ...receipt,
      packetId: receipt.packetId ?? task.id,
      title: task.title ?? null,
      completedAt: task.lastRun?.completedAt ?? task.lastRun?.updatedAt ?? null
    };
  }).filter((receipt) => {
    if (!receipt) {
      return false;
    }
    const key = receipt.receiptId ?? receipt.runId ?? `${receipt.packetId}:${receipt.completedAt ?? "unknown"}`;
    if (seen.has(key)) {
      return false;
    }
    seen.add(key);
    return true;
  }).slice(0, 5);
}
function buildDailyHome({ initTask, activeTasks, blockedTasks, visibleTasks, review, boundaryActionCards, completionConsistency, executionGaps, archivedHiddenCount = 0, responseLanguage = "zh" }) {
  const nextActions = buildStatusNextActionCards({ initTask, activeTasks, blockedTasks, visibleTasks, review, boundaryActionCards, completionConsistency, responseLanguage });
  return {
    presentation: "dove-status-home",
    liveContextFirst: true,
    nextActions,
    recentExecutionReceipts: buildRecentExecutionReceipts(visibleTasks),
    missionList: buildStatusMissionList(visibleTasks, { archivedHiddenCount, nextActions, responseLanguage }),
    completionConsistency,
    executionGaps,
    boundaryActionCards,
    suppressUserFacingDumps: ["raw mission counts", "raw status counts", "recent completed missions", "recent killed missions"]
  };
}
function selectDailyHomeNextCommand(statusHome, fallbackNextCommand) {
  const ranked = Array.isArray(statusHome?.nextActions) ? statusHome.nextActions : Array.isArray(statusHome?.nextSteps?.ranked) ? statusHome.nextSteps.ranked : [];
  const firstAction = ranked.find((card) => typeof card?.command === "string" && card.command.trim());
  return firstAction?.command ?? statusHome?.nextStep?.command ?? statusHome?.nextSteps?.suggestedNextCommand ?? fallbackNextCommand;
}
function applicableLessonsForTask(inputs, task) {
  const lessons = Array.isArray(inputs.operatorLessons.lessons) ? inputs.operatorLessons.lessons : [];
  const linkedLessonIds = new Set(normalizeStringArray13(task.lessonIds));
  return lessons.filter((lesson) => {
    const status = String(lesson.status ?? "active").trim().toLowerCase();
    const packetIds = normalizeStringArray13(lesson.packetIds);
    return status === "active" && (packetIds.length === 0 || packetIds.includes(task.id) || linkedLessonIds.has(lesson.id));
  }).slice(0, 10).map((lesson) => ({
    id: lesson.id,
    title: lesson.title,
    mustObey: lesson.mustObey ?? true,
    nextTime: normalizeStringArray13(lesson.nextTime)
  }));
}
function unresolvedDependencyIdsForTask(task, byId) {
  return mergeStringArrays(task.dependencies, task.blockedBy).filter((dependencyId) => {
    const dependency = byId.get(dependencyId);
    return !dependency || dependency.status !== "completed";
  });
}
function enrichStatusTasks(tasks, inputs) {
  const byId = new Map(tasks.map((task) => [task.id, task]));
  return tasks.map((task) => {
    const unresolvedDependencyIds = unresolvedDependencyIdsForTask(task, byId);
    const lastRun = lastRuntimeRunForTask(inputs, task.id);
    const lastEvent = lastRuntimeEventForTask(inputs, task.id);
    const continuationState = continuationForTask(inputs, task.id);
    const currentBoundary = openBoundaryForTask(task);
    const currentBoundaryBlocks = boundaryRecommendsBlocked({ ...task, currentBoundary });
    const blockedReason = task.blockedReason ?? (currentBoundaryBlocks ? currentBoundary?.reason : null) ?? (task.blockedBy.length > 0 ? `blocked-by:${task.blockedBy.join(",")}` : null) ?? (unresolvedDependencyIds.length > 0 ? `unresolved-dependencies:${unresolvedDependencyIds.join(",")}` : null);
    return {
      ...task,
      blockedReason,
      unresolvedDependencyIds,
      currentBoundary,
      actionableBoundary: summarizeActionableBoundary({ ...task, currentBoundary, blockedReason }),
      handoff: normalizeDoveHandoff(task.handoff, null),
      lastRun,
      lastEvent,
      lastStopReason: currentBoundary?.reason ?? lastRun?.stopReason ?? lastEvent?.summary ?? null,
      continuationState,
      applicableLessons: applicableLessonsForTask(inputs, task)
    };
  });
}
function hasTaskBlockerSignal(task) {
  return task.status === "blocked" || boundaryRecommendsBlocked(task) || Boolean(task.blockedReason) || task.blockedBy.length > 0 || normalizeStringArray13(task.unresolvedDependencyIds).length > 0;
}
function runtimeIndicatesInProgress(task) {
  return [task.lastRun?.status, task.lastRun?.outcome, task.continuationState?.kind].some((value) => String(value ?? "").includes("in-progress") || String(value ?? "").includes("continue"));
}
function runtimeIndicatesCompleted(task) {
  return Boolean(task.completedAt) || [task.lastRun?.status, task.lastRun?.outcome].some((value) => ["completed", "task-completed"].includes(String(value ?? "")));
}
function recommendedStatusForTask(task) {
  if (task.status === "archived" || isArchivedStatusTask(task)) {
    return "archived";
  }
  if (runtimeIndicatesCompleted(task)) {
    return "completed";
  }
  if (boundaryRecommendsBlocked(task) || hasTaskBlockerSignal(task)) {
    return "blocked";
  }
  if (runtimeIndicatesInProgress(task)) {
    return "in-progress";
  }
  if (task.status === "pending") {
    return "ready";
  }
  return task.status;
}
function buildStatusAdjustmentContract(tasks, responseLanguage = "zh") {
  const adjustableTasks = sortStatusTasks(tasks.filter((task) => task.level !== 0 && !["completed", "killed", "archived"].includes(task.status) && !isArchivedStatusTask(task)));
  const items = adjustableTasks.map((task, index) => {
    const recommendedStatus = recommendedStatusForTask(task);
    const adjustmentCard = buildStatusAdjustmentCard(task, recommendedStatus, responseLanguage);
    return {
      index: index + 1,
      packetId: task.id,
      title: task.title,
      currentStatus: task.status,
      recommendedStatus,
      level: task.level,
      stage: task.stage,
      domain: task.domain,
      blockedReason: task.blockedReason,
      lastStopReason: task.lastStopReason,
      currentBoundary: task.currentBoundary ?? null,
      actionableBoundary: task.actionableBoundary ?? null,
      handoff: task.handoff ?? null,
      lastEvent: task.lastEvent ?? null,
      adjustmentCard,
      choices: DOVE_TASK_STATUSES,
      confirmArgs: {
        adjustments: [{ packetId: task.id, status: recommendedStatus }],
        confirmed: true
      }
    };
  });
  return {
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    mutationTool: "apply_dove_status_adjustments",
    statusChoices: DOVE_TASK_STATUSES,
    itemCount: adjustableTasks.length,
    adjustmentCards: items.map((item) => item.adjustmentCard),
    items
  };
}
var STATUS_COMPACT_GROUP_LIMITS = {
  doing: 5,
  blocked: 5,
  todo: 5,
  done: 0,
  archived: 0
};
var STATUS_ADJUSTMENT_PREVIEW_LIMIT = 8;
function wantsFullDoveStatus(args = {}) {
  const detail = String(args.detail ?? args.view ?? args.resultMode ?? "").trim().toLowerCase();
  return detail === "full" || detail === "details" || detail === "debug" || booleanArg(args.full) || booleanArg(args.includeDetails);
}
function wantsMissionDetails(args = {}) {
  const detail = String(args.detail ?? args.view ?? args.resultMode ?? "").trim().toLowerCase();
  return booleanArg(args.showMissions) || booleanArg(args.includeMissionDetails) || booleanArg(args.missions) || ["missions", "mission-details", "mission-list"].includes(detail);
}
function wantsStatusAdjustmentPreview(args = {}) {
  const detail = String(args.detail ?? args.view ?? args.resultMode ?? "").trim().toLowerCase();
  return booleanArg(args.requestStatusAdjustment) || booleanArg(args.includeStatusAdjustmentPreview) || booleanArg(args.showStatusAdjustments) || ["status-adjustments", "adjustments", "status-preview"].includes(detail);
}
function compactStatusGroup(group = {}, limit = 0) {
  const items = Array.isArray(group.items) ? group.items : [];
  const shownItems = items.slice(0, limit);
  return {
    ...group,
    items: shownItems,
    itemCount: items.length,
    shownCount: shownItems.length,
    hiddenCount: Math.max(0, items.length - shownItems.length),
    defaultCollapsed: Boolean(group.defaultCollapsed || limit === 0)
  };
}
function compactStatusGroupCounts(groups = {}) {
  return Object.fromEntries(Object.keys(STATUS_COMPACT_GROUP_LIMITS).map((name) => {
    const group = groups[name] ?? {};
    const items = Array.isArray(group.items) ? group.items : [];
    return [name, {
      label: group.label ?? name,
      description: group.description ?? null,
      itemCount: group.itemCount ?? items.length,
      defaultCollapsed: true
    }];
  }));
}
function compactStatusMissionList(missionList = {}, options = {}) {
  const groups = missionList.groups ?? {};
  if (options.includeItems !== true) {
    const groupCounts = compactStatusGroupCounts(groups);
    const hiddenCount2 = Object.values(groupCounts).reduce((total, group) => total + (group.itemCount ?? 0), 0);
    return {
      presentation: missionList.presentation ?? "dove-mission-list",
      detail: "summary",
      summary: missionList.summary ?? {},
      statusModel: missionList.statusModel ?? {},
      defaultCollapsed: true,
      expandWhenAsked: true,
      expanded: false,
      missionItemsIncluded: false,
      groupsOmitted: true,
      groupCounts,
      hiddenItemCount: hiddenCount2,
      detailsAvailable: hiddenCount2 > 0
    };
  }
  const compactGroups = Object.fromEntries(Object.entries(STATUS_COMPACT_GROUP_LIMITS).map(([name, limit]) => [name, compactStatusGroup(groups[name], limit)]));
  const hiddenCount = Object.values(compactGroups).reduce((total, group) => total + (group.hiddenCount ?? 0), 0);
  return {
    ...missionList,
    detail: "compact",
    groups: compactGroups,
    previewLimits: STATUS_COMPACT_GROUP_LIMITS,
    hiddenItemCount: hiddenCount,
    detailsAvailable: hiddenCount > 0,
    expanded: true,
    missionItemsIncluded: true
  };
}
function compactWorkContract(contract) {
  if (!contract) {
    return null;
  }
  return {
    purpose: contract.purpose ?? null,
    deliverables: normalizeStringArray13(contract.deliverables).slice(0, 3),
    evidenceContract: normalizeStringArray13(contract.evidenceContract).slice(0, 3),
    doneCriteria: normalizeStringArray13(contract.doneCriteria).slice(0, 3),
    practicalImpact: contract.practicalImpact ?? null
  };
}
function compactStatusObject(fields) {
  return Object.fromEntries(Object.entries(fields).filter(([, value]) => {
    if (value === null || value === void 0) {
      return false;
    }
    if (Array.isArray(value)) {
      return value.length > 0;
    }
    if (value && typeof value === "object") {
      return Object.keys(value).length > 0;
    }
    return true;
  }));
}
function statusNoWriteTelemetry() {
  return {
    applied: false,
    count: 0,
    writeIntent: "none",
    rollbackEligible: "not-applicable"
  };
}
function buildStatusOperatorUnblock(data, responseLanguage = "zh") {
  return buildOperatorUnblock({ responseLanguage, ...data }, responseLanguage);
}
function publicStatusBoundaryType(value) {
  const type = String(value ?? "").trim();
  if (!type) {
    return null;
  }
  if (["awaiting-audio-review-output"].includes(type)) {
    return "awaiting-review-output";
  }
  if (["missing-secret-env", "provider-failed"].includes(type)) {
    return "awaiting-provider-output";
  }
  if (["awaiting-host-pass", "awaiting-host-results", "needs-host-results", "needs-completion-evidence", "needs-explicit-progress-step"].includes(type)) {
    return "host-tool-blocked";
  }
  if (type.startsWith("audio-review-") || type === "needs-review") {
    return "verification-failed";
  }
  return type;
}
function compactStatusBoundary(boundary, responseLanguage = "zh") {
  if (!boundary || typeof boundary !== "object" || Array.isArray(boundary)) {
    return null;
  }
  const implementationBoundaryType = String(boundary.type ?? boundary.boundaryType ?? "").trim() || null;
  const type = publicStatusBoundaryType(implementationBoundaryType);
  const requiredInputs = normalizeStringArray13(boundary.requiredInputs);
  const requiredActions = normalizeStringArray13(boundary.requiredActions);
  const command = boundary.command ?? boundary.nextAction ?? null;
  const operatorUnblock = buildStatusOperatorUnblock({
    reason: boundary.reason,
    boundaryType: type ?? implementationBoundaryType,
    boundary: {
      type: type ?? implementationBoundaryType,
      reason: boundary.reason,
      requiredInputs,
      requiredActions,
      command
    },
    requiredActions,
    requiredEvidence: normalizeStringArray13(boundary.requiredEvidence),
    nextAction: command
  }, responseLanguage);
  return compactStatusObject({
    id: boundary.id ?? boundary.boundaryId ?? null,
    type,
    status: boundary.status ?? null,
    packetId: boundary.packetId ?? null,
    summary: boundary.summary ?? operatorUnblock?.blockedSummary ?? null,
    requiredInputs,
    command,
    ownerRole: boundary.ownerRole ?? null,
    nextRole: boundary.nextRole ?? null,
    operatorUnblock,
    detail: compactStatusObject({
      implementationBoundaryType: implementationBoundaryType && type && implementationBoundaryType !== type ? implementationBoundaryType : null,
      implementationReason: boundary.reason ?? null,
      requiredActions
    })
  });
}
function compactStatusActionCard(card, responseLanguage = "zh") {
  if (!card || typeof card !== "object") {
    return card;
  }
  const boundary = compactStatusBoundary(card.boundary, responseLanguage);
  const boundaryType = publicStatusBoundaryType(card.boundaryType ?? card.boundary?.type);
  const requiredActions = normalizeStringArray13(card.requiredActions ?? card.boundary?.requiredActions);
  const requires = normalizeStringArray13(card.requires);
  const operatorUnblock = buildStatusOperatorUnblock({
    kind: card.kind,
    reason: card.reason,
    boundaryType,
    boundary: card.boundary,
    requiredActions,
    requiredEvidence: mergeStringArrays(card.evidenceRequired, card.requiredMaterials, requires),
    nextAction: card.copyableCommand ?? card.firstAction ?? card.command
  }, responseLanguage) ?? boundary?.operatorUnblock ?? null;
  return compactStatusObject({
    presentation: card.presentation,
    proposalOnly: card.proposalOnly,
    noAutoApply: card.noAutoApply,
    rank: card.rank,
    priority: card.priority,
    kind: card.kind,
    recoveryPrimaryKind: card.recoveryPrimaryKind,
    relatedActionCount: card.relatedActionCount,
    relatedActionKinds: normalizeStringArray13(card.relatedActionKinds).slice(0, 8),
    title: card.title,
    why: card.why,
    packetId: card.packetId,
    affectedParentIds: normalizeStringArray13(card.affectedParentIds),
    findingCount: card.findingCount,
    openChecklistChildCount: card.openChecklistChildCount,
    openChecklistChildIds: normalizeStringArray13(card.openChecklistChildIds).slice(0, 20),
    command: card.command,
    firstAction: card.firstAction,
    copyableCommand: card.copyableCommand,
    evidenceRequired: normalizeStringArray13(card.evidenceRequired).slice(0, 5),
    doneCriteria: normalizeStringArray13(card.doneCriteria).slice(0, 5),
    workContract: compactWorkContract(card.workContract),
    executionReadiness: card.executionReadiness ? {
      ready: Boolean(card.executionReadiness.ready),
      status: card.executionReadiness.status ?? null,
      missing: normalizeStringArray13(card.executionReadiness.missing).slice(0, 5)
    } : void 0,
    requiredMaterials: normalizeStringArray13(card.requiredMaterials).slice(0, 5),
    criteriaCoverage: card.criteriaCoverage ? {
      complete: Boolean(card.criteriaCoverage.complete),
      missing: normalizeStringArray13(card.criteriaCoverage.missing).slice(0, 5)
    } : void 0,
    boundaryId: card.boundaryId ?? card.boundary?.id ?? null,
    summary: card.summary ?? card.boundary?.summary ?? null,
    requiredInputs: normalizeStringArray13(card.requiredInputs ?? card.boundary?.requiredInputs).slice(0, 5),
    ownerRole: card.ownerRole ?? card.boundary?.ownerRole ?? null,
    nextRole: card.nextRole ?? card.boundary?.nextRole ?? null,
    boundaryType,
    boundary,
    operatorUnblock,
    detail: compactStatusObject({
      requiredActions: requiredActions.slice(0, 5),
      requires: requires.slice(0, 5)
    })
  });
}
function compactStatusAdjustmentItem(item) {
  return {
    index: item.index,
    packetId: item.packetId,
    title: item.title,
    currentStatus: item.currentStatus,
    recommendedStatus: item.recommendedStatus,
    level: item.level,
    stage: item.stage,
    domain: item.domain,
    blockedReason: item.blockedReason ?? null,
    lastStopReason: item.lastStopReason ?? null,
    boundaryType: item.currentBoundary?.type ?? item.actionableBoundary?.type ?? null,
    choices: item.choices,
    confirmArgs: item.confirmArgs
  };
}
function compactStatusAdjustmentContract(contract = {}, options = {}) {
  const items = Array.isArray(contract.items) ? contract.items : [];
  if (options.includeItems !== true) {
    return {
      proposalOnly: contract.proposalOnly,
      noAutoApply: contract.noAutoApply,
      writes: Array.isArray(contract.writes) ? contract.writes : [],
      mutationTool: contract.mutationTool,
      statusChoices: contract.statusChoices,
      itemCount: contract.itemCount ?? items.length,
      previewItemCount: 0,
      hiddenItemCount: items.length,
      adjustmentCards: [],
      items: [],
      defaultCollapsed: true,
      expandWhenAsked: true,
      confirmationRequiresExplicitRequest: true,
      statusAdjustmentItemsIncluded: false,
      detailsAvailable: items.length > 0
    };
  }
  const shownItems = items.slice(0, STATUS_ADJUSTMENT_PREVIEW_LIMIT).map(compactStatusAdjustmentItem);
  const adjustmentCards = Array.isArray(contract.adjustmentCards) ? contract.adjustmentCards.slice(0, STATUS_ADJUSTMENT_PREVIEW_LIMIT).map((card) => compactStatusActionCard(card, contract.responseLanguage ?? "zh")) : [];
  return {
    proposalOnly: contract.proposalOnly,
    noAutoApply: contract.noAutoApply,
    writes: Array.isArray(contract.writes) ? contract.writes : [],
    mutationTool: contract.mutationTool,
    statusChoices: contract.statusChoices,
    itemCount: contract.itemCount ?? items.length,
    previewItemCount: shownItems.length,
    hiddenItemCount: Math.max(0, items.length - shownItems.length),
    adjustmentCards,
    items: shownItems,
    defaultCollapsed: true,
    expandWhenAsked: true,
    confirmationRequiresExplicitRequest: true,
    statusAdjustmentItemsIncluded: true,
    detailsAvailable: items.length > shownItems.length
  };
}
function buildHostFileCheckpointNotice() {
  return {
    required: true,
    detected: false,
    kind: "host-file-checkpoint",
    status: "not-programmatically-verifiable",
    scope: "host-runtime",
    verificationRequired: true,
    projectFilesRequired: true,
    externalWriteCaptureRequired: true,
    externalWriteCaptureVerified: false
  };
}
function buildMutationRollbackModel(root) {
  const indexPath = resolveProjectPath(root, ARTIFACT_PATHS.mutationsIndex);
  let index = createMutationProvenanceIndex();
  if (fs14.existsSync(indexPath)) {
    try {
      index = normalizeMutationProvenanceIndex(JSON.parse(fs14.readFileSync(indexPath, "utf8")));
    } catch {
      index = createMutationProvenanceIndex();
    }
  }
  const summary = index.summary ?? createMutationProvenanceIndex().summary;
  return {
    patchPlanSupported: true,
    hostTrackedFileEditsRequired: true,
    directProcessWritesAreRollbackSafe: false,
    lastMutationMode: summary.lastMutationMode ?? null,
    lastAppliedBy: summary.lastAppliedBy ?? null,
    hostRollbackEligible: Boolean(summary.hostRollbackEligible),
    hostRollbackIneligibleReason: summary.hostRollbackIneligibleReason ?? null,
    recommendedMutationMode: summary.recommendedMutationMode ?? null,
    rollbackAdvice: summary.rollbackAdvice ?? null,
    hostCheckpointVerified: false,
    externalWriteCaptureVerified: false,
    doveRestoreSupported: false,
    mutationProvenancePath: ARTIFACT_PATHS.mutationsIndex,
    mutationCount: summary.mutationCount ?? 0,
    patchPlanCount: summary.patchPlanCount ?? 0,
    directProcessCount: summary.directProcessCount ?? 0
  };
}
function buildDurableContextNotice(root, responseLanguage = "zh") {
  const hostCheckpoint = buildHostFileCheckpointNotice(root);
  const mutationRollbackModel = buildMutationRollbackModel(root);
  const recoveryActions = [
    { kind: "refresh-status", command: "project:dove.status", mutation: false },
    { kind: "apply-mutation-plan-with-host-tracked-edits", command: "mutationMode: patch-plan", mutation: true, handledByHost: true, hostTrackedFileEditsRequired: true },
    { kind: "use-direct-process-as-unverified", command: "mutationMode: direct-process", mutation: true, hostRollbackEligible: false, hostRollbackIneligibleReason: mutationRollbackModel.hostRollbackIneligibleReason, rollbackAdvice: mutationRollbackModel.rollbackAdvice },
    { kind: "adjust-status", command: "apply_dove_status_adjustments", mutation: true, confirmationRequired: true }
  ];
  return {
    presentation: "dove-durable-context-notice",
    stateSource: "filesystem-durable-state",
    durableRoot: ARTIFACT_PATHS.doveRoot,
    rollbackCoverage: "host-tracked-mutation-plan-required",
    nativeHostRollbackRequiresFileCheckpoint: true,
    hostCheckpointDetected: hostCheckpoint.detected,
    hostCheckpointStatus: hostCheckpoint.status,
    hostCheckpoint,
    mutationRollbackModel,
    projectVisibilityRequired: true,
    projectVisibilityVerified: false,
    externalWriteCaptureRequired: true,
    externalWriteCaptureVerified: false,
    doveRestoreSupported: false,
    doveRestoreCommand: null,
    automaticRollback: false,
    trackedDurablePaths: [ARTIFACT_PATHS.state, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.mutationsIndex],
    localOnlyIgnoredPaths: [".dove/config.local.json"],
    summary: doveText(responseLanguage, "durableContextNoticeSummary"),
    recovery: doveText(responseLanguage, "durableContextNoticeRecovery"),
    recoveryActions
  };
}
function compactStatusBoundaryFromAction(action, responseLanguage = "zh") {
  if (!action) {
    return null;
  }
  return compactStatusBoundary(action.boundary, responseLanguage) ?? compactStatusBoundary({
    id: action.boundaryId,
    boundaryType: action.boundaryType,
    packetId: action.packetId,
    summary: action.summary,
    requiredInputs: action.requiredInputs,
    requiredActions: action.requiredActions,
    command: action.command,
    ownerRole: action.ownerRole,
    nextRole: action.nextRole
  }, responseLanguage);
}
function compactStatusRequiredEvidence({ primaryStep, boundary, boundaryActionCards, executionGaps }) {
  return mergeStringArrays(
    primaryStep?.evidenceRequired,
    primaryStep?.requiredMaterials,
    primaryStep?.requires,
    primaryStep?.doneCriteria,
    boundary?.requiredInputs,
    boundary?.requiredActions,
    boundaryActionCards.flatMap((card) => mergeStringArrays(card.evidenceRequired, card.requiredMaterials, card.requires)),
    executionGaps.evidenceRequired,
    executionGaps.requiredMaterials
  ).slice(0, 12);
}
function buildStatusIntentPrimaryAction(statusIntent, { gapStatus, executionCounts, boundaryActionCards, responseLanguage = "zh" }) {
  if (statusIntent === "project-status") {
    return null;
  }
  const contractTest = statusIntent === "contract-test";
  return compactStatusObject({
    presentation: "dove-status-intent-card",
    kind: statusIntent,
    title: statusInlineText(responseLanguage, contractTest ? "Dove contract \u68C0\u67E5" : "Dove \u5065\u5EB7\u68C0\u67E5", contractTest ? "Dove contract check" : "Dove health check"),
    why: statusInlineText(responseLanguage, contractTest ? "\u5F53\u524D\u8BF7\u6C42\u662F\u5728\u68C0\u67E5 Dove compact contract \u548C\u5DE5\u5177\u9762\uFF0C\u4E0D\u628A\u9879\u76EE backlog \u5F53\u4F5C\u4E3B\u4E0B\u4E00\u6B65\u3002" : "\u5F53\u524D\u8BF7\u6C42\u662F\u5728\u68C0\u67E5 Dove \u5F53\u524D\u5065\u5EB7\u72B6\u6001\uFF0C\u4E0D\u628A\u9879\u76EE backlog \u5F53\u4F5C\u4E3B\u4E0B\u4E00\u6B65\u3002", contractTest ? "This request checks the Dove compact contract and tool surface, so project backlog is not the primary next step." : "This request checks Dove health, so project backlog is not the primary next step."),
    command: "query_dove_status",
    firstAction: `query_dove_status intent: ${statusIntent}`,
    evidenceRequired: [],
    healthSummary: {
      gapStatus,
      boundaryCount: boundaryActionCards.length,
      blockingExecutionGapCount: executionCounts.blocking ?? 0
    }
  });
}
function buildHumanNextStep(card, responseLanguage = "zh") {
  if (!card) {
    return compactStatusObject({
      label: statusInlineText(responseLanguage, "\u73B0\u5728\u6CA1\u6709\u5FC5\u505A\u52A8\u4F5C", "No required action right now"),
      why: statusInlineText(responseLanguage, "Dove \u6CA1\u6709\u53D1\u73B0\u9700\u8981\u7ACB\u523B\u5904\u7406\u7684\u963B\u585E\u3002", "Dove did not find an immediate blocker.")
    });
  }
  const command = statusPublicCommandText(statusCardPublicCommand(card));
  const copyableCommand = statusPublicCommandText(statusCardCopyableCommand(card));
  const label = statusFirstPublicText(
    responseLanguage,
    90,
    card.title,
    card.label,
    card.operatorUnblock?.operatorAction,
    card.operatorUnblock?.nextOperatorAction,
    card.kind
  ) || statusInlineText(responseLanguage, "\u5904\u7406\u5F53\u524D\u4E0B\u4E00\u6B65", "Handle the current next step");
  const why = statusFirstPublicText(
    responseLanguage,
    140,
    card.operatorUnblock?.blockedSummary,
    card.operatorUnblock?.cannotContinueBecause,
    card.why,
    card.operatorUnblock?.summary
  );
  return compactStatusObject({
    label,
    why,
    command,
    copyableCommand
  });
}
function buildHumanNeedsAttention({ gapStatus, primaryStep, boundary, blockers, readErrors, executionCounts, projectBacklogRequiredEvidence, responseLanguage = "zh" }) {
  const operatorUnblock = primaryStep?.operatorUnblock ?? boundary?.operatorUnblock ?? null;
  const needs = mergeStringArrays(
    primaryStep?.evidenceRequired,
    operatorUnblock?.needs,
    operatorUnblock?.requiredEvidence,
    boundary?.requiredInputs,
    projectBacklogRequiredEvidence
  ).map((item) => statusPublicText(item, responseLanguage, 60)).filter(Boolean).slice(0, 8);
  if (gapStatus !== "blocked") {
    return compactStatusObject({
      status: "clear",
      summary: statusInlineText(responseLanguage, "\u5F53\u524D\u6CA1\u6709\u660E\u663E\u963B\u585E\u3002", "No obvious blocker right now."),
      needs
    });
  }
  const summary = statusFirstPublicText(
    responseLanguage,
    140,
    operatorUnblock?.summary,
    operatorUnblock?.blockedSummary,
    boundary?.summary,
    readErrors.length > 0 ? statusInlineText(responseLanguage, "Dove \u8BFB\u72B6\u6001\u65F6\u9047\u5230\u6587\u4EF6\u95EE\u9898\u3002", "Dove hit a file read problem while checking status.") : null,
    (executionCounts.blocking ?? 0) > 0 ? statusInlineText(responseLanguage, "Dove \u9700\u8981\u5148\u8865\u9F50\u4E00\u4E2A\u6267\u884C\u524D\u63D0\u3002", "Dove needs one execution prerequisite before continuing.") : null,
    blockers.length > 0 ? statusInlineText(responseLanguage, "Dove \u53D1\u73B0\u5F53\u524D\u5DE5\u4F5C\u6709\u963B\u585E\u3002", "Dove found a blocker in the current work.") : null,
    statusInlineText(responseLanguage, "Dove \u9700\u8981\u4F60\u5148\u5904\u7406\u4E00\u4E2A\u963B\u585E\u3002", "Dove needs one blocker handled first.")
  );
  const why = statusFirstPublicText(
    responseLanguage,
    160,
    operatorUnblock?.why,
    operatorUnblock?.cannotContinueBecause,
    primaryStep?.why
  );
  return compactStatusObject({
    status: "blocked",
    summary,
    why,
    needs
  });
}
function buildHumanChanges(writes = statusNoWriteTelemetry()) {
  const writeIntent = writes.writeIntent ?? (writes.applied ? "applied" : "none");
  return compactStatusObject({
    intent: writeIntent,
    applied: writes.applied === true,
    count: writes.count ?? 0,
    rollback: writes.rollbackEligible ?? (writeIntent === "none" ? "not-applicable" : "unverified")
  });
}
function buildHumanShowMore({ missionList, statusAdjustmentContract, writes, responseLanguage = "zh" }) {
  return compactStatusObject({
    text: statusInlineText(responseLanguage, "\u5982\u679C\u4F60\u660E\u786E\u8981\u5C55\u5F00\uFF0C\u53EF\u4EE5\u518D\u67E5\u770B\u4EFB\u52A1\u7EC6\u8282\u6216\u5B8C\u6574\u6CBB\u7406\u72B6\u6001\u3002", "If you explicitly want more detail, ask for task details or the full governance state."),
    noWriteSummary: writes.applied === true ? statusInlineText(responseLanguage, "\u8FD9\u6B21\u6709\u72B6\u6001\u53D8\u66F4\uFF1B\u9700\u8981\u5B8C\u6574\u8BB0\u5F55\u65F6\u518D\u5C55\u5F00\u5BA1\u8BA1\u7EC6\u8282\u3002", "This check included state changes; ask for audit details if needed.") : statusInlineText(responseLanguage, "\u8FD9\u6B21\u53EA\u662F\u8BFB\u53D6\u72B6\u6001\uFF0C\u6CA1\u6709\u5199\u5165\u3002", "This check only read status and made no writes."),
    detailsAvailable: true,
    missionDetailsAvailable: Boolean(missionList.detailsAvailable ?? missionList.itemCount ?? missionList.missionItemsIncluded),
    statusAdjustmentsAvailable: Boolean(statusAdjustmentContract.detailsAvailable ?? statusAdjustmentContract.itemCount)
  });
}
function buildHumanStatusHeadline({ statusIntent, gapStatus, nextStep, needsAttention, responseLanguage = "zh" }) {
  if (statusIntent === "health-check") {
    return statusInlineText(responseLanguage, "Dove \u5065\u5EB7\u68C0\u67E5\u5B8C\u6210\uFF1B\u8FD9\u6B21\u53EA\u662F\u68C0\u67E5\u7CFB\u7EDF\u72B6\u6001\uFF0C\u6CA1\u6709\u5199\u5165\u3002", "Dove health check completed; this only inspected system state and made no writes.");
  }
  if (statusIntent === "contract-test") {
    return statusInlineText(responseLanguage, "Dove compact contract \u68C0\u67E5\u5B8C\u6210\uFF1B\u9ED8\u8BA4\u5DE5\u5177\u9762\u548C full/debug \u5C55\u5F00\u53EF\u7528\u3002", "Dove compact contract check completed; default tool surface and full/debug expansion are available.");
  }
  if (gapStatus === "blocked") {
    const summary = needsAttention?.summary ?? statusInlineText(responseLanguage, "Dove \u6B63\u5728\u7B49\u5F85\u4F60\u5148\u5904\u7406\u4E00\u4E2A\u963B\u585E\u3002", "Dove is waiting for one blocker to be handled first.");
    return /Dove/i.test(summary) ? summary : statusInlineText(responseLanguage, `Dove ${summary}`, `Dove: ${summary}`);
  }
  if (nextStep?.label) {
    return statusInlineText(responseLanguage, `Dove \u53EF\u4EE5\u7EE7\u7EED\uFF1B\u4E0B\u4E00\u6B65\u662F\uFF1A${nextStep.label}`, `Dove can continue; next step: ${nextStep.label}`);
  }
  return statusInlineText(responseLanguage, "Dove \u5F53\u524D\u6CA1\u6709\u660E\u663E\u963B\u585E\u3002", "Dove has no obvious blocker right now.");
}
function buildCompactStatusHome({ result, missionList, nextActions, boundaryActionCards, statusAdjustmentContract, args }) {
  const summary = result.projectSummary ?? {};
  const current = result.current ?? {};
  const dashboard = result.dashboard ?? {};
  const responseLanguage = result.responseLanguage ?? "zh";
  const statusIntent = normalizeStatusIntent(args.intent ?? result.intent);
  const readErrors = Array.isArray(result.diagnostics?.readErrors) ? result.diagnostics.readErrors : [];
  const blockers = Array.isArray(dashboard.blockers) ? dashboard.blockers : [];
  const consistency = result.dailyHome?.completionConsistency ?? {};
  const completionConsistency = {
    status: consistency.status ?? "consistent",
    findingCount: consistency.findingCount ?? (Array.isArray(consistency.findings) ? consistency.findings.length : 0)
  };
  const executionGaps = result.dailyHome?.executionGaps ?? {};
  const executionCounts = {
    missingContract: executionGaps.counts?.missingContract ?? 0,
    missingMaterials: executionGaps.counts?.missingMaterials ?? 0,
    verificationGaps: executionGaps.counts?.verificationGaps ?? 0,
    readyBuilder: executionGaps.counts?.readyBuilder ?? 0,
    blocking: executionGaps.counts?.blocking ?? 0
  };
  const gapStatus = readErrors.length > 0 || blockers.length > 0 || completionConsistency.status === "needs-reconciliation" || executionCounts.blocking > 0 || boundaryActionCards.length > 0 ? "blocked" : "clear";
  const projectBacklogNextAction = nextActions[0] ?? null;
  const intentPrimaryAction = buildStatusIntentPrimaryAction(statusIntent, { gapStatus, executionCounts, boundaryActionCards, responseLanguage });
  const primaryStep = statusIntent === "project-status" ? projectBacklogNextAction : intentPrimaryAction;
  const boundarySourceAction = projectBacklogNextAction ?? boundaryActionCards[0] ?? null;
  const boundary = compactStatusBoundaryFromAction(boundarySourceAction, responseLanguage) ?? compactStatusBoundaryFromAction(boundaryActionCards[0], responseLanguage);
  const projectBacklogRequiredEvidence = statusIntent === "project-status" ? [] : compactStatusRequiredEvidence({ primaryStep: projectBacklogNextAction, boundary, boundaryActionCards, executionGaps });
  const writes = statusNoWriteTelemetry();
  const nextStep = buildHumanNextStep(primaryStep, responseLanguage);
  const needsAttention = buildHumanNeedsAttention({
    gapStatus,
    primaryStep,
    boundary,
    blockers,
    readErrors,
    executionCounts,
    projectBacklogRequiredEvidence,
    responseLanguage
  });
  const changes = buildHumanChanges(writes);
  const showMore = buildHumanShowMore({ missionList, statusAdjustmentContract, writes, responseLanguage });
  const headline = buildHumanStatusHeadline({ statusIntent, gapStatus, nextStep, needsAttention, responseLanguage });
  return compactStatusObject({
    presentation: "dove-project-situation-home",
    detail: "compact",
    liveContextFirst: true,
    intent: statusIntent,
    headline,
    scope: compactStatusObject({
      kind: "workspace",
      domain: current.domain ?? null,
      stage: current.stage ?? null,
      primaryRole: current.primaryRole ?? null
    }),
    currentContext: compactStatusObject({
      title: summary.title ?? null,
      objective: summary.objective ?? null,
      currentFocus: summary.currentFocus ?? null,
      domain: current.domain ?? null,
      stage: current.stage ?? null,
      primaryRole: current.primaryRole ?? null
    }),
    nextStep,
    needsAttention,
    changes,
    showMore,
    optionalMissionDetails: missionList.missionItemsIncluded ? missionList : null,
    statusAdjustmentPreview: statusAdjustmentContract.statusAdjustmentItemsIncluded ? statusAdjustmentContract : null,
    detailsAvailable: true
  });
}
function compactDoveStatusResult(result, args = {}) {
  const includeMissionDetails = wantsMissionDetails(args);
  const includeStatusAdjustmentPreview = wantsStatusAdjustmentPreview(args);
  const responseLanguage = result.responseLanguage ?? "zh";
  const missionList = compactStatusMissionList(result.dailyHome?.missionList, { includeItems: includeMissionDetails });
  const nextActions = (Array.isArray(result.dailyHome?.nextActions) ? result.dailyHome.nextActions : []).slice(0, 3).map((card) => compactStatusActionCard(card, responseLanguage));
  const boundaryActionCards = (Array.isArray(result.dailyHome?.boundaryActionCards) ? result.dailyHome.boundaryActionCards : []).slice(0, 5).map((card) => compactStatusActionCard(card, responseLanguage));
  const statusAdjustmentContract = compactStatusAdjustmentContract(result.statusAdjustmentContract, { includeItems: includeStatusAdjustmentPreview });
  const statusHome = buildCompactStatusHome({
    result,
    missionList,
    nextActions,
    boundaryActionCards,
    statusAdjustmentContract,
    args
  });
  return compactStatusObject({
    mode: result.mode,
    query: result.query,
    proposalOnly: result.proposalOnly,
    noAutoApply: result.noAutoApply,
    intent: statusHome.intent,
    responseLanguage: result.responseLanguage,
    detail: "compact",
    detailsAvailable: true,
    summary: statusHome.headline,
    headline: statusHome.headline,
    scope: statusHome.scope,
    currentContext: statusHome.currentContext,
    nextStep: statusHome.nextStep,
    needsAttention: statusHome.needsAttention,
    changes: statusHome.changes,
    showMore: statusHome.showMore,
    optionalMissionDetails: statusHome.optionalMissionDetails,
    statusAdjustmentPreview: statusHome.statusAdjustmentPreview,
    statusHome
  });
}
function missionUserGroupForStatus(status) {
  if (status === "archived") {
    return "archived";
  }
  if (status === "blocked") {
    return "blocked";
  }
  if (status === "in-progress") {
    return "doing";
  }
  if (status === "completed" || status === "killed") {
    return "done";
  }
  return "todo";
}
function summarizeStatusMissionListItem(task, index) {
  const recommendedStatus = recommendedStatusForTask(task);
  const group = missionUserGroupForStatus(recommendedStatus);
  return {
    index,
    packetId: task.id,
    title: task.title,
    group,
    status: task.status,
    recommendedStatus,
    stage: task.stage,
    domain: task.domain,
    level: task.level,
    blockedReason: task.blockedReason ?? null,
    lastStopReason: task.lastStopReason ?? null,
    boundaryType: task.currentBoundary?.type ?? null,
    evidenceRequired: normalizeStringArray13(task.evidenceExpectations),
    artifactRefs: normalizeStringArray13(task.artifactRefs)
  };
}
function missionPriorityNeeds(task, item, action) {
  return mergeStringArrays(
    action?.requiredMaterials,
    action?.evidenceRequired,
    action?.requiredInputs,
    action?.requires,
    action?.operatorUnblock?.needs,
    action?.operatorUnblock?.requiredEvidence,
    item?.evidenceRequired,
    task?.evidenceExpectations,
    task?.currentBoundary?.requiredInputs,
    task?.currentBoundary?.requiredActions,
    task?.workContract?.evidenceContract,
    task?.workContract?.deliverables
  ).map((value) => statusShortText(value, 80)).filter(Boolean).slice(0, 6);
}
function missionPriorityDoneCriteria(task, action) {
  return mergeStringArrays(action?.doneCriteria, task?.workContract?.doneCriteria).map((value) => statusShortText(value, 90)).filter(Boolean).slice(0, 4);
}
function taskDependsOnPacket(task, packetId) {
  if (!packetId || task?.id === packetId) {
    return false;
  }
  const dependencyIds = mergeStringArrays(task?.dependencies, task?.blockedBy, task?.unresolvedDependencyIds);
  if (dependencyIds.includes(packetId)) {
    return true;
  }
  return typeof task?.blockedReason === "string" && task.blockedReason.includes(packetId);
}
function missionPriorityUnlocks(tasks, packetId) {
  return sortStatusTasks(tasks.filter((task) => {
    if (!taskDependsOnPacket(task, packetId)) {
      return false;
    }
    const recommendedStatus = recommendedStatusForTask(task);
    return !["completed", "killed", "archived"].includes(recommendedStatus);
  })).slice(0, 5).map((task) => ({
    packetId: task.id,
    title: task.title,
    status: task.status,
    recommendedStatus: recommendedStatusForTask(task),
    blockedReason: task.blockedReason ?? null
  }));
}
function primaryMissionListAction(nextActions = [], itemById) {
  return (Array.isArray(nextActions) ? nextActions : []).find((action) => action?.packetId && itemById.has(action.packetId)) ?? null;
}
function fallbackMissionPriorityItem(items = []) {
  return items.find((item) => item.group === "doing") ?? items.find((item) => item.group === "blocked") ?? items.find((item) => item.group === "todo") ?? null;
}
function buildMissionPriorityLane(tasks, items, options = {}) {
  const itemById = new Map(items.map((item) => [item.packetId, item]));
  const taskById = new Map(tasks.map((task) => [task.id, task]));
  const primaryAction = primaryMissionListAction(options.nextActions, itemById);
  const focusItem = primaryAction?.packetId ? itemById.get(primaryAction.packetId) : fallbackMissionPriorityItem(items);
  if (!focusItem) {
    return null;
  }
  const focusTask = taskById.get(focusItem.packetId);
  const command = primaryAction ? statusCardPublicCommand(primaryAction) : null;
  const copyableCommand = primaryAction ? statusCardCopyableCommand(primaryAction) : null;
  const needs = missionPriorityNeeds(focusTask, focusItem, primaryAction);
  const unlocks = missionPriorityUnlocks(tasks, focusItem.packetId);
  const responseLanguage = options.responseLanguage ?? "zh";
  const summary = responseLanguage === "en" ? `Handle ${focusItem.packetId} first; it is the current bottleneck before the grouped mission list.` : `\u5148\u5904\u7406 ${focusItem.packetId}\uFF1B\u8FD9\u662F\u5C55\u5F00\u4EFB\u52A1\u5217\u8868\u524D\u7684\u5F53\u524D\u74F6\u9888\u3002`;
  return compactStatusObject({
    presentation: "dove-mission-priority-lane",
    summary,
    focus: compactStatusObject({
      packetId: focusItem.packetId,
      title: focusItem.title,
      status: focusItem.status,
      recommendedStatus: focusItem.recommendedStatus,
      group: focusItem.group,
      stage: focusItem.stage,
      domain: focusItem.domain
    }),
    action: compactStatusObject({
      label: primaryAction?.title ?? primaryAction?.label ?? null,
      why: primaryAction?.why ?? primaryAction?.operatorUnblock?.summary ?? primaryAction?.operatorUnblock?.operatorAction ?? null,
      command,
      copyableCommand,
      kind: primaryAction?.kind ?? null
    }),
    needs,
    doneCriteria: missionPriorityDoneCriteria(focusTask, primaryAction),
    unlocks,
    relatedActionCount: primaryAction?.relatedActionCount ?? null
  });
}
function openChecklistChildForStatusParent(task, parentId) {
  return task.parentId === parentId && task.creatorKind === "system" && !task.derivedFrom && !task.sourcePlanTaskId && !isArchivedStatusTask(task) && !["completed", "killed"].includes(task.status);
}
function buildCompletionConsistency(tasks = [], responseLanguage = "zh") {
  const byParentId = /* @__PURE__ */ new Map();
  for (const task of tasks) {
    if (!task.parentId || !openChecklistChildForStatusParent(task, task.parentId)) {
      continue;
    }
    byParentId.set(task.parentId, [...byParentId.get(task.parentId) ?? [], task]);
  }
  const findings = tasks.filter((task) => task.status === "completed" && byParentId.has(task.id)).map((parent) => {
    const openChildren = sortStatusTasks(byParentId.get(parent.id) ?? []);
    return {
      id: `completion-consistency-${parent.id}`,
      type: "completed-parent-open-checklist",
      severity: "blocking",
      parentId: parent.id,
      parentTitle: parent.title,
      parentDisplayStatus: "done",
      parentMachineStatus: parent.status,
      openChecklistChildIds: openChildren.map((child) => child.id),
      openChecklistChildren: openChildren.map((child) => ({ id: child.id, title: child.title, status: child.status, displayStatus: child.displayStatus ?? child.status })),
      nextAction: "project:dove.status",
      summary: responseLanguage === "en" ? "A done parent mission still has open checklist children." : "done \u7236 mission \u4E0B\u4ECD\u6709 open checklist \u5B50\u9879\u3002"
    };
  });
  return {
    status: findings.length > 0 ? "needs-reconciliation" : "consistent",
    findingCount: findings.length,
    findings
  };
}
function buildStatusMissionList(tasks, options = {}) {
  const items = sortStatusTasks(tasks.filter((task) => task.level !== 0)).map((task, index) => summarizeStatusMissionListItem(task, index + 1));
  const groups = {
    todo: { label: "todo", description: "pending or ready missions", defaultCollapsed: false, items: [] },
    doing: { label: "doing", description: "missions with in-progress foreground runtime state", defaultCollapsed: false, items: [] },
    blocked: { label: "blocked", description: "missions blocked by dependency, boundary, or missing evidence", defaultCollapsed: false, items: [] },
    done: { label: "done", description: "completed or killed missions", defaultCollapsed: true, items: [] },
    archived: { label: "archived", description: "archived missions hidden from the default project situation", defaultCollapsed: true, items: [] }
  };
  for (const item of items) {
    groups[item.group].items.push(item);
  }
  const priorityLane = buildMissionPriorityLane(tasks, items, options);
  return {
    presentation: "dove-mission-list",
    priorityLane,
    statusModel: {
      userGroups: ["todo", "doing", "blocked", "done", "archived"],
      machineStatuses: DOVE_TASK_STATUSES,
      mapping: {
        todo: ["pending", "ready"],
        doing: ["in-progress"],
        blocked: ["blocked"],
        done: ["completed", "killed"],
        archived: ["archived"]
      }
    },
    summary: {
      totalCount: items.length,
      openCount: groups.todo.items.length + groups.doing.items.length + groups.blocked.items.length,
      todoCount: groups.todo.items.length,
      doingCount: groups.doing.items.length,
      blockedCount: groups.blocked.items.length,
      doneCount: groups.done.items.length,
      archivedCount: groups.archived.items.length,
      archivedHiddenCount: Number(options.archivedHiddenCount ?? 0)
    },
    groups
  };
}
function buildProjectSummary({ title, objective, focus, initTask, tasks, activeTasks, blockedTasks, missionList, review, versions, experiments, blockers, nextCommand, returnStatus, archivedHiddenCount = 0 }) {
  return {
    title,
    objective,
    currentFocus: focus,
    initTaskId: initTask?.id ?? null,
    missionCount: tasks.filter((task) => task.level !== 0).length,
    openMissionCount: missionList?.summary?.openCount ?? activeTasks.length,
    todoMissionCount: missionList?.summary?.todoCount ?? 0,
    doingMissionCount: missionList?.summary?.doingCount ?? 0,
    activeMissionCount: activeTasks.length,
    blockedMissionCount: missionList?.summary?.blockedCount ?? blockedTasks.length,
    archivedMissionCount: missionList?.summary?.archivedCount ?? 0,
    archivedHiddenCount,
    statusCounts: countBy2(tasks.map((task) => task.status), DOVE_TASK_STATUSES),
    reviewVerdict: review.verdict,
    unresolvedConcernCount: review.unresolvedConcernCount,
    versionCount: versions.versionCount,
    experimentPlanCount: experiments.planCount,
    blockerCount: blockers.length,
    returnStatus,
    nextCommand
  };
}
function buildStatusTaskTree(tasks) {
  const byId = new Map(tasks.map((task) => [task.id, { ...task, children: [] }]));
  const roots = [];
  for (const task of byId.values()) {
    if (task.parentId && task.parentId !== task.id && byId.has(task.parentId)) {
      byId.get(task.parentId).children.push(task);
    } else {
      roots.push(task);
    }
  }
  const normalizeNode = (node) => ({ ...node, children: sortStatusTasks(node.children).map(normalizeNode) });
  return sortStatusTasks(roots).map(normalizeNode);
}
function summarizeStatusBlocker(blocker, index, responseLanguage = "zh") {
  if (blocker && typeof blocker === "object" && !Array.isArray(blocker)) {
    return {
      id: blocker.id ?? blocker.blockerId ?? `board-blocker-${index + 1}`,
      source: "board",
      summary: blocker.summary ?? blocker.reason ?? blocker.title ?? blocker.id ?? doveText(responseLanguage, "boardBlockerFallback", { index: index + 1 }),
      severity: blocker.severity ?? null,
      taskId: blocker.taskId ?? blocker.packetId ?? null
    };
  }
  return {
    id: `board-blocker-${index + 1}`,
    source: "board",
    summary: String(blocker ?? doveText(responseLanguage, "boardBlockerFallback", { index: index + 1 })),
    severity: null,
    taskId: null
  };
}
function buildStatusBlockers(inputs, blockedTasks, responseLanguage = "zh") {
  const boardBlockers = Array.isArray(inputs.board.blockers) ? inputs.board.blockers.map((blocker, index) => summarizeStatusBlocker(blocker, index, responseLanguage)) : [];
  const taskBlockers = blockedTasks.map((task) => ({
    id: `task-blocker-${task.id}`,
    source: "task",
    summary: task.blockedReason ?? (task.blockedBy.length > 0 ? doveText(responseLanguage, "taskBlockedBySummary", { id: task.id, blockers: task.blockedBy.join(", ") }) : doveText(responseLanguage, "taskMarkedBlockedSummary", { id: task.id })),
    severity: "blocking",
    taskId: task.id,
    blockedBy: task.blockedBy,
    unresolvedDependencyIds: normalizeStringArray13(task.unresolvedDependencyIds)
  }));
  return [...boardBlockers, ...taskBlockers];
}
function selectStatusNextCommand({ initTask, activeTasks, blockedTasks, review }) {
  if (!initTask) {
    return "project:dove.init";
  }
  if (blockedTasks.length > 0) {
    return "project:dove.status";
  }
  const nextActiveTask = activeTasks.find((task) => task.nextAction);
  if (nextActiveTask) {
    return nextActiveTask.nextAction;
  }
  if (activeTasks.length > 0) {
    return "project:dove.auto";
  }
  if ((review.unresolvedConcernCount ?? 0) > 0) {
    return "project:dove.review";
  }
  return initTask.nextAction ?? "project:dove.mission";
}
function summarizeStatusReview(inputs) {
  const concerns = Array.isArray(inputs.reviewConcerns.items) ? inputs.reviewConcerns.items : [];
  const openConcerns = concerns.filter((concern) => !["closed", "resolved", "accepted"].includes(String(concern.status ?? "open").trim().toLowerCase()));
  const unresolvedConcernIds = normalizeStringArray13(inputs.reviewState.unresolvedConcernIds);
  return {
    verdict: inputs.reviewState.lastVerdict ?? "not-reviewed",
    reviewedAt: inputs.reviewState.lastReviewedAt ?? null,
    reviewRound: inputs.reviewState.reviewRound ?? 0,
    openItemCount: Array.isArray(inputs.reviewState.openItems) ? inputs.reviewState.openItems.length : 0,
    concernCount: concerns.length,
    openConcernCount: openConcerns.length,
    unresolvedConcernCount: unresolvedConcernIds.length || openConcerns.length,
    unresolvedConcernIds,
    reviewerIndependence: inputs.reviewState.reviewerIndependence ?? null
  };
}
function summarizeStatusLessons(inputs) {
  const lessons = Array.isArray(inputs.operatorLessons.lessons) ? inputs.operatorLessons.lessons : [];
  const activeLessons2 = lessons.filter((lesson) => String(lesson.status ?? "active").trim().toLowerCase() === "active");
  const mustObeyLessons = activeLessons2.filter((lesson) => lesson.mustObey !== false);
  const summary = inputs.operatorLessons.summary && typeof inputs.operatorLessons.summary === "object" ? inputs.operatorLessons.summary : {};
  return {
    lessonCount: summary.lessonCount ?? lessons.length,
    activeLessonCount: summary.activeLessonCount ?? activeLessons2.length,
    mustObeyLessonCount: mustObeyLessons.length,
    topLessonIds: normalizeStringArray13(summary.topLessonIds).slice(0, 10),
    lessonsPath: summary.lessonsPath ?? ARTIFACT_PATHS.metaOperatorLessons
  };
}
function summarizeStatusVersions(inputs) {
  const versions = Array.isArray(inputs.versions.items) ? inputs.versions.items : [];
  const lineage = Array.isArray(inputs.versions.lineage) ? inputs.versions.lineage : [];
  const comparisons = Array.isArray(inputs.comparisons.items) ? inputs.comparisons.items : [];
  return {
    currentVersionId: inputs.versions.currentVersionId ?? null,
    versionCount: versions.length,
    lineageCount: lineage.length,
    comparisonCount: comparisons.length,
    activeComparisonTargets: normalizeStringArray13(inputs.comparisons.activeTargets),
    versionsPath: ARTIFACT_PATHS.versionsIndex
  };
}
function summarizeStatusExperiments(inputs) {
  return {
    planCount: Array.isArray(inputs.experimentPlans.items) ? inputs.experimentPlans.items.length : 0,
    resultCount: Array.isArray(inputs.experimentResults.items) ? inputs.experimentResults.items.length : 0,
    auditCount: Array.isArray(inputs.experimentAudits.items) ? inputs.experimentAudits.items.length : 0
  };
}
function normalizeStatusStageArg(value) {
  const raw = String(value ?? "").trim().toLowerCase();
  if (DOVE_TASK_STAGES.includes(raw)) {
    return raw;
  }
  if (["audit", "return"].includes(raw)) {
    return "audit";
  }
  if (raw === "execution") {
    return "execute";
  }
  if (["goal", "design", "checklist"].includes(raw)) {
    return "plan";
  }
  return null;
}
function inferDomain(args, inputs) {
  const explicit = normalizeDoveDomainId(args.domain ?? args.doveDomain ?? args.missionDomain, null);
  if (explicit) {
    return explicit;
  }
  const activePacket = inputs.packets.find((packet) => !archivedPacketStatus(packet.status) && !archivedPacketStatus(packet.lifecycleStatus));
  return activePacket?.doveDomain ?? normalizeDoveDomainId(inputs.workspaceIndex.dove?.currentDomain, "paper");
}
function inferStage(args, inputs) {
  const explicit = normalizeDoveMissionLifecycleStage(args.stage ?? args.missionStage, null);
  if (explicit) {
    return explicit;
  }
  return normalizeDoveMissionLifecycleStage(inputs.workspaceIndex.dove?.missionLifecycle?.currentStage, missionStageForPhase(inputs.board.currentPhase));
}
function inferGoal(args, inputs, responseLanguage = "zh") {
  const explicit = typeof args.goal === "string" && args.goal.trim() ? args.goal.trim() : null;
  if (explicit) {
    return explicit;
  }
  return inputs.board.currentFocus ?? inputs.board.objective ?? inputs.state.dove?.thesis ?? inputs.state.dove?.objective ?? inputs.state.dove?.title ?? doveText(responseLanguage, "queryFallbackGoal");
}
function collectTargetArtifacts(args, packets) {
  const explicit = normalizeStringArray13(args.targetArtifacts ?? args.artifacts ?? args.artifactPaths);
  if (explicit.length > 0) {
    return explicit;
  }
  return Array.from(new Set(packets.flatMap((packet) => [...packet.outputPaths, ...packet.evidenceLinks]))).slice(0, 12);
}
function buildMissionContract(root, args = {}) {
  const inputs = readDoveInputs(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args, { state: inputs.state });
  const domain = inferDomain(args, inputs);
  const stage = inferStage(args, inputs);
  const domainGuidance = selectDomainGuidance(inputs.workspaceIndex, domain);
  const primaryRole = primaryRoleForStage(stage);
  const targetArtifacts = collectTargetArtifacts(args, inputs.packets);
  const acceptanceChecks = normalizeStringArray13(args.acceptanceChecks).length > 0 ? normalizeStringArray13(args.acceptanceChecks) : domainGuidance.returnEvidence;
  const nextCommand = args.nextCommand ?? domainGuidance.stageRoutes?.[stage] ?? "project:dove.status";
  return {
    inputs,
    mission: {
      goal: inferGoal(args, inputs, responseLanguage),
      domain,
      stage,
      primaryRole: primaryRole.id,
      nextCommand,
      targetArtifacts,
      acceptanceChecks,
      returnProtocol: doveText(responseLanguage, "returnProtocol", { checks: acceptanceChecks.join(", ") }),
      domainGuidance: {
        label: domainGuidance.label,
        summary: domainGuidance.summary,
        stageRoutes: domainGuidance.stageRoutes,
        returnEvidence: domainGuidance.returnEvidence
      }
    },
    responseLanguage
  };
}
function artifactPathsReadForDove() {
  return [
    ARTIFACT_PATHS.state,
    ARTIFACT_PATHS.orchestrationBoard,
    ARTIFACT_PATHS.workspaceIndex,
    ARTIFACT_PATHS.doveRootManifest,
    ARTIFACT_PATHS.taskPacketsIndex,
    ARTIFACT_PATHS.taskPacketsPacketsDir,
    ARTIFACT_PATHS.runtimeContinuation,
    ARTIFACT_PATHS.runtimeEvents,
    ARTIFACT_PATHS.runtimeResults,
    ARTIFACT_PATHS.reviewState,
    ARTIFACT_PATHS.reviewConcerns,
    ARTIFACT_PATHS.versionsIndex,
    ARTIFACT_PATHS.versionComparisons,
    ARTIFACT_PATHS.experimentPlans,
    ARTIFACT_PATHS.experimentResults,
    ARTIFACT_PATHS.experimentAudits,
    ARTIFACT_PATHS.metaOperatorLessons,
    ARTIFACT_PATHS.checklist
  ];
}
var PAPER_PIPELINE_STAGE_METADATA = {
  init: { commandId: "project:dove.init", artifactPaths: [ARTIFACT_PATHS.state, ARTIFACT_PATHS.project, ARTIFACT_PATHS.researchContract] },
  sources: { commandId: "project:dove.source", artifactPaths: [ARTIFACT_PATHS.sources, ARTIFACT_PATHS.bibliography] },
  notes: { commandId: "project:dove.note", artifactPaths: [ARTIFACT_PATHS.notes] },
  research: { commandId: "project:dove.source", artifactPaths: [ARTIFACT_PATHS.researchBrief, ARTIFACT_PATHS.researchAgenda] },
  plan: { commandId: "project:dove.mission", artifactPaths: [ARTIFACT_PATHS.plan] },
  outline: { commandId: "project:dove.mission", artifactPaths: [ARTIFACT_PATHS.outline] },
  draft: { commandId: "project:dove.draft", artifactPaths: [ARTIFACT_PATHS.draftsDir] },
  experiments: { commandId: "project:dove.experience", artifactPaths: [ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits] },
  citations: { commandId: "project:dove.source", artifactPaths: [ARTIFACT_PATHS.bibliography, ARTIFACT_PATHS.citationLog] },
  review: { commandId: "project:dove.review", artifactPaths: [ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewConcerns] },
  rebuttal: { commandId: "project:dove.rebuttal", artifactPaths: [ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft] },
  versions: { commandId: "project:dove.version", artifactPaths: [ARTIFACT_PATHS.versionsIndex, ARTIFACT_PATHS.versionComparisons] },
  checklist: { commandId: "project:dove.status", artifactPaths: [ARTIFACT_PATHS.checklist] },
  return: { commandId: "project:dove.status", artifactPaths: [ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.versionsIndex, ARTIFACT_PATHS.checklist] }
};
function inspectPipelineArtifact(root, relativePath) {
  return {
    path: relativePath,
    exists: fs14.existsSync(path16.join(root, relativePath))
  };
}
function buildPaperPipelineStage(root, stageId, index) {
  const metadata = PAPER_PIPELINE_STAGE_METADATA[stageId];
  const artifacts = metadata.artifactPaths.map((artifactPath) => inspectPipelineArtifact(root, artifactPath));
  const availableArtifacts = artifacts.filter((item) => item.exists).map((item) => item.path);
  const missingArtifacts = artifacts.filter((item) => !item.exists).map((item) => item.path);
  return {
    id: stageId,
    order: index + 1,
    commandId: metadata.commandId,
    keyArtifacts: artifacts,
    availableArtifacts,
    missingArtifacts,
    status: missingArtifacts.length === 0 ? "ready" : availableArtifacts.length > 0 ? "partial" : "missing"
  };
}
function selectPaperPipelineNextCommand(stages) {
  const nextStage = stages.find((stage) => stage.status !== "ready");
  return nextStage?.commandId ?? "project:dove.status";
}
function booleanArg(value) {
  if (value === true) {
    return true;
  }
  if (typeof value === "string") {
    return ["1", "true", "yes", "on"].includes(value.trim().toLowerCase());
  }
  return false;
}
function buildMissionBoardMission(packet) {
  const missionStage = missionStageForPhase(packet.phase);
  const primaryRole = primaryRoleForStage(missionStage);
  return {
    ...packet,
    ...missionPacketAliases(packet),
    missionStage,
    primaryRole: primaryRole.id,
    archived: archivedPacketStatus(packet.status) || archivedPacketStatus(packet.lifecycleStatus)
  };
}
function matchesMissionBoardFilters(mission, args = {}) {
  const domain = normalizeDoveDomainId(args.domain ?? args.doveDomain ?? args.missionDomain, null);
  if (domain && mission.doveDomain !== domain) {
    return false;
  }
  const stage = normalizeDoveMissionLifecycleStage(args.stage ?? args.missionStage, null);
  if (stage && mission.missionStage !== stage) {
    return false;
  }
  const packetIds = normalizeStringArray13(args.packetId ?? args.packetIds ?? args.missionPacketId ?? args.missionPacketIds);
  if (packetIds.length > 0 && !packetIds.includes(mission.id) && !packetIds.includes(mission.packetId) && !packetIds.includes(mission.missionPacketId)) {
    return false;
  }
  const statuses = normalizeStringArray13(args.status ?? args.statuses);
  if (statuses.length > 0 && !statuses.includes(mission.lifecycleStatus) && !statuses.includes(mission.status)) {
    return false;
  }
  return booleanArg(args.includeArchived) || !mission.archived;
}
function queueNameForMission(mission) {
  const lifecycleStatus = mission.lifecycleStatus ?? "active";
  if (mission.archived || archivedPacketStatus(mission.status) || archivedPacketStatus(lifecycleStatus)) {
    return "archived";
  }
  if (lifecycleStatus === "review-needed") {
    return "reviewNeeded";
  }
  if (["handoff", "handoff-ready", "returned"].includes(lifecycleStatus)) {
    return "handoff";
  }
  if (["waiting", "ready"].includes(lifecycleStatus)) {
    return lifecycleStatus;
  }
  if (["stale", "blocked", "active"].includes(lifecycleStatus)) {
    return lifecycleStatus;
  }
  if (["done", "completed"].includes(mission.status) || lifecycleStatus === "completed") {
    return "ready";
  }
  return "active";
}
function buildMissionQueues(missions) {
  const queues = {
    ready: [],
    waiting: [],
    reviewNeeded: [],
    handoff: [],
    stale: [],
    active: [],
    blocked: [],
    archived: []
  };
  for (const mission of missions) {
    queues[queueNameForMission(mission)].push(mission);
  }
  return queues;
}
function countBy2(values, initialKeys = []) {
  const counts = Object.fromEntries(initialKeys.map((key) => [key, 0]));
  for (const value of values) {
    counts[value] = (counts[value] ?? 0) + 1;
  }
  return counts;
}
function buildMissionBoardCounts(missions) {
  const kernel = createDoveWorkspaceKernel();
  return {
    missionCount: missions.length,
    activeMissionCount: missions.filter((mission) => !mission.archived).length,
    reviewNeededMissionCount: missions.filter((mission) => mission.lifecycleStatus === "review-needed").length,
    domainCounts: countBy2(missions.map((mission) => mission.doveDomain), kernel.domainIds),
    stageCounts: countBy2(missions.map((mission) => mission.missionStage), kernel.missionLifecycle.stages),
    lifecycleStatusCounts: countBy2(missions.map((mission) => mission.lifecycleStatus ?? "active"))
  };
}
function uncheckedChecklistCount(checklist) {
  if (!checklist) {
    return 0;
  }
  return checklist.split(/\r?\n/).filter((line) => /^\s*- \[ \]/.test(line)).length;
}
function completedChecklistCount(checklist) {
  if (!checklist) {
    return 0;
  }
  return checklist.split(/\r?\n/).filter((line) => /^\s*- \[[xX]\]/.test(line)).length;
}
function classifyReturnStatus({ mission, inputs, paperAudit, engineeringEvidence }) {
  if (inputs.readErrors.length > 0) {
    return "blocked";
  }
  const reviewNeeded = inputs.packets.some((packet) => packet.lifecycleStatus === "review-needed") || ["needs-work", "rejected", "blocked"].includes(inputs.reviewState.lastVerdict);
  if (reviewNeeded) {
    return "needs-review";
  }
  const currentPackets = activePackets(inputs.packets);
  if (mission.domain === "engineering" && engineeringEvidence) {
    if (engineeringEvidence.readiness.hasReviewGap) {
      return "needs-review";
    }
    if (engineeringEvidence.readiness.hasFailedValidationOutput) {
      return "needs-execution";
    }
    if (engineeringEvidence.readiness.hasEvidenceGaps || engineeringEvidence.readiness.pathProblemCount > 0) {
      return "needs-audit";
    }
    if (engineeringEvidence.readiness.hasChecklistGap) {
      return "needs-execution";
    }
  }
  if (uncheckedChecklistCount(inputs.checklist) > 0 || currentPackets.some((packet) => ["active", "waiting", "blocked", "stale"].includes(packet.lifecycleStatus))) {
    return "needs-execution";
  }
  if (paperAudit?.severityCounts?.high > 0 || paperAudit?.severityCounts?.critical > 0) {
    return "needs-audit";
  }
  return "ready";
}
function nextCommandForReturnStatus(status, domain) {
  if (status === "needs-review") {
    return "project:dove.review";
  }
  if (status === "needs-execution") {
    return domain === "paper" ? "project:dove.status" : "project:dove.status";
  }
  if (status === "needs-audit") {
    return domain === "engineering" ? "project:dove.status" : "project:dove.review";
  }
  if (status === "blocked") {
    return "project:dove.mission";
  }
  return "project:dove.version";
}
function routeRequestText(args, mission) {
  return normalizeStringArray13([args.request, args.userRequest, args.goal, mission.goal]).join(" ").toLowerCase();
}
function selectRouteCommand(mission, args = {}) {
  const route = mission.domainGuidance.stageRoutes?.[mission.stage] ?? mission.nextCommand;
  const candidates = String(route ?? "project:dove.status").split(/\s+or\s+/).map((item) => item.trim()).filter(Boolean);
  if (candidates.length <= 1) {
    return candidates[0] ?? "project:dove.status";
  }
  const requestText = routeRequestText(args, mission);
  if (booleanArg(args.allowAutonomy)) {
    const autonomy = candidates.find((item) => item.includes(".auto") || item.includes("autonomy"));
    if (autonomy) {
      return autonomy;
    }
  }
  if (/\b(revise|revision|rebuttal|fix|review)\b/.test(requestText)) {
    const revision = candidates.find((item) => /revise|rebuttal|review/.test(item));
    if (revision) {
      return revision;
    }
  }
  return candidates[0];
}
function routeReason(mission, selectedCommand, args = {}, responseLanguage = "zh") {
  if (selectedCommand !== mission.nextCommand && String(mission.nextCommand).includes(" or ")) {
    return doveText(responseLanguage, "routeSelectedReason", { selectedCommand, nextCommand: mission.nextCommand });
  }
  if (booleanArg(args.allowAutonomy) && (selectedCommand.includes(".auto") || selectedCommand.includes("autonomy"))) {
    return doveText(responseLanguage, "routeAutoReason");
  }
  return doveText(responseLanguage, "routeDefaultReason", { stage: mission.stage, domain: mission.domain, selectedCommand });
}
function buildWorkspaceSummary(inputs) {
  const kernel = createDoveWorkspaceKernel();
  const authorityManifest = inputs.doveAuthorityManifest ?? inputs.workspaceIndex.dove?.authorityManifest ?? kernel.authorityManifest;
  return {
    kernelVersion: inputs.workspaceIndex.dove?.kernelVersion ?? kernel.kernelVersion,
    unified: true,
    explicitOnly: true,
    noHiddenRuntime: true,
    identity: inputs.workspaceIndex.dove?.identity ?? kernel.identity,
    authorityManifest,
    durableRoot: ARTIFACT_PATHS.doveRoot,
    authoritativeRoot: authorityManifest?.authoritativeRoot ?? ARTIFACT_PATHS.doveRoot
  };
}
function normalizePaperAuditFinding(finding, mission) {
  return {
    ...finding,
    missionDomain: mission.domain,
    missionStage: mission.stage,
    primaryRole: mission.primaryRole,
    blocking: ["critical", "high"].includes(finding.severity)
  };
}
function auditVerdictFromSeverityCounts(severityCounts = {}) {
  if ((severityCounts.critical ?? 0) > 0 || (severityCounts.high ?? 0) > 0) {
    return "blocking-findings";
  }
  if ((severityCounts.medium ?? 0) > 0 || (severityCounts.low ?? 0) > 0) {
    return "findings";
  }
  return "clear";
}
function queryPaperPipeline(root, args = {}) {
  const inputs = readDoveInputs(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args, { state: inputs.state });
  const currentStage = inferStage({ ...args, domain: "paper" }, inputs);
  const stageIds = [...PIPELINE_STAGE_ORDER, "return"];
  const stages = stageIds.map((stageId, index) => buildPaperPipelineStage(root, stageId, index));
  const stageCounts = countBy2(stages.map((stage) => stage.status), ["ready", "partial", "missing"]);
  return {
    mode: "paper-pipeline-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    current: {
      domain: "paper",
      missionStage: currentStage,
      boardPhase: inputs.board.currentPhase ?? null,
      boardAssignedRole: inputs.board.assignedRole ?? null,
      workspaceCurrentDomain: inputs.workspaceIndex.dove?.currentDomain ?? null,
      workspaceMissionStage: inputs.workspaceIndex.dove?.missionLifecycle?.currentStage ?? null
    },
    stages,
    summary: {
      stageCount: stages.length,
      readyStageCount: stageCounts.ready,
      partialStageCount: stageCounts.partial,
      missingStageCount: stageCounts.missing,
      activePacketCount: activePackets(inputs.packets).filter((packet) => ["paper", "experiment", "review"].includes(packet.doveDomain)).length,
      reviewVerdict: inputs.reviewState.lastVerdict ?? "not-reviewed",
      checklistUncheckedCount: uncheckedChecklistCount(inputs.checklist)
    },
    suggestedNextCommand: selectPaperPipelineNextCommand(stages),
    workspace: buildWorkspaceSummary(inputs),
    diagnostics: {
      readErrors: inputs.readErrors,
      artifactPathsRead: Array.from(/* @__PURE__ */ new Set([...artifactPathsReadForDove(), ...stages.flatMap((stage) => stage.keyArtifacts.map((item) => item.path))])),
      noRefresh: true,
      noCommandExecution: true,
      noExternalProcess: true,
      noGitInspection: true
    }
  };
}
function queryDoveStatus(root, args = {}) {
  const inputs = readDoveInputs(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args, { state: inputs.state });
  const statusIntent = normalizeStatusIntent(args.intent);
  const includeArchived = booleanArg(args.includeArchived);
  const allTasks = sortStatusTasks(enrichStatusTasks((Array.isArray(inputs.taskCatalog.packets) ? inputs.taskCatalog.packets : []).map((packet) => summarizeDoveStatusTask(packet, responseLanguage)).filter((task) => task.id), inputs));
  const archivedTasks = allTasks.filter(isArchivedStatusTask);
  const tasks = includeArchived ? allTasks : allTasks.filter((task) => !isArchivedStatusTask(task));
  const archivedHiddenCount = includeArchived ? 0 : archivedTasks.length;
  const consistencyTasks = allTasks.filter((task) => !isArchivedStatusTask(task));
  const requestedDomain = normalizeDoveDomainId(args.domain ?? args.doveDomain ?? args.missionDomain, null);
  const requestedStage = normalizeStatusStageArg(args.stage ?? args.missionStage);
  const requestedStatuses = normalizeStringArray13(args.status ?? args.statuses);
  const requestedPacketIds = normalizeStringArray13(args.packetId ?? args.packetIds ?? args.missionPacketId ?? args.missionPacketIds);
  const visibleTasks = tasks.filter((task) => {
    if (requestedDomain && task.domain !== requestedDomain) {
      return false;
    }
    if (requestedStage && task.stage !== requestedStage) {
      return false;
    }
    if (requestedStatuses.length > 0 && !requestedStatuses.includes(task.status) && !requestedStatuses.includes(String(task.lifecycleStatus ?? ""))) {
      return false;
    }
    if (requestedPacketIds.length > 0 && !requestedPacketIds.includes(task.id) && !requestedPacketIds.includes(task.packetId) && !requestedPacketIds.includes(task.missionPacketId)) {
      return false;
    }
    return true;
  });
  const initTask = tasks.find((task) => task.level === 0 && task.status !== "killed") ?? null;
  const activeStatusIds = /* @__PURE__ */ new Set(["pending", "ready", "in-progress", "blocked"]);
  const activeTasks = visibleTasks.filter((task) => task.level !== 0 && activeStatusIds.has(task.status));
  const actionableBoundaries = activeTasks.map(summarizeActionableBoundary).filter(Boolean);
  const boundaryActionCards = activeTasks.map((task) => buildBoundaryActionCard(task, responseLanguage)).filter(Boolean);
  const blockedTasks = activeTasks.filter(hasTaskBlockerSignal);
  const completedTasks = sortRecentStatusTasks(visibleTasks.filter((task) => task.status === "completed")).slice(0, 10);
  const killedTasks = sortRecentStatusTasks(visibleTasks.filter((task) => task.status === "killed")).slice(0, 10);
  const completionConsistency = buildCompletionConsistency(consistencyTasks, responseLanguage);
  const executionGaps = buildStatusExecutionGaps(activeTasks);
  const review = summarizeStatusReview(inputs);
  const blockers = buildStatusBlockers(inputs, blockedTasks, responseLanguage);
  const lessons = summarizeStatusLessons(inputs);
  const versions = summarizeStatusVersions(inputs);
  const experiments = summarizeStatusExperiments(inputs);
  const fallbackNextCommand = selectStatusNextCommand({ initTask, activeTasks, blockedTasks, review });
  const currentStage = requestedStage ?? activeTasks[0]?.stage ?? initTask?.stage ?? null;
  const currentDomain = requestedDomain ?? activeTasks[0]?.domain ?? initTask?.domain ?? normalizeDoveDomainId(inputs.workspaceIndex.dove?.currentDomain, null);
  const primaryRole = currentStage === "audit" ? "reviewer" : currentStage === "execute" ? "builder" : "planner";
  const returnStatus = inputs.readErrors.length > 0 ? "blocked" : blockers.length > 0 || completionConsistency.status === "needs-reconciliation" || (executionGaps.counts?.blocking ?? 0) > 0 ? "blocked" : activeTasks.length > 0 ? "in-progress" : review.unresolvedConcernCount > 0 ? "needs-review" : "ready";
  const levelCounts = countBy2(tasks.map((task) => String(task.level)), ["0", "1", "2", "3"]);
  const projectTitle = inputs.state.dove?.title ?? initTask?.title ?? doveText(responseLanguage, "projectTitleFallback");
  const projectObjective = inputs.state.dove?.objective ?? inputs.state.dove?.thesis ?? initTask?.summary ?? inputs.board.objective ?? null;
  const projectFocus = activeTasks[0]?.currentFocus ?? (activeTasks.length === 0 ? projectObjective : inputs.state.orchestration?.currentFocus ?? inputs.board.currentFocus ?? projectObjective);
  const dailyHome = buildDailyHome({ initTask, activeTasks, blockedTasks, visibleTasks, review, boundaryActionCards, completionConsistency, executionGaps, archivedHiddenCount, responseLanguage });
  const nextCommand = selectDailyHomeNextCommand(dailyHome, fallbackNextCommand);
  const projectSummary = buildProjectSummary({ title: projectTitle, objective: projectObjective, focus: projectFocus, initTask, tasks, activeTasks, blockedTasks, missionList: dailyHome.missionList, review, versions, experiments, blockers, nextCommand, returnStatus, archivedHiddenCount });
  const statusAdjustmentContract = buildStatusAdjustmentContract(visibleTasks, responseLanguage);
  const durableContextNotice = buildDurableContextNotice(root, responseLanguage);
  const preActionGuidance = buildPreActionGuidance({
    surface: "dove.status",
    responseLanguage,
    request: args.request ?? args.userRequest ?? args.prompt ?? null,
    roleId: primaryRole,
    currentContext: {
      title: projectTitle,
      objective: projectObjective,
      currentFocus: projectFocus,
      domain: currentDomain,
      stage: currentStage,
      primaryRole
    },
    operatorLessons: inputs.operatorLessons,
    nextAction: dailyHome.nextActions?.[0] ?? nextCommand,
    routeHint: nextCommand,
    workflowKind: "status",
    domain: currentDomain,
    stage: currentStage,
    statusSummary: {
      returnStatus,
      activeTaskCount: activeTasks.length,
      blockerCount: blockers.length,
      unresolvedConcernCount: review.unresolvedConcernCount,
      readErrorCount: inputs.readErrors.length,
      statusAdjustmentCount: statusAdjustmentContract.itemCount ?? 0,
      executionGaps
    }
  });
  const fullResult = {
    mode: "dove-status-query",
    query: true,
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    intent: statusIntent,
    responseLanguage,
    detail: "full",
    projectSummary,
    durableContextNotice,
    preActionGuidance,
    statusAdjustmentContract,
    dailyHome,
    actionableBoundaries,
    boundaryActionCards,
    current: {
      domain: currentDomain,
      stage: currentStage,
      primaryRole,
      nextCommand
    },
    dashboard: {
      projectSummary,
      statusAdjustmentContract,
      dailyHome,
      project: {
        title: projectTitle,
        objective: projectObjective,
        responseLanguage,
        durableRoot: ARTIFACT_PATHS.doveRoot,
        authoritativeRoot: inputs.doveAuthorityManifest.authoritativeRoot ?? ARTIFACT_PATHS.doveRoot,
        durableContextNotice,
        currentFocus: projectFocus,
        nextAction: nextCommand,
        pipeline: {
          currentStage: inputs.state.pipeline?.currentStage ?? inputs.board.currentPhase ?? null,
          lastCompletedStage: inputs.state.pipeline?.lastCompletedStage ?? null,
          resumeCommand: inputs.state.pipeline?.resumeCommand ?? "project:dove.status"
        }
      },
      board: {
        phase: inputs.board.currentPhase ?? null,
        intentType: inputs.board.intentType ?? null,
        assignedRole: inputs.board.assignedRole ?? null,
        continuationStatus: inputs.board.continuationState?.status ?? null,
        reviewRequiredBeforeFinalize: inputs.board.reviewRequiredBeforeFinalize ?? false
      },
      runtime: {
        continuation: {
          ...inputs.runtimeContinuation.summary,
          items: Array.isArray(inputs.runtimeContinuation.items) ? inputs.runtimeContinuation.items : []
        },
        results: inputs.runtimeResults.summary,
        events: inputs.runtimeEvents.summary
      },
      init: initTask,
      tasks: {
        tree: buildStatusTaskTree(visibleTasks),
        active: activeTasks,
        grouped: dailyHome.missionList,
        blocked: blockedTasks,
        actionableBoundaries,
        boundaryActionCards,
        recentCompleted: completedTasks,
        recentKilled: killedTasks,
        activeTaskIds: activeTasks.map((task) => task.id),
        counts: {
          total: tasks.length,
          visible: visibleTasks.length,
          active: activeTasks.length,
          blocked: blockedTasks.length,
          completed: tasks.filter((task) => task.status === "completed").length,
          killed: tasks.filter((task) => task.status === "killed").length,
          archived: tasks.filter((task) => task.status === "archived" || isArchivedStatusTask(task)).length,
          archivedHidden: archivedHiddenCount,
          byStatus: countBy2(tasks.map((task) => task.status), DOVE_TASK_STATUSES),
          byDomain: countBy2(tasks.map((task) => task.domain), DOVE_TASK_DOMAINS),
          byStage: countBy2(tasks.map((task) => task.stage), DOVE_TASK_STAGES),
          byLevel: levelCounts
        },
        index: {
          path: ARTIFACT_PATHS.taskPacketsIndex,
          version: inputs.taskPackets.version ?? null,
          activeInitId: inputs.taskPackets.taskModel?.activeInitId ?? initTask?.id ?? null,
          activeTaskIds: normalizeStringArray13(inputs.taskPackets.taskModel?.activeTaskIds).length > 0 ? normalizeStringArray13(inputs.taskPackets.taskModel?.activeTaskIds) : activeTasks.map((task) => task.id)
        }
      },
      blockers,
      review,
      lessons,
      versions,
      experiments,
      checklist: {
        path: ARTIFACT_PATHS.checklist,
        uncheckedCount: uncheckedChecklistCount(inputs.checklist),
        completedCount: completedChecklistCount(inputs.checklist)
      },
      returnReadiness: {
        status: returnStatus,
        activeTaskCount: activeTasks.length,
        blockerCount: blockers.length,
        unresolvedConcernCount: review.unresolvedConcernCount,
        readErrorCount: inputs.readErrors.length
      },
      nextAction: nextCommand
    },
    board: {
      domain: currentDomain,
      stage: currentStage,
      primaryRole,
      nextCommand,
      phase: inputs.board.currentPhase ?? null,
      assignedRole: inputs.board.assignedRole ?? null
    },
    suggestedNextCommand: nextCommand,
    diagnostics: {
      readErrors: inputs.readErrors,
      artifactPathsRead: artifactPathsReadForDove(),
      derivedReports: {
        navigationReportPath: ARTIFACT_PATHS.navigationReport,
        wikiPath: ARTIFACT_PATHS.wiki
      },
      primaryStateSources: [ARTIFACT_PATHS.state, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.taskPacketsPacketsDir, ARTIFACT_PATHS.runtimeContinuation, ARTIFACT_PATHS.runtimeEvents, ARTIFACT_PATHS.runtimeResults, ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.versionsIndex, ARTIFACT_PATHS.metaOperatorLessons],
      durableContextNotice,
      mayRefreshDerivedSurfaces: false,
      noCommandExecution: true,
      noExternalProcess: true,
      noGitInspection: true,
      noSourceMutation: true
    }
  };
  return wantsFullDoveStatus(args) ? fullResult : compactDoveStatusResult(fullResult, args);
}
function queryDoveOrchestrate(root, args = {}) {
  const { inputs, mission, responseLanguage } = buildMissionContract(root, args);
  const recommendedCommand = selectRouteCommand(mission, args);
  return {
    mode: "dove-orchestrate-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    mission,
    route: {
      recommendedCommand,
      nextCommand: recommendedCommand,
      reason: routeReason(mission, recommendedCommand, args, responseLanguage),
      roleBoundary: {
        primaryRole: mission.primaryRole,
        reviewerIsolationRequired: mission.primaryRole === "reviewer" || mission.stage === "audit"
      },
      domainStageRoutes: mission.domainGuidance.stageRoutes
    },
    workspace: buildWorkspaceSummary(inputs),
    diagnostics: {
      readErrors: inputs.readErrors,
      artifactPathsRead: artifactPathsReadForDove(),
      noRefresh: true,
      noCommandExecution: true,
      noGitInspection: true
    }
  };
}
function queryDoveAudit(root, args = {}) {
  const { inputs, mission, responseLanguage } = buildMissionContract(root, args);
  const paperAudit = queryPaperAudit(root, { scope: args.scope ?? mission.goal });
  const returnReadiness = queryDoveReturn(root, args);
  return {
    mode: "dove-audit-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    mission,
    audit: {
      verdict: auditVerdictFromSeverityCounts(paperAudit.severityCounts),
      mode: paperAudit.mode,
      scope: args.scope ?? mission.goal,
      severityCounts: paperAudit.severityCounts,
      categoryCounts: paperAudit.categoryCounts,
      findingCount: paperAudit.findings.length,
      suggestedNextCommands: paperAudit.suggestedNextCommands
    },
    findings: paperAudit.findings.map((finding) => normalizePaperAuditFinding(finding, mission)),
    returnReadiness: {
      returnStatus: returnReadiness.returnStatus,
      nextCommand: returnReadiness.nextCommand,
      missingReturnEvidence: returnReadiness.missingReturnEvidence,
      engineeringEvidence: returnReadiness.engineeringEvidence,
      checklist: returnReadiness.checklist,
      review: returnReadiness.review
    },
    workspace: buildWorkspaceSummary(inputs),
    diagnostics: {
      readErrors: [...inputs.readErrors, ...paperAudit.diagnostics?.readErrors ?? [], ...returnReadiness.diagnostics?.readErrors ?? []],
      artifactPathsRead: Array.from(/* @__PURE__ */ new Set([
        ...artifactPathsReadForDove(),
        ...paperAudit.artifactPathsRead ?? [],
        ...returnReadiness.diagnostics?.artifactPathsRead ?? []
      ])),
      noRefresh: true,
      noCommandExecution: true,
      noGitInspection: true
    }
  };
}
function queryDoveMissionBoard(root, args = {}) {
  const { inputs, mission, responseLanguage } = buildMissionContract(root, args);
  const boardStage = normalizeDoveMissionLifecycleStage(args.stage ?? args.missionStage, missionStageForPhase(inputs.board.currentPhase));
  const boardRole = primaryRoleForStage(boardStage);
  const allMissions = inputs.packets.map(buildMissionBoardMission);
  const missions = allMissions.filter((item) => matchesMissionBoardFilters(item, args));
  return {
    mode: "dove-mission-board-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    workspace: {
      ...buildWorkspaceSummary(inputs),
      asReadSnapshot: true,
      workspaceIndexUpdatedAt: inputs.workspaceIndex.updatedAt ?? null
    },
    board: {
      goal: mission.goal,
      domain: mission.domain,
      stage: boardStage,
      boardPhase: inputs.board.currentPhase ?? null,
      boardAssignedRole: inputs.board.assignedRole ?? null,
      primaryRole: boardRole.id,
      nextCommand: mission.nextCommand,
      currentFocus: inputs.board.currentFocus ?? inputs.workspaceIndex.currentFocus ?? null,
      objective: inputs.board.objective ?? null,
      intentType: inputs.board.intentType ?? null,
      nextAction: inputs.board.nextAction ?? inputs.workspaceIndex.nextAction ?? null,
      continuationState: inputs.board.continuationState ?? null,
      reviewRequiredBeforeFinalize: Boolean(inputs.board.reviewRequiredBeforeFinalize),
      acceptanceChecks: mission.acceptanceChecks,
      returnProtocol: mission.returnProtocol
    },
    missions,
    queues: buildMissionQueues(missions),
    counts: buildMissionBoardCounts(allMissions),
    filters: {
      domain: normalizeDoveDomainId(args.domain ?? args.doveDomain ?? args.missionDomain, null),
      stage: normalizeDoveMissionLifecycleStage(args.stage ?? args.missionStage, null),
      packetIds: normalizeStringArray13(args.packetId ?? args.packetIds ?? args.missionPacketId ?? args.missionPacketIds),
      statuses: normalizeStringArray13(args.status ?? args.statuses),
      includeArchived: booleanArg(args.includeArchived)
    },
    diagnostics: {
      readErrors: inputs.readErrors,
      artifactPathsRead: artifactPathsReadForDove(),
      noRefresh: true,
      noCommandExecution: true,
      noGitInspection: true
    }
  };
}
function queryDoveMission(root, args = {}) {
  const { inputs, mission, responseLanguage } = buildMissionContract(root, args);
  return {
    mode: "dove-mission-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    mission,
    workspace: buildWorkspaceSummary(inputs),
    packets: inputs.packets,
    diagnostics: {
      readErrors: inputs.readErrors,
      artifactPathsRead: artifactPathsReadForDove()
    }
  };
}
var DOVE_MISSION_LAUNCH_FIELDS = /* @__PURE__ */ new Set([
  "sourceType",
  "sourceId",
  "goal",
  "domain",
  "doveDomain",
  "missionDomain",
  "stage",
  "missionStage",
  "targetArtifacts",
  "artifacts",
  "artifactPaths",
  "acceptanceChecks",
  "acceptanceCriteria",
  "returnProtocol",
  "packetId",
  "missionPacketId",
  "followThroughId",
  "selectedConversionPathKey",
  "title",
  "summary",
  "status",
  "currentFocus",
  "nextAction",
  "nextCommand",
  "dependencies",
  "evidenceLinks",
  "outputPaths",
  "decisionSummary",
  "rationale",
  "executeBy",
  "reviewAfter"
]);
function assertMissionLaunchArgs(args) {
  if (!args || typeof args !== "object" || Array.isArray(args)) {
    throw new Error("launchDoveMission requires a packet-only argument object.");
  }
  const unknownFields = Object.keys(args).filter((field) => !DOVE_MISSION_LAUNCH_FIELDS.has(field));
  if (unknownFields.length > 0) {
    throw new Error(`launchDoveMission rejects non-mission fields: ${unknownFields.join(", ")}.`);
  }
}
function launchDoveMission(root, args = {}) {
  assertGovernanceMutationRegistered("launch-dove-mission", "guarded");
  assertMissionLaunchArgs(args);
  throw new Error(
    "Public launch_dove_mission is disabled: mission packet creation requires a pre-existing, dynamically valid system-owned program approval, program run, runtime execution claim, and trusted authority provenance. No public caller field grants that authority."
  );
}
function queryDoveReturn(root, args = {}) {
  const { inputs, mission, responseLanguage } = buildMissionContract(root, args);
  const validationEvidencePaths = mergeStringArrays(args.validationEvidencePaths, args.validationEvidence, args.evidencePaths);
  const paperAudit = ["paper", "experiment", "review"].includes(mission.domain) ? queryPaperAudit(root, { scope: args.scope ?? mission.goal }) : null;
  const completedChecks = completedChecklistCount(inputs.checklist);
  const uncheckedChecks = uncheckedChecklistCount(inputs.checklist);
  const engineeringEvidence = mission.domain === "engineering" ? buildEngineeringEvidence(root, args, inputs, mission, completedChecks, uncheckedChecks) : null;
  const status = classifyReturnStatus({ mission, inputs, paperAudit, engineeringEvidence });
  const evidenceRead = Array.from(/* @__PURE__ */ new Set([
    ...mission.targetArtifacts,
    ...engineeringEvidence ? engineeringEvidence.evidenceReadPaths : validationEvidencePaths,
    ARTIFACT_PATHS.workspaceIndex,
    ARTIFACT_PATHS.taskPacketsIndex,
    ARTIFACT_PATHS.reviewState,
    ARTIFACT_PATHS.checklist,
    ARTIFACT_PATHS.versionsIndex,
    ARTIFACT_PATHS.versionComparisons
  ])).filter(Boolean);
  const acceptanceMissing = mission.acceptanceChecks.filter((check) => {
    const normalized = check.toLowerCase();
    if (mission.domain === "engineering") {
      if (normalized.includes("changed")) {
        return !engineeringEvidence.changedFiles.satisfied;
      }
      if (normalized.includes("test") || normalized.includes("validation") || normalized.includes("output")) {
        return !engineeringEvidence.validationEvidence.satisfied || engineeringEvidence.validationOutput.status !== "passed";
      }
    } else if (normalized.includes("test") || normalized.includes("validation")) {
      return validationEvidencePaths.length === 0 && !inputs.packets.some((packet) => packet.evidenceLinks.length > 0);
    }
    if (normalized.includes("checklist")) {
      return !inputs.checklist || uncheckedChecks > 0;
    }
    if (normalized.includes("review")) {
      return inputs.reviewState.lastVerdict === "not-reviewed";
    }
    return false;
  });
  const missingReturnEvidence = Array.from(/* @__PURE__ */ new Set([
    ...acceptanceMissing,
    ...engineeringEvidence ? engineeringEvidence.missingEvidence.map((item) => item.requirement) : []
  ]));
  return {
    mode: "dove-return-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    returnStatus: status,
    acceptanceVerdict: status === "ready" ? doveText(responseLanguage, "returnReadyVerdict") : doveText(responseLanguage, "returnNotReadyVerdict"),
    nextCommand: nextCommandForReturnStatus(status, mission.domain),
    mission,
    evidenceRead,
    missingReturnEvidence,
    lessonRitual: {
      command: "project:dove.lessons",
      optional: true,
      when: doveText(responseLanguage, "lessonRitualWhen"),
      requiredFields: ["title", "problem", "decisions", "pitfalls", "validation", "nextTime"],
      noAutoCapture: true,
      noAutoApply: true,
      noExecution: true
    },
    workspace: buildWorkspaceSummary(inputs),
    checklist: {
      completedCount: completedChecks,
      uncheckedCount: uncheckedChecks,
      path: ARTIFACT_PATHS.checklist
    },
    review: {
      lastVerdict: inputs.reviewState.lastVerdict ?? "not-reviewed",
      unresolvedConcernIds: normalizeStringArray13(inputs.reviewState.unresolvedConcernIds),
      openItems: normalizeStringArray13(inputs.reviewState.openItems)
    },
    packets: inputs.packets,
    engineeringEvidence,
    audit: paperAudit ? {
      mode: paperAudit.mode,
      severityCounts: paperAudit.severityCounts,
      findingCount: paperAudit.findings.length,
      suggestedNextCommands: paperAudit.suggestedNextCommands
    } : null,
    diagnostics: {
      readErrors: inputs.readErrors,
      artifactPathsRead: evidenceRead,
      declaredInputsOnly: mission.domain === "engineering",
      noCommandExecution: mission.domain === "engineering",
      noGitInspection: mission.domain === "engineering"
    }
  };
}

// src/core/public-status.mjs
import fs15 from "node:fs";
import path17 from "node:path";
var PUBLIC_STATUS_VERSION = 1;
var GLOBAL_PUBLIC_STATUS_VERSION = 1;
var PUBLIC_TASK_LIMIT = 12;
var PUBLIC_RECENT_LIMIT = 8;
var SECRET_PATTERNS = [
  { pattern: /sk-ant-[A-Za-z0-9_-]+/g, replacement: "<redacted>" },
  { pattern: /\bBearer\s+[A-Za-z0-9._~+\/-]+=*/gi, replacement: "Bearer <redacted>" },
  { pattern: /\b(api[_-]?key|auth[_-]?token|access[_-]?token|secret|password|passwd|pwd)\s*[:=]\s*[^\s,;"']+/gi, replacement: (match, label) => `${label}=<redacted>` }
];
function redactText(value) {
  let text3 = String(value ?? "");
  for (const item of SECRET_PATTERNS) {
    text3 = text3.replace(item.pattern, item.replacement);
  }
  return text3;
}
function publicString(value, maxLength = 320) {
  const text3 = redactText(value).replace(/\s+/g, " ").trim();
  if (!text3) {
    return null;
  }
  return text3.length > maxLength ? `${text3.slice(0, maxLength - 1)}\u2026` : text3;
}
function publicStringArray(value, limit = 10, maxLength = 220) {
  const values = Array.isArray(value) ? value : typeof value === "string" ? [value] : [];
  return Array.from(new Set(values.map((item) => publicString(item, maxLength)).filter(Boolean))).slice(0, limit);
}
function firstPublicString(...values) {
  for (const value of values) {
    const normalized = publicString(value);
    if (normalized) {
      return normalized;
    }
  }
  return null;
}
function publicTask(task) {
  if (!task || typeof task !== "object") {
    return null;
  }
  return {
    id: publicString(task.id, 160),
    title: publicString(task.title, 220) ?? publicString(task.id, 160),
    summary: firstPublicString(task.summary, task.currentFocus, task.lastStopReason),
    status: publicString(task.status, 80),
    stage: publicString(task.stage, 80),
    domain: publicString(task.domain, 80),
    level: Number.isFinite(Number(task.level)) ? Number(task.level) : null,
    creatorKind: publicString(task.creatorKind, 80),
    currentFocus: publicString(task.currentFocus),
    nextAction: publicString(task.nextAction, 160),
    blockedReason: publicString(task.blockedReason ?? task.lastStopReason),
    boundary: task.actionableBoundary ? {
      id: publicString(task.actionableBoundary.id, 160),
      type: publicString(task.actionableBoundary.type, 120),
      reason: publicString(task.actionableBoundary.reason ?? task.actionableBoundary.summary),
      requiredInputs: publicStringArray(task.actionableBoundary.requiredInputs),
      requiredActions: publicStringArray(task.actionableBoundary.requiredActions),
      ownerRole: publicString(task.actionableBoundary.ownerRole, 80),
      nextRole: publicString(task.actionableBoundary.nextRole, 80)
    } : null,
    evidenceExpectations: publicStringArray(task.evidenceExpectations),
    evidenceLinks: publicStringArray(task.evidenceLinks, 8),
    artifactRefs: publicStringArray(task.artifactRefs, 8),
    updatedAt: publicString(task.updatedAt, 80),
    completedAt: publicString(task.completedAt, 80)
  };
}
function publicAction(action) {
  if (!action || typeof action !== "object") {
    return null;
  }
  return {
    rank: Number.isFinite(Number(action.rank)) ? Number(action.rank) : null,
    title: publicString(action.title ?? action.label, 220),
    why: publicString(action.why ?? action.reason ?? action.summary),
    command: publicString(action.command, 160),
    packetId: publicString(action.packetId, 160),
    boundaryType: publicString(action.boundaryType, 120),
    requires: publicStringArray(action.requires ?? [...publicStringArray(action.requiredInputs), ...publicStringArray(action.requiredActions)])
  };
}
function publicReview(review) {
  const reviewerIndependence = review?.reviewerIndependence;
  const reviewerIndependenceSummary = reviewerIndependence && typeof reviewerIndependence === "object" ? firstPublicString(reviewerIndependence.summary, reviewerIndependence.status, reviewerIndependence.reviewerRole) : publicString(reviewerIndependence, 160);
  return {
    verdict: publicString(review?.verdict, 80) ?? "not-reviewed",
    reviewedAt: publicString(review?.reviewedAt, 80),
    unresolvedConcernCount: Number(review?.unresolvedConcernCount ?? 0),
    openConcernCount: Number(review?.openConcernCount ?? 0),
    reviewerIndependence: reviewerIndependenceSummary
  };
}
function publicRuntime(status) {
  const runtime = status.dashboard?.runtime ?? {};
  return {
    continuation: {
      continuationCount: Number(runtime.continuation?.continuationCount ?? 0),
      currentKind: publicString(runtime.continuation?.currentKind, 120),
      currentPacketId: publicString(runtime.continuation?.currentPacketId, 160),
      currentCommand: publicString(runtime.continuation?.currentCommand, 160),
      overview: publicString(runtime.continuation?.overview)
    },
    results: {
      runCount: Number(runtime.results?.runCount ?? 0),
      completedCount: Number(runtime.results?.completedCount ?? 0),
      errorCount: Number(runtime.results?.errorCount ?? 0),
      lastStatus: publicString(runtime.results?.lastStatus, 120),
      lastOutcome: publicString(runtime.results?.lastOutcome, 160),
      overview: publicString(runtime.results?.overview)
    },
    events: {
      eventCount: Number(runtime.events?.eventCount ?? 0),
      lastEventType: publicString(runtime.events?.lastEventType, 120),
      overview: publicString(runtime.events?.overview)
    }
  };
}
function publicTaskSection(tasks, limit) {
  return (Array.isArray(tasks) ? tasks : []).map(publicTask).filter(Boolean).slice(0, limit);
}
function publicDocumentEntry(entry) {
  if (!entry || typeof entry !== "object" || entry.publicSafe !== true) {
    return null;
  }
  return {
    id: publicString(entry.id, 160),
    packetId: publicString(entry.packetId, 160),
    documentId: publicString(entry.documentId, 160),
    title: publicString(entry.title, 220) ?? publicString(entry.documentId, 160),
    documentKind: publicString(entry.documentKind, 80),
    status: publicString(entry.status, 80),
    evidenceScope: publicString(entry.evidenceScope, 80),
    summary: publicString(entry.summary),
    artifactRefs: publicStringArray(entry.artifactRefs, 6),
    evidenceLinks: publicStringArray(entry.evidenceLinks, 6),
    updatedAt: publicString(entry.updatedAt, 80)
  };
}
function publicDocuments(root) {
  const ledger = normalizeDocumentLedgerIndex(readJson(root, ARTIFACT_PATHS.documentsLedger, createDocumentLedgerIndex));
  const publicSafeLedgerEntries = ledger.entries.filter((entry) => entry.publicSafe === true);
  const publicSafeEntries = publicSafeLedgerEntries.map(publicDocumentEntry).filter(Boolean).slice(-PUBLIC_RECENT_LIMIT);
  return {
    counts: {
      total: publicSafeLedgerEntries.length,
      internalEvidence: publicSafeLedgerEntries.filter((entry) => entry.evidenceScope === "internal").length,
      externalEvidence: publicSafeLedgerEntries.filter((entry) => entry.evidenceScope === "external").length,
      mixedEvidence: publicSafeLedgerEntries.filter((entry) => entry.evidenceScope === "mixed").length,
      publicSafe: publicSafeLedgerEntries.length
    },
    recentPublicSafe: publicSafeEntries,
    ledgerPath: ARTIFACT_PATHS.documentsLedger
  };
}
function countRuntimeEntries(root) {
  const results = readJson(root, ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex);
  const events = readJson(root, ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex);
  const taskPackets = readJson(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex);
  return {
    taskPacketIndexItems: Array.isArray(taskPackets.items) ? taskPackets.items.length : 0,
    runtimeResultEntries: Array.isArray(results.entries) ? results.entries.length : 0,
    runtimeEventEntries: Array.isArray(events.entries) ? events.entries.length : 0
  };
}
function markdownList(items, formatter) {
  if (!Array.isArray(items) || items.length === 0) {
    return "- \u6682\u65E0\n";
  }
  return items.map(formatter).join("\n") + "\n";
}
function renderMarkdown(snapshot) {
  const activeTasks = markdownList(snapshot.tasks.active, (task) => `- ${task.title} \`${task.id}\` \u2014 ${task.status}${task.nextAction ? `\uFF1B\u4E0B\u4E00\u6B65\uFF1A${task.nextAction}` : ""}`);
  const blockedTasks = markdownList(snapshot.tasks.blocked, (task) => `- ${task.title} \`${task.id}\` \u2014 ${task.blockedReason ?? task.boundary?.reason ?? "\u5DF2\u963B\u585E"}`);
  const recentCompleted = markdownList(snapshot.tasks.recentCompleted, (task) => `- ${task.title} \`${task.id}\`${task.completedAt ? ` \u2014 ${task.completedAt}` : ""}`);
  const actions = markdownList(snapshot.nextActions, (action) => `- ${action.title ?? action.command} ${action.command ? `\`${action.command}\`` : ""}${action.why ? ` \u2014 ${action.why}` : ""}`);
  const documents = markdownList(snapshot.documents.recentPublicSafe, (entry) => `- ${entry.title} \`${entry.documentId ?? entry.id}\` \u2014 ${entry.documentKind}/${entry.status}/${entry.evidenceScope}${entry.summary ? `\uFF1B${entry.summary}` : ""}`);
  return `# Dove \u9879\u76EE\u8FDB\u5C55

> \u81EA\u52A8\u751F\u6210\uFF1A${snapshot.generatedAt}
> \u6765\u6E90\uFF1ADove durable state \u7684\u516C\u5F00\u5B89\u5168\u6458\u8981\uFF1B\u4E0D\u5305\u542B raw transcripts\u3001\u79C1\u5BC6\u63A8\u7406\u3001\u73AF\u5883\u53D8\u91CF\u6216\u5B8C\u6574 .dove dump\u3002

## \u76EE\u6807

- \u9879\u76EE\uFF1A${snapshot.project.title ?? "\u672A\u8BBE\u7F6E"}
- \u76EE\u6807\uFF1A${snapshot.project.objective ?? "\u672A\u8BBE\u7F6E"}
- \u5F53\u524D\u7126\u70B9\uFF1A${snapshot.project.currentFocus ?? "\u6682\u65E0"}
- \u4E0B\u4E00\u6B65\uFF1A${snapshot.project.nextAction ?? "project:dove.status"}

## \u8FDB\u5C55\u6982\u89C8

- \u6D3B\u8DC3\u4EFB\u52A1\uFF1A${snapshot.progress.counts.active}
- \u963B\u585E\u4EFB\u52A1\uFF1A${snapshot.progress.counts.blocked}
- \u5DF2\u5B8C\u6210\u4EFB\u52A1\uFF1A${snapshot.progress.counts.completed}
- \u5DF2\u6740\u6B7B\u4EFB\u52A1\uFF1A${snapshot.progress.counts.killed}
- \u5DF2\u5F52\u6863\u4EFB\u52A1\uFF1A${snapshot.progress.counts.archived}${snapshot.progress.counts.archivedHidden ? `\uFF08\u9ED8\u8BA4\u9690\u85CF ${snapshot.progress.counts.archivedHidden}\uFF09` : ""}
- Review verdict\uFF1A${snapshot.progress.review.verdict}
- Runtime\uFF1A${snapshot.progress.runtime.results.lastStatus ?? "never-run"}/${snapshot.progress.runtime.results.lastOutcome ?? "not-started"}
- \u516C\u5F00\u5B89\u5168\u6587\u6863/\u8BC1\u636E\uFF1A${snapshot.documents.counts.publicSafe}/${snapshot.documents.counts.total}

## \u5F53\u524D\u6D3B\u8DC3\u4EFB\u52A1

${activeTasks}
## \u963B\u585E / \u8FB9\u754C

${blockedTasks}
## \u6700\u8FD1\u5B8C\u6210

${recentCompleted}
## \u5EFA\u8BAE\u4E0B\u4E00\u6B65

${actions}
## \u516C\u5F00\u6587\u6863 / \u8BC1\u636E\u6458\u8981

${documents}
## \u516C\u5F00\u8FB9\u754C

- \u4EC5\u516C\u5F00\u6D3E\u751F\u6458\u8981\uFF0C\u4E0D\u516C\u5F00 raw runtime entries\u3001raw document ledger entries \u6216\u6587\u6863\u6B63\u6587\u3002
- \u4E0D\u516C\u5F00 Claude/host transcript\u3001\u9690\u85CF\u63A8\u7406\u3001\u73AF\u5883\u53D8\u91CF\u3001token\u3001\u5BC6\u7801\u6216\u5B8C\u6574 .dove \u5185\u5BB9\u3002
- \u82E5\u8981\u5916\u7F51\u8BBF\u95EE\uFF0C\u5EFA\u8BAE\u53EA\u66B4\u9732 \`.dove/public\`\uFF0C\u5E76\u5728 Cloudflare/\u53CD\u4EE3\u5C42\u52A0\u8BBF\u95EE\u63A7\u5236\u3002
`;
}
function escapeHtml(value) {
  return String(value ?? "").replace(/[&<>"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" })[char]);
}
function htmlTaskList(tasks) {
  if (!tasks.length) {
    return "<li>\u6682\u65E0</li>";
  }
  return tasks.map((task) => `<li><strong>${escapeHtml(task.title)}</strong> <code>${escapeHtml(task.id)}</code><br><span>${escapeHtml(task.status)}${task.nextAction ? ` \xB7 \u4E0B\u4E00\u6B65\uFF1A${escapeHtml(task.nextAction)}` : ""}</span>${task.summary ? `<p>${escapeHtml(task.summary)}</p>` : ""}</li>`).join("\n");
}
function htmlActionList(actions) {
  if (!actions.length) {
    return "<li>\u6682\u65E0</li>";
  }
  return actions.map((action) => `<li><strong>${escapeHtml(action.title ?? action.command)}</strong>${action.command ? ` <code>${escapeHtml(action.command)}</code>` : ""}${action.why ? `<p>${escapeHtml(action.why)}</p>` : ""}</li>`).join("\n");
}
function htmlDocumentList(documents) {
  if (!documents.length) {
    return "<li>\u6682\u65E0</li>";
  }
  return documents.map((entry) => `<li><strong>${escapeHtml(entry.title)}</strong> <code>${escapeHtml(entry.documentId ?? entry.id)}</code><br><span>${escapeHtml(entry.documentKind)}/${escapeHtml(entry.status)} \xB7 ${escapeHtml(entry.evidenceScope)}</span>${entry.summary ? `<p>${escapeHtml(entry.summary)}</p>` : ""}</li>`).join("\n");
}
function renderHtml(snapshot) {
  return `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>${escapeHtml(snapshot.project.title ?? "Dove \u9879\u76EE\u8FDB\u5C55")}</title>
  <style>
    :root { color-scheme: light dark; font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
    body { margin: 0; background: #0f172a; color: #e2e8f0; }
    main { max-width: 980px; margin: 0 auto; padding: 32px 20px 56px; }
    a { color: #93c5fd; }
    .hero, section { background: rgba(15, 23, 42, 0.82); border: 1px solid rgba(148, 163, 184, 0.28); border-radius: 18px; padding: 20px; margin: 16px 0; box-shadow: 0 18px 48px rgba(15, 23, 42, 0.28); }
    h1, h2 { margin: 0 0 12px; }
    .meta, .muted { color: #94a3b8; }
    .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 12px; }
    .metric { background: rgba(30, 41, 59, 0.72); border-radius: 14px; padding: 14px; }
    .metric strong { display: block; font-size: 1.8rem; }
    li { margin: 0 0 12px; }
    code { background: rgba(148, 163, 184, 0.18); border-radius: 6px; padding: 2px 6px; }
  </style>
</head>
<body>
<main>
  <div class="hero">
    <p class="meta">Dove public status \xB7 ${escapeHtml(snapshot.generatedAt)}</p>
    <h1>${escapeHtml(snapshot.project.title ?? "Dove \u9879\u76EE\u8FDB\u5C55")}</h1>
    <p>${escapeHtml(snapshot.project.objective ?? "\u672A\u8BBE\u7F6E\u9879\u76EE\u76EE\u6807")}</p>
    <p><strong>\u5F53\u524D\u7126\u70B9\uFF1A</strong>${escapeHtml(snapshot.project.currentFocus ?? "\u6682\u65E0")}</p>
    <p><strong>\u4E0B\u4E00\u6B65\uFF1A</strong><code>${escapeHtml(snapshot.project.nextAction ?? "project:dove.status")}</code></p>
    <p><a href="status.json">status.json</a> \xB7 <a href="status.md">status.md</a></p>
  </div>

  <section>
    <h2>\u8FDB\u5C55\u6982\u89C8</h2>
    <div class="grid">
      <div class="metric"><strong>${snapshot.progress.counts.active}</strong><span>\u6D3B\u8DC3</span></div>
      <div class="metric"><strong>${snapshot.progress.counts.blocked}</strong><span>\u963B\u585E</span></div>
      <div class="metric"><strong>${snapshot.progress.counts.completed}</strong><span>\u5B8C\u6210</span></div>
      <div class="metric"><strong>${snapshot.progress.counts.killed}</strong><span>\u6740\u6B7B</span></div>
      <div class="metric"><strong>${snapshot.progress.counts.archived}${snapshot.progress.counts.archivedHidden ? ` / ${snapshot.progress.counts.archivedHidden}` : ""}</strong><span>\u5F52\u6863 / \u9ED8\u8BA4\u9690\u85CF</span></div>
      <div class="metric"><strong>${snapshot.documents.counts.publicSafe}/${snapshot.documents.counts.total}</strong><span>\u516C\u5F00\u6587\u6863/\u8BC1\u636E</span></div>
    </div>
    <p class="muted">Review: ${escapeHtml(snapshot.progress.review.verdict)} \xB7 Runtime: ${escapeHtml(snapshot.progress.runtime.results.lastStatus ?? "never-run")}/${escapeHtml(snapshot.progress.runtime.results.lastOutcome ?? "not-started")}</p>
  </section>

  <section><h2>\u5F53\u524D\u6D3B\u8DC3\u4EFB\u52A1</h2><ul>${htmlTaskList(snapshot.tasks.active)}</ul></section>
  <section><h2>\u963B\u585E / \u8FB9\u754C</h2><ul>${htmlTaskList(snapshot.tasks.blocked)}</ul></section>
  <section><h2>\u6700\u8FD1\u5B8C\u6210</h2><ul>${htmlTaskList(snapshot.tasks.recentCompleted)}</ul></section>
  <section><h2>\u5EFA\u8BAE\u4E0B\u4E00\u6B65</h2><ul>${htmlActionList(snapshot.nextActions)}</ul></section>
  <section><h2>\u516C\u5F00\u6587\u6863 / \u8BC1\u636E\u6458\u8981</h2><ul>${htmlDocumentList(snapshot.documents.recentPublicSafe)}</ul></section>
  <section><h2>\u516C\u5F00\u8FB9\u754C</h2><p class="muted">\u6B64\u9875\u9762\u53EA\u53D1\u5E03 Dove durable state \u7684\u8131\u654F\u6458\u8981\uFF0C\u4E0D\u53D1\u5E03 raw transcripts\u3001raw document ledger entries\u3001\u6587\u6863\u6B63\u6587\u3001\u79C1\u5BC6\u63A8\u7406\u3001\u73AF\u5883\u53D8\u91CF\u3001token\u3001\u5BC6\u7801\u6216\u5B8C\u6574 .dove \u5185\u5BB9\u3002\u5916\u7F51\u8BBF\u95EE\u65F6\u5EFA\u8BAE\u53EA\u66B4\u9732 <code>.dove/public</code> \u5E76\u542F\u7528\u8BBF\u95EE\u63A7\u5236\u3002</p></section>
</main>
</body>
</html>
`;
}
function isPlainObject4(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function arrayValue(value) {
  if (Array.isArray(value)) {
    return value;
  }
  return value === void 0 || value === null ? [] : [value];
}
function slugify5(value, fallback) {
  const raw = publicString(value, 160) ?? fallback;
  const slug = String(raw ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
  return slug || fallback;
}
function dedupeProjects(projects) {
  const byRoot = /* @__PURE__ */ new Map();
  for (const project of projects) {
    byRoot.set(project.root, project);
  }
  return Array.from(byRoot.values());
}
function withCollisionSafeSlugs(projects) {
  const used = /* @__PURE__ */ new Map();
  return projects.map((project, index) => {
    const base = slugify5(project.slug ?? project.id ?? project.title ?? path17.basename(project.root), `project-${index + 1}`);
    const count = used.get(base) ?? 0;
    used.set(base, count + 1);
    const slug = count === 0 ? base : `${base}-${count + 1}`;
    return {
      ...project,
      id: project.id ?? slug,
      slug,
      title: project.title ?? path17.basename(project.root)
    };
  });
}
function resolveGlobalStatusSelection(root, options = {}) {
  const env = options.env ?? process.env;
  const config = loadDoveConfig(root, env);
  const explicitProjects = normalizeGlobalStatusProjects([
    ...arrayValue(options.projects),
    ...arrayValue(options.projectRoots).map((projectRoot) => ({ root: projectRoot })),
    ...arrayValue(options.projectRoot).map((projectRoot) => ({ root: projectRoot }))
  ]);
  const configuredProjects = config.globalStatus.projects;
  const selected = explicitProjects.length > 0 ? options.includeConfig ? [...configuredProjects, ...explicitProjects] : explicitProjects : configuredProjects;
  return {
    env,
    config,
    outputDir: resolveDoveGlobalStatusOutputDir(options.outputDir ?? config.globalStatus.outputDir, env),
    projects: withCollisionSafeSlugs(dedupeProjects(selected))
  };
}
function readProjectPublicStatus(project) {
  const jsonPath = path17.join(project.root, ARTIFACT_PATHS.publicStatusJson);
  if (!fs15.existsSync(jsonPath)) {
    return { status: "missing", project, snapshot: null, reason: `${ARTIFACT_PATHS.publicStatusJson} is missing` };
  }
  try {
    const snapshot = JSON.parse(fs15.readFileSync(jsonPath, "utf8"));
    if (!isPlainObject4(snapshot) || snapshot.mode !== "dove-public-status") {
      return { status: "invalid", project, snapshot: null, reason: "status.json is not a Dove public status snapshot" };
    }
    return { status: "published", project, snapshot, reason: null };
  } catch {
    return { status: "invalid", project, snapshot: null, reason: "status.json could not be parsed" };
  }
}
function projectLinks(slug) {
  return {
    html: `projects/${slug}/index.html`,
    json: `projects/${slug}/status.json`,
    markdown: `projects/${slug}/status.md`
  };
}
function publicProjectCard(readResult) {
  const { project, snapshot, status, reason } = readResult;
  const title = publicString(project.title ?? snapshot?.project?.title, 220) ?? project.slug;
  return {
    id: publicString(project.id, 160) ?? project.slug,
    slug: project.slug,
    title,
    status,
    generatedAt: publicString(snapshot?.generatedAt, 80),
    summary: status === "published" ? firstPublicString(snapshot?.project?.currentFocus, snapshot?.project?.objective, snapshot?.project?.nextAction) : publicString(reason, 220),
    project: status === "published" ? {
      title: publicString(snapshot?.project?.title, 220),
      objective: publicString(snapshot?.project?.objective),
      currentFocus: publicString(snapshot?.project?.currentFocus),
      nextAction: publicString(snapshot?.project?.nextAction, 160)
    } : null,
    progress: status === "published" ? {
      counts: snapshot?.progress?.counts ?? {},
      review: publicReview(snapshot?.progress?.review),
      runtime: snapshot?.progress?.runtime ?? {}
    } : null,
    documents: status === "published" ? snapshot?.documents ?? null : null,
    links: projectLinks(project.slug)
  };
}
function buildGlobalStatusSnapshotFromReadResults(readResults, generatedAt) {
  const projectCards = readResults.map(publicProjectCard);
  const counts = {
    configured: readResults.length,
    published: projectCards.filter((project) => project.status === "published").length,
    missing: projectCards.filter((project) => project.status === "missing").length,
    invalid: projectCards.filter((project) => project.status === "invalid").length,
    skipped: projectCards.filter((project) => project.status === "skipped").length
  };
  return {
    version: GLOBAL_PUBLIC_STATUS_VERSION,
    mode: "dove-global-public-status",
    generatedAt,
    counts,
    projects: projectCards,
    publicArtifacts: {
      json: "status.json",
      markdown: "status.md",
      html: "index.html",
      projectsDir: "projects"
    },
    privacy: {
      sanitized: true,
      derivedOnly: true,
      rawDoveDumpIncluded: false,
      absoluteRootsIncluded: false,
      transcriptsIncluded: false,
      privateReasoningIncluded: false,
      environmentIncluded: false,
      runtimeEntriesIncluded: false,
      documentLedgerRawEntriesIncluded: false,
      documentBodiesIncluded: false,
      cloudflareTunnelStarted: false
    }
  };
}
function renderGlobalMarkdown(snapshot) {
  const projects = markdownList(snapshot.projects, (project) => {
    const counts = project.progress?.counts ?? {};
    const active = Number(counts.active ?? 0);
    const blocked = Number(counts.blocked ?? 0);
    const completed = Number(counts.completed ?? 0);
    const archived = Number(counts.archived ?? 0);
    const archivedHidden = Number(counts.archivedHidden ?? 0);
    const archiveSummary = archived > 0 || archivedHidden > 0 ? ` / \u5F52\u6863 ${archived}${archivedHidden ? `\uFF08\u9690\u85CF ${archivedHidden}\uFF09` : ""}` : "";
    return `- [${project.title}](${project.links.html}) \u2014 ${project.status}\uFF1B\u6D3B\u8DC3 ${active} / \u963B\u585E ${blocked} / \u5B8C\u6210 ${completed}${archiveSummary}${project.summary ? `\uFF1B${project.summary}` : ""}`;
  });
  return `# Dove \u5168\u5C40\u9879\u76EE\u8FDB\u5C55

> \u81EA\u52A8\u751F\u6210\uFF1A${snapshot.generatedAt}
> \u6765\u6E90\uFF1A\u5404\u9879\u76EE \`.dove/public\` \u7684\u516C\u5F00\u5B89\u5168\u6458\u8981\uFF1B\u4E0D\u5305\u542B\u672C\u673A\u7EDD\u5BF9\u8DEF\u5F84\u3001raw .dove dump\u3001transcript\u3001\u79C1\u5BC6\u63A8\u7406\u3001\u73AF\u5883\u53D8\u91CF\u6216\u6587\u6863\u6B63\u6587\u3002

## \u6982\u89C8

- \u5DF2\u914D\u7F6E\u9879\u76EE\uFF1A${snapshot.counts.configured}
- \u5DF2\u53D1\u5E03\u9879\u76EE\uFF1A${snapshot.counts.published}
- \u7F3A\u5931 public status\uFF1A${snapshot.counts.missing}
- \u65E0\u6548 public status\uFF1A${snapshot.counts.invalid}

## \u9879\u76EE

${projects}
## \u516C\u5F00\u8FB9\u754C

- \u8FD9\u4E2A\u5168\u5C40\u9875\u9762\u53EA\u805A\u5408\u6BCF\u4E2A\u9879\u76EE\u5DF2\u7ECF\u516C\u5F00\u5B89\u5168\u7684 \`.dove/public/status.*\`\u3002
- \u4E0D\u626B\u63CF\u6574\u53F0\u7535\u8111\uFF0C\u4E0D\u542F\u52A8 HTTP server \u6216 Cloudflare tunnel\u3002
- \u82E5\u8981\u5916\u7F51\u8BBF\u95EE\uFF0C\u5EFA\u8BAE\u53EA\u66B4\u9732\u8FD9\u4E2A\u5168\u5C40 public \u76EE\u5F55\uFF0C\u5E76\u5728 Cloudflare/\u53CD\u4EE3\u5C42\u52A0\u8BBF\u95EE\u63A7\u5236\u3002
`;
}
function htmlProjectCards(projects) {
  if (!projects.length) {
    return "<li>\u6682\u65E0\u5DF2\u914D\u7F6E\u9879\u76EE</li>";
  }
  return projects.map((project) => {
    const counts = project.progress?.counts ?? {};
    const archived = Number(counts.archived ?? 0);
    const archivedHidden = Number(counts.archivedHidden ?? 0);
    const archiveSummary = archived > 0 || archivedHidden > 0 ? ` \xB7 \u5F52\u6863 ${archived}${archivedHidden ? `\uFF08\u9690\u85CF ${archivedHidden}\uFF09` : ""}` : "";
    return `<li><strong><a href="${escapeHtml(project.links.html)}">${escapeHtml(project.title)}</a></strong> <code>${escapeHtml(project.status)}</code><br><span>\u6D3B\u8DC3 ${escapeHtml(counts.active ?? 0)} \xB7 \u963B\u585E ${escapeHtml(counts.blocked ?? 0)} \xB7 \u5B8C\u6210 ${escapeHtml(counts.completed ?? 0)}${escapeHtml(archiveSummary)}</span>${project.summary ? `<p>${escapeHtml(project.summary)}</p>` : ""}</li>`;
  }).join("\n");
}
function renderGlobalHtml(snapshot) {
  return `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Dove \u5168\u5C40\u9879\u76EE\u8FDB\u5C55</title>
  <style>
    :root { color-scheme: light dark; font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
    body { margin: 0; background: #0f172a; color: #e2e8f0; }
    main { max-width: 980px; margin: 0 auto; padding: 32px 20px 56px; }
    a { color: #93c5fd; }
    .hero, section { background: rgba(15, 23, 42, 0.82); border: 1px solid rgba(148, 163, 184, 0.28); border-radius: 18px; padding: 20px; margin: 16px 0; box-shadow: 0 18px 48px rgba(15, 23, 42, 0.28); }
    h1, h2 { margin: 0 0 12px; }
    .meta, .muted { color: #94a3b8; }
    .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 12px; }
    .metric { background: rgba(30, 41, 59, 0.72); border-radius: 14px; padding: 14px; }
    .metric strong { display: block; font-size: 1.8rem; }
    li { margin: 0 0 14px; }
    code { background: rgba(148, 163, 184, 0.18); border-radius: 6px; padding: 2px 6px; }
  </style>
</head>
<body>
<main>
  <div class="hero">
    <p class="meta">Dove global public status \xB7 ${escapeHtml(snapshot.generatedAt)}</p>
    <h1>Dove \u5168\u5C40\u9879\u76EE\u8FDB\u5C55</h1>
    <p>\u805A\u5408\u6240\u6709\u663E\u5F0F\u6CE8\u518C\u9879\u76EE\u7684\u516C\u5F00\u5B89\u5168\u72B6\u6001\u9875\u3002</p>
    <p><a href="status.json">status.json</a> \xB7 <a href="status.md">status.md</a></p>
  </div>
  <section>
    <h2>\u6982\u89C8</h2>
    <div class="grid">
      <div class="metric"><strong>${snapshot.counts.configured}</strong><span>\u5DF2\u914D\u7F6E</span></div>
      <div class="metric"><strong>${snapshot.counts.published}</strong><span>\u5DF2\u53D1\u5E03</span></div>
      <div class="metric"><strong>${snapshot.counts.missing}</strong><span>\u7F3A\u5931</span></div>
      <div class="metric"><strong>${snapshot.counts.invalid}</strong><span>\u65E0\u6548</span></div>
    </div>
  </section>
  <section><h2>\u9879\u76EE</h2><ul>${htmlProjectCards(snapshot.projects)}</ul></section>
  <section><h2>\u516C\u5F00\u8FB9\u754C</h2><p class="muted">\u6B64\u9875\u9762\u53EA\u805A\u5408\u5404\u9879\u76EE\u5DF2\u53D1\u5E03\u7684\u516C\u5F00\u5B89\u5168\u6458\u8981\uFF0C\u4E0D\u5305\u542B\u672C\u673A\u7EDD\u5BF9\u8DEF\u5F84\u3001raw .dove dump\u3001transcript\u3001\u79C1\u5BC6\u63A8\u7406\u3001\u73AF\u5883\u53D8\u91CF\u3001token\u3001\u5BC6\u7801\u6216\u6587\u6863\u6B63\u6587\u3002Dove \u4E0D\u4F1A\u81EA\u52A8\u542F\u52A8 HTTP server \u6216 Cloudflare tunnel\u3002</p></section>
</main>
</body>
</html>
`;
}
function projectPlaceholderMarkdown(project, status, reason) {
  const title = publicString(project.title, 220) ?? project.slug;
  return `# ${title}

- \u72B6\u6001\uFF1A${status}
- \u539F\u56E0\uFF1A${publicString(reason, 220) ?? "\u9879\u76EE public status \u4E0D\u53EF\u7528"}
`;
}
function projectPlaceholderHtml(project, status, reason) {
  const title = publicString(project.title, 220) ?? project.slug;
  return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${escapeHtml(title)}</title></head><body><main><h1>${escapeHtml(title)}</h1><p>\u72B6\u6001\uFF1A${escapeHtml(status)}</p><p>${escapeHtml(publicString(reason, 220) ?? "\u9879\u76EE public status \u4E0D\u53EF\u7528")}</p><p><a href="../../index.html">\u8FD4\u56DE\u5168\u5C40\u9996\u9875</a></p></main></body></html>
`;
}
function ensureAbsoluteDir(dirPath) {
  fs15.mkdirSync(dirPath, { recursive: true });
}
function writeAbsoluteJson(filePath, value) {
  ensureAbsoluteDir(path17.dirname(filePath));
  fs15.writeFileSync(filePath, `${JSON.stringify(value, null, 2)}
`, "utf8");
}
function writeAbsoluteText(filePath, value) {
  ensureAbsoluteDir(path17.dirname(filePath));
  fs15.writeFileSync(filePath, value, "utf8");
}
function projectRelativeOutputPath(root, filePath) {
  const relativePath = path17.relative(path17.resolve(root), path17.resolve(filePath));
  if (!relativePath || relativePath.startsWith("..") || path17.isAbsolute(relativePath)) {
    return null;
  }
  return relativePath.split(path17.sep).join("/");
}
function writeOutputJson(root, filePath, value) {
  const relativePath = projectRelativeOutputPath(root, filePath);
  if (relativePath) {
    writeJson(root, relativePath, value);
    return;
  }
  if (isPatchPlanMode(root)) {
    throw new Error("publish_dove_global_status patch-plan requires outputDir to stay inside the target project.");
  }
  writeAbsoluteJson(filePath, value);
}
function writeOutputText(root, filePath, value) {
  const relativePath = projectRelativeOutputPath(root, filePath);
  if (relativePath) {
    writeText(root, relativePath, value);
    return;
  }
  if (isPatchPlanMode(root)) {
    throw new Error("publish_dove_global_status patch-plan requires outputDir to stay inside the target project.");
  }
  writeAbsoluteText(filePath, value);
}
function readPublicText(project, relativePath, fallback) {
  const fullPath = path17.join(project.root, relativePath);
  try {
    return fs15.existsSync(fullPath) ? fs15.readFileSync(fullPath, "utf8") : fallback;
  } catch {
    return fallback;
  }
}
function writeGlobalProjectArtifacts(root, outputDir, readResult) {
  const { project, snapshot, status, reason } = readResult;
  const projectDir = path17.join(outputDir, "projects", project.slug);
  const projectSnapshot = snapshot ?? {
    version: 1,
    mode: "dove-global-project-placeholder",
    status,
    title: publicString(project.title, 220) ?? project.slug,
    generatedAt: null,
    summary: publicString(reason, 220),
    links: projectLinks(project.slug),
    privacy: {
      sanitized: true,
      derivedOnly: true,
      absoluteRootsIncluded: false
    }
  };
  const markdown = snapshot ? readPublicText(project, ARTIFACT_PATHS.publicStatusMarkdown, projectPlaceholderMarkdown(project, status, reason)) : projectPlaceholderMarkdown(project, status, reason);
  const html = snapshot ? readPublicText(project, ARTIFACT_PATHS.publicStatusHtml, projectPlaceholderHtml(project, status, reason)) : projectPlaceholderHtml(project, status, reason);
  writeOutputJson(root, path17.join(projectDir, "status.json"), projectSnapshot);
  writeOutputText(root, path17.join(projectDir, "status.md"), markdown);
  writeOutputText(root, path17.join(projectDir, "index.html"), html);
  return [
    path17.join(projectDir, "status.json"),
    path17.join(projectDir, "status.md"),
    path17.join(projectDir, "index.html")
  ].map((filePath) => path17.relative(outputDir, filePath).split(path17.sep).join("/"));
}
function refreshGlobalProjectPublicStatus(project, options = {}) {
  try {
    if (!fs15.existsSync(project.root) || !fs15.statSync(project.root).isDirectory()) {
      return { status: "skipped", project, snapshot: null, reason: "project root is missing" };
    }
    publishDoveStatus(project.root, {
      includeArchived: Boolean(options.includeArchived),
      responseLanguage: options.responseLanguage,
      generatedAt: options.generatedAt
    });
    return null;
  } catch {
    return { status: "skipped", project, snapshot: null, reason: "project public status refresh failed" };
  }
}
function publishDoveGlobalStatus(root, options = {}) {
  assertGovernanceMutationRegistered("publish-dove-global-status", "exempt");
  if (options.refresh && isPatchPlanMode(root)) {
    throw new Error("publish_dove_global_status patch-plan does not support refresh; publish each project status with patch-plan before aggregating.");
  }
  const selection = resolveGlobalStatusSelection(root, options);
  const refreshResults = [];
  const readResults = selection.projects.map((project) => {
    const refreshFailure = options.refresh ? refreshGlobalProjectPublicStatus(project, options) : null;
    if (refreshFailure) {
      refreshResults.push({ slug: project.slug, title: publicString(project.title, 220), status: refreshFailure.status, reason: refreshFailure.reason });
      return refreshFailure;
    }
    if (options.refresh) {
      refreshResults.push({ slug: project.slug, title: publicString(project.title, 220), status: "refreshed" });
    }
    return readProjectPublicStatus(project);
  });
  const snapshot = buildGlobalStatusSnapshotFromReadResults(readResults, options.generatedAt ?? nowIso());
  const markdown = renderGlobalMarkdown(snapshot);
  const html = renderGlobalHtml(snapshot);
  const writes = [];
  writeOutputJson(root, path17.join(selection.outputDir, "status.json"), snapshot);
  writes.push("status.json");
  writeOutputText(root, path17.join(selection.outputDir, "status.md"), markdown);
  writes.push("status.md");
  writeOutputText(root, path17.join(selection.outputDir, "index.html"), html);
  writes.push("index.html");
  for (const readResult of readResults) {
    writes.push(...writeGlobalProjectArtifacts(root, selection.outputDir, readResult));
  }
  return {
    mode: "dove-global-public-status-publish",
    status: "published",
    generatedAt: snapshot.generatedAt,
    outputDir: selection.outputDir,
    projectCount: selection.projects.length,
    refresh: Boolean(options.refresh),
    refreshResults,
    writes,
    publicArtifacts: snapshot.publicArtifacts,
    snapshot,
    privacy: snapshot.privacy,
    noDaemon: true,
    noScheduler: true,
    noExternalProcess: true,
    cloudflareTunnelStarted: false
  };
}
function buildDovePublicStatus(root, options = {}) {
  ensureWorkspace(root);
  const status = queryDoveStatus(root, {
    includeArchived: Boolean(options.includeArchived),
    responseLanguage: options.responseLanguage,
    detail: "full"
  });
  const tasks = status.dashboard?.tasks ?? {};
  const project = status.dashboard?.project ?? {};
  const generatedAt = options.generatedAt ?? nowIso();
  return {
    version: PUBLIC_STATUS_VERSION,
    mode: "dove-public-status",
    generatedAt,
    responseLanguage: status.responseLanguage,
    project: {
      title: publicString(project.title ?? status.projectSummary?.title),
      objective: publicString(project.objective ?? status.projectSummary?.objective),
      currentFocus: publicString(project.currentFocus ?? status.projectSummary?.focus),
      nextAction: publicString(project.nextAction ?? status.suggestedNextCommand, 160),
      durableRoot: ARTIFACT_PATHS.doveRoot
    },
    requirements: {
      evidenceExpectations: publicStringArray(status.dashboard?.init?.evidenceExpectations),
      acceptanceSource: "durable-task-packets"
    },
    documents: publicDocuments(root),
    progress: {
      returnStatus: publicString(status.dashboard?.returnReadiness?.status, 80),
      counts: {
        total: Number(tasks.counts?.total ?? 0),
        active: Number(tasks.counts?.active ?? 0),
        blocked: Number(tasks.counts?.blocked ?? 0),
        completed: Number(tasks.counts?.completed ?? 0),
        killed: Number(tasks.counts?.killed ?? 0),
        archived: Number(tasks.counts?.archived ?? 0),
        archivedHidden: Number(tasks.counts?.archivedHidden ?? 0),
        byStatus: tasks.counts?.byStatus ?? {}
      },
      review: publicReview(status.dashboard?.review),
      lessons: {
        activeLessonCount: Number(status.dashboard?.lessons?.activeLessonCount ?? 0),
        mustObeyLessonCount: Number(status.dashboard?.lessons?.mustObeyLessonCount ?? 0),
        lessonsPath: publicString(status.dashboard?.lessons?.lessonsPath, 220)
      },
      versions: status.dashboard?.versions ?? {},
      experiments: status.dashboard?.experiments ?? {},
      runtime: publicRuntime(status)
    },
    tasks: {
      init: publicTask(status.dashboard?.init),
      active: publicTaskSection(tasks.active, PUBLIC_TASK_LIMIT),
      blocked: publicTaskSection(tasks.blocked, PUBLIC_TASK_LIMIT),
      recentCompleted: publicTaskSection(tasks.recentCompleted, PUBLIC_RECENT_LIMIT)
    },
    nextActions: (Array.isArray(status.dailyHome?.nextActions) ? status.dailyHome.nextActions : []).map(publicAction).filter(Boolean),
    sourceSummary: {
      ...countRuntimeEntries(root),
      primaryStateSources: [
        ARTIFACT_PATHS.state,
        ARTIFACT_PATHS.taskPacketsIndex,
        ARTIFACT_PATHS.runtimeResults,
        ARTIFACT_PATHS.runtimeEvents,
        ARTIFACT_PATHS.documentsLedger,
        ARTIFACT_PATHS.reviewState,
        ARTIFACT_PATHS.metaOperatorLessons
      ]
    },
    publicArtifacts: {
      json: ARTIFACT_PATHS.publicStatusJson,
      markdown: ARTIFACT_PATHS.publicStatusMarkdown,
      html: ARTIFACT_PATHS.publicStatusHtml
    },
    privacy: {
      sanitized: true,
      derivedOnly: true,
      rawDoveDumpIncluded: false,
      transcriptsIncluded: false,
      privateReasoningIncluded: false,
      environmentIncluded: false,
      runtimeEntriesIncluded: false,
      documentLedgerRawEntriesIncluded: false,
      documentBodiesIncluded: false,
      cloudflareTunnelStarted: false
    }
  };
}
function publishDoveStatus(root, options = {}) {
  assertGovernanceMutationRegistered("publish-dove-status", "exempt");
  const snapshot = buildDovePublicStatus(root, options);
  const markdown = renderMarkdown(snapshot);
  const html = renderHtml(snapshot);
  writeJson(root, ARTIFACT_PATHS.publicStatusJson, snapshot);
  writeText(root, ARTIFACT_PATHS.publicStatusMarkdown, markdown);
  writeText(root, ARTIFACT_PATHS.publicStatusHtml, html);
  return {
    mode: "dove-public-status-publish",
    status: "published",
    generatedAt: snapshot.generatedAt,
    writes: [ARTIFACT_PATHS.publicStatusJson, ARTIFACT_PATHS.publicStatusMarkdown, ARTIFACT_PATHS.publicStatusHtml],
    publicArtifacts: snapshot.publicArtifacts,
    snapshot,
    privacy: snapshot.privacy,
    noDaemon: true,
    noScheduler: true,
    noExternalProcess: true,
    cloudflareTunnelStarted: false
  };
}

// src/core/global-status-serving.mjs
import { spawn, spawnSync } from "node:child_process";
import crypto9 from "node:crypto";
import fs16 from "node:fs";
import http from "node:http";
import os2 from "node:os";
import path18 from "node:path";
var AUTH_COOKIE_MAX_AGE_SECONDS = 7 * 24 * 60 * 60;

// src/core/documents.mjs
import fs17 from "node:fs";
import path19 from "node:path";
function slugify6(value, fallback = "document") {
  return String(value ?? fallback).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || fallback;
}
function normalizeString6(value, fallback = "") {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray14(value) {
  const source = Array.isArray(value) ? value : typeof value === "string" ? [value] : [];
  return Array.from(new Set(source.map((item) => String(item).trim()).filter(Boolean)));
}
function normalizeAllowed2(value, allowed, fallback) {
  const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
  return allowed.includes(normalized) ? normalized : fallback;
}
function normalizeRelativePath3(value) {
  const text3 = normalizeString6(value, null);
  if (!text3) {
    return null;
  }
  const normalized = text3.replace(/\\/g, "/").replace(/^\/+/, "");
  if (normalized.split("/").includes("..")) {
    return null;
  }
  return normalized;
}
function assertWritableDocumentPath(relativePath) {
  if (!relativePath || !relativePath.startsWith(`${ARTIFACT_PATHS.documentsDir}/`) || relativePath === ARTIFACT_PATHS.documentsLedger) {
    throw new Error(`Document writes must target ${ARTIFACT_PATHS.documentsDir}/ and cannot overwrite the ledger.`);
  }
}
function uniqueRelativePath(root, relativePath) {
  const parsed = path19.posix.parse(relativePath);
  let candidate = relativePath;
  let suffix = 2;
  while (fs17.existsSync(resolvePath(root, candidate))) {
    candidate = path19.posix.join(parsed.dir, `${parsed.name}-${suffix}${parsed.ext || ".md"}`);
    suffix += 1;
  }
  return candidate;
}
function generatedDocumentPath(timestamp, documentKind, title) {
  const safeTime = timestamp.replace(/[:.]/g, "-");
  return path19.posix.join(ARTIFACT_PATHS.documentsDir, documentKind, `${safeTime}-${slugify6(title)}.md`);
}
function uniqueEntryId(entries, seed) {
  const existing = new Set(entries.map((entry) => entry.id));
  let candidate = slugify6(seed, "document-entry");
  let suffix = 2;
  while (existing.has(candidate)) {
    candidate = `${slugify6(seed, "document-entry")}-${suffix}`;
    suffix += 1;
  }
  return candidate;
}
function publicSafeFromArgs(args = {}) {
  return args.publicSafe === true || args.visibility === "public-safe";
}
function documentGuidanceSummary(root, args = {}, target = {}, entry = {}) {
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: "dove.documents",
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: args.title ?? args.documentTitle ?? args.summary ?? entry.title ?? null,
    roleId: "builder",
    subagentSpecialty: "researcher",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "execute",
      primaryRole: "builder"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: "project:dove.status",
    routeHint: "project:dove.status",
    workflowKind: "document-evidence",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "execute",
    tags: ["document", "evidence", "provenance"],
    statusSummary: {
      documentId: entry.documentId ?? null,
      documentKind: entry.documentKind ?? null,
      evidenceScope: entry.evidenceScope ?? null,
      publicSafe: entry.publicSafe === true
    }
  }));
}
function buildResultCard({ entry, writes, createdDocument, appendedDocument, responseLanguage }) {
  const evidence = [...entry.evidenceLinks, ...entry.artifactRefs, ...entry.sourceRefs];
  return buildCommandResultCard({
    surface: "dove.documents",
    command: "record_document_evidence",
    title: entry.title,
    status: "recorded",
    happened: createdDocument ? "\u5DF2\u65B0\u5EFA\u5F52\u6863\u6587\u6863\u5E76\u8BB0\u5F55\u6587\u6863\u8BC1\u636E\u3002" : appendedDocument ? "\u5DF2\u8FFD\u52A0\u5F52\u6863\u6587\u6863\u5E76\u8BB0\u5F55\u6587\u6863\u8BC1\u636E\u3002" : "\u5DF2\u8BB0\u5F55\u6587\u6863\u8BC1\u636E\u7D22\u5F15\uFF0C\u4E0D\u8986\u76D6\u539F\u59CB\u6587\u6863\u3002",
    durableWrites: writes,
    evidence,
    validation: ["\u672C\u6B21\u53EA\u8BB0\u5F55\u663E\u5F0F\u4F20\u5165\u7684\u6587\u6863/\u8BC1\u636E\u5143\u6570\u636E\uFF0C\u672A\u58F0\u660E\u989D\u5916\u9A8C\u8BC1\u7ED3\u679C\u3002"],
    nextActions: [{
      title: "\u67E5\u770B\u5F53\u524D Dove \u72B6\u6001",
      why: "\u786E\u8BA4\u8FD9\u6761\u6587\u6863\u8BC1\u636E\u5DF2\u7ECF\u8FDB\u5165\u540E\u7EED\u5199\u4F5C\u6216 review \u7684\u53EF\u7528\u6750\u6599\u3002"
    }]
  }, responseLanguage);
}
function filteredEntries(entries, filters) {
  return entries.filter((entry) => {
    if (filters.packetId && entry.packetId !== filters.packetId) return false;
    if (filters.documentKind && entry.documentKind !== filters.documentKind) return false;
    if (filters.status && entry.status !== filters.status) return false;
    if (filters.evidenceScope && entry.evidenceScope !== filters.evidenceScope) return false;
    if (typeof filters.publicSafe === "boolean" && entry.publicSafe !== filters.publicSafe) return false;
    return true;
  });
}
function queryDocumentLedger(root, args = {}) {
  const ledger = normalizeDocumentLedgerIndex(readJson(root, ARTIFACT_PATHS.documentsLedger, createDocumentLedgerIndex));
  const filters = {
    packetId: normalizeString6(args.packetId ?? args.taskPacketId ?? args.missionPacketId ?? args.taskId, null),
    documentKind: args.documentKind ? normalizeAllowed2(args.documentKind, DOVE_DOCUMENT_KINDS, null) : null,
    status: args.status ? normalizeAllowed2(args.status, DOVE_DOCUMENT_STATUSES, null) : null,
    evidenceScope: args.evidenceScope ? normalizeAllowed2(args.evidenceScope, DOVE_DOCUMENT_EVIDENCE_SCOPES, null) : null,
    publicSafe: typeof args.publicSafe === "boolean" ? args.publicSafe : null
  };
  const limit = Number.isFinite(Number(args.limit)) ? Math.max(1, Math.min(100, Math.floor(Number(args.limit)))) : 25;
  const entries = filteredEntries(ledger.entries, filters).slice(-limit);
  return {
    mode: "document-ledger-query",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    filters,
    summary: ledger.summary,
    entries,
    privacy: {
      documentBodiesIncluded: false,
      rawTranscriptIncluded: false,
      privateReasoningIncluded: false,
      environmentIncluded: false
    }
  };
}
function recordDocumentEvidence(root, args = {}) {
  assertGovernanceMutationRegistered("record-document-evidence", "guarded");
  const target = assertTaskScopedMutationTarget(root, "record-document-evidence", args);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "draft",
    "researcher",
    "Recording document evidence"
  );
  ensureWorkspace(root);
  const timestamp = nowIso();
  const documentKind = normalizeAllowed2(args.documentKind ?? args.kind, DOVE_DOCUMENT_KINDS, "other");
  const status = normalizeAllowed2(args.status, DOVE_DOCUMENT_STATUSES, "created");
  const title = normalizeString6(args.title ?? args.documentTitle ?? args.summary, "Document evidence");
  const body = normalizeString6(args.body ?? args.content, "");
  let documentPath = normalizeRelativePath3(args.documentPath ?? args.path);
  const writes = [ARTIFACT_PATHS.documentsLedger];
  let createdDocument = false;
  let appendedDocument = false;
  if (args.createDocument === true && args.appendDocument === true) {
    throw new Error("Choose either createDocument or appendDocument, not both.");
  }
  if (args.createDocument === true) {
    if (!body) {
      throw new Error("createDocument requires a non-empty body or content field.");
    }
    const requestedPath = normalizeRelativePath3(args.documentPath ?? args.path);
    const basePath = requestedPath && requestedPath.startsWith(`${ARTIFACT_PATHS.documentsDir}/`) ? requestedPath : generatedDocumentPath(timestamp, documentKind, title);
    assertWritableDocumentPath(basePath);
    documentPath = uniqueRelativePath(root, basePath.endsWith(".md") ? basePath : `${basePath}.md`);
    writeText(root, documentPath, body.endsWith("\n") ? body : `${body}
`);
    createdDocument = true;
    writes.push(documentPath);
  }
  if (args.appendDocument === true) {
    if (!body) {
      throw new Error("appendDocument requires a non-empty body or content field.");
    }
    documentPath = normalizeRelativePath3(args.documentPath ?? args.path);
    assertWritableDocumentPath(documentPath);
    appendText(root, documentPath, body.startsWith("\n") ? body : `
${body.endsWith("\n") ? body : `${body}
`}`);
    appendedDocument = true;
    writes.push(documentPath);
  }
  const ledger = normalizeDocumentLedgerIndex(readJson(root, ARTIFACT_PATHS.documentsLedger, createDocumentLedgerIndex));
  const seed = args.id ?? args.documentId ?? documentPath ?? `${target.packetId}-${documentKind}-${title}`;
  const id = uniqueEntryId(ledger.entries, seed);
  const entry = {
    id,
    packetId: target.packetId,
    documentId: normalizeString6(args.documentId, id),
    title,
    documentPath,
    documentKind,
    status,
    evidenceScope: normalizeAllowed2(args.evidenceScope ?? args.scope, DOVE_DOCUMENT_EVIDENCE_SCOPES, "internal"),
    publicSafe: publicSafeFromArgs(args),
    summary: normalizeString6(args.summary, ""),
    context: normalizeString6(args.context ?? args.reason, ""),
    sourceRefs: normalizeStringArray14(args.sourceRefs ?? args.sourceIds),
    artifactRefs: normalizeStringArray14(args.artifactRefs ?? args.artifactPaths),
    evidenceLinks: normalizeStringArray14(args.evidenceLinks ?? args.evidencePaths),
    claimIds: normalizeStringArray14(args.claimIds ?? args.claims),
    createdAt: timestamp,
    updatedAt: timestamp,
    createdBy: normalizeString6(args.createdBy ?? args.actorRole, "operator"),
    appendOnly: true,
    rawTranscriptIncluded: false,
    privateReasoningIncluded: false,
    environmentIncluded: false
  };
  const nextLedger = normalizeDocumentLedgerIndex({
    ...ledger,
    entries: [...ledger.entries, entry],
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.documentsLedger, nextLedger);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const preActionGuidanceSummary = documentGuidanceSummary(root, args, target, entry);
  return {
    mode: "document-evidence-record",
    status: "recorded",
    packetId: target.packetId,
    entry: nextLedger.entries.at(-1),
    ledger: {
      path: ARTIFACT_PATHS.documentsLedger,
      summary: nextLedger.summary
    },
    writes,
    createdDocument,
    appendedDocument,
    privacy: {
      rawTranscriptIncluded: false,
      privateReasoningIncluded: false,
      environmentIncluded: false,
      defaultEvidenceScope: "internal",
      publicProjectionDerivedOnly: true
    },
    preActionGuidanceSummary,
    resultCard: buildResultCard({ entry, writes, createdDocument, appendedDocument, responseLanguage })
  };
}

// src/core/operational-outcome.mjs
var PROPOSAL_STATUSES = /* @__PURE__ */ new Set([
  "needs-confirmation",
  "needs-task-selection"
]);
var CONFIRMED_EXECUTION_FAILURE_STATUSES = /* @__PURE__ */ new Set([
  "awaiting-host-pass",
  "awaiting-host-results",
  "needs-host-results"
]);
var OPERATIONAL_FAILURE_STATUSES = /* @__PURE__ */ new Set([
  "auto-read-only-step-no-progress",
  "blocked",
  "blocked-boundary",
  "blocked-missing-materials",
  "failed",
  "materialization-failed",
  "missing-required-materials",
  "missing-secret-env",
  "needs-completion-evidence",
  "needs-explicit-progress-step",
  "needs-source-verification",
  "no-op",
  "noop",
  "provider-failed",
  "qa-needs-attention",
  "source-provenance-unverified",
  "step-no-progress",
  "unexpected-step-status",
  "verification-failed",
  "workflow-error-boundary"
]);
function normalizeStatus(value) {
  return typeof value === "string" ? value.trim().toLowerCase() : "";
}
function isProposalOnlyOutcome(result) {
  if (!result || typeof result !== "object") {
    return false;
  }
  const status = normalizeStatus(result.status ?? result.outcome);
  return result.proposalOnly === true || PROPOSAL_STATUSES.has(status);
}
function isOperationalFailureOutcome(result, { confirmed = false } = {}) {
  if (!result || typeof result !== "object") {
    return false;
  }
  if (!confirmed && isProposalOnlyOutcome(result)) {
    return false;
  }
  const status = normalizeStatus(result.status ?? result.outcome);
  return OPERATIONAL_FAILURE_STATUSES.has(status) || confirmed && CONFIRMED_EXECUTION_FAILURE_STATUSES.has(status);
}

// src/core/workflow-goals.mjs
import fs18 from "node:fs";
import path20 from "node:path";
var WORKFLOW_GOAL_CONTRACTS = [
  {
    id: "operator-host-pass-without-results",
    surface: "dove.operator",
    objective: "A confirmed foreground operator pass must not claim execution progress for host-pass-required work unless a safe internal step or an explicit host result exists.",
    pressureTest: "Seed a ready source task that requires host provenance, preview the operator queue, then confirm run_dove_operator without taskResults.",
    acceptanceCriteria: [
      "Preview classifies the task as host-pass-required and does not report auto-runnable work.",
      "Confirmed execution without taskResults returns needs-host-results.",
      "No task is reported as updated and the result card does not expose affected packet ids.",
      "The task remains ready and has no synthetic boundary.",
      "No runtime result entry is persisted for the run.",
      "The response returns material-specific required actions for the host pass."
    ],
    failureMode: "fake-foreground-progress-without-evidence",
    failureReflection: {
      lessonCaptureRequired: true,
      lessonCommand: "project:dove.lessons",
      remediationRequired: true,
      operatorFollowThroughRequired: true,
      regressionArtifacts: [
        "scripts/validate-workflow-goals.mjs",
        "tests/integration/workflow-goals.test.mjs",
        "tests/integration/mcp-tools.test.mjs"
      ],
      remediationTargets: [
        "src/core/task-workflow.mjs",
        "src/core/workflow-goals.mjs",
        "src/core/command-manifest.mjs"
      ],
      summary: "If this goal fails, close the fix only after adding or updating an explicit lesson/remediation note and keeping this scenario in the workflow-goals gate."
    }
  },
  {
    id: "mission-contract-materializes-without-execution",
    surface: "dove.mission",
    objective: "A Dove mission must materialize only an exact approved task contract, reject bare or stale confirmation, and avoid recording pass/runtime execution results.",
    pressureTest: "Replay one create_dove_task proposal's exact confirmArgs, then use a separate proposal to reject bare confirmation and stale pass/result fields without materialization.",
    acceptanceCriteria: [
      "The proposal returns mission-contract and contract-handoff semantics with no writes.",
      "Bare confirmed:true without the proposal digest is rejected without materialization.",
      "A stale confirmation that changes the approved contract is rejected without materialization.",
      "Replaying the exact confirmArgs returns materialized with contractMaterialized true.",
      "The task remains ready, no runtime result is persisted, and no mission pass recorder fields are returned."
    ],
    failureMode: "mission-confirmation-substitution-or-fake-execution",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/mcp-tools.test.mjs"],
      remediationTargets: ["src/core/task-workflow.mjs", "src/core/workflow-goals.mjs", "src/core/command-manifest.mjs"],
      summary: "If mission confirmation accepts a bare or stale contract, or materialization records runtime results, repair create_dove_task so only exact proposal replay reaches contract handoff."
    })
  },
  {
    id: "mission-completion-requires-evidence",
    surface: "dove.result-recording",
    objective: "An explicit Dove result recording pass must not mark a task completed from a bare completion flag or summary without explicit evidence, artifacts, validation output, or concrete plan-output missions.",
    pressureTest: "Seed a ready mission task, then call record_dove_mission_pass with resultStatus completed and a summary but no evidence or artifacts.",
    acceptanceCriteria: [
      "The mission pass returns needs-completion-evidence and remains proposal-only.",
      "The task remains ready instead of completed.",
      "No runtime result entry is persisted for the rejected completion.",
      "The response names the missing evidence/artifact action."
    ],
    failureMode: "fake-mission-completion-without-evidence",
    failureReflection: {
      lessonCaptureRequired: true,
      lessonCommand: "project:dove.lessons",
      remediationRequired: true,
      operatorFollowThroughRequired: true,
      regressionArtifacts: [
        "scripts/validate-workflow-goals.mjs",
        "tests/integration/workflow-goals.test.mjs",
        "tests/integration/mcp-tools.test.mjs"
      ],
      remediationTargets: [
        "src/core/task-workflow.mjs",
        "src/core/workflow-goals.mjs",
        "src/core/result-cards.mjs"
      ],
      summary: "If mission completion can succeed without evidence, repair the mission pass contract and keep this pressure scenario executable."
    }
  },
  {
    id: "auto-read-only-step-cannot-complete",
    surface: "dove.auto",
    objective: "A confirmed Dove auto run must not treat read-only status or lesson queries as durable task progress or completion.",
    pressureTest: "Seed a ready task and confirm run_dove_auto with a single dove.status step marked completeTask.",
    acceptanceCriteria: [
      "The auto run returns needs-explicit-progress-step.",
      "No auto iteration is recorded or persisted as completed work.",
      "The task remains ready instead of in-progress, blocked, or completed.",
      "The response requires an explicit write/generation/review step."
    ],
    failureMode: "read-only-auto-step-fake-completion",
    failureReflection: {
      lessonCaptureRequired: true,
      lessonCommand: "project:dove.lessons",
      remediationRequired: true,
      operatorFollowThroughRequired: true,
      regressionArtifacts: [
        "scripts/validate-workflow-goals.mjs",
        "tests/integration/workflow-goals.test.mjs",
        "tests/integration/mcp-tools.test.mjs"
      ],
      remediationTargets: [
        "src/core/task-workflow.mjs",
        "src/core/workflow-goals.mjs",
        "src/core/result-cards.mjs"
      ],
      summary: "If read-only auto steps can complete tasks, repair auto step classification and keep this pressure scenario executable."
    }
  },
  {
    id: "experience-blocked-audit-not-bridged",
    surface: "dove.experience",
    objective: "An experience result with blocked audit integrity must not be reported as a bridged claim update.",
    pressureTest: "Seed a task and call run_experience_workflow with an incomplete result that lacks required evidence and methodology.",
    acceptanceCriteria: [
      "The workflow returns needs-review rather than bridged.",
      "The audit verdict is blocked and integrity flags are visible.",
      "Any bridge entry is held rather than applied.",
      "No claim state is upgraded from blocked audit evidence."
    ],
    failureMode: "blocked-experiment-audit-reported-as-bridged",
    failureReflection: {
      lessonCaptureRequired: true,
      lessonCommand: "project:dove.lessons",
      remediationRequired: true,
      operatorFollowThroughRequired: true,
      regressionArtifacts: [
        "scripts/validate-workflow-goals.mjs",
        "tests/integration/workflow-goals.test.mjs",
        "tests/integration/evidence-integrity.test.mjs"
      ],
      remediationTargets: [
        "src/core/experience-workflow.mjs",
        "src/core/workflow-goals.mjs",
        "src/core/result-cards.mjs"
      ],
      summary: "If blocked experiment audits are presented as bridged results, repair the audit-to-claim bridge and keep this pressure scenario executable."
    }
  },
  {
    id: "plan-completion-requires-executable-children",
    surface: "dove.result-recording",
    objective: "A completed planning result must not complete unless it returns explicit executable child missions with contracts.",
    pressureTest: "Seed a plan-stage task, then record an explicit completed result with summary and criteria evidence but no child mission output.",
    acceptanceCriteria: [
      "The mission pass returns plan-output-not-executable and remains proposal-only.",
      "The plan task remains ready instead of completed.",
      "No runtime result entry is persisted for the rejected plan pass.",
      "The response requires explicit executable child missions."
    ],
    failureMode: "plan-pass-completed-without-executable-child-work",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/mcp-tools.test.mjs"],
      remediationTargets: ["src/core/task-workflow.mjs", "src/core/workflow-goals.mjs", "src/core/command-manifest.mjs"],
      summary: "If a plan pass can complete without explicit executable child contracts, repair plan-output gating and keep this pressure scenario executable."
    })
  },
  {
    id: "plan-child-contract-requires-criteria",
    surface: "dove.result-recording",
    objective: "A plan-derived child mission must not be materialized from an explicit result child contract that lacks convergence criteria.",
    pressureTest: "Seed a plan-stage task, then record an explicit completed result with one child mission whose executionContract omits convergence.criteria.",
    acceptanceCriteria: [
      "The mission pass returns plan-output-not-executable.",
      "The response identifies the child mission as not executable.",
      "The response names convergence.criteria as missing.",
      "No child task is created from the invalid plan output."
    ],
    failureMode: "plan-child-materialized-without-convergence-criteria",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/mcp-tools.test.mjs"],
      remediationTargets: ["src/core/task-workflow.mjs", "src/core/workflow-goals.mjs"],
      summary: "If a plan child can materialize without convergence criteria, repair child executionContract readiness checks and keep this scenario executable."
    })
  },
  {
    id: "status-adjust-completion-requires-criteria",
    surface: "dove.status",
    objective: "A status adjustment must not mark a task complete unless verifiedCriteria covers the execution contract criteria.",
    pressureTest: "Seed a ready task with an executable contract, then apply a confirmed completed status adjustment with summary and evidence but no verifiedCriteria coverage.",
    acceptanceCriteria: [
      "The status adjustment returns rejected.",
      "The rejection contains a verification-failed completion block.",
      "The task remains ready instead of completed.",
      "The response requires verified criteria coverage."
    ],
    failureMode: "status-adjustment-fake-completion-without-criteria",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/dove-query.test.mjs"],
      remediationTargets: ["src/core/task-workflow.mjs", "src/core/dove.mjs", "src/core/workflow-goals.mjs"],
      summary: "If status adjustment can complete without verified criteria, repair the shared completion gate and keep this pressure scenario executable."
    })
  },
  {
    id: "operator-host-result-requires-criteria",
    surface: "dove.operator",
    objective: "A host pass result supplied to the operator must not complete a task without convergence criteria coverage.",
    pressureTest: "Seed a host-pass-required task, then confirm run_dove_operator with a completed taskResult that has evidence but no verifiedCriteria.",
    acceptanceCriteria: [
      "The operator records a verification-failed iteration.",
      "The task becomes blocked with a verification-failed boundary instead of completed.",
      "The operator result is recorded only as a boundary-producing pass.",
      "The response requires verified criteria coverage."
    ],
    failureMode: "operator-host-result-fake-completion-without-criteria",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/mcp-tools.test.mjs"],
      remediationTargets: ["src/core/task-workflow.mjs", "src/core/workflow-goals.mjs"],
      summary: "If operator host results can complete without criteria coverage, repair host-result completion verification and keep this pressure scenario executable."
    })
  },
  {
    id: "auto-completion-requires-criteria",
    surface: "dove.auto",
    objective: "A Dove auto step that produces artifacts must still route to verification failure when verifiedCriteria does not cover the task contract.",
    pressureTest: "Seed a ready task with an executable contract, then run a concrete auto note step marked completeTask without verifiedCriteria.",
    acceptanceCriteria: [
      "The auto run returns verification-failed.",
      "The task becomes blocked rather than completed.",
      "The boundary type is verification-failed.",
      "The response requires verified criteria coverage."
    ],
    failureMode: "auto-artifact-step-fake-completion-without-criteria",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/unit/phase6-hardening.test.mjs"],
      remediationTargets: ["src/core/task-workflow.mjs", "src/core/workflow-goals.mjs"],
      summary: "If auto artifacts can complete tasks without criteria coverage, repair auto completion verification and keep this scenario executable."
    })
  },
  {
    id: "status-routes-missing-execution-contract",
    surface: "dove.status",
    objective: "Status must route an active task without an executable contract to a visible Planner next action instead of treating the task as ordinary mission display.",
    pressureTest: "Seed a ready task, remove its executionContract from durable packet state, then query full Dove status.",
    acceptanceCriteria: [
      "Status returns blocked because executionGaps has a missingContract count.",
      "The first ranked recovery action wraps missing-executable-contract as recoveryPrimaryKind.",
      "The next action points to the Planner/mission surface.",
      "preActionGuidance reports planner as the execution next role."
    ],
    failureMode: "status-hides-missing-executable-contract",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/dove-query.test.mjs"],
      remediationTargets: ["src/core/dove.mjs", "src/core/pre-action-guidance.mjs", "src/core/workflow-goals.mjs"],
      summary: "If status does not rank missing executable contracts as Planner work, repair status routing and keep this pressure scenario executable."
    })
  },
  {
    id: "public-surfaces-stay-flat",
    surface: "dove.status",
    objective: "Dove must not expose public role or mission-board slash surfaces when improving workflow orchestration.",
    pressureTest: "Inspect the public command manifest and assert that planner, builder, reviewer, missions, board, and list surfaces are absent.",
    acceptanceCriteria: [
      "The command manifest contains no dove.planner surface.",
      "The command manifest contains no dove.builder or dove.reviewer surface.",
      "The command manifest contains no dove.missions, dove.board, or dove.list surface.",
      "The existing flat Dove surfaces remain present."
    ],
    failureMode: "public-slash-surface-sprawl",
    failureReflection: workflowFailureReflection({
      regressionArtifacts: ["scripts/validate-workflow-goals.mjs", "tests/integration/workflow-goals.test.mjs", "tests/integration/mcp-tools.test.mjs"],
      remediationTargets: ["src/core/command-manifest.mjs", "src/core/workflow-goals.mjs"],
      summary: "If role or mission-board slash surfaces appear, remove the public surface sprawl and keep this pressure scenario executable."
    })
  }
];
var CONTRACT_BY_ID = new Map(WORKFLOW_GOAL_CONTRACTS.map((contract) => [contract.id, contract]));
function workflowFailureReflection({ regressionArtifacts, remediationTargets, summary }) {
  return {
    lessonCaptureRequired: true,
    lessonCommand: "project:dove.lessons",
    remediationRequired: true,
    operatorFollowThroughRequired: true,
    regressionArtifacts,
    remediationTargets,
    summary
  };
}

// src/core/network-search.mjs
var DEFAULT_KIND = "scholarly";
var SEARCH_KINDS = /* @__PURE__ */ new Set(["scholarly", "web", "all"]);
var DEFAULT_LIMIT = 8;
var MAX_LIMIT = 50;
var DEFAULT_TIMEOUT_MS2 = 12e3;
var MAX_QUERY_CHARS = 500;
var POST_FILTER_OVERFETCH_FACTOR = 3;
var SECRET_VALUE_PATTERN = /\b(?:Bearer\s+[A-Za-z0-9._~+/=-]+|sk-[A-Za-z0-9_-]{12,}|(?:api[_-]?key|token|secret|password)\s*[:=]\s*[^\s,}]+)\b/giu;
var NETWORK_SEARCH_PROVIDER_REGISTRY = Object.freeze([
  Object.freeze({ id: "openalex", kind: "scholarly", access: "public", defaultLimit: 5, maxLimit: 25, capabilities: ["works", "doi", "open-access", "authors", "year"], filters: Object.freeze({ year: "native", domains: "post", fieldsOfStudy: "post", openAccessOnly: "post", locale: "post" }) }),
  Object.freeze({ id: "crossref", kind: "scholarly", access: "public", defaultLimit: 5, maxLimit: 20, capabilities: ["works", "doi", "open-access", "authors", "year"], filters: Object.freeze({ year: "native", domains: "post", openAccessOnly: "post", locale: "post" }) }),
  Object.freeze({ id: "arxiv", kind: "scholarly", access: "public", defaultLimit: 5, maxLimit: 20, capabilities: ["preprints", "arxiv-id", "authors", "year"], filters: Object.freeze({ year: "post", domains: "post", openAccessOnly: "post" }) }),
  Object.freeze({ id: "europe-pmc", kind: "scholarly", access: "public", defaultLimit: 5, maxLimit: 25, capabilities: ["papers", "doi", "pubmed-id", "open-access", "authors", "year"], filters: Object.freeze({ year: "post", domains: "post", openAccessOnly: "post", locale: "post" }) }),
  Object.freeze({ id: "public-web", kind: "web", access: "public", defaultLimit: 0, maxLimit: 0, unavailable: true, capabilities: ["status"], filters: Object.freeze({}) })
]);
var DEFAULT_NETWORK_SEARCH_PROVIDER_IDS2 = Object.freeze(["openalex", "crossref", "arxiv", "europe-pmc"]);
var PROVIDER_BY_ID = new Map(NETWORK_SEARCH_PROVIDER_REGISTRY.map((provider) => [provider.id, provider]));
function isPlainObject5(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function normalizeString7(value, fallback = null) {
  if (typeof value !== "string") {
    return fallback;
  }
  const trimmed = value.trim();
  return trimmed || fallback;
}
function normalizeBoolean3(value, fallback = false) {
  if (typeof value === "boolean") {
    return value;
  }
  if (typeof value === "string") {
    const normalized = value.trim().toLowerCase();
    if (["1", "true", "yes", "on"].includes(normalized)) {
      return true;
    }
    if (["0", "false", "no", "off"].includes(normalized)) {
      return false;
    }
  }
  return fallback;
}
function normalizePositiveInteger2(value, fallback, min, max) {
  const numeric = typeof value === "number" ? value : Number(value);
  if (!Number.isFinite(numeric)) {
    return fallback;
  }
  return Math.min(max, Math.max(min, Math.trunc(numeric)));
}
function normalizeStringArray15(value) {
  const rawItems = Array.isArray(value) ? value : typeof value === "string" ? value.split(",") : [];
  return Array.from(new Set(rawItems.map((item) => normalizeString7(item)).filter(Boolean)));
}
function normalizeProviderId(value) {
  return normalizeString7(value)?.toLowerCase() ?? null;
}
function normalizeProviderIds(value) {
  const ids = normalizeStringArray15(value).map(normalizeProviderId).filter(Boolean);
  for (const id of ids) {
    if (!PROVIDER_BY_ID.has(id)) {
      throw new Error(`Unsupported Dove network search provider: ${id}`);
    }
  }
  return ids;
}
function normalizeDomains(value) {
  const domains = normalizeStringArray15(value).map((domain) => domain.toLowerCase());
  for (const domain of domains) {
    if (domain.includes("://") || domain.includes("/") || domain.length > 253 || !/^[a-z0-9.-]+$/iu.test(domain)) {
      throw new Error(`Dove network search domain filters must be bare hostnames: ${domain}`);
    }
  }
  return domains;
}
function normalizeYear(value) {
  const text3 = typeof value === "number" ? String(Math.trunc(value)) : normalizeString7(value);
  if (!text3) {
    return null;
  }
  if (!/^\d{4}(?:-\d{4})?$/u.test(text3)) {
    throw new Error("Dove network search year must be YYYY or YYYY-YYYY.");
  }
  return text3;
}
function normalizeLocale(value) {
  const locale = normalizeString7(value);
  if (!locale) {
    return null;
  }
  if (!/^[a-z]{2,3}(?:[-_][A-Za-z0-9]{2,8})?$/iu.test(locale)) {
    throw new Error(`Dove network search locale must be a compact locale code: ${locale}`);
  }
  return locale.replace("_", "-").toLowerCase();
}
function normalizeNetworkSearchConfigForCore(config = {}) {
  const source = isPlainObject5(config) ? config : {};
  return {
    enabled: normalizeBoolean3(source.enabled, true),
    defaultProviderIds: normalizeProviderIds(source.defaultProviderIds ?? source.defaultProviders ?? DEFAULT_NETWORK_SEARCH_PROVIDER_IDS2),
    disabledProviderIds: normalizeProviderIds(source.disabledProviderIds ?? source.disabledProviders ?? []),
    providerSettings: isPlainObject5(source.providerSettings) ? source.providerSettings : {},
    timeoutMs: normalizePositiveInteger2(source.timeoutMs, DEFAULT_TIMEOUT_MS2, 1e3, 6e4),
    maxResults: normalizePositiveInteger2(source.maxResults ?? source.limit, DEFAULT_LIMIT, 1, MAX_LIMIT)
  };
}
function normalizeNetworkSearchQuery(rawArgs = {}, config = {}) {
  const args = isPlainObject5(rawArgs) ? rawArgs : {};
  const normalizedConfig = normalizeNetworkSearchConfigForCore(config);
  const query = normalizeString7(args.query);
  if (!query) {
    throw new Error("Dove network search requires a non-empty query.");
  }
  if (query.length > MAX_QUERY_CHARS) {
    throw new Error(`Dove network search query must be ${MAX_QUERY_CHARS} characters or fewer.`);
  }
  const kind = normalizeString7(args.kind, DEFAULT_KIND).toLowerCase();
  if (!SEARCH_KINDS.has(kind)) {
    throw new Error(`Dove network search kind must be one of: ${Array.from(SEARCH_KINDS).join(", ")}.`);
  }
  return {
    query,
    kind,
    limit: normalizePositiveInteger2(args.limit ?? args.maxResults, normalizedConfig.maxResults, 1, Math.min(MAX_LIMIT, normalizedConfig.maxResults || MAX_LIMIT)),
    year: normalizeYear(args.year),
    domains: normalizeDomains(args.domains),
    fieldsOfStudy: normalizeStringArray15(args.fieldsOfStudy),
    openAccessOnly: normalizeBoolean3(args.openAccessOnly, false),
    providerIds: normalizeProviderIds(args.providerIds ?? args.providers),
    locale: normalizeLocale(args.locale)
  };
}
function redactSensitiveText(value) {
  const text3 = String(value ?? "");
  return text3.replace(SECRET_VALUE_PATTERN, "[REDACTED]");
}
function sanitizeError(error) {
  return redactSensitiveText(error instanceof Error ? error.message : String(error)).slice(0, 500);
}
function safeUrl(value) {
  const text3 = normalizeString7(value);
  if (!text3) {
    return null;
  }
  try {
    const url = new URL(text3);
    if (!["http:", "https:"].includes(url.protocol)) {
      return null;
    }
    return url.toString();
  } catch {
    return null;
  }
}
function hostnameFromUrl(value) {
  const url = safeUrl(value);
  return url ? new URL(url).hostname.toLowerCase() : null;
}
function doiUrl(doi) {
  const normalized = normalizeDoi2(doi);
  return normalized ? `https://doi.org/${normalized}` : null;
}
function normalizeDoi2(value) {
  const text3 = normalizeString7(value)?.replace(/^https?:\/\/(?:dx\.)?doi\.org\//iu, "") ?? null;
  if (!text3) {
    return null;
  }
  const cleaned = text3.trim().toLowerCase();
  return cleaned.startsWith("10.") ? cleaned : null;
}
function normalizeTitle(value) {
  return normalizeString7(Array.isArray(value) ? value[0] : value);
}
function normalizeAuthors(value) {
  if (isPlainObject5(value)) {
    return normalizeAuthors(value.author ?? value.authors ?? value.fullName ?? value.name);
  }
  if (Array.isArray(value)) {
    return value.map((item) => {
      if (typeof item === "string") {
        return normalizeString7(item);
      }
      if (isPlainObject5(item)) {
        return normalizeString7(item.fullName ?? item.name ?? item.display_name ?? [item.given, item.family].filter(Boolean).join(" "));
      }
      return null;
    }).filter(Boolean).slice(0, 12);
  }
  const text3 = normalizeString7(value);
  return text3 ? text3.split(/\s*[,;]\s*/u).map((item) => normalizeString7(item)).filter(Boolean).slice(0, 12) : [];
}
function normalizePublishedAt(year, dateParts) {
  const textYear = typeof year === "number" ? String(year) : normalizeString7(year);
  if (textYear && /^\d{4}/u.test(textYear)) {
    return textYear.slice(0, 10);
  }
  const parts = Array.isArray(dateParts?.[0]) ? dateParts[0] : Array.isArray(dateParts) ? dateParts : null;
  if (!parts?.[0]) {
    return null;
  }
  return parts.slice(0, 3).map((part) => String(part).padStart(2, "0")).join("-");
}
function cleanSnippet(value) {
  const text3 = normalizeString7(value);
  if (!text3) {
    return null;
  }
  return text3.replace(/<[^>]+>/gu, " ").replace(/\s+/gu, " ").trim().slice(0, 600);
}
function decodeXml(value) {
  return String(value ?? "").replace(/&lt;/gu, "<").replace(/&gt;/gu, ">").replace(/&amp;/gu, "&").replace(/&quot;/gu, '"').replace(/&#39;/gu, "'").replace(/\s+/gu, " ").trim();
}
function extractXmlText(xml, tag) {
  const match = xml.match(new RegExp(`<${tag}(?:\\s[^>]*)?>([\\s\\S]*?)<\\/${tag}>`, "iu"));
  return match ? decodeXml(match[1]) : null;
}
function abstractFromInvertedIndex(index) {
  if (!isPlainObject5(index)) {
    return null;
  }
  const pairs = [];
  for (const [word, positions] of Object.entries(index)) {
    if (!Array.isArray(positions)) {
      continue;
    }
    for (const position of positions) {
      if (Number.isInteger(position)) {
        pairs[position] = word;
      }
    }
  }
  return pairs.filter(Boolean).join(" ") || null;
}
function buildParams(values) {
  const params = new URLSearchParams();
  for (const [key, value] of Object.entries(values)) {
    if (value === null || value === void 0 || value === "") {
      continue;
    }
    params.set(key, String(value));
  }
  return params;
}
async function fetchText(url, { timeoutMs, fetchFn, headers = {} }) {
  if (typeof fetchFn !== "function") {
    throw new Error("No fetch implementation is available for Dove network search.");
  }
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), timeoutMs);
  try {
    const response = await fetchFn(url, {
      signal: controller.signal,
      headers: {
        Accept: "application/json, application/xml, text/xml;q=0.9, */*;q=0.8",
        "User-Agent": "DoveNetworkSearch/1.0",
        ...headers
      }
    });
    if (!response?.ok) {
      throw new Error(`HTTP ${response?.status ?? "error"} from ${new URL(url).hostname}`);
    }
    return await response.text();
  } catch (error) {
    if (error?.name === "AbortError") {
      throw new Error(`Timed out after ${timeoutMs}ms`);
    }
    throw error;
  } finally {
    clearTimeout(timer);
  }
}
async function fetchJson(url, options) {
  const text3 = await fetchText(url, options);
  try {
    return JSON.parse(text3);
  } catch (error) {
    throw new Error(`Invalid JSON response: ${sanitizeError(error)}`);
  }
}
var FILTER_NAMES = Object.freeze(["year", "domains", "fieldsOfStudy", "openAccessOnly", "locale"]);
var PROVIDER_FILTER_SUPPORT = Object.freeze(Object.fromEntries(NETWORK_SEARCH_PROVIDER_REGISTRY.map((provider) => [
  provider.id,
  Object.freeze(Object.fromEntries(FILTER_NAMES.filter((name) => provider.filters?.[name]).map((name) => [name, provider.filters[name]])))
])));
function requestedFilterNames(query) {
  return FILTER_NAMES.filter((name) => name === "openAccessOnly" ? query.openAccessOnly : Array.isArray(query[name]) ? query[name].length > 0 : Boolean(query[name]));
}
function providerFilterPlan(provider, query) {
  const support = PROVIDER_FILTER_SUPPORT[provider.id] ?? {};
  const requested = requestedFilterNames(query);
  return {
    appliedFilters: requested.filter((name) => Boolean(support[name])),
    unsupportedFilters: requested.filter((name) => !support[name]),
    filterModes: Object.fromEntries(requested.filter((name) => Boolean(support[name])).map((name) => [name, support[name]]))
  };
}
function normalizeComparableText(value) {
  return String(value ?? "").normalize("NFKC").toLowerCase().replace(/[^\p{L}\p{N}]+/gu, " ").trim();
}
function normalizedLocaleBase(value) {
  return normalizeString7(value)?.toLowerCase().replace("_", "-").split("-")[0] ?? null;
}
function candidateMatchesDomain(candidate, domains) {
  const candidateDomains = normalizeStringArray15(candidate.domains).map((domain) => domain.toLowerCase());
  return domains.some((requested) => candidateDomains.some((candidateDomain) => candidateDomain === requested || candidateDomain.endsWith(`.${requested}`)));
}
function candidateMatchesFields(candidate, fieldsOfStudy) {
  const candidateFields = normalizeStringArray15(candidate.fieldsOfStudy).map(normalizeComparableText).filter(Boolean);
  return fieldsOfStudy.some((requested) => {
    const normalized = normalizeComparableText(requested);
    return candidateFields.some((candidateField) => candidateField === normalized || candidateField.includes(normalized) || normalized.includes(candidateField));
  });
}
function postFilterCandidates(candidates, query, filterPlan) {
  const postFilters = new Set(Object.entries(filterPlan.filterModes).filter(([, mode]) => mode === "post").map(([name]) => name));
  return candidates.filter((candidate) => {
    if (postFilters.has("openAccessOnly") && candidate.openAccess !== true) {
      return false;
    }
    if (postFilters.has("year")) {
      const candidateYear = normalizeString7(candidate.publishedAt)?.slice(0, 4);
      if (!candidateYear) {
        return false;
      }
      const [start, end] = query.year.split("-").map((item) => Number(item));
      const year = Number(candidateYear);
      if (year < start || year > (end || start)) {
        return false;
      }
    }
    if (postFilters.has("domains") && !candidateMatchesDomain(candidate, query.domains)) {
      return false;
    }
    if (postFilters.has("fieldsOfStudy") && !candidateMatchesFields(candidate, query.fieldsOfStudy)) {
      return false;
    }
    if (postFilters.has("locale") && normalizedLocaleBase(candidate.locale) !== normalizedLocaleBase(query.locale)) {
      return false;
    }
    return true;
  });
}
function normalizeCandidate(candidate, provider) {
  const title = normalizeTitle(candidate.title);
  if (!title) {
    return null;
  }
  const doi = normalizeDoi2(candidate.doi);
  const url = safeUrl(candidate.url) ?? doiUrl(doi);
  const candidateDomains = normalizeStringArray15(candidate.domains).map((domain) => domain.toLowerCase());
  const urlHostname = hostnameFromUrl(url);
  if (urlHostname) {
    candidateDomains.push(urlHostname);
  }
  if (!url && !doi && !candidate.arxivId && !candidate.pubmedId && !candidate.semanticScholarId) {
    return null;
  }
  return {
    lifecycle: "candidate",
    title,
    url,
    snippet: cleanSnippet(candidate.snippet),
    sourceName: normalizeString7(candidate.sourceName, provider.id),
    publishedAt: normalizeString7(candidate.publishedAt),
    authors: normalizeAuthors(candidate.authors),
    domains: Array.from(new Set(candidateDomains)),
    fieldsOfStudy: normalizeStringArray15(candidate.fieldsOfStudy),
    locale: normalizedLocaleBase(candidate.locale),
    doi,
    arxivId: normalizeString7(candidate.arxivId),
    pubmedId: normalizeString7(candidate.pubmedId),
    semanticScholarId: normalizeString7(candidate.semanticScholarId),
    openAccess: candidate.openAccess === true ? true : candidate.openAccess === false ? false : null,
    providerId: provider.id,
    provenance: {
      providerId: provider.id,
      providerName: provider.id,
      access: provider.access,
      retrievedAt: (/* @__PURE__ */ new Date()).toISOString()
    },
    score: Number.isFinite(candidate.score) ? candidate.score : 0,
    warnings: normalizeStringArray15(candidate.warnings)
  };
}
function canonicalUrlKey(value) {
  const url = safeUrl(value);
  if (!url) {
    return null;
  }
  const parsed = new URL(url);
  parsed.hash = "";
  parsed.search = "";
  parsed.hostname = parsed.hostname.toLowerCase();
  return parsed.toString().replace(/\/$/u, "").toLowerCase();
}
function titleKey(value) {
  return normalizeString7(value)?.toLowerCase().replace(/[^\p{L}\p{N}]+/gu, " ").trim() ?? null;
}
function dedupeKey(candidate) {
  if (candidate.doi) {
    return `doi:${candidate.doi}`;
  }
  if (candidate.arxivId) {
    return `arxiv:${candidate.arxivId.toLowerCase()}`;
  }
  if (candidate.pubmedId) {
    return `pmid:${candidate.pubmedId}`;
  }
  const url = canonicalUrlKey(candidate.url);
  if (url) {
    return `url:${url}`;
  }
  const title = titleKey(candidate.title);
  return title ? `title:${title}` : null;
}
function textTokens(value) {
  return Array.from(new Set(normalizeComparableText(value).split(/\s+/u).filter(Boolean)));
}
function tokenOverlapScore(value, queryTokens) {
  if (queryTokens.length === 0) {
    return 0;
  }
  const tokens = new Set(textTokens(value));
  return queryTokens.filter((token) => tokens.has(token)).length / queryTokens.length;
}
function publicationRecencyScore(publishedAt) {
  const year = Number(normalizeString7(publishedAt)?.slice(0, 4));
  if (!Number.isInteger(year)) {
    return 0;
  }
  const age = Math.max(0, (/* @__PURE__ */ new Date()).getUTCFullYear() - year);
  return Math.max(0, 10 - age * 0.75);
}
function scoreCandidate(candidate, query) {
  const normalizedQuery = normalizeComparableText(query.query);
  const normalizedTitle = normalizeComparableText(candidate.title);
  const queryTokens = textTokens(query.query);
  const titleOverlap = tokenOverlapScore(candidate.title, queryTokens);
  const snippetOverlap = tokenOverlapScore(candidate.snippet, queryTokens);
  const fieldOverlap = Math.max(0, ...candidate.fieldsOfStudy.map((field) => tokenOverlapScore(field, queryTokens)));
  const authority = Math.min(8, Math.max(0, Number.isFinite(candidate.score) ? candidate.score : 0));
  let score = authority;
  if (normalizedTitle === normalizedQuery) {
    score += 70;
  } else if (normalizedQuery && normalizedTitle.includes(normalizedQuery)) {
    score += 52;
  }
  score += titleOverlap * 42;
  score += snippetOverlap * 20;
  score += fieldOverlap * 16;
  score += publicationRecencyScore(candidate.publishedAt);
  if (candidate.doi || candidate.arxivId || candidate.pubmedId || candidate.semanticScholarId) {
    score += 2;
  }
  if (candidate.url) {
    score += 1;
  }
  if (candidate.openAccess === true) {
    score += 0.5;
  }
  return score;
}
function dedupeAndRank(candidates, query) {
  const byKey = /* @__PURE__ */ new Map();
  for (const candidate of candidates) {
    const key = dedupeKey(candidate);
    if (!key) {
      continue;
    }
    const scored = { ...candidate, score: scoreCandidate(candidate, query) };
    const existing = byKey.get(key);
    if (!existing || scored.score > existing.score) {
      byKey.set(key, {
        ...scored,
        provenance: {
          ...scored.provenance,
          mergedProviderIds: Array.from(new Set([...existing?.provenance?.mergedProviderIds ?? [], existing?.providerId, scored.providerId].filter(Boolean)))
        }
      });
    } else if (existing) {
      existing.provenance.mergedProviderIds = Array.from(new Set([...existing.provenance?.mergedProviderIds ?? [], scored.providerId].filter(Boolean)));
    }
  }
  return Array.from(byKey.values()).sort((left, right) => right.score - left.score || (right.publishedAt ?? "").localeCompare(left.publishedAt ?? "")).slice(0, query.limit);
}
function selectedProviderIds(query, config) {
  if (query.providerIds.length > 0) {
    const conflicts = query.providerIds.filter((id) => !providerVisibleForKind(PROVIDER_BY_ID.get(id), query.kind));
    if (conflicts.length > 0) {
      throw new Error(`Dove network search provider-kind conflict: ${conflicts.join(", ")} cannot be used for kind ${query.kind}.`);
    }
    return query.providerIds;
  }
  if (query.kind === "web") {
    return ["public-web"];
  }
  if (query.kind === "all") {
    return Array.from(/* @__PURE__ */ new Set([...config.defaultProviderIds, "public-web"]));
  }
  return config.defaultProviderIds.filter((id) => PROVIDER_BY_ID.get(id)?.kind === "scholarly");
}
function providerVisibleForKind(provider, kind) {
  return Boolean(provider) && (kind === "all" || provider.kind === kind);
}
function providerReport(provider, fields = {}) {
  return {
    providerId: provider.id,
    kind: provider.kind,
    access: provider.access,
    status: fields.status ?? "available",
    resultCount: fields.resultCount ?? 0,
    fetchedCount: fields.fetchedCount ?? 0,
    message: fields.message ?? null,
    error: fields.error ?? null,
    appliedFilters: fields.appliedFilters ?? [],
    unsupportedFilters: fields.unsupportedFilters ?? [],
    filterModes: fields.filterModes ?? {},
    capabilities: provider.capabilities
  };
}
function publicWebUnavailableReport() {
  const provider = PROVIDER_BY_ID.get("public-web");
  return providerReport(provider, {
    status: "unavailable",
    message: "Dove \u8FD8\u6CA1\u6709\u5185\u7F6E\u7A33\u5B9A\u7684\u514D key \u901A\u7528\u7F51\u9875\u641C\u7D22 provider\uFF1B\u8BF7\u5148\u7528\u5BBF\u4E3B\u516C\u5F00\u641C\u7D22\u6838\u5B9E\u7F51\u9875\uFF0C\u518D\u628A\u9A8C\u8BC1\u8FC7\u7684\u6765\u6E90\u767B\u8BB0\u4E3A source \u6216 note\u3002"
  });
}
async function withTimeout(operation, timeoutMs) {
  let timer;
  try {
    return await Promise.race([
      operation,
      new Promise((resolve, reject) => {
        timer = setTimeout(() => reject(new Error(`Timed out after ${timeoutMs}ms`)), timeoutMs);
      })
    ]);
  } finally {
    clearTimeout(timer);
  }
}
async function runProvider(provider, query, config, fetchFn) {
  if (provider.unavailable) {
    return {
      candidates: [],
      report: { ...publicWebUnavailableReport(), unsupportedFilters: requestedFilterNames(query) }
    };
  }
  const limit = Math.min(query.limit, provider.maxLimit || query.limit);
  const timeoutMs = normalizePositiveInteger2(config.providerSettings?.[provider.id]?.timeoutMs, config.timeoutMs, 1e3, 6e4);
  const filterPlan = providerFilterPlan(provider, query);
  const needsPostFilter = Object.values(filterPlan.filterModes).includes("post");
  const fetchLimit = needsPostFilter ? Math.min(provider.maxLimit || limit, Math.max(limit, limit * POST_FILTER_OVERFETCH_FACTOR)) : limit;
  const providerQuery = { ...query, limit: fetchLimit };
  try {
    const rawCandidates = await withTimeout(Promise.resolve().then(() => PROVIDER_ADAPTERS[provider.id](providerQuery, { timeoutMs, fetchFn })), timeoutMs);
    const candidates = postFilterCandidates(rawCandidates.map((candidate) => normalizeCandidate(candidate, provider)).filter(Boolean), query, filterPlan).slice(0, limit);
    return {
      candidates,
      report: providerReport(provider, { status: "ok", resultCount: candidates.length, fetchedCount: rawCandidates.length, ...filterPlan })
    };
  } catch (error) {
    return {
      candidates: [],
      report: providerReport(provider, { status: "error", error: sanitizeError(error), message: "Provider search failed.", ...filterPlan })
    };
  }
}
async function searchOpenAlex(query, options) {
  const params = buildParams({
    search: query.query,
    "per-page": query.limit,
    select: "id,doi,title,display_name,publication_year,authorships,open_access,primary_location,locations,primary_topic,topics,language,cited_by_count,abstract_inverted_index"
  });
  if (query.year && !query.year.includes("-")) {
    params.set("filter", `from_publication_date:${query.year}-01-01,to_publication_date:${query.year}-12-31`);
  } else if (query.year) {
    const [start, end] = query.year.split("-");
    params.set("filter", `from_publication_date:${start}-01-01,to_publication_date:${end}-12-31`);
  }
  const json = await fetchJson(`https://api.openalex.org/works?${params.toString()}`, options);
  return Array.isArray(json.results) ? json.results.map((item) => ({
    title: item.title ?? item.display_name,
    url: item.doi ?? item.primary_location?.landing_page_url ?? item.id,
    snippet: abstractFromInvertedIndex(item.abstract_inverted_index) ?? item.primary_location?.source?.display_name,
    sourceName: item.primary_location?.source?.display_name ?? "OpenAlex",
    publishedAt: normalizePublishedAt(item.publication_year),
    authors: Array.isArray(item.authorships) ? item.authorships.map((authorship) => authorship.author?.display_name).filter(Boolean) : [],
    domains: Array.from(new Set([item.primary_location, ...Array.isArray(item.locations) ? item.locations : []].flatMap((location) => [hostnameFromUrl(location?.landing_page_url), hostnameFromUrl(location?.pdf_url)]).filter(Boolean))),
    fieldsOfStudy: Array.from(new Set([item.primary_topic?.display_name, ...Array.isArray(item.topics) ? item.topics.map((topic) => topic?.display_name) : []].filter(Boolean))),
    locale: item.language,
    doi: item.doi,
    openAccess: item.open_access?.is_oa === true,
    score: Number(item.cited_by_count ?? 0) > 0 ? Math.log10(Number(item.cited_by_count) + 1) : 0
  })) : [];
}
function crossrefOpenAccess(item) {
  const links = Array.isArray(item.link) ? item.link : [];
  if (links.some((link) => /^https?:/iu.test(link?.URL ?? "") && /(?:application\/pdf|text\/html)/iu.test(link?.["content-type"] ?? ""))) {
    return true;
  }
  return item.license ? Array.isArray(item.license) ? item.license.length > 0 : true : null;
}
async function searchCrossref(query, options) {
  const params = buildParams({ query: query.query, rows: query.limit, select: "DOI,title,URL,link,license,author,published,published-print,published-online,container-title,abstract,language,is-referenced-by-count" });
  if (query.year && !query.year.includes("-")) {
    params.set("filter", `from-pub-date:${query.year}-01-01,until-pub-date:${query.year}-12-31`);
  } else if (query.year) {
    const [start, end] = query.year.split("-");
    params.set("filter", `from-pub-date:${start}-01-01,until-pub-date:${end}-12-31`);
  }
  const json = await fetchJson(`https://api.crossref.org/works?${params.toString()}`, options);
  const items = json.message?.items;
  return Array.isArray(items) ? items.map((item) => ({
    title: item.title,
    url: item.URL ?? doiUrl(item.DOI),
    snippet: item.abstract ?? item["container-title"]?.[0],
    sourceName: item["container-title"]?.[0] ?? "Crossref",
    publishedAt: normalizePublishedAt(null, item.published?.["date-parts"] ?? item["published-online"]?.["date-parts"] ?? item["published-print"]?.["date-parts"]),
    authors: normalizeAuthors(item.author),
    domains: Array.from(new Set([hostnameFromUrl(item.URL), ...Array.isArray(item.link) ? item.link.map((link) => hostnameFromUrl(link?.URL)) : []].filter(Boolean))),
    locale: item.language,
    doi: item.DOI,
    openAccess: crossrefOpenAccess(item),
    score: Number(item["is-referenced-by-count"] ?? 0) > 0 ? Math.log10(Number(item["is-referenced-by-count"]) + 1) : 0
  })) : [];
}
async function searchArxiv(query, options) {
  const params = buildParams({
    search_query: `all:${query.query}`,
    start: 0,
    max_results: query.limit,
    sortBy: "relevance",
    sortOrder: "descending"
  });
  const xml = await fetchText(`https://export.arxiv.org/api/query?${params.toString()}`, { ...options, headers: { Accept: "application/atom+xml, text/xml;q=0.9" } });
  return Array.from(xml.matchAll(/<entry>([\s\S]*?)<\/entry>/giu)).map((match) => {
    const entry = match[1];
    const idUrl = extractXmlText(entry, "id");
    const arxivId = idUrl?.split("/abs/")[1]?.replace(/v\d+$/u, "") ?? null;
    return {
      title: extractXmlText(entry, "title"),
      url: idUrl,
      snippet: extractXmlText(entry, "summary"),
      sourceName: "arXiv",
      publishedAt: extractXmlText(entry, "published")?.slice(0, 10),
      authors: Array.from(entry.matchAll(/<author>[\s\S]*?<name>([\s\S]*?)<\/name>[\s\S]*?<\/author>/giu)).map((author) => decodeXml(author[1])),
      domains: [hostnameFromUrl(idUrl)].filter(Boolean),
      arxivId,
      openAccess: true,
      score: 2
    };
  });
}
async function searchEuropePmc(query, options) {
  const params = buildParams({ query: query.query, format: "json", pageSize: query.limit, resultType: "core" });
  const json = await fetchJson(`https://www.ebi.ac.uk/europepmc/webservices/rest/search?${params.toString()}`, options);
  const results = json.resultList?.result;
  return Array.isArray(results) ? results.map((item) => {
    const url = item.doi ? doiUrl(item.doi) : item.pmid ? `https://europepmc.org/article/MED/${item.pmid}` : item.pmcid ? `https://europepmc.org/article/PMC/${item.pmcid}` : null;
    return {
      title: item.title,
      url,
      snippet: item.abstractText,
      sourceName: item.journalTitle ?? "Europe PMC",
      publishedAt: normalizePublishedAt(item.firstPublicationDate ?? item.pubYear),
      authors: item.authorList?.author ? normalizeAuthors(item.authorList.author) : normalizeAuthors(item.authorString),
      domains: [hostnameFromUrl(url)].filter(Boolean),
      locale: item.language ?? item.lang,
      doi: item.doi,
      pubmedId: item.pmid,
      openAccess: item.isOpenAccess === "Y" || item.inEPMC === "Y",
      score: Number(item.citedByCount ?? 0) > 0 ? Math.log10(Number(item.citedByCount) + 1) : 0
    };
  }) : [];
}
var PROVIDER_ADAPTERS = {
  openalex: searchOpenAlex,
  crossref: searchCrossref,
  arxiv: searchArxiv,
  "europe-pmc": searchEuropePmc
};
function buildSearchSummary(status, candidates, reports, query) {
  if (!candidates.length) {
    const failedCount = reports.filter((report) => report.status === "error").length;
    const unavailableCount = reports.filter((report) => report.status === "unavailable").length;
    if (query.kind === "web" || unavailableCount === reports.length) {
      return "\u6CA1\u6709\u53EF\u7528\u7684\u514D key \u901A\u7528\u7F51\u9875\u641C\u7D22 provider\uFF1B\u8FD9\u6B21\u6CA1\u6709\u767B\u8BB0\u4EFB\u4F55\u6765\u6E90\u3002";
    }
    if (failedCount > 0) {
      return "\u8FD9\u6B21\u8054\u7F51\u641C\u7D22\u6CA1\u6709\u5F97\u5230\u53EF\u9A8C\u8BC1\u5019\u9009\uFF0C\u5E76\u4E14\u6709 provider \u5931\u8D25\uFF1B\u4E0D\u8981\u628A\u5B83\u5F53\u6210\u5DF2\u5B8C\u6210\u68C0\u7D22\u3002";
    }
    return "\u8FD9\u6B21\u8054\u7F51\u641C\u7D22\u6CA1\u6709\u5F97\u5230\u53EF\u9A8C\u8BC1\u5019\u9009\uFF1B\u4E0D\u8981\u767B\u8BB0\u6765\u6E90\u6216\u751F\u6210 claim\u3002";
  }
  const warningCount = reports.filter((report) => report.status !== "ok").length;
  return warningCount > 0 ? `\u627E\u5230 ${candidates.length} \u4E2A\u5019\u9009\u6765\u6E90\uFF0C\u4F46\u6709 ${warningCount} \u4E2A provider \u4E0D\u53EF\u7528\u6216\u5931\u8D25\u3002` : `\u627E\u5230 ${candidates.length} \u4E2A\u5019\u9009\u6765\u6E90\u3002`;
}
function buildNeedsAttention(status, reports, candidates) {
  const failed = reports.filter((report) => report.status === "error");
  const unavailable = reports.filter((report) => report.status === "unavailable" || report.status === "disabled");
  if (status === "blocked") {
    return {
      status: "blocked",
      summary: "\u6CA1\u6709\u53EF\u9A8C\u8BC1\u5019\u9009\u6765\u6E90\u3002",
      why: unavailable.length > 0 ? unavailable[0].message : failed[0]?.error ?? "\u641C\u7D22\u8FD4\u56DE\u96F6\u7ED3\u679C\u3002",
      needs: ["\u6362\u4E00\u4E2A\u67E5\u8BE2\u8BCD\uFF0C\u6216\u7528\u5BBF\u4E3B\u516C\u5F00\u641C\u7D22\u6838\u5B9E\u7F51\u9875\u540E\u518D\u767B\u8BB0\u6765\u6E90\u3002"]
    };
  }
  if (failed.length > 0 || unavailable.length > 0) {
    return {
      status: "partial",
      summary: "\u90E8\u5206 provider \u6CA1\u6709\u4EA7\u51FA\u3002",
      why: unavailable[0]?.message ?? failed[0]?.error,
      needs: candidates.length > 0 ? ["\u5148\u4EBA\u5DE5\u6253\u5F00\u5019\u9009\u7ED3\u679C\u6838\u5B9E\uFF0C\u518D\u767B\u8BB0\u4E3A source\u3002"] : []
    };
  }
  return null;
}
async function executeNetworkSearch(rawArgs = {}, config = {}, options = {}) {
  const normalizedConfig = normalizeNetworkSearchConfigForCore(config);
  const query = normalizeNetworkSearchQuery(rawArgs, normalizedConfig);
  if (!normalizedConfig.enabled) {
    return {
      status: "blocked",
      summary: "Dove \u8054\u7F51\u641C\u7D22\u5DF2\u5728\u914D\u7F6E\u4E2D\u5173\u95ED\uFF1B\u8FD9\u6B21\u6CA1\u6709\u6267\u884C\u641C\u7D22\u3002",
      scope: { kind: "search", status: "blocked" },
      query,
      candidates: [],
      providerReports: [],
      nextStep: { label: "\u5F00\u542F networkSearch \u540E\u518D\u641C\u7D22\u3002", why: "\u641C\u7D22\u5173\u95ED\u65F6\u4E0D\u80FD\u751F\u6210\u5019\u9009\u6765\u6E90\u3002" },
      needsAttention: { status: "blocked", summary: "\u8054\u7F51\u641C\u7D22\u5173\u95ED\u3002", needs: ["\u542F\u7528\u516C\u5F00 provider \u540E\u91CD\u8BD5\u3002"] },
      showMore: { text: "\u5C55\u5F00\u7ED3\u679C\u53EF\u67E5\u770B\u67E5\u8BE2\u53C2\u6570\u548C provider \u72B6\u6001\u3002" }
    };
  }
  const disabled = new Set(normalizedConfig.disabledProviderIds);
  const providerIds = selectedProviderIds(query, normalizedConfig);
  const selectedProviders = providerIds.map((id) => PROVIDER_BY_ID.get(id)).filter((provider) => provider && providerVisibleForKind(provider, query.kind));
  const fetchFn = options.fetchFn ?? globalThis.fetch;
  const providerResults = await Promise.all(selectedProviders.map((provider) => {
    if (disabled.has(provider.id) || normalizedConfig.providerSettings?.[provider.id]?.enabled === false) {
      return { candidates: [], report: providerReport(provider, { status: "disabled", message: "Provider disabled in Dove networkSearch config.", ...providerFilterPlan(provider, query) }) };
    }
    return runProvider(provider, query, normalizedConfig, fetchFn);
  }));
  const providerReports = providerResults.map((result) => result.report);
  const allCandidates = providerResults.flatMap((result) => result.candidates);
  const candidates = dedupeAndRank(allCandidates, query);
  const status = candidates.length > 0 ? "ok" : "blocked";
  return {
    status,
    summary: buildSearchSummary(status, candidates, providerReports, query),
    scope: { kind: "search", status, currentFocus: query.query },
    query,
    candidates,
    providerReports,
    nextStep: {
      label: candidates.length > 0 ? "\u6253\u5F00\u5019\u9009\u6765\u6E90\u6838\u5B9E\u6807\u9898\u3001DOI \u548C\u539F\u6587\u3002" : "\u6362\u67E5\u8BE2\u8BCD\u6216\u6539\u7528\u5BBF\u4E3B\u516C\u5F00\u641C\u7D22\u6838\u5B9E\u3002",
      why: "\u8054\u7F51\u641C\u7D22\u53EA\u4EA7\u51FA\u5019\u9009\uFF0C\u4E0D\u80FD\u76F4\u63A5\u767B\u8BB0\u6765\u6E90\u6216\u751F\u6210 claim\u3002",
      requiredActions: candidates.length > 0 ? ["\u6838\u5B9E\u5019\u9009\u6765\u6E90", "\u628A\u9A8C\u8BC1\u8FC7\u7684\u6765\u6E90\u4EA4\u7ED9 source/note/evidence \u6D41\u7A0B"] : ["\u91CD\u65B0\u68C0\u7D22\u6216\u63D0\u4F9B\u53EF\u9A8C\u8BC1 URL"]
    },
    needsAttention: buildNeedsAttention(status, providerReports, candidates),
    showMore: { text: "\u5C55\u5F00\u7ED3\u679C\u53EF\u67E5\u770B\u5019\u9009\u5217\u8868\u548C provider \u72B6\u6001\uFF1B\u9ED8\u8BA4 compact \u4E0D\u5C55\u793A\u539F\u59CB\u8FD4\u56DE\u3002" },
    diagnostics: {
      providerCount: providerReports.length,
      candidateCountBeforeDedupe: allCandidates.length,
      fetchedCount: providerReports.reduce((sum, report) => sum + report.fetchedCount, 0),
      appliedFilters: Array.from(new Set(providerReports.flatMap((report) => report.appliedFilters))),
      unsupportedFilters: Array.from(new Set(providerReports.flatMap((report) => report.unsupportedFilters)))
    }
  };
}
async function searchNetwork(root, args = {}, env = process.env, options = {}) {
  return executeNetworkSearch(args, loadNetworkSearchConfig(root, env), options);
}
function providerStatus(provider, config) {
  const disabled = new Set(config.disabledProviderIds);
  if (provider.unavailable) {
    return publicWebUnavailableReport();
  }
  if (!config.enabled || disabled.has(provider.id) || config.providerSettings?.[provider.id]?.enabled === false) {
    return providerReport(provider, { status: "disabled", message: "Provider disabled by Dove networkSearch config." });
  }
  return providerReport(provider, { status: "available" });
}
function queryNetworkSearchProviders(root, args = {}, env = process.env) {
  const config = normalizeNetworkSearchConfigForCore(loadNetworkSearchConfig(root, env));
  const kind = normalizeString7(args.kind, "all").toLowerCase();
  if (!SEARCH_KINDS.has(kind)) {
    throw new Error(`Dove network search kind must be one of: ${Array.from(SEARCH_KINDS).join(", ")}.`);
  }
  const requestedIds = normalizeProviderIds(args.providerIds ?? args.providers);
  const conflicts = requestedIds.filter((id) => !providerVisibleForKind(PROVIDER_BY_ID.get(id), kind));
  if (conflicts.length > 0) {
    throw new Error(`Dove network search provider-kind conflict: ${conflicts.join(", ")} cannot be used for kind ${kind}.`);
  }
  const providers = NETWORK_SEARCH_PROVIDER_REGISTRY.filter((provider) => requestedIds.length === 0 || requestedIds.includes(provider.id)).filter((provider) => providerVisibleForKind(provider, kind));
  const providerReports = providers.map((provider) => providerStatus(provider, config));
  const availableCount = providerReports.filter((report) => report.status === "available").length;
  const unavailableCount = providerReports.filter((report) => report.status !== "available").length;
  return {
    status: availableCount > 0 ? "ok" : "blocked",
    summary: availableCount > 0 ? `\u5F53\u524D\u6709 ${availableCount} \u4E2A\u516C\u5F00\u514D key \u641C\u7D22 provider \u53EF\u7528\u3002` : "\u5F53\u524D\u6CA1\u6709\u53EF\u7528\u7684\u516C\u5F00\u514D key \u641C\u7D22 provider\u3002",
    scope: { kind: "search", status: availableCount > 0 ? "ok" : "blocked" },
    providers: providerReports,
    defaultProviderIds: config.defaultProviderIds,
    nextStep: {
      label: availableCount > 0 ? "\u76F4\u63A5\u7528\u641C\u7D22\u5DE5\u5177\u53D1\u73B0\u5019\u9009\u6765\u6E90\u3002" : "\u542F\u7528\u516C\u5F00 provider \u6216\u7528\u5BBF\u4E3B\u516C\u5F00\u641C\u7D22\u6838\u5B9E\u3002",
      why: "Dove \u53EA\u5185\u7F6E\u514D key provider\uFF1B\u641C\u7D22\u7ED3\u679C\u4ECD\u9700\u4EBA\u5DE5\u6838\u5B9E\u540E\u518D\u8FDB\u5165\u8BC1\u636E\u94FE\u3002"
    },
    needsAttention: unavailableCount > 0 ? {
      status: availableCount > 0 ? "partial" : "blocked",
      summary: `${unavailableCount} \u4E2A provider \u4E0D\u53EF\u7528\u6216\u5173\u95ED\u3002`,
      needs: ["\u4E0D\u8981\u628A\u4E0D\u53EF\u7528 provider \u5F53\u6210\u5DF2\u68C0\u7D22\u5B8C\u6210\u3002"]
    } : null,
    showMore: { text: "\u5C55\u5F00\u7ED3\u679C\u53EF\u67E5\u770B provider \u80FD\u529B\uFF1B\u9ED8\u8BA4 compact \u4E0D\u5C55\u793A\u5185\u90E8\u5B57\u6BB5\u3002" }
  };
}

// src/core/artifacts.mjs
import fs19 from "node:fs";
import path22 from "node:path";

// src/core/packet-step-result.mjs
import path21 from "node:path";

// src/core/runtime-state.mjs
import crypto10 from "node:crypto";
var LEASE_TTL_MS = 10 * 60 * 1e3;
function summarizeEvents(entries = []) {
  const last = entries.at(-1) ?? null;
  const lastEventType = last?.eventType ?? last?.type ?? null;
  return {
    eventCount: entries.length,
    lastEventType,
    lastRunId: last?.runId ?? null,
    overview: entries.length > 0 ? `${entries.length} autonomous control-plane event(s) recorded. Latest event: ${lastEventType}.` : "No autonomous control-plane events have been recorded yet.",
    eventsPath: ARTIFACT_PATHS.runtimeEvents
  };
}
function summarizeResults(entries = []) {
  const last = entries.at(-1) ?? null;
  const lastCheckpointEntry = [...entries].reverse().find((entry) => entry?.checkpointSnapshot) ?? null;
  const lastEscalationEntry = [...entries].reverse().find((entry) => entry?.escalationSnapshot) ?? null;
  return {
    runCount: entries.length,
    completedCount: entries.filter((entry) => entry.status === "completed").length,
    noopCount: entries.filter((entry) => entry.status === "noop").length,
    errorCount: entries.filter((entry) => entry.status === "error").length,
    checkpointCount: entries.filter((entry) => entry?.checkpointSnapshot).length,
    escalationCount: entries.filter((entry) => entry?.escalationSnapshot).length,
    lastRunId: last?.runId ?? null,
    lastStatus: last?.status ?? "never-run",
    lastOutcome: last?.outcome ?? "not-started",
    lastCheckpointPacketId: lastCheckpointEntry?.checkpointSnapshot?.packetId ?? null,
    lastCheckpointSummary: lastCheckpointEntry?.checkpointSnapshot?.summary ?? null,
    lastCheckpointAt: lastCheckpointEntry?.checkpointSnapshot?.recordedAt ?? null,
    lastEscalationPacketId: lastEscalationEntry?.escalationSnapshot?.packetId ?? null,
    lastEscalationFollowThroughId: lastEscalationEntry?.escalationSnapshot?.followThroughId ?? null,
    lastEscalationAt: lastEscalationEntry?.escalationSnapshot?.recordedAt ?? null,
    overview: entries.length > 0 ? `Autonomous control-plane runs: ${entries.length} total, ${entries.filter((entry) => entry.status === "completed").length} completed, ${entries.filter((entry) => entry.status === "noop").length} no-op, ${entries.filter((entry) => entry.status === "error").length} error.` : "No autonomous control-plane results have been recorded yet.",
    resultsPath: ARTIFACT_PATHS.runtimeResults
  };
}
function loadRuntimeArtifacts(root) {
  return {
    controllerState: normalizeRuntimeControllerState(readJson(root, ARTIFACT_PATHS.runtimeControllerState, createRuntimeControllerState)),
    continuation: normalizeRuntimeContinuationIndex(readJson(root, ARTIFACT_PATHS.runtimeContinuation, createRuntimeContinuationIndex)),
    leases: normalizeRuntimeLeasesIndex(readJson(root, ARTIFACT_PATHS.runtimeLeases, createRuntimeLeasesIndex)),
    events: normalizeRuntimeEventsIndex(readJson(root, ARTIFACT_PATHS.runtimeEvents, createRuntimeEventsIndex)),
    results: normalizeRuntimeResultsIndex(readJson(root, ARTIFACT_PATHS.runtimeResults, createRuntimeResultsIndex))
  };
}
function saveRuntimeArtifacts(root, artifacts) {
  writeJson(root, ARTIFACT_PATHS.runtimeControllerState, artifacts.controllerState);
  writeJson(root, ARTIFACT_PATHS.runtimeContinuation, artifacts.continuation);
  writeJson(root, ARTIFACT_PATHS.runtimeLeases, artifacts.leases);
  writeJson(root, ARTIFACT_PATHS.runtimeEvents, artifacts.events);
  writeJson(root, ARTIFACT_PATHS.runtimeResults, artifacts.results);
}
function appendEvent(artifacts, entry) {
  const entries = [...artifacts.events.entries ?? [], entry];
  artifacts.events = {
    ...artifacts.events,
    entries,
    summary: summarizeEvents(entries),
    updatedAt: entry.recordedAt
  };
}
function appendResult(artifacts, entry) {
  const entries = [...artifacts.results.entries ?? [], entry];
  artifacts.results = {
    ...artifacts.results,
    entries,
    summary: summarizeResults(entries),
    updatedAt: entry.recordedAt
  };
}

// src/core/packet-step-result.mjs
function normalizeString8(value, fallback = "") {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray16(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean))) : [];
}
function plainObject2(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function activeStatus(status) {
  const normalized = String(status ?? "").trim().toLowerCase();
  return !["completed", "killed", ...DOVE_ARCHIVED_TASK_STATUSES].includes(normalized);
}
function normalizeStatus2(value, fallback = "pending") {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  return DOVE_TASK_STATUSES.includes(normalized) ? normalized : fallback;
}
function archivedTaskStatus(task) {
  return [task?.status, task?.lifecycleStatus].some((status) => DOVE_ARCHIVED_TASK_STATUSES.includes(String(status ?? "").trim().toLowerCase()));
}
function normalizeIndex(index = createTaskPacketsIndex()) {
  const base = createTaskPacketsIndex();
  const items = Array.isArray(index.items) ? index.items.filter((item) => item && typeof item === "object" && !Array.isArray(item)) : [];
  const activeInit = items.find((item) => item.level === 0 && activeStatus(item.status)) ?? null;
  const activeItems = items.filter((item) => item.level !== 0 && activeStatus(item.status));
  const lifecycleCounts = {};
  const stageCounts = Object.fromEntries(DOVE_TASK_STAGES.map((stage) => [stage, 0]));
  const domainCounts = Object.fromEntries(DOVE_TASK_DOMAINS.map((domain) => [domain, 0]));
  const levelCounts = {};
  for (const item of items) {
    const status = archivedTaskStatus(item) ? "archived" : normalizeStatus2(item.status ?? item.lifecycleStatus, "pending");
    lifecycleCounts[status] = (lifecycleCounts[status] ?? 0) + 1;
    if (DOVE_TASK_STAGES.includes(item.stage)) stageCounts[item.stage] += 1;
    if (DOVE_TASK_DOMAINS.includes(item.domain)) domainCounts[item.domain] += 1;
    const levelKey = String(Number.isFinite(item.level) ? item.level : 3);
    levelCounts[levelKey] = (levelCounts[levelKey] ?? 0) + 1;
  }
  return {
    ...base,
    ...index,
    version: base.version,
    items,
    taskModel: {
      ...base.taskModel,
      ...index.taskModel && typeof index.taskModel === "object" && !Array.isArray(index.taskModel) ? index.taskModel : {},
      activeInitId: activeInit?.id ?? null,
      activeTaskIds: activeItems.map((item) => item.id)
    },
    lifecycleCounts,
    stageCounts,
    domainCounts,
    levelCounts,
    updatedAt: index.updatedAt ?? null
  };
}
function taskPacketPath(packetId) {
  return path21.join(ARTIFACT_PATHS.taskPacketsPacketsDir, `${packetId}.json`);
}
function taskContextPath(packetId) {
  return path21.join(ARTIFACT_PATHS.packetContextsDir, `${packetId}.json`);
}
function writePacket(root, packet) {
  const { context: _derivedContext, ...packetRecord } = packet;
  writeJson(root, taskPacketPath(packet.id), packetRecord);
  writeJson(root, taskContextPath(packet.id), {
    id: packet.id,
    parentId: packet.parentId,
    rootId: packet.rootId,
    level: packet.level,
    creatorKind: packet.creatorKind,
    stage: packet.stage,
    domain: packet.domain,
    status: packet.status,
    dependencies: packet.dependencies,
    blockedBy: packet.blockedBy,
    lessonIds: packet.lessonIds,
    artifactRefs: packet.artifactRefs,
    evidenceLinks: packet.evidenceLinks,
    sourceIds: packet.sourceIds,
    noteIds: packet.noteIds,
    claimIds: packet.claimIds,
    outputPaths: packet.outputPaths,
    validationEvidencePaths: packet.validationEvidencePaths,
    verificationEvidencePaths: packet.verificationEvidencePaths,
    verifiedCriteria: packet.verifiedCriteria,
    contextPolicy: packet.contextPolicy,
    executionContract: normalizeDoveExecutionContract(packet.executionContract, null),
    currentFocus: packet.currentFocus,
    nextAction: packet.nextAction,
    completedAt: packet.completedAt ?? null,
    blockedReason: packet.blockedReason ?? null,
    killedAt: packet.killedAt ?? null,
    killReason: packet.killReason ?? null,
    ownerRole: packet.ownerRole ?? null,
    nextRole: packet.nextRole ?? null,
    boundary: packet.boundary ?? null,
    boundaryHistory: Array.isArray(packet.boundaryHistory) ? packet.boundaryHistory : [],
    handoff: packet.handoff ?? null,
    lastTransition: packet.lastTransition ?? null,
    updatedAt: packet.updatedAt
  });
}
function indexItem(packet) {
  const { context: _derivedContext, ...item } = packet;
  return {
    ...item,
    completedAt: packet.completedAt ?? null,
    blockedReason: packet.blockedReason ?? null,
    killedAt: packet.killedAt ?? null,
    killReason: packet.killReason ?? null,
    ownerRole: packet.ownerRole ?? null,
    nextRole: packet.nextRole ?? null,
    boundary: packet.boundary ?? null,
    boundaryHistory: Array.isArray(packet.boundaryHistory) ? packet.boundaryHistory : [],
    handoff: packet.handoff ?? null,
    lastTransition: packet.lastTransition ?? null,
    executionContract: normalizeDoveExecutionContract(packet.executionContract, null)
  };
}
function ownerRoleFor(task = {}) {
  if (task.stage === "audit") return "reviewer";
  if (task.stage === "plan") return "planner";
  return "builder";
}
function transitionBoundaryFor(task, status, fields, timestamp) {
  const explicit = normalizeDoveBoundary(fields.boundary, null);
  if (explicit) return { ...explicit, packetId: explicit.packetId ?? task.id, createdAt: explicit.createdAt ?? timestamp };
  const type = normalizeString8(fields.boundaryType, status === "blocked" ? "blocked-boundary" : null);
  if (!type) return null;
  return normalizeDoveBoundary({
    id: normalizeTaskPacketId(fields.boundaryId ?? `boundary-${task.id}-${type}-${Date.now().toString(36)}`),
    type,
    status: "open",
    packetId: task.id,
    runId: fields.runId ?? null,
    sourceSurface: fields.sourceSurface ?? fields.surface ?? null,
    command: fields.command ?? null,
    reason: fields.reason ?? fields.blockedReason ?? fields.stopReason ?? "",
    summary: fields.summary ?? fields.blockedReason ?? fields.reason ?? fields.stopReason ?? "",
    requiredInputs: fields.requiredInputs ?? [],
    requiredActions: fields.requiredActions ?? [],
    ownerRole: fields.ownerRole ?? task.ownerRole ?? ownerRoleFor(task),
    nextRole: fields.nextRole ?? task.nextRole ?? task.ownerRole ?? ownerRoleFor(task),
    createdAt: timestamp
  }, null);
}
function transitionHandoffFor(task, fields, boundary, timestamp) {
  const ownerRole = normalizeDovePrimaryRoleId(fields.ownerRole ?? task.ownerRole, ownerRoleFor(task));
  const nextRole = normalizeDovePrimaryRoleId(fields.nextRole ?? boundary?.nextRole, ownerRole);
  if (nextRole === ownerRole) return null;
  return {
    id: normalizeTaskPacketId(fields.handoffId ?? `handoff-${task.id}-${ownerRole}-to-${nextRole}-${Date.now().toString(36)}`),
    status: "pending",
    fromRole: ownerRole,
    toRole: nextRole,
    reason: fields.reason ?? boundary?.reason ?? "",
    summary: fields.summary ?? boundary?.summary ?? "",
    boundaryId: boundary?.id ?? null,
    sourceRunId: fields.runId ?? null,
    requestedAt: timestamp
  };
}
function persistentLifecycleFields(fields = {}) {
  const allowedKeys = ["currentFocus", "nextAction", "lessonIds", "artifactRefs", "evidenceLinks", "validationEvidencePaths", "verificationEvidencePaths", "verifiedCriteria", "executionContract", "outputPaths", "claimIds", "noteIds", "experimentIds", "resultIds", "auditIds", "versionIds", "rebuttalIssueIds", "sourceIds", "archivedAt", "archiveReason"];
  return Object.fromEntries(allowedKeys.filter((key) => fields[key] !== void 0).map((key) => [key, fields[key]]));
}
function runtimeEventId(packetId, type, count) {
  return normalizeTaskPacketId(`event-${packetId}-${type}-${Date.now().toString(36)}-${count}`);
}
function recordLifecycleEvents(root, packet, transition, openedBoundary, resolvedBoundary, handoff) {
  const artifacts = loadRuntimeArtifacts(root);
  const baseCount = artifacts.events.entries?.length ?? 0;
  const base = {
    packetId: packet.id,
    runId: transition.runId ?? null,
    surface: transition.surface ?? transition.sourceSurface ?? null,
    command: transition.command ?? null,
    fromStatus: transition.fromStatus,
    toStatus: transition.toStatus,
    summary: transition.summary ?? "",
    evidenceLinks: normalizeStringArray16(transition.evidenceLinks),
    artifactRefs: normalizeStringArray16(transition.artifactRefs),
    timestamp: transition.timestamp,
    recordedAt: transition.timestamp
  };
  const events = [{ ...base, id: runtimeEventId(packet.id, "task-lifecycle-transitioned", baseCount + 1), type: "task.lifecycle.transitioned", eventType: "task.lifecycle.transitioned", boundaryId: openedBoundary?.id ?? resolvedBoundary?.id ?? null, handoffId: handoff?.id ?? null }];
  if (resolvedBoundary) events.push({ ...base, id: runtimeEventId(packet.id, "task-boundary-resolved", baseCount + events.length + 1), type: "task.boundary.resolved", eventType: "task.boundary.resolved", boundaryId: resolvedBoundary.id, boundary: resolvedBoundary, handoffId: null });
  if (openedBoundary) events.push({ ...base, id: runtimeEventId(packet.id, "task-boundary-opened", baseCount + events.length + 1), type: "task.boundary.opened", eventType: "task.boundary.opened", boundaryId: openedBoundary.id, boundary: openedBoundary, handoffId: handoff?.id ?? null });
  if (handoff) events.push({ ...base, id: runtimeEventId(packet.id, "task-handoff-requested", baseCount + events.length + 1), type: "task.handoff.requested", eventType: "task.handoff.requested", boundaryId: openedBoundary?.id ?? resolvedBoundary?.id ?? null, handoffId: handoff.id, handoff });
  for (const event of events) appendEvent(artifacts, event);
  saveRuntimeArtifacts(root, artifacts);
}
function updatePacketLifecycle(root, task, status, fields = {}, options = {}) {
  const timestamp = nowIso();
  const fullTask = readTaskPacketCatalog(root).byId.get(task.id) ?? task;
  options.assertCompletionReady?.(root, fullTask, status, resolveDoveResponseLanguage(root, fields));
  const fromStatus = fullTask.status;
  const artifactRefs = fields.artifactRefs !== void 0 ? normalizeStringArray16(fields.artifactRefs) : fullTask.artifactRefs;
  const evidenceLinks = fields.evidenceLinks !== void 0 ? normalizeStringArray16(fields.evidenceLinks) : fullTask.evidenceLinks;
  const statusFields = {};
  if (status === "in-progress" && !fullTask.startedAt) statusFields.startedAt = fields.startedAt ?? timestamp;
  if (status === "completed") {
    statusFields.completedAt = fields.completedAt ?? timestamp;
    statusFields.blockedReason = null;
  }
  if (status === "blocked") statusFields.blockedReason = normalizeString8(fields.blockedReason ?? fields.reason ?? fields.stopReason, fullTask.blockedReason ?? "");
  if (status !== "blocked" && fullTask.status === "blocked") statusFields.blockedReason = null;
  if (status === "killed") {
    statusFields.killedAt = fields.killedAt ?? timestamp;
    statusFields.killReason = normalizeString8(fields.killReason ?? fields.reason, fullTask.killReason ?? "");
  }
  if (status === "archived") {
    statusFields.archivedAt = fields.archivedAt ?? timestamp;
    statusFields.archiveReason = normalizeString8(fields.archiveReason ?? fields.reason, fullTask.archiveReason ?? "");
    statusFields.blockedReason = null;
  }
  const openedBoundary = status === "blocked" || fields.boundary || fields.boundaryType ? transitionBoundaryFor(fullTask, status, { ...fields, artifactRefs }, timestamp) : null;
  const resolvedBoundary = fullTask.boundary?.status === "open" && !openedBoundary && status !== "blocked" ? normalizeDoveBoundary({ ...fullTask.boundary, status: "resolved", resolvedAt: timestamp, resolution: fields.reason ?? `status:${status}` }, null) : null;
  const ownerRole = normalizeDovePrimaryRoleId(fields.ownerRole ?? fullTask.ownerRole, ownerRoleFor(fullTask));
  const nextRole = normalizeDovePrimaryRoleId(fields.nextRole ?? openedBoundary?.nextRole ?? fullTask.nextRole, ownerRole);
  const handoff = transitionHandoffFor(fullTask, { ...fields, ownerRole, nextRole }, openedBoundary, timestamp);
  const lastTransition = { fromStatus, toStatus: status, reason: normalizeString8(fields.reason ?? fields.stopReason ?? fields.blockedReason, ""), summary: normalizeString8(fields.summary, ""), surface: normalizeString8(fields.surface ?? fields.sourceSurface, null), command: normalizeString8(fields.command, null), runId: normalizeString8(fields.runId, null), boundaryId: openedBoundary?.id ?? resolvedBoundary?.id ?? null, handoffId: handoff?.id ?? null, transitionedAt: timestamp };
  const packet = { ...fullTask, ...persistentLifecycleFields(fields), ...statusFields, artifactRefs, evidenceLinks, status, lifecycleStatus: status, ownerRole, nextRole, boundary: openedBoundary, boundaryHistory: [...Array.isArray(fullTask.boundaryHistory) ? fullTask.boundaryHistory : [], ...resolvedBoundary ? [resolvedBoundary] : [], ...openedBoundary ? [openedBoundary] : []].slice(-20), handoff: handoff ?? (resolvedBoundary ? null : fullTask.handoff ?? null), lastTransition, updatedAt: timestamp };
  writePacket(root, packet);
  const currentIndex = normalizeIndex(readJson(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex));
  const item = indexItem(packet);
  const nextIndex = normalizeIndex({ ...currentIndex, items: [...currentIndex.items.filter((existing) => existing.id !== item.id), item].sort((left, right) => (left.level ?? 3) - (right.level ?? 3) || String(left.createdAt ?? "").localeCompare(String(right.createdAt ?? "")) || left.id.localeCompare(right.id)), updatedAt: packet.updatedAt });
  writeJson(root, ARTIFACT_PATHS.taskPacketsIndex, nextIndex);
  const state = loadState(root);
  saveState(root, { ...state, orchestration: { ...state.orchestration, activeTaskIds: nextIndex.taskModel.activeTaskIds, currentFocus: packet.currentFocus ?? state.orchestration.currentFocus, nextAction: packet.nextAction ?? state.orchestration.nextAction } });
  recordLifecycleEvents(root, packet, { ...lastTransition, timestamp, evidenceLinks: fields.evidenceLinks, artifactRefs }, openedBoundary, resolvedBoundary, handoff);
  return packet;
}
function packetContinuationAction(task = {}, output = {}) {
  const outputAction = normalizeString8(output?.nextAction ?? output?.boundary?.nextAction, null);
  if (outputAction && outputAction !== "project:dove.mission") return outputAction;
  const packetAction = normalizeString8(task.nextAction, null);
  return packetAction && packetAction !== "project:dove.mission" ? packetAction : "project:dove.auto";
}
function applyPacketStepResult(root, task, options = {}) {
  const fullTask = readTaskPacketCatalog(root).byId.get(task.id) ?? task;
  const output = plainObject2(options.output);
  if (output.packetLifecycleApplied === true && output.task?.id === fullTask.id) return readTaskPacketCatalog(root).byId.get(fullTask.id) ?? output.task;
  const classified = options.classified;
  if (!classified) throw new Error("applyPacketStepResult requires a classified step result.");
  const artifactRefs = normalizeStringArray16([...normalizeStringArray16(fullTask.artifactRefs), ...normalizeStringArray16(classified.artifactRefs), ...normalizeStringArray16(output.artifactRefs ?? output.artifactPaths)]);
  const evidenceLinks = normalizeStringArray16([...normalizeStringArray16(fullTask.evidenceLinks), ...normalizeStringArray16(classified.evidenceLinks), ...normalizeStringArray16(output.evidenceLinks ?? output.evidencePaths)]);
  const fields = {
    runId: options.runId ?? output.runId ?? null,
    surface: options.surface ?? "dove.auto",
    command: options.command ?? null,
    summary: normalizeString8(options.summary ?? output.summary ?? classified.outcome, classified.outcome),
    reason: normalizeString8(options.reason ?? classified.stopReason, ""),
    stopReason: classified.stopReason,
    currentFocus: normalizeString8(options.currentFocus ?? output.summary ?? output.review?.summary ?? classified.outcome, fullTask.currentFocus ?? fullTask.title),
    nextAction: normalizeString8(options.nextAction, packetContinuationAction(fullTask, output)),
    artifactRefs,
    evidenceLinks,
    validationEvidencePaths: normalizeStringArray16(output.validationEvidencePaths),
    verificationEvidencePaths: normalizeStringArray16(classified.verificationEvidencePaths),
    verifiedCriteria: normalizeDoveVerifiedCriteria(classified.verifiedCriteria),
    executionContract: output.executionContract ?? fullTask.executionContract,
    sourceIds: normalizeStringArray16(output.sourceIds ?? fullTask.sourceIds),
    noteIds: normalizeStringArray16(output.noteIds ?? fullTask.noteIds),
    claimIds: normalizeStringArray16(output.claimIds ?? fullTask.claimIds),
    outputPaths: normalizeStringArray16(output.outputPaths ?? fullTask.outputPaths)
  };
  if (classified.terminal) {
    const boundary = normalizeDoveBoundary(output.boundary, null);
    return updatePacketLifecycle(root, fullTask, "blocked", { ...fields, boundary, boundaryType: boundary?.type ?? normalizeDoveBoundaryType(classified.outcome, "blocked-boundary"), requiredInputs: boundary?.requiredInputs ?? output.requiredInputs ?? [], requiredActions: boundary?.requiredActions ?? classified.requiredActions ?? output.requiredActions ?? [], ownerRole: boundary?.ownerRole ?? output.ownerRole, nextRole: boundary?.nextRole ?? output.nextRole });
  }
  const nextStatus = ["ready", "blocked"].includes(fullTask.status) ? "in-progress" : fullTask.status;
  return updatePacketLifecycle(root, fullTask, nextStatus, fields);
}

// src/core/artifacts.mjs
function slugify7(value) {
  return String(value).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function normalizeIdentifier(value, fallback) {
  return slugify7(value ?? fallback);
}
function normalizeStringArray17(values, fallback = []) {
  const source = Array.isArray(values) ? values : fallback;
  return Array.from(new Set(source.filter((value) => typeof value === "string" && value.trim()).map((value) => value.trim())));
}
function hasNonEmptyString(value) {
  return typeof value === "string" && value.trim();
}
function hasStructuredNoteSynthesis(args = {}) {
  return hasNonEmptyString(args.summary) || normalizeStringArray17(args.quotes).length > 0 || normalizeStringArray17(args.claims).length > 0 || normalizeStringArray17(args.openQuestions).length > 0;
}
function artifactPacketResult(root, target, { command, output, classified, nextAction, currentFocus }) {
  return applyPacketStepResult(root, target.packet, {
    surface: command,
    command,
    output,
    classified,
    nextAction,
    currentFocus
  });
}
function assertNoRetiredArtifactControls(args = {}, actionLabel) {
  const forbidden = Object.keys(args).filter(
    (key) => key.startsWith("policyOverride") || key === "skipBoardUpdate" || key === "skipRefreshDurableSurfaces" || key === "skipFollowThroughReady" || key === "skipSyncPhase"
  );
  if (forbidden.length > 0) {
    throw new Error(`${actionLabel} no longer accepts retired governance input: ${forbidden.join(", ")}.`);
  }
}
var COMPLETION_LIKE_SECTION_STATUSES = /* @__PURE__ */ new Set([
  "ready",
  "review-ready",
  "done",
  "complete",
  "completed",
  "final",
  "approved"
]);
function isCompletionLikeSectionStatus(value) {
  return COMPLETION_LIKE_SECTION_STATUSES.has(String(value ?? "").trim().toLowerCase());
}
function assertSubstantiveDraftForSection(root, section, sectionId) {
  const draftPath = section?.draftPath ?? `${ARTIFACT_PATHS.draftsDir}/${sectionId}.md`;
  const inspection = inspectDeclaredPath(root, draftPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  if (inspection.status !== "existing") {
    throw new Error(`set_section_status cannot mark section ${sectionId} complete without a non-empty draft artifact at ${draftPath}: ${inspection.reason ?? inspection.status}`);
  }
  return inspection.normalizedPath;
}
function artifactGuidanceSummary(root, args = {}, details = {}) {
  const packet = details.packet ?? null;
  const stage = details.stage ?? packet?.stage ?? null;
  const domain = details.domain ?? packet?.domain ?? null;
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: details.surface,
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: details.request ?? args.title ?? args.summary ?? args.body ?? args.intent ?? null,
    roleId: details.roleId,
    subagentSpecialty: details.subagentSpecialty,
    packet,
    currentContext: {
      domain,
      stage,
      primaryRole: details.roleId ?? null
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: details.nextAction ?? null,
    routeHint: details.routeHint ?? details.nextAction ?? null,
    workflowKind: details.workflowKind,
    domain,
    stage,
    tags: details.tags ?? [],
    statusSummary: details.statusSummary
  }));
}
function localizedText2(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function artifactResultCard(root, args = {}, details = {}) {
  return buildCommandResultCard(details, resolveDoveResponseLanguage(root, args));
}
function normalizeRelativePath4(value, fallback) {
  const normalized = String(value ?? fallback).trim().replace(/\\/g, "/").replace(/^\.\//, "");
  return normalized || fallback;
}
function figurePathLooksPortable2(relativePath) {
  if (typeof relativePath !== "string") {
    return false;
  }
  return relativePath.startsWith(".dove/figures/");
}
function placeholderSegmentsForFigure(item) {
  const placeholders = Array.isArray(item.segmentPlaceholders) && item.segmentPlaceholders.length > 0 ? item.segmentPlaceholders : item.requiredVisualElements;
  return placeholders.map((segment, index) => {
    if (typeof segment === "string") {
      return {
        id: `${item.id}-segment-${index + 1}`,
        label: segment,
        stageStatus: "placeholder",
        sourceSectionIds: item.sourceSections,
        targetClaimIds: item.targetClaimIds,
        notes: []
      };
    }
    return {
      id: normalizeIdentifier(segment?.id, `${item.id}-segment-${index + 1}`),
      label: segment?.label ?? segment?.title ?? `Segment ${index + 1}`,
      stageStatus: segment?.stageStatus ?? "placeholder",
      sourceSectionIds: normalizeStringArray17(segment?.sourceSectionIds, item.sourceSections),
      targetClaimIds: normalizeStringArray17(segment?.targetClaimIds, item.targetClaimIds),
      notes: Array.isArray(segment?.notes) ? segment.notes : []
    };
  });
}
function buildFigureContractArtifacts(item, timestamp) {
  const placeholderSegments = placeholderSegmentsForFigure(item);
  const brief = {
    id: item.briefId,
    figureId: item.id,
    stage: "brief",
    stageOrder: 1,
    sourceSections: item.sourceSections,
    sourceArtifactPaths: item.sourceArtifactPaths,
    targetClaimIds: item.targetClaimIds,
    relatedExperimentIds: item.relatedExperimentIds,
    reviewConcernIds: item.reviewConcernIds,
    rebuttalIssueIds: item.rebuttalIssueIds,
    narrativeIntent: item.narrativeIntent,
    requiredVisualElements: item.requiredVisualElements,
    materialRequirements: item.materialRequirements,
    captionIntent: item.captionIntent,
    outputFormat: item.outputFormat,
    downstreamArtifactIds: [item.segmentId, item.templateId, item.editableArtifactId, item.finalArtifactId],
    updatedAt: timestamp
  };
  const segments = {
    id: item.segmentId,
    figureId: item.id,
    stage: "segments",
    stageOrder: 2,
    briefId: item.briefId,
    templateId: item.templateId,
    placeholderSegments,
    updatedAt: timestamp
  };
  const templates = {
    id: item.templateId,
    figureId: item.id,
    stage: "template",
    stageOrder: 3,
    briefId: item.briefId,
    segmentId: item.segmentId,
    templateSvgPath: item.templateSvgPath,
    editableSvgPath: item.editableSvgPath,
    finalSvgPath: item.finalSvgPath,
    generationMode: item.generationMode,
    generationProviderId: item.generationProviderId,
    outputFormat: item.outputFormat,
    templatePlan: `Use ${item.requiredVisualElements.join(", ") || "the planned visual elements"} to build an editable ${item.outputFormat.toUpperCase()} template tied to ${item.targetClaimIds.join(", ") || "the planned claims"}.`,
    updatedAt: timestamp
  };
  const editable = {
    id: item.editableArtifactId,
    figureId: item.id,
    stage: "editable",
    stageOrder: 4,
    templateId: item.templateId,
    editableSvgPath: item.editableSvgPath,
    templateSvgPath: item.templateSvgPath,
    finalSvgPath: item.finalSvgPath,
    generationMode: item.generationMode,
    generationProviderId: item.generationProviderId,
    latestGenerationRunId: item.latestGenerationRunId ?? null,
    captionId: item.captionId ?? null,
    reviewNotes: item.reviewNotes,
    linkedReviewConcernIds: item.reviewConcernIds,
    linkedRebuttalIssueIds: item.rebuttalIssueIds,
    updatedAt: timestamp
  };
  const final = {
    id: item.finalArtifactId,
    figureId: item.id,
    stage: "final-contract",
    stageOrder: 5,
    editableArtifactId: item.editableArtifactId,
    finalSvgPath: item.finalSvgPath,
    sourceSections: item.sourceSections,
    targetClaimIds: item.targetClaimIds,
    relatedExperimentIds: item.relatedExperimentIds,
    reviewConcernIds: item.reviewConcernIds,
    rebuttalIssueIds: item.rebuttalIssueIds,
    generationMode: item.generationMode,
    generationProviderId: item.generationProviderId,
    outputFormat: item.outputFormat,
    captionIntent: item.captionIntent,
    generationRunId: item.latestGenerationRunId ?? null,
    captionId: item.captionId ?? null,
    readinessStatus: item.status === "final" || item.status === "approved" || item.status === "generated" ? "ready-for-finalization" : "needs-review",
    deliveryChecklist: [
      "Confirm material requirements are resolved.",
      "Confirm final SVG came from a validated generation import.",
      "Keep caption and provenance durable before finalization claims."
    ],
    updatedAt: timestamp
  };
  return { brief, segments, templates, editable, final };
}
function responseOwnerForFigureIssue2(issue) {
  if ((issue.rebuttalIssueIds ?? []).length > 0) return "rebuttal-lead";
  if ((issue.experimentIds ?? []).length > 0) return "experiment-planner";
  if ((issue.claimIds ?? []).length > 0) return "researcher";
  return "planner";
}
function figureIssueId2(figureId, code) {
  return `${figureId}-${code}`;
}
function normalizeFigureArtifactPath2(value) {
  if (typeof value !== "string") {
    return null;
  }
  const normalized = value.trim().replace(/\\/g, "/").replace(/^\.\//, "");
  return normalized || null;
}
function isSafeProjectRelativePath2(relativePath) {
  if (!relativePath || path22.posix.isAbsolute(relativePath)) {
    return false;
  }
  const normalized = path22.posix.normalize(relativePath);
  return normalized !== ".." && !normalized.startsWith("../");
}
function arraysEqual(left = [], right = []) {
  return left.length === right.length && left.every((value, index) => value === right[index]);
}
function buildPathIssue({ figure, code, severity, stage, summary, artifactPaths, timestamp, claimIds, experimentIds, reviewConcernIds, rebuttalIssueIds }) {
  return {
    id: figureIssueId2(figure.id, code),
    figureId: figure.id,
    severity,
    code,
    stage,
    summary,
    claimIds: claimIds ?? figure.targetClaimIds,
    experimentIds: experimentIds ?? figure.relatedExperimentIds,
    reviewConcernIds: reviewConcernIds ?? figure.reviewConcernIds,
    rebuttalIssueIds: rebuttalIssueIds ?? figure.rebuttalIssueIds,
    artifactPaths,
    updatedAt: timestamp
  };
}
var FIGURE_QA_STOP_WORDS = /* @__PURE__ */ new Set([
  "and",
  "are",
  "chart",
  "diagram",
  "draw",
  "figure",
  "flow",
  "for",
  "from",
  "generated",
  "graph",
  "into",
  "node",
  "paper",
  "show",
  "shows",
  "the",
  "this",
  "using",
  "with",
  "workflow"
]);
function normalizeQaText(value) {
  return String(value ?? "").toLowerCase().replace(/<[^>]+>/g, " ").replace(/[^a-z0-9]+/g, " ").replace(/\s+/g, " ").trim();
}
function qaTokens(value) {
  return normalizeQaText(value).split(" ").filter((token) => token.length > 2 && !FIGURE_QA_STOP_WORDS.has(token));
}
function textCoversQaLabel(text3, label) {
  const labelTokens = qaTokens(label);
  if (labelTokens.length === 0) {
    return false;
  }
  const textTokens2 = new Set(qaTokens(text3));
  return labelTokens.every((token) => textTokens2.has(token));
}
function textSharesQaToken(text3, targets = []) {
  const textTokens2 = new Set(qaTokens(text3));
  return targets.some((target) => qaTokens(target).some((token) => textTokens2.has(token)));
}
function readSafeFigureJson(root, relativePath) {
  const normalizedPath = normalizeFigureArtifactPath2(relativePath);
  if (!normalizedPath || !isSafeProjectRelativePath2(normalizedPath)) {
    return null;
  }
  return readJson(root, normalizedPath, null);
}
function inspectFigureArtifactPath(root, relativePath, options = {}) {
  const normalizedPath = normalizeFigureArtifactPath2(relativePath);
  if (!normalizedPath || !isSafeProjectRelativePath2(normalizedPath)) {
    return {
      path: relativePath,
      normalizedPath,
      status: "unsafe",
      exists: false,
      file: false,
      reason: "missing or unsafe path"
    };
  }
  const context = currentMutationContext(root);
  const overlayOperation = context?.operations().find((operation) => operation.relativePath === normalizedPath) ?? null;
  if (overlayOperation) {
    const text3 = options.readText ? context.readText(normalizedPath, "") : null;
    const sizeBytes = Buffer.byteLength(text3 ?? context.readText(normalizedPath, ""), "utf8");
    if (options.rejectBookkeeping && isBookkeepingArtifactPath(normalizedPath)) {
      return {
        path: relativePath,
        normalizedPath,
        status: "bookkeeping",
        exists: true,
        file: true,
        sizeBytes,
        reason: "path is a navigation, status, runtime, task, or ledger record rather than substantive work evidence"
      };
    }
    if (options.requireNonEmpty && sizeBytes <= 0) {
      return {
        path: relativePath,
        normalizedPath,
        status: "empty",
        exists: true,
        file: true,
        sizeBytes,
        reason: "path is an empty file"
      };
    }
    return {
      path: relativePath,
      normalizedPath,
      status: "existing",
      exists: true,
      file: true,
      sizeBytes,
      ...options.readText ? { text: text3 } : {}
    };
  }
  return inspectDeclaredPath(root, normalizedPath, options);
}
function readSafeFigureText(root, relativePath) {
  const inspection = inspectFigureArtifactPath(root, relativePath, {
    requireNonEmpty: true,
    rejectBookkeeping: true,
    readText: true
  });
  return inspection.status === "existing" ? inspection.text ?? "" : "";
}
function semanticCoverageStrings(value, field) {
  if (!value || typeof value !== "object") {
    return [];
  }
  return normalizeStringArray17(value[field]);
}
function semanticReviewPassed(value) {
  if (!value || typeof value !== "object") {
    return false;
  }
  const status = String(value.status ?? value.reviewStatus ?? value.verdict ?? "").trim().toLowerCase();
  return value.reviewed === true || value.humanReviewed === true || ["passed", "verified", "approved"].includes(status);
}
function claimHasMaterialProvenance(claim) {
  return normalizeStringArray17(claim?.sourceIds).length > 0 || normalizeStringArray17(claim?.noteIds).length > 0 || normalizeStringArray17(claim?.experimentIds).length > 0 || normalizeStringArray17(claim?.evidenceLinks).length > 0;
}
function buildFigureQa(root) {
  const state = loadState(root);
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const experimentPlans = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const experimentResults = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const reviewConcerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const rebuttalIssues = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const figures = readJson(root, ARTIFACT_PATHS.figuresIndex, { version: 1, items: [], updatedAt: null });
  const briefs = readJson(root, ARTIFACT_PATHS.figureBriefs, { version: 1, items: [], updatedAt: null });
  const segments = readJson(root, ARTIFACT_PATHS.figureSegments, { version: 1, items: [], updatedAt: null });
  const templates = readJson(root, ARTIFACT_PATHS.figureTemplates, { version: 1, items: [], updatedAt: null });
  const editable = readJson(root, ARTIFACT_PATHS.figureEditableIndex, { version: 1, items: [], updatedAt: null });
  const finals = readJson(root, ARTIFACT_PATHS.figureFinalIndex, { version: 1, items: [], updatedAt: null });
  const materials = readJson(root, ARTIFACT_PATHS.figureMaterials, { version: 1, items: [], updatedAt: null });
  const generations = readJson(root, ARTIFACT_PATHS.figureGenerations, { version: 1, items: [], updatedAt: null });
  const captions = readJson(root, ARTIFACT_PATHS.figureCaptions, { version: 1, items: [], updatedAt: null });
  const claimIds = new Set((evidence.claims ?? []).map((claim) => claim.id));
  const sectionIds = new Set(Object.keys(state.sections ?? {}));
  const experimentIds = /* @__PURE__ */ new Set([
    ...(experimentPlans.items ?? []).map((item) => item.id),
    ...(experimentResults.items ?? []).map((item) => item.experimentId)
  ]);
  const reviewConcernIds = new Set((reviewConcerns.items ?? []).map((item) => item.id));
  const rebuttalIssueIds = new Set((rebuttalIssues.items ?? []).map((item) => item.id));
  const briefByFigure = new Map((briefs.items ?? []).map((item) => [item.figureId, item]));
  const segmentByFigure = new Map((segments.items ?? []).map((item) => [item.figureId, item]));
  const templateByFigure = new Map((templates.items ?? []).map((item) => [item.figureId, item]));
  const editableByFigure = new Map((editable.items ?? []).map((item) => [item.figureId, item]));
  const finalByFigure = new Map((finals.items ?? []).map((item) => [item.figureId, item]));
  const materialByFigure = new Map((materials.items ?? []).map((item) => [item.figureId, item]));
  const generationsByFigure = /* @__PURE__ */ new Map();
  for (const generation of generations.items ?? []) {
    const current = generationsByFigure.get(generation.figureId) ?? [];
    current.push(generation);
    generationsByFigure.set(generation.figureId, current);
  }
  const captionByFigure = new Map((captions.items ?? []).map((item) => [item.figureId, item]));
  const stagePathUsage = /* @__PURE__ */ new Map();
  const timestamp = nowIso();
  const items = [];
  const issues = [];
  function normalizeFigureListField(figure, field, stage, artifactPaths) {
    const rawValue = figure[field];
    const normalized = normalizeStringArray17(rawValue);
    const malformed = rawValue !== void 0 && rawValue !== null && !Array.isArray(rawValue);
    if (malformed) {
      issues.push(buildPathIssue({
        figure,
        code: `malformed-${field}`,
        severity: "medium",
        stage,
        summary: `Figure ${figure.id} has a malformed ${field} field and it was ignored during QA validation.`,
        artifactPaths,
        timestamp
      }));
    }
    return normalized;
  }
  for (const figure of figures.items ?? []) {
    for (const [field, stage] of [["templateSvgPath", "template"], ["editableSvgPath", "editable"], ["finalSvgPath", "final-contract"]]) {
      const normalizedPath = normalizeFigureArtifactPath2(figure[field]);
      if (!normalizedPath || !isSafeProjectRelativePath2(normalizedPath)) {
        continue;
      }
      const current = stagePathUsage.get(normalizedPath) ?? [];
      current.push({ figureId: figure.id, field, stage });
      stagePathUsage.set(normalizedPath, current);
    }
  }
  for (const figure of figures.items ?? []) {
    const figureIssues = [];
    const sourceSections = normalizeFigureListField(figure, "sourceSections", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const sourceArtifactPaths = normalizeFigureListField(figure, "sourceArtifactPaths", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const targetClaimIds = normalizeFigureListField(figure, "targetClaimIds", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const relatedExperimentIds = normalizeFigureListField(figure, "relatedExperimentIds", "brief", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa]);
    const reviewConcernIdsForFigure = normalizeFigureListField(figure, "reviewConcernIds", "editable", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureQa]);
    const rebuttalIssueIdsForFigure = normalizeFigureListField(figure, "rebuttalIssueIds", "final-contract", [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa]);
    const brief = briefByFigure.get(figure.id);
    const segment = segmentByFigure.get(figure.id);
    const template = templateByFigure.get(figure.id);
    const editableArtifact = editableByFigure.get(figure.id);
    const finalArtifact = finalByFigure.get(figure.id);
    const stageArtifacts = {
      brief: Boolean(brief),
      segments: Boolean(segment),
      template: Boolean(template),
      editable: Boolean(editableArtifact),
      finalContract: Boolean(finalArtifact)
    };
    const stageArtifactPaths = [
      ARTIFACT_PATHS.figuresIndex,
      ARTIFACT_PATHS.figureBriefs,
      ARTIFACT_PATHS.figureSegments,
      ARTIFACT_PATHS.figureTemplates,
      ARTIFACT_PATHS.figureEditableIndex,
      ARTIFACT_PATHS.figureFinalIndex,
      ARTIFACT_PATHS.figureMaterials,
      ARTIFACT_PATHS.figureGenerations,
      ARTIFACT_PATHS.figureCaptions,
      ARTIFACT_PATHS.figureQa
    ];
    const fileChecks = {
      stagedArtifacts: {},
      sourceArtifacts: []
    };
    for (const [stageName, present] of Object.entries(stageArtifacts)) {
      if (!present) {
        figureIssues.push({
          id: figureIssueId2(figure.id, `missing-${stageName}`),
          figureId: figure.id,
          severity: "high",
          code: "missing-stage-artifact",
          stage: stageName,
          summary: `Figure ${figure.id} is missing the ${stageName} stage artifact.`,
          claimIds: figure.targetClaimIds,
          experimentIds: figure.relatedExperimentIds,
          reviewConcernIds: figure.reviewConcernIds,
          rebuttalIssueIds: figure.rebuttalIssueIds,
          artifactPaths: stageArtifactPaths,
          updatedAt: timestamp
        });
      }
    }
    if (targetClaimIds.length === 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "missing-claim-linkage"),
        figureId: figure.id,
        severity: "high",
        code: "missing-claim-linkage",
        stage: "brief",
        summary: `Figure ${figure.id} has no linked target claims.`,
        claimIds: [],
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const missingSections = sourceSections.filter((sectionId) => !sectionIds.has(sectionId));
    if (missingSections.length > 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "missing-source-sections"),
        figureId: figure.id,
        severity: "medium",
        code: "missing-source-sections",
        stage: "brief",
        summary: `Figure ${figure.id} references unknown source sections: ${missingSections.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const missingClaims = targetClaimIds.filter((claimId) => !claimIds.has(claimId));
    if (missingClaims.length > 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "unknown-claims"),
        figureId: figure.id,
        severity: "high",
        code: "unknown-claims",
        stage: "brief",
        summary: `Figure ${figure.id} references unknown target claims: ${missingClaims.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const missingExperiments = relatedExperimentIds.filter((experimentId) => !experimentIds.has(experimentId));
    if (missingExperiments.length > 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "unknown-experiments"),
        figureId: figure.id,
        severity: "medium",
        code: "unknown-experiments",
        stage: "brief",
        summary: `Figure ${figure.id} references unknown related experiments: ${missingExperiments.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const missingReviewConcerns = reviewConcernIdsForFigure.filter((id) => !reviewConcernIds.has(id));
    if (missingReviewConcerns.length > 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "unknown-review-concerns"),
        figureId: figure.id,
        severity: "medium",
        code: "unknown-review-concerns",
        stage: "editable",
        summary: `Figure ${figure.id} references unknown review concerns: ${missingReviewConcerns.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const missingRebuttalIssues = rebuttalIssueIdsForFigure.filter((id) => !rebuttalIssueIds.has(id));
    if (missingRebuttalIssues.length > 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "unknown-rebuttal-issues"),
        figureId: figure.id,
        severity: "medium",
        code: "unknown-rebuttal-issues",
        stage: "final-contract",
        summary: `Figure ${figure.id} references unknown rebuttal issues: ${missingRebuttalIssues.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const portablePathProblems = [figure.templateSvgPath, figure.editableSvgPath, figure.finalSvgPath].filter((relativePath) => !figurePathLooksPortable2(relativePath));
    if (portablePathProblems.length > 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "non-portable-paths"),
        figureId: figure.id,
        severity: "high",
        code: "non-portable-paths",
        stage: "template",
        summary: `Figure ${figure.id} uses non-portable artifact paths: ${portablePathProblems.join(", ")}.`,
        claimIds: targetClaimIds,
        experimentIds: relatedExperimentIds,
        reviewConcernIds: reviewConcernIdsForFigure,
        rebuttalIssueIds: rebuttalIssueIdsForFigure,
        artifactPaths: [ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const stagePathChecks = [
      {
        field: "templateSvgPath",
        stage: "template",
        label: "template SVG",
        value: figure.templateSvgPath,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureQa]
      },
      {
        field: "editableSvgPath",
        stage: "editable",
        label: "editable SVG",
        value: figure.editableSvgPath,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureQa]
      },
      {
        field: "finalSvgPath",
        stage: "final-contract",
        label: "final SVG",
        value: figure.finalSvgPath,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa]
      }
    ];
    for (const pathCheck of stagePathChecks) {
      const normalizedPath = normalizeFigureArtifactPath2(pathCheck.value);
      const inspection = normalizedPath ? inspectFigureArtifactPath(root, normalizedPath, { requireNonEmpty: true, rejectBookkeeping: true }) : { status: "unsafe", reason: "missing path" };
      const exists = inspection.status === "existing";
      fileChecks.stagedArtifacts[pathCheck.field] = { path: normalizedPath, exists, status: inspection.status, reason: inspection.reason ?? null };
      if (!normalizedPath || inspection.status === "unsafe") {
        figureIssues.push(buildPathIssue({
          figure,
          code: `malformed-${pathCheck.field}`,
          severity: "high",
          stage: pathCheck.stage,
          summary: `Figure ${figure.id} has a malformed ${pathCheck.label.toLowerCase()} path.`,
          artifactPaths: pathCheck.artifactPaths,
          timestamp
        }));
        continue;
      }
      if (!exists) {
        figureIssues.push(buildPathIssue({
          figure,
          code: `${inspection.status}-${pathCheck.field}-file`,
          severity: "high",
          stage: pathCheck.stage,
          summary: `Figure ${figure.id} cannot use the ${pathCheck.label.toLowerCase()} file at ${normalizedPath}: ${inspection.reason ?? inspection.status}.`,
          artifactPaths: pathCheck.artifactPaths,
          timestamp
        }));
      }
    }
    const distinctStagePaths = stagePathChecks.map((item) => normalizeFigureArtifactPath2(item.value)).filter((item) => item && isSafeProjectRelativePath2(item));
    if (new Set(distinctStagePaths).size !== distinctStagePaths.length) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "colliding-stage-paths",
        severity: "high",
        stage: "template",
        summary: `Figure ${figure.id} reuses the same file path for multiple staged SVG artifacts.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    for (const pathCheck of stagePathChecks) {
      const normalizedPath = normalizeFigureArtifactPath2(pathCheck.value);
      const collisions = normalizedPath ? stagePathUsage.get(normalizedPath) ?? [] : [];
      if (collisions.some((entry) => entry.figureId !== figure.id)) {
        const otherFigureIds = Array.from(new Set(collisions.filter((entry) => entry.figureId !== figure.id).map((entry) => entry.figureId))).sort();
        figureIssues.push(buildPathIssue({
          figure,
          code: `shared-${pathCheck.field}`,
          severity: "high",
          stage: pathCheck.stage,
          summary: `Figure ${figure.id} shares ${pathCheck.label.toLowerCase()} path ${normalizedPath} with ${otherFigureIds.join(", ")}.`,
          artifactPaths: pathCheck.artifactPaths,
          timestamp
        }));
      }
    }
    const normalizedSourceArtifactPaths = sourceArtifactPaths.map(normalizeFigureArtifactPath2);
    for (let index = 0; index < normalizedSourceArtifactPaths.length; index += 1) {
      const sourcePath = normalizedSourceArtifactPaths[index];
      const inspection = sourcePath ? inspectDeclaredPath(root, sourcePath, { requireNonEmpty: true, rejectBookkeeping: true }) : { status: "unsafe", reason: "missing path" };
      const exists = inspection.status === "existing";
      fileChecks.sourceArtifacts.push({ path: sourcePath, exists, status: inspection.status, reason: inspection.reason ?? null });
      if (!sourcePath || inspection.status === "unsafe") {
        figureIssues.push(buildPathIssue({
          figure,
          code: `malformed-source-artifact-${index + 1}`,
          severity: "medium",
          stage: "brief",
          summary: `Figure ${figure.id} has a malformed source artifact path entry.`,
          artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
          timestamp
        }));
        continue;
      }
      if (!exists) {
        figureIssues.push(buildPathIssue({
          figure,
          code: `${inspection.status}-source-artifact-${index + 1}`,
          severity: "medium",
          stage: "brief",
          summary: `Figure ${figure.id} cannot use source artifact ${sourcePath}: ${inspection.reason ?? inspection.status}.`,
          artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
          timestamp
        }));
      }
    }
    const templatePathMismatch = template && [
      template.templateSvgPath !== figure.templateSvgPath,
      template.editableSvgPath !== figure.editableSvgPath,
      template.finalSvgPath !== figure.finalSvgPath
    ].some(Boolean);
    if (templatePathMismatch) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "inconsistent-template-stage-paths",
        severity: "high",
        stage: "template",
        summary: `Figure ${figure.id} has inconsistent staged SVG paths between the figure index and template artifact.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    const editablePathMismatch = editableArtifact && [
      editableArtifact.templateSvgPath !== figure.templateSvgPath,
      editableArtifact.editableSvgPath !== figure.editableSvgPath,
      editableArtifact.finalSvgPath !== figure.finalSvgPath
    ].some(Boolean);
    if (editablePathMismatch) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "inconsistent-editable-stage-paths",
        severity: "high",
        stage: "editable",
        summary: `Figure ${figure.id} has inconsistent staged SVG paths between the figure index and editable artifact.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (finalArtifact && finalArtifact.finalSvgPath !== figure.finalSvgPath) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "inconsistent-final-contract-path",
        severity: "high",
        stage: "final-contract",
        summary: `Figure ${figure.id} has inconsistent final SVG paths between the figure index and final contract.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (brief && !arraysEqual(brief.sourceArtifactPaths ?? [], figure.sourceArtifactPaths ?? [])) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "inconsistent-source-artifact-paths",
        severity: "medium",
        stage: "brief",
        summary: `Figure ${figure.id} has inconsistent source artifact paths between the figure index and brief artifact.`,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (editableArtifact && editableArtifact.finalSvgPath !== figure.finalSvgPath) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "inconsistent-final-path"),
        figureId: figure.id,
        severity: "high",
        code: "inconsistent-final-path",
        stage: "editable",
        summary: `Figure ${figure.id} has inconsistent final SVG paths across staged artifacts.`,
        claimIds: figure.targetClaimIds,
        experimentIds: figure.relatedExperimentIds,
        reviewConcernIds: figure.reviewConcernIds,
        rebuttalIssueIds: figure.rebuttalIssueIds,
        artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    const materialRecord = materialByFigure.get(figure.id);
    const figureGenerations = generationsByFigure.get(figure.id) ?? [];
    const importedGeneration = figureGenerations.find((item) => item.status === "imported" && item.finalSvgPath === figure.finalSvgPath) ?? figureGenerations.find((item) => item.status === "imported");
    const caption = captionByFigure.get(figure.id);
    const outputManifest = readSafeFigureJson(root, importedGeneration?.outputManifestPath);
    const finalSvgContent = readSafeFigureText(root, figure.finalSvgPath);
    const semanticCoverage = outputManifest?.semanticCoverage ?? importedGeneration?.semanticCoverage ?? {};
    const semanticReview = outputManifest?.semanticReview ?? importedGeneration?.semanticReview ?? semanticCoverage;
    const declaredVisualCoverage = normalizeStringArray17([
      ...semanticCoverageStrings(semanticCoverage, "visualElements"),
      ...semanticCoverageStrings(semanticCoverage, "coveredVisualElements"),
      ...normalizeStringArray17(outputManifest?.visualElements)
    ]);
    const semanticText = [
      caption?.text,
      finalSvgContent,
      outputManifest?.caption,
      outputManifest?.description,
      outputManifest?.revisedPrompt,
      semanticCoverage.summary
    ].filter(Boolean).join("\n");
    const requiredVisualElements = normalizeStringArray17(figure.requiredVisualElements);
    const coveredVisualElements = requiredVisualElements.filter((item) => declaredVisualCoverage.some((covered) => textCoversQaLabel(covered, item)) || textCoversQaLabel(semanticText, item));
    const missingVisualElements = requiredVisualElements.filter((item) => !coveredVisualElements.includes(item));
    const linkedClaims = targetClaimIds.map((claimId) => (evidence.claims ?? []).find((claim) => claim.id === claimId)).filter(Boolean);
    const ungroundedClaims = linkedClaims.filter((claim) => !claimHasMaterialProvenance(claim));
    const captionText = caption?.text ?? "";
    const captionGrounded = Boolean(captionText.trim()) && (linkedClaims.length === 0 ? textSharesQaToken(captionText, [figure.narrativeIntent, figure.purpose, ...sourceSections]) : linkedClaims.every((claim) => textSharesQaToken(captionText, [claim.text, claim.summary, claim.gap, ...claim.sourceIds ?? [], ...claim.noteIds ?? [], ...claim.experimentIds ?? []])));
    const rasterWrapper = Boolean(importedGeneration?.rasterImagePath ?? outputManifest?.rasterImagePath) || /<image\b/i.test(finalSvgContent);
    const rasterReviewPassed = !rasterWrapper || semanticReviewPassed(semanticReview);
    const figureSemanticCoverage = {
      requiredVisualElements,
      coveredVisualElements,
      missingVisualElements,
      captionGrounded,
      rasterWrapper,
      rasterSemanticReviewPassed: rasterReviewPassed,
      materialProvenanceReady: Boolean(materialRecord) && (materialRecord.missingRequirementIds ?? []).length === 0 && ungroundedClaims.length === 0
    };
    if (!materialRecord) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "missing-material-record",
        severity: "medium",
        stage: "materials",
        summary: `Figure ${figure.id} has no material-discovery record.`,
        artifactPaths: [ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    } else if ((materialRecord.missingRequirementIds ?? []).length > 0) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "missing-generation-materials",
        severity: "medium",
        stage: "materials",
        summary: `Figure ${figure.id} has unresolved material requirements: ${materialRecord.missingRequirementIds.join(", ")}.`,
        artifactPaths: [ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (ungroundedClaims.length > 0) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "ungrounded-claim-materials",
        severity: "high",
        stage: "materials",
        summary: `Figure ${figure.id} links claims without source, note, experiment, or evidence provenance: ${ungroundedClaims.map((claim) => claim.id).join(", ")}.`,
        artifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (figureGenerations.length === 0) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "missing-generation-run",
        severity: "medium",
        stage: "generation",
        summary: `Figure ${figure.id} has no prepared or imported generation run.`,
        artifactPaths: [ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    } else if (!importedGeneration && figure.status === "generated") {
      figureIssues.push(buildPathIssue({
        figure,
        code: "missing-imported-generation",
        severity: "high",
        stage: "generation",
        summary: `Figure ${figure.id} is marked generated but has no imported generation run.`,
        artifactPaths: [ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (importedGeneration && importedGeneration.packetId && materialRecord?.packetId && importedGeneration.packetId !== materialRecord.packetId) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "generation-material-packet-mismatch",
        severity: "high",
        stage: "generation",
        summary: `Figure ${figure.id} generation packet does not match its material packet.`,
        artifactPaths: [ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (rasterWrapper && !rasterReviewPassed) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "raster-semantic-review-required",
        severity: "medium",
        stage: "generation",
        summary: `Figure ${figure.id} wraps a raster image in SVG; safety validation passed, but semantic visual coverage still needs explicit review evidence.`,
        artifactPaths: [ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureQa, importedGeneration?.outputManifestPath, figure.finalSvgPath].filter(Boolean),
        timestamp
      }));
    }
    if (!caption) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "missing-caption",
        severity: "medium",
        stage: "caption",
        summary: `Figure ${figure.id} has no durable caption explaining its purpose.`,
        artifactPaths: [ARTIFACT_PATHS.figureCaptions, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    } else if (!caption.text || !String(caption.text).trim()) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "empty-caption",
        severity: "high",
        stage: "caption",
        summary: `Figure ${figure.id} has an empty caption.`,
        artifactPaths: [ARTIFACT_PATHS.figureCaptions, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    } else if (!captionGrounded) {
      figureIssues.push(buildPathIssue({
        figure,
        code: "ungrounded-caption",
        severity: "high",
        stage: "caption",
        summary: `Figure ${figure.id} caption is not grounded in linked claims, materials, or declared figure intent.`,
        artifactPaths: [ARTIFACT_PATHS.figureCaptions, ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.figureQa],
        timestamp
      }));
    }
    if (((figure.reviewConcernIds ?? []).length > 0 || (figure.rebuttalIssueIds ?? []).length > 0) && (figure.reviewNotes ?? []).length === 0) {
      figureIssues.push({
        id: figureIssueId2(figure.id, "missing-review-notes"),
        figureId: figure.id,
        severity: "medium",
        code: "missing-review-notes",
        stage: "editable",
        summary: `Figure ${figure.id} links to review or rebuttal context but has no durable review notes.`,
        claimIds: figure.targetClaimIds,
        experimentIds: figure.relatedExperimentIds,
        reviewConcernIds: figure.reviewConcernIds,
        rebuttalIssueIds: figure.rebuttalIssueIds,
        artifactPaths: [ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.figureQa],
        updatedAt: timestamp
      });
    }
    issues.push(...figureIssues.map((issue) => ({
      ...issue,
      responseOwnerRole: responseOwnerForFigureIssue2(issue)
    })));
    items.push({
      figureId: figure.id,
      qaStatus: figureIssues.some((issue) => issue.severity === "high") ? "blocked" : figureIssues.length > 0 ? "needs-review" : "ready",
      issueCount: figureIssues.length,
      openIssueIds: figureIssues.map((issue) => issue.id),
      stageArtifacts,
      targetClaimIds: figure.targetClaimIds,
      relatedExperimentIds: figure.relatedExperimentIds,
      reviewConcernIds: figure.reviewConcernIds,
      rebuttalIssueIds: figure.rebuttalIssueIds,
      fileChecks,
      materialStatus: materialRecord?.status ?? "missing",
      generationStatus: importedGeneration?.status ?? (figureGenerations[0]?.status ?? "missing"),
      captionStatus: caption?.text ? "ready" : "missing",
      semanticCoverage: figureSemanticCoverage,
      diagnostics: {
        lexicalVisualCoverage: {
          coveredVisualElements,
          missingVisualElements,
          authoritative: false
        }
      },
      latestGenerationRunId: importedGeneration?.id ?? figureGenerations[0]?.id ?? null,
      latestOutputManifestPath: importedGeneration?.outputManifestPath ?? figureGenerations[0]?.outputManifestPath ?? null,
      captionId: caption?.id ?? null,
      updatedAt: timestamp
    });
  }
  return { version: 1, items, issues, updatedAt: timestamp };
}
function evaluateFigurePipeline(root) {
  return buildFigureQa(root);
}
function summarizeFigureQa(root, figureId, workspaceValidation = {}) {
  const qa = workspaceValidation?.qa && typeof workspaceValidation.qa === "object" ? workspaceValidation.qa : readJson(root, ARTIFACT_PATHS.figureQa, { version: 1, items: [], issues: [], updatedAt: null });
  const issues = (qa.issues ?? []).filter((issue) => issue?.figureId === figureId);
  const item = (qa.items ?? []).find((entry) => entry.figureId === figureId) ?? null;
  return {
    scope: "figure",
    figureId,
    item,
    qaStatus: item?.qaStatus ?? (issues.length > 0 ? "needs-review" : "ready"),
    issueCount: issues.length,
    issues,
    openIssueIds: issues.map((issue) => issue.id),
    qaPath: workspaceValidation?.qaPath ?? ARTIFACT_PATHS.figureQa,
    workspaceIssueCount: typeof workspaceValidation?.issueCount === "number" ? workspaceValidation.issueCount : (qa.issues ?? []).length,
    workspaceFigureCount: typeof workspaceValidation?.figureCount === "number" ? workspaceValidation.figureCount : (qa.items ?? []).length
  };
}
function validateFigurePipeline(root) {
  const qa = buildFigureQa(root);
  writeJson(root, ARTIFACT_PATHS.figureQa, qa);
  refreshDurableSurfaces(root, {
    type: "validate-figure-pipeline",
    summary: `Validated figure pipeline for ${qa.items.length} figures with ${qa.issues.length} issues.`,
    artifactPaths: [ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureSegments, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureCaptions]
  });
  return { figureCount: qa.items.length, issueCount: qa.issues.length, qaPath: ARTIFACT_PATHS.figureQa, qa };
}
function isStrictMode(state, args = {}) {
  return Boolean(args.strictMode ?? state.settings?.strictMode);
}
function assertStrictCondition(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}
function stageRank(stage) {
  const index = PIPELINE_STAGE_ORDER.indexOf(stage);
  return index === -1 ? -1 : index;
}
function assertStageAtLeast(state, minimumStage, message) {
  const currentRank = Math.max(stageRank(state.pipeline?.currentStage), stageRank(state.pipeline?.lastCompletedStage));
  assertStrictCondition(currentRank >= stageRank(minimumStage), message);
}
function updatePipeline(state, currentStage, resumeCommand) {
  return {
    ...state,
    pipeline: {
      ...state.pipeline,
      currentStage,
      lastCompletedStage: currentStage,
      resumeCommand,
      updatedAt: nowIso()
    },
    orchestration: {
      ...state.orchestration,
      phase: currentStage
    }
  };
}
function assertPhaseMutationAllowed(root, stage, role, actionLabel = "Updating the workflow phase") {
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    stage,
    role,
    actionLabel
  );
}
function syncPhase(root, state, { stage, resumeCommand, role, objective, evidenceLinks, experimentIds, rebuttalIssueIds, activeComparisonTargets, intentType, currentFocus, nextAction, reviewRequiredBeforeFinalize } = {}) {
  assertPhaseMutationAllowed(root, stage, role);
  const nextState = updatePipeline(state, stage, resumeCommand);
  saveState(root, nextState);
  upsertSystemOrchestrationBoard(root, {
    phase: stage,
    assignedRole: role,
    objective,
    evidenceLinks,
    experimentIds,
    rebuttalIssueIds,
    activeComparisonTargets,
    intentType,
    currentFocus,
    nextAction,
    ...typeof reviewRequiredBeforeFinalize === "boolean" ? { reviewRequiredBeforeFinalize } : {}
  });
  return nextState;
}
function renderPlan(args, state, board) {
  const zh = state.settings?.responseLanguage !== "en";
  const sections = Array.isArray(args.sections) && args.sections.length > 0 ? args.sections.map((section) => `- [ ] ${section}`).join("\n") : Object.values(state.sections).map((section) => `- [ ] ${section.title}`).join("\n");
  const evidenceGaps = Array.isArray(args.evidenceGaps) && args.evidenceGaps.length > 0 ? args.evidenceGaps.map((item) => `- ${item}`).join("\n") : zh ? "- \u5C1A\u672A\u8BB0\u5F55\u8BC1\u636E gap\u3002" : "- No evidence gaps recorded yet.";
  const milestones = Array.isArray(args.milestones) && args.milestones.length > 0 ? args.milestones.map((item, index) => `${index + 1}. ${item}`).join("\n") : zh ? "1. \u5237\u65B0\u7F16\u6392\u770B\u677F\n2. \u6269\u5C55\u7814\u7A76\u7B80\u62A5\n3. \u89C4\u5212\u5B9E\u9A8C\n4. \u8D77\u8349\u7AE0\u8282\n5. \u8FD0\u884C review loop" : "1. Refresh the orchestration board\n2. Expand research brief\n3. Plan experiments\n4. Draft sections\n5. Run review loop";
  const figures = Array.isArray(args.figures) && args.figures.length > 0 ? args.figures.map((item) => `- ${item}`).join("\n") : zh ? "- \u5C1A\u672A\u89C4\u5212 figures\u3002" : "- No figures planned yet.";
  const boardTasks = board.tasks.length > 0 ? board.tasks.map((task) => `- [${task.status === "done" ? "x" : " "}] ${task.title} (${task.assignedRole}) \u2192 ${task.nextAction}`).join("\n") : zh ? "- \u5C1A\u672A\u8BB0\u5F55\u770B\u677F\u4EFB\u52A1\u3002" : "- No board tasks recorded yet.";
  const blockers = board.blockers.length > 0 ? board.blockers.map((item) => `- [${item.status}] ${item.summary}`).join("\n") : zh ? "- \u6CA1\u6709\u5F00\u653E\u963B\u585E\u3002" : "- No open blockers recorded.";
  if (zh) {
    return [
      "# \u5F53\u524D Dove \u4EFB\u52A1\u8BA1\u5212",
      "",
      `## Thesis

${args.thesis ?? state.dove.thesis}`,
      "",
      `## Audience

${args.audience ?? state.dove.audience}`,
      "",
      `## \u4EFB\u52A1\u76EE\u6807

${board.doveObjective}`,
      "",
      `## \u5F53\u524D\u7126\u70B9

${board.currentFocus}`,
      "",
      `## \u4E0B\u4E00\u6B65

${board.nextAction}`,
      "",
      `## \u7AE0\u8282\u8BA1\u5212

${sections}`,
      "",
      `## \u8BC1\u636E gaps

${evidenceGaps}`,
      "",
      `## \u770B\u677F\u4EFB\u52A1

${boardTasks}`,
      "",
      `## \u6D3B\u8DC3\u963B\u585E

${blockers}`,
      "",
      `## Figures and tables

${figures}`,
      "",
      `## Milestones

${milestones}`,
      "",
      `## Notes

${args.notes ?? "\u5C1A\u65E0\u989D\u5916\u8BA1\u5212 notes\u3002"}`
    ].join("\n");
  }
  return [
    "# Current Dove mission plan",
    "",
    `## Thesis

${args.thesis ?? state.dove.thesis}`,
    "",
    `## Audience

${args.audience ?? state.dove.audience}`,
    "",
    `## Mission objective

${board.doveObjective}`,
    "",
    `## Current focus

${board.currentFocus}`,
    "",
    `## Next action

${board.nextAction}`,
    "",
    `## Section plan

${sections}`,
    "",
    `## Evidence gaps

${evidenceGaps}`,
    "",
    `## Board tasks

${boardTasks}`,
    "",
    `## Active blockers

${blockers}`,
    "",
    `## Figures and tables

${figures}`,
    "",
    `## Milestones

${milestones}`,
    "",
    `## Notes

${args.notes ?? "No additional planning notes yet."}`
  ].join("\n");
}
function renderOutline(args, state, board) {
  const zh = state.settings?.responseLanguage !== "en";
  const placeholder = zh ? "\u5F85\u5B9A" : "TBD";
  const sections = Array.isArray(args.sections) && args.sections.length > 0 ? args.sections : Object.values(state.sections).map((section) => ({ id: section.id, title: section.title, goal: placeholder, status: section.status }));
  if (zh) {
    return [
      "# \u5F53\u524D outline",
      "",
      `- \u6D3B\u8DC3\u9636\u6BB5: ${board.currentPhase}`,
      `- \u5206\u914D\u89D2\u8272: ${board.assignedRole}`,
      `- \u5F53\u524D\u7126\u70B9: ${board.currentFocus}`,
      `- \u4E0B\u4E00\u6B65: ${board.nextAction}`,
      "",
      ...sections.flatMap((section) => [
        `## ${section.title ?? section.id}`,
        "",
        `- Section ID: ${section.id ?? slugify7(section.title)}`,
        `- \u72B6\u6001: ${section.status ?? "planned"}`,
        `- \u76EE\u6807: ${section.goal ?? placeholder}`,
        `- \u8BC1\u636E\u7126\u70B9: ${section.evidenceFocus ?? placeholder}`,
        ""
      ])
    ].join("\n");
  }
  return [
    "# Current outline",
    "",
    `- Active phase: ${board.currentPhase}`,
    `- Assigned role: ${board.assignedRole}`,
    `- Current focus: ${board.currentFocus}`,
    `- Next action: ${board.nextAction}`,
    "",
    ...sections.flatMap((section) => [
      `## ${section.title ?? section.id}`,
      "",
      `- Section ID: ${section.id ?? slugify7(section.title)}`,
      `- Status: ${section.status ?? "planned"}`,
      `- Goal: ${section.goal ?? placeholder}`,
      `- Evidence focus: ${section.evidenceFocus ?? placeholder}`,
      ""
    ])
  ].join("\n");
}
function renderChecklist(state, reviewState, board, plans, results, issues, versions, workspaceIndex) {
  const openItems = Array.isArray(reviewState.openItems) ? reviewState.openItems : [];
  const draftedSections = Object.values(state.sections).filter((section) => section.status !== "planned").length;
  if (state.settings?.responseLanguage !== "en") {
    return [
      "# Dove \u4EFB\u52A1\u68C0\u67E5\u6E05\u5355",
      "",
      "## \u7F16\u6392",
      "",
      `- [ ] \u4FDD\u6301\u9636\u6BB5 \`${board.currentPhase}\` \u7684\u770B\u677F\u6700\u65B0`,
      `- [ ] \u4FDD\u6301\u5F53\u524D\u7126\u70B9\u660E\u786E\uFF1A${board.currentFocus}`,
      `- [ ] \u89E3\u51B3 ${board.blockers.filter((item) => item.status !== "resolved").length} \u4E2A\u672A\u5173\u95ED\u963B\u585E`,
      "- [ ] \u89D2\u8272\u53D8\u5316\u65F6\u8FFD\u52A0 handoff",
      "",
      "## \u7814\u7A76\u8BB0\u5FC6",
      "",
      "- [ ] \u5728 `.dove/sources/index.json` \u6CE8\u518C\u6838\u5FC3\u6765\u6E90",
      "- [ ] \u5728 `.dove/notes/index.json` \u6355\u83B7\u7ED3\u6784\u5316\u7B14\u8BB0",
      "- [ ] \u7EF4\u62A4 `.dove/research/brief.md` \u548C `.dove/research/agenda.json`",
      "",
      "## \u5199\u4F5C\u4E3B\u7EBF",
      "",
      `- [ ] \u8D77\u8349 ${Object.keys(state.sections).length} \u4E2A\u7AE0\u8282\uFF08\u5F53\u524D\u6D3B\u8DC3\uFF1A${draftedSections}\uFF09`,
      "- [ ] \u4FDD\u6301 `.dove/outline/current-outline.md` \u4E0E\u8BA1\u5212\u5BF9\u9F50",
      "",
      "## \u5B9E\u9A8C",
      "",
      `- [ ] \u4FDD\u6301 ${plans.items.length} \u4E2A\u5B9E\u9A8C\u8BA1\u5212\u7531 claim \u9A71\u52A8`,
      `- [ ] \u8BB0\u5F55 ${results.items.length} \u4E2A\u5E26\u8BC1\u636E\u94FE\u63A5\u7684\u5B9E\u9A8C\u7ED3\u679C`,
      "- [ ] \u5C06\u5B9E\u9A8C\u5BA1\u8BA1\u4E0E\u539F\u59CB\u7ED3\u679C\u5206\u5F00\u6301\u4E45\u5316",
      "- [ ] \u4E3A claim \u72B6\u6001\u53D8\u5316\u6301\u4E45\u5316 result-to-claim bridge \u4E8B\u4EF6",
      "",
      "## \u5BA1\u67E5\u4E0E rebuttal",
      "",
      ...openItems.length > 0 ? openItems.map((item) => `- [ ] ${item}`) : ["- [ ] \u8FD0\u884C `project:dove.review` \u5E76\u628A\u53D1\u73B0\u8F6C\u4E3A\u884C\u52A8\u9879\u3002"],
      `- [ ] \u4FDD\u6301 ${(reviewState.unresolvedConcernIds ?? []).length} \u4E2A\u672A\u89E3\u51B3 concern \u5728\u5BA1\u67E5\u8F6E\u6B21\u4E2D\u53EF\u89C1`,
      `- [ ] \u4FDD\u6301 ${issues.items.length} \u4E2A rebuttal issue \u5DF2\u89C4\u8303\u5316\u5E76\u5206\u8BCA`,
      "",
      "## \u7248\u672C\u4E0E\u5DE5\u4F5C\u533A",
      "",
      `- [ ] \u5FEB\u7167\u8BBA\u6587\u7248\u672C\uFF08\u5F53\u524D\u5FEB\u7167\u6570\uFF1A${versions.items.length}\uFF09`,
      `- [ ] \u6BD4\u8F83\u6D3B\u8DC3\u76EE\u6807\uFF1A${board.activeComparisonTargets.join(", ") || "\u65E0"}`,
      `- [ ] \u4FDD\u6301\u5DE5\u4F5C\u533A\u7D22\u5F15\u53EF\u6062\u590D\uFF08${workspaceIndex.activePackets?.length ?? 0} \u4E2A\u6D3B\u8DC3 packet\uFF09`
    ].join("\n");
  }
  return [
    "# Dove mission checklist",
    "",
    "## Orchestration",
    "",
    `- [ ] Keep the board current for phase \`${board.currentPhase}\``,
    `- [ ] Keep current focus explicit: ${board.currentFocus}`,
    `- [ ] Resolve ${board.blockers.filter((item) => item.status !== "resolved").length} open blockers`,
    "- [ ] Append a handoff when roles change",
    "",
    "## Research memory",
    "",
    "- [ ] Register core sources in `.dove/sources/index.json`",
    "- [ ] Capture structured notes in `.dove/notes/index.json`",
    "- [ ] Maintain `.dove/research/brief.md` and `.dove/research/agenda.json`",
    "",
    "## Writing spine",
    "",
    `- [ ] Draft ${Object.keys(state.sections).length} sections (currently active: ${draftedSections})`,
    "- [ ] Keep `.dove/outline/current-outline.md` aligned with the plan",
    "",
    "## Experiments",
    "",
    `- [ ] Keep ${plans.items.length} experiment plans claim-driven`,
    `- [ ] Record ${results.items.length} experiment results with evidence links`,
    "- [ ] Persist experiment audits separately from raw results",
    "- [ ] Persist result-to-claim bridge events for claim state changes",
    "",
    "## Review + rebuttal",
    "",
    ...openItems.length > 0 ? openItems.map((item) => `- [ ] ${item}`) : ["- [ ] Run `project:dove.review` and convert findings into actions."],
    `- [ ] Keep ${(reviewState.unresolvedConcernIds ?? []).length} unresolved concerns visible across review rounds`,
    `- [ ] Keep ${issues.items.length} rebuttal issues normalized and triaged`,
    "",
    "## Versions + workspace",
    "",
    `- [ ] Snapshot paper versions (current snapshots: ${versions.items.length})`,
    `- [ ] Compare active targets: ${board.activeComparisonTargets.join(", ") || "none"}`,
    `- [ ] Keep workspace index resumable (${workspaceIndex.activePackets?.length ?? 0} active packets)`
  ].join("\n");
}
function renderQueryPack(notesIndex, sourcesIndex, agenda, workspaceIndex) {
  return [
    "# Query pack",
    "",
    `- Current focus: ${workspaceIndex.currentFocus ?? "unknown"}`,
    `- Next action: ${workspaceIndex.nextAction ?? "unknown"}`,
    "",
    "## Source IDs",
    "",
    ...sourcesIndex.items.length > 0 ? sourcesIndex.items.map((source) => `- ${source.id}: ${source.title}`) : ["- No sources registered yet."],
    "",
    "## Note prompts",
    "",
    ...notesIndex.items.length > 0 ? notesIndex.items.map((note) => `- ${note.id}: convert into section evidence for ${note.sectionId ?? "unknown section"}`) : ["- No notes captured yet."],
    "",
    "## Research agenda",
    "",
    ...agenda.agenda.length > 0 ? agenda.agenda.map((item) => `- ${item}`) : ["- No research agenda recorded."]
  ].join("\n");
}
function renderWiki(state, board, sourcesIndex, notesIndex, evidenceIndex, concernsIndex, issues, plans, results, versions, workspaceIndex) {
  return [
    "# Paper wiki",
    "",
    `## Thesis

${state.dove.thesis}`,
    "",
    "## Orchestration board",
    "",
    `- Objective: ${board.doveObjective}`,
    `- Phase: ${board.currentPhase}`,
    `- Intent: ${board.intentType}`,
    `- Assigned role: ${board.assignedRole}`,
    `- Current focus: ${board.currentFocus}`,
    `- Next action: ${board.nextAction}`,
    `- Active comparison targets: ${board.activeComparisonTargets.join(", ") || "none"}`,
    "",
    "## Source inventory",
    "",
    ...sourcesIndex.items.length > 0 ? sourcesIndex.items.map((source) => `- ${source.id}: ${source.title}`) : ["- No sources registered yet."],
    "",
    "## Evidence inventory",
    "",
    ...evidenceIndex.claims.length > 0 ? evidenceIndex.claims.map((claim) => `- ${claim.id}: ${claim.text} [${claim.status}/${claim.confidence}]`) : ["- No claims promoted yet."],
    "",
    "## Experiments",
    "",
    ...plans.items.length > 0 ? plans.items.map((plan) => `- ${plan.id}: ${plan.title} (${plan.status})`) : ["- No experiment plans recorded."],
    ...results.items.length > 0 ? ["", "## Experiment results", "", ...results.items.map((result) => `- ${result.id}: ${result.summary || result.outcome}`)] : [],
    "",
    "## Working notes",
    "",
    ...notesIndex.items.length > 0 ? notesIndex.items.map((note) => `- ${note.id}: ${note.summary || note.title}`) : ["- No notes captured yet."],
    "",
    "## Reviewer concerns",
    "",
    ...concernsIndex.items.length > 0 ? concernsIndex.items.map((item) => `- ${item.id}: ${item.summary} (${item.status})`) : ["- No persistent concerns recorded."],
    "",
    "## Rebuttal issues",
    "",
    ...issues.items.length > 0 ? issues.items.map((issue) => `- ${issue.id}: ${issue.summary} (${issue.status})`) : ["- No rebuttal issues recorded."],
    "",
    "## Workspace overview",
    "",
    `- Active roles: ${(workspaceIndex.activeRoles ?? []).join(", ") || "none"}`,
    `- Active packets: ${(workspaceIndex.activePackets ?? []).length}`,
    `- Unresolved concerns: ${(workspaceIndex.unresolvedConcernIds ?? []).join(", ") || "none"}`,
    "",
    "## Version lineage",
    "",
    ...versions.items.length > 0 ? versions.items.map((item) => `- ${item.id}: ${item.summary}`) : ["- No snapshots yet."]
  ].join("\n");
}
function bibEntryType(sourceType) {
  if (sourceType === "paper" || sourceType === "article") {
    return "article";
  }
  if (sourceType === "inproceedings" || sourceType === "conference") {
    return "inproceedings";
  }
  return "misc";
}
function renderBibEntry(source) {
  const authors = Array.isArray(source.authors) && source.authors.length > 0 ? source.authors.join(" and ") : "Unknown";
  const fields = [
    `  author = {${authors}}`,
    `  title = {${source.title ?? "Untitled Source"}}`
  ];
  if (source.year) fields.push(`  year = {${source.year}}`);
  if (source.locator) fields.push(`  howpublished = {${source.locator}}`);
  return `@${bibEntryType(source.sourceType)}{${source.citationKey ?? source.id},
${fields.join(",\n")}
}`;
}
function renderCitationLog(sourcesIndex, citedKeys, missingKeys) {
  return [
    "# Citation log",
    "",
    `- Updated: ${nowIso()}`,
    "",
    "## Registered sources",
    "",
    ...sourcesIndex.items.length > 0 ? sourcesIndex.items.map((source) => `- [${citedKeys.has(source.citationKey ?? source.id) ? "cited" : "registered"}] ${source.citationKey ?? source.id}: ${source.title}`) : ["- No sources registered yet."],
    "",
    "## Missing citation keys",
    "",
    ...missingKeys.length > 0 ? missingKeys.map((key) => `- [missing] ${key}`) : ["- None"]
  ].join("\n");
}
function renderRebuttalDraft(reviewState, claimsIndex, issuesPath, strategyPath, responseDraftPath) {
  return [
    "# Rebuttal Notes",
    "",
    "## Reviewer concerns",
    "",
    ...reviewState.openItems.length > 0 ? reviewState.openItems.map((item) => `- ${item}`) : ["- No open reviewer concerns recorded."],
    "",
    "## Evidence-backed responses",
    "",
    ...claimsIndex.claims.length > 0 ? claimsIndex.claims.map((claim) => `- ${claim.id}: ${claim.text}`) : ["- No evidence-backed claims recorded yet."],
    "",
    "## Durable rebuttal artifacts",
    "",
    `- Issues: ${issuesPath}`,
    `- Strategy: ${strategyPath}`,
    `- Response draft: ${responseDraftPath}`,
    "",
    "## Remaining limitations",
    "",
    "- Document unresolved concerns honestly here."
  ].join("\n");
}
var WIKI_RELATION_DEFINITIONS = {
  "uses-source": {
    familyId: "evidence-grounding",
    familyLabel: "Evidence grounding",
    familySummary: "Links ideas and claims back to durable notes and registered sources.",
    groupId: "idea-source-grounding",
    groupLabel: "Idea-to-source grounding",
    groupSummary: "Shows which note ideas are explicitly backed by registered sources.",
    fromEntityTypes: ["idea"],
    toEntityTypes: ["source"],
    semantics: "Tracks that a note idea draws on a registered source.",
    directionalMeaning: {
      forward: "Idea uses source",
      reverse: "Source informs idea"
    },
    endpointSemantics: {
      from: {
        role: "grounded-idea",
        summary: "A durable note or idea that should stay traceable to source evidence."
      },
      to: {
        role: "registered-source",
        summary: "A bibliographic source that can legitimately ground note content."
      }
    },
    companionRelationTypes: ["supported-by-idea", "supported-by-source"],
    sourceArtifactPaths: [ARTIFACT_PATHS.notes, ARTIFACT_PATHS.sources]
  },
  "supported-by-source": {
    familyId: "evidence-grounding",
    familyLabel: "Evidence grounding",
    familySummary: "Links ideas and claims back to durable notes and registered sources.",
    groupId: "claim-source-support",
    groupLabel: "Claim-to-source support",
    groupSummary: "Shows which claims directly cite registered sources.",
    fromEntityTypes: ["claim"],
    toEntityTypes: ["source"],
    semantics: "Tracks that a claim cites a registered source directly.",
    directionalMeaning: {
      forward: "Claim cites source",
      reverse: "Source supports claim"
    },
    endpointSemantics: {
      from: {
        role: "evidence-backed-claim",
        summary: "A claim that should preserve traceable source coverage."
      },
      to: {
        role: "registered-source",
        summary: "A source that can directly support a durable claim."
      }
    },
    companionRelationTypes: ["supported-by-idea", "uses-source"],
    sourceArtifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.sources]
  },
  "supported-by-idea": {
    familyId: "evidence-grounding",
    familyLabel: "Evidence grounding",
    familySummary: "Links ideas and claims back to durable notes and registered sources.",
    groupId: "claim-idea-grounding",
    groupLabel: "Claim-to-idea grounding",
    groupSummary: "Shows which claims remain anchored to durable notes or ideas.",
    fromEntityTypes: ["claim"],
    toEntityTypes: ["idea"],
    semantics: "Tracks that a claim is grounded in a durable note or idea.",
    directionalMeaning: {
      forward: "Claim is grounded by idea",
      reverse: "Idea informs claim"
    },
    endpointSemantics: {
      from: {
        role: "evidence-backed-claim",
        summary: "A claim that should retain note-level grounding before broader drafting."
      },
      to: {
        role: "durable-note-idea",
        summary: "A note-derived idea that preserves intermediate reasoning or evidence capture."
      }
    },
    companionRelationTypes: ["supported-by-source", "uses-source"],
    sourceArtifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.notes]
  },
  "tested-by-experiment": {
    familyId: "validation-loop",
    familyLabel: "Validation loop",
    familySummary: "Connects claims to experiments so validation obligations stay explicit and resumable.",
    groupId: "claim-experiment-validation",
    groupLabel: "Claim-to-experiment validation",
    groupSummary: "Shows which claims still depend on experimental validation work.",
    fromEntityTypes: ["claim"],
    toEntityTypes: ["experiment"],
    semantics: "Tracks that a claim should be validated by an experiment artifact.",
    directionalMeaning: {
      forward: "Claim is tested by experiment",
      reverse: "Experiment validates claim"
    },
    endpointSemantics: {
      from: {
        role: "validation-target-claim",
        summary: "A claim that still carries an experimental validation obligation."
      },
      to: {
        role: "experiment-artifact",
        summary: "A planned or completed experiment that can validate the claim."
      }
    },
    companionRelationTypes: ["tests-claim"],
    sourceArtifactPaths: [ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults]
  },
  "tests-claim": {
    familyId: "validation-loop",
    familyLabel: "Validation loop",
    familySummary: "Connects claims to experiments so validation obligations stay explicit and resumable.",
    groupId: "experiment-claim-scope",
    groupLabel: "Experiment-to-claim scope",
    groupSummary: "Shows which claims each experiment plan is explicitly scoped to validate.",
    fromEntityTypes: ["experiment"],
    toEntityTypes: ["claim"],
    semantics: "Tracks that an experiment plan is scoped to validate a claim.",
    directionalMeaning: {
      forward: "Experiment tests claim",
      reverse: "Claim is under experiment scope"
    },
    endpointSemantics: {
      from: {
        role: "experiment-artifact",
        summary: "A durable experiment plan or related experiment artifact."
      },
      to: {
        role: "validation-target-claim",
        summary: "A claim that the experiment should confirm, qualify, or refute."
      }
    },
    companionRelationTypes: ["tested-by-experiment"],
    sourceArtifactPaths: [ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.evidence]
  },
  "concerns-claim": {
    familyId: "review-pressure",
    familyLabel: "Review pressure",
    familySummary: "Captures where reviewer pressure or integrity risk is concentrated across claims.",
    groupId: "concern-claim-pressure",
    groupLabel: "Concern-to-claim pressure",
    groupSummary: "Shows which claims are directly challenged by review concerns.",
    fromEntityTypes: ["review concern"],
    toEntityTypes: ["claim"],
    semantics: "Tracks that a review concern challenges or blocks a claim.",
    directionalMeaning: {
      forward: "Concern challenges claim",
      reverse: "Claim is pressured by concern"
    },
    endpointSemantics: {
      from: {
        role: "review-concern",
        summary: "A reviewer-raised concern that needs durable resolution."
      },
      to: {
        role: "pressured-claim",
        summary: "A claim carrying active review pressure or blockage."
      }
    },
    companionRelationTypes: [],
    sourceArtifactPaths: [ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.evidence]
  }
};
function uniqueStringArray(values = []) {
  return Array.from(new Set(values.filter((value) => typeof value === "string" && value.trim()).map((value) => value.trim())));
}
function countBy3(items, resolveKey) {
  return items.reduce((accumulator, item) => {
    const key = resolveKey(item);
    accumulator[key] = (accumulator[key] ?? 0) + 1;
    return accumulator;
  }, {});
}
function summarizeSeverityCounts(items = []) {
  return items.reduce((accumulator, item) => {
    const severity = item?.severity ?? "unknown";
    accumulator[severity] = (accumulator[severity] ?? 0) + 1;
    return accumulator;
  }, {});
}
function expectedEntityTypesFor(definition, endpoint) {
  const key = endpoint === "from" ? "fromEntityTypes" : "toEntityTypes";
  const legacyKey = endpoint === "from" ? "fromEntityType" : "toEntityType";
  return uniqueStringArray(definition?.[key] ?? (definition?.[legacyKey] ? [definition[legacyKey]] : []));
}
function topCountKeys(counts = {}, limit = 3) {
  return Object.entries(counts).sort((left, right) => right[1] - left[1] || left[0].localeCompare(right[0])).slice(0, limit).map(([key]) => key);
}
function buildWikiRelation(root, relation, entityById, relationIdCounts) {
  const definition = WIKI_RELATION_DEFINITIONS[relation.relationType] ?? null;
  const fromEntity = entityById.get(relation.fromId) ?? null;
  const toEntity = entityById.get(relation.toId) ?? null;
  const sourceArtifactPaths = uniqueStringArray(relation.sourceArtifactPaths ?? definition?.sourceArtifactPaths ?? []);
  const expectedFromEntityTypes = expectedEntityTypesFor(definition, "from");
  const expectedToEntityTypes = expectedEntityTypesFor(definition, "to");
  const reasons = [];
  if (!definition) {
    reasons.push({
      code: "unknown-relation-type",
      severity: "medium",
      message: `Relation ${relation.id} uses unsupported relationType ${relation.relationType}.`
    });
  }
  if (!fromEntity) {
    reasons.push({
      code: "dangling-from-entity",
      severity: "high",
      message: `Relation ${relation.id} points to a missing source endpoint ${relation.fromId}.`,
      entityId: relation.fromId
    });
  }
  if (!toEntity) {
    reasons.push({
      code: "dangling-to-entity",
      severity: "high",
      message: `Relation ${relation.id} points to a missing target endpoint ${relation.toId}.`,
      entityId: relation.toId
    });
  }
  if (expectedFromEntityTypes.length > 0 && fromEntity && !expectedFromEntityTypes.includes(fromEntity.entityType)) {
    reasons.push({
      code: "invalid-from-entity-type",
      severity: "high",
      message: `Relation ${relation.id} expects ${expectedFromEntityTypes.join("/")} at ${relation.fromId} but found ${fromEntity.entityType}.`,
      entityId: relation.fromId,
      expectedEntityType: expectedFromEntityTypes[0] ?? null,
      expectedEntityTypes: expectedFromEntityTypes,
      actualEntityType: fromEntity.entityType
    });
  }
  if (expectedToEntityTypes.length > 0 && toEntity && !expectedToEntityTypes.includes(toEntity.entityType)) {
    reasons.push({
      code: "invalid-to-entity-type",
      severity: "high",
      message: `Relation ${relation.id} expects ${expectedToEntityTypes.join("/")} at ${relation.toId} but found ${toEntity.entityType}.`,
      entityId: relation.toId,
      expectedEntityType: expectedToEntityTypes[0] ?? null,
      expectedEntityTypes: expectedToEntityTypes,
      actualEntityType: toEntity.entityType
    });
  }
  if ((relationIdCounts.get(relation.id) ?? 0) > 1) {
    reasons.push({
      code: "duplicate-relation-id",
      severity: "medium",
      message: `Relation id ${relation.id} is duplicated in the typed wiki index.`
    });
  }
  const sourceArtifactChecks = sourceArtifactPaths.map((artifactPath) => {
    const exists = fs19.existsSync(resolvePath(root, artifactPath));
    if (!exists) {
      reasons.push({
        code: "missing-source-artifact-file",
        severity: "high",
        message: `Relation ${relation.id} depends on missing source artifact ${artifactPath}.`,
        artifactPath
      });
    }
    return { artifactPath, exists };
  });
  const endpointChecks = [
    {
      endpoint: "from",
      entityId: relation.fromId,
      exists: Boolean(fromEntity),
      expectedEntityType: expectedFromEntityTypes[0] ?? null,
      expectedEntityTypes: expectedFromEntityTypes,
      actualEntityType: fromEntity?.entityType ?? null
    },
    {
      endpoint: "to",
      entityId: relation.toId,
      exists: Boolean(toEntity),
      expectedEntityType: expectedToEntityTypes[0] ?? null,
      expectedEntityTypes: expectedToEntityTypes,
      actualEntityType: toEntity?.entityType ?? null
    }
  ];
  const integrityStatus = reasons.length > 0 ? "degraded" : "healthy";
  const highestSeverity = reasons.some((reason) => reason.severity === "high") ? "high" : reasons.length > 0 ? "medium" : "none";
  return {
    ...relation,
    fromEntityType: fromEntity?.entityType ?? null,
    toEntityType: toEntity?.entityType ?? null,
    sourceArtifactPaths,
    taxonomy: {
      familyId: definition?.familyId ?? "uncategorized",
      familyLabel: definition?.familyLabel ?? "Uncategorized",
      familySummary: definition?.familySummary ?? "Typed wiki relation family.",
      groupId: definition?.groupId ?? "uncategorized",
      groupLabel: definition?.groupLabel ?? "Uncategorized",
      groupSummary: definition?.groupSummary ?? "Typed wiki relation group.",
      companionRelationTypes: uniqueStringArray(definition?.companionRelationTypes ?? []),
      directionalMeaning: {
        forward: definition?.directionalMeaning?.forward ?? definition?.semantics ?? "Typed wiki relation",
        reverse: definition?.directionalMeaning?.reverse ?? "Reverse meaning not documented."
      },
      endpointSemantics: {
        from: {
          role: definition?.endpointSemantics?.from?.role ?? null,
          summary: definition?.endpointSemantics?.from?.summary ?? null
        },
        to: {
          role: definition?.endpointSemantics?.to?.role ?? null,
          summary: definition?.endpointSemantics?.to?.summary ?? null
        }
      }
    },
    semantics: {
      relationType: relation.relationType,
      label: definition?.semantics ?? "Typed wiki relation",
      expectedFromEntityType: expectedFromEntityTypes[0] ?? null,
      expectedToEntityType: expectedToEntityTypes[0] ?? null,
      expectedFromEntityTypes,
      expectedToEntityTypes,
      directionalMeaning: {
        forward: definition?.directionalMeaning?.forward ?? definition?.semantics ?? "Typed wiki relation",
        reverse: definition?.directionalMeaning?.reverse ?? "Reverse meaning not documented."
      }
    },
    integrity: {
      status: integrityStatus,
      severity: highestSeverity,
      reasons,
      endpointChecks,
      sourceArtifactChecks
    }
  };
}
function buildRelationRepairItem(relation) {
  const reasonCodes = uniqueStringArray((relation.integrity?.reasons ?? []).map((reason) => reason.code));
  const severity = (relation.integrity?.reasons ?? []).reduce((level, reason) => {
    if (reason.severity === "high") return "high";
    return level === "high" ? level : "medium";
  }, "medium");
  const reasonText = (relation.integrity?.reasons ?? []).map((reason) => reason.message).join(" ");
  return {
    id: `repair-${relation.id}`,
    frontierType: "typed-wiki-relation",
    severity,
    relationId: relation.id,
    taxonomyFamilyId: relation.taxonomy?.familyId ?? null,
    taxonomyGroupId: relation.taxonomy?.groupId ?? null,
    summary: `Repair typed wiki relation ${relation.id} (${relation.relationType}, ${relation.taxonomy?.familyLabel ?? "uncategorized"} / ${relation.taxonomy?.groupLabel ?? "uncategorized"}).`,
    reasons: reasonText || `Relation ${relation.id} is degraded.`,
    reasonCodes,
    artifactPath: ARTIFACT_PATHS.wikiRelations,
    relatedArtifactPaths: uniqueStringArray([ARTIFACT_PATHS.wikiEntities, ...relation.sourceArtifactPaths]),
    nextAction: `Repair the local artifacts for ${relation.id}, then rerun project:dove.status or refresh_wiki.`
  };
}
function buildRelationFamilyRepairItem(familySummary) {
  const topReasonCodes = topCountKeys(familySummary.integrityReasonCounts, 4);
  return {
    id: `repair-relation-family-${familySummary.id}`,
    frontierType: "typed-wiki-relation-family",
    severity: familySummary.severity === "high" ? "high" : "medium",
    taxonomyFamilyId: familySummary.id,
    taxonomyGroupIds: familySummary.groupIds,
    summary: `Repair typed wiki relation family ${familySummary.label} (${familySummary.degradedCount}/${familySummary.totalRelations} degraded relations).`,
    reasons: `${familySummary.overview}${topReasonCodes.length > 0 ? ` Top issues: ${topReasonCodes.join(", ")}.` : ""}`,
    reasonCodes: topReasonCodes,
    artifactPath: ARTIFACT_PATHS.wikiRelations,
    relatedArtifactPaths: uniqueStringArray([ARTIFACT_PATHS.wikiEntities, ...familySummary.sourceArtifactPaths]),
    nextAction: `Repair ${familySummary.degradedCount} degraded ${familySummary.label.toLowerCase()} relations, then rerun project:dove.status or refresh_wiki.`
  };
}
function summarizeTaxonomyBucket({ id, label, summary, relations, kind }) {
  const degraded = relations.filter((relation) => relation.integrity?.status === "degraded");
  const integrityReasonCounts = countBy3(degraded.flatMap((relation) => relation.integrity?.reasons ?? []), (reason) => reason.code ?? "unknown");
  const severityCounts = summarizeSeverityCounts(degraded.map((relation) => ({ severity: relation.integrity?.severity ?? "medium" })));
  const sourceArtifactPaths = uniqueStringArray(relations.flatMap((relation) => relation.sourceArtifactPaths ?? []));
  const companionRelationTypes = uniqueStringArray(relations.flatMap((relation) => relation.taxonomy?.companionRelationTypes ?? []));
  const groupIds = uniqueStringArray(relations.map((relation) => relation.taxonomy?.groupId).filter(Boolean));
  const endpointPairs = uniqueStringArray(relations.map((relation) => `${relation.semantics?.expectedFromEntityType ?? relation.fromEntityType ?? "unknown"}->${relation.semantics?.expectedToEntityType ?? relation.toEntityType ?? "unknown"}`));
  const topReasonCodes = topCountKeys(integrityReasonCounts, 3);
  const topRelationType = topCountKeys(countBy3(relations, (relation) => relation.relationType ?? "unknown"), 1)[0] ?? "none";
  return {
    id,
    label,
    summary,
    totalRelations: relations.length,
    healthyCount: relations.length - degraded.length,
    degradedCount: degraded.length,
    relationTypeCounts: countBy3(relations, (relation) => relation.relationType ?? "unknown"),
    integrityReasonCounts,
    severityCounts,
    severity: severityCounts.high > 0 ? "high" : degraded.length > 0 ? "medium" : "none",
    relationIds: relations.map((relation) => relation.id),
    degradedRelationIds: degraded.map((relation) => relation.id),
    groupIds,
    sourceArtifactPaths,
    companionRelationTypes,
    endpointPairs,
    topReasonCodes,
    overview: degraded.length > 0 ? `${label} has ${degraded.length} degraded ${kind} relation${degraded.length === 1 ? "" : "s"} out of ${relations.length}; dominant type ${topRelationType}; top issues ${topReasonCodes.join(", ") || "none"}.` : `${label} covers ${relations.length} ${kind} relation${relations.length === 1 ? "" : "s"} and is currently healthy.`
  };
}
function summarizeWikiRelations(relations) {
  const degraded = relations.filter((relation) => relation.integrity?.status === "degraded");
  const familyBuckets = /* @__PURE__ */ new Map();
  const groupBuckets = /* @__PURE__ */ new Map();
  for (const relation of relations) {
    const familyId = relation.taxonomy?.familyId ?? "uncategorized";
    const groupId = relation.taxonomy?.groupId ?? "uncategorized";
    if (!familyBuckets.has(familyId)) {
      familyBuckets.set(familyId, {
        id: familyId,
        label: relation.taxonomy?.familyLabel ?? "Uncategorized",
        summary: relation.taxonomy?.familySummary ?? "Typed wiki relation family.",
        relations: []
      });
    }
    familyBuckets.get(familyId).relations.push(relation);
    if (!groupBuckets.has(groupId)) {
      groupBuckets.set(groupId, {
        id: groupId,
        label: relation.taxonomy?.groupLabel ?? "Uncategorized",
        summary: relation.taxonomy?.groupSummary ?? "Typed wiki relation group.",
        relations: []
      });
    }
    groupBuckets.get(groupId).relations.push(relation);
  }
  const families = Array.from(familyBuckets.values()).map((bucket) => summarizeTaxonomyBucket({ ...bucket, kind: "family" })).sort((left, right) => right.degradedCount - left.degradedCount || right.totalRelations - left.totalRelations || left.id.localeCompare(right.id));
  const groups = Array.from(groupBuckets.values()).map((bucket) => summarizeTaxonomyBucket({ ...bucket, kind: "group" })).sort((left, right) => right.degradedCount - left.degradedCount || right.totalRelations - left.totalRelations || left.id.localeCompare(right.id));
  const degradedFamilies = families.filter((family) => family.degradedCount > 0);
  const degradedGroups = groups.filter((group) => group.degradedCount > 0);
  return {
    totalRelations: relations.length,
    healthyCount: relations.length - degraded.length,
    degradedCount: degraded.length,
    relationTypeCounts: countBy3(relations, (relation) => relation.relationType ?? "unknown"),
    integrityReasonCounts: countBy3(degraded.flatMap((relation) => relation.integrity?.reasons ?? []), (reason) => reason.code ?? "unknown"),
    repairFrontier: degraded.map(buildRelationRepairItem),
    taxonomyRepairFrontier: degradedFamilies.map(buildRelationFamilyRepairItem),
    taxonomy: {
      familyCount: families.length,
      groupCount: groups.length,
      degradedFamilyCount: degradedFamilies.length,
      degradedGroupCount: degradedGroups.length,
      familyCounts: countBy3(relations, (relation) => relation.taxonomy?.familyId ?? "uncategorized"),
      groupCounts: countBy3(relations, (relation) => relation.taxonomy?.groupId ?? "uncategorized"),
      topDegradedFamilyIds: degradedFamilies.slice(0, 3).map((family) => family.id),
      topDegradedGroupIds: degradedGroups.slice(0, 3).map((group) => group.id),
      families,
      groups,
      overview: families.length > 0 ? `${families.length} typed wiki relation families across ${groups.length} groups; ${degradedFamilies.length} families currently degraded.` : "No typed wiki relation taxonomy has been summarized yet."
    }
  };
}
function createWikiArtifacts(root, state, board, sourcesIndex, notesIndex, evidenceIndex, concernsIndex, decisions, questions, plans, results, issues) {
  const entities = [];
  const relations = [];
  entities.push({ id: "paper-current", entityType: "paper", label: state.dove.title, summary: state.dove.objective, status: board.currentPhase, updatedAt: nowIso() });
  for (const note of notesIndex.items ?? []) {
    entities.push({ id: `idea-${note.id}`, entityType: "idea", label: note.title, summary: note.summary ?? "", sectionId: note.sectionId ?? null, updatedAt: note.updatedAt ?? nowIso() });
    for (const sourceId of note.sourceIds ?? []) {
      relations.push({ id: `idea-${note.id}-uses-${sourceId}`, fromId: `idea-${note.id}`, toId: sourceId, relationType: "uses-source", updatedAt: nowIso() });
    }
  }
  for (const source of sourcesIndex.items ?? []) {
    entities.push({ id: source.id, entityType: "source", label: source.title, summary: source.abstract ?? "", status: source.sourceType ?? "source", updatedAt: source.addedAt ?? nowIso() });
  }
  for (const claim of evidenceIndex.claims ?? []) {
    entities.push({ id: claim.id, entityType: "claim", label: claim.text, summary: `${claim.status}/${claim.confidence}`, sectionId: claim.sectionId, status: claim.status, updatedAt: evidenceIndex.updatedAt ?? nowIso() });
    for (const sourceId of claim.sourceIds ?? []) relations.push({ id: `${claim.id}-supported-by-${sourceId}`, fromId: claim.id, toId: sourceId, relationType: "supported-by-source", updatedAt: nowIso() });
    for (const noteId of claim.noteIds ?? []) relations.push({ id: `${claim.id}-supported-by-note-${noteId}`, fromId: claim.id, toId: `idea-${noteId}`, relationType: "supported-by-idea", updatedAt: nowIso() });
    for (const experimentId of claim.experimentIds ?? []) relations.push({ id: `${claim.id}-tested-by-${experimentId}`, fromId: claim.id, toId: `experiment-${experimentId}`, relationType: "tested-by-experiment", updatedAt: nowIso() });
  }
  for (const plan of plans.items ?? []) {
    entities.push({ id: `experiment-${plan.id}`, entityType: "experiment", label: plan.title, summary: plan.methodology ?? "", status: plan.status, updatedAt: plan.updatedAt ?? nowIso() });
    if (plan.claimId) relations.push({ id: `experiment-${plan.id}-tests-${plan.claimId}`, fromId: `experiment-${plan.id}`, toId: plan.claimId, relationType: "tests-claim", updatedAt: nowIso() });
  }
  for (const concern of concernsIndex.items ?? []) {
    entities.push({ id: concern.id, entityType: "review concern", label: concern.summary, summary: concern.reviewerRationale ?? "", status: concern.status, updatedAt: concern.updatedAt ?? nowIso() });
    for (const claimId of concern.claimIds ?? []) relations.push({ id: `${concern.id}-concerns-${claimId}`, fromId: concern.id, toId: claimId, relationType: "concerns-claim", updatedAt: nowIso() });
  }
  for (const issue of issues.items ?? []) {
    entities.push({ id: `question-${issue.id}`, entityType: "question", label: issue.summary, summary: issue.responseDirection ?? "", status: issue.status, updatedAt: issue.updatedAt ?? nowIso() });
  }
  for (const decision of decisions) {
    entities.push({ id: `decision-${decision.id}`, entityType: "decision", label: decision.summary, summary: decision.rationale ?? "", packetId: decision.packetId ?? null, updatedAt: decision.recordedAt ?? nowIso() });
  }
  for (const question of questions) {
    entities.push({ id: `question-${question.id}`, entityType: "question", label: question.summary, summary: question.origin ?? "", packetId: question.packetId ?? null, sectionId: question.sectionId ?? null, status: question.status, updatedAt: nowIso() });
  }
  const entityById = new Map(entities.map((entity) => [entity.id, entity]));
  const relationIdCounts = relations.reduce((accumulator, relation) => accumulator.set(relation.id, (accumulator.get(relation.id) ?? 0) + 1), /* @__PURE__ */ new Map());
  const typedRelations = relations.map((relation) => buildWikiRelation(root, relation, entityById, relationIdCounts));
  return {
    entities: { version: 1, items: entities, updatedAt: nowIso() },
    relations: { version: 3, items: typedRelations, summary: summarizeWikiRelations(typedRelations), updatedAt: nowIso() }
  };
}
function normalizeMaterialRequirements(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return value.map((item, index) => {
    if (typeof item === "string" && item.trim()) {
      return {
        id: slugify7(item),
        type: "operator-material",
        label: item.trim(),
        status: "needs-operator",
        artifactPath: null,
        summary: "Operator-declared material requirement."
      };
    }
    if (!item || typeof item !== "object") {
      return null;
    }
    const label = item.label ?? item.summary ?? item.id ?? `Material ${index + 1}`;
    return {
      id: slugify7(item.id ?? label),
      type: item.type ?? "operator-material",
      label,
      status: item.status ?? "needs-operator",
      artifactPath: item.artifactPath || item.path ? normalizeRelativePath4(item.artifactPath ?? item.path, null) : null,
      summary: item.summary ?? label
    };
  }).filter(Boolean);
}
function normalizeFigureItem(item = {}, index = 0) {
  const id = slugify7(item.id ?? item.name ?? `figure-${index + 1}`);
  const briefId = `${id}-brief`;
  const templateId = `${id}-template`;
  return {
    id,
    name: item.name ?? item.id ?? `Figure ${index + 1}`,
    purpose: item.purpose ?? item.narrativeIntent ?? "TBD",
    sourceSections: normalizeStringArray17(item.sourceSections, item.sourceSection ? [item.sourceSection] : []),
    sourceArtifactPaths: normalizeStringArray17(item.sourceArtifactPaths, item.sourceArtifactPath ? [item.sourceArtifactPath] : []),
    targetClaimIds: normalizeStringArray17(item.targetClaimIds, item.targetClaimId ? [item.targetClaimId] : []),
    relatedExperimentIds: normalizeStringArray17(item.relatedExperimentIds, item.experimentIds),
    reviewConcernIds: normalizeStringArray17(item.reviewConcernIds, item.reviewConcernId ? [item.reviewConcernId] : []),
    rebuttalIssueIds: normalizeStringArray17(item.rebuttalIssueIds, item.rebuttalIssueId ? [item.rebuttalIssueId] : []),
    narrativeIntent: item.narrativeIntent ?? item.purpose ?? "Explain the linked method/result clearly.",
    requiredVisualElements: normalizeStringArray17(item.requiredVisualElements, Array.isArray(item.inputs) ? item.inputs : []),
    materialRequirements: normalizeMaterialRequirements(item.materialRequirements),
    generationProviderId: typeof item.generationProviderId === "string" && item.generationProviderId.trim() ? item.generationProviderId.trim() : null,
    generationMode: typeof item.generationMode === "string" && item.generationMode.trim() ? item.generationMode.trim() : "prepare-import",
    captionIntent: item.captionIntent ?? item.narrativeIntent ?? item.purpose ?? "Explain what the figure shows and why it matters.",
    outputFormat: typeof item.outputFormat === "string" && item.outputFormat.trim() ? item.outputFormat.trim().toLowerCase() : "svg",
    generationConstraints: normalizeStringArray17(item.generationConstraints),
    latestGenerationRunId: typeof item.latestGenerationRunId === "string" && item.latestGenerationRunId.trim() ? item.latestGenerationRunId.trim() : null,
    captionId: typeof item.captionId === "string" && item.captionId.trim() ? item.captionId.trim() : null,
    owner: item.owner ?? "manual",
    mode: item.mode ?? "manual",
    status: item.status ?? "planned",
    briefId,
    segmentId: `${id}-segments`,
    templateId,
    editableArtifactId: `${id}-editable`,
    finalArtifactId: `${id}-final`,
    templateSvgPath: normalizeRelativePath4(item.templateSvgPath, `.dove/figures/${id}.template.svg`),
    editableSvgPath: normalizeRelativePath4(item.editableSvgPath, `.dove/figures/${id}.editable.svg`),
    finalSvgPath: normalizeRelativePath4(item.finalSvgPath, `.dove/figures/${id}.final.svg`),
    reviewNotes: Array.isArray(item.reviewNotes) ? item.reviewNotes : [],
    segmentPlaceholders: placeholderSegmentsForFigure({
      ...item,
      id,
      sourceSections: normalizeStringArray17(item.sourceSections, item.sourceSection ? [item.sourceSection] : []),
      targetClaimIds: normalizeStringArray17(item.targetClaimIds, item.targetClaimId ? [item.targetClaimId] : []),
      requiredVisualElements: normalizeStringArray17(item.requiredVisualElements, Array.isArray(item.inputs) ? item.inputs : [])
    })
  };
}
function initProject(root, args = {}) {
  assertPhaseMutationAllowed(root, "init", "planner", "Initializing the project workflow");
  ensureWorkspace(root);
  const defaults = createDefaultState();
  let state = loadState(root);
  state = {
    ...state,
    dove: {
      ...state.dove,
      title: args.title ?? state.dove.title,
      venue: args.venue ?? state.dove.venue,
      objective: args.objective ?? state.dove.objective,
      deadline: args.deadline ?? state.dove.deadline,
      thesis: args.thesis ?? state.dove.thesis,
      audience: args.audience ?? state.dove.audience
    },
    settings: {
      ...state.settings,
      strictMode: Boolean(args.strictMode ?? state.settings?.strictMode ?? false)
    },
    sections: state.sections ?? defaults.sections
  };
  const zh = state.settings?.responseLanguage !== "en";
  state = syncPhase(root, state, {
    stage: "init",
    resumeCommand: "project:dove.status",
    role: "planner",
    objective: state.dove.objective,
    intentType: "plan",
    currentFocus: zh ? "\u5BF9\u9F50\u9879\u76EE\u76EE\u6807\u548C\u5DE5\u4F5C\u6D41\u5951\u7EA6\u3002" : "Align the project goal and workflow contract.",
    nextAction: zh ? "\u5237\u65B0\u770B\u677F\uFF0C\u7136\u540E\u6CE8\u518C sources \u548C research questions\u3002" : "Refresh the board, then register sources and research questions."
  });
  writeText(root, ARTIFACT_PATHS.project, zh ? `# \u9879\u76EE\u7B80\u62A5

- \u5DE5\u4F5C\u6807\u9898: ${state.dove.title}
- Venue: ${state.dove.venue}
- \u76EE\u6807: ${state.dove.objective}
- \u622A\u6B62\u65F6\u95F4: ${state.dove.deadline || "\u5F85\u5B9A"}

## Thesis

${state.dove.thesis}

## Audience

${state.dove.audience}
` : `# Project brief

- Working title: ${state.dove.title}
- Venue: ${state.dove.venue}
- Objective: ${state.dove.objective}
- Deadline: ${state.dove.deadline || "TBD"}

## Thesis

${state.dove.thesis}

## Audience

${state.dove.audience}
`);
  writeText(root, ARTIFACT_PATHS.researchContract, zh ? `# \u7814\u7A76\u5951\u7EA6

## \u9879\u76EE

- \u6807\u9898: ${state.dove.title}
- Venue: ${state.dove.venue}
- \u76EE\u6807: ${state.dove.objective}

## \u5DE5\u4F5C\u89C4\u5219

- \u4E0D\u5199\u65E0\u652F\u6491 claims\u3002
- \u4E0D\u51ED\u8BB0\u5FC6\u5F15\u7528\u3002
- \u6BCF\u4E2A\u9636\u6BB5\u540E\u4FDD\u7559\u6301\u4E45\u4EA7\u7269\u3002
- \u4FDD\u6301 orchestration board \u548C handoffs \u6700\u65B0\u3002
- \u9AD8\u98CE\u9669\u53D8\u66F4 finalization \u524D\u5FC5\u987B review\u3002
` : `# Research contract

## Project

- Title: ${state.dove.title}
- Venue: ${state.dove.venue}
- Objective: ${state.dove.objective}

## Working rules

- No unsupported claims.
- No citation from memory.
- Preserve durable artifacts after every stage.
- Keep the orchestration board and handoffs current.
- Require review-before-finalize for high-risk changes.
`);
  const initialResearchAgenda = {
    version: 1,
    objective: state.dove.objective,
    agenda: zh ? [
      "\u6F84\u6E05\u8BBA\u6587\u76EE\u6807\u4E0E\u8D21\u732E\u3002",
      "\u8D77\u8349\u66F4\u5F3A claims \u524D\u5148\u5EFA\u7ACB\u8BC1\u636E\u57FA\u7840\u3002"
    ] : [
      "Clarify the paper objective and contribution.",
      "Build an evidence base before drafting stronger claims."
    ],
    evidenceBacklog: zh ? ["\u81F3\u5C11\u6CE8\u518C\u4E00\u4E2A\u6301\u4E45 source \u548C note\u3002"] : ["Register at least one durable source and note."],
    updatedAt: nowIso()
  };
  writeJson(root, ARTIFACT_PATHS.researchAgenda, initialResearchAgenda);
  writeText(root, ARTIFACT_PATHS.researchBrief, zh ? `# \u7814\u7A76\u7B80\u62A5

## \u76EE\u6807

${initialResearchAgenda.objective}

## \u8BAE\u7A0B

${initialResearchAgenda.agenda.map((item) => `- ${item}`).join("\n")}

## \u8BC1\u636E backlog

${initialResearchAgenda.evidenceBacklog.map((item) => `- ${item}`).join("\n")}` : `# Research brief

## Objective

${initialResearchAgenda.objective}

## Agenda

${initialResearchAgenda.agenda.map((item) => `- ${item}`).join("\n")}

## Evidence backlog

${initialResearchAgenda.evidenceBacklog.map((item) => `- ${item}`).join("\n")}`);
  refreshDurableSurfaces(root, {
    type: "init-project",
    summary: zh ? `\u5DF2\u521D\u59CB\u5316\u9879\u76EE ${state.dove.title}\u3002` : `Initialized project ${state.dove.title}.`,
    artifactPaths: [ARTIFACT_PATHS.project, ARTIFACT_PATHS.researchContract, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.workspaceIndex]
  });
  return state;
}
function readState(root) {
  const state = loadState(root);
  return { ...state, orchestrationBoard: loadBoard(root), workspaceIndex: queryWorkspaceIndex(root) };
}
var CALLER_MINTABLE_SOURCE_TRUST_FIELDS = /* @__PURE__ */ new Set([
  "verified",
  "verification",
  "verificationRecord",
  "verificationMethod",
  "checkedMaterial",
  "auditEvidence",
  "decision",
  "role",
  "actorRole",
  "capability",
  "token",
  "envToken"
]);
function assertNoCallerMintedSourceTrust(input = {}, label = "register_source") {
  const forbidden = Object.keys(input).filter((key) => CALLER_MINTABLE_SOURCE_TRUST_FIELDS.has(key));
  if (forbidden.length > 0) {
    throw new Error(`${label} does not accept caller-minted trust fields: ${forbidden.join(", ")}. Use verify_source after independently checking durable source material.`);
  }
  if (input.lifecycle !== void 0 && input.lifecycle !== "candidate") {
    throw new Error(`${label} can only register lifecycle candidate; use verify_source for verified or rejected decisions.`);
  }
}
function sourceInputsFromArgs(args = {}) {
  assertNoCallerMintedSourceTrust(args);
  if (Array.isArray(args.sources)) {
    return args.sources.map((item) => {
      const input = item && typeof item === "object" && !Array.isArray(item) ? item : {};
      assertNoCallerMintedSourceTrust(input);
      return input;
    });
  }
  return [args];
}
function hasSourceProvenance(input = {}) {
  return [input.locator, input.doi, input.url, input.arxivId, input.pmid, input.citationKey].some((value) => typeof value === "string" && value.trim());
}
var UNVERIFIED_SOURCE_EVIDENCE_PATTERNS = [
  { code: "webfetch-blocked", pattern: /\b(?:web\s*fetch|webfetch|fetch)\b[^\n]*(?:blocked|failed|error|unable|could not|safe[- ]domain|safe domain|network restriction|enterprise security)/iu },
  { code: "websearch-zero-results", pattern: /\b(?:web\s*search|websearch|search)\b[^\n]*(?:did\s*0\s*searches|0\s*searches|zero\s*(?:searches|results)|no\s*(?:searches|results)|blocked|failed|error)/iu },
  { code: "safe-domain-verification-blocked", pattern: /(?:unable to verify.*domain|domain.*safe to fetch|safe[- ]domain verification|enterprise security policies|network restrictions)/iu },
  { code: "host-tool-blocked", pattern: /\b(?:host tool|safety classifier|tool availability|mcp|retrieval)\b[^\n]*(?:blocked|unavailable|failed|denied|rejected)/iu }
];
function sourceVerificationText(input = {}, context = {}) {
  return [input.origin, context.origin, input.abstract].filter((value) => typeof value === "string" && value.trim()).join("\n");
}
function unverifiedSourceEvidence(input = {}, context = {}) {
  const text3 = sourceVerificationText(input, context);
  if (!text3) {
    return null;
  }
  const match = UNVERIFIED_SOURCE_EVIDENCE_PATTERNS.find(({ pattern }) => pattern.test(text3));
  if (!match) {
    return null;
  }
  return {
    code: match.code,
    evidence: text3.length > 240 ? `${text3.slice(0, 237)}...` : text3
  };
}
function findUnverifiedSourceInputs(inputs = [], args = {}) {
  return inputs.map((input, index) => {
    const evidence = unverifiedSourceEvidence(input, { origin: args.origin });
    if (!evidence) {
      return null;
    }
    return {
      index: index + 1,
      sourceId: input.sourceId ?? input.citationKey ?? input.title ?? input.locator ?? `source-${index + 1}`,
      title: input.title ?? null,
      locator: input.locator ?? null,
      reason: evidence.code,
      evidence: evidence.evidence
    };
  }).filter(Boolean);
}
function unverifiedSourceResult(root, args = {}, target = {}, rejectedSources = []) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const zh = responseLanguage !== "en";
  const packetId = target.packet?.id ?? null;
  const requiredActions = [
    "retry-host-search-or-use-authorized-retrieval",
    "provide-verifiable-source-title-and-locator",
    "call-register-source-only-after-successful-access-or-operator-provided-material"
  ];
  const summary = zh ? "\u6765\u6E90\u68C0\u7D22\u6216\u6293\u53D6\u5931\u8D25\uFF0CDove \u672A\u767B\u8BB0 source provenance\u3002" : "Source search or fetch failed, so Dove did not register source provenance.";
  const boundary = {
    id: normalizeIdentifier(`boundary-${packetId ?? "source"}-host-tool-blocked`, `boundary-${Date.now().toString(36)}`),
    type: "host-tool-blocked",
    status: "open",
    packetId,
    sourceSurface: "dove.source",
    command: "register_source",
    reason: "source-provenance-unverified",
    summary,
    requiredInputs: [],
    requiredActions,
    ownerRole: "builder",
    nextRole: "builder",
    createdAt: nowIso()
  };
  const preActionGuidanceSummary = artifactGuidanceSummary(root, args, {
    surface: "dove.source",
    roleId: "builder",
    subagentSpecialty: "researcher",
    packet: target.packet,
    stage: target.packet?.stage ?? "execute",
    workflowKind: "source",
    nextAction: "project:dove.status",
    tags: ["source", "evidence", "research", "boundary"],
    statusSummary: { rejectedSourceCount: rejectedSources.length, sourceCount: 0 }
  });
  const result = {
    status: "needs-source-verification",
    outcome: "source-provenance-unverified",
    stopReason: "source-provenance-unverified",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    sourceCount: 0,
    rejectedSourceCount: rejectedSources.length,
    rejectedSources,
    packetId,
    boundary,
    requiredActions,
    message: zh ? "\u68C0\u7D22/\u6293\u53D6\u5931\u8D25\u30010 \u641C\u7D22\u7ED3\u679C\u6216 safe-domain \u963B\u65AD\u4E0D\u80FD\u4F5C\u4E3A source provenance \u767B\u8BB0\uFF1B\u8BF7\u5148\u63D0\u4F9B\u53EF\u9A8C\u8BC1\u6765\u6E90\u6216\u6388\u6743\u7684 host \u68C0\u7D22\u7ED3\u679C\u3002" : "Failed fetch/search, zero search results, or safe-domain blocks cannot be registered as source provenance; provide verifiable sources or authorized host retrieval results first.",
    nextAction: "project:dove.status",
    preActionGuidanceSummary
  };
  return {
    ...result,
    resultCard: buildCommandResultCard({
      surface: "dove.source",
      command: "register_source",
      packetId,
      status: result.status,
      outcome: result.outcome,
      summary,
      durableWrites: [],
      evidence: [],
      validation: [],
      stopReason: result.stopReason,
      boundary,
      nextActions: [{
        kind: "provide-evidence-or-result",
        title: zh ? "\u8865\u5145\u53EF\u9A8C\u8BC1\u6765\u6E90\u8BC1\u636E" : "Provide verifiable source evidence",
        command: "project:dove.source",
        packetId,
        boundaryType: boundary.type,
        requiredActions
      }],
      preActionGuidanceSummary
    }, responseLanguage)
  };
}
function findExistingSourceIndex(sources, input = {}, baseId = null) {
  return sources.items.findIndex((item) => baseId && item.id === baseId || input.citationKey && item.citationKey === input.citationKey);
}
function assertSourceInputHasProvenance(input = {}, sources, baseId) {
  if (findExistingSourceIndex(sources, input, baseId) >= 0) {
    return;
  }
  if (!hasSourceProvenance(input)) {
    throw new Error("register_source requires each new source to include verifiable provenance such as a locator, DOI, URL, arXiv id, PMID, or citationKey; a title alone is not source provenance.");
  }
}
function mergeIds(...values) {
  return Array.from(new Set(values.flatMap((value) => normalizeStringArray17(value))));
}
function upsertSourceItem(sources, input = {}, context = {}) {
  const sourceIndex = context.initialCount + context.itemOffset + 1;
  const baseId = normalizeIdentifier(input.sourceId, `${slugify7(input.citationKey ?? input.title ?? `source-${sourceIndex}`)}${input.year ? `-${input.year}` : ""}`);
  const existingIndex = findExistingSourceIndex(sources, input, baseId);
  const existing = existingIndex >= 0 ? sources.items[existingIndex] : null;
  const packetIds = mergeIds(existing?.packetIds, input.packetIds, context.packetId ? [context.packetId] : []);
  const source = {
    ...existing ?? {},
    id: baseId,
    citationKey: input.citationKey ?? existing?.citationKey ?? baseId,
    title: input.title ?? existing?.title ?? "Untitled Source",
    authors: Array.isArray(input.authors) ? input.authors : existing?.authors ?? [],
    year: input.year ?? existing?.year ?? "",
    locator: input.locator ?? existing?.locator ?? "",
    doi: input.doi ?? existing?.doi ?? "",
    url: input.url ?? existing?.url ?? "",
    arxivId: input.arxivId ?? existing?.arxivId ?? "",
    pmid: input.pmid ?? existing?.pmid ?? "",
    sourceType: input.sourceType ?? context.sourceType ?? existing?.sourceType ?? "paper",
    abstract: input.abstract ?? existing?.abstract ?? "",
    origin: input.origin ?? context.origin ?? existing?.origin ?? "manual",
    lifecycle: "candidate",
    packetIds,
    addedAt: existing?.addedAt ?? context.timestamp,
    updatedAt: context.timestamp
  };
  source.fingerprint = sourceIdentityFingerprint(source);
  if (existing?.fingerprint === source.fingerprint && ["verified", "rejected"].includes(existing.lifecycle)) {
    source.lifecycle = existing.lifecycle;
  }
  if (existingIndex >= 0) sources.items[existingIndex] = source;
  else sources.items.push(source);
  return source;
}
function syncCitationArtifacts(root, args = {}) {
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const citedKeys = /* @__PURE__ */ new Set();
  for (const draftFile of listDraftFiles(root)) {
    const draftPath = `${ARTIFACT_PATHS.draftsDir}/${draftFile}`;
    const draftContent = readText(root, draftPath, "");
    for (const key of extractCitationKeysFromText(draftContent)) citedKeys.add(key);
  }
  const missingKeys = Array.from(citedKeys).filter((key) => !sources.items.some((source) => source.id === key || source.citationKey === key)).sort();
  const selectedSources = args.citedOnly ? sources.items.filter((source) => citedKeys.has(source.citationKey ?? source.id) || citedKeys.has(source.id)) : sources.items;
  const bibliography = selectedSources.map(renderBibEntry).join("\n\n");
  writeText(root, ARTIFACT_PATHS.bibliography, bibliography ? `${bibliography}
` : "");
  writeText(root, ARTIFACT_PATHS.citationLog, renderCitationLog(sources, citedKeys, missingKeys));
  return { sources, citedKeys, missingKeys };
}
function registerSource(root, args = {}) {
  assertGovernanceMutationRegistered("register-source", "guarded");
  const target = assertTaskScopedMutationTarget(root, "register-source", args);
  assertFollowThroughReady(root, "Registering a source", args);
  assertPhaseMutationAllowed(root, "sources", "researcher", "Registering a source");
  ensureWorkspace(root);
  const timestamp = nowIso();
  const sourceInputs = sourceInputsFromArgs(args);
  if (sourceInputs.length === 0) {
    throw new Error("register_source requires at least one source.");
  }
  const unverifiedSources = findUnverifiedSourceInputs(sourceInputs, args);
  if (unverifiedSources.length > 0) {
    return unverifiedSourceResult(root, args, target, unverifiedSources);
  }
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const initialCount = sources.items.length;
  const registered = sourceInputs.map((input, index) => {
    const sourceIndex = initialCount + index + 1;
    const baseId = normalizeIdentifier(input.sourceId, `${slugify7(input.citationKey ?? input.title ?? `source-${sourceIndex}`)}${input.year ? `-${input.year}` : ""}`);
    assertSourceInputHasProvenance(input, sources, baseId);
    return upsertSourceItem(sources, input, {
      initialCount,
      itemOffset: index,
      origin: args.origin,
      sourceType: args.sourceType,
      packetId: target.packet?.id,
      timestamp
    });
  });
  sources.updatedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.sources, sources);
  syncCitationArtifacts(root, { preservePhase: true });
  const state = loadState(root);
  syncPhase(root, state, {
    stage: "sources",
    resumeCommand: "project:dove.source",
    role: "researcher",
    intentType: "research",
    currentFocus: `Register and curate sources for ${state.dove.title}.`,
    nextAction: "Capture notes from the strongest source next."
  });
  refreshDurableSurfaces(root, {
    type: "register-source",
    summary: registered.length === 1 ? `Registered source ${registered[0].id}.` : `Registered ${registered.length} sources.`,
    artifactPaths: [ARTIFACT_PATHS.sources, ARTIFACT_PATHS.citationLog, ARTIFACT_PATHS.queryPack]
  });
  const guidanceSummary = artifactGuidanceSummary(root, args, {
    surface: "dove.source",
    roleId: "builder",
    subagentSpecialty: "researcher",
    packet: target.packet,
    stage: target.packet?.stage ?? "execute",
    workflowKind: "source",
    nextAction: "project:dove.note",
    tags: ["source", "evidence", "research"],
    statusSummary: { sourceIds: registered.map((source) => source.id), sourceCount: registered.length }
  });
  const artifactWrites = {
    primaryArtifactPaths: [ARTIFACT_PATHS.sources],
    synthesisArtifactPaths: [ARTIFACT_PATHS.bibliography, ARTIFACT_PATHS.citationLog, ARTIFACT_PATHS.queryPack],
    refreshOnlyArtifactPaths: [ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.sessionJournal]
  };
  const packetArtifactRefs = mergeIds(target.packet?.artifactRefs, [ARTIFACT_PATHS.sources]);
  const packetEvidenceLinks = mergeIds(target.packet?.evidenceLinks);
  const task = artifactPacketResult(root, target, {
    command: "dove.source",
    output: {
      status: "registered",
      summary: registered.length === 1 ? `Registered source ${registered[0].id}.` : `Registered ${registered.length} sources.`,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks,
      sourceIds: mergeIds(target.packet?.sourceIds, registered.map((source) => source.id)),
      nextAction: "project:dove.note"
    },
    classified: {
      status: "step-completed",
      outcome: "dove.source-registered",
      stopReason: null,
      terminal: false,
      canCompleteTask: false,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks
    },
    nextAction: "project:dove.note",
    currentFocus: registered.length === 1 ? `Registered source ${registered[0].title}.` : `Registered ${registered.length} source materials.`
  });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const resultCard = artifactResultCard(root, args, {
    surface: "dove.source",
    command: "register_source",
    title: localizedText2(responseLanguage, "\u6765\u6E90\u5DF2\u767B\u8BB0", "Source registered"),
    status: "registered",
    happened: registered.length === 1 ? localizedText2(responseLanguage, "\u5DF2\u767B\u8BB0 1 \u6761\u53EF\u8FFD\u6EAF\u6765\u6E90\u3002", "Registered one traceable source.") : localizedText2(responseLanguage, `\u5DF2\u767B\u8BB0 ${registered.length} \u6761\u53EF\u8FFD\u6EAF\u6765\u6E90\u3002`, `Registered ${registered.length} traceable sources.`),
    durableWrites: [localizedText2(responseLanguage, "\u6765\u6E90\u7D22\u5F15\u548C\u5F15\u7528\u6750\u6599\u5DF2\u66F4\u65B0\u3002", "Source index and citation materials were updated.")],
    evidence: [localizedText2(responseLanguage, "\u5DF2\u8BB0\u5F55\u6765\u6E90\u51FA\u5904\uFF0C\u540E\u7EED\u53EF\u8F6C\u6210\u5199\u4F5C\u7B14\u8BB0\u3002", "Source provenance is recorded and can be turned into writing notes.")],
    nextActions: [{
      title: localizedText2(responseLanguage, "\u628A\u6765\u6E90\u6C89\u6DC0\u6210\u5199\u4F5C\u8981\u70B9", "Turn the source into writing notes"),
      why: localizedText2(responseLanguage, "\u6765\u6E90\u53EA\u662F\u6750\u6599\u5165\u53E3\uFF0C\u4E0B\u4E00\u6B65\u8981\u62BD\u51FA\u548C\u5F53\u524D\u4EFB\u52A1\u6709\u5173\u7684\u8BBA\u70B9\u3001\u5F15\u7528\u6216\u7591\u95EE\u3002", "A source is only intake; the next step is extracting task-relevant claims, quotes, or questions.")
    }]
  });
  if (!Array.isArray(args.sources)) {
    return {
      ...registered[0],
      task,
      packetLifecycleApplied: true,
      artifactWrites,
      preActionGuidanceSummary: guidanceSummary,
      resultCard
    };
  }
  return {
    status: "registered",
    sourceCount: registered.length,
    sourceIds: registered.map((source) => source.id),
    citationKeys: registered.map((source) => source.citationKey),
    items: registered,
    packetId: target.packet?.id ?? null,
    task,
    packetLifecycleApplied: true,
    artifactWrites,
    preActionGuidanceSummary: guidanceSummary,
    resultCard
  };
}
function verifySource(root, args = {}) {
  assertGovernanceMutationRegistered("verify-source", "guarded");
  const target = assertTaskScopedMutationTarget(root, "verify-source", args);
  assertFollowThroughReady(root, "Verifying a source", args);
  const allowed = /* @__PURE__ */ new Set(["packetId", "taskPacketId", "missionPacketId", "taskId", "target", "packetTarget", "taskName", "sourceId", "decision", "method", "checkedMaterial", "auditEvidence", "mutationMode", "resultMode"]);
  const unknown = Object.keys(args).filter((key) => !allowed.has(key));
  if (unknown.length > 0) throw new Error(`verify_source does not accept unknown input: ${unknown.join(", ")}.`);
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 2, items: [], updatedAt: null });
  const source = sourceReferenceMap(sources.items ?? []).get(args.sourceId) ?? null;
  const prepared = prepareSourceVerification(root, source, args, target.packet?.id);
  const record = prepared.record;
  writeJson(root, ARTIFACT_PATHS.sourceVerifications, prepared.index);
  source.lifecycle = record.decision;
  source.fingerprint = record.fingerprint;
  source.updatedAt = record.checkedAt;
  sources.version = 2;
  sources.updatedAt = record.checkedAt;
  writeJson(root, ARTIFACT_PATHS.sources, sources);
  refreshDurableSurfaces(root, {
    type: "verify-source",
    summary: `${record.decision === "verified" ? "Verified" : "Rejected"} source ${source.id}.`,
    artifactPaths: [ARTIFACT_PATHS.sources, ARTIFACT_PATHS.sourceVerifications]
  });
  return { source, verification: record, packetId: target.packet?.id ?? null };
}
function upsertNote(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-note", "guarded");
  assertNoRetiredArtifactControls(args, "upsert_note");
  const target = assertTaskScopedMutationTarget(root, "upsert-note", args);
  assertFollowThroughReady(root, "Recording a structured note", args);
  return persistNote(root, args, target, { updatePhase: true });
}
function persistNote(root, args, target, { updatePhase }) {
  if (updatePhase) {
    assertPhaseMutationAllowed(root, "notes", "researcher", "Recording a structured note");
  }
  ensureWorkspace(root);
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const knownSourceIds = new Set(sources.items.flatMap((item) => [item.id, item.citationKey].filter(Boolean)));
  const sourceIdsProvided = Array.isArray(args.sourceIds);
  const requestedSourceIds = sourceIdsProvided ? normalizeStringArray17(args.sourceIds) : [];
  const unknownSourceIds = requestedSourceIds.filter((id) => !knownSourceIds.has(id));
  if (unknownSourceIds.length > 0) throw new Error(`Note references unknown sources: ${unknownSourceIds.join(", ")}`);
  const noteId = normalizeIdentifier(args.noteId, `${args.sectionId ?? "general"}-${args.title ?? `note-${notes.items.length + 1}`}`);
  const existingIndex = notes.items.findIndex((item) => item.id === noteId);
  const existing = existingIndex >= 0 ? notes.items[existingIndex] : null;
  if (!hasStructuredNoteSynthesis(args)) {
    throw new Error("upsert_note requires each note write to include a summary, quote, claim, or open question; metadata-only note updates are not research progress.");
  }
  const nextSourceIds = sourceIdsProvided ? requestedSourceIds : existing?.sourceIds ?? [];
  if (nextSourceIds.length === 0) {
    throw new Error("upsert_note requires sourceIds linking the note to registered source provenance.");
  }
  const note = {
    ...existing ?? {},
    id: noteId,
    title: args.title ?? existing?.title ?? noteId.replace(/-/g, " "),
    sectionId: normalizeIdentifier(args.sectionId, existing?.sectionId ?? "introduction"),
    sourceIds: nextSourceIds,
    packetIds: mergeIds(existing?.packetIds, args.packetIds, target.packet?.id ? [target.packet.id] : []),
    summary: args.summary ?? existing?.summary ?? "",
    quotes: Array.isArray(args.quotes) ? normalizeStringArray17(args.quotes) : existing?.quotes ?? [],
    claims: Array.isArray(args.claims) ? normalizeStringArray17(args.claims) : existing?.claims ?? [],
    openQuestions: Array.isArray(args.openQuestions) ? normalizeStringArray17(args.openQuestions) : existing?.openQuestions ?? [],
    updatedAt: nowIso()
  };
  if (existingIndex >= 0) notes.items[existingIndex] = note;
  else notes.items.push(note);
  notes.updatedAt = nowIso();
  writeJson(root, ARTIFACT_PATHS.notes, notes);
  const agenda = readJson(root, ARTIFACT_PATHS.researchAgenda, { version: 1, objective: loadState(root).dove.objective, agenda: [], evidenceBacklog: [], updatedAt: null });
  writeText(root, ARTIFACT_PATHS.queryPack, renderQueryPack(notes, sources, agenda, queryWorkspaceIndex(root)));
  if (updatePhase) {
    const state = loadState(root);
    syncPhase(root, state, {
      stage: "notes",
      resumeCommand: "project:dove.experience",
      role: "researcher",
      intentType: "research",
      currentFocus: note.summary || note.title,
      nextAction: "Promote any supported findings into durable claims."
    });
  }
  refreshDurableSurfaces(root, {
    type: "upsert-note",
    summary: `Updated note ${note.id}.`,
    artifactPaths: [ARTIFACT_PATHS.notes, ARTIFACT_PATHS.queryPack, ARTIFACT_PATHS.sessionSummary]
  });
  const packetArtifactRefs = mergeIds(target.packet?.artifactRefs, [ARTIFACT_PATHS.notes]);
  const packetEvidenceLinks = mergeIds(target.packet?.evidenceLinks);
  const task = artifactPacketResult(root, target, {
    command: "dove.note",
    output: {
      status: "recorded",
      summary: note.summary || `Updated note ${note.id}.`,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks,
      noteIds: mergeIds(target.packet?.noteIds, [note.id]),
      nextAction: "project:dove.experience"
    },
    classified: {
      status: "step-completed",
      outcome: "dove.note-recorded",
      stopReason: null,
      terminal: false,
      canCompleteTask: true,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks
    },
    nextAction: "project:dove.experience",
    currentFocus: note.summary || note.title
  });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const resultCard = artifactResultCard(root, args, {
    surface: "dove.note",
    command: "upsert_note",
    title: localizedText2(responseLanguage, "\u7B14\u8BB0\u5DF2\u6C89\u6DC0", "Note captured"),
    status: "recorded",
    happened: localizedText2(responseLanguage, "\u5DF2\u628A\u6750\u6599\u6574\u7406\u6210\u7ED3\u6784\u5316\u5199\u4F5C\u7B14\u8BB0\u3002", "Captured the material as a structured writing note."),
    durableWrites: [localizedText2(responseLanguage, "\u7B14\u8BB0\u7D22\u5F15\u548C\u68C0\u7D22\u6750\u6599\u5DF2\u66F4\u65B0\u3002", "Note index and retrieval materials were updated.")],
    evidence: [
      note.summary ? localizedText2(responseLanguage, "\u5DF2\u8BB0\u5F55\u6458\u8981\u3002", "Summary recorded.") : null,
      note.quotes.length > 0 ? localizedText2(responseLanguage, "\u5DF2\u8BB0\u5F55\u53EF\u5F15\u7528\u7247\u6BB5\u3002", "Quotable material recorded.") : null,
      note.claims.length > 0 ? localizedText2(responseLanguage, "\u5DF2\u8BB0\u5F55\u5019\u9009\u8BBA\u70B9\u3002", "Candidate claims recorded.") : null,
      note.openQuestions.length > 0 ? localizedText2(responseLanguage, "\u5DF2\u8BB0\u5F55\u5F85\u56DE\u7B54\u95EE\u9898\u3002", "Open questions recorded.") : null
    ],
    nextActions: [{
      title: localizedText2(responseLanguage, "\u51B3\u5B9A\u662F\u5426\u8FDB\u5165\u5B9E\u9A8C\u3001\u8349\u7A3F\u6216\u8865\u8BC1\u636E", "Decide whether to experiment, draft, or add evidence"),
      why: localizedText2(responseLanguage, "\u7B14\u8BB0\u5DF2\u7ECF\u53EF\u7528\uFF0C\u4E0B\u4E00\u6B65\u8981\u770B\u5B83\u80FD\u652F\u6491\u8BBA\u70B9\u3001\u5B9E\u9A8C\u8BBE\u8BA1\uFF0C\u8FD8\u662F\u9700\u8981\u7EE7\u7EED\u627E\u6750\u6599\u3002", "The note is usable; next decide whether it supports a claim, experiment design, or more evidence gathering.")
    }]
  });
  return {
    ...note,
    task,
    packetLifecycleApplied: true,
    artifactWrites: {
      primaryArtifactPaths: [ARTIFACT_PATHS.notes],
      synthesisArtifactPaths: [ARTIFACT_PATHS.queryPack],
      refreshOnlyArtifactPaths: [ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.sessionJournal]
    },
    resultCard,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.note",
      roleId: "builder",
      subagentSpecialty: "researcher",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "note",
      nextAction: "project:dove.experience",
      tags: ["note", "evidence", "research"],
      statusSummary: { noteId: note.id, sourceIdCount: note.sourceIds.length }
    })
  };
}
function upsertPlan(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-plan", "guarded");
  const target = assertTaskScopedMutationTarget(root, "upsert-plan", args);
  assertFollowThroughReady(root, "Updating the Dove mission plan", args);
  const state = loadState(root);
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  if (isStrictMode(state, args)) {
    assertStageAtLeast(state, "notes", "Strict mode requires source and note capture before planning.");
    assertStrictCondition(sources.items.length > 0, "Strict mode requires at least one registered source before planning.");
    assertStrictCondition(notes.items.length > 0, "Strict mode requires at least one structured note before planning.");
  }
  const zh = state.settings?.responseLanguage !== "en";
  const nextState = syncPhase(root, {
    ...state,
    dove: { ...state.dove, thesis: args.thesis ?? state.dove.thesis, audience: args.audience ?? state.dove.audience }
  }, {
    stage: "plan",
    resumeCommand: "project:dove.status",
    role: "planner",
    intentType: "plan",
    currentFocus: zh ? "\u5C06\u4EFB\u52A1\u76EE\u6807\u8F6C\u5316\u4E3A\u6709\u8FB9\u754C\u7684\u8BBE\u8BA1\u8BA1\u5212\u3002" : "Convert the mission goal into a scoped design plan.",
    nextAction: zh ? "\u6267\u884C\u524D\u540C\u6B65\u68C0\u67E5\u6E05\u5355\u3002" : "Sync the checklist before execution.",
    reviewRequiredBeforeFinalize: true
  });
  writeText(root, ARTIFACT_PATHS.plan, renderPlan(args, nextState, loadBoard(root)));
  refreshDurableSurfaces(root, {
    type: "upsert-plan",
    summary: zh ? "\u5DF2\u66F4\u65B0\u5F53\u524D Dove \u4EFB\u52A1\u8BA1\u5212\u3002" : "Updated current Dove mission plan.",
    artifactPaths: [ARTIFACT_PATHS.plan, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.sessionSummary]
  });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const resultCard = artifactResultCard(root, args, {
    surface: "dove.plan",
    command: "upsert_plan",
    title: localizedText2(responseLanguage, "\u4EFB\u52A1\u8BA1\u5212\u5DF2\u66F4\u65B0", "Mission plan updated"),
    status: "updated",
    happened: localizedText2(responseLanguage, "\u5DF2\u628A\u5F53\u524D\u76EE\u6807\u6536\u675F\u6210\u53EF\u6267\u884C\u8BA1\u5212\u3002", "Converted the current goal into an actionable plan."),
    durableWrites: [localizedText2(responseLanguage, "\u5F53\u524D\u8BA1\u5212\u548C\u6062\u590D\u6458\u8981\u5DF2\u66F4\u65B0\u3002", "Current plan and resume summary were updated.")],
    scope: { thesis: nextState.dove.thesis },
    nextActions: [{
      title: localizedText2(responseLanguage, "\u540C\u6B65\u68C0\u67E5\u6E05\u5355\u540E\u518D\u6267\u884C", "Sync the checklist before execution"),
      why: localizedText2(responseLanguage, "\u8BA1\u5212\u53EA\u662F\u8303\u56F4\u8FB9\u754C\uFF0C\u6267\u884C\u524D\u8FD8\u8981\u786E\u8BA4\u5177\u4F53\u8BC1\u636E\u548C\u5B8C\u6210\u6761\u4EF6\u3002", "The plan defines scope; evidence and completion checks still need to be aligned before execution.")
    }]
  });
  return {
    planPath: ARTIFACT_PATHS.plan,
    thesis: nextState.dove.thesis,
    resultCard,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.plan",
      roleId: "planner",
      packet: target.packet,
      stage: target.packet?.stage ?? "plan",
      workflowKind: "plan",
      nextAction: "project:dove.status",
      tags: ["plan", "gate", "scope"],
      statusSummary: { planPath: ARTIFACT_PATHS.plan, thesis: nextState.dove.thesis }
    })
  };
}
function upsertOutline(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-outline", "guarded");
  const target = assertTaskScopedMutationTarget(root, "upsert-outline", args);
  assertFollowThroughReady(root, "Updating the paper outline", args);
  assertPhaseMutationAllowed(root, "outline", "planner", "Updating the paper outline");
  let state = loadState(root);
  const zh = state.settings?.responseLanguage !== "en";
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  if (isStrictMode(state, args)) {
    assertStageAtLeast(state, "plan", "Strict mode requires planning before outlining.");
    assertStrictCondition(evidence.claims.length > 0, "Strict mode requires at least one evidence-backed claim before outlining.");
  }
  const providedSections = Array.isArray(args.sections) ? args.sections : [];
  for (const section of providedSections) {
    const sectionId = normalizeIdentifier(section.id, section.title ?? "section");
    state.sections[sectionId] = {
      ...state.sections[sectionId] ?? { id: sectionId, draftPath: `.dove/drafts/${sectionId}.md`, claimIds: [] },
      title: section.title ?? state.sections[sectionId]?.title ?? sectionId,
      status: section.status ?? state.sections[sectionId]?.status ?? "planned",
      summary: section.goal ?? state.sections[sectionId]?.summary ?? ""
    };
  }
  state = syncPhase(root, state, {
    stage: "outline",
    resumeCommand: "project:dove.draft",
    role: "planner",
    intentType: "plan",
    currentFocus: zh ? "\u5C06\u8BA1\u5212\u8F6C\u5316\u4E3A\u9010\u7AE0\u8282 outline\u3002" : "Translate the plan into a section-by-section outline.",
    nextAction: zh ? "\u4E0B\u4E00\u6B65\u8D77\u8349\u6700\u9AD8\u6760\u6746\u7684\u7AE0\u8282\u3002" : "Draft the highest-leverage section next.",
    reviewRequiredBeforeFinalize: true
  });
  writeText(root, ARTIFACT_PATHS.outline, renderOutline(args, state, loadBoard(root)));
  refreshDurableSurfaces(root, {
    type: "upsert-outline",
    summary: zh ? "\u5DF2\u66F4\u65B0\u5F53\u524D outline\u3002" : "Updated current outline.",
    artifactPaths: [ARTIFACT_PATHS.outline, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.sessionSummary]
  });
  const sectionCount = Object.keys(state.sections).length;
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const resultCard = artifactResultCard(root, args, {
    surface: "dove.draft",
    command: "upsert_outline",
    title: localizedText2(responseLanguage, "\u5927\u7EB2\u5DF2\u66F4\u65B0", "Outline updated"),
    status: "updated",
    happened: localizedText2(responseLanguage, `\u5DF2\u6574\u7406 ${sectionCount} \u4E2A\u7AE0\u8282\u7684\u5199\u4F5C\u7ED3\u6784\u3002`, `Organized the writing structure for ${sectionCount} sections.`),
    durableWrites: [localizedText2(responseLanguage, "\u5F53\u524D\u5927\u7EB2\u548C\u6062\u590D\u6458\u8981\u5DF2\u66F4\u65B0\u3002", "Current outline and resume summary were updated.")],
    scope: { sectionCount },
    nextActions: [{
      title: localizedText2(responseLanguage, "\u9009\u62E9\u6700\u9AD8\u6760\u6746\u7AE0\u8282\u5F00\u59CB\u8D77\u8349", "Draft the highest-leverage section next"),
      why: localizedText2(responseLanguage, "\u5927\u7EB2\u5DF2\u7ECF\u7ED9\u51FA\u7ED3\u6784\uFF0C\u4E0B\u4E00\u6B65\u8981\u628A\u6700\u80FD\u652F\u6491\u76EE\u6807\u7684\u90E8\u5206\u5199\u6210\u6B63\u6587\u3002", "The outline gives structure; next turn the most important section into draft text.")
    }]
  });
  return {
    outlinePath: ARTIFACT_PATHS.outline,
    sectionCount,
    resultCard,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.draft",
      roleId: "planner",
      packet: target.packet,
      stage: target.packet?.stage ?? "plan",
      workflowKind: "outline",
      nextAction: "project:dove.draft",
      tags: ["outline", "draft", "gate"],
      statusSummary: { outlinePath: ARTIFACT_PATHS.outline, sectionCount: Object.keys(state.sections).length }
    })
  };
}
function upsertDraft(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-draft", "guarded");
  const target = assertTaskScopedMutationTarget(root, "upsert-draft", args);
  assertFollowThroughReady(root, "Updating a draft section", args);
  assertPhaseMutationAllowed(root, "draft", "researcher", "Updating a draft section");
  const sectionId = normalizeIdentifier(args.sectionId, "introduction");
  const state = loadState(root);
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  if (isStrictMode(state, args)) {
    assertStageAtLeast(state, "outline", "Strict mode requires an approved outline stage before drafting.");
    assertStrictCondition(evidence.claims.length > 0, "Strict mode requires evidence-backed claims before drafting.");
  }
  if (!hasNonEmptyString(args.body)) {
    throw new Error("upsert_draft requires body content; use set_section_status for metadata-only section updates.");
  }
  const draftPath = `${ARTIFACT_PATHS.draftsDir}/${sectionId}.md`;
  const title = args.title ?? sectionId.replace(/-/g, " ");
  const body = args.body;
  writeText(root, draftPath, body);
  state.sections[sectionId] = {
    ...state.sections[sectionId] ?? { id: sectionId, title, draftPath, claimIds: [] },
    title,
    status: args.status ?? "drafting",
    draftPath,
    summary: args.summary ?? state.sections[sectionId]?.summary ?? ""
  };
  syncPhase(root, state, {
    stage: "draft",
    resumeCommand: "project:dove.review",
    role: "researcher",
    intentType: "write",
    currentFocus: `Draft ${title}.`,
    nextAction: "Refresh the review loop before making completion claims.",
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "upsert-draft",
    summary: `Updated draft for section ${sectionId}.`,
    artifactPaths: [draftPath, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport]
  });
  const packetArtifactRefs = mergeIds(target.packet?.artifactRefs, [draftPath]);
  const task = artifactPacketResult(root, target, {
    command: "dove.draft",
    output: {
      status: state.sections[sectionId].status,
      summary: args.summary ?? `Updated draft for section ${sectionId}.`,
      draftPath,
      sectionId,
      artifactRefs: packetArtifactRefs,
      nextAction: "project:dove.review"
    },
    classified: {
      status: "step-completed",
      outcome: "dove.draft-updated",
      stopReason: null,
      terminal: false,
      canCompleteTask: true,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: []
    },
    nextAction: "project:dove.review",
    currentFocus: args.summary ?? `Draft ${title}.`
  });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const resultCard = artifactResultCard(root, args, {
    surface: "dove.draft",
    command: "upsert_draft",
    title: localizedText2(responseLanguage, "\u8349\u7A3F\u5DF2\u66F4\u65B0", "Draft updated"),
    status: state.sections[sectionId].status,
    happened: localizedText2(responseLanguage, `\u5DF2\u66F4\u65B0\u201C${title}\u201D\u8FD9\u4E00\u6BB5\u6B63\u6587\u3002`, `Updated the draft body for \u201C${title}\u201D.`),
    durableWrites: [localizedText2(responseLanguage, "\u8349\u7A3F\u6B63\u6587\u548C\u9879\u76EE\u5BFC\u822A\u5DF2\u66F4\u65B0\u3002", "Draft body and project navigation were updated.")],
    scope: { section: title, status: state.sections[sectionId].status },
    nextActions: [{
      title: localizedText2(responseLanguage, "\u9001\u53BB review \u68C0\u67E5\u8BC1\u636E\u662F\u5426\u591F\u7528", "Send it to review for evidence checking"),
      why: localizedText2(responseLanguage, "\u8349\u7A3F\u5DF2\u7ECF\u843D\u5730\uFF0C\u4E0B\u4E00\u6B65\u8981\u786E\u8BA4\u5B83\u7684\u8BBA\u70B9\u3001\u5F15\u7528\u548C\u5B9E\u9A8C\u652F\u6491\u662F\u5426\u7AD9\u5F97\u4F4F\u3002", "The draft exists; next verify whether its claims, citations, and experiment support hold up.")
    }]
  });
  return {
    draftPath,
    sectionId,
    task,
    packetLifecycleApplied: true,
    resultCard,
    artifactWrites: {
      primaryArtifactPaths: [draftPath],
      refreshOnlyArtifactPaths: [ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary, ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.sessionJournal]
    },
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.draft",
      roleId: "builder",
      subagentSpecialty: "researcher",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "draft",
      nextAction: "project:dove.review",
      tags: ["draft", "claims", "citations"],
      statusSummary: { sectionId, draftPath }
    })
  };
}
function setSectionStatus(root, args = {}) {
  assertGovernanceMutationRegistered("set-section-status", "guarded");
  const target = assertTaskScopedMutationTarget(root, "set-section-status", args);
  assertFollowThroughReady(root, "Updating a section status", args);
  const board = loadBoard(root);
  assertPhaseMutationAllowed(root, "draft", "researcher", "Updating a section status");
  const sectionId = normalizeIdentifier(args.sectionId, "introduction");
  const state = loadState(root);
  const nextStatus = args.status ?? "planned";
  if (isCompletionLikeSectionStatus(nextStatus)) {
    const existingSection = state.sections[sectionId];
    if (!existingSection) {
      throw new Error(`set_section_status cannot mark new section ${sectionId} as ${nextStatus}; create a draft with upsert_draft first.`);
    }
    assertSubstantiveDraftForSection(root, existingSection, sectionId);
  }
  state.sections[sectionId] = {
    ...state.sections[sectionId] ?? { id: sectionId, title: sectionId, draftPath: `.dove/drafts/${sectionId}.md`, claimIds: [] },
    status: nextStatus,
    summary: args.summary ?? state.sections[sectionId]?.summary ?? ""
  };
  saveState(root, state);
  upsertSystemOrchestrationBoard(root, {
    phase: state.pipeline.currentStage,
    assignedRole: board.assignedRole,
    currentFocus: args.summary ?? board.currentFocus,
    nextAction: board.nextAction,
    tasks: board.tasks.map((task) => {
      if (task.id !== sectionId && task.id !== `${sectionId}-draft`) return task;
      return { ...task, status: isCompletionLikeSectionStatus(nextStatus) ? "done" : task.status, notes: args.summary ?? task.notes };
    })
  });
  refreshDurableSurfaces(root, {
    type: "set-section-status",
    summary: `Set section ${sectionId} to ${state.sections[sectionId].status}.`,
    artifactPaths: [ARTIFACT_PATHS.state, ARTIFACT_PATHS.orchestrationBoard, ARTIFACT_PATHS.sessionSummary]
  });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const sectionTitle = state.sections[sectionId].title ?? sectionId;
  const resultCard = artifactResultCard(root, args, {
    surface: "dove.draft",
    command: "set_section_status",
    title: localizedText2(responseLanguage, "\u7AE0\u8282\u72B6\u6001\u5DF2\u66F4\u65B0", "Section status updated"),
    status: state.sections[sectionId].status,
    happened: localizedText2(responseLanguage, `\u5DF2\u628A\u201C${sectionTitle}\u201D\u6807\u8BB0\u4E3A ${state.sections[sectionId].status}\u3002`, `Marked \u201C${sectionTitle}\u201D as ${state.sections[sectionId].status}.`),
    durableWrites: [localizedText2(responseLanguage, "\u7AE0\u8282\u72B6\u6001\u548C\u4EFB\u52A1\u770B\u677F\u5DF2\u66F4\u65B0\u3002", "Section status and task board were updated.")],
    scope: { section: sectionTitle, status: state.sections[sectionId].status },
    nextActions: [{
      title: localizedText2(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u786E\u8BA4\u4E0B\u4E00\u6B65", "Return to status for the next step"),
      why: localizedText2(responseLanguage, "\u72B6\u6001\u53D8\u5316\u4F1A\u5F71\u54CD\u4E0B\u4E00\u6B65\u662F\u7EE7\u7EED\u5199\u3001\u8865\u8BC1\u636E\uFF0C\u8FD8\u662F\u8FDB\u5165 review\u3002", "The status change affects whether the next step is drafting, evidence work, or review.")
    }]
  });
  return {
    ...state.sections[sectionId],
    resultCard,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.draft",
      roleId: "planner",
      packet: target.packet,
      stage: target.packet?.stage ?? "plan",
      workflowKind: "section-status",
      nextAction: "project:dove.status",
      tags: ["section", "status", "gate"],
      statusSummary: { sectionId, status: state.sections[sectionId].status }
    })
  };
}
function syncChecklist(root) {
  assertGovernanceMutationRegistered("sync-checklist", "exempt");
  const state = loadState(root);
  const board = loadBoard(root);
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, { version: 2, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  const plans = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const results = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const issues = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const versions = readJson(root, ARTIFACT_PATHS.versionsIndex, { version: 1, currentVersionId: null, items: [], lineage: [], updatedAt: null });
  const workspaceIndex = queryWorkspaceIndex(root);
  writeText(root, ARTIFACT_PATHS.checklist, renderChecklist(state, reviewState, board, plans, results, issues, versions, workspaceIndex));
  refreshDurableSurfaces(root, {
    type: "sync-checklist",
    summary: state.settings?.responseLanguage === "en" ? "Refreshed checklist from current workspace state." : "\u5DF2\u6839\u636E\u5F53\u524D\u5DE5\u4F5C\u533A\u72B6\u6001\u5237\u65B0\u68C0\u67E5\u6E05\u5355\u3002",
    artifactPaths: [ARTIFACT_PATHS.checklist, ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.workspaceIndex]
  });
  return {
    checklistPath: ARTIFACT_PATHS.checklist,
    openItemCount: reviewState.openItems.length,
    preActionGuidanceSummary: artifactGuidanceSummary(root, {}, {
      surface: "dove.status",
      roleId: "planner",
      stage: state.pipeline.currentStage,
      workflowKind: "checklist",
      nextAction: "project:dove.status",
      tags: ["checklist", "gate", "status"],
      statusSummary: { checklistPath: ARTIFACT_PATHS.checklist, openItemCount: reviewState.openItems.length }
    })
  };
}
function upsertFigurePlan(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-figure-plan", "guarded");
  const target = assertTaskScopedMutationTarget(root, "upsert-figure-plan", args);
  assertFollowThroughReady(root, "Updating the figure plan", args);
  assertPhaseMutationAllowed(
    root,
    "experiments",
    "builder",
    "Updating the figure plan"
  );
  const figures = readJson(root, ARTIFACT_PATHS.figuresIndex, { version: 1, items: [], updatedAt: null });
  const normalizedItems = (Array.isArray(args.items) ? args.items : []).map((item, index) => ({
    ...normalizeFigureItem(item, index),
    packetId: target.packetId
  }));
  const timestamp = nowIso();
  const stagedArtifacts = normalizedItems.map((item) => buildFigureContractArtifacts(item, timestamp));
  figures.items = normalizedItems.map((item) => ({
    ...item,
    qaArtifactPath: ARTIFACT_PATHS.figureQa,
    stageArtifacts: {
      briefId: item.briefId,
      segmentId: item.segmentId,
      templateId: item.templateId,
      editableArtifactId: item.editableArtifactId,
      finalArtifactId: item.finalArtifactId
    },
    updatedAt: timestamp
  }));
  figures.updatedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.figuresIndex, figures);
  writeJson(root, ARTIFACT_PATHS.figureBriefs, {
    version: 1,
    items: stagedArtifacts.map((item) => item.brief),
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.figureSegments, {
    version: 1,
    items: stagedArtifacts.map((item) => item.segments),
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.figureTemplates, {
    version: 1,
    items: stagedArtifacts.map((item) => item.templates),
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.figureEditableIndex, {
    version: 1,
    items: stagedArtifacts.map((item) => item.editable),
    updatedAt: timestamp
  });
  writeJson(root, ARTIFACT_PATHS.figureFinalIndex, {
    version: 1,
    items: stagedArtifacts.map((item) => item.final),
    updatedAt: timestamp
  });
  const qa = buildFigureQa(root);
  writeJson(root, ARTIFACT_PATHS.figureQa, qa);
  const content = [
    "# Figures backlog",
    "",
    "Dove tracks each figure from user intent through material discovery, safe generation/import, caption provenance, and QA.",
    "",
    "## Generation workflow",
    "",
    "1. Brief -> define source sections, target claims, experiments, review/rebuttal linkage, and caption intent.",
    "2. Materials -> discover required data, source artifacts, notes, claims, and experiments before drawing.",
    "3. Generation input -> prepare a durable run bundle and prompt for a local or external drawing provider.",
    "4. Import -> accept only declared, safe generated assets and record provenance.",
    "5. Caption and QA -> explain what the figure is for and validate the final artifact against linked evidence.",
    "",
    ...normalizedItems.length > 0 ? normalizedItems.flatMap((item) => [
      `## ${item.name}`,
      "",
      `- Purpose: ${item.purpose}`,
      `- Source sections: ${item.sourceSections.join(", ") || "none"}`,
      `- Source artifacts: ${item.sourceArtifactPaths.join(", ") || "none"}`,
      `- Target claims: ${item.targetClaimIds.join(", ") || "none"}`,
      `- Related experiments: ${item.relatedExperimentIds.join(", ") || "none"}`,
      `- Review concerns: ${item.reviewConcernIds.join(", ") || "none"}`,
      `- Rebuttal issues: ${item.rebuttalIssueIds.join(", ") || "none"}`,
      `- Narrative intent: ${item.narrativeIntent}`,
      `- Required visual elements: ${item.requiredVisualElements.join(", ") || "none"}`,
      `- Material requirements: ${item.materialRequirements.map((material) => material.label).join(", ") || "none"}`,
      `- Segment placeholders: ${item.segmentPlaceholders.map((segment) => segment.label).join(", ") || "none"}`,
      `- Template SVG path: ${item.templateSvgPath}`,
      `- Editable SVG path: ${item.editableSvgPath}`,
      `- Final SVG path: ${item.finalSvgPath}`,
      `- Generation mode: ${item.generationMode}`,
      `- Generation provider: ${item.generationProviderId ?? "none"}`,
      `- Output format: ${item.outputFormat}`,
      `- Caption intent: ${item.captionIntent}`,
      `- Owner: ${item.owner}`,
      `- Mode: ${item.mode}`,
      `- Status: ${item.status}`,
      `- Review notes: ${item.reviewNotes.join(" | ") || "none"}`,
      ""
    ]) : ["No figures planned yet."]
  ].join("\n");
  writeText(root, ARTIFACT_PATHS.figuresReadme, content);
  refreshDurableSurfaces(root, {
    type: "upsert-figure-plan",
    summary: `Updated figure backlog with ${normalizedItems.length} items.`,
    artifactPaths: [ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureBriefs, ARTIFACT_PATHS.figureSegments, ARTIFACT_PATHS.figureTemplates, ARTIFACT_PATHS.figureEditableIndex, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureCaptions, ARTIFACT_PATHS.figureQa, ARTIFACT_PATHS.figuresReadme]
  });
  return {
    figureCount: normalizedItems.length,
    qaIssueCount: qa.issues.length,
    qaPath: ARTIFACT_PATHS.figureQa,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.figure",
      roleId: "builder",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "figure",
      nextAction: "project:dove.figure",
      tags: ["figure", "artifact-provenance", "qa"],
      statusSummary: { figureCount: normalizedItems.length, qaIssueCount: qa.issues.length }
    })
  };
}
function syncCitations(root, args = {}) {
  assertGovernanceMutationRegistered("sync-citations", "guarded");
  assertFollowThroughReady(root, "Updating citation artifacts", args);
  const currentState = loadState(root);
  const currentBoard = loadBoard(root);
  assertPhaseMutationAllowed(
    root,
    args.preservePhase ? currentState.pipeline.currentStage : "citations",
    args.preservePhase ? currentBoard.assignedRole : "researcher",
    "Updating citation artifacts"
  );
  ensureWorkspace(root);
  const { sources, citedKeys, missingKeys } = syncCitationArtifacts(root, args);
  const state = loadState(root);
  syncPhase(root, state, args.preservePhase ? {
    stage: state.pipeline.currentStage,
    resumeCommand: state.pipeline.resumeCommand,
    role: loadBoard(root).assignedRole,
    intentType: loadBoard(root).intentType,
    currentFocus: loadBoard(root).currentFocus,
    nextAction: loadBoard(root).nextAction
  } : {
    stage: "citations",
    resumeCommand: "project:dove.review",
    role: "researcher",
    intentType: "review",
    currentFocus: "Reconcile bibliography coverage with cited drafts.",
    nextAction: "Run the review loop after citation sync completes.",
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "sync-citations",
    summary: `Synchronized citations for ${citedKeys.size} cited keys.`,
    artifactPaths: [ARTIFACT_PATHS.bibliography, ARTIFACT_PATHS.citationLog, ARTIFACT_PATHS.navigationReport]
  });
  return {
    sourceCount: sources.items.length,
    citedKeyCount: citedKeys.size,
    missingKeys,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.draft",
      roleId: "builder",
      subagentSpecialty: "researcher",
      stage: state.pipeline.currentStage,
      workflowKind: "citations",
      nextAction: "project:dove.review",
      tags: ["citations", "evidence", "review"],
      statusSummary: { sourceCount: sources.items.length, citedKeyCount: citedKeys.size, missingKeyCount: missingKeys.length }
    })
  };
}
function refreshWiki(root, args = {}) {
  assertGovernanceMutationRegistered("refresh-wiki", "guarded");
  assertNoRetiredArtifactControls(args, "refresh_wiki");
  assertFollowThroughReady(root, "Refreshing the wiki", args);
  return persistWikiRefresh(root, args);
}
function persistWikiRefresh(root, args) {
  ensureWorkspace(root);
  const state = loadState(root);
  const board = loadBoard(root);
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const concerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 1, items: [], updatedAt: null });
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const agenda = readJson(root, ARTIFACT_PATHS.researchAgenda, { version: 1, objective: state.dove.objective, agenda: [], evidenceBacklog: [], updatedAt: null });
  const plans = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const results = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const issues = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const versions = readJson(root, ARTIFACT_PATHS.versionsIndex, { version: 1, currentVersionId: null, items: [], lineage: [], updatedAt: null });
  const navigation = refreshDurableSurfaces(root, {
    type: "refresh-wiki-prepass",
    summary: "Prepared navigation state before typed wiki refresh.",
    artifactPaths: [ARTIFACT_PATHS.navigationReport, ARTIFACT_PATHS.workspaceIndex]
  });
  const workspaceIndex = queryWorkspaceIndex(root);
  const typed = createWikiArtifacts(root, state, board, sources, notes, evidence, concerns, navigation.decisions, navigation.openQuestions, plans, results, issues);
  writeJson(root, ARTIFACT_PATHS.wikiEntities, typed.entities);
  writeJson(root, ARTIFACT_PATHS.wikiRelations, typed.relations);
  writeText(root, ARTIFACT_PATHS.wiki, renderWiki(state, board, sources, notes, evidence, concerns, issues, plans, results, versions, workspaceIndex));
  writeText(root, ARTIFACT_PATHS.queryPack, renderQueryPack(notes, sources, agenda, workspaceIndex));
  syncPhase(root, state, {
    stage: board.currentPhase,
    resumeCommand: state.pipeline.resumeCommand,
    role: board.assignedRole,
    intentType: board.intentType,
    currentFocus: board.currentFocus,
    nextAction: board.nextAction,
    reviewRequiredBeforeFinalize: board.reviewRequiredBeforeFinalize
  });
  refreshDurableSurfaces(root, {
    type: "refresh-wiki",
    summary: "Refreshed wiki, typed wiki indexes, query pack, and navigation surfaces.",
    artifactPaths: [ARTIFACT_PATHS.wiki, ARTIFACT_PATHS.queryPack, ARTIFACT_PATHS.wikiEntities, ARTIFACT_PATHS.wikiRelations, ARTIFACT_PATHS.navigationReport]
  });
  return {
    wikiPath: ARTIFACT_PATHS.wiki,
    noteCount: notes.items.length,
    claimCount: evidence.claims.length,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.status",
      roleId: "planner",
      stage: board.currentPhase ?? state.pipeline.currentStage,
      workflowKind: "wiki-refresh",
      nextAction: "project:dove.status",
      tags: ["wiki", "context", "evidence"],
      statusSummary: { wikiPath: ARTIFACT_PATHS.wiki, noteCount: notes.items.length, claimCount: evidence.claims.length }
    })
  };
}
function buildRebuttal(root, args = {}) {
  assertGovernanceMutationRegistered("build-rebuttal", "guarded");
  const target = assertTaskScopedMutationTarget(root, "build-rebuttal", args);
  assertFollowThroughReady(root, "Building the rebuttal draft", args);
  assertPhaseMutationAllowed(root, "rebuttal", "builder", "Building the rebuttal draft");
  const issues = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  if (!Array.isArray(issues.items) || issues.items.length === 0) {
    return missingRebuttalIssuesResult(root, args, "build_rebuttal");
  }
  ensureWorkspace(root);
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, { version: 2, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  const claims = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  buildRebuttalStrategy(root, args);
  const draftPath = `${ARTIFACT_PATHS.draftsDir}/rebuttal.md`;
  writeText(root, draftPath, renderRebuttalDraft(reviewState, claims, ARTIFACT_PATHS.rebuttalIssues, ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft));
  const state = loadState(root);
  state.sections.rebuttal = {
    ...state.sections.rebuttal ?? { id: "rebuttal", title: "Rebuttal Notes", draftPath, claimIds: [] },
    status: reviewState.openItems.length > 0 ? "drafting" : "ready",
    draftPath,
    summary: "Artifact-backed rebuttal draft"
  };
  syncPhase(root, state, {
    stage: "rebuttal",
    resumeCommand: "project:dove.version",
    role: "builder",
    intentType: "respond",
    currentFocus: "Convert normalized concerns into an evidence-backed rebuttal.",
    nextAction: "Snapshot the paper once rebuttal changes stabilize.",
    rebuttalIssueIds: readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null }).items.map((issue) => issue.id),
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "build-rebuttal",
    summary: "Built artifact-backed rebuttal draft.",
    artifactPaths: [draftPath, ARTIFACT_PATHS.rebuttalStrategy, ARTIFACT_PATHS.rebuttalResponseDraft]
  });
  return {
    draftPath,
    openReviewItemCount: reviewState.openItems.length,
    preActionGuidanceSummary: artifactGuidanceSummary(root, args, {
      surface: "dove.rebuttal",
      roleId: "builder",
      subagentSpecialty: "revision-lead",
      packet: target.packet,
      stage: target.packet?.stage ?? "execute",
      workflowKind: "rebuttal",
      nextAction: "project:dove.review",
      tags: ["rebuttal", "revision", "evidence"],
      statusSummary: { openReviewItemCount: reviewState.openItems.length, draftPath }
    })
  };
}
function listWorkspaceArtifacts(root) {
  return listArtifacts(root);
}

// src/core/review-scope.mjs
function normalizeStrings(value) {
  const values = Array.isArray(value) ? value : typeof value === "string" ? [value] : [];
  return Array.from(new Set(values.map((item) => String(item).trim()).filter(Boolean)));
}
function localPaths(values = []) {
  return normalizeStrings(values).filter((item) => !isExternalArtifactReference(item)).map((item) => normalizeProjectRelativePath(item)).filter((item) => item.ok).map((item) => item.normalizedPath);
}
function packetPaths(packet = {}) {
  const context = packet.context && typeof packet.context === "object" ? packet.context : {};
  const criteria = [packet.verifiedCriteria, context.verifiedCriteria].flat().filter(Boolean);
  const scopedRecords = [packet.scopedRecords, context.scopedRecords].flat().filter(Boolean);
  return localPaths([
    packet.packetPath,
    packet.packetContextPath,
    ...normalizeStrings(packet.artifactRefs),
    ...normalizeStrings(context.artifactRefs),
    ...normalizeStrings(packet.outputPaths),
    ...normalizeStrings(context.outputPaths),
    ...normalizeStrings(packet.evidenceLinks),
    ...normalizeStrings(context.evidenceLinks),
    ...normalizeStrings(packet.validationEvidencePaths),
    ...normalizeStrings(context.validationEvidencePaths),
    ...normalizeStrings(packet.verificationEvidencePaths),
    ...normalizeStrings(context.verificationEvidencePaths),
    ...criteria.flatMap((criterion) => normalizeStrings(criterion?.evidencePaths)),
    ...scopedRecords.flatMap((record) => normalizeStrings(record?.paths ?? record?.artifactPaths ?? record?.evidencePaths))
  ]);
}
function descendantsOf(packet, packets) {
  const included = /* @__PURE__ */ new Map([[packet.id, packet]]);
  let changed = true;
  while (changed) {
    changed = false;
    for (const candidate of packets) {
      if (!included.has(candidate.id) && candidate.parentId && included.has(candidate.parentId)) {
        included.set(candidate.id, candidate);
        changed = true;
      }
    }
  }
  return Array.from(included.values());
}
function entityIds(packets, field) {
  return Array.from(new Set(packets.flatMap((packet) => normalizeStrings(packet[field]))));
}
function packetBoundRecordIds(root, includedPacketIds, relativePath, collectionField, idField = "id") {
  const included = new Set(includedPacketIds);
  const collection = readJson(root, relativePath, { [collectionField]: [] });
  return Array.from(new Set((collection?.[collectionField] ?? []).filter((item) => normalizeStrings(item?.packetIds ?? item?.packetId).some((packetId) => included.has(packetId))).map((item) => item?.[idField]).filter(Boolean)));
}
function packetBoundRecordPaths(root, includedPacketIds) {
  const included = new Set(includedPacketIds);
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { claims: [] });
  const paths = [];
  const scopedClaims = (evidence.claims ?? []).filter((claim) => normalizeStrings(claim.packetIds ?? claim.packetId).some((packetId) => included.has(packetId)));
  if (scopedClaims.length > 0) {
    paths.push(ARTIFACT_PATHS.evidence, ARTIFACT_PATHS.claims);
    for (const claim of scopedClaims) {
      if (claim.sectionId) paths.push(`${ARTIFACT_PATHS.draftsDir}/${claim.sectionId}.md`);
    }
  }
  const figures = readJson(root, ARTIFACT_PATHS.figuresIndex, { items: [] });
  const scopedFigures = (figures.items ?? []).filter((item) => normalizeStrings(item.packetIds ?? item.packetId).some((packetId) => included.has(packetId)));
  if (scopedFigures.length > 0) {
    paths.push(ARTIFACT_PATHS.figuresIndex, ARTIFACT_PATHS.figureEditableIndex);
    for (const figure of scopedFigures) paths.push(...localPaths([figure.templateSvgPath, figure.finalSvgPath, figure.sourceSvgPath]));
  }
  return Array.from(new Set(paths));
}
function scopeError(message, details = {}) {
  const error = new Error(message);
  error.code = "REVIEW_SCOPE_INVALID";
  Object.assign(error, details);
  return error;
}
function sameStringSet2(left, right) {
  return JSON.stringify(normalizeStrings(left).sort()) === JSON.stringify(normalizeStrings(right).sort());
}
function canonicalReviewExchangePathFailure(root, relativePath, label) {
  const normalized = normalizeProjectRelativePath(relativePath);
  if (!normalized.ok) {
    return `${label}-unsafe-path`;
  }
  const inspection = inspectDeclaredPath(root, normalized.normalizedPath, {
    requireNonEmpty: true
  });
  if (inspection.status !== "existing") {
    return `${label}-${inspection.status}`;
  }
  if (inspection.canonicalRelativePath !== normalized.normalizedPath) {
    return `${label}-noncanonical-realpath`;
  }
  return null;
}
function verifyCanonicalReviewImportScope(root, target, {
  runId,
  manifest,
  input,
  handoff,
  canonicalPaths,
  isolationModel,
  contextPolicy
} = {}) {
  const failures = [];
  if (manifest?.runId !== runId || input?.runId !== runId || handoff?.runId !== runId) failures.push("canonical-run-id-mismatch");
  if (manifest?.inputPath !== canonicalPaths?.inputPath) failures.push("noncanonical-input-path");
  if (manifest?.handoffPath !== canonicalPaths?.handoffPath) failures.push("noncanonical-handoff-path");
  if (manifest?.reportPath !== canonicalPaths?.reportPath) failures.push("noncanonical-report-path");
  if (handoff?.inputPath !== canonicalPaths?.inputPath) failures.push("handoff-input-path-mismatch");
  if (handoff?.reportPath !== canonicalPaths?.reportPath) failures.push("handoff-report-path-mismatch");
  if (input?.outputContract?.handoffPath !== canonicalPaths?.handoffPath) failures.push("output-contract-handoff-path-mismatch");
  if (input?.outputContract?.reportPath !== canonicalPaths?.reportPath) failures.push("output-contract-report-path-mismatch");
  if (input?.isolationModel !== isolationModel) failures.push("isolation-model-mismatch");
  if (input?.privacyBoundary?.writerPrivateTranscriptShared !== false || input?.privacyBoundary?.reviewerPrivateTranscriptShouldReturn !== false) {
    failures.push("privacy-boundary-mismatch");
  }
  if (contextPolicy !== void 0) {
    if (input?.contextPolicy !== contextPolicy) failures.push("audio-context-policy-mismatch");
    if (input?.privacyBoundary?.projectContextShared !== false || input?.privacyBoundary?.orchestrationBoardShared !== false) {
      failures.push("audio-context-boundary-mismatch");
    }
    for (const field of ["finalPlanPaths", "finalResultPaths", "explicitArtifactPaths"]) {
      if (!sameStringSet2(manifest?.[field], input?.[field])) {
        failures.push(`canonical-audio-${field}-mismatch`);
      }
    }
    const classifiedPaths = normalizeStrings([
      ...normalizeStrings(input?.finalPlanPaths),
      ...normalizeStrings(input?.finalResultPaths),
      ...normalizeStrings(input?.explicitArtifactPaths)
    ]);
    if (!sameStringSet2(classifiedPaths, manifest?.reviewedArtifactPaths)) {
      failures.push("canonical-audio-classification-set-mismatch");
    }
  }
  let scope = null;
  try {
    scope = assertReviewMaterials(buildReviewScope(root, target), "review import");
  } catch {
    failures.push("canonical-reviewed-artifact-scope-mismatch");
  }
  if (scope) {
    if (manifest?.packetId !== scope.packetId || input?.packetId !== scope.packetId) {
      failures.push("canonical-packet-mismatch");
    }
    if (!sameStringSet2(manifest?.includedPacketIds, scope.includedPacketIds) || !sameStringSet2(input?.includedPacketIds, scope.includedPacketIds)) {
      failures.push("canonical-included-packet-set-mismatch");
    }
    if (!sameStringSet2(manifest?.reviewedArtifactPaths, scope.substantiveArtifactPaths) || !sameStringSet2(input?.reviewedArtifactPaths, scope.substantiveArtifactPaths) || !sameStringSet2(handoff?.reviewedArtifactPaths, scope.substantiveArtifactPaths)) {
      failures.push("canonical-reviewed-artifact-set-mismatch");
    }
    const allowedArtifactPaths = new Set(scope.reviewedArtifactPaths);
    const allowedClaimIds = new Set(scope.claimIds);
    const allowedExperimentIds = new Set(scope.experimentIds);
    for (const finding of handoff?.findings ?? []) {
      if (normalizeStrings(finding?.linkedArtifactPaths).some((item) => !allowedArtifactPaths.has(item))) {
        failures.push("finding-artifact-outside-canonical-scope");
      }
      if (normalizeStrings(finding?.claimIds).some((item) => !allowedClaimIds.has(item))) {
        failures.push("finding-claim-outside-canonical-scope");
      }
      if (normalizeStrings(finding?.experimentIds).some((item) => !allowedExperimentIds.has(item))) {
        failures.push("finding-experiment-outside-canonical-scope");
      }
    }
  }
  return {
    ok: failures.length === 0,
    failures: Array.from(new Set(failures)),
    scope
  };
}
function buildReviewScope(root, target, options = {}) {
  const catalog = options.catalog ?? readTaskPacketCatalog(root);
  const packet = catalog.byId.get(target?.packetId) ?? target?.packet;
  if (!packet?.id) {
    throw scopeError("Review scope requires a resolved durable task packet.");
  }
  const includedPackets = descendantsOf(packet, catalog.packets);
  const includedPacketIds = includedPackets.map((item) => item.id);
  const defaultPaths = Array.from(/* @__PURE__ */ new Set([
    ...includedPackets.flatMap(packetPaths),
    ...packetBoundRecordPaths(root, includedPacketIds)
  ]));
  const explicitPaths = localPaths([
    ...normalizeStrings(options.reviewedArtifactPaths),
    ...normalizeStrings(options.artifactPaths)
  ]);
  if (explicitPaths.length > 0) {
    const consistency = analyzeArtifactConsistency(packet, catalog.packets, explicitPaths);
    if (consistency.hasConflict) {
      const owners = consistency.conflictingMatches.map((item) => `${item.packetId}(${item.relation})`).join(", ");
      throw scopeError(`Reviewed artifact paths conflict with packet ${packet.id}: ${owners}.`, { artifactResolution: consistency });
    }
    const allowed = new Set(defaultPaths);
    const invalid = explicitPaths.filter((item) => !allowed.has(item));
    if (invalid.length > 0) {
      throw scopeError(`Reviewed artifact paths must be owned by packet ${packet.id} or its descendants: ${invalid.join(", ")}.`);
    }
  }
  const reviewedArtifactPaths = Array.from(new Set(explicitPaths.length > 0 ? explicitPaths : defaultPaths));
  const inspections = reviewedArtifactPaths.map((artifactPath) => inspectDeclaredPath(root, artifactPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true,
    requireSemanticEvidence: true
  }));
  const substantiveArtifactPaths = inspections.filter((item) => item.status === "existing").map((item) => item.canonicalRelativePath ?? item.normalizedPath);
  return {
    packetId: packet.id,
    packet,
    includedPacketIds,
    includedPackets,
    claimIds: Array.from(/* @__PURE__ */ new Set([...entityIds(includedPackets, "claimIds"), ...packetBoundRecordIds(root, includedPacketIds, ARTIFACT_PATHS.evidence, "claims")])),
    noteIds: Array.from(/* @__PURE__ */ new Set([...entityIds(includedPackets, "noteIds"), ...packetBoundRecordIds(root, includedPacketIds, ARTIFACT_PATHS.notes, "items")])),
    experimentIds: Array.from(/* @__PURE__ */ new Set([...entityIds(includedPackets, "experimentIds"), ...packetBoundRecordIds(root, includedPacketIds, ARTIFACT_PATHS.experimentPlans, "items")])),
    figureIds: packetBoundRecordIds(root, includedPacketIds, ARTIFACT_PATHS.figuresIndex, "items"),
    resultIds: Array.from(/* @__PURE__ */ new Set([...entityIds(includedPackets, "resultIds"), ...packetBoundRecordIds(root, includedPacketIds, ARTIFACT_PATHS.experimentResults, "items")])),
    auditIds: Array.from(/* @__PURE__ */ new Set([...entityIds(includedPackets, "auditIds"), ...packetBoundRecordIds(root, includedPacketIds, ARTIFACT_PATHS.experimentAudits, "items")])),
    rebuttalIssueIds: entityIds(includedPackets, "rebuttalIssueIds"),
    versionIds: entityIds(includedPackets, "versionIds"),
    reviewedArtifactPaths,
    substantiveArtifactPaths,
    artifactInspections: inspections,
    hasReviewMaterials: substantiveArtifactPaths.length > 0
  };
}
function assertReviewMaterials(scope, label = "review") {
  if (!scope?.hasReviewMaterials) {
    const details = (scope?.artifactInspections ?? []).map((item) => `${item.normalizedPath ?? item.path}:${item.reason ?? item.status}`).join(", ") || "no packet-owned artifact paths";
    throw scopeError(`${label} requires at least one existing non-empty non-bookkeeping substantive artifact in packet scope (${details}).`, {
      boundaryType: "missing-review-materials",
      packetId: scope?.packetId ?? null
    });
  }
  return scope;
}

// src/core/reviews.mjs
var UNRESOLVED_CONCERN_STATUSES2 = /* @__PURE__ */ new Set(["open", "awaiting-author-response", "author-response-submitted", "escalated", "contested"]);
var AUTHOR_RESPONSE_PENDING_STATUSES = /* @__PURE__ */ new Set(["open", "awaiting-author-response"]);
var REVIEWER_RULING_PENDING_STATUSES = /* @__PURE__ */ new Set(["author-response-submitted", "contested"]);
var REVIEW_VERDICTS = /* @__PURE__ */ new Set(["coherent", "needs-revision", "needs-evidence", "blocked"]);
function normalizeStringArray18(value) {
  const values = Array.isArray(value) ? value : typeof value === "string" ? [value] : [];
  return Array.from(new Set(values.map((item) => String(item).trim()).filter(Boolean)));
}
function normalizeReviewVerdict(value) {
  const verdict = typeof value === "string" ? value.trim() : "";
  if (!REVIEW_VERDICTS.has(verdict)) {
    throw new Error(`append_review_log requires verdict to be one of: ${Array.from(REVIEW_VERDICTS).join(", ")}.`);
  }
  return verdict;
}
function normalizeRequiredReviewSummary(value) {
  const summary = typeof value === "string" ? value.trim() : "";
  if (!summary) {
    throw new Error("append_review_log requires a substantive review summary; default review text is not progress.");
  }
  return summary;
}
function inspectReviewArtifact(root, rawPath, label) {
  const inspection = inspectDeclaredPath(root, rawPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  if (inspection.status !== "existing") {
    throw new Error(`${label} must be an existing non-empty non-bookkeeping file: ${inspection.normalizedPath ?? rawPath ?? "<missing>"} (${inspection.reason ?? inspection.status}).`);
  }
  return inspection.normalizedPath;
}
function normalizeReviewedArtifactPaths(root, args = {}) {
  const reviewedArtifactPaths = normalizeStringArray18([
    ...normalizeStringArray18(args.reviewedArtifactPaths),
    ...normalizeStringArray18(args.artifactPaths),
    ...normalizeStringArray18(args.linkedArtifactPaths)
  ]);
  return reviewedArtifactPaths.map((artifactPath) => inspectReviewArtifact(root, artifactPath, "reviewedArtifactPaths"));
}
function normalizeExistingReportPath(root, args = {}) {
  const reportPath = args.reportPath ?? args.reviewReportPath ?? null;
  if (!reportPath) {
    return null;
  }
  return inspectReviewArtifact(root, reportPath, "reportPath");
}
function renderReviewReport(entry) {
  return [
    "# Review report",
    "",
    `- Timestamp: ${entry.timestamp}`,
    `- Stage: ${entry.stage}`,
    `- Scope: ${entry.scope}`,
    `- Verdict: ${entry.verdict}`,
    `- Summary: ${entry.summary}`,
    "",
    "## Reviewed artifacts",
    "",
    ...entry.reviewedArtifactPaths.length > 0 ? entry.reviewedArtifactPaths.map((artifactPath) => `- ${artifactPath}`) : ["- None"],
    "",
    "## Findings",
    "",
    ...entry.findings.length > 0 ? entry.findings.map((finding) => `- [${finding.severity}] ${finding.summary}`) : ["- None"],
    "",
    "## Action items",
    "",
    ...entry.actionItems.length > 0 ? entry.actionItems.map((item) => `- [ ] ${item}`) : ["- [ ] None"],
    ""
  ].join("\n");
}
function hasExplicitReviewBoundary(args = {}) {
  return normalizeStringArray18(args.requiredInputs).length > 0 || normalizeStringArray18(args.requiredActions).length > 0 || typeof args.boundaryReason === "string" || typeof args.boundaryType === "string";
}
function localizedText3(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function isProofOnlyReview(entry = {}) {
  const findings = Array.isArray(entry.findings) ? entry.findings : [];
  return findings.length > 0 && findings.every((finding) => finding?.methodologicalCategory === "review-proof");
}
function reviewResultCard(root, args = {}, entry, command = "append_review_log") {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const findingCount = entry.findings?.length ?? 0;
  const actionItemCount = entry.actionItems?.length ?? 0;
  const proofOnly = isProofOnlyReview(entry);
  return buildCommandResultCard({
    surface: "dove.review",
    command,
    title: localizedText3(responseLanguage, "review \u7ED3\u679C\u5DF2\u8BB0\u5F55", "Review result recorded"),
    status: entry.verdict,
    happened: entry.verdict === "coherent" ? localizedText3(responseLanguage, "Reviewer \u8BA4\u4E3A\u5F53\u524D\u6750\u6599\u57FA\u672C\u81EA\u6D3D\u3002", "The reviewer found the current materials coherent.") : localizedText3(responseLanguage, "Reviewer \u8BB0\u5F55\u4E86\u9700\u8981\u5904\u7406\u7684\u95EE\u9898\u3002", "The reviewer recorded issues that need attention."),
    durableWrites: [localizedText3(responseLanguage, "\u5BA1\u6838\u8BB0\u5F55\u3001\u95EE\u9898\u72B6\u6001\u548C\u4FEE\u8BA2\u8BA1\u5212\u5DF2\u66F4\u65B0\u3002", "Review log, concern state, and revision plan were updated.")],
    evidence: [localizedText3(responseLanguage, `\u53D1\u73B0 ${findingCount} \u4E2A\u95EE\u9898\uFF0C\u7559\u4E0B ${actionItemCount} \u4E2A\u884C\u52A8\u9879\u3002`, `Found ${findingCount} issues and left ${actionItemCount} action items.`)],
    validation: [entry.summary],
    scope: { verdict: entry.verdict, findingCount, actionItemCount },
    nextActions: [{
      title: entry.verdict === "coherent" ? localizedText3(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u51B3\u5B9A\u662F\u5426\u6536\u5C3E", "Return to status and decide whether to close") : proofOnly ? localizedText3(responseLanguage, "\u4FDD\u6301 Reviewer ownership \u5E76\u63D0\u4EA4\u6743\u5A01 proof", "Keep Reviewer ownership and submit authoritative proof") : localizedText3(responseLanguage, "\u628A\u6700\u9AD8\u4F18\u5148\u7EA7\u95EE\u9898\u53D8\u6210\u4FEE\u8BA2\u52A8\u4F5C", "Turn the highest-priority issue into revision work"),
      why: entry.verdict === "coherent" ? localizedText3(responseLanguage, "review \u5DF2\u901A\u8FC7\uFF0C\u4E0B\u4E00\u6B65\u5E94\u9009\u62E9\u7EE7\u7EED\u5199\u3001\u505A\u7248\u672C\u5FEB\u7167\u6216\u6536\u5C3E\u3002", "Review passed; next choose whether to keep drafting, snapshot, or close.") : proofOnly ? localizedText3(responseLanguage, "\u5F53\u524D\u6CA1\u6709 substantive finding\uFF0C\u53EA\u6709 Reviewer proof \u7F3A\u5931\uFF0C\u4E0D\u80FD\u8DEF\u7531\u4E3A Builder \u4FEE\u8BA2\u3002", "There is no substantive finding; only Reviewer proof is missing, so this must not route to Builder revision.") : localizedText3(responseLanguage, "review \u5DF2\u6307\u51FA\u7F3A\u53E3\uFF0C\u4E0B\u4E00\u6B65\u8981\u5148\u4FEE\u6750\u6599\u800C\u4E0D\u662F\u5BA3\u5E03\u5B8C\u6210\u3002", "Review found gaps, so the next step is repair rather than claiming completion.")
    }]
  }, responseLanguage);
}
function slugify8(value) {
  return String(value).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function renderReviewEntry(entry) {
  const reviewedArtifactPaths = normalizeStringArray18(
    entry.reviewedArtifactPaths
  );
  const findings = Array.isArray(entry.findings) ? entry.findings : [];
  const actionItems = normalizeStringArray18(
    entry.actionItems
  );
  return [
    `## ${entry.timestamp} \u2014 ${entry.stage}`,
    "",
    `- Scope: ${entry.scope}`,
    `- Verdict: ${entry.verdict}`,
    `- Summary: ${entry.summary}`,
    `- Report: ${entry.reportPath ?? "none"}`,
    `- Reviewed artifacts: ${reviewedArtifactPaths.length > 0 ? reviewedArtifactPaths.join(", ") : "none"}`,
    `- Review required before finalize: ${entry.reviewRequiredBeforeFinalize}`,
    "- Findings:",
    ...findings.length > 0 ? findings.map((item) => `  - [${item.severity}] ${item.summary}${item.responseOwnerRole ? ` (response owner: ${item.responseOwnerRole})` : ""}`) : ["  - None recorded"],
    "- Action items:",
    ...actionItems.length > 0 ? actionItems.map((item) => `  - ${item}`) : ["  - None recorded"],
    ""
  ].join("\n");
}
function renderRevisionPlan(review) {
  return [
    "# Current revision plan",
    "",
    `- Verdict: ${review.verdict}`,
    `- Updated: ${review.timestamp}`,
    "",
    "## Action items",
    "",
    ...review.actionItems.length > 0 ? review.actionItems.map((item) => `- [ ] ${item}`) : ["- [ ] No action items recorded."]
  ].join("\n");
}
function concernFingerprint(concern = {}, index = 0) {
  const summary = concern.summary ?? `concern-${index + 1}`;
  const claimIds = Array.isArray(concern.claimIds) ? concern.claimIds.join("-") : "";
  const experimentIds = Array.isArray(concern.experimentIds) ? concern.experimentIds.join("-") : "";
  return slugify8(`${concern.packetId ?? "unscoped"}-${summary}-${claimIds}-${experimentIds}`);
}
function normalizeConcernStatus(status, fallback = "open") {
  const allowed = /* @__PURE__ */ new Set(["open", "awaiting-author-response", "author-response-submitted", "escalated", "contested", "resolved", "retired"]);
  return allowed.has(status) ? status : fallback;
}
function defaultResponseOwnerRole(concern = {}) {
  if ((concern.experimentIds ?? []).length > 0) {
    return "experiment-planner";
  }
  if ((concern.claimIds ?? []).length > 0) {
    return "researcher";
  }
  return "planner";
}
function escalationThresholdForSeverity(severity = "medium") {
  return severity === "high" ? 1 : severity === "medium" ? 2 : 3;
}
function deriveConcernStatus(existing, concern, context = {}) {
  const explicitStatus = concern.status ? normalizeConcernStatus(concern.status) : null;
  if (explicitStatus && ["resolved", "retired"].includes(explicitStatus)) {
    return explicitStatus;
  }
  if (explicitStatus && explicitStatus !== "open") {
    return explicitStatus;
  }
  if (concern.authorRebuttalSummary) {
    return "author-response-submitted";
  }
  const previousRecurrence = existing?.recurrenceCount ?? 0;
  const recurrenceCount = previousRecurrence + 1;
  const threshold = concern.escalationThreshold ?? existing?.escalationThreshold ?? escalationThresholdForSeverity(concern.severity);
  const shouldEscalate = recurrenceCount >= threshold || concern.severity === "high" || (context.integrityCriticalConcernIds ?? /* @__PURE__ */ new Set()).has(concern.id);
  if (shouldEscalate) {
    return existing?.authorRebuttalSummary ? "contested" : "escalated";
  }
  return "awaiting-author-response";
}
function summarizeConcernState(items = []) {
  const concernStatusCounts = {};
  const unresolvedConcernIds = [];
  const escalatedConcernIds = [];
  const pendingAuthorResponseIds = [];
  const pendingReviewerRulingIds = [];
  for (const item of items) {
    concernStatusCounts[item.status] = (concernStatusCounts[item.status] ?? 0) + 1;
    if (UNRESOLVED_CONCERN_STATUSES2.has(item.status)) {
      unresolvedConcernIds.push(item.id);
    }
    if (item.status === "escalated") {
      escalatedConcernIds.push(item.id);
    }
    if (AUTHOR_RESPONSE_PENDING_STATUSES.has(item.status)) {
      pendingAuthorResponseIds.push(item.id);
    }
    if (REVIEWER_RULING_PENDING_STATUSES.has(item.status)) {
      pendingReviewerRulingIds.push(item.id);
    }
  }
  return {
    unresolvedConcernIds,
    escalatedConcernIds,
    pendingAuthorResponseIds,
    pendingReviewerRulingIds,
    concernStatusCounts
  };
}
function normalizeConcern(concern = {}, index = 0) {
  return {
    id: slugify8(concern.id ?? concernFingerprint(concern, index)),
    packetId: concern.packetId ?? null,
    includedPacketIds: normalizeStringArray18(concern.includedPacketIds),
    reviewedArtifactPaths: normalizeStringArray18(concern.reviewedArtifactPaths),
    summary: concern.summary ?? `Concern ${index + 1}`,
    severity: concern.severity ?? "medium",
    status: normalizeConcernStatus(concern.status ?? "open"),
    raisedByRole: concern.raisedByRole ?? "reviewer",
    responseOwnerRole: concern.responseOwnerRole ?? defaultResponseOwnerRole(concern),
    reviewerRationale: concern.reviewerRationale ?? concern.summary ?? "",
    authorRebuttalSummary: concern.authorRebuttalSummary ?? "",
    rulingOutcome: concern.rulingOutcome ?? "pending",
    reviewerDisposition: concern.reviewerDisposition ?? "pending",
    recurrenceCount: Number.isInteger(concern.recurrenceCount) ? concern.recurrenceCount : 1,
    firstSeenAt: concern.firstSeenAt ?? nowIso(),
    lastSeenAt: concern.lastSeenAt ?? nowIso(),
    reviewRoundFirstSeen: concern.reviewRoundFirstSeen ?? 1,
    reviewRoundLastSeen: concern.reviewRoundLastSeen ?? 1,
    escalationLevel: Number.isInteger(concern.escalationLevel) ? concern.escalationLevel : 0,
    escalationThreshold: concern.escalationThreshold ?? escalationThresholdForSeverity(concern.severity),
    escalationReason: concern.escalationReason ?? "",
    linkedAuditIds: Array.isArray(concern.linkedAuditIds) ? concern.linkedAuditIds : [],
    linkedBridgeIds: Array.isArray(concern.linkedBridgeIds) ? concern.linkedBridgeIds : [],
    linkedArtifactPaths: Array.isArray(concern.linkedArtifactPaths) ? concern.linkedArtifactPaths : [],
    claimIds: Array.isArray(concern.claimIds) ? concern.claimIds : [],
    experimentIds: Array.isArray(concern.experimentIds) ? concern.experimentIds : [],
    sourceReviewIds: normalizeStringArray18(concern.sourceReviewIds),
    sourceExecutionClaimIds: normalizeStringArray18(concern.sourceExecutionClaimIds),
    updatedAt: nowIso()
  };
}
function upsertConcernLedger(root, concerns = [], context = {}) {
  const current = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const merged = new Map((current.items ?? []).map((item, index) => {
    const normalized = normalizeConcern(item, index);
    return [normalized.id, normalized];
  }));
  const touched = [];
  const integrityCriticalConcernIds = new Set(Array.isArray(context.integrityCriticalConcernIds) ? context.integrityCriticalConcernIds : []);
  const reviewRound = Number.isInteger(context.reviewRound) ? context.reviewRound : 1;
  for (const concern of concerns.map(normalizeConcern)) {
    const existing = merged.get(concern.id);
    const status = deriveConcernStatus(existing, concern, { ...context, integrityCriticalConcernIds });
    const repeatedExecutionClaim = Boolean(
      context.executionClaimId && existing?.sourceExecutionClaimIds?.includes(context.executionClaimId)
    );
    const recurrenceCount = concern.status === "resolved" ? existing?.recurrenceCount ?? 1 : status === "resolved" ? existing?.recurrenceCount ?? 1 : repeatedExecutionClaim ? existing?.recurrenceCount ?? 1 : (existing?.recurrenceCount ?? 0) + 1;
    const escalationThreshold = concern.escalationThreshold ?? existing?.escalationThreshold ?? escalationThresholdForSeverity(concern.severity);
    const escalationLevel = ["escalated", "contested"].includes(status) ? Math.max(existing?.escalationLevel ?? 0, Math.max(1, recurrenceCount - escalationThreshold + 1)) : 0;
    const escalationReason = ["escalated", "contested"].includes(status) ? integrityCriticalConcernIds.has(concern.id) ? "Integrity-critical concern remains unresolved." : recurrenceCount >= escalationThreshold ? `Concern remained unresolved across ${recurrenceCount} review rounds.` : `${concern.severity} severity concern requires reviewer escalation.` : "";
    const next = {
      ...existing ?? {},
      ...concern,
      status,
      raisedByRole: existing?.raisedByRole ?? concern.raisedByRole ?? "reviewer",
      responseOwnerRole: concern.responseOwnerRole ?? existing?.responseOwnerRole ?? defaultResponseOwnerRole(concern),
      reviewerDisposition: status === "resolved" ? "accepted" : concern.reviewerDisposition ?? existing?.reviewerDisposition ?? "pending",
      firstSeenAt: existing?.firstSeenAt ?? concern.firstSeenAt ?? nowIso(),
      lastSeenAt: nowIso(),
      reviewRoundFirstSeen: existing?.reviewRoundFirstSeen ?? reviewRound,
      reviewRoundLastSeen: reviewRound,
      recurrenceCount,
      escalationThreshold,
      escalationLevel,
      escalationReason,
      linkedAuditIds: Array.from(/* @__PURE__ */ new Set([...existing?.linkedAuditIds ?? [], ...concern.linkedAuditIds ?? [], ...context.auditIds ?? []])),
      linkedBridgeIds: Array.from(/* @__PURE__ */ new Set([...existing?.linkedBridgeIds ?? [], ...concern.linkedBridgeIds ?? [], ...context.bridgeIds ?? []])),
      linkedArtifactPaths: Array.from(/* @__PURE__ */ new Set([...existing?.linkedArtifactPaths ?? [], ...concern.linkedArtifactPaths ?? []])),
      sourceReviewIds: Array.from(/* @__PURE__ */ new Set([...existing?.sourceReviewIds ?? [], ...concern.sourceReviewIds ?? [], ...[context.reviewId].filter(Boolean)])),
      sourceExecutionClaimIds: Array.from(/* @__PURE__ */ new Set([...existing?.sourceExecutionClaimIds ?? [], ...concern.sourceExecutionClaimIds ?? [], ...[context.executionClaimId].filter(Boolean)])),
      updatedAt: nowIso()
    };
    merged.set(next.id, next);
    touched.push(next);
  }
  const items = Array.from(merged.values()).sort((left, right) => left.id.localeCompare(right.id));
  writeJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items, updatedAt: nowIso() });
  const summary = summarizeConcernState(items);
  const adversarialState = readJson(root, ARTIFACT_PATHS.adversarialReviewState, {
    version: 2,
    round: 0,
    unresolvedConcernIds: [],
    escalatedConcernIds: [],
    pendingAuthorResponseIds: [],
    pendingReviewerRulingIds: [],
    concernStatusCounts: {},
    escalationThresholds: { high: 1, medium: 2, low: 3 },
    lastAuditIds: [],
    lastBridgeIds: [],
    updatedAt: null
  });
  writeJson(root, ARTIFACT_PATHS.adversarialReviewState, {
    ...adversarialState,
    version: 2,
    round: (adversarialState.round ?? 0) + 1,
    unresolvedConcernIds: summary.unresolvedConcernIds,
    escalatedConcernIds: summary.escalatedConcernIds,
    pendingAuthorResponseIds: summary.pendingAuthorResponseIds,
    pendingReviewerRulingIds: summary.pendingReviewerRulingIds,
    concernStatusCounts: summary.concernStatusCounts,
    escalationThresholds: { high: 1, medium: 2, low: 3 },
    lastAuditIds: Array.isArray(context.auditIds) ? context.auditIds : adversarialState.lastAuditIds,
    lastBridgeIds: Array.isArray(context.bridgeIds) ? context.bridgeIds : adversarialState.lastBridgeIds,
    updatedAt: nowIso()
  });
  if (touched.length > 0 && !context.isRetry) {
    appendText(root, ARTIFACT_PATHS.reviewDebateLog, `
## ${nowIso()} \u2014 review round

${touched.map((concern) => `- ${concern.id} [${concern.status}] reviewer=${concern.raisedByRole} response-owner=${concern.responseOwnerRole} recurrence=${concern.recurrenceCount}: ${concern.summary}`).join("\n")}
`);
  }
  return { items, ...summary };
}
var SYSTEM_OWNED_REVIEW_FIELDS = /* @__PURE__ */ new Set([
  "authorizationProvenance",
  "authorizationProvenanceHistory",
  "authorizationFingerprint",
  "authorityStepId",
  "authorityStepIndex",
  "claimId",
  "executionClaimId",
  "runtimeRunId",
  "leaseId",
  "sourceExecutionClaimId",
  "sourceExecutionClaimIds",
  "sourceReviewExecutionClaimId",
  "sourceReviewId",
  "sourceReviewIds",
  "reviewRequiredBeforeFinalize"
]);
var INTERNAL_REVIEW_CONTROL_FIELDS = /* @__PURE__ */ new Set([
  "skipBoardUpdate",
  "skipRefreshDurableSurfaces"
]);
function collectForbiddenPublicReviewPaths(value, path28 = "", seen = /* @__PURE__ */ new WeakSet()) {
  if (!value || typeof value !== "object") {
    return [];
  }
  if (seen.has(value)) {
    return [];
  }
  seen.add(value);
  if (Array.isArray(value)) {
    return value.flatMap(
      (item, index) => collectForbiddenPublicReviewPaths(item, `${path28}[${index}]`, seen)
    );
  }
  return Object.entries(value).flatMap(([key, nestedValue]) => {
    const fieldPath = path28 ? `${path28}.${key}` : key;
    const forbidden = key.startsWith("policyOverride") || INTERNAL_REVIEW_CONTROL_FIELDS.has(key) || SYSTEM_OWNED_REVIEW_FIELDS.has(key);
    return [
      ...forbidden ? [fieldPath] : [],
      ...collectForbiddenPublicReviewPaths(nestedValue, fieldPath, seen)
    ];
  });
}
function assertNoPublicReviewBypassControls(args = {}) {
  const forbidden = collectForbiddenPublicReviewPaths(args);
  if (forbidden.length > 0) {
    throw new Error(
      `Public review mutations do not accept system-owned provenance or internal control fields: ${forbidden.join(", ")}.`
    );
  }
}
function appendReviewLog(root, args = {}) {
  assertGovernanceMutationRegistered("append-review-log", "guarded");
  assertNoPublicReviewBypassControls(args);
  assertTaskScopedMutationTarget(root, "append-review-log", args);
  assertFollowThroughReady(root, "Recording a review log", args);
  return persistReviewLog(root, args);
}
function persistReviewLog(root, args = {}, runtimeContext = {}) {
  const timestamp = args.timestamp ?? nowIso();
  const verdict = normalizeReviewVerdict(args.verdict);
  const summary = normalizeRequiredReviewSummary(args.summary);
  const reviewedArtifactPaths = normalizeReviewedArtifactPaths(root, args);
  let reportPath = normalizeExistingReportPath(root, args);
  const findings = Array.isArray(args.findings) ? args.findings.map((item) => ({
    severity: item.severity ?? "medium",
    summary: item.summary ?? String(item),
    claimIds: item.claimIds ?? [],
    experimentIds: item.experimentIds ?? [],
    responseOwnerRole: item.responseOwnerRole,
    reviewerAction: item.reviewerAction,
    linkedAuditIds: item.linkedAuditIds ?? [],
    linkedBridgeIds: item.linkedBridgeIds ?? [],
    linkedArtifactPaths: item.linkedArtifactPaths ?? [],
    methodologicalCategory: item.methodologicalCategory ?? null
  })) : [];
  const actionItems = Array.isArray(args.actionItems) ? args.actionItems : [];
  const independentReviewProof = verdict === "coherent" ? findCurrentIndependentReviewProof(root, {
    packetId: args.packetId ?? void 0,
    reviewedArtifactPaths
  }) : { ok: false, proof: null };
  if (verdict === "coherent") {
    if (reviewedArtifactPaths.length === 0) {
      throw new Error("append_review_log cannot record a coherent review without at least one existing non-empty reviewed artifact.");
    }
    if (!reportPath && args.autoGeneratedReviewReport !== true) {
      throw new Error("append_review_log cannot record a coherent review without an existing non-empty review report file.");
    }
    if (!independentReviewProof.ok) {
      throw new Error("append_review_log cannot record a coherent review without current-hash-bound independent isolated Reviewer proof for every reviewed artifact.");
    }
  } else if (findings.length === 0 && actionItems.length === 0 && !hasExplicitReviewBoundary(args)) {
    throw new Error("append_review_log requires findings, actionItems, or an explicit material boundary for non-coherent reviews.");
  }
  const entry = {
    id: runtimeContext.executionClaimId ?? args.id ?? `review-${slugify8(`${args.packetId ?? "unscoped"}-${args.stage ?? "manual-review"}-${timestamp}`)}`,
    packetId: args.packetId ?? null,
    includedPacketIds: normalizeStringArray18(args.includedPacketIds ?? [args.packetId].filter(Boolean)),
    timestamp,
    stage: args.stage ?? "manual-review",
    scope: args.scope ?? "current paper materials",
    verdict,
    summary,
    reviewRequiredBeforeFinalize: Boolean(args.reviewRequiredBeforeFinalize ?? true),
    reportPath,
    reviewedArtifactPaths,
    resolvedConcernIds: normalizeStringArray18(args.resolvedConcernIds),
    findings,
    actionItems,
    ...independentReviewProof.ok ? { independentReviewProof: independentReviewProof.proof } : {},
    ...runtimeContext.authorizationProvenance ? { authorizationProvenance: runtimeContext.authorizationProvenance } : {}
  };
  if (!entry.reportPath && args.autoGeneratedReviewReport === true) {
    writeText(root, ARTIFACT_PATHS.reviewReport, renderReviewReport(entry));
    entry.reportPath = ARTIFACT_PATHS.reviewReport;
  }
  const reviewState = readJson(root, ARTIFACT_PATHS.reviewState, {
    version: 3,
    history: [],
    openItems: [],
    lastVerdict: "not-reviewed",
    lastReviewedAt: null,
    unresolvedConcernIds: [],
    escalatedConcernIds: [],
    pendingAuthorResponseIds: [],
    pendingReviewerRulingIds: [],
    reviewRound: 0,
    reviewerIndependence: { reviewerRole: "reviewer", responseOwnerRoles: [], separationMaintained: true }
  });
  const existingHistoryIndex = (reviewState.history ?? []).findIndex((item) => item.id === entry.id);
  const isRetry = existingHistoryIndex >= 0;
  const reviewRound = isRetry ? reviewState.history[existingHistoryIndex].reviewRound ?? reviewState.reviewRound ?? 1 : (reviewState.reviewRound ?? 0) + 1;
  entry.reviewRound = reviewRound;
  const integrityCriticalConcernIds = entry.findings.filter((finding) => finding.severity === "high" || finding.methodologicalCategory === "integrity").map((finding, index) => concernFingerprint({
    id: `review-${entry.stage}-${index + 1}`,
    packetId: entry.packetId,
    summary: finding.summary,
    claimIds: finding.claimIds,
    experimentIds: finding.experimentIds
  }));
  const currentConcerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const concernInputs = entry.verdict === "coherent" ? (currentConcerns.items ?? []).filter((item) => entry.resolvedConcernIds.includes(item.id) && UNRESOLVED_CONCERN_STATUSES2.has(item.status)).map((item) => ({
    ...item,
    status: "resolved",
    rulingOutcome: "accepted",
    reviewerDisposition: "accepted",
    linkedArtifactPaths: Array.from(new Set([...item.linkedArtifactPaths ?? [], ARTIFACT_PATHS.reviewLog, entry.reportPath, ...entry.reviewedArtifactPaths].filter(Boolean)))
  })) : entry.findings.map((finding, index) => ({
    id: concernFingerprint({
      id: `review-${entry.stage}-${index + 1}`,
      packetId: entry.packetId,
      summary: finding.summary,
      claimIds: finding.claimIds,
      experimentIds: finding.experimentIds
    }),
    packetId: entry.packetId,
    includedPacketIds: entry.includedPacketIds,
    reviewedArtifactPaths: entry.reviewedArtifactPaths,
    summary: finding.summary,
    severity: finding.severity,
    responseOwnerRole: finding.responseOwnerRole,
    reviewerRationale: finding.summary,
    linkedAuditIds: finding.linkedAuditIds,
    linkedBridgeIds: finding.linkedBridgeIds,
    linkedArtifactPaths: [ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.revisionPlan, entry.reportPath, ...entry.reviewedArtifactPaths, ...finding.linkedArtifactPaths].filter(Boolean),
    claimIds: finding.claimIds,
    experimentIds: finding.experimentIds
  }));
  const concernLedger = upsertConcernLedger(root, concernInputs, {
    reviewRound,
    integrityCriticalConcernIds,
    reviewId: entry.id,
    executionClaimId: runtimeContext.executionClaimId ?? null,
    isRetry
  });
  const nextHistory = [...reviewState.history ?? []];
  if (existingHistoryIndex >= 0) {
    nextHistory[existingHistoryIndex] = entry;
  } else {
    nextHistory.push(entry);
  }
  writeText(root, ARTIFACT_PATHS.reviewLog, [
    "# Review log",
    "",
    ...nextHistory.flatMap((item) => [renderReviewEntry(item), ""])
  ].join("\n"));
  const previousPerPacket = reviewState.perPacket && typeof reviewState.perPacket === "object" ? reviewState.perPacket : {};
  const nextPerPacket = entry.packetId ? {
    ...previousPerPacket,
    [entry.packetId]: {
      packetId: entry.packetId,
      includedPacketIds: entry.includedPacketIds,
      verdict: entry.verdict,
      reviewedAt: timestamp,
      reviewId: entry.id,
      reviewedArtifactPaths: entry.reviewedArtifactPaths,
      unresolvedConcernIds: concernLedger.items.filter((item) => item.packetId === entry.packetId && UNRESOLVED_CONCERN_STATUSES2.has(item.status)).map((item) => item.id)
    }
  } : previousPerPacket;
  const workspaceVerdict = Object.values(nextPerPacket).some((item) => item.verdict === "blocked") ? "blocked" : Object.values(nextPerPacket).some((item) => item.verdict === "needs-evidence") ? "needs-evidence" : Object.values(nextPerPacket).some((item) => item.verdict === "needs-revision") ? "needs-revision" : Object.values(nextPerPacket).length > 0 ? "coherent" : "not-reviewed";
  const nextReviewState = {
    ...reviewState,
    version: 4,
    perPacket: nextPerPacket,
    lastVerdict: workspaceVerdict,
    lastReviewedAt: timestamp,
    reviewRound: Math.max(reviewState.reviewRound ?? 0, reviewRound),
    history: nextHistory,
    openItems: entry.actionItems,
    unresolvedConcernIds: concernLedger.unresolvedConcernIds,
    escalatedConcernIds: concernLedger.escalatedConcernIds,
    pendingAuthorResponseIds: concernLedger.pendingAuthorResponseIds,
    pendingReviewerRulingIds: concernLedger.pendingReviewerRulingIds,
    reviewerIndependence: {
      reviewerRole: "reviewer",
      responseOwnerRoles: Array.from(new Set(concernLedger.items.filter((item) => UNRESOLVED_CONCERN_STATUSES2.has(item.status)).map((item) => item.responseOwnerRole))),
      separationMaintained: concernLedger.items.every((item) => item.raisedByRole === "reviewer" && item.responseOwnerRole !== "reviewer")
    }
  };
  writeJson(root, ARTIFACT_PATHS.reviewState, nextReviewState);
  writeText(root, ARTIFACT_PATHS.revisionPlan, renderRevisionPlan(entry));
  const state = loadState(root);
  saveState(root, {
    ...state,
    pipeline: {
      ...state.pipeline,
      currentStage: "review",
      lastCompletedStage: "review",
      resumeCommand: "project:dove.draft",
      updatedAt: timestamp
    },
    reviews: {
      lastVerdict: workspaceVerdict,
      perPacket: nextPerPacket,
      lastReviewedAt: timestamp,
      openItems: entry.actionItems,
      unresolvedConcernIds: concernLedger.unresolvedConcernIds
    }
  });
  if (runtimeContext.skipBoardUpdate !== true) {
    upsertSystemOrchestrationBoard(root, {
      phase: "review",
      assignedRole: "reviewer",
      intentType: entry.verdict === "coherent" ? "review" : "repair",
      currentFocus: entry.summary,
      nextAction: entry.verdict === "coherent" ? "Refresh downstream artifacts before finalization claims." : "Turn the highest-severity review findings into concrete revision work.",
      reviewRequiredBeforeFinalize: entry.reviewRequiredBeforeFinalize,
      blockers: entry.actionItems.map((item, index) => ({
        id: `review-blocker-${index + 1}`,
        summary: item,
        status: entry.verdict === "coherent" ? "resolved" : "open",
        assignedRole: "reviewer",
        currentFocus: item,
        nextAction: "Resolve the review blocker and re-run review."
      })),
      continuationState: {
        status: entry.verdict === "coherent" ? "ready-to-resume" : "blocked",
        lastCheckpoint: `Review verdict recorded: ${entry.verdict}.`,
        checkpointHistory: [{ summary: `Review verdict recorded: ${entry.verdict}.`, recordedAt: timestamp }]
      }
    });
  }
  if (runtimeContext.skipRefreshDurableSurfaces !== true) {
    refreshDurableSurfaces(root, {
      type: "append-review-log",
      summary: `Recorded review verdict ${entry.verdict}.`,
      artifactPaths: [ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.adversarialReviewState, ARTIFACT_PATHS.revisionPlan, entry.reportPath].filter(Boolean)
    });
  }
  return {
    ...entry,
    resultCard: reviewResultCard(root, args, entry)
  };
}
function upsertRevisionPlan(root, args = {}) {
  assertGovernanceMutationRegistered("upsert-revision-plan", "guarded");
  assertTaskScopedMutationTarget(root, "upsert-revision-plan", args);
  assertFollowThroughReady(root, "Updating the revision plan", args);
  assertReviewProofBoundaryTransition(root, loadBoard(root), "review", "planner", "Updating the revision plan");
  const timestamp = args.updatedAt ?? nowIso();
  const summary = args.summary ?? "Manual revision plan update.";
  const items = Array.isArray(args.items) ? args.items : [];
  const content = [
    "# Current revision plan",
    "",
    `- Updated: ${timestamp}`,
    `- Summary: ${summary}`,
    "",
    "## Action items",
    "",
    ...items.length > 0 ? items.map((item) => `- [ ] ${item}`) : ["- [ ] No action items recorded."]
  ].join("\n");
  writeText(root, ARTIFACT_PATHS.revisionPlan, content);
  upsertSystemOrchestrationBoard(root, {
    phase: "review",
    assignedRole: "planner",
    intentType: "repair",
    currentFocus: summary,
    nextAction: items[0] ?? "Refresh the review loop once revisions land.",
    tasks: items.map((item, index) => ({
      id: `revision-task-${index + 1}`,
      title: item,
      status: "pending",
      assignedRole: "planner",
      currentFocus: item,
      nextAction: "Complete the revision and refresh review surfaces."
    })),
    blockers: items.map((item, index) => ({
      id: `revision-blocker-${index + 1}`,
      summary: item,
      status: "open",
      assignedRole: "planner",
      currentFocus: item,
      nextAction: "Resolve the revision blocker before finalization."
    })),
    reviewRequiredBeforeFinalize: true
  });
  refreshDurableSurfaces(root, {
    type: "upsert-revision-plan",
    summary: `Updated revision plan with ${items.length} items.`,
    artifactPaths: [ARTIFACT_PATHS.revisionPlan, ARTIFACT_PATHS.taskPacketsIndex, ARTIFACT_PATHS.sessionSummary]
  });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  return {
    updatedAt: timestamp,
    itemCount: items.length,
    resultCard: buildCommandResultCard({
      surface: "dove.draft",
      command: "upsert_revision_plan",
      title: localizedText3(responseLanguage, "\u4FEE\u8BA2\u8BA1\u5212\u5DF2\u66F4\u65B0", "Revision plan updated"),
      status: items.length > 0 ? "needs-revision" : "updated",
      happened: localizedText3(responseLanguage, `\u5DF2\u6574\u7406 ${items.length} \u4E2A\u4FEE\u8BA2\u52A8\u4F5C\u3002`, `Organized ${items.length} revision actions.`),
      durableWrites: [localizedText3(responseLanguage, "\u4FEE\u8BA2\u8BA1\u5212\u548C\u4EFB\u52A1\u770B\u677F\u5DF2\u66F4\u65B0\u3002", "Revision plan and task board were updated.")],
      scope: { itemCount: items.length, summary },
      nextActions: [{
        title: localizedText3(responseLanguage, "\u5B8C\u6210\u4FEE\u8BA2\u540E\u91CD\u65B0 review", "Run review again after revisions"),
        why: localizedText3(responseLanguage, "\u4FEE\u8BA2\u8BA1\u5212\u53EA\u662F\u5F85\u529E\uFF0C\u53EA\u6709\u91CD\u65B0 review \u901A\u8FC7\u540E\u624D\u80FD\u8BF4\u6750\u6599\u95ED\u73AF\u3002", "A revision plan is only pending work; the material is closed only after review passes again.")
      }]
    }, responseLanguage)
  };
}
function normalizeReviewIssue(issue = {}, index = 0) {
  return {
    id: slugify8(issue.id ?? issue.summary ?? `review-issue-${index + 1}`),
    reviewer: issue.reviewer ?? "review-loop",
    summary: issue.summary ?? `Issue ${index + 1}`,
    severity: issue.severity ?? "medium",
    status: issue.status ?? "open",
    evidenceLinks: normalizeStringArray18(issue.evidenceLinks),
    claimIds: normalizeStringArray18(issue.claimIds),
    experimentIds: normalizeStringArray18(issue.experimentIds),
    responseDirection: issue.responseDirection ?? "clarify",
    ...issue.authorizationProvenance ? { authorizationProvenance: issue.authorizationProvenance } : {},
    authorizationProvenanceHistory: Array.isArray(issue.authorizationProvenanceHistory) ? issue.authorizationProvenanceHistory : [],
    sourceReviewIds: normalizeStringArray18(issue.sourceReviewIds),
    sourceReviewExecutionClaimId: issue.sourceReviewExecutionClaimId ?? null,
    updatedAt: nowIso()
  };
}
function persistReviewRebuttalIssues(root, reviewEntry, findings = [], context = {}) {
  const current = readJson(root, ARTIFACT_PATHS.rebuttalIssues, {
    version: 1,
    items: [],
    updatedAt: null
  });
  const merged = new Map((current.items ?? []).map((issue, index) => {
    const normalized = normalizeReviewIssue(issue, index);
    return [normalized.id, normalized];
  }));
  for (const [index, finding] of findings.entries()) {
    const id = slugify8(`${reviewEntry.id}-${concernFingerprint({
      summary: finding.summary,
      claimIds: finding.claimIds,
      experimentIds: finding.experimentIds
    }, index)}`);
    const existing = merged.get(id) ?? null;
    const provenanceHistory = context.authorizationProvenance ? [...existing?.authorizationProvenanceHistory ?? [], context.authorizationProvenance].filter((item, itemIndex, items2) => items2.findIndex((candidate) => JSON.stringify(candidate) === JSON.stringify(item)) === itemIndex) : existing?.authorizationProvenanceHistory ?? [];
    merged.set(id, normalizeReviewIssue({
      ...existing,
      id,
      reviewer: context.reviewer,
      summary: finding.summary,
      severity: finding.severity,
      status: reviewEntry.verdict === "coherent" ? "resolved" : "open",
      evidenceLinks: loadBoard(root).evidenceLinks,
      claimIds: finding.claimIds,
      experimentIds: finding.experimentIds,
      responseDirection: finding.severity === "high" ? "fix" : "clarify",
      ...context.authorizationProvenance ? {
        authorizationProvenance: context.authorizationProvenance,
        authorizationProvenanceHistory: provenanceHistory
      } : {},
      sourceReviewIds: Array.from(/* @__PURE__ */ new Set([...existing?.sourceReviewIds ?? [], reviewEntry.id])),
      sourceReviewExecutionClaimId: context.executionClaimId ?? existing?.sourceReviewExecutionClaimId ?? null
    }, index));
  }
  const items = Array.from(merged.values()).sort((left, right) => left.id.localeCompare(right.id));
  const next = { version: 1, items, updatedAt: nowIso() };
  writeJson(root, ARTIFACT_PATHS.rebuttalIssues, next);
  return next;
}
function transitionToLocalReviewer(root, args = {}, target = {}) {
  const board = loadBoard(root);
  if (board.currentPhase === "review" && board.assignedRole === "reviewer") {
    return null;
  }
  return upsertSystemOrchestrationBoard(root, {
    phase: "review",
    assignedRole: "reviewer",
    intentType: "review",
    currentFocus: args.scope ?? target.packet?.title ?? board.currentFocus,
    nextAction: "Run the local evidence-aware review pass.",
    handoffSummary: `Handing ${target.packet?.title ?? "the current work"} to the local reviewer for an evidence-aware review.`,
    evidenceLinks: board.evidenceLinks
  });
}
function runReviewLoop(root, args = {}) {
  assertGovernanceMutationRegistered("run-review-loop", "guarded");
  assertNoPublicReviewBypassControls(args);
  const target = assertTaskScopedMutationTarget(root, "run-review-loop", args);
  assertFollowThroughReady(root, "Running the review pass", args);
  const reviewScope = assertReviewMaterials(buildReviewScope(root, target, args), "run_review_loop");
  transitionToLocalReviewer(root, args, target);
  const entry = persistReviewLoop(root, args, {}, reviewScope);
  const proofOnly = isProofOnlyReview(entry);
  const preActionGuidance = buildPreActionGuidance({
    surface: "dove.review",
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: args.scope ?? args.stage ?? "current paper pipeline",
    roleId: "reviewer",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: args.stage ?? target.packet?.stage ?? "audit",
      primaryRole: "reviewer"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: entry.verdict === "coherent" ? "project:dove.status" : proofOnly ? "project:dove.review" : "project:dove.rebuttal",
    routeHint: "project:dove.review",
    workflowKind: "review",
    domain: target.packet?.domain ?? null,
    stage: args.stage ?? target.packet?.stage ?? "audit",
    tags: ["review", "independent-audit", "evidence"],
    statusSummary: {
      verdict: entry.verdict,
      findingCount: entry.findings?.length ?? 0,
      actionItemCount: entry.actionItems?.length ?? 0
    }
  });
  return {
    ...entry,
    reviewStatePath: ARTIFACT_PATHS.reviewState,
    reviewLogPath: ARTIFACT_PATHS.reviewLog,
    reviewConcernsPath: ARTIFACT_PATHS.reviewConcerns,
    revisionPlanPath: ARTIFACT_PATHS.revisionPlan,
    preActionGuidance,
    resultCard: reviewResultCard(root, args, entry, "run_review_loop")
  };
}
function persistReviewLoop(root, args = {}, runtimeContext = {}, reviewScope = null) {
  const scope = reviewScope ?? assertReviewMaterials(buildReviewScope(root, assertTaskScopedMutationTarget(root, "run-review-loop", args), args), "run_review_loop");
  const evidence = evaluateEvidence(root);
  const claimIds = new Set(scope.claimIds);
  const experimentIds = new Set(scope.experimentIds);
  const resultIds = new Set(scope.resultIds);
  const auditIds = new Set(scope.auditIds);
  const reviewedPaths = new Set(scope.substantiveArtifactPaths);
  const inScopeClaim = (claim) => claimIds.has(claim?.id);
  const inScopeDraft = (item) => {
    const fileName = item?.draftFile ?? (item?.claim?.sectionId ? `${item.claim.sectionId}.md` : null);
    return Boolean(fileName) && reviewedPaths.has(`${ARTIFACT_PATHS.draftsDir}/${fileName}`);
  };
  const findings = [];
  for (const claim of evidence.unsupportedClaims.filter(inScopeClaim)) findings.push({ severity: "high", summary: `Claim ${claim.id} has no source support.`, claimIds: [claim.id], responseOwnerRole: "researcher", methodologicalCategory: "evidence" });
  for (const claim of evidence.weakClaims.filter(inScopeClaim)) findings.push({ severity: "medium", summary: `Claim ${claim.id} is weakly supported and should be strengthened.`, claimIds: [claim.id], responseOwnerRole: "researcher", methodologicalCategory: "evidence" });
  for (const item of evidence.missingSourceRefs.filter((item2) => inScopeClaim(item2.claim))) findings.push({ severity: "high", summary: `Claim ${item.claim.id} references missing sources: ${item.missing.join(", ")}.`, claimIds: [item.claim.id], responseOwnerRole: "researcher", methodologicalCategory: "evidence" });
  for (const item of evidence.missingNoteRefs.filter((item2) => inScopeClaim(item2.claim))) findings.push({ severity: "medium", summary: `Claim ${item.claim.id} references missing notes: ${item.missing.join(", ")}.`, claimIds: [item.claim.id], responseOwnerRole: "researcher", methodologicalCategory: "evidence" });
  for (const item of evidence.missingCitationRefs.filter(inScopeDraft)) findings.push({ severity: "high", summary: `${item.draftFile} cites unknown source key ${item.key}.`, responseOwnerRole: "researcher", methodologicalCategory: "citation" });
  const experimentResults = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  for (const result of (experimentResults.items ?? []).filter((item) => resultIds.has(item.id) || experimentIds.has(item.experimentId))) {
    if (["failed", "refutes"].includes(result.outcome)) findings.push({ severity: "high", summary: `Experiment ${result.experimentId} returned ${result.outcome} and needs claim/rebuttal follow-up.`, experimentIds: [result.experimentId], claimIds: result.claimId ? [result.claimId] : [], responseOwnerRole: "experiment-planner", methodologicalCategory: "integrity", linkedAuditIds: result.latestAuditId ? [result.latestAuditId] : [], linkedBridgeIds: result.latestBridgeId ? [result.latestBridgeId] : [] });
    if (result.outcome === "inconclusive") findings.push({ severity: "medium", summary: `Experiment ${result.experimentId} is inconclusive and weakens claim confidence.`, experimentIds: [result.experimentId], claimIds: result.claimId ? [result.claimId] : [], responseOwnerRole: "experiment-planner", methodologicalCategory: "integrity", linkedAuditIds: result.latestAuditId ? [result.latestAuditId] : [], linkedBridgeIds: result.latestBridgeId ? [result.latestBridgeId] : [] });
  }
  for (const audit of evidence.auditIntegrityFlags.filter((item) => auditIds.has(item.id) || experimentIds.has(item.experimentId))) findings.push({ severity: "high", summary: `Experiment audit ${audit.id} raised integrity flags: ${audit.integrityFlags.join(", ")}.`, experimentIds: audit.experimentId ? [audit.experimentId] : [], claimIds: audit.claimId ? [audit.claimId] : [], responseOwnerRole: "experiment-planner", methodologicalCategory: "integrity", linkedAuditIds: [audit.id], linkedArtifactPaths: [ARTIFACT_PATHS.experimentAudits] });
  for (const bridgeProblem of evidence.claimBridgeProblems.filter((item) => inScopeClaim(item.claim))) findings.push({ severity: "high", summary: `Claim ${bridgeProblem.claim.id} has a result-to-claim bridge problem (${bridgeProblem.reason}).`, claimIds: [bridgeProblem.claim.id], experimentIds: bridgeProblem.claim.experimentIds ?? [], responseOwnerRole: "experiment-planner", methodologicalCategory: "integrity", linkedBridgeIds: bridgeProblem.bridgeId ? [bridgeProblem.bridgeId] : [], linkedAuditIds: bridgeProblem.auditIds ?? [], linkedArtifactPaths: [ARTIFACT_PATHS.claimBridgeLog] });
  for (const item of evidence.draftClaimMismatches.filter((candidate) => inScopeClaim(candidate.claim))) findings.push({ severity: "medium", summary: item.reason === "missing-draft" ? `Claim ${item.claim.id} targets section ${item.claim.sectionId} but no draft exists for that section.` : `Draft for ${item.claim.sectionId} does not cite any expected source for claim ${item.claim.id}.`, claimIds: [item.claim.id], responseOwnerRole: "researcher", methodologicalCategory: item.reason === "missing-draft" ? "draft" : "citation" });
  for (const todo of evidence.citationTodos.filter(inScopeDraft)) findings.push({ severity: "medium", summary: `${todo.draftFile}:${todo.line} still has a citation TODO.`, responseOwnerRole: "researcher", methodologicalCategory: "citation" });
  const figureIds = new Set(scope.figureIds ?? []);
  const figureQa = evaluateFigurePipeline(root);
  for (const issue of (figureQa.issues ?? []).filter((item) => figureIds.has(item.figureId) || (item.artifactPaths ?? []).some((artifactPath) => reviewedPaths.has(artifactPath)) || (item.claimIds ?? []).some((id) => claimIds.has(id)) || (item.experimentIds ?? []).some((id) => experimentIds.has(id)))) {
    findings.push({ severity: issue.severity, summary: issue.summary, claimIds: issue.claimIds, experimentIds: issue.experimentIds, responseOwnerRole: issue.responseOwnerRole, methodologicalCategory: "figure", linkedArtifactPaths: issue.artifactPaths });
  }
  const scopedFindings = findings.map((finding) => ({ ...finding, packetId: scope.packetId, includedPacketIds: scope.includedPacketIds, reviewedArtifactPaths: scope.substantiveArtifactPaths }));
  const independentProof = findCurrentIndependentReviewProof(root, {
    packetId: scope.packetId,
    reviewedArtifactPaths: scope.substantiveArtifactPaths
  });
  if (scopedFindings.length === 0 && !independentProof.ok) {
    scopedFindings.push({
      packetId: scope.packetId,
      includedPacketIds: scope.includedPacketIds,
      reviewedArtifactPaths: scope.substantiveArtifactPaths,
      severity: "high",
      summary: "Current reviewed artifacts lack current-hash-bound independent isolated Reviewer proof.",
      claimIds: [],
      experimentIds: [],
      responseOwnerRole: "reviewer",
      methodologicalCategory: "review-proof",
      linkedArtifactPaths: scope.substantiveArtifactPaths
    });
  }
  const proofOnly = scopedFindings.length > 0 && scopedFindings.every((finding) => finding.methodologicalCategory === "review-proof");
  const actionItems = scopedFindings.map((finding) => finding.summary);
  const verdict = scopedFindings.some((finding) => finding.severity === "high") ? "needs-evidence" : scopedFindings.length > 0 ? "needs-revision" : "coherent";
  const entry = persistReviewLog(root, {
    ...args,
    packetId: scope.packetId,
    includedPacketIds: scope.includedPacketIds,
    stage: args.stage ?? "review-pass",
    scope: args.scope ?? `packet ${scope.packetId} and descendants`,
    verdict,
    summary: verdict === "coherent" ? "The selected packet scope is internally consistent and backed by current isolated Reviewer proof." : independentProof.ok ? "The selected packet scope requires explicit Builder follow-up before another review pass." : "The selected packet scope reached the Reviewer-owned proof boundary; current isolated Reviewer proof is required before coherence.",
    findings: scopedFindings,
    actionItems,
    reviewedArtifactPaths: scope.substantiveArtifactPaths,
    autoGeneratedReviewReport: true,
    reviewRequiredBeforeFinalize: true
  }, runtimeContext);
  entry.packetId = scope.packetId;
  entry.includedPacketIds = scope.includedPacketIds;
  entry.reviewedArtifactSet = scope.substantiveArtifactPaths;
  entry.independentReviewProof = independentProof.ok ? independentProof.proof : null;
  entry.preflightOnly = !independentProof.ok;
  persistReviewRebuttalIssues(root, entry, scopedFindings, { reviewer: args.reviewer ?? "review-pass", authorizationProvenance: runtimeContext.authorizationProvenance ?? null, executionClaimId: runtimeContext.executionClaimId ?? null });
  upsertConcernLedger(root, [], { auditIds: scope.auditIds, bridgeIds: [] });
  return entry;
}

// src/core/isolated-review.mjs
import crypto11 from "node:crypto";
import fs20 from "node:fs";
import path23 from "node:path";
var REVIEW_VERDICTS2 = /* @__PURE__ */ new Set(["coherent", "needs-revision", "needs-evidence", "blocked"]);
var HANDOFF_STATUSES = /* @__PURE__ */ new Set(["completed", "blocked", "failed"]);
function slugify9(value) {
  return String(value).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "isolated-review";
}
function normalizeStringArray19(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return value.filter((item) => typeof item === "string").map((item) => item.trim()).filter(Boolean);
}
function isolatedGuidanceSummary(root, args = {}, target = {}, details = {}) {
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: "dove.review",
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: args.instructions ?? args.scope ?? "isolated review",
    roleId: "reviewer",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "audit",
      primaryRole: "reviewer"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: details.nextAction ?? "project:dove.review",
    routeHint: "project:dove.review",
    workflowKind: "isolated-review",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "audit",
    tags: ["review", "isolated-handoff", "independent-audit"],
    statusSummary: details.statusSummary
  }));
}
function stableJson2(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function sha256Text(value) {
  return crypto11.createHash("sha256").update(value).digest("hex");
}
function hashFile(fullPath) {
  if (!fs20.existsSync(fullPath)) {
    return null;
  }
  return crypto11.createHash("sha256").update(fs20.readFileSync(fullPath)).digest("hex");
}
function relativeRunPath(runId, leaf) {
  return path23.posix.join(ARTIFACT_PATHS.isolatedReviewsDir, runId, leaf);
}
function normalizeRunId(value) {
  const candidate = slugify9(value ?? `review-${(/* @__PURE__ */ new Date()).toISOString().replace(/[:.]/g, "-")}`);
  if (!/^[a-z0-9][a-z0-9-]{0,79}$/.test(candidate)) {
    throw new Error("isolated review runId must be 1-80 lowercase letters, digits, or hyphens");
  }
  return candidate;
}
function safeArtifactPath(root, relativePath) {
  const normalized = path23.posix.normalize(String(relativePath).replaceAll(path23.sep, "/"));
  if (normalized.startsWith("../") || normalized === ".." || path23.isAbsolute(normalized)) {
    throw new Error(`Refusing unsafe isolated review artifact path: ${relativePath}`);
  }
  const fullPath = path23.resolve(root, normalized);
  const rootPath = path23.resolve(root);
  if (fullPath !== rootPath && !fullPath.startsWith(`${rootPath}${path23.sep}`)) {
    throw new Error(`Refusing isolated review artifact outside workspace: ${relativePath}`);
  }
  return normalized;
}
function artifactEntry(root, relativePath) {
  const safePath = safeArtifactPath(root, relativePath);
  const inspection = inspectDeclaredPath(root, safePath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  return {
    path: safePath,
    exists: inspection.exists,
    usable: inspection.status === "existing",
    status: inspection.status,
    reason: inspection.reason ?? null,
    sizeBytes: inspection.sizeBytes ?? null,
    sha256: inspection.status === "existing" ? hashFile(resolvePath(root, safePath)) : null
  };
}
function assertSubstantiveArtifactEntries(artifacts, label) {
  const usable = artifacts.filter((item) => item.usable);
  if (usable.length === 0) {
    const reasons = artifacts.map((item) => `${item.path}:${item.reason ?? item.status}`).join(", ") || "no artifact paths provided";
    throw new Error(`${label} requires at least one existing non-empty non-bookkeeping reviewed artifact (${reasons}).`);
  }
  return usable;
}
function normalizeFinding(finding = {}, index = 0) {
  const summary = typeof finding.summary === "string" && finding.summary.trim() ? finding.summary.trim() : `Isolated review finding ${index + 1}`;
  const severity = ["low", "medium", "high"].includes(finding.severity) ? finding.severity : "medium";
  return {
    id: slugify9(finding.id ?? `${severity}-${summary}`),
    severity,
    summary,
    responseOwnerRole: typeof finding.responseOwnerRole === "string" && finding.responseOwnerRole.trim() ? finding.responseOwnerRole.trim() : "planner",
    claimIds: normalizeStringArray19(finding.claimIds),
    experimentIds: normalizeStringArray19(finding.experimentIds),
    linkedArtifactPaths: normalizeStringArray19(finding.linkedArtifactPaths)
  };
}
function requiredStringField(raw, field, label = field) {
  if (typeof raw[field] !== "string" || !raw[field].trim()) {
    throw new Error(`isolated review handoff requires ${label}`);
  }
  return raw[field].trim();
}
function normalizeHandoff(root, raw = {}) {
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    throw new Error("isolated review handoff must be a JSON object");
  }
  const runId = normalizeRunId(requiredStringField(raw, "runId"));
  const verdict = requiredStringField(raw, "verdict");
  if (!REVIEW_VERDICTS2.has(verdict)) {
    throw new Error(`isolated review handoff has unsupported verdict: ${verdict}`);
  }
  const status = requiredStringField(raw, "status");
  if (!HANDOFF_STATUSES.has(status)) {
    throw new Error(`isolated review handoff has unsupported status: ${status}`);
  }
  if (status !== "completed" && verdict === "coherent") {
    throw new Error(`isolated review handoff status ${status} cannot import a coherent verdict.`);
  }
  const findings = Array.isArray(raw.findings) ? raw.findings.map(normalizeFinding) : [];
  const actionItems = normalizeStringArray19(raw.actionItems ?? findings.map((finding) => finding.summary));
  const reviewedArtifactPaths = normalizeStringArray19(raw.reviewedArtifactPaths ?? raw.artifactPaths).map((item) => safeArtifactPath(root, item));
  return {
    version: 1,
    runId,
    status,
    verdict,
    reviewerId: requiredStringField(raw, "reviewerId"),
    reviewerSessionId: typeof raw.reviewerSessionId === "string" && raw.reviewerSessionId.trim() ? raw.reviewerSessionId.trim() : null,
    timestamp: typeof raw.timestamp === "string" && raw.timestamp.trim() ? raw.timestamp.trim() : nowIso(),
    summary: requiredStringField(raw, "summary"),
    inputPath: safeArtifactPath(root, requiredStringField(raw, "inputPath")),
    inputSha256: requiredStringField(raw, "inputSha256"),
    reportPath: safeArtifactPath(root, requiredStringField(raw, "reportPath")),
    reviewedArtifactPaths,
    findings,
    actionItems,
    privateTranscriptImported: false
  };
}
function isolatedReviewVerificationFailure(runId, failures, manifest) {
  return {
    status: "verification-failed",
    boundaryType: "verification-failed",
    boundary: {
      type: "verification-failed",
      requiredActions: ["reprepare-review-artifacts", "rerun-isolated-review"],
      failures
    },
    requiredActions: ["reprepare-review-artifacts", "rerun-isolated-review"],
    runId,
    inputPath: manifest.inputPath,
    handoffPath: manifest.handoffPath,
    reportPath: manifest.reportPath,
    imported: false
  };
}
function renderImportedReviewLogEntry({ handoff, reportPath, reportSha256 }) {
  return [
    `## ${handoff.timestamp} \u2014 isolated-review`,
    "",
    `- Run: ${handoff.runId}`,
    `- Reviewer: ${handoff.reviewerId}${handoff.reviewerSessionId ? ` (${handoff.reviewerSessionId})` : ""}`,
    `- Status: ${handoff.status}`,
    `- Verdict: ${handoff.verdict}`,
    `- Summary: ${handoff.summary}`,
    `- Input: ${handoff.inputPath} (${handoff.inputSha256 ?? "missing-hash"})`,
    `- Report: ${reportPath ?? "none"}${reportSha256 ? ` (${reportSha256})` : ""}`,
    "- Findings:",
    ...handoff.findings.length > 0 ? handoff.findings.map((item) => `  - [${item.severity}] ${item.summary} (response owner: ${item.responseOwnerRole})`) : ["  - None recorded"],
    "- Action items:",
    ...handoff.actionItems.length > 0 ? handoff.actionItems.map((item) => `  - ${item}`) : ["  - None recorded"],
    ""
  ].join("\n");
}
function upsertImportedConcerns(root, handoff, packetId) {
  const current = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const existing = new Map((current.items ?? []).map((item) => [item.id, item]));
  for (const finding of handoff.findings) {
    const id = `isolated-${handoff.runId}-${finding.id}`;
    existing.set(id, {
      ...existing.get(id) ?? {},
      id,
      packetId,
      summary: finding.summary,
      severity: finding.severity,
      status: handoff.verdict === "coherent" ? "resolved" : "awaiting-author-response",
      raisedByRole: "reviewer",
      responseOwnerRole: finding.responseOwnerRole,
      reviewerRationale: finding.summary,
      authorRebuttalSummary: existing.get(id)?.authorRebuttalSummary ?? "",
      rulingOutcome: existing.get(id)?.rulingOutcome ?? "pending",
      reviewerDisposition: existing.get(id)?.reviewerDisposition ?? "pending",
      recurrenceCount: Number.isInteger(existing.get(id)?.recurrenceCount) ? existing.get(id).recurrenceCount + 1 : 1,
      firstSeenAt: existing.get(id)?.firstSeenAt ?? handoff.timestamp,
      lastSeenAt: handoff.timestamp,
      reviewRoundFirstSeen: existing.get(id)?.reviewRoundFirstSeen ?? 1,
      reviewRoundLastSeen: Number.isInteger(existing.get(id)?.reviewRoundLastSeen) ? existing.get(id).reviewRoundLastSeen + 1 : 1,
      escalationLevel: finding.severity === "high" ? 1 : 0,
      escalationThreshold: finding.severity === "high" ? 1 : finding.severity === "medium" ? 2 : 3,
      escalationReason: finding.severity === "high" ? "High-severity isolated reviewer finding." : "",
      linkedAuditIds: [],
      linkedBridgeIds: [],
      linkedArtifactPaths: Array.from(/* @__PURE__ */ new Set([relativeRunPath(handoff.runId, "handoff.json"), ...finding.linkedArtifactPaths ?? []])),
      claimIds: finding.claimIds,
      experimentIds: finding.experimentIds,
      updatedAt: nowIso()
    });
  }
  const items = Array.from(existing.values()).sort((left, right) => left.id.localeCompare(right.id));
  writeJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items, updatedAt: nowIso() });
}
function transitionToIsolatedReviewer(root, args, target, runId, reviewedArtifactPaths) {
  const board = loadBoard(root);
  if (board.currentPhase === "review" && board.assignedRole === "reviewer") {
    return board;
  }
  return upsertSystemOrchestrationBoard(root, {
    ...args,
    phase: "review",
    assignedRole: "reviewer",
    intentType: "review",
    currentFocus: args.scope ?? target.packet?.title ?? `Isolated review ${runId}`,
    nextAction: `Complete isolated review ${runId} and return only the declared handoff and report artifacts.`,
    handoffSummary: `Handing ${target.packet?.title ?? "the current work"} to the isolated reviewer for run ${runId}.`,
    evidenceLinks: Array.from(/* @__PURE__ */ new Set([...board.evidenceLinks ?? [], ...reviewedArtifactPaths])),
    reviewRequiredBeforeFinalize: true
  });
}
function isolatedReviewReturnTransition(handoff, independentProof = null) {
  if (handoff.verdict === "coherent") {
    if (independentProof?.authoritative !== true) {
      return {
        phase: "review",
        assignedRole: "reviewer",
        intentType: "review",
        nextAction: "Submit authoritative proof from an approved Reviewer runtime execution claim through the private capability.",
        proofBoundary: true
      };
    }
    return {
      phase: "plan",
      assignedRole: "planner",
      intentType: "plan",
      nextAction: "Inspect Dove status and choose the next governed finalization step."
    };
  }
  return {
    phase: "rebuttal",
    assignedRole: "builder",
    intentType: "respond",
    nextAction: handoff.actionItems[0] ?? "Address the isolated review findings through governed revision work."
  };
}
function returnImportedIsolatedReview(root, args, handoff, handoffPath, reportPath, transition) {
  const board = loadBoard(root);
  const evidenceLinks = Array.from(/* @__PURE__ */ new Set([...board.evidenceLinks ?? [], handoffPath, reportPath, ...handoff.reviewedArtifactPaths]));
  const handoffSummary = transition.proofBoundary ? `Isolated reviewer ${handoff.reviewerId} returned public coherent material for ${handoff.runId}, but authoritative Reviewer runtime proof is still required; Reviewer ownership is retained.` : `Isolated reviewer ${handoff.reviewerId} returned ${handoff.verdict} for ${handoff.runId}; workflow routing now returns to ${transition.assignedRole} for the recorded next action.`;
  if (transition.proofBoundary && board.currentPhase === "review" && board.assignedRole === "reviewer") {
    appendSystemHandoff(root, {
      fromRole: "reviewer",
      toRole: "reviewer",
      phase: "review",
      intentType: "review",
      summary: handoffSummary,
      currentFocus: handoff.summary,
      nextAction: transition.nextAction,
      evidenceLinks
    });
  }
  return upsertSystemOrchestrationBoard(root, {
    ...args,
    ...transition,
    currentFocus: handoff.summary,
    handoffSummary,
    evidenceLinks,
    reviewRequiredBeforeFinalize: true
  });
}
function prepareIsolatedReview(root, args = {}) {
  assertGovernanceMutationRegistered("prepare-isolated-review", "guarded");
  const target = assertTaskScopedMutationTarget(root, "prepare-isolated-review", args);
  assertFollowThroughReady(root, "Preparing an isolated reviewer input bundle", args);
  const runId = normalizeRunId(args.runId);
  const explicitArtifactPaths = normalizeStringArray19([
    ...normalizeStringArray19(args.reviewedArtifactPaths),
    ...normalizeStringArray19(args.artifactPaths)
  ]).map((item) => safeArtifactPath(root, item));
  if (explicitArtifactPaths.length > 0) {
    buildReviewScope(root, target, { reviewedArtifactPaths: explicitArtifactPaths });
  }
  const reviewScope = assertReviewMaterials(
    buildReviewScope(root, target),
    "prepare_isolated_review"
  );
  const reviewedArtifactPaths = reviewScope.substantiveArtifactPaths;
  const runDir = path23.posix.join(ARTIFACT_PATHS.isolatedReviewsDir, runId);
  const timestamp = nowIso();
  const state = loadState(root);
  const artifacts = reviewedArtifactPaths.map((relativePath) => artifactEntry(root, relativePath));
  const usableArtifacts = assertSubstantiveArtifactEntries(artifacts, "prepare_isolated_review");
  const snapshot = snapshotReviewedArtifacts(root, usableArtifacts.map((artifact) => artifact.path), "prepare_isolated_review");
  const reviewBoard = transitionToIsolatedReviewer(root, args, target, runId, snapshot.reviewedArtifacts.map((artifact) => artifact.path));
  const input = {
    version: 1,
    runId,
    packetId: reviewScope.packetId,
    includedPacketIds: reviewScope.includedPacketIds,
    createdAt: timestamp,
    isolationModel: "parallel-session-file-handoff",
    mediatorRole: args.mediatorRole ?? "editor",
    reviewerRole: args.reviewerRole ?? "reviewer",
    scope: args.scope ?? "current paper pipeline",
    instructions: args.instructions ?? "Review the submitted artifact bundle independently. Do not assume access to writer-session private context.",
    paper: {
      title: state.dove.title,
      venue: state.dove.venue,
      objective: state.dove.objective,
      thesis: state.dove.thesis,
      audience: state.dove.audience
    },
    board: {
      currentPhase: reviewBoard.currentPhase,
      assignedRole: reviewBoard.assignedRole,
      intentType: reviewBoard.intentType,
      currentFocus: reviewBoard.currentFocus,
      nextAction: reviewBoard.nextAction,
      reviewRequiredBeforeFinalize: reviewBoard.reviewRequiredBeforeFinalize
    },
    reviewedArtifactPaths: snapshot.reviewedArtifacts.map((artifact) => artifact.path),
    reviewedArtifacts: snapshot.reviewedArtifacts,
    reviewedArtifactSetSha256: snapshot.reviewedArtifactSetSha256,
    artifacts,
    outputContract: {
      handoffPath: relativeRunPath(runId, "handoff.json"),
      reportPath: relativeRunPath(runId, "report.md"),
      requiredHandoffFields: ["runId", "status", "verdict", "reviewerId", "summary", "inputPath", "inputSha256", "reportPath", "reviewedArtifactPaths", "findings", "actionItems"]
    },
    privacyBoundary: {
      writerPrivateTranscriptShared: false,
      reviewerPrivateTranscriptShouldReturn: false,
      acceptedExchangeArtifacts: [relativeRunPath(runId, "input.json"), relativeRunPath(runId, "clarifications.json"), relativeRunPath(runId, "handoff.json"), relativeRunPath(runId, "report.md")]
    }
  };
  const inputText = stableJson2(input);
  const inputSha256 = sha256Text(inputText);
  const manifest = {
    version: 1,
    runId,
    packetId: reviewScope.packetId,
    includedPacketIds: reviewScope.includedPacketIds,
    status: "prepared",
    createdAt: timestamp,
    updatedAt: timestamp,
    inputPath: relativeRunPath(runId, "input.json"),
    inputSha256,
    handoffPath: relativeRunPath(runId, "handoff.json"),
    reportPath: relativeRunPath(runId, "report.md"),
    reviewedArtifactPaths: snapshot.reviewedArtifacts.map((artifact) => artifact.path),
    reviewedArtifacts: snapshot.reviewedArtifacts,
    reviewedArtifactSetSha256: snapshot.reviewedArtifactSetSha256,
    importedAt: null,
    outputSha256: null,
    reportSha256: null
  };
  writeText(root, manifest.inputPath, inputText);
  writeJson(root, relativeRunPath(runId, "manifest.json"), manifest);
  writeJson(root, relativeRunPath(runId, "clarifications.json"), { version: 1, runId, items: [], updatedAt: timestamp });
  return {
    status: "prepared",
    runId,
    runDir,
    inputPath: manifest.inputPath,
    inputSha256,
    handoffPath: manifest.handoffPath,
    reportPath: manifest.reportPath,
    reviewedArtifactPaths: snapshot.reviewedArtifacts.map((artifact) => artifact.path),
    reviewedArtifacts: snapshot.reviewedArtifacts,
    reviewedArtifactSetSha256: snapshot.reviewedArtifactSetSha256,
    preActionGuidanceSummary: isolatedGuidanceSummary(root, args, target, {
      nextAction: "import_isolated_review",
      statusSummary: {
        status: "prepared",
        runId,
        reviewedArtifactCount: usableArtifacts.length
      }
    })
  };
}
function importIsolatedReview(root, args = {}) {
  assertGovernanceMutationRegistered("import-isolated-review", "guarded");
  const target = assertTaskScopedMutationTarget(root, "import-isolated-review", args);
  assertFollowThroughReady(root, "Importing an isolated reviewer handoff", args);
  const runId = normalizeRunId(args.runId);
  const manifestPath = relativeRunPath(runId, "manifest.json");
  const canonicalPaths = {
    inputPath: relativeRunPath(runId, "input.json"),
    handoffPath: relativeRunPath(runId, "handoff.json"),
    reportPath: relativeRunPath(runId, "report.md")
  };
  const handoffPath = args.handoffPath ? safeArtifactPath(root, args.handoffPath) : canonicalPaths.handoffPath;
  const reportPath = args.reportPath ? safeArtifactPath(root, args.reportPath) : canonicalPaths.reportPath;
  const manifest = readJson(root, manifestPath, null);
  if (!manifest || typeof manifest !== "object") {
    throw new Error(`Missing isolated review manifest for ${runId}`);
  }
  if (manifest.status !== "prepared") {
    throw new Error(`Isolated review ${runId} is not importable from manifest status ${manifest.status ?? "unknown"}; prepare a fresh review run.`);
  }
  if (manifest.packetId !== target.packetId) {
    throw new Error(`Isolated review ${runId} belongs to packet ${manifest.packetId ?? "unknown"}, not ${target.packetId}.`);
  }
  if (handoffPath !== canonicalPaths.handoffPath || reportPath !== canonicalPaths.reportPath) {
    return isolatedReviewVerificationFailure(runId, ["noncanonical-exchange-path"], manifest);
  }
  const handoffPathFailure = canonicalReviewExchangePathFailure(
    root,
    handoffPath,
    "handoff"
  );
  if (handoffPathFailure) {
    return isolatedReviewVerificationFailure(runId, [handoffPathFailure], manifest);
  }
  const handoffFullPath = resolvePath(root, handoffPath);
  const handoff = normalizeHandoff(root, JSON.parse(fs20.readFileSync(handoffFullPath, "utf8")));
  if (handoff.runId !== runId) {
    throw new Error(`Isolated review handoff runId mismatch: expected ${runId}, received ${handoff.runId}`);
  }
  const inputPathFailure = canonicalReviewExchangePathFailure(
    root,
    canonicalPaths.inputPath,
    "input"
  );
  if (inputPathFailure) {
    return isolatedReviewVerificationFailure(runId, [inputPathFailure], manifest);
  }
  const inputFullPath = resolvePath(root, canonicalPaths.inputPath);
  let input;
  let actualInputSha256;
  try {
    actualInputSha256 = sha256File(inputFullPath);
    input = JSON.parse(fs20.readFileSync(inputFullPath, "utf8"));
  } catch (error) {
    return isolatedReviewVerificationFailure(runId, [`input-unreadable:${error instanceof Error ? error.message : String(error)}`], manifest);
  }
  const verification = verifyPreparedReviewSnapshot(root, {
    manifest,
    input,
    inputPath: manifest.inputPath,
    actualInputSha256,
    handoffInputPath: handoff.inputPath,
    handoffInputSha256: handoff.inputSha256,
    handoffReviewedArtifactPaths: handoff.reviewedArtifactPaths
  });
  if (!verification.ok) {
    return isolatedReviewVerificationFailure(runId, verification.failures, manifest);
  }
  const canonicalScopeVerification = verifyCanonicalReviewImportScope(root, target, {
    runId,
    manifest,
    input,
    handoff,
    canonicalPaths,
    isolationModel: "parallel-session-file-handoff"
  });
  if (!canonicalScopeVerification.ok) {
    return isolatedReviewVerificationFailure(runId, canonicalScopeVerification.failures, manifest);
  }
  if (handoff.reportPath !== reportPath) {
    return isolatedReviewVerificationFailure(runId, ["report-path-mismatch"], manifest);
  }
  const reportPathFailure = canonicalReviewExchangePathFailure(
    root,
    reportPath,
    "report"
  );
  if (reportPathFailure) {
    return isolatedReviewVerificationFailure(runId, [reportPathFailure], manifest);
  }
  const reportInspection = inspectDeclaredPath(root, reportPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  if (reportInspection.status !== "existing") {
    throw new Error(`Isolated review report is not a usable non-empty file at ${reportPath}: ${reportInspection.reason ?? reportInspection.status}`);
  }
  const handoffSha256 = hashFile(handoffFullPath);
  const reportFullPath = resolvePath(root, reportPath);
  const reportSha256 = hashFile(reportFullPath);
  const anticipatedTransition = isolatedReviewReturnTransition(handoff);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    anticipatedTransition.phase,
    anticipatedTransition.assignedRole,
    "Importing an isolated review"
  );
  assertSystemOrchestrationBoardUpdate(root, {
    ...args,
    ...anticipatedTransition,
    currentFocus: handoff.summary,
    reviewRequiredBeforeFinalize: true
  });
  appendText(root, ARTIFACT_PATHS.reviewLog, renderImportedReviewLogEntry({ handoff, reportPath, reportSha256 }));
  upsertImportedConcerns(root, handoff, target.packetId);
  const updatedManifest = {
    version: 1,
    runId,
    packetId: target.packetId,
    includedPacketIds: canonicalScopeVerification.scope.includedPacketIds,
    status: "imported",
    createdAt: manifest.createdAt,
    updatedAt: nowIso(),
    importedAt: nowIso(),
    inputPath: canonicalPaths.inputPath,
    inputSha256: actualInputSha256,
    handoffPath,
    reportPath,
    reviewedArtifactPaths: canonicalScopeVerification.scope.substantiveArtifactPaths,
    reviewedArtifacts: verification.reviewedArtifacts,
    reviewedArtifactSetSha256: verification.reviewedArtifactSetSha256,
    outputSha256: handoffSha256,
    reportSha256,
    verdict: handoff.verdict,
    importedVerdict: handoff.verdict,
    reviewerId: handoff.reviewerId,
    authoritative: false,
    independentReviewProof: null,
    reviewProofRequired: handoff.verdict === "coherent"
  };
  writeJson(root, manifestPath, updatedManifest);
  const independentProof = handoff.verdict === "coherent" ? verifyImportedIsolatedReviewProof(root, resolvePath(root, manifestPath), {
    packetId: target.packetId,
    reviewedArtifactPaths: canonicalScopeVerification.scope.substantiveArtifactPaths
  }) : { ok: false, authoritative: false, proof: null, failures: [] };
  const returnTransition = isolatedReviewReturnTransition(handoff, independentProof);
  if (returnTransition.proofBoundary) {
    recordReviewProofRequiredBoundary(root, {
      reviewKind: "isolated",
      runId,
      packetId: target.packetId,
      includedPacketIds: canonicalScopeVerification.scope.includedPacketIds,
      timestamp: handoff.timestamp,
      handoffPath,
      reportPath,
      reviewedArtifactPaths: canonicalScopeVerification.scope.substantiveArtifactPaths
    });
  }
  returnImportedIsolatedReview(root, args, handoff, handoffPath, reportPath, returnTransition);
  refreshDurableSurfaces(root, {
    type: "isolated-review-import",
    summary: `Imported isolated review ${runId} with verdict ${handoff.verdict}.`,
    artifactPaths: [ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewConcerns, ARTIFACT_PATHS.orchestrationHandoffs, reportPath, ARTIFACT_PATHS.workspaceIndex, ARTIFACT_PATHS.sessionSummary]
  });
  return {
    status: "imported",
    runId,
    verdict: returnTransition.proofBoundary ? "needs-evidence" : handoff.verdict,
    importedVerdict: handoff.verdict,
    reviewerId: handoff.reviewerId,
    summary: handoff.summary,
    topConcerns: handoff.findings.slice(0, 5).map((finding) => finding.summary),
    actionItems: handoff.actionItems,
    handoffPath,
    reportPath,
    inputPath: manifest.inputPath,
    inputSha256: manifest.inputSha256,
    handoffSha256,
    reportSha256,
    authoritative: independentProof.authoritative === true,
    independentReviewProof: null,
    reviewProofRequired: returnTransition.proofBoundary === true,
    proofFailures: independentProof.failures ?? [],
    ...returnTransition.proofBoundary ? {
      boundary: {
        type: "review-proof-required",
        ownerRole: "reviewer",
        nextRole: "reviewer",
        nextAction: returnTransition.nextAction,
        requiredActions: ["submit-authoritative-reviewer-runtime-proof"]
      },
      ownerRole: "reviewer",
      nextRole: "reviewer",
      nextAction: returnTransition.nextAction
    } : {},
    privateTranscriptImported: false,
    preActionGuidanceSummary: isolatedGuidanceSummary(root, args, target, {
      nextAction: returnTransition.proofBoundary ? "project:dove.review" : handoff.verdict === "coherent" ? "project:dove.status" : "project:dove.rebuttal",
      statusSummary: {
        status: "imported",
        runId,
        verdict: returnTransition.proofBoundary ? "needs-evidence" : handoff.verdict,
        importedVerdict: handoff.verdict,
        authoritative: independentProof.authoritative === true,
        reviewProofRequired: returnTransition.proofBoundary === true,
        findingCount: handoff.findings.length,
        actionItemCount: handoff.actionItems.length
      }
    })
  };
}

// src/core/figure-generation.mjs
import { spawnSync as spawnSync2 } from "node:child_process";
import crypto12 from "node:crypto";
import fs21 from "node:fs";
import path24 from "node:path";
function slugify10(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function normalizeStringArray20(value, fallback = []) {
  const source = Array.isArray(value) ? value : fallback;
  return Array.from(new Set(source.filter((item) => typeof item === "string" && item.trim()).map((item) => item.trim())));
}
function normalizePlainObject2(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
var FIGURE_MATERIAL_INPUT_KEYS = /* @__PURE__ */ new Set([
  "id",
  "type",
  "label",
  "summary",
  "artifactPath",
  "path"
]);
var FIGURE_SEMANTIC_COVERAGE_INPUT_KEYS = /* @__PURE__ */ new Set([
  "observations",
  "evidencePaths",
  "artifactPaths"
]);
var FIGURE_SEMANTIC_REVIEW_INPUT_KEYS = /* @__PURE__ */ new Set([
  "summary",
  "observations",
  "evidencePaths",
  "artifactPaths"
]);
var FIGURE_MANIFEST_SUCCESS_ASSERTION_FIELDS = /* @__PURE__ */ new Set([
  "semanticCoverage",
  "semanticReview",
  "visualElements",
  "coveredVisualElements",
  "passed",
  "reviewed",
  "humanReviewed",
  "approved",
  "status",
  "verdict",
  "reviewStatus"
]);
function assertFigurePlainObject(value, label, surface) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${surface} requires an object at ${label}.`);
  }
}
function assertFigureObjectKeys(value, allowedKeys, label, surface) {
  assertFigurePlainObject(value, label, surface);
  const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
  if (unknown.length > 0) {
    throw new Error(`${surface} does not accept unknown input ${unknown.map((key) => `${label}.${key}`).join(", ")}.`);
  }
}
function assertFigureString(value, label, surface) {
  if (typeof value !== "string") {
    throw new Error(`${surface} requires a string at ${label}.`);
  }
}
function assertFigureStringArray(value, label, surface) {
  if (!Array.isArray(value)) {
    throw new Error(`${surface} requires an array at ${label}.`);
  }
  value.forEach((item, index) => assertFigureString(item, `${label}[${index}]`, surface));
}
function assertFigureMaterialItem(value, label, surface) {
  assertFigureObjectKeys(value, FIGURE_MATERIAL_INPUT_KEYS, label, surface);
  for (const key of FIGURE_MATERIAL_INPUT_KEYS) {
    if (Object.hasOwn(value, key)) {
      assertFigureString(value[key], `${label}.${key}`, surface);
    }
  }
}
function assertFigureSemanticObservation(value, allowedKeys, label, surface) {
  assertFigureObjectKeys(value, allowedKeys, label, surface);
  if (Object.hasOwn(value, "summary")) {
    assertFigureString(value.summary, `${label}.summary`, surface);
  }
  for (const key of ["observations", "evidencePaths", "artifactPaths"]) {
    if (Object.hasOwn(value, key)) {
      assertFigureStringArray(value[key], `${label}.${key}`, surface);
    }
  }
}
function assertPrepareFigureGenerationInput(args = {}) {
  const surface = "prepare_figure_generation";
  if (Object.hasOwn(args, "materialHints")) {
    if (!Array.isArray(args.materialHints)) {
      throw new Error(`${surface} requires an array at $.materialHints.`);
    }
    args.materialHints.forEach((item, index) => {
      if (typeof item !== "string") {
        assertFigureMaterialItem(item, `$.materialHints[${index}]`, surface);
      }
    });
  }
}
function assertImportFigureGenerationInput(args = {}) {
  const surface = "import_figure_generation";
  if (Object.hasOwn(args, "semanticCoverage")) {
    assertFigureSemanticObservation(
      args.semanticCoverage,
      FIGURE_SEMANTIC_COVERAGE_INPUT_KEYS,
      "$.semanticCoverage",
      surface
    );
  }
  if (Object.hasOwn(args, "semanticReview")) {
    assertFigureSemanticObservation(
      args.semanticReview,
      FIGURE_SEMANTIC_REVIEW_INPUT_KEYS,
      "$.semanticReview",
      surface
    );
  }
}
function assertManifestDoesNotClaimSemanticSuccess(manifest, label) {
  const supplied = Object.keys(manifest ?? {}).filter((key) => FIGURE_MANIFEST_SUCCESS_ASSERTION_FIELDS.has(key));
  if (supplied.length > 0) {
    throw new Error(`${label} cannot set caller-controlled semantic success input: ${supplied.join(", ")}.`);
  }
}
function normalizeSemanticCoverage(value) {
  const source = normalizePlainObject2(value);
  if (!source) {
    return null;
  }
  return {
    observations: normalizeStringArray20(source.observations),
    evidencePaths: normalizeStringArray20(source.evidencePaths),
    artifactPaths: normalizeStringArray20(source.artifactPaths)
  };
}
function normalizeSemanticReview(value) {
  const source = normalizePlainObject2(value);
  if (!source) {
    return null;
  }
  return {
    ...typeof source.summary === "string" ? { summary: source.summary.trim() } : {},
    observations: normalizeStringArray20(source.observations),
    evidencePaths: normalizeStringArray20(source.evidencePaths),
    artifactPaths: normalizeStringArray20(source.artifactPaths)
  };
}
function figureGenerationGuidanceSummary(root, args = {}, target = {}, details = {}) {
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: "dove.figure",
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: args.intent ?? args.description ?? args.caption ?? args.captionDraft ?? args.figureId ?? args.id ?? null,
    roleId: "builder",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "execute",
      primaryRole: "builder"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: details.nextAction ?? "project:dove.figure",
    routeHint: details.nextAction ?? "project:dove.figure",
    workflowKind: "figure",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "execute",
    tags: ["figure", "artifact-provenance", "qa"],
    statusSummary: details.statusSummary
  }));
}
function normalizeRelativePath5(value, fallback = null) {
  const source = typeof value === "string" && value.trim().length > 0 ? value.trim() : fallback;
  if (!source) {
    return null;
  }
  return path24.posix.normalize(String(source).replace(/\\/g, "/").replace(/^\.\//, ""));
}
function isSafeProjectRelativePath3(relativePath) {
  if (!relativePath || path24.posix.isAbsolute(relativePath)) {
    return false;
  }
  const normalized = path24.posix.normalize(relativePath);
  return normalized !== ".." && !normalized.startsWith("../");
}
function assertSafeFigurePath(relativePath, label, allowedPrefixes = [".dove/figures/"]) {
  const normalized = normalizeRelativePath5(relativePath);
  if (!normalized || !isSafeProjectRelativePath3(normalized) || !allowedPrefixes.some((prefix) => normalized.startsWith(prefix))) {
    throw new Error(`${label} must stay under ${allowedPrefixes.join(" or ")}: ${relativePath ?? "<missing>"}`);
  }
  return normalized;
}
function assertNoLegacyFinalSvgInput(value, label) {
  if (value && typeof value === "object" && Object.prototype.hasOwnProperty.call(value, "finalSvgPath")) {
    throw new Error(`${label} no longer accepts finalSvgPath as input; use sourceSvgPath for generated SVG input or targetFinalSvgPath for the final artifact target.`);
  }
}
function assertNoLegacyProviderOutputFields(value, label) {
  for (const field of ["finalSvgPath", "finalSvgContent", "svg"]) {
    if (value && typeof value === "object" && Object.prototype.hasOwnProperty.call(value, field)) {
      throw new Error(`${label} must use sourceSvgPath or svgContent; ${field} is not accepted.`);
    }
  }
}
function assertSafeFigureSourcePath(relativePath, label, runId) {
  return assertSafeFigurePath(relativePath, label, [`${generationRunDir(runId)}/`]);
}
function assertSafeFigureFinalTargetPath(relativePath, label) {
  const normalized = assertSafeFigurePath(relativePath, label);
  if (normalized.startsWith(".dove/figures/runs/")) {
    throw new Error(`${label} must be a final figure artifact outside .dove/figures/runs/: ${relativePath ?? "<missing>"}`);
  }
  return normalized;
}
function generationRunDir(runId) {
  return `.dove/figures/runs/${runId}`;
}
function defaultRunId(figureId) {
  return slugify10(`${figureId}-${(/* @__PURE__ */ new Date()).toISOString().replace(/[^0-9]/g, "").slice(0, 14)}`);
}
function readFigures(root) {
  return readJson(root, ARTIFACT_PATHS.figuresIndex, { version: 1, items: [], updatedAt: null });
}
function resolveFigure(root, args = {}) {
  const figures = readFigures(root);
  const rawFigureId = args.figureId ?? args.id;
  const figureId = rawFigureId ? slugify10(rawFigureId) : null;
  if (!figureId && (figures.items ?? []).length !== 1) {
    throw new Error("Figure generation requires figureId when the figure backlog does not contain exactly one item.");
  }
  const figure = figureId ? (figures.items ?? []).find((item) => item.id === figureId) : (figures.items ?? [])[0];
  if (!figure) {
    throw new Error(`Figure generation target not found in ${ARTIFACT_PATHS.figuresIndex}: ${figureId}`);
  }
  return { figures, figure };
}
function findById(items = [], id) {
  return items.find((item) => item.id === id || item.citationKey === id || item.experimentId === id);
}
function materialRequirement({ type, refId, label, status, artifactPath = null, summary = "", source = "dove", evidence = {} }) {
  return {
    id: slugify10(`${type}-${refId ?? label}`),
    type,
    refId: refId ?? null,
    label: label ?? refId ?? type,
    status,
    artifactPath,
    summary,
    source,
    evidence
  };
}
function materialInspection(root, artifactPath) {
  const normalized = normalizeRelativePath5(artifactPath);
  if (!normalized) {
    return { normalized, inspection: null, available: false, reason: "missing artifact path" };
  }
  const inspection = inspectDeclaredPath(root, normalized, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  return {
    normalized,
    inspection,
    available: inspection.status === "existing",
    reason: inspection.reason ?? inspection.status
  };
}
function sourceArtifactRequirement(root, artifactPath, index) {
  const inspected = materialInspection(root, artifactPath);
  return materialRequirement({
    type: "source-artifact",
    refId: inspected.normalized ?? `source-artifact-${index + 1}`,
    label: inspected.normalized ?? `Source artifact ${index + 1}`,
    status: inspected.available ? "available" : "missing",
    artifactPath: inspected.normalized,
    summary: inspected.available ? "Source artifact is present and non-empty." : `Source artifact is not usable: ${inspected.reason}.`,
    evidence: { inspection: inspected.inspection }
  });
}
function buildMaterialRequirements(root, figure, args = {}) {
  const state = readJson(root, ARTIFACT_PATHS.state, { sections: {} });
  const sources = readJson(root, ARTIFACT_PATHS.sources, { version: 1, items: [], updatedAt: null });
  const notes = readJson(root, ARTIFACT_PATHS.notes, { version: 1, items: [], updatedAt: null });
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const experimentPlans = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const experimentResults = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  const reviewConcerns = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const rebuttalIssues = readJson(root, ARTIFACT_PATHS.rebuttalIssues, { version: 1, items: [], updatedAt: null });
  const requirements = [];
  const linkedClaimIds = normalizeStringArray20(figure.targetClaimIds);
  const linkedClaims = linkedClaimIds.map((claimId) => findById(evidence.claims ?? [], claimId));
  const sourceIds = normalizeStringArray20(linkedClaims.flatMap((claim) => claim?.sourceIds ?? []));
  const noteIds = normalizeStringArray20(linkedClaims.flatMap((claim) => claim?.noteIds ?? []));
  const experimentIds = normalizeStringArray20([...figure.relatedExperimentIds ?? [], ...linkedClaims.flatMap((claim) => claim?.experimentIds ?? [])]);
  for (const sectionId of normalizeStringArray20(figure.sourceSections)) {
    const section = state.sections?.[sectionId];
    requirements.push(materialRequirement({
      type: "section",
      refId: sectionId,
      label: section?.title ?? sectionId,
      status: section ? "available" : "missing",
      artifactPath: section?.draftPath ?? null,
      summary: section?.summary ?? (section ? "Section metadata is available." : "Linked source section is missing from state."),
      evidence: { sectionStatus: section?.status ?? null }
    }));
  }
  for (const [index, artifactPath] of normalizeStringArray20(figure.sourceArtifactPaths).entries()) {
    requirements.push(sourceArtifactRequirement(root, artifactPath, index));
  }
  for (const claimId of linkedClaimIds) {
    const claim = findById(evidence.claims ?? [], claimId);
    requirements.push(materialRequirement({
      type: "claim",
      refId: claimId,
      label: claim?.text ?? claimId,
      status: claim ? "available" : "missing",
      artifactPath: ARTIFACT_PATHS.evidence,
      summary: claim?.gap ?? claim?.status ?? (claim ? "Claim is available." : "Linked target claim is missing."),
      evidence: { sourceIds: claim?.sourceIds ?? [], noteIds: claim?.noteIds ?? [], experimentIds: claim?.experimentIds ?? [] }
    }));
  }
  for (const sourceId of sourceIds) {
    const source = findById(sources.items ?? [], sourceId);
    requirements.push(materialRequirement({
      type: "source",
      refId: sourceId,
      label: source?.title ?? sourceId,
      status: source ? "available" : "missing",
      artifactPath: ARTIFACT_PATHS.sources,
      summary: source?.abstract ?? (source ? "Source metadata is available." : "Claim-linked source is missing."),
      evidence: { citationKey: source?.citationKey ?? null, sourceType: source?.sourceType ?? null }
    }));
  }
  for (const noteId of noteIds) {
    const note = findById(notes.items ?? [], noteId);
    requirements.push(materialRequirement({
      type: "note",
      refId: noteId,
      label: note?.title ?? noteId,
      status: note ? "available" : "missing",
      artifactPath: ARTIFACT_PATHS.notes,
      summary: note?.summary ?? (note ? "Note is available." : "Claim-linked note is missing."),
      evidence: { sourceIds: note?.sourceIds ?? [], sectionId: note?.sectionId ?? null }
    }));
  }
  for (const experimentId of experimentIds) {
    const plan = findById(experimentPlans.items ?? [], experimentId);
    const results = (experimentResults.items ?? []).filter((result) => result.experimentId === experimentId || result.id === experimentId);
    requirements.push(materialRequirement({
      type: "experiment",
      refId: experimentId,
      label: plan?.title ?? experimentId,
      status: plan || results.length > 0 ? "available" : "missing",
      artifactPath: ARTIFACT_PATHS.experimentResults,
      summary: results[0]?.summary ?? plan?.methodology ?? (plan ? "Experiment plan is available." : "Linked experiment is missing."),
      evidence: { planId: plan?.id ?? null, resultIds: results.map((result) => result.id) }
    }));
  }
  for (const concernId of normalizeStringArray20(figure.reviewConcernIds)) {
    const concern = findById(reviewConcerns.items ?? [], concernId);
    requirements.push(materialRequirement({
      type: "review-concern",
      refId: concernId,
      label: concern?.summary ?? concernId,
      status: concern ? "available" : "missing",
      artifactPath: ARTIFACT_PATHS.reviewConcerns,
      summary: concern?.reviewerRationale ?? (concern ? "Review concern is available." : "Linked review concern is missing.")
    }));
  }
  for (const issueId of normalizeStringArray20(figure.rebuttalIssueIds)) {
    const issue = findById(rebuttalIssues.items ?? [], issueId);
    requirements.push(materialRequirement({
      type: "rebuttal-issue",
      refId: issueId,
      label: issue?.summary ?? issueId,
      status: issue ? "available" : "missing",
      artifactPath: ARTIFACT_PATHS.rebuttalIssues,
      summary: issue?.responseDirection ?? (issue ? "Rebuttal issue is available." : "Linked rebuttal issue is missing.")
    }));
  }
  for (const visualElement of normalizeStringArray20(figure.requiredVisualElements)) {
    requirements.push(materialRequirement({
      type: "visual-element",
      refId: visualElement,
      label: visualElement,
      status: "available",
      summary: "Visual element is declared as a drawing instruction in the figure plan.",
      source: "figure-plan"
    }));
  }
  for (const requirement of Array.isArray(figure.materialRequirements) ? figure.materialRequirements : []) {
    const inspected = materialInspection(root, requirement.artifactPath ?? requirement.path);
    requirements.push(materialRequirement({
      type: requirement.type ?? "operator-material",
      refId: requirement.id ?? requirement.label,
      label: requirement.label ?? requirement.id ?? "Operator material",
      status: inspected.normalized ? inspected.available ? "available" : "missing" : "needs-operator",
      artifactPath: inspected.normalized,
      summary: requirement.summary ?? (inspected.available ? "Operator-declared material artifact is present and non-empty." : `Operator-declared material is not usable: ${inspected.reason}.`),
      source: "figure-plan",
      evidence: { inspection: inspected.inspection }
    }));
  }
  const hints = Array.isArray(args.materialHints) ? args.materialHints : [];
  for (let index = 0; index < hints.length; index += 1) {
    const hint = hints[index];
    if (typeof hint === "string") {
      requirements.push(materialRequirement({ type: "operator-hint", refId: `hint-${index + 1}`, label: hint, status: "needs-operator", summary: hint, source: "operator" }));
    } else if (hint && typeof hint === "object") {
      const inspected = materialInspection(root, hint.artifactPath ?? hint.path);
      requirements.push(materialRequirement({
        type: hint.type ?? "operator-hint",
        refId: hint.id ?? `hint-${index + 1}`,
        label: hint.label ?? hint.summary ?? `Hint ${index + 1}`,
        status: inspected.normalized ? inspected.available ? "available" : "missing" : "needs-operator",
        artifactPath: inspected.normalized,
        summary: hint.summary ?? hint.label ?? (inspected.available ? "Operator-provided material artifact is present and non-empty." : `Operator-provided material is not usable: ${inspected.reason}.`),
        source: "operator",
        evidence: { inspection: inspected.inspection }
      }));
    }
  }
  const byId = /* @__PURE__ */ new Map();
  for (const requirement of requirements) {
    byId.set(requirement.id, requirement);
  }
  return Array.from(byId.values());
}
function providerIdIsNone(value) {
  return typeof value === "string" && value.trim().toLowerCase() === "none";
}
function providerIdIsGptImage2(value) {
  return typeof value === "string" && value.trim().toLowerCase().replace(/[-_]/g, "") === "gptimage2";
}
function normalizeProviderId2(value) {
  if (typeof value !== "string") {
    return value ?? null;
  }
  const trimmed = value.trim();
  return trimmed.length > 0 ? trimmed : null;
}
function selectedProviderIdFor(config, providerId) {
  if (providerIdIsNone(providerId)) {
    return null;
  }
  const explicitProviderId = normalizeProviderId2(providerId);
  if (explicitProviderId) {
    return explicitProviderId;
  }
  return providerIdIsNone(config.defaultProviderId) ? null : normalizeProviderId2(config.defaultProviderId);
}
function selectProvider(config, providerId, env) {
  const selectedProviderId = selectedProviderIdFor(config, providerId);
  const provider = selectedProviderId ? config.providers.find((item) => item.id === selectedProviderId) ?? (providerIdIsGptImage2(selectedProviderId) ? createGptImage2FigureProvider() : null) : null;
  if (selectedProviderId && !provider) {
    throw new Error(`Unknown figure generation provider: ${selectedProviderId}`);
  }
  if (!provider) {
    return {
      provider: null,
      readiness: {
        status: "not-configured",
        summary: "No figure generation provider is configured; use the prompt/input bundle with an external drawing tool, then import its output."
      }
    };
  }
  const secretConfigured = provider.apiKeyEnv ? Boolean(env[provider.apiKeyEnv]) : true;
  return {
    provider,
    readiness: {
      status: secretConfigured ? "ready" : "missing-secret-env",
      summary: secretConfigured ? `Provider ${provider.id} is configured.` : `Provider ${provider.id} requires environment variable ${provider.apiKeyEnv}.`,
      apiKeyEnv: provider.apiKeyEnv ?? null,
      apiKeyEnvConfigured: secretConfigured
    }
  };
}
function providerRequestSummary(provider, readiness) {
  if (!provider) {
    return { providerId: null, providerType: null, readiness };
  }
  return {
    providerId: provider.id,
    providerType: provider.type,
    endpoint: provider.endpoint ?? null,
    command: provider.command ?? null,
    model: provider.model ?? null,
    imageSize: provider.imageSize ?? null,
    imageQuality: provider.imageQuality ?? null,
    imageBackground: provider.imageBackground ?? null,
    timeoutMs: provider.timeoutMs,
    maxPromptChars: provider.maxPromptChars,
    maxSvgBytes: provider.maxSvgBytes,
    secret: provider.apiKeyEnv ? { apiKeyEnv: provider.apiKeyEnv, configured: readiness.apiKeyEnvConfigured } : null,
    readiness
  };
}
function buildGenerationPrompt({ figure, packet, requirements, constraints, outputFormat, provider }) {
  const lines = [
    `# Figure generation request: ${figure.name ?? figure.id}`,
    "",
    "## Purpose",
    figure.purpose ?? figure.narrativeIntent ?? "Explain the linked paper evidence clearly.",
    "",
    "## Caption intent",
    figure.captionIntent ?? figure.narrativeIntent ?? figure.purpose ?? "Explain what the figure shows and why it matters.",
    "",
    "## Durable task packet",
    `- Packet id: ${packet.id}`,
    `- Packet title: ${packet.title ?? packet.id}`,
    `- Current focus: ${packet.currentFocus ?? "none"}`,
    "",
    "## Required output",
    `- Format: ${outputFormat}`,
    `- Dove final artifact target: ${figure.finalSvgPath}`,
    "- Return an output manifest with sourceSvgPath and caption, or return svgContent so Dove can import it into the final artifact target.",
    "",
    "## Visual elements",
    ...normalizeStringArray20(figure.requiredVisualElements).length > 0 ? normalizeStringArray20(figure.requiredVisualElements).map((item) => `- ${item}`) : ["- No visual elements declared yet; infer a minimal evidence-linked figure from the materials."],
    "",
    "## Materials",
    ...requirements.map((item) => `- [${item.status}] ${item.type}:${item.refId ?? item.id} \u2014 ${item.label}${item.artifactPath ? ` (${item.artifactPath})` : ""}. ${item.summary}`),
    "",
    "## Constraints",
    ...constraints.length > 0 ? constraints.map((item) => `- ${item}`) : ["- Keep labels editable and avoid unsupported visual claims.", "- Use only the provided evidence and mark missing information visibly."],
    "",
    "## Provider hint",
    provider ? `Use provider ${provider.id} (${provider.type})${provider.model ? ` with model ${provider.model}` : ""}.` : "No provider is configured; this prompt is provider-neutral."
  ];
  return `${lines.join("\n")}
`;
}
function truncatePrompt(prompt, maxChars) {
  if (prompt.length <= maxChars) {
    return prompt;
  }
  return `${prompt.slice(0, Math.max(0, maxChars - 80))}

[Prompt truncated to fit configured maxPromptChars.]
`;
}
function updateIndexItem(index, item) {
  const items = Array.isArray(index.items) ? index.items : [];
  return {
    ...index,
    items: [...items.filter((existing) => existing.id !== item.id), item],
    updatedAt: item.updatedAt ?? nowIso()
  };
}
function sha2562(content) {
  return crypto12.createHash("sha256").update(content).digest("hex");
}
function svgSafetyIssues(content, maxSvgBytes) {
  const issues = [];
  if (Buffer.byteLength(content, "utf8") > maxSvgBytes) {
    issues.push(`SVG exceeds maxSvgBytes (${maxSvgBytes}).`);
  }
  if (!/^\s*(?:<\?xml\s[^>]*>\s*)?(?:<!--[\s\S]*?-->\s*)*<svg(?:\s|>)/i.test(content)) {
    issues.push("Generated output must be an SVG document.");
  }
  const checks = [
    [/<script\b/i, "SVG must not contain script elements."],
    [/\son[a-z]+\s*=/i, "SVG must not contain inline event handlers."],
    [/<foreignObject\b/i, "SVG must not contain foreignObject."],
    [/(?:href|xlink:href)\s*=\s*["']\s*(?:[a-z][a-z0-9+.-]*:|\/\/)/i, "SVG must not reference remote, javascript, data, file, or other URL schemes."],
    [/url\(\s*["']?(?:[a-z][a-z0-9+.-]*:|\/\/)/i, "SVG must not reference remote, javascript, data, file, or other URL resources."]
  ];
  for (const [pattern, message] of checks) {
    if (pattern.test(content)) {
      issues.push(message);
    }
  }
  return issues;
}
function readOutputManifest(root, runId, outputManifestPath, outputDefaults = {}) {
  const defaultPath = `${generationRunDir(runId)}/output.json`;
  const manifestPath = assertSafeFigurePath(outputManifestPath ?? defaultPath, "outputManifestPath", [generationRunDir(runId), ".dove/figures/"]);
  const inspection = inspectDeclaredPath(root, manifestPath, { requireNonEmpty: true });
  if (inspection.status === "existing") {
    return { manifestPath, manifest: readJson(root, manifestPath, {}) };
  }
  if (inspection.status !== "missing") {
    throw new Error(`Figure output manifest is not a usable file at ${manifestPath}: ${inspection.reason ?? inspection.status}`);
  }
  const manifest = { ...outputDefaults, runId, generatedAt: nowIso() };
  writeJson(root, manifestPath, manifest);
  return { manifestPath, manifest };
}
function assertManifestHasNoInlineSecrets(root, manifestPath, manifest, label) {
  try {
    assertNoInlineSecrets(manifest, label);
  } catch (error) {
    writeJson(root, manifestPath, redactDoveConfig(manifest));
    throw error;
  }
}
function parseProviderOutput(rawOutput) {
  if (rawOutput && typeof rawOutput === "object") {
    return rawOutput;
  }
  const text3 = String(rawOutput ?? "").trim();
  if (!text3) {
    return null;
  }
  if (text3.startsWith("{")) {
    return JSON.parse(text3);
  }
  if (text3.startsWith("<svg")) {
    return { svgContent: text3 };
  }
  throw new Error("Figure provider returned neither JSON nor SVG output.");
}
function writeProviderOutput(root, runId, providerOutput, timestamp) {
  const output = parseProviderOutput(providerOutput);
  if (!output) {
    const defaultManifestPath = `${generationRunDir(runId)}/output.json`;
    if (fs21.existsSync(resolvePath(root, defaultManifestPath))) {
      const manifest2 = readJson(root, defaultManifestPath, {});
      assertManifestHasNoInlineSecrets(root, defaultManifestPath, manifest2, "figureGeneration.providerOutputManifest");
      return { outputManifestPath: defaultManifestPath, manifest: manifest2 };
    }
    throw new Error("Figure provider did not return output and did not write output.json.");
  }
  assertNoInlineSecrets(output, "figureGeneration.providerOutput");
  assertNoLegacyProviderOutputFields(output, "figureGeneration.providerOutput");
  const outputManifestPath = assertSafeFigurePath(output.outputManifestPath ?? `${generationRunDir(runId)}/output.json`, "provider output manifest", [`${generationRunDir(runId)}/`]);
  const svgContent = output.svgContent;
  const sourceSvgPath = assertSafeFigureSourcePath(output.sourceSvgPath ?? output.svgPath ?? `${generationRunDir(runId)}/final.svg`, "provider source SVG path", runId);
  const manifest = { ...output, runId, sourceSvgPath, generatedAt: output.generatedAt ?? timestamp };
  delete manifest.svgContent;
  if (typeof svgContent === "string" && svgContent.trim()) {
    writeText(root, sourceSvgPath, svgContent.endsWith("\n") ? svgContent : `${svgContent}
`);
  }
  writeJson(root, outputManifestPath, manifest);
  return { outputManifestPath, manifest };
}
function assertSpawnSucceeded(result, label, timeoutMs) {
  if (result.error) {
    const message = result.error?.code === "ETIMEDOUT" ? `${label} timed out after ${timeoutMs}ms.` : result.error.message;
    throw new Error(message);
  }
  if (result.status !== 0) {
    throw new Error(`${label} failed with exit ${result.status}: ${String(result.stderr ?? "").slice(0, 1e3)}`);
  }
}
function invokeExternalCommandProvider(root, provider, inputPath, runId, timestamp) {
  const result = spawnSync2(provider.command, [resolvePath(root, inputPath)], {
    cwd: root,
    encoding: "utf8",
    timeout: provider.timeoutMs,
    maxBuffer: provider.maxSvgBytes * 2 + 1e5
  });
  assertSpawnSucceeded(result, `Figure provider ${provider.id}`, provider.timeoutMs);
  return writeProviderOutput(root, runId, result.stdout, timestamp);
}
function postProviderJson(provider, label, payload, maxBuffer = provider.maxSvgBytes * 2 + 1e5) {
  const script = `
const chunks = [];
process.stdin.on("data", (chunk) => chunks.push(chunk));
process.stdin.on("end", async () => {
  try {
    const payload = JSON.parse(Buffer.concat(chunks).toString("utf8"));
    const controller = new AbortController();
    const timer = setTimeout(() => controller.abort(), payload.timeoutMs);
    const headers = { "content-type": "application/json", "accept": payload.accept || "application/json" };
    if (payload.apiKey) headers.authorization = \`Bearer \${payload.apiKey}\`;
    const response = await fetch(payload.endpoint, { method: "POST", headers, body: JSON.stringify(payload.request), signal: controller.signal });
    clearTimeout(timer);
    const body = await response.text();
    process.stdout.write(JSON.stringify({ ok: response.ok, status: response.status, contentType: response.headers.get("content-type") || "", body }));
  } catch (error) {
    process.stderr.write(error instanceof Error ? error.message : String(error));
    process.exit(1);
  }
});
`;
  const result = spawnSync2(process.execPath, ["-e", script], {
    input: JSON.stringify(payload),
    encoding: "utf8",
    timeout: provider.timeoutMs + 1e3,
    maxBuffer
  });
  assertSpawnSucceeded(result, label, provider.timeoutMs);
  const response = JSON.parse(result.stdout);
  if (!response.ok) {
    throw new Error(`${label} returned ${response.status}: ${String(response.body ?? "").slice(0, 1e3)}`);
  }
  return response;
}
function invokeHttpJsonProvider(root, provider, input, prompt, env, runId, timestamp) {
  const apiKey = provider.apiKeyEnv ? env[provider.apiKeyEnv] : null;
  if (provider.apiKeyEnv && !apiKey) {
    throw new Error(`Figure provider ${provider.id} requires environment variable ${provider.apiKeyEnv}.`);
  }
  const response = postProviderJson(provider, `HTTP figure provider ${provider.id}`, {
    endpoint: provider.endpoint,
    apiKey,
    timeoutMs: provider.timeoutMs,
    accept: "application/json, image/svg+xml, text/plain",
    request: {
      model: provider.model ?? void 0,
      prompt,
      input
    }
  });
  const providerOutput = response.contentType.includes("json") ? JSON.parse(response.body) : response.body;
  return writeProviderOutput(root, runId, providerOutput, timestamp);
}
function xmlEscape(value) {
  return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
function dimensionsFromImageSize(value) {
  const match = typeof value === "string" ? value.match(/^(\d+)x(\d+)$/) : null;
  return match ? { width: Number(match[1]), height: Number(match[2]) } : { width: 1024, height: 1024 };
}
function openAiImageRequest(provider, prompt) {
  const request = {
    model: provider.model ?? "gpt-image-2",
    prompt,
    n: 1
  };
  if (provider.imageSize) {
    request.size = provider.imageSize;
  }
  if (provider.imageQuality) {
    request.quality = provider.imageQuality;
  }
  if (provider.imageBackground) {
    request.background = provider.imageBackground;
  }
  return request;
}
function openAiImageData(responseBody, providerId) {
  const parsed = JSON.parse(responseBody);
  const item = Array.isArray(parsed.data) ? parsed.data[0] : null;
  const b64 = item?.b64_json ?? item?.image?.b64_json;
  if (typeof b64 !== "string" || b64.trim().length === 0) {
    throw new Error(`OpenAI image provider ${providerId} did not return b64_json image data.`);
  }
  return {
    b64,
    revisedPrompt: item?.revised_prompt ?? item?.revisedPrompt ?? null,
    responseId: parsed.id ?? null
  };
}
function invokeOpenAiImageProvider(root, provider, input, prompt, env, runId, timestamp) {
  const apiKey = provider.apiKeyEnv ? env[provider.apiKeyEnv] : null;
  if (provider.apiKeyEnv && !apiKey) {
    throw new Error(`Figure provider ${provider.id} requires environment variable ${provider.apiKeyEnv}.`);
  }
  const response = postProviderJson(provider, `OpenAI image figure provider ${provider.id}`, {
    endpoint: provider.endpoint,
    apiKey,
    timeoutMs: provider.timeoutMs,
    accept: "application/json",
    request: openAiImageRequest(provider, prompt)
  }, provider.maxSvgBytes * 8 + 1e5);
  const image = openAiImageData(response.body, provider.id);
  const imagePath = `${generationRunDir(runId)}/gpt-image2.png`;
  writeBinary(root, imagePath, Buffer.from(image.b64, "base64"));
  const sourceSvgPath = assertSafeFigureSourcePath(`${generationRunDir(runId)}/gpt-image2.svg`, "OpenAI image sourceSvgPath", runId);
  const targetFinalSvgPath = assertSafeFigureFinalTargetPath(input.figure?.finalSvgPath, "OpenAI image final artifact target");
  const href = path24.posix.relative(path24.posix.dirname(targetFinalSvgPath), imagePath) || path24.posix.basename(imagePath);
  const { width, height } = dimensionsFromImageSize(provider.imageSize);
  const title = xmlEscape(input.figure?.name ?? input.figureId ?? "Generated figure");
  const desc = xmlEscape(input.figure?.purpose ?? "Generated by an OpenAI image provider and wrapped as a local SVG artifact.");
  const svgContent = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${width} ${height}" role="img" aria-labelledby="title desc"><title id="title">${title}</title><desc id="desc">${desc}</desc><image href="${xmlEscape(href)}" x="0" y="0" width="${width}" height="${height}" preserveAspectRatio="xMidYMid meet"/></svg>
`;
  return writeProviderOutput(root, runId, {
    sourceSvgPath,
    svgContent,
    caption: `${input.figure?.name ?? input.figureId ?? "Figure"} generated with ${provider.id}.`,
    rasterImagePath: imagePath,
    providerResponseId: image.responseId,
    revisedPrompt: image.revisedPrompt,
    model: provider.model ?? "gpt-image-2",
    imageSize: provider.imageSize ?? null,
    generatedAt: timestamp
  }, timestamp);
}
function executeFigureProvider(root, provider, input, prompt, env, runId, timestamp) {
  if (provider.type === "external-command") {
    return invokeExternalCommandProvider(root, provider, input.inputPath ?? `${generationRunDir(runId)}/input.json`, runId, timestamp);
  }
  if (provider.type === "http-json") {
    return invokeHttpJsonProvider(root, provider, input, prompt, env, runId, timestamp);
  }
  if (provider.type === "openai-image") {
    return invokeOpenAiImageProvider(root, provider, input, prompt, env, runId, timestamp);
  }
  throw new Error(`Unsupported figure provider type: ${provider.type}`);
}
function patchPlanProviderExecutionBoundary(provider) {
  return {
    status: "awaiting-provider-output",
    reason: `Figure provider ${provider.id} requires direct-process execution because provider calls may spawn processes, call external APIs, or write binary outputs that patch-plan cannot safely represent.`,
    providerId: provider.id,
    providerType: provider.type,
    requiredMutationMode: "direct-process",
    requiredActions: ["retry-with-mutationMode-direct-process", "import-manual-figure-output"],
    directProcessRequired: true,
    hostRollbackEligible: false
  };
}
function updateFigureIndexesForImport(root, figure, generation, caption, timestamp) {
  const figures = readFigures(root);
  figures.items = (figures.items ?? []).map((item) => item.id === figure.id ? {
    ...item,
    status: "generated",
    latestGenerationRunId: generation.id,
    captionId: caption.id,
    finalSvgPath: generation.finalSvgPath,
    updatedAt: timestamp
  } : item);
  figures.updatedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.figuresIndex, figures);
  const finals = readJson(root, ARTIFACT_PATHS.figureFinalIndex, { version: 1, items: [], updatedAt: null });
  finals.items = (finals.items ?? []).map((item) => item.figureId === figure.id ? {
    ...item,
    generationRunId: generation.id,
    captionId: caption.id,
    finalSvgPath: generation.finalSvgPath,
    readinessStatus: "ready-for-finalization",
    updatedAt: timestamp
  } : item);
  finals.updatedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.figureFinalIndex, finals);
  const editable = readJson(root, ARTIFACT_PATHS.figureEditableIndex, { version: 1, items: [], updatedAt: null });
  editable.items = (editable.items ?? []).map((item) => item.figureId === figure.id ? {
    ...item,
    generationRunId: generation.id,
    captionId: caption.id,
    finalSvgPath: generation.finalSvgPath,
    updatedAt: timestamp
  } : item);
  editable.updatedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.figureEditableIndex, editable);
}
function buildCaptionText(figure, generation, providedCaption) {
  const base = typeof providedCaption === "string" && providedCaption.trim().length > 0 ? providedCaption.trim() : `${figure.name ?? figure.id} shows ${figure.narrativeIntent ?? figure.purpose ?? "the linked paper evidence"} for ${normalizeStringArray20(figure.targetClaimIds).join(", ") || "the current paper argument"}.`;
  const provenance = `Generated/imported through run ${generation.id}${generation.providerId ? ` using provider ${generation.providerId}` : ""}.`;
  return base.includes(generation.id) ? base : `${base} ${provenance}`;
}
function prepareFigureGeneration(root, args = {}) {
  assertGovernanceMutationRegistered("prepare-figure-generation", "guarded");
  assertPrepareFigureGenerationInput(args);
  const target = assertTaskScopedMutationTarget(root, "prepare-figure-generation", args);
  assertFollowThroughReady(root, "Preparing figure generation", args);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Preparing figure generation"
  );
  ensureWorkspace(root);
  const { env: _env, ...safeArgs } = args;
  assertNoInlineSecrets(safeArgs, "figureGeneration.args");
  const { figure } = resolveFigure(root, args);
  const config = loadFigureGenerationConfig(root, args.env ?? process.env);
  const { provider, readiness } = selectProvider(config, args.providerId ?? figure.generationProviderId, args.env ?? process.env);
  const timestamp = nowIso();
  const runId = slugify10(args.runId ?? defaultRunId(figure.id));
  const runDir = generationRunDir(runId);
  const inputPath = `${runDir}/input.json`;
  const promptPath = `${runDir}/prompt.md`;
  const outputManifestPath = `${runDir}/output.json`;
  const manifestPath = `${runDir}/manifest.json`;
  const outputFormat = args.outputFormat ?? figure.outputFormat ?? "svg";
  const constraints = normalizeStringArray20(args.constraints, Array.isArray(figure.generationConstraints) ? figure.generationConstraints : []);
  const requirements = buildMaterialRequirements(root, figure, args);
  const missingRequirementIds = requirements.filter((item) => item.status !== "available").map((item) => item.id);
  const materialRecord = {
    id: `${figure.id}-materials`,
    figureId: figure.id,
    packetId: target.packetId,
    runId,
    status: missingRequirementIds.length > 0 ? "needs-materials" : "ready",
    requirements,
    missingRequirementIds,
    updatedAt: timestamp
  };
  const prompt = truncatePrompt(buildGenerationPrompt({ figure, packet: target.packet, requirements, constraints, outputFormat, provider }), provider?.maxPromptChars ?? config.maxPromptChars);
  const input = {
    version: 1,
    runId,
    figureId: figure.id,
    packetId: target.packetId,
    figure: {
      id: figure.id,
      name: figure.name,
      purpose: figure.purpose,
      narrativeIntent: figure.narrativeIntent,
      finalSvgPath: figure.finalSvgPath,
      targetClaimIds: figure.targetClaimIds ?? [],
      relatedExperimentIds: figure.relatedExperimentIds ?? []
    },
    materialRecord,
    outputFormat,
    constraints,
    providerRequest: providerRequestSummary(provider, readiness),
    config: redactDoveConfig({ figureGeneration: { ...config, providers: provider ? [provider] : [] } }).figureGeneration,
    inputPath,
    promptPath,
    expectedOutputManifestPath: outputManifestPath,
    createdAt: timestamp
  };
  const materials = readJson(root, ARTIFACT_PATHS.figureMaterials, { version: 1, items: [], updatedAt: null });
  writeJson(root, ARTIFACT_PATHS.figureMaterials, updateIndexItem(materials, materialRecord));
  writeJson(root, inputPath, input);
  writeText(root, promptPath, prompt);
  let generation = {
    id: runId,
    figureId: figure.id,
    packetId: target.packetId,
    status: "prepared",
    providerId: provider?.id ?? null,
    providerType: provider?.type ?? null,
    materialRecordId: materialRecord.id,
    inputPath,
    promptPath,
    outputManifestPath,
    manifestPath,
    outputFormat,
    missingRequirementIds,
    readiness,
    createdAt: timestamp,
    updatedAt: timestamp
  };
  const generations = readJson(root, ARTIFACT_PATHS.figureGenerations, { version: 1, items: [], updatedAt: null });
  let providerExecution = null;
  if (args.executeProvider === true) {
    if (!provider) {
      providerExecution = { status: "failed", error: "No figure generation provider is configured." };
    } else if (isPatchPlanMode(root)) {
      providerExecution = patchPlanProviderExecutionBoundary(provider);
      generation = { ...generation, status: providerExecution.status, providerExecution, updatedAt: timestamp };
    } else if (readiness.status === "missing-secret-env") {
      providerExecution = {
        status: "missing-secret-env",
        error: readiness.summary,
        apiKeyEnv: readiness.apiKeyEnv ?? provider.apiKeyEnv ?? null
      };
      generation = { ...generation, status: "missing-secret-env", providerError: providerExecution.error, updatedAt: timestamp };
    } else if (missingRequirementIds.length > 0 && args.allowMissingMaterials !== true) {
      providerExecution = { status: "skipped-missing-materials", missingRequirementIds };
    } else {
      try {
        const output = executeFigureProvider(root, provider, input, prompt, args.env ?? process.env, runId, timestamp);
        providerExecution = { status: "completed", outputManifestPath: output.outputManifestPath };
        generation = { ...generation, status: "provider-output-ready", outputManifestPath: output.outputManifestPath, providerExecutedAt: timestamp, updatedAt: timestamp };
      } catch (error) {
        providerExecution = { status: "failed", error: error instanceof Error ? error.message : String(error) };
        generation = { ...generation, status: "provider-failed", providerError: providerExecution.error, updatedAt: timestamp };
      }
    }
  }
  if (providerExecution && generation.status === "prepared") {
    generation = { ...generation, status: providerExecution.status, providerExecution, updatedAt: timestamp };
  } else if (providerExecution) {
    generation = { ...generation, providerExecution };
  }
  writeJson(root, ARTIFACT_PATHS.figureGenerations, updateIndexItem(generations, generation));
  writeJson(root, manifestPath, generation);
  refreshDurableSurfaces(root, {
    type: "prepare-figure-generation",
    summary: `Prepared figure generation run ${runId} for ${figure.id} with ${missingRequirementIds.length} missing materials${providerExecution ? `; provider execution ${providerExecution.status}` : ""}.`,
    artifactPaths: [ARTIFACT_PATHS.figureMaterials, ARTIFACT_PATHS.figureGenerations, inputPath, promptPath, outputManifestPath, manifestPath]
  });
  return {
    runId,
    figureId: figure.id,
    packetId: target.packetId,
    materialStatus: materialRecord.status,
    missingRequirementIds,
    providerReadiness: readiness,
    providerExecution,
    inputPath,
    promptPath,
    outputManifestPath: generation.outputManifestPath,
    manifestPath,
    preActionGuidanceSummary: figureGenerationGuidanceSummary(root, args, target, {
      nextAction: missingRequirementIds.length > 0 ? "project:dove.figure" : "project:dove.review",
      statusSummary: {
        runId,
        figureId: figure.id,
        materialStatus: materialRecord.status,
        missingRequirementCount: missingRequirementIds.length,
        providerExecutionStatus: providerExecution?.status ?? null
      }
    })
  };
}
function importFigureGeneration(root, args = {}) {
  assertGovernanceMutationRegistered("import-figure-generation", "guarded");
  assertImportFigureGenerationInput(args);
  const target = assertTaskScopedMutationTarget(root, "import-figure-generation", args);
  assertFollowThroughReady(root, "Importing figure generation", args);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Importing figure generation"
  );
  ensureWorkspace(root);
  assertNoLegacyFinalSvgInput(args, "import_figure_generation");
  const { env: _env, svgContent: _svgContent, ...safeArgs } = args;
  assertNoInlineSecrets(safeArgs, "figureGeneration.importArgs");
  const { figure } = resolveFigure(root, args);
  const runId = slugify10(args.runId ?? "");
  if (!runId) {
    throw new Error("import_figure_generation requires runId.");
  }
  const generations = readJson(root, ARTIFACT_PATHS.figureGenerations, { version: 1, items: [], updatedAt: null });
  const existingGeneration = (generations.items ?? []).find((item) => item.id === runId);
  if (!existingGeneration) {
    throw new Error(`Figure generation run not found: ${runId}`);
  }
  if (existingGeneration.figureId !== figure.id) {
    throw new Error(`Figure generation run ${runId} belongs to ${existingGeneration.figureId}, not ${figure.id}.`);
  }
  if (existingGeneration.packetId !== target.packetId) {
    throw new Error(`Figure generation run ${runId} belongs to packet ${existingGeneration.packetId}, not ${target.packetId}.`);
  }
  const defaultSourceSvgPath = `${generationRunDir(runId)}/final.svg`;
  const { manifestPath: outputManifestPath, manifest } = readOutputManifest(root, runId, args.outputManifestPath, {
    sourceSvgPath: args.sourceSvgPath ?? defaultSourceSvgPath,
    caption: args.caption ?? args.captionDraft
  });
  assertManifestHasNoInlineSecrets(root, outputManifestPath, manifest, "figureGeneration.outputManifest");
  assertNoLegacyFinalSvgInput(manifest, "figureGeneration.outputManifest");
  assertNoLegacyProviderOutputFields(manifest, "figureGeneration.outputManifest");
  const providerOwnedManifest = existingGeneration.providerExecution?.status === "completed" && outputManifestPath === existingGeneration.outputManifestPath;
  if (!providerOwnedManifest) {
    assertManifestDoesNotClaimSemanticSuccess(manifest, "figureGeneration.outputManifest");
  }
  const semanticCoverage = normalizeSemanticCoverage(args.semanticCoverage ?? existingGeneration.semanticCoverage);
  const semanticReview = normalizeSemanticReview(args.semanticReview ?? existingGeneration.semanticReview);
  if (semanticCoverage || semanticReview) {
    writeJson(root, outputManifestPath, {
      ...manifest,
      ...semanticCoverage ? { semanticCoverage } : {},
      ...semanticReview ? { semanticReview } : {}
    });
  }
  const rawSvgContent = typeof args.svgContent === "string" ? args.svgContent : typeof manifest.svgContent === "string" ? manifest.svgContent : null;
  const sourceSvgPath = assertSafeFigureSourcePath(args.sourceSvgPath ?? manifest.sourceSvgPath ?? manifest.svgPath ?? defaultSourceSvgPath, "sourceSvgPath", runId);
  const finalSvgPath = assertSafeFigureFinalTargetPath(figure.finalSvgPath, "figure.finalSvgPath");
  const config = loadFigureGenerationConfig(root, args.env ?? process.env);
  let svgContent;
  if (rawSvgContent) {
    svgContent = rawSvgContent.endsWith("\n") ? rawSvgContent : `${rawSvgContent}
`;
    writeText(root, sourceSvgPath, svgContent);
  } else {
    const sourceInspection = inspectDeclaredPath(root, sourceSvgPath, {
      requireNonEmpty: true,
      readText: true,
      maxBytes: config.maxSvgBytes
    });
    if (sourceInspection.status !== "existing") {
      throw new Error(`Generated SVG output is not a usable non-empty file at ${sourceSvgPath}: ${sourceInspection.reason ?? sourceInspection.status}`);
    }
    svgContent = sourceInspection.text ?? readText(root, sourceSvgPath, "");
  }
  const provider = existingGeneration.providerId ? config.providers.find((item) => item.id === existingGeneration.providerId) : null;
  const maxSvgBytes = provider?.maxSvgBytes ?? config.maxSvgBytes;
  const safetyIssues = svgSafetyIssues(svgContent, maxSvgBytes);
  if (safetyIssues.length > 0) {
    throw new Error(`Generated SVG failed safety validation: ${safetyIssues.join(" ")}`);
  }
  upsertSystemOrchestrationBoard(root, {
    reviewRequiredBeforeFinalize: true
  });
  if (sourceSvgPath !== finalSvgPath) {
    writeText(root, finalSvgPath, svgContent);
  }
  const timestamp = nowIso();
  const generation = {
    ...existingGeneration,
    status: "imported",
    outputManifestPath,
    sourceSvgPath,
    finalSvgPath,
    finalSha256: sha2562(svgContent),
    importedAt: timestamp,
    updatedAt: timestamp,
    safety: { status: "passed", checks: ["script", "event-handlers", "foreignObject", "remote-hrefs", "remote-url-resources"], maxSvgBytes },
    ...semanticCoverage ? { semanticCoverage } : {},
    ...semanticReview ? { semanticReview } : {}
  };
  const caption = {
    id: slugify10(args.captionId ?? `${figure.id}-${runId}-caption`),
    figureId: figure.id,
    runId,
    packetId: target.packetId,
    text: buildCaptionText(figure, generation, args.caption ?? args.captionDraft ?? manifest.caption),
    purpose: figure.purpose ?? figure.narrativeIntent ?? "Explain the linked paper evidence.",
    targetClaimIds: normalizeStringArray20(figure.targetClaimIds),
    relatedExperimentIds: normalizeStringArray20(figure.relatedExperimentIds),
    provenance: {
      providerId: generation.providerId,
      providerType: generation.providerType,
      runId,
      outputManifestPath,
      finalSha256: generation.finalSha256
    },
    updatedAt: timestamp
  };
  writeJson(root, ARTIFACT_PATHS.figureGenerations, updateIndexItem(generations, { ...generation, captionId: caption.id }));
  const captions = readJson(root, ARTIFACT_PATHS.figureCaptions, { version: 1, items: [], updatedAt: null });
  writeJson(root, ARTIFACT_PATHS.figureCaptions, updateIndexItem(captions, caption));
  writeJson(root, `${generationRunDir(runId)}/manifest.json`, { ...generation, captionId: caption.id });
  updateFigureIndexesForImport(root, figure, { ...generation, captionId: caption.id }, caption, timestamp);
  const validation = validateFigurePipeline(root);
  const figureQa = summarizeFigureQa(root, figure.id, validation);
  const independentReviewProof = findCurrentIndependentReviewProof(root, {
    packetId: target.packetId,
    reviewedArtifactPaths: [finalSvgPath]
  });
  refreshDurableSurfaces(root, {
    type: "import-figure-generation",
    summary: `Imported figure generation run ${runId} for ${figure.id}.`,
    artifactPaths: [ARTIFACT_PATHS.figureGenerations, ARTIFACT_PATHS.figureCaptions, ARTIFACT_PATHS.figureFinalIndex, ARTIFACT_PATHS.figureQa, finalSvgPath]
  });
  return {
    runId,
    figureId: figure.id,
    packetId: target.packetId,
    captionId: caption.id,
    sourceSvgPath,
    finalSvgPath,
    finalSha256: generation.finalSha256,
    independentReviewProof: independentReviewProof.ok ? independentReviewProof.proof : null,
    qaIssueCount: figureQa.issueCount,
    workspaceQaIssueCount: figureQa.workspaceIssueCount,
    qaPath: figureQa.qaPath,
    figureQa,
    preActionGuidanceSummary: figureGenerationGuidanceSummary(root, args, target, {
      nextAction: figureQa.issueCount > 0 ? "project:dove.figure" : "project:dove.review",
      statusSummary: {
        runId,
        figureId: figure.id,
        captionId: caption.id,
        sourceSvgPath,
        finalSvgPath,
        qaIssueCount: figureQa.issueCount,
        workspaceQaIssueCount: figureQa.workspaceIssueCount
      }
    })
  };
}

// src/core/figure-workflow.mjs
import fs22 from "node:fs";
import path25 from "node:path";
function slugify11(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "figure";
}
function normalizeStringArray21(value, fallback = []) {
  const source = Array.isArray(value) ? value : fallback;
  return Array.from(new Set(source.filter((item) => typeof item === "string" && item.trim()).map((item) => item.trim())));
}
function firstText(...values) {
  return values.find((value) => typeof value === "string" && value.trim().length > 0)?.trim() ?? null;
}
var FIGURE_MATERIAL_INPUT_KEYS2 = /* @__PURE__ */ new Set([
  "id",
  "type",
  "label",
  "summary",
  "artifactPath",
  "path"
]);
var FIGURE_SEMANTIC_COVERAGE_INPUT_KEYS2 = /* @__PURE__ */ new Set([
  "observations",
  "evidencePaths",
  "artifactPaths"
]);
var FIGURE_SEMANTIC_REVIEW_INPUT_KEYS2 = /* @__PURE__ */ new Set([
  "summary",
  "observations",
  "evidencePaths",
  "artifactPaths"
]);
function assertFigurePlainObject2(value, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`run_figure_workflow requires an object at ${label}.`);
  }
}
function assertFigureString2(value, label) {
  if (typeof value !== "string") {
    throw new Error(`run_figure_workflow requires a string at ${label}.`);
  }
}
function assertFigureStringArray2(value, label) {
  if (!Array.isArray(value)) {
    throw new Error(`run_figure_workflow requires an array at ${label}.`);
  }
  value.forEach((item, index) => assertFigureString2(item, `${label}[${index}]`));
}
function assertFigureObjectKeys2(value, allowedKeys, label) {
  assertFigurePlainObject2(value, label);
  const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
  if (unknown.length > 0) {
    throw new Error(`run_figure_workflow does not accept unknown input ${unknown.map((key) => `${label}.${key}`).join(", ")}.`);
  }
}
function assertFigureMaterialItem2(value, label) {
  assertFigureObjectKeys2(value, FIGURE_MATERIAL_INPUT_KEYS2, label);
  for (const key of FIGURE_MATERIAL_INPUT_KEYS2) {
    if (Object.hasOwn(value, key)) {
      assertFigureString2(value[key], `${label}.${key}`);
    }
  }
}
function assertFigureSemanticObservation2(value, allowedKeys, label) {
  assertFigureObjectKeys2(value, allowedKeys, label);
  if (Object.hasOwn(value, "summary")) {
    assertFigureString2(value.summary, `${label}.summary`);
  }
  for (const key of ["observations", "evidencePaths", "artifactPaths"]) {
    if (Object.hasOwn(value, key)) {
      assertFigureStringArray2(value[key], `${label}.${key}`);
    }
  }
}
function assertFigurePublicInput(args = {}) {
  if (Object.hasOwn(args, "materialRequirements")) {
    if (!Array.isArray(args.materialRequirements)) {
      throw new Error("run_figure_workflow requires an array at $.materialRequirements.");
    }
    args.materialRequirements.forEach((item, index) => assertFigureMaterialItem2(item, `$.materialRequirements[${index}]`));
  }
  if (Object.hasOwn(args, "materialHints")) {
    if (!Array.isArray(args.materialHints)) {
      throw new Error("run_figure_workflow requires an array at $.materialHints.");
    }
    args.materialHints.forEach((item, index) => {
      if (typeof item !== "string") {
        assertFigureMaterialItem2(item, `$.materialHints[${index}]`);
      }
    });
  }
  if (Object.hasOwn(args, "semanticCoverage")) {
    assertFigureSemanticObservation2(
      args.semanticCoverage,
      FIGURE_SEMANTIC_COVERAGE_INPUT_KEYS2,
      "$.semanticCoverage"
    );
  }
  if (Object.hasOwn(args, "semanticReview")) {
    assertFigureSemanticObservation2(
      args.semanticReview,
      FIGURE_SEMANTIC_REVIEW_INPUT_KEYS2,
      "$.semanticReview"
    );
  }
}
function compactObject2(fields) {
  return Object.fromEntries(Object.entries(fields).filter(([, value]) => {
    if (value === null || value === void 0) {
      return false;
    }
    if (Array.isArray(value)) {
      return value.length > 0;
    }
    if (value && typeof value === "object") {
      return Object.keys(value).length > 0;
    }
    return true;
  }));
}
function normalizeRelativePath6(value) {
  return path25.posix.normalize(String(value ?? "").trim().replace(/\\/g, "/").replace(/^\.\//, ""));
}
function isSafeProjectRelativePath4(relativePath) {
  return Boolean(relativePath) && !path25.posix.isAbsolute(relativePath) && relativePath !== ".." && !relativePath.startsWith("../");
}
function assertSafeFinalTargetPath(relativePath, label) {
  const normalized = normalizeRelativePath6(relativePath);
  if (!isSafeProjectRelativePath4(normalized) || !normalized.startsWith(".dove/figures/") || normalized.startsWith(".dove/figures/runs/") || !normalized.endsWith(".svg")) {
    throw new Error(`${label} must be a final SVG artifact under .dove/figures/ and outside .dove/figures/runs/: ${relativePath ?? "<missing>"}`);
  }
  return normalized;
}
function assertNoLegacyFinalSvgInput2(value, label) {
  if (value && typeof value === "object" && Object.prototype.hasOwnProperty.call(value, "finalSvgPath")) {
    throw new Error(`${label} no longer accepts finalSvgPath as input; use sourceSvgPath for generated SVG input or targetFinalSvgPath for the final artifact target.`);
  }
}
function existingFigureById(root, figureId) {
  const figures = readJson(root, ARTIFACT_PATHS.figuresIndex, { version: 1, items: [], updatedAt: null });
  return {
    figures,
    existing: (figures.items ?? []).find((item) => item.id === figureId) ?? null
  };
}
function readInferenceContext(root) {
  const state = readJson(root, ARTIFACT_PATHS.state, { sections: {} });
  const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
  const experimentPlans = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  const experimentResults = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
  return { state, evidence, experimentPlans, experimentResults };
}
function inferClaimIds(args, existing, context) {
  const explicit = normalizeStringArray21(args.targetClaimIds ?? args.claimIds, existing?.targetClaimIds ?? []);
  if (explicit.length > 0) {
    return explicit;
  }
  const claims = context.evidence.claims ?? [];
  return claims.length === 1 ? [claims[0].id] : [];
}
function inferSourceSections(args, existing, context, targetClaimIds) {
  const explicit = normalizeStringArray21(args.sourceSections ?? args.sectionIds, existing?.sourceSections ?? []);
  if (explicit.length > 0) {
    return explicit;
  }
  const claimSections = targetClaimIds.map((claimId) => (context.evidence.claims ?? []).find((claim) => claim.id === claimId)?.sectionId).filter(Boolean);
  if (claimSections.length > 0) {
    return normalizeStringArray21(claimSections);
  }
  const sectionIds = Object.keys(context.state.sections ?? {});
  return sectionIds.length === 1 ? sectionIds : [];
}
function inferExperimentIds(args, existing, context, targetClaimIds) {
  const explicit = normalizeStringArray21(args.relatedExperimentIds ?? args.experimentIds, existing?.relatedExperimentIds ?? []);
  if (explicit.length > 0) {
    return explicit;
  }
  const claimExperimentIds = targetClaimIds.flatMap((claimId) => {
    const claim = (context.evidence.claims ?? []).find((item) => item.id === claimId);
    return normalizeStringArray21(claim?.experimentIds);
  });
  if (claimExperimentIds.length > 0) {
    return normalizeStringArray21(claimExperimentIds);
  }
  const ids = normalizeStringArray21([
    ...(context.experimentPlans.items ?? []).map((item) => item.id),
    ...(context.experimentResults.items ?? []).map((item) => item.experimentId)
  ]);
  return ids.length === 1 ? ids : [];
}
function buildFigureItem(root, args, target) {
  const intent = firstText(args.intent, args.description, args.summary, args.purpose, args.captionIntent, args.name, args.title) ?? "Requested paper figure";
  const rawFigureId = args.figureId ?? args.id ?? args.name ?? args.title ?? intent;
  const figureId = slugify11(rawFigureId);
  const { figures, existing } = existingFigureById(root, figureId);
  const context = readInferenceContext(root);
  const targetClaimIds = inferClaimIds(args, existing, context);
  const sourceSections = inferSourceSections(args, existing, context, targetClaimIds);
  const relatedExperimentIds = inferExperimentIds(args, existing, context, targetClaimIds);
  const requiredVisualElements = normalizeStringArray21(
    args.requiredVisualElements ?? args.visualElements,
    normalizeStringArray21(existing?.requiredVisualElements, [intent])
  );
  const item = {
    ...existing ?? {},
    id: figureId,
    name: firstText(args.name, args.title, existing?.name, intent) ?? figureId,
    purpose: firstText(args.purpose, args.intent, args.description, existing?.purpose, intent) ?? intent,
    sourceSections,
    sourceArtifactPaths: normalizeStringArray21(args.sourceArtifactPaths ?? args.artifactPaths, existing?.sourceArtifactPaths ?? []),
    targetClaimIds,
    relatedExperimentIds,
    reviewConcernIds: normalizeStringArray21(args.reviewConcernIds ?? args.concernIds, existing?.reviewConcernIds ?? []),
    rebuttalIssueIds: normalizeStringArray21(args.rebuttalIssueIds ?? args.issueIds, existing?.rebuttalIssueIds ?? []),
    narrativeIntent: firstText(args.narrativeIntent, args.intent, args.description, existing?.narrativeIntent, intent) ?? intent,
    requiredVisualElements,
    materialRequirements: Array.isArray(args.materialRequirements) ? args.materialRequirements : existing?.materialRequirements ?? [],
    generationProviderId: args.providerId ?? args.generationProviderId ?? existing?.generationProviderId ?? null,
    generationMode: args.generationMode ?? existing?.generationMode ?? "workflow",
    generationConstraints: normalizeStringArray21(args.constraints, existing?.generationConstraints ?? []),
    outputFormat: args.outputFormat ?? existing?.outputFormat ?? "svg",
    captionIntent: firstText(args.captionIntent, args.caption, args.captionDraft, existing?.captionIntent, intent) ?? intent,
    templateSvgPath: args.templateSvgPath ?? existing?.templateSvgPath ?? `.dove/figures/${figureId}.template.svg`,
    editableSvgPath: args.editableSvgPath ?? existing?.editableSvgPath ?? `.dove/figures/${figureId}.editable.svg`,
    finalSvgPath: assertSafeFinalTargetPath(args.targetFinalSvgPath ?? existing?.finalSvgPath ?? `.dove/figures/${figureId}.final.svg`, "targetFinalSvgPath"),
    owner: args.owner ?? existing?.owner ?? target.packet.assignedRole ?? "builder",
    mode: args.mode ?? existing?.mode ?? "generated",
    status: existing?.status ?? "planned",
    reviewNotes: normalizeStringArray21(args.reviewNotes, existing?.reviewNotes ?? [])
  };
  const items = [...(figures.items ?? []).filter((figure) => figure.id !== figureId), item];
  return { figureId, item, items };
}
function placeholderSvg(figure) {
  const label = (figure.name ?? figure.id).replace(/[<>&]/g, "");
  return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 400"><title>${label}</title><text x="32" y="48">${label}</text></svg>
`;
}
function ensureSvgFile(root, relativePath, content) {
  if (!relativePath || fs22.existsSync(resolvePath(root, relativePath))) {
    return false;
  }
  if (!isPatchPlanMode(root)) {
    ensureDir(path25.dirname(resolvePath(root, relativePath)));
  }
  writeText(root, relativePath, content);
  return true;
}
function ensureStageSvgFiles(root, figure) {
  const content = placeholderSvg(figure);
  return {
    templateCreated: ensureSvgFile(root, figure.templateSvgPath, content),
    editableCreated: ensureSvgFile(root, figure.editableSvgPath, content)
  };
}
function hasImportableOutput(args, prepared) {
  return Boolean(
    args.svgContent || args.sourceSvgPath || args.outputManifestPath || prepared.providerExecution?.status === "completed"
  );
}
function hasExplicitFigureMaterialInput(args = {}) {
  return Boolean(
    args.svgContent || args.sourceSvgPath || args.outputManifestPath || firstText(args.caption, args.captionDraft, args.captionIntent) || normalizeStringArray21(args.requiredVisualElements ?? args.visualElements).length > 0 || normalizeStringArray21(args.sourceArtifactPaths ?? args.artifactPaths).length > 0 || Array.isArray(args.materialRequirements) && args.materialRequirements.length > 0 || Array.isArray(args.materialHints) && args.materialHints.length > 0
  );
}
function figureNeedsPrePlanMaterialBoundary(args, item) {
  if (args.allowMissingMaterials === true) {
    return false;
  }
  const hasEvidenceLinkage = normalizeStringArray21(item.targetClaimIds).length > 0 || normalizeStringArray21(item.relatedExperimentIds).length > 0;
  return !hasEvidenceLinkage && !hasExplicitFigureMaterialInput(args);
}
function prePlanFigureBoundary(figureId, runId) {
  return {
    id: `${figureId}-${runId}-figure-materials`,
    type: "missing-required-materials",
    reason: `Figure ${figureId} needs claim/experiment linkage or explicit visual/caption/material input before Dove can write a figure plan.`,
    requiredInputs: ["claim-or-experiment-linkage", "visual-elements-or-caption-or-source-material"],
    requiredActions: ["link-figure-to-claim-or-experiment", "provide-figure-visual-or-caption-material"],
    artifactRefs: [],
    nextAction: "project:dove.figure",
    ownerRole: "builder",
    nextRole: "builder",
    detail: { implementationBoundaryType: "missing-figure-materials-before-plan" }
  };
}
function statusFor(prepared, imported, figureQa) {
  if (["missing-secret-env", "failed"].includes(prepared.providerExecution?.status)) {
    return "blocked-boundary";
  }
  if (imported) {
    return figureQa.issueCount === 0 && imported.independentReviewProof ? "validated" : "qa-needs-attention";
  }
  if ((prepared.missingRequirementIds ?? []).length > 0) {
    return "blocked-missing-materials";
  }
  return "prepared-awaiting-output";
}
function figureArtifactRefs(validation) {
  return normalizeStringArray21([
    ARTIFACT_PATHS.figuresIndex,
    ARTIFACT_PATHS.figureGenerations,
    ARTIFACT_PATHS.figureMaterials,
    ARTIFACT_PATHS.figureQa,
    validation?.qaPath
  ]);
}
function figureValidationEvidencePaths(validation) {
  return normalizeStringArray21([validation?.qaPath]);
}
function figureBoundaryFor(status, prepared, validation, figureQa, figureId, runId) {
  const artifactRefs = figureArtifactRefs(validation);
  const providerStatus2 = prepared.providerExecution?.status ?? null;
  if (status === "blocked-missing-materials") {
    return {
      id: `${figureId}-${runId}-missing-materials`,
      type: "missing-required-materials",
      reason: `Figure ${figureId} is missing required materials: ${(prepared.missingRequirementIds ?? []).join(", ")}.`,
      requiredInputs: prepared.missingRequirementIds ?? [],
      requiredActions: ["provide-figure-materials", "resolve-missing-figure-requirements"],
      artifactRefs,
      nextAction: "project:dove.figure",
      ownerRole: "builder",
      nextRole: "builder"
    };
  }
  if (providerStatus2 === "missing-secret-env") {
    const apiKeyEnv = prepared.providerExecution?.apiKeyEnv ?? prepared.providerReadiness?.apiKeyEnv ?? "provider-api-key-env";
    return {
      id: `${figureId}-${runId}-awaiting-provider-output`,
      type: "awaiting-provider-output",
      reason: `Figure ${figureId} is awaiting provider configuration before output can be imported.`,
      requiredInputs: [apiKeyEnv],
      requiredActions: ["set-provider-api-key-env", "retry-figure-provider", "import-manual-figure-output"],
      artifactRefs,
      nextAction: "project:dove.figure",
      ownerRole: "builder",
      nextRole: "builder",
      detail: compactObject2({
        implementationBoundaryType: "missing-secret-env",
        providerStatus: providerStatus2,
        apiKeyEnv,
        providerError: prepared.providerExecution?.error
      })
    };
  }
  if (providerStatus2 === "failed") {
    return {
      id: `${figureId}-${runId}-awaiting-provider-output`,
      type: "awaiting-provider-output",
      reason: `Figure ${figureId} is awaiting provider recovery or manual output import.`,
      requiredInputs: ["provider-error-resolution-or-manual-output"],
      requiredActions: ["fix-figure-provider-and-retry", "import-manual-figure-output"],
      artifactRefs,
      nextAction: "project:dove.figure",
      ownerRole: "builder",
      nextRole: "builder",
      detail: compactObject2({
        implementationBoundaryType: "provider-failed",
        providerStatus: providerStatus2,
        providerError: prepared.providerExecution?.error
      })
    };
  }
  if (status === "prepared-awaiting-output") {
    const providerRequiredActions = normalizeStringArray21(prepared.providerExecution?.requiredActions);
    const providerRequiredInputs = prepared.providerExecution?.requiredMutationMode ? [`mutationMode: ${prepared.providerExecution.requiredMutationMode}`] : [];
    return {
      id: `${figureId}-${runId}-awaiting-provider-output`,
      type: "awaiting-provider-output",
      reason: `Figure ${figureId} is awaiting provider output or manual output import.`,
      requiredInputs: normalizeStringArray21([...providerRequiredInputs, "sourceSvgPath-or-outputManifestPath-or-svgContent"]),
      requiredActions: normalizeStringArray21([...providerRequiredActions, "run-provider-or-import-output", "provide-source-svg-or-output-manifest"]),
      artifactRefs,
      nextAction: "project:dove.figure",
      ownerRole: "builder",
      nextRole: "builder",
      detail: compactObject2({
        implementationBoundaryType: providerStatus2,
        providerReason: prepared.providerExecution?.reason,
        requiredMutationMode: prepared.providerExecution?.requiredMutationMode
      })
    };
  }
  if (status === "qa-needs-attention") {
    const proofMissing = figureQa.issueCount === 0;
    return {
      id: `${figureId}-${runId}-qa-needs-attention`,
      type: "verification-failed",
      reason: proofMissing ? `Figure ${figureId} has current diagnostics but lacks independent Reviewer proof bound to the current final SVG hash.` : `Figure ${figureId} has ${figureQa.issueCount} diagnostic issue(s) to fix before independent review.`,
      requiredInputs: proofMissing ? ["current-final-svg-isolated-review-proof"] : ["current-figure-diagnostic-findings"],
      requiredActions: proofMissing ? ["prepare-isolated-review-for-current-final-svg", "import-coherent-isolated-review"] : ["resolve-current-figure-diagnostics", "rerun-figure-workflow", "obtain-current-final-svg-isolated-review-proof"],
      artifactRefs,
      nextAction: "project:dove.review",
      ownerRole: proofMissing ? "reviewer" : "builder",
      nextRole: proofMissing ? null : "reviewer",
      detail: { implementationBoundaryType: proofMissing ? "missing-review-proof" : "needs-review" }
    };
  }
  return null;
}
function figureText(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function figureResultHappened(status, { prepared, figureQa, imported, responseLanguage }) {
  const missing = normalizeStringArray21(prepared.missingRequirementIds).join("\u3001");
  const apiKeyEnv = prepared.providerExecution?.apiKeyEnv ?? prepared.providerReadiness?.apiKeyEnv ?? "provider API key";
  if (status === "validated") {
    return figureText(responseLanguage, "\u8FD9\u5F20\u56FE\u5DF2\u7ECF\u901A\u8FC7\u5F53\u524D\u56FE\u68C0\u67E5\uFF0C\u53EF\u4EE5\u8FDB\u5165 review\u3002", "This figure passed the current-figure checks and is ready for review.");
  }
  if (status === "qa-needs-attention") {
    return figureText(responseLanguage, `\u8FD9\u5F20\u56FE\u5DF2\u7ECF\u5BFC\u5165\uFF0C\u4F46\u5F53\u524D\u56FE\u8FD8\u6709 ${figureQa.issueCount} \u4E2A\u9700\u8981\u4FEE\u7684\u95EE\u9898\u3002`, `This figure was imported, but it still has ${figureQa.issueCount} review issue(s) to handle first.`);
  }
  if (status === "blocked-missing-materials") {
    return missing ? figureText(responseLanguage, `\u8FD9\u5F20\u56FE\u8FD8\u7F3A\u6750\u6599\uFF1A${missing}\u3002`, `This figure is still missing materials: ${missing}.`) : figureText(responseLanguage, "\u8FD9\u5F20\u56FE\u8FD8\u7F3A\u6750\u6599\uFF0C\u6682\u65F6\u4E0D\u80FD\u7EE7\u7EED\u751F\u6210\u6216\u5BFC\u5165\u3002", "This figure is missing materials, so generation or import cannot continue yet.");
  }
  if (prepared.providerExecution?.status === "missing-secret-env") {
    return figureText(responseLanguage, `\u8FD9\u5F20\u56FE\u8FD8\u4E0D\u80FD\u8C03\u7528\u753B\u56FE\u670D\u52A1\uFF0C\u7F3A\u5C11 ${apiKeyEnv}\uFF1B\u4E5F\u53EF\u4EE5\u5148\u8D70\u624B\u5DE5 SVG \u5BFC\u5165\u3002`, `This figure cannot call the drawing provider yet because ${apiKeyEnv} is missing; manual SVG import is still available.`);
  }
  if (prepared.providerExecution?.status === "failed") {
    return figureText(responseLanguage, "\u8FD9\u5F20\u56FE\u7684\u753B\u56FE\u670D\u52A1\u751F\u6210\u5931\u8D25\u4E86\uFF1B\u53EF\u4EE5\u4FEE\u597D\u670D\u52A1\u540E\u91CD\u8BD5\uFF0C\u4E5F\u53EF\u4EE5\u76F4\u63A5\u5BFC\u5165\u624B\u5DE5 SVG\u3002", "Drawing-provider generation failed for this figure; fix the provider and retry, or import a manual SVG.");
  }
  if (!imported) {
    return figureText(responseLanguage, "\u8FD9\u5F20\u56FE\u7684\u8BA1\u5212\u548C\u6750\u6599\u5305\u5DF2\u7ECF\u51C6\u5907\u597D\uFF0C\u8FD8\u7F3A SVG \u8F93\u51FA\u3002", "This figure has its plan and material bundle ready, but still needs SVG output.");
  }
  return figureText(responseLanguage, "\u8FD9\u5F20\u56FE\u5DF2\u7ECF\u66F4\u65B0\u3002", "This figure was updated.");
}
function figureResultNextTitle(status, { prepared, responseLanguage }) {
  const missing = normalizeStringArray21(prepared.missingRequirementIds).join("\u3001");
  if (status === "validated") {
    return figureText(responseLanguage, "\u628A\u8FD9\u5F20\u56FE\u9001\u5165 review", "Send this figure to review");
  }
  if (status === "qa-needs-attention") {
    return figureText(responseLanguage, "\u5148\u5904\u7406\u5F53\u524D\u8FD9\u5F20\u56FE\u9700\u8981\u4FEE\u7684\u95EE\u9898", "Handle this figure's review issues first");
  }
  if (status === "blocked-missing-materials") {
    return missing ? figureText(responseLanguage, `\u5148\u8865\u8FD9\u5F20\u56FE\u7684\u6750\u6599\uFF1A${missing}`, `Provide this figure's missing materials: ${missing}`) : figureText(responseLanguage, "\u5148\u8865\u8FD9\u5F20\u56FE\u7F3A\u7684\u6750\u6599", "Provide this figure's missing materials first");
  }
  if (prepared.providerExecution?.status === "missing-secret-env") {
    const apiKeyEnv = prepared.providerExecution?.apiKeyEnv ?? prepared.providerReadiness?.apiKeyEnv ?? "provider API key";
    return figureText(responseLanguage, `\u914D\u7F6E ${apiKeyEnv}\uFF0C\u6216\u6539\u7528\u624B\u5DE5 SVG \u5BFC\u5165`, `Configure ${apiKeyEnv}, or use manual SVG import`);
  }
  if (prepared.providerExecution?.status === "failed") {
    return figureText(responseLanguage, "\u4FEE\u597D\u753B\u56FE\u670D\u52A1\u540E\u91CD\u8BD5\uFF0C\u6216\u76F4\u63A5\u5BFC\u5165\u624B\u5DE5 SVG", "Fix the drawing provider and retry, or import a manual SVG");
  }
  return figureText(responseLanguage, "\u63D0\u4F9B SVG \u8F93\u51FA\u540E\u518D\u5BFC\u5165", "Provide SVG output and import it");
}
function buildFigureResultCard({ status, figureId, runId, target, boundary, prepared, imported, figureQa, responseLanguage, writesApplied = true }) {
  const actionCommand = boundary?.nextAction ?? (status === "validated" ? "project:dove.review" : "project:dove.figure");
  const card = buildCommandResultCard({
    surface: "dove.figure",
    command: "run_figure_workflow",
    packetId: target.packetId,
    runId,
    title: figureText(responseLanguage, "\u56FE\u8868\u5DE5\u4F5C\u6D41\u7ED3\u679C", "Figure workflow result"),
    status,
    outcome: boundary?.type ?? status,
    happened: figureResultHappened(status, { figureId, prepared, figureQa, imported, responseLanguage }),
    durableWrites: !writesApplied || status === "blocked-missing-materials" && boundary?.detail?.implementationBoundaryType === "missing-figure-materials-before-plan" ? [] : imported ? [figureText(responseLanguage, "\u5DF2\u66F4\u65B0\u56FE\u8868\u8BA1\u5212\u3001caption provenance \u548C\u5F53\u524D\u56FE\u68C0\u67E5\u72B6\u6001\u3002", "Updated the figure plan, caption provenance, and this figure's check state.")] : [figureText(responseLanguage, "\u5DF2\u66F4\u65B0\u56FE\u8868\u8BA1\u5212\u548C\u751F\u6210\u6750\u6599\u5305\u3002", "Updated the figure plan and generation material bundle.")],
    boundary,
    scope: compactObject2({ kind: "figure", figureId, packetId: target.packetId }),
    nextActions: [{
      title: figureResultNextTitle(status, { prepared, responseLanguage }),
      why: boundary?.reason,
      command: actionCommand,
      boundary,
      boundaryType: boundary?.type,
      requiredInputs: boundary?.requiredInputs,
      requiredActions: boundary?.requiredActions,
      ownerRole: boundary?.ownerRole,
      nextRole: boundary?.nextRole
    }]
  }, responseLanguage);
  delete card.evidence;
  delete card.validation;
  delete card.codeChanges;
  return card;
}
function runFigureWorkflow(root, args = {}) {
  assertGovernanceMutationRegistered("run-figure-workflow", "guarded");
  assertFigurePublicInput(args);
  const target = assertTaskScopedMutationTarget(root, "run-figure-workflow", args);
  assertNoLegacyFinalSvgInput2(args, "run_figure_workflow");
  const { env: _env, svgContent: _svgContent, ...safeArgs } = args;
  assertNoInlineSecrets(safeArgs, "figureWorkflow.args");
  const { figureId, item, items } = buildFigureItem(root, args, target);
  const runId = slugify11(args.runId ?? `${figureId}-run`);
  if (figureNeedsPrePlanMaterialBoundary(args, item)) {
    const responseLanguage2 = resolveDoveResponseLanguage(root, args);
    const boundary2 = prePlanFigureBoundary(figureId, runId);
    const prepared2 = { missingRequirementIds: boundary2.requiredInputs, materialStatus: "needs-materials", providerExecution: null };
    const figureQa2 = { issueCount: 0, workspaceIssueCount: 0, qaPath: null };
    const resultCard2 = buildFigureResultCard({
      status: "blocked-missing-materials",
      figureId,
      runId,
      target,
      boundary: boundary2,
      prepared: prepared2,
      imported: null,
      figureQa: figureQa2,
      responseLanguage: responseLanguage2
    });
    return {
      status: "blocked-missing-materials",
      resultCard: resultCard2,
      figureId,
      runId,
      packetId: target.packetId,
      boundary: boundary2,
      boundaryType: boundary2.type,
      requiredActions: boundary2.requiredActions,
      artifactRefs: [],
      validationEvidencePaths: [],
      nextAction: boundary2.nextAction,
      plan: null,
      stageFiles: { templateCreated: false, editableCreated: false },
      materialStatus: "needs-materials",
      missingRequirementIds: boundary2.requiredInputs,
      imported: null,
      validation: null,
      figureQa: figureQa2,
      diagnostics: { providerReadiness: null, providerExecution: null, prepared: null },
      captionId: null,
      finalSvgPath: null,
      qaIssueCount: 0,
      workspaceQaIssueCount: 0,
      qaPath: null
    };
  }
  assertFollowThroughReady(root, "Running the figure workflow", args);
  ensureWorkspace(root);
  const plan = upsertFigurePlan(root, { packetId: target.packetId, items });
  const stageFiles = ensureStageSvgFiles(root, item);
  const executeProvider = args.executeProvider === true;
  const prepared = prepareFigureGeneration(root, {
    packetId: target.packetId,
    figureId,
    runId,
    providerId: args.providerId,
    constraints: args.constraints,
    outputFormat: args.outputFormat,
    ...Object.hasOwn(args, "materialHints") ? { materialHints: args.materialHints } : {},
    executeProvider,
    allowMissingMaterials: args.allowMissingMaterials,
    env: args.env
  });
  let imported = null;
  if (hasImportableOutput(args, prepared)) {
    imported = importFigureGeneration(root, {
      packetId: target.packetId,
      figureId,
      runId: prepared.runId,
      outputManifestPath: args.outputManifestPath,
      caption: args.caption,
      captionDraft: args.captionDraft,
      captionId: args.captionId,
      sourceSvgPath: args.sourceSvgPath,
      svgContent: args.svgContent,
      ...Object.hasOwn(args, "semanticCoverage") ? { semanticCoverage: args.semanticCoverage } : {},
      ...Object.hasOwn(args, "semanticReview") ? { semanticReview: args.semanticReview } : {},
      env: args.env
    });
  }
  const validation = validateFigurePipeline(root);
  const figureQa = summarizeFigureQa(root, figureId, validation);
  const status = statusFor(prepared, imported, figureQa);
  const artifactRefs = figureArtifactRefs(validation);
  const validationEvidencePaths = figureValidationEvidencePaths(figureQa);
  const boundary = figureBoundaryFor(status, prepared, validation, figureQa, figureId, prepared.runId);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const resultCard = buildFigureResultCard({ status, figureId, runId: prepared.runId, target, boundary, prepared, imported, figureQa, responseLanguage, writesApplied: !isPatchPlanMode(root) });
  const preActionGuidance = buildPreActionGuidance({
    surface: "dove.figure",
    responseLanguage,
    request: firstText(args.intent, args.description, args.summary, args.purpose, args.captionIntent, args.name, args.title),
    roleId: "builder",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "execute",
      primaryRole: "builder"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: boundary?.nextAction ?? (imported ? "project:dove.review" : "project:dove.figure"),
    routeHint: "project:dove.figure",
    workflowKind: "figure",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "execute",
    tags: ["figure", "artifact-provenance", "qa"],
    statusSummary: {
      status,
      materialStatus: prepared.materialStatus,
      missingRequirementCount: prepared.missingRequirementIds?.length ?? 0,
      qaIssueCount: figureQa.issueCount,
      workspaceQaIssueCount: figureQa.workspaceIssueCount,
      imported: Boolean(imported)
    }
  });
  return {
    status,
    resultCard,
    preActionGuidance,
    figureId,
    runId: prepared.runId,
    packetId: target.packetId,
    boundary,
    boundaryType: boundary?.type ?? null,
    requiredActions: boundary?.requiredActions ?? [],
    artifactRefs,
    validationEvidencePaths,
    nextAction: boundary?.nextAction ?? (imported ? "project:dove.review" : "project:dove.figure"),
    plan,
    stageFiles,
    materialStatus: prepared.materialStatus,
    missingRequirementIds: prepared.missingRequirementIds,
    imported,
    validation,
    figureQa,
    diagnostics: {
      providerReadiness: prepared.providerReadiness,
      providerExecution: prepared.providerExecution,
      prepared
    },
    captionId: imported?.captionId ?? null,
    finalSvgPath: imported?.finalSvgPath ?? null,
    qaIssueCount: figureQa.issueCount,
    workspaceQaIssueCount: figureQa.workspaceIssueCount,
    qaPath: figureQa.qaPath
  };
}

// src/core/task-workflow.mjs
import crypto13 from "node:crypto";
import fs23 from "node:fs";
import path26 from "node:path";

// src/core/experience-workflow.mjs
function slugify12(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "experience";
}
function normalizeString9(value, fallback = "") {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray22(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean))) : [];
}
function hasNonEmptyString2(value) {
  return typeof value === "string" && value.trim();
}
var EXPERIENCE_PLAN_INPUT_KEYS = /* @__PURE__ */ new Set([
  "id",
  "experimentId",
  "goal",
  "idea",
  "title",
  "methodology",
  "method",
  "successMetric",
  "metric",
  "comparisonTargets",
  "baselines",
  "claimId"
]);
var EXPERIENCE_PLAN_STRING_KEYS = /* @__PURE__ */ new Set([
  "id",
  "experimentId",
  "goal",
  "idea",
  "title",
  "methodology",
  "method",
  "successMetric",
  "metric",
  "claimId"
]);
var EXPERIENCE_RESULT_INPUT_KEYS = /* @__PURE__ */ new Set([
  "id",
  "resultId",
  "experimentId",
  "claimId",
  "outcome",
  "summary",
  "resultSummary",
  "evidenceLinks",
  "artifactPaths",
  "comparisonTargets"
]);
var EXPERIENCE_RESULT_STRING_KEYS = /* @__PURE__ */ new Set([
  "id",
  "resultId",
  "experimentId",
  "claimId",
  "outcome",
  "summary",
  "resultSummary"
]);
function assertExperiencePlainObject(value, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`run_experience_workflow requires an object at ${label}.`);
  }
}
function assertExperienceString(value, label) {
  if (typeof value !== "string") {
    throw new Error(`run_experience_workflow requires a string at ${label}.`);
  }
}
function assertExperienceStringArray(value, label) {
  if (!Array.isArray(value)) {
    throw new Error(`run_experience_workflow requires an array at ${label}.`);
  }
  value.forEach((item, index) => assertExperienceString(item, `${label}[${index}]`));
}
function assertExperienceNestedInput(value, allowedKeys, stringKeys, arrayKeys, label) {
  assertExperiencePlainObject(value, label);
  const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
  if (unknown.length > 0) {
    throw new Error(`run_experience_workflow does not accept unknown input ${unknown.map((key) => `${label}.${key}`).join(", ")}.`);
  }
  for (const key of stringKeys) {
    if (Object.hasOwn(value, key)) {
      assertExperienceString(value[key], `${label}.${key}`);
    }
  }
  for (const key of arrayKeys) {
    if (Object.hasOwn(value, key)) {
      assertExperienceStringArray(value[key], `${label}.${key}`);
    }
  }
}
function assertExperiencePublicInput(args = {}) {
  if (Object.hasOwn(args, "plan")) {
    assertExperienceNestedInput(
      args.plan,
      EXPERIENCE_PLAN_INPUT_KEYS,
      EXPERIENCE_PLAN_STRING_KEYS,
      /* @__PURE__ */ new Set(["comparisonTargets", "baselines"]),
      "$.plan"
    );
  }
  if (Object.hasOwn(args, "result")) {
    assertExperienceNestedInput(
      args.result,
      EXPERIENCE_RESULT_INPUT_KEYS,
      EXPERIENCE_RESULT_STRING_KEYS,
      /* @__PURE__ */ new Set(["evidenceLinks", "artifactPaths", "comparisonTargets"]),
      "$.result"
    );
  }
}
function hasExperienceObjective(rawPlan = {}, args = {}) {
  return [rawPlan.experimentId, rawPlan.id, rawPlan.goal, rawPlan.idea, args.idea, rawPlan.title].some(hasNonEmptyString2);
}
function localizedText4(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function publicIntegrityActions(flags = [], responseLanguage = "zh") {
  const labels = {
    "missing-claim-link": localizedText4(responseLanguage, "\u8865\u4E0A\u8981\u9A8C\u8BC1\u7684\u8BBA\u70B9\u3002", "Add the claim this result should support."),
    "missing-evidence-links": localizedText4(responseLanguage, "\u8865\u4E0A\u53EF\u6838\u67E5\u7684\u5B9E\u9A8C\u7ED3\u679C\u6750\u6599\u3002", "Attach checkable experiment evidence."),
    "missing-methodology": localizedText4(responseLanguage, "\u8BF4\u660E\u5B9E\u9A8C\u65B9\u6CD5\u3002", "Describe the experiment method."),
    "missing-success-metric": localizedText4(responseLanguage, "\u8BF4\u660E\u6210\u529F\u6307\u6807\u3002", "Describe the success metric."),
    "missing-result-summary": localizedText4(responseLanguage, "\u8865\u4E00\u6BB5\u7ED3\u679C\u6458\u8981\u3002", "Add a result summary."),
    "pending-outcome": localizedText4(responseLanguage, "\u7ED9\u51FA\u660E\u786E\u5B9E\u9A8C\u7ED3\u8BBA\u3002", "Record a concrete experiment outcome."),
    "missing-evidence-file": localizedText4(responseLanguage, "\u63D0\u4F9B\u5B58\u5728\u7684\u672C\u5730\u5B9E\u9A8C\u8BC1\u636E\u6587\u4EF6\u3002", "Provide an existing local experiment evidence file."),
    "empty-evidence-file": localizedText4(responseLanguage, "\u628A\u7A7A\u5B9E\u9A8C\u8BC1\u636E\u6587\u4EF6\u66FF\u6362\u4E3A\u771F\u5B9E\u7ED3\u679C\u6750\u6599\u3002", "Replace empty experiment evidence with real result material."),
    "directory-evidence-file": localizedText4(responseLanguage, "\u5F15\u7528\u5177\u4F53\u5B9E\u9A8C\u8BC1\u636E\u6587\u4EF6\uFF0C\u4E0D\u8981\u5F15\u7528\u76EE\u5F55\u3002", "Reference a concrete evidence file, not a directory."),
    "unsafe-evidence-path": localizedText4(responseLanguage, "\u4F7F\u7528\u9879\u76EE\u5185\u76F8\u5BF9\u8BC1\u636E\u8DEF\u5F84\u3002", "Use a project-relative evidence path."),
    "bookkeeping-evidence-file": localizedText4(responseLanguage, "\u6539\u7528\u771F\u5B9E\u5B9E\u9A8C\u7ED3\u679C\u6587\u4EF6\uFF0C\u4E0D\u8981\u7528\u72B6\u6001\u3001\u5BFC\u822A\u6216 ledger \u8BB0\u5F55\u3002", "Use real result files instead of status, navigation, or ledger records."),
    "unsupported-evidence-file": localizedText4(responseLanguage, "\u5F15\u7528\u666E\u901A\u6587\u4EF6\u5F62\u5F0F\u7684\u5B9E\u9A8C\u8BC1\u636E\u3002", "Reference a regular file as experiment evidence."),
    "unreadable-evidence-file": localizedText4(responseLanguage, "\u4FEE\u590D\u4E0D\u53EF\u8BFB\u53D6\u7684\u5B9E\u9A8C\u8BC1\u636E\u6587\u4EF6\u3002", "Fix unreadable experiment evidence.")
  };
  return normalizeStringArray22(flags.map((flag) => labels[flag] ?? localizedText4(responseLanguage, "\u8865\u9F50\u5B9E\u9A8C\u5BA1\u8BA1\u6307\u51FA\u7684\u7F3A\u53E3\u3002", "Fill the gap raised by experiment review.")));
}
function publicExperienceBoundary(boundary, audit, responseLanguage = "zh") {
  if (!boundary) {
    return null;
  }
  const heldForMissingClaim = normalizeStringArray22(boundary.requiredActions).includes("create-or-link-claim-before-bridge");
  const integrityFlags = Array.isArray(audit?.integrityFlags) && audit.integrityFlags.length > 0 ? audit.integrityFlags : boundary.requiredInputs;
  return {
    type: boundary.type,
    summary: heldForMissingClaim ? localizedText4(responseLanguage, "\u5B9E\u9A8C\u6750\u6599\u68C0\u67E5\u5DF2\u901A\u8FC7\uFF0C\u4F46\u76EE\u6807\u8BBA\u70B9\u4E0D\u5B58\u5728\uFF0C\u56E0\u6B64\u7ED3\u679C\u5C1A\u672A\u6865\u63A5\u5230\u8BBA\u70B9\u3002", "The experiment material check passed, but the target claim does not exist, so the result has not been bridged to a claim.") : boundary.type === "needs-review" ? localizedText4(responseLanguage, "\u5B9E\u9A8C\u7ED3\u679C\u8FD8\u9700\u8981 review \u5224\u65AD\u662F\u5426\u80FD\u652F\u6491\u8BBA\u70B9\u3002", "The experiment result still needs review before it can support the claim.") : localizedText4(responseLanguage, "\u5B9E\u9A8C\u6750\u6599\u8FD8\u4E0D\u591F\uFF0C\u4E0D\u80FD\u76F4\u63A5\u63A8\u8FDB\u6210\u8BBA\u70B9\u8BC1\u636E\u3002", "The experiment material is not complete enough to promote as claim evidence."),
    requiredActions: heldForMissingClaim ? [localizedText4(responseLanguage, "\u5148\u521B\u5EFA\u76EE\u6807\u8BBA\u70B9\uFF0C\u6216\u628A\u7ED3\u679C\u6539\u94FE\u5230\u5DF2\u6709\u8BBA\u70B9\uFF0C\u518D\u91CD\u8BD5\u6865\u63A5\u3002", "Create the target claim or link the result to an existing claim before retrying the bridge.")] : publicIntegrityActions(integrityFlags, responseLanguage)
  };
}
function renderClaimsMarkdown3(claims = []) {
  return [
    "# Claims From Results",
    "",
    ...claims.flatMap((claim) => [
      `## ${claim.id}`,
      "",
      `- Status: ${claim.status ?? "unknown"}`,
      `- Confidence: ${claim.confidence ?? "unknown"}`,
      `- Summary: ${claim.summary ?? claim.statement ?? "No summary recorded."}`,
      `- Experiments: ${(claim.experimentIds ?? []).join(", ") || "none"}`,
      ""
    ])
  ].join("\n");
}
function normalizeOutcome(value) {
  return ["supports", "refutes", "inconclusive", "failed", "pending"].includes(value) ? value : "pending";
}
function upsertById(items, item) {
  const existing = items.findIndex((entry) => entry.id === item.id);
  if (existing >= 0) {
    items[existing] = { ...items[existing], ...item };
  } else {
    items.push(item);
  }
  return items;
}
function requiredActionsForIntegrityFlags(flags = []) {
  const actions = {
    "missing-claim-link": "link-result-to-claim-or-create-claim",
    "missing-evidence-links": "attach-experiment-evidence",
    "missing-methodology": "provide-experiment-methodology",
    "missing-success-metric": "provide-success-metric",
    "missing-result-summary": "provide-result-summary",
    "pending-outcome": "provide-concrete-outcome",
    "missing-evidence-file": "attach-existing-experiment-evidence-file",
    "empty-evidence-file": "replace-empty-experiment-evidence",
    "directory-evidence-file": "attach-file-not-directory",
    "unsafe-evidence-path": "use-project-relative-evidence-path",
    "bookkeeping-evidence-file": "attach-substantive-experiment-artifact",
    "unsupported-evidence-file": "attach-regular-evidence-file",
    "unreadable-evidence-file": "fix-unreadable-experiment-evidence"
  };
  return normalizeStringArray22(flags.map((flag) => actions[flag] ?? `resolve-${flag}`));
}
function experienceBoundaryFor({ status, plan, result, audit, bridge, artifactRefs }) {
  if (audit?.auditVerdict === "blocked") {
    const requiredActions = requiredActionsForIntegrityFlags(audit.integrityFlags);
    const materialFlags = ["missing-claim-link", "missing-evidence-links", "missing-evidence-file", "empty-evidence-file", "directory-evidence-file", "unsafe-evidence-path", "bookkeeping-evidence-file", "unsupported-evidence-file", "unreadable-evidence-file", "missing-methodology", "missing-success-metric", "missing-result-summary"];
    const hasMissingMaterials = audit.integrityFlags.some((flag) => materialFlags.includes(flag));
    return {
      id: `${result?.id ?? plan.id}-audit-blocked`,
      type: hasMissingMaterials ? "missing-required-materials" : "needs-review",
      reason: `Experiment audit ${audit.id} is blocked: ${audit.integrityFlags.join(", ")}.`,
      requiredInputs: audit.integrityFlags,
      requiredActions,
      artifactRefs,
      nextAction: hasMissingMaterials ? "project:dove.experience" : "project:dove.review",
      ownerRole: "builder",
      nextRole: hasMissingMaterials ? "builder" : "reviewer"
    };
  }
  if (bridge?.status === "held-missing-claim") {
    return {
      id: `${bridge.id}-missing-claim`,
      type: "missing-required-materials",
      reason: bridge.reason,
      requiredInputs: [bridge.claimId],
      requiredActions: ["create-or-link-claim-before-bridge"],
      artifactRefs,
      nextAction: "project:dove.experience",
      ownerRole: "builder",
      nextRole: "builder"
    };
  }
  if (bridge?.status === "held-audit-blocked") {
    return {
      id: `${bridge.id}-audit-blocked`,
      type: "needs-review",
      reason: bridge.reason,
      requiredInputs: audit?.integrityFlags ?? [],
      requiredActions: ["resolve-experiment-audit-flags"],
      artifactRefs,
      nextAction: "project:dove.review",
      ownerRole: "builder",
      nextRole: "reviewer"
    };
  }
  if (status === "recorded" && result && !bridge) {
    return {
      id: `${result.id}-bridge-required`,
      type: "missing-required-materials",
      reason: `Experiment result ${result.id} is recorded but not linked to a durable claim bridge.`,
      requiredInputs: ["claimId"],
      requiredActions: ["link-result-to-claim-or-create-claim"],
      artifactRefs,
      nextAction: "project:dove.experience",
      ownerRole: "builder",
      nextRole: "builder"
    };
  }
  return null;
}
function runExperienceWorkflow(root, args = {}) {
  assertGovernanceMutationRegistered("run-experience-workflow", "guarded");
  assertExperiencePublicInput(args);
  const target = assertTaskScopedMutationTarget(root, "run-experience-workflow", args);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Running the experience workflow"
  );
  ensureWorkspace(root);
  const timestamp = nowIso();
  const rawPlan = args.plan && typeof args.plan === "object" ? args.plan : args;
  const resultInput = args.result && typeof args.result === "object" ? args.result : args.outcome || args.resultSummary || args.summary || normalizeStringArray22(args.evidenceLinks ?? args.artifactPaths).length > 0 ? args : null;
  if (!hasExperienceObjective(rawPlan, args)) {
    throw new Error("run_experience_workflow requires an experiment goal, title, idea, or experimentId.");
  }
  const experimentId = slugify12(rawPlan.experimentId ?? rawPlan.id ?? rawPlan.goal ?? rawPlan.title);
  const claimId = normalizeString9(rawPlan.claimId ?? args.claimId, null);
  const plan = {
    id: experimentId,
    packetId: target.packetId,
    title: normalizeString9(rawPlan.title ?? rawPlan.goal, experimentId.replace(/-/g, " ")),
    goal: normalizeString9(rawPlan.goal ?? rawPlan.idea ?? args.idea ?? rawPlan.title, ""),
    methodology: normalizeString9(rawPlan.methodology ?? rawPlan.method, ""),
    successMetric: normalizeString9(rawPlan.successMetric ?? rawPlan.metric, ""),
    comparisonTargets: normalizeStringArray22(rawPlan.comparisonTargets ?? rawPlan.baselines),
    claimId,
    status: "planned",
    createdAt: timestamp,
    updatedAt: timestamp
  };
  if (!resultInput && (!plan.methodology || !plan.successMetric)) {
    const missing = [!plan.methodology ? "missing-methodology" : null, !plan.successMetric ? "missing-success-metric" : null].filter(Boolean);
    const responseLanguage2 = resolveDoveResponseLanguage(root, args);
    const boundary2 = {
      id: `${experimentId}-plan-materials`,
      type: "missing-required-materials",
      reason: `Experiment plan ${experimentId} needs methodology and successMetric before it can be recorded as real progress: ${missing.join(", ")}.`,
      requiredInputs: missing,
      requiredActions: requiredActionsForIntegrityFlags(missing),
      artifactRefs: [],
      nextAction: "project:dove.experience",
      ownerRole: "builder",
      nextRole: "builder"
    };
    const resultCard2 = buildCommandResultCard({
      surface: "dove.experience",
      command: "run_experience_workflow",
      title: localizedText4(responseLanguage2, "\u5B9E\u9A8C\u6750\u6599\u4E0D\u8DB3", "Experiment material missing"),
      status: "missing-required-materials",
      happened: localizedText4(responseLanguage2, "\u6CA1\u6709\u5199\u5165\u5B9E\u9A8C\u8BA1\u5212\uFF1Bmethodology \u548C successMetric \u662F\u6700\u4F4E\u6750\u6599\u8FB9\u754C\u3002", "No experiment plan was written; methodology and successMetric are the minimum material boundary."),
      durableWrites: [],
      boundary: publicExperienceBoundary(boundary2, { integrityFlags: missing }, responseLanguage2),
      nextActions: [{
        title: localizedText4(responseLanguage2, "\u8865\u5B9E\u9A8C\u65B9\u6CD5\u548C\u6210\u529F\u6307\u6807", "Add method and success metric"),
        why: localizedText4(responseLanguage2, "\u53EA\u6709\u9898\u76EE\u6216 idea \u53EA\u662F\u5360\u4F4D\uFF0C\u4E0D\u80FD\u7B97\u5B9E\u9A8C\u63A8\u8FDB\u3002", "A title or idea alone is a placeholder, not experiment progress.")
      }]
    }, responseLanguage2);
    return {
      status: "missing-required-materials",
      packetId: target.packetId,
      plan,
      result: null,
      audit: null,
      bridge: null,
      boundary: boundary2,
      boundaryType: boundary2.type,
      requiredActions: boundary2.requiredActions,
      artifactRefs: [],
      validationEvidencePaths: [],
      nextAction: boundary2.nextAction,
      artifacts: [],
      resultCard: resultCard2
    };
  }
  const plansIndex = readJson(root, ARTIFACT_PATHS.experimentPlans, { version: 1, items: [], updatedAt: null });
  plansIndex.items = upsertById(Array.isArray(plansIndex.items) ? plansIndex.items : [], plan);
  plansIndex.updatedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.experimentPlans, plansIndex);
  let result = null;
  let audit = null;
  let bridge = null;
  if (resultInput) {
    const resultsIndex = readJson(root, ARTIFACT_PATHS.experimentResults, { version: 1, items: [], updatedAt: null });
    result = {
      id: slugify12(resultInput.resultId ?? resultInput.id ?? `${experimentId}-result`),
      packetId: target.packetId,
      experimentId,
      claimId: normalizeString9(resultInput.claimId ?? claimId, null),
      outcome: normalizeOutcome(resultInput.outcome),
      summary: normalizeString9(resultInput.summary ?? resultInput.resultSummary, ""),
      evidenceLinks: normalizeStringArray22(resultInput.evidenceLinks ?? resultInput.artifactPaths),
      comparisonTargets: normalizeStringArray22(resultInput.comparisonTargets),
      createdAt: timestamp,
      updatedAt: timestamp
    };
    resultsIndex.items = upsertById(Array.isArray(resultsIndex.items) ? resultsIndex.items : [], result);
    resultsIndex.updatedAt = timestamp;
    writeJson(root, ARTIFACT_PATHS.experimentResults, resultsIndex);
    const evidenceIntegrity = evidencePathProblemFlags(root, result.evidenceLinks);
    const integrityFlags = [];
    if (!result.claimId) integrityFlags.push("missing-claim-link");
    integrityFlags.push(...evidenceIntegrity.flags);
    if (!plan.methodology) integrityFlags.push("missing-methodology");
    if (!plan.successMetric) integrityFlags.push("missing-success-metric");
    if (!result.summary) integrityFlags.push("missing-result-summary");
    if (result.outcome === "pending") integrityFlags.push("pending-outcome");
    const uniqueIntegrityFlags = Array.from(new Set(integrityFlags));
    audit = {
      id: slugify12(`${result.id}-audit`),
      packetId: target.packetId,
      experimentId,
      resultId: result.id,
      claimId: result.claimId,
      auditVerdict: uniqueIntegrityFlags.length === 0 ? "clean" : "blocked",
      integrityFlags: uniqueIntegrityFlags,
      auditFindings: uniqueIntegrityFlags.map((flag) => `Experience workflow flagged ${flag}.`),
      evidencePathIntegrity: evidenceIntegrity.pathEvidence,
      bridgeReadiness: uniqueIntegrityFlags.length === 0 ? "ready" : "blocked",
      updatedAt: timestamp
    };
    const auditsIndex = readJson(root, ARTIFACT_PATHS.experimentAudits, { version: 1, items: [], updatedAt: null });
    auditsIndex.items = upsertById(Array.isArray(auditsIndex.items) ? auditsIndex.items : [], audit);
    auditsIndex.updatedAt = timestamp;
    writeJson(root, ARTIFACT_PATHS.experimentAudits, auditsIndex);
    if (result.claimId) {
      const evidence = readJson(root, ARTIFACT_PATHS.evidence, { version: 3, claims: [], updatedAt: null });
      const claimIndex = (evidence.claims ?? []).findIndex((claim) => claim.id === result.claimId);
      const claimExists = claimIndex >= 0;
      const bridgeReady = claimExists && audit.auditVerdict === "clean";
      const nextStatus = audit.auditVerdict === "clean" && result.outcome === "supports" ? "supported" : audit.auditVerdict === "clean" && ["refutes", "failed"].includes(result.outcome) ? "refuted" : "needs-review";
      bridge = {
        id: slugify12(`${result.id}-bridge`),
        packetId: target.packetId,
        experimentId,
        resultId: result.id,
        claimId: result.claimId,
        status: bridgeReady ? "applied" : claimExists ? "held-audit-blocked" : "held-missing-claim",
        mapping: result.outcome,
        auditId: audit.id,
        claimStateAfter: bridgeReady ? nextStatus : claimExists ? "needs-review" : null,
        reason: bridgeReady ? result.summary : claimExists ? `Audit ${audit.id} is blocked: ${audit.integrityFlags.join(", ")}.` : `Claim ${result.claimId} does not exist yet.`,
        updatedAt: timestamp
      };
      const bridgeLog = readJson(root, ARTIFACT_PATHS.claimBridgeLog, { version: 1, items: [], updatedAt: null });
      bridgeLog.items = upsertById(Array.isArray(bridgeLog.items) ? bridgeLog.items : [], bridge);
      bridgeLog.updatedAt = timestamp;
      writeJson(root, ARTIFACT_PATHS.claimBridgeLog, bridgeLog);
      if (bridgeReady) {
        evidence.claims[claimIndex] = {
          ...evidence.claims[claimIndex],
          status: nextStatus,
          confidence: nextStatus === "supported" ? "high" : "low",
          experimentIds: Array.from(/* @__PURE__ */ new Set([...evidence.claims[claimIndex].experimentIds ?? [], experimentId])),
          latestBridgeId: bridge.id,
          latestAuditId: audit.id
        };
        evidence.updatedAt = timestamp;
        writeJson(root, ARTIFACT_PATHS.evidence, evidence);
        writeText(root, ARTIFACT_PATHS.claims, renderClaimsMarkdown3(evidence.claims));
      }
    }
  }
  const artifactRefs = [ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits, ARTIFACT_PATHS.claimBridgeLog];
  const status = result ? audit?.auditVerdict === "clean" && bridge?.status === "applied" ? "bridged" : audit?.auditVerdict === "clean" && !bridge ? "recorded" : "needs-review" : "planned";
  const boundary = experienceBoundaryFor({ status, plan, result, audit, bridge, artifactRefs });
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const preActionGuidance = buildPreActionGuidance({
    surface: "dove.experience",
    responseLanguage,
    request: normalizeString9(rawPlan.goal ?? rawPlan.idea ?? rawPlan.title ?? args.idea, null),
    roleId: "builder",
    subagentSpecialty: "experiment-planner",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "execute",
      primaryRole: "builder"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: boundary?.nextAction ?? (result ? "project:dove.review" : "project:dove.experience"),
    routeHint: "project:dove.experience",
    workflowKind: "experience",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "execute",
    tags: ["experiment", "claim-bridge", "audit"],
    statusSummary: {
      status,
      experimentId,
      hasResult: Boolean(result),
      auditVerdict: audit?.auditVerdict ?? null,
      bridgeStatus: bridge?.status ?? null
    }
  });
  const resultCard = buildCommandResultCard({
    surface: "dove.experience",
    command: "run_experience_workflow",
    title: localizedText4(responseLanguage, "\u5B9E\u9A8C\u8BB0\u5F55\u5DF2\u66F4\u65B0", "Experience workflow updated"),
    status,
    happened: result ? localizedText4(responseLanguage, "\u5DF2\u8BB0\u5F55\u5B9E\u9A8C\u8BA1\u5212\u3001\u7ED3\u679C\uFF0C\u5E76\u5B8C\u6210\u8BC1\u636E\u5B8C\u6574\u6027\u68C0\u67E5\u3002", "Recorded the experiment plan and result, then checked evidence readiness.") : localizedText4(responseLanguage, "\u5DF2\u8BB0\u5F55\u5B9E\u9A8C\u8BA1\u5212\uFF0C\u7B49\u5F85\u7ED3\u679C\u6750\u6599\u3002", "Recorded the experiment plan and is waiting for result material."),
    durableWrites: [localizedText4(responseLanguage, "\u5B9E\u9A8C\u8BA1\u5212\u3001\u7ED3\u679C\u3001\u5BA1\u8BA1\u548C\u8BBA\u70B9\u8854\u63A5\u72B6\u6001\u5DF2\u66F4\u65B0\u3002", "Experiment plan, result, review, and claim-link state were updated.")],
    evidence: result?.summary ? [localizedText4(responseLanguage, "\u5DF2\u8BB0\u5F55\u5B9E\u9A8C\u7ED3\u679C\u6458\u8981\u3002", "Experiment result summary recorded.")] : [],
    validation: audit ? [audit.auditVerdict === "clean" ? localizedText4(responseLanguage, "\u5B9E\u9A8C\u6750\u6599\u68C0\u67E5\u901A\u8FC7\u3002", "Experiment material check passed.") : localizedText4(responseLanguage, "\u5B9E\u9A8C\u6750\u6599\u68C0\u67E5\u53D1\u73B0\u7F3A\u53E3\u3002", "Experiment material check found gaps.")] : [],
    boundary: publicExperienceBoundary(boundary, audit, responseLanguage),
    scope: {
      goal: plan.goal,
      outcome: result?.outcome ?? null,
      review: audit?.auditVerdict ?? null,
      claimImpact: bridge?.status === "applied" ? localizedText4(responseLanguage, "\u5DF2\u5F71\u54CD\u8BBA\u70B9\u72B6\u6001", "Claim state updated") : null
    },
    nextActions: [{
      title: bridge?.status === "held-missing-claim" ? localizedText4(responseLanguage, "\u5148\u521B\u5EFA\u6216\u91CD\u94FE\u76EE\u6807\u8BBA\u70B9", "Create or relink the target claim first") : boundary ? localizedText4(responseLanguage, "\u5148\u8865\u9F50\u5B9E\u9A8C\u6750\u6599", "Fill the experiment material gaps first") : result ? localizedText4(responseLanguage, "\u8FDB\u5165 review \u68C0\u67E5\u652F\u6491\u529B\u5EA6", "Review the support strength next") : localizedText4(responseLanguage, "\u8865\u5B9E\u9A8C\u7ED3\u679C\u548C\u8BC1\u636E", "Add experiment result and evidence"),
      why: bridge?.status === "held-missing-claim" ? localizedText4(responseLanguage, "\u6750\u6599\u5BA1\u8BA1\u5DF2\u901A\u8FC7\uFF0C\u4F46\u7F3A\u5C11\u53EF\u63A5\u6536\u8BE5\u7ED3\u679C\u7684\u6301\u4E45\u8BBA\u70B9\uFF0C\u5F53\u524D\u4E0D\u80FD\u7B97\u5DF2\u6865\u63A5\u5B8C\u6210\u3002", "The material audit passed, but no durable claim exists to receive the result, so the bridge is not complete.") : boundary ? localizedText4(responseLanguage, "\u5F53\u524D\u7ED3\u679C\u8FD8\u4E0D\u80FD\u5B89\u5168\u652F\u6491\u8BBA\u6587\u8BBA\u70B9\u3002", "The current result cannot safely support the paper claim yet.") : localizedText4(responseLanguage, "\u5B9E\u9A8C\u8BB0\u5F55\u5DF2\u7ECF\u53EF\u7528\u4E8E\u4E0B\u4E00\u6B65\u5224\u65AD\uFF0C\u4F46\u8FD8\u9700\u8981 review \u786E\u8BA4\u4E0D\u8981\u8FC7\u5EA6\u4E3B\u5F20\u3002", "The experiment record is ready for the next decision, but review should confirm it is not over-claimed.")
    }]
  }, responseLanguage);
  return {
    status,
    preActionGuidance,
    resultCard,
    packetId: target.packetId,
    plan,
    result,
    audit,
    bridge,
    boundary,
    boundaryType: boundary?.type ?? null,
    requiredActions: boundary?.requiredActions ?? [],
    artifactRefs,
    validationEvidencePaths: audit ? [ARTIFACT_PATHS.experimentAudits] : [],
    nextAction: boundary?.nextAction ?? (result ? "project:dove.review" : "project:dove.experience"),
    artifacts: artifactRefs
  };
}

// src/core/dove-review-loop.mjs
function slugify13(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "review-loop";
}
function normalizeStringArray23(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => typeof item === "string" ? item.trim() : "").filter(Boolean))) : [];
}
function hasNonEmptyString3(value) {
  return typeof value === "string" && value.trim();
}
var RETIRED_REVIEW_LOOP_FIELDS = /* @__PURE__ */ new Set(["maxIterations", "draft", "draftBody", "sectionId", "experience", "experienceGoal", "finalPlanPaths", "finalResultPaths"]);
function assertNoRetiredReviewLoopControls(value, inputPath = "$", seen = /* @__PURE__ */ new WeakSet()) {
  if (!value || typeof value !== "object") {
    return;
  }
  if (seen.has(value)) {
    return;
  }
  seen.add(value);
  for (const [key, item] of Object.entries(value)) {
    const itemPath = Array.isArray(value) ? `${inputPath}[${key}]` : `${inputPath}.${key}`;
    if (RETIRED_REVIEW_LOOP_FIELDS.has(key) || key.startsWith("policyOverride") || key === "skipBoardUpdate" || key === "skipRefreshDurableSurfaces" || key === "skipFollowThroughReady" || key === "skipSyncPhase") {
      throw new Error(
        `run_dove_review_loop no longer accepts retired governance input ${key} at ${itemPath}.`
      );
    }
    assertNoRetiredReviewLoopControls(item, itemPath, seen);
  }
}
function localizedText5(responseLanguage, zh, en) {
  return responseLanguage === "en" ? en : zh;
}
function reviewLoopStopSummary(status, stopReason, responseLanguage) {
  if (status === "coherent") {
    return localizedText5(responseLanguage, "review-loop \u5DF2\u786E\u8BA4\u5F53\u524D\u6750\u6599\u57FA\u672C\u81EA\u6D3D\u3002", "The review loop found the current materials coherent.");
  }
  if (stopReason === "review-proof-required") {
    return localizedText5(responseLanguage, "review-loop \u505C\u5728 Reviewer-owned proof \u8FB9\u754C\uFF1B\u5C1A\u672A\u6388\u6743 Builder \u4FEE\u8BA2\u3002", "The review loop stopped at a Reviewer-owned proof boundary; no Builder revision is authorized.");
  }
  if (stopReason === "builder-revision-required") {
    return localizedText5(responseLanguage, "review-loop \u5DF2\u5B8C\u6210 Reviewer \u68C0\u67E5\uFF0C\u5E76\u505C\u5728\u663E\u5F0F Builder \u4FEE\u8BA2\u8FB9\u754C\uFF1B\u6CA1\u6709\u6539\u5199\u8349\u7A3F\u6216\u5B9E\u9A8C\u6750\u6599\u3002", "The review loop completed the Reviewer pass and stopped at an explicit Builder revision boundary without changing draft or experiment material.");
  }
  if (status === "blocked") {
    return localizedText5(responseLanguage, "review-loop \u505C\u5728\u9700\u8981\u4FEE\u590D\u7684\u95EE\u9898\u4E0A\u3002", "The review loop stopped on an issue that needs repair.");
  }
  return localizedText5(responseLanguage, "review-loop \u5DF2\u7528\u5B8C\u672C\u8F6E\u9884\u7B97\uFF0C\u9700\u8981\u4EBA\u5DE5\u51B3\u5B9A\u4E0B\u4E00\u6B65\u3002", "The review loop used its iteration budget and needs an operator decision.");
}
function packetBuilderRoute(packet = {}) {
  const route = hasNonEmptyString3(packet.nextAction) ? packet.nextAction.trim() : "";
  return route && route !== "project:dove.mission" ? route : "project:dove.auto";
}
function buildBuilderRevisionBoundary({ runId, packetId, packet, review, draftRequested, experienceRequested }) {
  if (review.verdict === "coherent" && !draftRequested && !experienceRequested) {
    return null;
  }
  const reviewActions = review.verdict === "coherent" ? [] : normalizeStringArray23(review.actionItems);
  const proofBoundary = (review.findings ?? []).some((finding) => finding.methodologicalCategory === "review-proof");
  const requiredActions = proofBoundary ? ["prepare-current-artifacts-for-isolated-review", "import-current-hash-bound-isolated-review-proof"] : normalizeStringArray23([
    "transfer-reviewer-owned-board-to-builder-before-revision",
    ...reviewActions,
    draftRequested ? "apply-supplied-draft-in-builder-owned-pass" : "",
    experienceRequested ? "run-supplied-experience-in-builder-owned-pass" : "",
    "return-revised-material-for-review"
  ]);
  const requestedWork = [draftRequested ? "draft" : null, experienceRequested ? "experience" : null].filter(Boolean);
  const reason = requestedWork.length > 0 ? `Reviewer pass completed before the requested ${requestedWork.join(" and ")} work. No builder-owned material was changed while the board remained reviewer-owned.` : `Reviewer pass found ${reviewActions.length} action item(s) and stopped before any builder revision.`;
  return {
    id: `${runId}-builder-revision`,
    type: "fix-required",
    status: "open",
    packetId,
    runId,
    sourceSurface: "dove.review-loop",
    command: "run_dove_review_loop",
    reason,
    summary: proofBoundary ? "Keep the boundary Reviewer-owned and obtain an isolated review proof for the current artifact hashes." : "Explicitly transfer ownership to a Builder revision pass, apply the required changes there, and return the revised material for another Reviewer pass.",
    requiredInputs: proofBoundary ? ["current-hash-bound-isolated-review-proof"] : [],
    requiredActions,
    ownerRole: "reviewer",
    nextRole: proofBoundary ? "reviewer" : "builder",
    nextAction: proofBoundary ? "project:dove.review" : packetBuilderRoute(packet),
    requestedWork,
    writes: []
  };
}
function publicBuilderRevisionActions({ draftRequested, experienceRequested }, responseLanguage) {
  return [
    localizedText5(responseLanguage, "\u5148\u663E\u5F0F\u628A\u4FEE\u8BA2\u5DE5\u4F5C\u4EA4\u7ED9 Builder\u3002", "Explicitly hand the revision work to a Builder first."),
    draftRequested ? localizedText5(responseLanguage, "\u5728 Builder pass \u4E2D\u5E94\u7528\u63D0\u4F9B\u7684\u8349\u7A3F\u5185\u5BB9\u3002", "Apply the supplied draft content in the Builder pass.") : null,
    experienceRequested ? localizedText5(responseLanguage, "\u5728 Builder/experiment pass \u4E2D\u8FD0\u884C\u63D0\u4F9B\u7684\u5B9E\u9A8C\u5DE5\u4F5C\u3002", "Run the supplied experiment work in the Builder/experiment pass.") : null,
    localizedText5(responseLanguage, "\u4FEE\u8BA2\u5B8C\u6210\u540E\u628A\u6750\u6599\u4EA4\u56DE Reviewer \u518D\u68C0\u67E5\u3002", "Return the revised material to the Reviewer for another pass.")
  ].filter(Boolean);
}
function publicReviewerProofActions(responseLanguage) {
  return [localizedText5(
    responseLanguage,
    "\u4FDD\u6301 Reviewer ownership\uFF1B\u901A\u8FC7\u53D7\u4FE1 Reviewer runtime capability \u63D0\u4EA4\u5F53\u524D\u5B8C\u6574 artifact hashes \u7684\u6743\u5A01 proof\u3002",
    "Keep Reviewer ownership and submit authoritative proof for the current complete artifact hashes through the trusted Reviewer runtime capability."
  )];
}
function runDoveReviewLoop(root, args = {}) {
  assertGovernanceMutationRegistered("run-dove-review-loop", "guarded");
  assertNoRetiredReviewLoopControls(args);
  const target = assertTaskScopedMutationTarget(root, "run-dove-review-loop", args);
  ensureWorkspace(root);
  loadState(root);
  const runId = slugify13(args.runId ?? `review-pass-${target.packetId}-${Date.now().toString(36)}`);
  const draftRequested = false;
  const experienceRequested = false;
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const reviewArgs = {
    packetId: target.packetId,
    scope: args.scope ?? args.instructions ?? "review-loop material check",
    stage: args.stage ?? target.packet?.stage ?? "audit",
    artifactPaths: normalizeStringArray23(args.artifactPaths),
    reviewedArtifactPaths: normalizeStringArray23(args.reviewedArtifactPaths)
  };
  if (hasNonEmptyString3(args.responseLanguage)) {
    reviewArgs.responseLanguage = args.responseLanguage;
  } else if (hasNonEmptyString3(args.language)) {
    reviewArgs.language = args.language;
  }
  const reviewEntry = runReviewLoop(root, reviewArgs);
  const boundary = buildBuilderRevisionBoundary({
    runId,
    packetId: target.packetId,
    packet: target.packet,
    review: reviewEntry,
    draftRequested,
    experienceRequested
  });
  const proofBoundary = boundary?.nextRole === "reviewer";
  const status = boundary ? "needs-review" : "coherent";
  const stopReason = boundary ? proofBoundary ? "review-proof-required" : "builder-revision-required" : "review-coherent";
  const review = {
    ...reviewEntry,
    preActionGuidance: buildPreActionGuidance({
      surface: "dove.review",
      responseLanguage,
      request: args.scope ?? args.instructions ?? "review-loop material check",
      roleId: "reviewer",
      packet: target.packet,
      currentContext: {
        domain: target.packet?.domain ?? null,
        stage: target.packet?.stage ?? "audit",
        primaryRole: "reviewer"
      },
      operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
      nextAction: boundary?.nextAction ?? "project:dove.status",
      routeHint: boundary?.nextAction ?? "project:dove.status",
      nextHumanAction: boundary ? proofBoundary ? localizedText5(responseLanguage, "\u4FDD\u6301 Reviewer ownership \u5E76\u63D0\u4EA4\u53D7\u4FE1 runtime proof\u3002", "Keep Reviewer ownership and submit trusted runtime proof.") : localizedText5(responseLanguage, "\u663E\u5F0F\u4EA4\u63A5\u7ED9 Builder \u5B8C\u6210\u4FEE\u8BA2\uFF0C\u518D\u4EA4\u56DE Reviewer\u3002", "Explicitly hand off to a Builder for revision, then return to the Reviewer.") : localizedText5(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u9009\u62E9\u4E0B\u4E00\u6B65\u3002", "Return to status and choose the next step."),
      workflowKind: "review",
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "audit",
      tags: ["review", "independent-audit", "evidence", proofBoundary ? "reviewer-proof-boundary" : "builder-boundary"],
      statusSummary: {
        verdict: reviewEntry.verdict,
        findingCount: reviewEntry.findings?.length ?? 0,
        actionItemCount: reviewEntry.actionItems?.length ?? 0,
        boundaryType: boundary?.type ?? null
      }
    })
  };
  const pass = {
    review,
    boundary
  };
  const timestamp = nowIso();
  const loopRecord = {
    id: runId,
    packetId: target.packetId,
    status,
    stopReason,
    boundaryType: boundary?.type ?? null,
    requiredActions: boundary?.requiredActions ?? [],
    ownerRole: boundary?.ownerRole ?? "reviewer",
    nextRole: boundary?.nextRole ?? null,
    passCount: 1,
    reviewStatus: review.verdict,
    reviewRunId: review.timestamp ?? null,
    verdict: review.verdict ?? null,
    findingCount: review.findings?.length ?? 0,
    actionItemCount: review.actionItems?.length ?? 0,
    createdAt: timestamp
  };
  const stateIndex = readJson(root, ARTIFACT_PATHS.reviewState, { version: 2, history: [], openItems: [], lastVerdict: "not-reviewed", lastReviewedAt: null, unresolvedConcernIds: [] });
  stateIndex.history = [...Array.isArray(stateIndex.history) ? stateIndex.history : [], loopRecord];
  stateIndex.lastVerdict = status;
  stateIndex.lastReviewedAt = timestamp;
  writeJson(root, ARTIFACT_PATHS.reviewState, stateIndex);
  appendText(root, ARTIFACT_PATHS.reviewLog, `## ${timestamp} \u2014 dove-review-loop

- Run: ${runId}
- Packet: ${target.packetId}
- Status: ${status}
- Stop reason: ${stopReason}
- Boundary: ${boundary?.type ?? "none"}
- Review pass: single independent pass
- Builder mutation: none; revision requires a new explicit handoff call

`);
  const packetArtifactRefs = normalizeStringArray23([
    ...target.packet?.artifactRefs ?? [],
    reviewEntry.reviewStatePath,
    reviewEntry.reviewLogPath,
    ARTIFACT_PATHS.reviewState,
    ARTIFACT_PATHS.reviewLog
  ]);
  const packetEvidenceLinks = normalizeStringArray23([
    ...target.packet?.evidenceLinks ?? [],
    ...reviewEntry.reviewedArtifactPaths ?? [],
    ...reviewEntry.reviewedArtifactSet ?? []
  ]);
  const task = applyPacketStepResult2(root, target.packet, {
    runId,
    surface: "dove.review-loop",
    command: "dove.review-loop",
    output: {
      status,
      runId,
      summary: reviewEntry.summary,
      nextAction: boundary?.nextAction ?? "project:dove.status",
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks,
      boundary,
      requiredActions: boundary?.requiredActions ?? [],
      ownerRole: boundary?.ownerRole,
      nextRole: boundary?.nextRole
    },
    classified: boundary ? {
      status: "blocked-boundary",
      outcome: boundary.type,
      stopReason,
      terminal: true,
      canCompleteTask: false,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks,
      requiredActions: boundary.requiredActions
    } : {
      status: "step-completed",
      outcome: "dove.review-loop-coherent",
      stopReason: null,
      terminal: false,
      canCompleteTask: true,
      artifactRefs: packetArtifactRefs,
      evidenceLinks: packetEvidenceLinks
    },
    nextAction: boundary?.nextAction ?? "project:dove.status",
    currentFocus: boundary?.summary ?? reviewEntry.summary
  });
  const preActionGuidance = buildPreActionGuidance({
    surface: "dove.review",
    responseLanguage,
    request: args.instructions ?? args.scope ?? args.summary ?? "review loop",
    roleId: "reviewer",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "audit",
      primaryRole: "reviewer"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: boundary?.nextAction ?? "project:dove.status",
    routeHint: boundary?.nextAction ?? "project:dove.status",
    nextHumanAction: boundary ? proofBoundary ? localizedText5(responseLanguage, "\u4FDD\u6301 Reviewer ownership \u5E76\u63D0\u4EA4\u53D7\u4FE1 runtime proof\u3002", "Keep Reviewer ownership and submit trusted runtime proof.") : localizedText5(responseLanguage, "\u663E\u5F0F\u4EA4\u63A5\u7ED9 Builder \u5B8C\u6210\u4FEE\u8BA2\uFF0C\u518D\u4EA4\u56DE Reviewer\u3002", "Explicitly hand off to a Builder for revision, then return to the Reviewer.") : localizedText5(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u9009\u62E9\u4E0B\u4E00\u6B65\u3002", "Return to status and choose the next step."),
    workflowKind: "review-loop",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "audit",
    tags: ["review", "evidence", proofBoundary ? "reviewer-proof-boundary" : "builder-boundary", "iteration"],
    statusSummary: {
      status,
      stopReason,
      passCount: 1,
      boundaryType: boundary?.type ?? null,
      requiredActions: boundary?.requiredActions ?? []
    }
  });
  const publicRequiredActions = boundary ? proofBoundary ? publicReviewerProofActions(responseLanguage) : publicBuilderRevisionActions({ draftRequested, experienceRequested }, responseLanguage) : [];
  const resultCard = buildCommandResultCard({
    surface: "dove.review-loop",
    command: "run_dove_review_loop",
    title: boundary ? proofBoundary ? localizedText5(responseLanguage, "review-loop \u5DF2\u505C\u5728 Reviewer proof \u8FB9\u754C", "Review loop stopped at the Reviewer proof boundary") : localizedText5(responseLanguage, "review-loop \u5DF2\u505C\u5728 Builder \u4FEE\u8BA2\u8FB9\u754C", "Review loop stopped at the Builder revision boundary") : localizedText5(responseLanguage, "review-loop \u5DF2\u505C\u6B62", "Review loop stopped"),
    status,
    stopReason,
    happened: reviewLoopStopSummary(status, stopReason, responseLanguage),
    durableWrites: [localizedText5(responseLanguage, "\u5BA1\u6838\u72B6\u6001\u548C\u5BA1\u6838\u65E5\u5FD7\u5DF2\u66F4\u65B0\uFF1B\u8349\u7A3F\u548C\u5B9E\u9A8C\u6750\u6599\u672A\u6539\u52A8\u3002", "Review state and review log were updated; draft and experiment material were not changed.")],
    evidence: [localizedText5(responseLanguage, "\u672C\u6B21\u8C03\u7528\u5B8C\u6210\u4E00\u6B21\u72EC\u7ACB Reviewer \u68C0\u67E5\u3002", "This call completed one independent Reviewer pass.")],
    validation: [reviewLoopStopSummary(status, stopReason, responseLanguage)],
    boundary: boundary ? {
      type: boundary.type,
      summary: proofBoundary ? localizedText5(responseLanguage, "\u5F53\u524D board \u4FDD\u6301 Reviewer-owned\uFF1B\u8FD9\u4E0D\u662F Builder \u4FEE\u8BA2\u8BF7\u6C42\u3002", "The board remains Reviewer-owned; this is not a Builder revision request.") : localizedText5(responseLanguage, "\u5F53\u524D board \u4ECD\u7531 Reviewer \u6301\u6709\uFF1B\u5FC5\u987B\u663E\u5F0F\u4EA4\u63A5\u7ED9 Builder \u540E\u624D\u80FD\u4FEE\u8BA2\u3002", "The board remains Reviewer-owned; revision requires an explicit handoff to a Builder."),
      nextAction: boundary.nextAction,
      requiredActions: publicRequiredActions
    } : null,
    scope: {
      reviewPasses: 1,
      status,
      builderMutationApplied: false
    },
    nextActions: [{
      title: boundary ? proofBoundary ? localizedText5(responseLanguage, "\u7531 Reviewer runtime \u63D0\u4EA4\u6743\u5A01 proof", "Submit authoritative proof through the Reviewer runtime") : localizedText5(responseLanguage, "\u663E\u5F0F\u4EA4\u63A5\u7ED9 Builder \u4FEE\u8BA2", "Explicitly hand off to a Builder for revision") : localizedText5(responseLanguage, "\u56DE\u5230\u72B6\u6001\u9875\u9009\u62E9\u4E0B\u4E00\u6B65", "Return to status for the next step"),
      why: boundary ? proofBoundary ? localizedText5(responseLanguage, "\u5F53\u524D\u53EA\u6709 proof \u7F3A\u5931\uFF0C\u6CA1\u6709 substantive finding\uFF0C\u56E0\u6B64\u4E0D\u80FD\u8DEF\u7531\u4E3A Builder \u4FEE\u8BA2\u3002", "Only proof is missing; there is no substantive finding, so this must not route to Builder revision.") : localizedText5(responseLanguage, "\u8FD9\u6B21\u8C03\u7528\u53EA\u5B8C\u6210 Reviewer pass\uFF0C\u4E0D\u4F1A\u542F\u52A8 mission\u3001auto\u3001rebuttal\uFF0C\u4E5F\u4E0D\u4F1A\u5728 Reviewer ownership \u4E0B\u5199\u8349\u7A3F\u6216\u5B9E\u9A8C\u3002", "This call completes only the Reviewer pass; it does not start mission, auto, or rebuttal, and it does not write draft or experiment material under Reviewer ownership.") : localizedText5(responseLanguage, "\u5F53\u524D\u6750\u6599\u5DF2\u7ECF\u901A\u8FC7\u8FD9\u4E00\u8F6E\u68C0\u67E5\uFF0C\u53EF\u4EE5\u51B3\u5B9A\u7EE7\u7EED\u5199\u4F5C\u3001\u5F52\u6863\u6216\u8FDB\u5165\u7248\u672C\u5FEB\u7167\u3002", "This pass is coherent, so the next decision can be drafting, closure, or versioning."),
      requiredActions: publicRequiredActions
    }]
  }, responseLanguage);
  return {
    status,
    preActionGuidance,
    resultCard,
    stopReason,
    packetId: target.packetId,
    runId,
    task,
    packetLifecycleApplied: true,
    pass,
    review,
    boundary,
    boundaryType: boundary?.type ?? null,
    requiredActions: boundary?.requiredActions ?? [],
    ownerRole: boundary?.ownerRole ?? "reviewer",
    nextRole: boundary?.nextRole ?? null,
    nextAction: boundary?.nextAction ?? "project:dove.status",
    deferredBuilderWork: boundary ? {
      draftRequested,
      experienceRequested,
      writes: [],
      requiredActions: boundary.requiredActions
    } : null,
    reviewStatePath: ARTIFACT_PATHS.reviewState,
    reviewLogPath: ARTIFACT_PATHS.reviewLog
  };
}

// src/core/task-workflow.mjs
function slugify14(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "task";
}
function normalizeString10(value, fallback = "") {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray24(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean))) : [];
}
function assertNoRetiredAutoControls(value, inputPath = "$", seen = /* @__PURE__ */ new WeakSet()) {
  if (!value || typeof value !== "object") {
    return;
  }
  if (seen.has(value)) {
    return;
  }
  seen.add(value);
  for (const [key, item] of Object.entries(value)) {
    const itemPath = Array.isArray(value) ? `${inputPath}[${key}]` : `${inputPath}.${key}`;
    if (key === "executionReceipt") {
      throw new Error(
        `run_dove_auto does not accept caller-controlled executionReceipt at ${itemPath}.`
      );
    }
    if (key.startsWith("policyOverride") || key === "skipBoardUpdate" || key === "skipRefreshDurableSurfaces" || key === "skipFollowThroughReady" || key === "skipSyncPhase") {
      throw new Error(
        `run_dove_auto no longer accepts retired governance input ${key} at ${itemPath}.`
      );
    }
    assertNoRetiredAutoControls(item, itemPath, seen);
  }
}
function normalizeAllowed3(value, allowed, fallback) {
  const normalized = typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, "-") : "";
  return allowed.includes(normalized) ? normalized : fallback;
}
function normalizeStatus3(value, fallback = "pending") {
  return normalizeAllowed3(value, DOVE_TASK_STATUSES, fallback);
}
function normalizeLevel2(value, fallback) {
  const numeric = Number(value);
  return Number.isFinite(numeric) ? Math.max(0, Math.floor(numeric)) : fallback;
}
function requestedLevel(args = {}) {
  for (const value of [args.level, args.taskLevel, args.missionLevel]) {
    if (value === null || value === void 0 || value === "" || typeof value === "boolean") {
      continue;
    }
    const numeric = Number(value);
    if (Number.isFinite(numeric)) {
      return Math.max(0, Math.floor(numeric));
    }
  }
  return null;
}
function normalizeMissionLevel(args = {}, fallback = 3) {
  const explicit = requestedLevel(args);
  const level = explicit ?? normalizeLevel2(fallback, 3);
  if (level === 0) {
    throw new Error("Level 0 is reserved for the unique Dove init task. Use /dove:init for level-0 work.");
  }
  return Math.max(1, level);
}
function normalizeChildLevel(args = {}, parentLevel) {
  const explicit = requestedLevel(args);
  const minimumLevel = parentLevel + 1;
  const level = explicit ?? minimumLevel;
  if (level <= parentLevel) {
    throw new Error(`System checklist task level ${level} must be greater than parent mission level ${parentLevel}.`);
  }
  return level;
}
function hasExplicitConfirmation(args = {}) {
  return args.confirmed === true || args.confirm === true;
}
function assertUnambiguousConfirmation(args = {}) {
  if (args.confirmed === true && args.confirm === true) {
    throw new Error("Dove mission replay must provide only one confirmation flag: confirmed or confirm.");
  }
}
function taskPacketPath2(packetId) {
  return path26.join(ARTIFACT_PATHS.taskPacketsPacketsDir, `${packetId}.json`);
}
function taskContextPath2(packetId) {
  return path26.join(ARTIFACT_PATHS.packetContextsDir, `${packetId}.json`);
}
var ARCHIVED_TASK_STATUSES = new Set(DOVE_ARCHIVED_TASK_STATUSES);
function activeStatus2(status) {
  const normalized = String(status ?? "").trim().toLowerCase();
  return !["completed", "killed"].includes(normalized) && !ARCHIVED_TASK_STATUSES.has(normalized);
}
function archivedTaskStatus2(task) {
  return [task?.status, task?.lifecycleStatus].some((status) => ARCHIVED_TASK_STATUSES.has(String(status ?? "").trim().toLowerCase()));
}
function openChecklistChildForParent(task, parentId) {
  return task?.parentId === parentId && task.creatorKind === "system" && !task.derivedFrom && !task.sourcePlanTaskId && !archivedTaskStatus2(task) && activeStatus2(normalizeStatus3(task.status ?? task.lifecycleStatus, "pending"));
}
function openChecklistChildrenForParent(root, parent) {
  const catalog = readTaskPacketCatalog(root);
  return Array.from(catalog.byId.values()).filter((task) => openChecklistChildForParent(task, parent.id));
}
function checklistCompletionBlock(root, task, status, responseLanguage = "zh") {
  if (status !== "completed") {
    return null;
  }
  const openChecklistChildren = openChecklistChildrenForParent(root, task);
  if (openChecklistChildren.length === 0) {
    return null;
  }
  const message = responseLanguage === "en" ? "The parent mission cannot be marked done while checklist children remain open." : "\u7236 mission \u4ECD\u6709 open checklist \u5B50\u9879\uFF0C\u4E0D\u80FD\u6807\u8BB0\u4E3A done\u3002";
  return {
    status: "needs-checklist-reconciliation",
    requestedStatus: "done",
    machineStatus: "completed",
    packetId: task.id,
    title: task.title,
    openChecklistChildIds: openChecklistChildren.map((child) => child.id),
    openChecklistChildren: openChecklistChildren.map((child) => ({
      id: child.id,
      packetId: child.id,
      title: child.title,
      status: child.status,
      displayStatus: child.status === "completed" || child.status === "killed" ? "done" : child.status,
      parentId: child.parentId
    })),
    nextAction: "project:dove.status",
    message
  };
}
function normalizeIndex2(index = createTaskPacketsIndex()) {
  const base = createTaskPacketsIndex();
  const items = Array.isArray(index.items) ? index.items.filter((item) => item && typeof item === "object" && !Array.isArray(item)) : [];
  const activeInit = items.find((item) => item.level === 0 && activeStatus2(item.status)) ?? null;
  const activeItems = items.filter((item) => item.level !== 0 && activeStatus2(item.status));
  const lifecycleCounts = {};
  const stageCounts = Object.fromEntries(DOVE_TASK_STAGES.map((stage) => [stage, 0]));
  const domainCounts = Object.fromEntries(DOVE_TASK_DOMAINS.map((domain) => [domain, 0]));
  const levelCounts = {};
  for (const item of items) {
    const status = archivedTaskStatus2(item) ? "archived" : normalizeStatus3(item.status ?? item.lifecycleStatus, "pending");
    lifecycleCounts[status] = (lifecycleCounts[status] ?? 0) + 1;
    if (DOVE_TASK_STAGES.includes(item.stage)) {
      stageCounts[item.stage] += 1;
    }
    if (DOVE_TASK_DOMAINS.includes(item.domain)) {
      domainCounts[item.domain] += 1;
    }
    const levelKey = String(Number.isFinite(item.level) ? item.level : 3);
    levelCounts[levelKey] = (levelCounts[levelKey] ?? 0) + 1;
  }
  return {
    ...base,
    ...index,
    version: base.version,
    items,
    taskModel: {
      ...base.taskModel,
      ...index.taskModel && typeof index.taskModel === "object" && !Array.isArray(index.taskModel) ? index.taskModel : {},
      activeInitId: activeInit?.id ?? null,
      activeTaskIds: activeItems.map((item) => item.id)
    },
    lifecycleCounts,
    stageCounts,
    domainCounts,
    levelCounts,
    updatedAt: index.updatedAt ?? null
  };
}
function writePacket2(root, packet) {
  const { context: _derivedContext, ...packetRecord } = packet;
  writeJson(root, taskPacketPath2(packet.id), packetRecord);
  writeJson(root, taskContextPath2(packet.id), {
    id: packet.id,
    parentId: packet.parentId,
    rootId: packet.rootId,
    level: packet.level,
    creatorKind: packet.creatorKind,
    stage: packet.stage,
    domain: packet.domain,
    status: packet.status,
    dependencies: packet.dependencies,
    blockedBy: packet.blockedBy,
    lessonIds: packet.lessonIds,
    artifactRefs: packet.artifactRefs,
    evidenceLinks: packet.evidenceLinks,
    contextPolicy: packet.contextPolicy,
    executionContract: normalizeDoveExecutionContract(packet.executionContract, null),
    currentFocus: packet.currentFocus,
    nextAction: packet.nextAction,
    completedAt: packet.completedAt ?? null,
    blockedReason: packet.blockedReason ?? null,
    killedAt: packet.killedAt ?? null,
    killReason: packet.killReason ?? null,
    ownerRole: packet.ownerRole ?? null,
    nextRole: packet.nextRole ?? null,
    boundary: packet.boundary ?? null,
    boundaryHistory: Array.isArray(packet.boundaryHistory) ? packet.boundaryHistory : [],
    handoff: packet.handoff ?? null,
    lastTransition: packet.lastTransition ?? null,
    updatedAt: packet.updatedAt
  });
}
function upsertIndexItem(index, packet) {
  const item = {
    id: packet.id,
    title: packet.title,
    summary: packet.summary,
    parentId: packet.parentId,
    rootId: packet.rootId,
    level: packet.level,
    creatorKind: packet.creatorKind,
    stage: packet.stage,
    domain: packet.domain,
    status: packet.status,
    lifecycleStatus: packet.lifecycleStatus,
    dependencies: packet.dependencies,
    blockedBy: packet.blockedBy,
    createdAt: packet.createdAt,
    updatedAt: packet.updatedAt,
    completedAt: packet.completedAt ?? null,
    blockedReason: packet.blockedReason ?? null,
    killedAt: packet.killedAt ?? null,
    killReason: packet.killReason ?? null,
    ownerRole: packet.ownerRole ?? null,
    nextRole: packet.nextRole ?? null,
    boundary: packet.boundary ?? null,
    boundaryHistory: Array.isArray(packet.boundaryHistory) ? packet.boundaryHistory : [],
    handoff: packet.handoff ?? null,
    lastTransition: packet.lastTransition ?? null,
    lessonIds: packet.lessonIds,
    artifactRefs: packet.artifactRefs,
    evidenceLinks: packet.evidenceLinks,
    contextPolicy: packet.contextPolicy,
    executionContract: normalizeDoveExecutionContract(packet.executionContract, null),
    packetPath: packet.packetPath,
    packetContextPath: packet.packetContextPath,
    currentFocus: packet.currentFocus,
    nextAction: packet.nextAction
  };
  const items = [...(index.items ?? []).filter((existing) => existing.id !== item.id), item].sort((left, right) => (left.level ?? 3) - (right.level ?? 3) || String(left.createdAt ?? "").localeCompare(String(right.createdAt ?? "")) || left.id.localeCompare(right.id));
  return normalizeIndex2({ ...index, items, updatedAt: packet.updatedAt });
}
function loadTaskIndex(root) {
  return normalizeIndex2(readJson(root, ARTIFACT_PATHS.taskPacketsIndex, createTaskPacketsIndex));
}
function saveTaskIndex(root, index) {
  const normalized = normalizeIndex2(index);
  writeJson(root, ARTIFACT_PATHS.taskPacketsIndex, normalized);
  return normalized;
}
function initPacket(index) {
  return (index.items ?? []).find((item) => item.level === 0 && item.status !== "killed") ?? null;
}
function workflowSignals(text3) {
  const value = String(text3 ?? "").toLowerCase();
  const source = /\b(source|sources|citation|citations|literature|paper search|reference|references|bibliography|template|templates|author kit|latex|overleaf|venue|venues|guideline|guidelines|style file|style files|call for papers|cfp|ranking|rankings|journal|journals|conference|conferences|evidence[-_ ]?urls?|provenance|deposit|bind|archive)\b|来源|引用|文献|模板|作者包|会议|期刊|一区|高水平|写作风格|审稿偏好|证据链接|沉淀|绑定|归档/u.test(value);
  const reviewLoop = /\b(review-loop|revision loop)\b|审稿循环|评审循环/u.test(value);
  const engineeringSubject = /\b(engineer(?:ing)?|code|api|endpoint|implementation|implement|build|compile|lint|typecheck|test|tests|testing|ci)\b/u.test(value);
  const engineeringAction = /\b(implement|implementation|build|compile|lint|typecheck|test|tests|testing|validate|validation|verify|check|fix|ship)\b/u.test(value);
  const explicitReview = /\b(?:code[- ]review|peer[- ]review|reviewer|review findings|review feedback|audit report|review\b[^.!?。！？\n]{0,120}\b(?:api|endpoint|implementation|code))\b|(?:代码|api|接口|端点|实现)[^。！？\n]{0,40}(?:审查|评审)|(?:审查|评审)[^。！？\n]{0,40}(?:代码|api|接口|端点|实现)|同行评审|审稿意见|审查报告/u.test(value);
  const engineeringValidation = engineeringSubject && engineeringAction && !explicitReview;
  const review = explicitReview || !engineeringValidation && /\b(review|audit|audio|verify|check|integrity)\b|审查|审核|复审|检查|完整性/u.test(value);
  const figure = /\b(figure|diagram|pipeline overview)\b|示意图|流程图|图表/u.test(value);
  const draft = /\b(draft|revise|section|introduction|abstract)\b|草稿|修改|章节|引言|摘要/u.test(value);
  const experiment = /\b(experiment|ablation|baseline|metric|result)\b|实验|消融|指标|结果/u.test(value);
  const plan = /\b(plan|design|outline|proposal)\b|规划|计划|方案/u.test(value);
  const paper = source || figure || draft || /\b(paper|claim|rebuttal|reviewer)\b|论文|返修|审稿/u.test(value);
  return { source, reviewLoop, review, figure, draft, experiment, plan, paper };
}
function workflowCommandFromSignals(signals = {}) {
  if (signals.source) {
    return "dove.source";
  }
  if (signals.reviewLoop) {
    return "dove.review-loop";
  }
  if (signals.review) {
    return "dove.review";
  }
  if (signals.figure) {
    return "dove.figure";
  }
  if (signals.draft) {
    return "dove.draft";
  }
  if (signals.experiment) {
    return "dove.experience";
  }
  return null;
}
function projectCommandForWorkflow(command) {
  return command ? `project:${command}` : null;
}
function classifyTask(args = {}) {
  const text3 = [args.goal, args.objective, args.prompt, args.title, args.summary, args.intent].map((item) => String(item ?? "").toLowerCase()).join(" ");
  const explicitStage = normalizeAllowed3(args.stage ?? args.missionStage, DOVE_TASK_STAGES, null);
  const explicitDomain = normalizeAllowed3(args.domain ?? args.doveDomain ?? args.missionDomain, DOVE_TASK_DOMAINS, null);
  const signals = workflowSignals(text3);
  const workflowCommand = workflowCommandFromSignals(signals);
  const stage = explicitStage ?? (signals.source ? "execute" : signals.review || signals.reviewLoop ? "audit" : signals.plan ? "plan" : "execute");
  const domain = explicitDomain ?? (signals.experiment ? "experiment" : signals.paper ? "paper" : "engineering");
  return {
    stage,
    domain,
    workflowCommand,
    rationale: [`stage=${stage}`, `domain=${domain}`, workflowCommand ? `workflow=${workflowCommand}` : null].filter(Boolean)
  };
}
function nextCommandFor(classification) {
  const workflowCommand = projectCommandForWorkflow(classification.workflowCommand);
  if (workflowCommand) {
    return workflowCommand;
  }
  if (classification.stage === "audit") {
    return "project:dove.review";
  }
  if (classification.domain === "experiment") {
    return "project:dove.experience";
  }
  if (classification.domain === "paper" && classification.stage === "execute") {
    return "project:dove.draft";
  }
  return "project:dove.auto";
}
function publicMissionCommand(command, fallback = "project:dove.auto") {
  const normalized = normalizeString10(command, "");
  if (!normalized) {
    return fallback;
  }
  if (normalized.startsWith("project:dove.")) {
    return normalized;
  }
  const toolRoutes = {
    run_dove_auto: "project:dove.auto",
    run_dove_operator: "project:dove.operator",
    create_dove_task: "project:dove.mission",
    record_dove_mission_pass: "project:dove.mission",
    run_audio_review: "project:dove.review",
    run_dove_review_loop: "project:dove.review-loop",
    run_experience_workflow: "project:dove.experience",
    run_figure_workflow: "project:dove.figure",
    register_source: "project:dove.source",
    upsert_note: "project:dove.note",
    upsert_draft: "project:dove.draft",
    build_rebuttal: "project:dove.rebuttal"
  };
  return toolRoutes[normalized] ?? fallback;
}
function copyableMissionCommand(command, packetId) {
  const publicCommand = publicMissionCommand(command);
  return packetId ? `${publicCommand} --packet-id ${packetId}` : publicCommand;
}
function normalizeContractStringArray(value) {
  if (Array.isArray(value)) {
    return normalizeStringArray24(value);
  }
  return typeof value === "string" && value.trim() ? [value.trim()] : [];
}
function firstContractStringArray(...values) {
  for (const value of values) {
    const normalized = normalizeContractStringArray(value);
    if (normalized.length > 0) {
      return normalized;
    }
  }
  return [];
}
function defaultContractDeliverables(task = {}, responseLanguage = "zh") {
  if (task.stage === "audit") {
    return normalizeContractStringArray(doveText(responseLanguage, "workContractDeliverablesAudit", { title: task.title }));
  }
  if (task.stage === "plan") {
    return normalizeContractStringArray(doveText(responseLanguage, "workContractDeliverablesPlan", { title: task.title }));
  }
  if (task.domain === "experiment") {
    return normalizeContractStringArray(doveText(responseLanguage, "workContractDeliverablesExperiment", { title: task.title }));
  }
  if (task.domain === "paper") {
    return normalizeContractStringArray(doveText(responseLanguage, "workContractDeliverablesPaper", { title: task.title }));
  }
  return normalizeContractStringArray(doveText(responseLanguage, "workContractDeliverablesEngineering", { title: task.title }));
}
function defaultContractEvidence(task = {}, responseLanguage = "zh") {
  const evidence = normalizeContractStringArray(task.evidenceExpectations);
  return evidence.length > 0 ? evidence : normalizeContractStringArray(doveText(responseLanguage, "workContractEvidenceDefault", { title: task.title }));
}
function defaultContractDoneCriteria(task = {}, responseLanguage = "zh") {
  return normalizeContractStringArray(doveText(responseLanguage, "workContractDoneDefault", { title: task.title }));
}
function defaultContractOutOfScope(responseLanguage = "zh") {
  return normalizeContractStringArray(doveText(responseLanguage, "workContractOutOfScopeDefault"));
}
function defaultContractImpact(task = {}, responseLanguage = "zh") {
  if (task.stage === "audit") {
    return doveText(responseLanguage, "workContractImpactAudit", { title: task.title });
  }
  if (task.stage === "plan") {
    return doveText(responseLanguage, "workContractImpactPlan", { title: task.title });
  }
  if (task.domain === "experiment") {
    return doveText(responseLanguage, "workContractImpactExperiment", { title: task.title });
  }
  if (task.domain === "paper") {
    return doveText(responseLanguage, "workContractImpactPaper", { title: task.title });
  }
  return doveText(responseLanguage, "workContractImpactEngineering", { title: task.title });
}
function routeLabelFor(command, responseLanguage = "zh") {
  if (command === "project:dove.auto") {
    return doveText(responseLanguage, "workContractRouteAutoLabel");
  }
  if (command === "project:dove.review" || command === "project:dove.review-loop") {
    return doveText(responseLanguage, "workContractRouteReviewLabel");
  }
  return doveText(responseLanguage, "workContractRoutePrimaryLabel");
}
function routeWhenFor(command, responseLanguage = "zh") {
  if (command === "project:dove.auto") {
    return doveText(responseLanguage, "workContractRouteAutoWhen");
  }
  if (command === "project:dove.review" || command === "project:dove.review-loop") {
    return doveText(responseLanguage, "workContractRouteReviewWhen");
  }
  return doveText(responseLanguage, "workContractRoutePrimaryWhen");
}
function normalizeContractRoutes(routes, task = {}, responseLanguage = "zh") {
  return (Array.isArray(routes) ? routes : []).map((route, index) => {
    const source = typeof route === "string" ? { command: route } : plainObject3(route);
    const command = publicMissionCommand(source.command ?? source.nextAction ?? source.workflow, publicMissionCommand(task.nextAction ?? nextCommandFor(task)));
    if (!command) {
      return null;
    }
    return {
      label: normalizeString10(source.label ?? source.title, routeLabelFor(command, responseLanguage)),
      command,
      copyableCommand: normalizeString10(source.copyableCommand ?? source.copyCommand, copyableMissionCommand(command, task.id)),
      packetId: normalizeString10(source.packetId ?? source.taskPacketId ?? source.missionPacketId, task.id ?? null),
      when: normalizeString10(source.when ?? source.reason, routeWhenFor(command, responseLanguage)),
      role: normalizeDovePrimaryRoleId(source.role ?? source.ownerRole ?? task.nextRole ?? task.ownerRole, ownerRoleFor2(task)),
      evidenceRequired: firstContractStringArray(source.evidenceRequired, source.evidenceContract, source.evidenceExpectations, task.evidenceExpectations),
      doneCriteria: firstContractStringArray(source.doneCriteria, task.workContract?.doneCriteria),
      rank: Number.isFinite(source.rank) ? source.rank : index + 1
    };
  }).filter(Boolean);
}
function defaultContractRoutes(task = {}, responseLanguage = "zh") {
  const primary = publicMissionCommand(task.nextAction ?? nextCommandFor(task));
  const routes = [{ command: primary }];
  if (primary !== "project:dove.auto") {
    routes.push({ command: "project:dove.auto" });
  }
  return normalizeContractRoutes(routes, task, responseLanguage);
}
function normalizeWorkContract(task = {}, args = {}, classification = {}, responseLanguage = "zh") {
  const classifiedTask = {
    ...task,
    stage: task.stage ?? classification.stage ?? "execute",
    domain: task.domain ?? classification.domain ?? "engineering"
  };
  const explicit = plainObject3(args.workContract ?? task.workContract);
  const deliverables = firstContractStringArray(explicit.deliverables, args.deliverables, defaultContractDeliverables(classifiedTask, responseLanguage));
  const evidenceContract = firstContractStringArray(explicit.evidenceContract, args.evidenceContract, args.evidenceExpectations, args.acceptanceChecks, defaultContractEvidence(classifiedTask, responseLanguage));
  const doneCriteria = firstContractStringArray(explicit.doneCriteria, args.doneCriteria, args.acceptanceChecks, defaultContractDoneCriteria(classifiedTask, responseLanguage));
  const outOfScope = firstContractStringArray(explicit.outOfScope, explicit.outOfScopeItems, args.outOfScope, args.outOfScopeItems, defaultContractOutOfScope(responseLanguage));
  const recommendedRoutes = normalizeContractRoutes(explicit.recommendedRoutes ?? args.recommendedRoutes, { ...classifiedTask, workContract: { doneCriteria } }, responseLanguage);
  return {
    purpose: normalizeString10(explicit.purpose ?? args.purpose, doveText(responseLanguage, "workContractPurpose", { title: classifiedTask.title, stage: classifiedTask.stage, domain: classifiedTask.domain })),
    deliverables,
    outOfScope,
    evidenceContract,
    doneCriteria,
    recommendedRoutes: recommendedRoutes.length > 0 ? recommendedRoutes : defaultContractRoutes({ ...classifiedTask, workContract: { doneCriteria } }, responseLanguage),
    practicalImpact: normalizeString10(explicit.practicalImpact ?? args.practicalImpact, defaultContractImpact(classifiedTask, responseLanguage))
  };
}
function executionChainTypeFor(task = {}, classification = {}) {
  const stage = task.stage ?? classification.stage;
  const domain = task.domain ?? classification.domain;
  if (stage === "plan") {
    return "plan-to-executable-missions";
  }
  if (domain === "experiment") {
    return "experiment-plan-result-audit";
  }
  if (domain === "paper") {
    return "paper-source-note-draft-review";
  }
  return "engineering-host-pass-verify";
}
function roleSequenceForExecutionContract(task = {}, classification = {}) {
  const stage = task.stage ?? classification.stage;
  if (stage === "plan") {
    return ["planner", "builder", "reviewer"];
  }
  if (stage === "audit") {
    return ["reviewer"];
  }
  return ["builder", "reviewer"];
}
function defaultExecutionFailureRoutes(task = {}, responseLanguage = "zh") {
  const routes = [
    { on: "missing-required-materials", boundaryType: "missing-required-materials", nextAction: "project:dove.status", requiredActions: ["provide-required-materials"] },
    { on: "verification-failed", boundaryType: "verification-failed", nextAction: "project:dove.status", requiredActions: ["provide-verified-criteria"] },
    { on: "reviewer-rejection", boundaryType: "fix-required", nextAction: "project:dove.status", requiredActions: ["revise-and-return-evidence"] }
  ];
  if (task.stage === "plan") {
    routes.unshift({ on: "plan-output-not-executable", boundaryType: "plan-output-not-executable", nextAction: "project:dove.status", requiredActions: ["provide-executable-child-missions"] });
  }
  return routes.map((route) => ({
    ...route,
    requiredActions: route.requiredActions.length > 0 ? route.requiredActions : [doveText(responseLanguage, "compactCardNoEvidence")]
  }));
}
function defaultExecutionImplementation(task = {}, workContract = {}, responseLanguage = "zh") {
  const implementation = firstContractStringArray(workContract.deliverables, task.evidenceExpectations);
  if (implementation.length > 0) {
    return implementation;
  }
  return [doveText(responseLanguage, "executePlannedWork")];
}
function defaultExecutionContract(task = {}, args = {}, classification = {}, workContract = {}, responseLanguage = "zh") {
  const nextAction = normalizeString10(task.nextAction ?? args.nextAction, nextCommandFor(classification));
  return {
    chainType: executionChainTypeFor(task, classification),
    roleSequence: roleSequenceForExecutionContract(task, classification),
    readFirst: normalizeStringArray24(args.readFirst),
    action: nextAction,
    implementation: defaultExecutionImplementation(task, workContract, responseLanguage),
    files: [],
    materials: {
      requiredInputs: normalizeStringArray24(args.requiredInputs),
      requiredArtifacts: normalizeStringArray24(args.requiredArtifacts),
      sourceRefs: normalizeStringArray24(args.sourceRefs),
      artifactRefs: normalizeStringArray24(task.artifactRefs)
    },
    convergence: {
      criteria: firstContractStringArray(workContract.doneCriteria, args.acceptanceChecks),
      verificationCommands: normalizeStringArray24(args.verificationCommands),
      evidenceRequired: firstContractStringArray(workContract.evidenceContract, task.evidenceExpectations),
      definitionOfDone: normalizeString10(workContract.practicalImpact, "")
    },
    failureRoutes: defaultExecutionFailureRoutes(task, responseLanguage)
  };
}
function mergeExecutionContract(task = {}, args = {}, classification = {}, workContract = {}, responseLanguage = "zh") {
  return normalizeDoveExecutionContract(args.executionContract ?? task.executionContract, defaultExecutionContract(task, args, classification, workContract, responseLanguage));
}
function ownerRoleFor2(classification = {}) {
  if (classification.stage === "audit") {
    return "reviewer";
  }
  if (classification.stage === "plan") {
    return "planner";
  }
  return "builder";
}
function compactScope(task = {}, responseLanguage = "zh") {
  return doveText(responseLanguage, "compactCardScope", {
    stage: task.stage ?? "execute",
    domain: task.domain ?? "engineering",
    status: task.status ?? "pending"
  });
}
function compactEvidence(task = {}, responseLanguage = "zh") {
  const evidence = normalizeStringArray24(task.evidenceExpectations);
  return evidence.length > 0 ? evidence : [doveText(responseLanguage, "compactCardNoEvidence")];
}
function compactStepLabels(steps = []) {
  if (!Array.isArray(steps)) {
    return [];
  }
  return steps.map((step) => {
    if (typeof step === "string") {
      return step;
    }
    if (step && typeof step === "object" && !Array.isArray(step)) {
      return [step.command, step.completeTask === true ? "complete" : null].filter(Boolean).join(" ");
    }
    return "";
  }).filter(Boolean);
}
function buildTaskConfirmationCard(task = {}, context = {}, responseLanguage = "zh") {
  const workContract = task.workContract ?? context.workContract ?? null;
  const executionContract = normalizeDoveExecutionContract(task.executionContract ?? context.executionContract, null);
  const executionReadiness = doveExecutionContractReadiness(executionContract);
  return {
    presentation: "compact-task-card",
    packetId: task.id ?? null,
    title: task.title ?? doveText(responseLanguage, "taskFallbackTitle"),
    why: workContract?.practicalImpact ?? task.summary ?? context.why ?? "",
    scope: compactScope(task, responseLanguage),
    stage: task.stage ?? null,
    domain: task.domain ?? null,
    status: task.status ?? null,
    level: task.level ?? null,
    firstAction: workContract?.recommendedRoutes?.[0]?.copyableCommand ?? context.firstAction ?? task.nextAction ?? doveText(responseLanguage, "compactCardFirstActionFallback"),
    evidenceRequired: executionContract?.convergence?.evidenceRequired ?? workContract?.evidenceContract ?? compactEvidence(task, responseLanguage),
    deliverables: workContract?.deliverables ?? [],
    doneCriteria: executionContract?.convergence?.criteria ?? workContract?.doneCriteria ?? [],
    executionContract,
    executionReadiness,
    recommendedRoutes: workContract?.recommendedRoutes ?? [],
    preActionGuidance: context.preActionGuidance ?? null,
    boundaryOrResume: context.boundaryOrResume ?? doveText(responseLanguage, "compactCardBoundaryFallback"),
    confirmation: doveText(responseLanguage, "compactCardNoAutomaticExecution"),
    confirmationRequired: true,
    proposalOnly: true,
    noAutoApply: true
  };
}
function buildAutoConfirmationCard(task = {}, autoPlan = {}, context = {}, responseLanguage = "zh") {
  const proposedSteps = compactStepLabels(autoPlan.proposedSteps);
  return {
    presentation: "compact-auto-card",
    packetId: task.id ?? null,
    title: task.title ?? doveText(responseLanguage, "taskFallbackTitle"),
    scope: compactScope(task, responseLanguage),
    maxIterations: context.maxIterations ?? null,
    firstAction: proposedSteps[0] ?? task.nextAction ?? doveText(responseLanguage, "compactCardFirstActionFallback"),
    proposedSteps,
    preActionGuidance: context.preActionGuidance ?? null,
    safeToRun: autoPlan.safeToRun ?? false,
    requiresHostPass: autoPlan.requiresHostPass ?? false,
    why: autoPlan.whyThisStep ?? task.summary ?? "",
    evidenceRequired: compactEvidence(task, responseLanguage),
    boundaryOrResume: doveText(responseLanguage, "compactCardBoundaryFallback"),
    confirmation: doveText(responseLanguage, "compactCardNoAutomaticExecution"),
    confirmationRequired: true,
    proposalOnly: true,
    noAutoApply: true
  };
}
function buildOperatorQueueCard(task = {}, context = {}, responseLanguage = "zh") {
  return {
    presentation: "compact-operator-queue-card",
    queue: context.queue ?? "pending",
    packetId: task.id ?? null,
    title: task.title ?? doveText(responseLanguage, "taskFallbackTitle"),
    status: task.status ?? null,
    scope: compactScope(task, responseLanguage),
    firstAction: task.nextAction ?? context.firstAction ?? doveText(responseLanguage, "compactCardFirstActionFallback"),
    why: context.why ?? task.rationale ?? task.summary ?? "",
    evidenceRequired: compactEvidence(task, responseLanguage),
    preActionGuidance: context.preActionGuidance ?? null,
    boundaryOrResume: task.blockedReason ?? task.nextAction ?? doveText(responseLanguage, "compactCardBoundaryFallback"),
    confirmation: doveText(responseLanguage, "compactCardNoAutomaticExecution"),
    confirmationRequired: true,
    proposalOnly: true,
    noAutoApply: true
  };
}
function taskWorkflowDurableWrites(responseLanguage = "zh", { runtime = false, lifecycle = true, planConversion = null } = {}) {
  const writes = lifecycle ? [
    doveText(responseLanguage, "resultCardTaskPacketUpdated"),
    doveText(responseLanguage, "resultCardTaskIndexUpdated"),
    doveText(responseLanguage, "resultCardRuntimeEventRecorded")
  ] : [];
  if (runtime) {
    writes.push(doveText(responseLanguage, "resultCardRuntimeResultRecorded"));
  }
  if (planConversion?.createdMissions?.length > 0 || planConversion?.reusedMissions?.length > 0) {
    writes.push(`${ARTIFACT_PATHS.taskPacketsIndex}: plan missions ${planConversion.createdMissions?.length ?? 0} created, ${planConversion.reusedMissions?.length ?? 0} reused`);
  }
  return writes;
}
function buildWorkflowHandoffSuggestion(task = {}, result = {}, responseLanguage = "zh") {
  const boundary = result.boundary ?? task.boundary ?? null;
  const statusText = `${result.status ?? ""} ${result.outcome ?? ""} ${result.stopReason ?? ""}`;
  const actionableStop = Boolean(boundary) || /awaiting|blocked|required|missing|failed|exhausted|needs-/u.test(statusText);
  if (!actionableStop) {
    return null;
  }
  const ownerRole = boundary?.ownerRole ?? task.ownerRole ?? null;
  const nextRole = boundary?.nextRole ?? task.nextRole ?? ownerRole;
  const requiredInputs = normalizeStringArray24(boundary?.requiredInputs);
  const requiredActions = normalizeStringArray24(boundary?.requiredActions);
  const requires = normalizeStringArray24([...requiredInputs, ...requiredActions]);
  const reason = boundary?.reason ?? result.stopReason ?? null;
  const roleTransfer = ownerRole && nextRole && ownerRole !== nextRole;
  return {
    presentation: "dove-handoff-suggestion",
    boundaryId: boundary?.id ?? null,
    boundaryType: boundary?.type ?? result.outcome ?? result.status ?? null,
    ownerRole,
    nextRole,
    handoff: boundary?.handoff ?? task.handoff ?? null,
    requiredInputs,
    requiredActions,
    requires,
    reason,
    summary: roleTransfer ? doveText(responseLanguage, "resultCardHandoffRoleTransfer", { ownerRole, nextRole }) : doveText(responseLanguage, "resultCardHandoffResolveBoundary", { ownerRole })
  };
}
function workflowActionMetadata(task = {}, result = {}, handoffSuggestion = null) {
  const boundary = result.boundary ?? task.boundary ?? null;
  const hasHandoffContext = Boolean(boundary || handoffSuggestion);
  const requiredInputs = hasHandoffContext ? normalizeStringArray24(boundary?.requiredInputs ?? handoffSuggestion?.requiredInputs) : [];
  const requiredActions = hasHandoffContext ? normalizeStringArray24(boundary?.requiredActions ?? handoffSuggestion?.requiredActions) : [];
  return {
    boundary,
    boundaryId: boundary?.id ?? handoffSuggestion?.boundaryId ?? null,
    boundaryType: boundary?.type ?? handoffSuggestion?.boundaryType ?? (hasHandoffContext ? result.outcome ?? result.status ?? null : null),
    ownerRole: hasHandoffContext ? boundary?.ownerRole ?? handoffSuggestion?.ownerRole ?? task.ownerRole ?? null : null,
    nextRole: hasHandoffContext ? boundary?.nextRole ?? handoffSuggestion?.nextRole ?? task.nextRole ?? null : null,
    handoff: hasHandoffContext ? boundary?.handoff ?? task.handoff ?? handoffSuggestion?.handoff ?? null : null,
    requiredInputs,
    requiredActions,
    requires: normalizeStringArray24([...requiredInputs, ...requiredActions]),
    handoffSuggestion
  };
}
function withWorkflowActionMetadata(actions = [], task = {}, result = {}, responseLanguage = "zh", handoffSuggestion = null) {
  const suggestion = handoffSuggestion ?? buildWorkflowHandoffSuggestion(task, result, responseLanguage);
  const metadata = workflowActionMetadata(task, result, suggestion);
  return (Array.isArray(actions) ? actions : []).map((action) => ({
    ...metadata,
    ...action,
    boundary: action.boundary ?? metadata.boundary,
    boundaryId: action.boundaryId ?? metadata.boundaryId,
    boundaryType: action.boundaryType ?? metadata.boundaryType,
    ownerRole: action.ownerRole ?? metadata.ownerRole,
    nextRole: action.nextRole ?? metadata.nextRole,
    handoff: action.handoff ?? metadata.handoff,
    requiredInputs: action.requiredInputs ?? metadata.requiredInputs,
    requiredActions: action.requiredActions ?? metadata.requiredActions,
    requires: action.requires ?? metadata.requires,
    handoffSuggestion: action.handoffSuggestion ?? metadata.handoffSuggestion
  }));
}
function operatorHandoffSuggestion(result = {}, responseLanguage = "zh") {
  if (result.awaitingResultTaskIds?.length > 0) {
    const requiredActions = normalizeStringArray24(result.awaitingRequiredActions);
    return {
      presentation: "dove-handoff-suggestion",
      boundaryType: "awaiting-host-pass-result",
      ownerRole: "builder",
      nextRole: "builder",
      requires: result.awaitingResultTaskIds,
      requiredActions: requiredActions.length > 0 ? requiredActions : ["provide-host-pass-result"],
      reason: result.stopReason,
      summary: doveText(responseLanguage, "resultCardHandoffProvideEvidence")
    };
  }
  if (result.blockerPlanConversion?.createdMissions?.length > 0) {
    return {
      presentation: "dove-handoff-suggestion",
      boundaryType: "blocked-boundary",
      ownerRole: "planner",
      nextRole: "planner",
      requires: result.blockerPlanConversion.createdMissions.map((mission) => mission.id),
      summary: doveText(responseLanguage, "resultCardHandoffResolveBoundary", { ownerRole: "planner" })
    };
  }
  const proposedBlockedTaskIds = normalizeStringArray24(result.blockerPlanConversion?.proposedBlockedTaskIds);
  if (proposedBlockedTaskIds.length > 0) {
    return {
      presentation: "dove-handoff-suggestion",
      boundaryType: "blocked-boundary",
      ownerRole: "planner",
      nextRole: "planner",
      requires: proposedBlockedTaskIds,
      requiredActions: ["rerun-operator-with-blockerInvestigationMode-create"],
      reason: result.stopReason,
      summary: doveText(responseLanguage, "resultCardHandoffResolveBoundary", { ownerRole: "planner" })
    };
  }
  return null;
}
function missionResultCard(task = {}, result = {}, context = {}, responseLanguage = "zh") {
  const handoffSuggestion = buildWorkflowHandoffSuggestion(task, result, responseLanguage);
  const nextAction = context.nextAction ?? result.iterations?.[0]?.output?.nextAction;
  const baseNextActions = Array.isArray(context.nextActions) && context.nextActions.length > 0 ? context.nextActions : nextAction ? [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: nextAction, packetId: task.id }] : [];
  return buildCommandResultCard({
    surface: "dove.mission",
    command: "record_dove_mission_pass",
    packetId: task.id,
    title: task.title,
    runId: result.id,
    status: result.status,
    outcome: result.outcome,
    summary: context.summary ?? result.iterations?.[0]?.output?.summary,
    stopReason: result.stopReason,
    taskStatusBefore: result.taskStatusBefore,
    taskStatusAfter: result.taskStatusAfter,
    evidenceLinks: context.evidenceLinks,
    artifactRefs: context.artifactRefs,
    validationEvidence: context.validationEvidence,
    executionReceipt: context.executionReceipt ?? result.executionReceipt ?? result.iterations?.[0]?.output?.executionReceipt,
    evidenceExplanation: context.evidenceExplanation,
    artifactResolution: context.artifactResolution,
    durableWrites: taskWorkflowDurableWrites(responseLanguage, { runtime: true, lifecycle: true, planConversion: context.planConversion }),
    boundary: task.boundary ?? null,
    nextAction,
    nextActions: withWorkflowActionMetadata(baseNextActions, task, result, responseLanguage, handoffSuggestion),
    preActionGuidanceSummary: context.preActionGuidanceSummary ?? null,
    foreground: result.foreground,
    background: result.background,
    daemon: result.daemon
  }, responseLanguage);
}
function latestExecutionReceipt(iterations = [], fallback = null) {
  return [...Array.isArray(iterations) ? iterations : []].reverse().map((iteration) => normalizeDoveExecutionReceipt(iteration.output?.executionReceipt ?? iteration.executionReceipt, null)).find(Boolean) ?? normalizeDoveExecutionReceipt(fallback, null);
}
function autoResultCard(task = {}, result = {}, context = {}, responseLanguage = "zh") {
  const evidenceLinks = result.iterations?.flatMap((iteration) => normalizeStringArray24(iteration.output?.evidenceLinks ?? iteration.evidenceLinks)) ?? [];
  const artifactRefs = result.iterations?.flatMap((iteration) => normalizeStringArray24(iteration.output?.artifactRefs ?? iteration.artifactRefs)) ?? [];
  const executionReceipt = latestExecutionReceipt(result.iterations, result.executionReceipt);
  const handoffSuggestion = buildWorkflowHandoffSuggestion(task, result, responseLanguage);
  const baseNextActions = context.nextActions ?? (result.status === "awaiting-host-pass" ? [{ title: doveText(responseLanguage, "resultCardNextProvideEvidence"), command: "record_dove_mission_pass", packetId: task.id, confirmationRequired: true }] : [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: context.nextAction ?? task.nextAction ?? "project:dove.status", packetId: task.id }]);
  const nextActions = withWorkflowActionMetadata(baseNextActions, task, result, responseLanguage, handoffSuggestion);
  return buildCommandResultCard({
    surface: "dove.auto",
    command: "run_dove_auto",
    packetId: task.id,
    title: task.title,
    runId: result.id,
    status: result.status,
    outcome: result.outcome,
    summary: result.outcome,
    stopReason: result.stopReason,
    taskStatusBefore: result.taskStatusBefore,
    taskStatusAfter: result.taskStatusAfter,
    evidenceLinks,
    artifactRefs,
    executionReceipt,
    durableWrites: taskWorkflowDurableWrites(responseLanguage, { runtime: true, lifecycle: result.taskStatusBefore !== result.taskStatusAfter || Boolean(result.boundary) }),
    boundary: result.boundary ?? task.boundary ?? null,
    nextActions,
    preActionGuidanceSummary: context.preActionGuidanceSummary ?? null,
    foreground: result.foreground,
    background: result.background,
    daemon: result.daemon
  }, responseLanguage);
}
function operatorResultCard(result = {}, context = {}, responseLanguage = "zh") {
  const evidenceLinks = result.iterations?.flatMap((iteration) => normalizeStringArray24(iteration.evidenceLinks ?? iteration.output?.evidenceLinks)) ?? [];
  const artifactRefs = result.iterations?.flatMap((iteration) => normalizeStringArray24(iteration.artifactRefs ?? iteration.output?.artifactRefs)) ?? [];
  const executionReceipt = latestExecutionReceipt(result.iterations, result.executionReceipt);
  const createdBlockerCount = result.blockerPlanConversion?.createdCount ?? result.blockerPlanConversion?.createdMissions?.length ?? 0;
  const reusedBlockerCount = result.blockerPlanConversion?.reusedCount ?? result.blockerPlanConversion?.reusedMissions?.length ?? 0;
  const proposedBlockerCount = result.blockerPlanConversion?.proposedBlockedTaskIds?.length ?? 0;
  const skippedHostPassCount = result.skippedHostPassTaskIds?.length ?? 0;
  const runtimeRecorded = result.runtimeRecorded !== false;
  const handoffSuggestion = operatorHandoffSuggestion(result, responseLanguage);
  const requiredActions = normalizeStringArray24([
    ...normalizeStringArray24(result.awaitingRequiredActions),
    ...normalizeStringArray24(result.boundaryRequiredActions)
  ]);
  const baseNextActions = result.awaitingResultTaskIds?.length > 0 || result.actionRequired === true ? [{ title: doveText(responseLanguage, "resultCardNextProvideEvidence"), command: "project:dove.status", requires: result.awaitingResultTaskIds, requiredActions }] : [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: context.nextAction ?? "project:dove.status" }];
  const nextActions = baseNextActions.map((action) => handoffSuggestion ? {
    ...action,
    boundaryId: handoffSuggestion.boundaryId,
    boundaryType: handoffSuggestion.boundaryType,
    ownerRole: handoffSuggestion.ownerRole,
    nextRole: handoffSuggestion.nextRole,
    requiredInputs: handoffSuggestion.requiredInputs,
    requiredActions: handoffSuggestion.requiredActions,
    requires: action.requires ?? handoffSuggestion.requires,
    handoffSuggestion
  } : action);
  return buildCommandResultCard({
    surface: "dove.operator",
    command: "run_dove_operator",
    packetIds: result.updatedTaskIds,
    runId: result.id,
    status: result.status,
    outcome: result.outcome,
    summary: `updated=${result.updatedTaskIds?.length ?? 0}; awaiting-results=${result.awaitingResultTaskIds?.length ?? 0}; skipped-host-pass=${skippedHostPassCount}; blockers-proposed=${proposedBlockerCount}; blockers-created=${createdBlockerCount}; blockers-reused=${reusedBlockerCount}`,
    stopReason: result.stopReason,
    evidenceLinks,
    artifactRefs,
    executionReceipt,
    durableWrites: [
      ...runtimeRecorded ? [doveText(responseLanguage, "resultCardRuntimeResultRecorded")] : [],
      ...result.updatedTaskIds?.length > 0 ? [doveText(responseLanguage, "resultCardTaskPacketUpdated"), doveText(responseLanguage, "resultCardTaskIndexUpdated"), doveText(responseLanguage, "resultCardRuntimeEventRecorded")] : [],
      ...createdBlockerCount > 0 ? [`${ARTIFACT_PATHS.taskPacketsIndex}: blocker investigation missions created=${createdBlockerCount}`] : []
    ],
    nextAction: context.nextAction ?? "project:dove.status",
    nextActions,
    preActionGuidanceSummary: context.preActionGuidanceSummary ?? null,
    foreground: result.foreground,
    background: result.background,
    daemon: result.daemon
  }, responseLanguage);
}
function statusAdjustmentResultCard(result = {}, responseLanguage = "zh") {
  const applied = Array.isArray(result.applied) ? result.applied : [];
  const skipped = Array.isArray(result.skipped) ? result.skipped : [];
  const rejected = Array.isArray(result.rejected) ? result.rejected : [];
  return buildCommandResultCard({
    surface: "dove.status",
    command: "apply_dove_status_adjustments",
    packetIds: applied.map((item) => item.packetId),
    status: result.status,
    outcome: result.status,
    summary: `applied=${applied.length}; skipped=${skipped.length}; rejected=${rejected.length}`,
    executionReceipt: result.executionReceipt ?? result.executionReceipts?.[0] ?? null,
    durableWrites: applied.length > 0 ? [doveText(responseLanguage, "resultCardTaskPacketUpdated"), doveText(responseLanguage, "resultCardTaskIndexUpdated"), doveText(responseLanguage, "resultCardRuntimeEventRecorded"), doveText(responseLanguage, "resultCardRuntimeResultRecorded")] : [],
    nextAction: "project:dove.status",
    nextActions: [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: "project:dove.status" }],
    foreground: true,
    background: false,
    daemon: false
  }, responseLanguage);
}
function readOperatorLessonsIndex(root) {
  return readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] });
}
function activeLessons(root, packetId = null) {
  const lessons = readOperatorLessonsIndex(root);
  return (Array.isArray(lessons.lessons) ? lessons.lessons : []).filter((lesson) => {
    const status = lesson.status ?? "active";
    const packetIds = normalizeStringArray24(lesson.packetIds);
    return status === "active" && (!packetId || packetIds.length === 0 || packetIds.includes(packetId));
  }).map((lesson) => ({ id: lesson.id, title: lesson.title, mustObey: lesson.mustObey ?? true, nextTime: lesson.nextTime ?? [] }));
}
function preActionGuidanceForTask(root, surface, task = {}, context = {}, responseLanguage = "zh") {
  return buildPreActionGuidance({
    surface,
    responseLanguage,
    request: context.request ?? task.summary ?? task.title ?? null,
    roleId: context.roleId ?? task.ownerRole ?? task.nextRole,
    subagentSpecialty: context.subagentSpecialty,
    packet: task,
    currentContext: {
      domain: task.domain ?? context.domain ?? null,
      stage: task.stage ?? context.stage ?? null,
      primaryRole: context.roleId ?? task.ownerRole ?? task.nextRole ?? null
    },
    operatorLessons: context.operatorLessons ?? readOperatorLessonsIndex(root),
    nextAction: context.nextAction ?? task.nextAction ?? null,
    routeHint: context.routeHint ?? task.nextAction ?? null,
    workflowKind: context.workflowKind,
    domain: task.domain ?? context.domain ?? null,
    stage: task.stage ?? context.stage ?? null,
    tags: context.tags ?? []
  });
}
function preActionGuidanceSummaryForTask(root, surface, task = {}, context = {}, responseLanguage = "zh") {
  return summarizePreActionGuidance(preActionGuidanceForTask(root, surface, task, context, responseLanguage));
}
function requestTextFromArgs(args = {}) {
  return normalizeString10(args.goal ?? args.objective ?? args.prompt ?? args.title ?? args.summary ?? args.request ?? args.userRequest, null);
}
function readStateForTaskContract(root) {
  return readJson(root, ARTIFACT_PATHS.state, createDefaultState);
}
var MISSION_PROPOSAL_VERSION = 1;
var MISSION_TOP_LEVEL_VOLATILE_FIELDS = /* @__PURE__ */ new Set(["createdAt", "updatedAt", "completedAt"]);
var MISSION_REPLAY_CONTROL_FIELDS = /* @__PURE__ */ new Set(["proposalDigest", "confirm", "confirmed"]);
var INITIAL_DEMAND_GOVERNANCE_FIELDS = /* @__PURE__ */ new Set([
  "creatorKind",
  "ownerRole",
  "nextRole",
  "boundary",
  "handoff"
]);
var CANONICAL_BOUNDARY_KEYS = /* @__PURE__ */ new Set([
  "id",
  "type",
  "status",
  "packetId",
  "runId",
  "sourceSurface",
  "command",
  "reason",
  "summary",
  "requiredInputs",
  "requiredActions",
  "ownerRole",
  "nextRole",
  "createdAt",
  "resolvedAt",
  "resolution"
]);
var CANONICAL_HANDOFF_KEYS = /* @__PURE__ */ new Set([
  "id",
  "status",
  "fromRole",
  "toRole",
  "reason",
  "summary",
  "boundaryId",
  "sourceRunId",
  "requestedAt",
  "acceptedAt",
  "completedAt"
]);
function assertInitialDemandDoesNotSetGovernance(args = {}, surface = "Dove mission") {
  if (hasExplicitConfirmation(args)) {
    return;
  }
  const supplied = [...INITIAL_DEMAND_GOVERNANCE_FIELDS].filter((key) => Object.hasOwn(args, key));
  if (supplied.length > 0) {
    throw new Error(`${surface} initial demand cannot set system-owned governance input: ${supplied.join(", ")}.`);
  }
}
function assertCanonicalMissionGovernanceReplay(args = {}, surface = "Dove mission") {
  if (!hasExplicitConfirmation(args)) {
    return;
  }
  for (const [field, allowedKeys] of [["boundary", CANONICAL_BOUNDARY_KEYS], ["handoff", CANONICAL_HANDOFF_KEYS]]) {
    const value = args[field];
    if (value === null || value === void 0) {
      continue;
    }
    if (!value || typeof value !== "object" || Array.isArray(value)) {
      throw new Error(`${surface} replay requires ${field} to be a canonical object or null.`);
    }
    const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
    if (unknown.length > 0) {
      throw new Error(`${surface} replay does not accept unknown ${field} input: ${unknown.join(", ")}.`);
    }
  }
}
function stableContractValue(value) {
  if (Array.isArray(value)) {
    return value.map(stableContractValue);
  }
  if (value && typeof value === "object") {
    return Object.fromEntries(
      Object.entries(value).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => [key, stableContractValue(item)])
    );
  }
  return value;
}
function stableMissionPacket(packet) {
  if (!packet || typeof packet !== "object" || Array.isArray(packet)) {
    return packet;
  }
  return Object.fromEntries(
    Object.entries(packet).filter(([key]) => !MISSION_TOP_LEVEL_VOLATILE_FIELDS.has(key))
  );
}
function canonicalMissionWorkspace(root) {
  return fs23.realpathSync.native(path26.resolve(root));
}
function missionProposalMutationMode(root, args = {}) {
  const explicitMode = Object.prototype.hasOwnProperty.call(args, "mutationMode") ? normalizeMutationMode(args.mutationMode) : null;
  const currentMode = currentMutationContext(root)?.mutationMode ?? null;
  if (currentMode && explicitMode && currentMode !== explicitMode) {
    throw new Error(`Dove mission mutationMode ${explicitMode} does not match the active mutation context mode ${currentMode}.`);
  }
  return currentMode ?? explicitMode ?? "direct-process";
}
function missionProposalEnvelope(root, contract, args = {}) {
  return {
    version: MISSION_PROPOSAL_VERSION,
    action: "create-dove-task",
    workspace: canonicalMissionWorkspace(root),
    mutationMode: missionProposalMutationMode(root, args),
    initMaterializationRequired: contract.proposedInit !== null,
    proposedInit: stableMissionPacket(contract.proposedInit),
    proposedTask: stableMissionPacket(contract.packet),
    checklistTasks: contract.checklistTasks.map(stableMissionPacket)
  };
}
function missionReplayFields(args = {}) {
  const { mutationMode: _mutationMode, proposalToken: _proposalToken, ...replayArgs } = Object.fromEntries(
    Object.entries(args).filter(([key]) => !MISSION_REPLAY_CONTROL_FIELDS.has(key))
  );
  return replayArgs;
}
function missionProposalDigest(root, contract, args = {}, replayFields = missionReplayFields(args)) {
  return crypto13.createHash("sha256").update(JSON.stringify(stableContractValue({
    envelope: missionProposalEnvelope(root, contract, args),
    replayFields
  }))).digest("hex");
}
function missionContractReplayFields(root, contract, proposalVersion = MISSION_PROPOSAL_VERSION) {
  const {
    packet,
    checklistTasks,
    proposedInit,
    responseLanguage
  } = contract;
  return {
    proposalVersion,
    proposalWorkspace: canonicalMissionWorkspace(root),
    responseLanguage,
    ...proposedInit ? {
      initId: proposedInit.id,
      initTitle: proposedInit.title,
      initObjective: proposedInit.summary,
      initDomain: proposedInit.domain,
      initStatus: proposedInit.status,
      initArtifactRefs: proposedInit.artifactRefs
    } : {},
    id: packet.id,
    goal: packet.summary,
    title: packet.title,
    summary: packet.summary,
    stage: packet.stage,
    domain: packet.domain,
    level: packet.level,
    creatorKind: packet.creatorKind,
    status: packet.status,
    dependencies: packet.dependencies,
    blockedBy: packet.blockedBy,
    ownerRole: packet.ownerRole,
    nextRole: packet.nextRole,
    boundary: packet.boundary,
    handoff: packet.handoff,
    currentFocus: packet.currentFocus,
    nextAction: packet.nextAction,
    evidenceExpectations: packet.evidenceExpectations,
    workContract: packet.workContract,
    executionContract: packet.executionContract,
    artifactRefs: packet.artifactRefs,
    contextPolicy: packet.contextPolicy,
    lessonIds: packet.lessonIds,
    checklist: false,
    autoChecklist: false,
    createChecklist: false,
    checklistItems: checklistTasks.map((task) => ({
      id: task.id,
      title: task.title,
      summary: task.summary,
      level: task.level,
      stage: task.stage,
      domain: task.domain,
      status: task.status,
      dependencies: task.dependencies,
      blockedBy: task.blockedBy,
      evidenceExpectations: task.evidenceExpectations,
      artifactRefs: task.artifactRefs,
      nextAction: task.nextAction,
      workContract: task.workContract,
      executionContract: task.executionContract
    }))
  };
}
function missionConfirmArgs(root, contract, mutationMode) {
  const replayFields = missionContractReplayFields(root, contract);
  return {
    confirmed: true,
    mutationMode,
    ...replayFields,
    proposalDigest: missionProposalDigest(root, contract, { mutationMode }, replayFields)
  };
}
function buildPacket(root, args, init, classification, overrides = {}) {
  const state = readStateForTaskContract(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args, { state });
  const timestamp = nowIso();
  const creatorKind = overrides.creatorKind ?? normalizeAllowed3(args.creatorKind, DOVE_TASK_CREATOR_KINDS, "user");
  const dependencies = normalizeStringArray24(args.dependencies ?? args.dependencyIds);
  const blockedBy = normalizeStringArray24(args.blockedBy ?? args.blockerIds);
  const requestedTitle = normalizeString10(overrides.title ?? args.title ?? args.goal ?? args.objective ?? args.prompt, doveText(responseLanguage, "taskFallbackTitle"));
  const id = normalizeTaskPacketId(overrides.id ?? args.id ?? args.packetId ?? `task-${slugify14(requestedTitle)}-${Date.now().toString(36)}`);
  const level = overrides.level ?? normalizeMissionLevel(args, state.settings.taskModel.userDefaultLevel);
  const status = normalizeStatus3(overrides.status ?? args.status, dependencies.length > 0 || blockedBy.length > 0 ? "blocked" : "ready");
  const basePacket = {
    id,
    title: requestedTitle,
    summary: normalizeString10(overrides.summary ?? args.summary ?? args.goal ?? args.objective ?? args.prompt, requestedTitle),
    parentId: overrides.parentId ?? init.id,
    rootId: overrides.rootId ?? init.rootId ?? init.id,
    level,
    creatorKind,
    stage: classification.stage,
    domain: classification.domain,
    status,
    lifecycleStatus: status,
    dependencies,
    blockedBy,
    createdAt: timestamp,
    updatedAt: timestamp,
    completedAt: null,
    blockedReason: null,
    killedAt: null,
    killReason: null,
    ownerRole: normalizeDovePrimaryRoleId(overrides.ownerRole ?? args.ownerRole, ownerRoleFor2(classification)),
    nextRole: normalizeDovePrimaryRoleId(overrides.nextRole ?? args.nextRole, overrides.ownerRole ?? args.ownerRole ?? ownerRoleFor2(classification)),
    boundary: normalizeDoveBoundary(overrides.boundary ?? args.boundary, null),
    boundaryHistory: [],
    handoff: normalizeDoveHandoff(overrides.handoff ?? args.handoff, null),
    lastTransition: null,
    lessonIds: normalizeStringArray24(args.lessonIds),
    artifactRefs: normalizeStringArray24(args.artifactRefs ?? args.artifactPaths),
    contextPolicy: normalizeString10(args.contextPolicy, DOVE_AUDIO_CONTEXT_POLICY),
    packetPath: taskPacketPath2(id),
    packetContextPath: taskContextPath2(id),
    currentFocus: normalizeString10(overrides.currentFocus ?? args.currentFocus ?? args.goal ?? args.objective ?? args.prompt, requestedTitle),
    nextAction: normalizeString10(overrides.nextAction ?? args.nextAction, nextCommandFor(classification)),
    evidenceExpectations: normalizeStringArray24([
      ...normalizeStringArray24(args.evidenceExpectations),
      ...normalizeStringArray24(args.acceptanceChecks)
    ]),
    ...overrides.extraFields && typeof overrides.extraFields === "object" && !Array.isArray(overrides.extraFields) ? overrides.extraFields : {}
  };
  const workContract = normalizeWorkContract(basePacket, args, classification, responseLanguage);
  return {
    ...basePacket,
    workContract,
    executionContract: mergeExecutionContract(basePacket, args, classification, workContract, responseLanguage)
  };
}
function explicitChecklistItems(args = {}) {
  for (const value of [args.checklistItems, args.subtasks, args.systemTasks]) {
    if (Array.isArray(value)) {
      return { explicit: true, items: value };
    }
  }
  if (Array.isArray(args.checklist)) {
    return { explicit: true, items: args.checklist };
  }
  return { explicit: false, items: [] };
}
function normalizeChecklistItem(item, index, responseLanguage = "zh") {
  if (typeof item === "string") {
    return { title: item };
  }
  const source = item && typeof item === "object" && !Array.isArray(item) ? item : {};
  return {
    id: source.id,
    title: normalizeString10(source.title ?? source.goal ?? source.summary, doveText(responseLanguage, "checklistItem", { index: index + 1 })),
    summary: normalizeString10(source.summary ?? source.goal ?? source.title, ""),
    level: requestedLevel(source),
    stage: source.stage ?? source.missionStage,
    domain: source.domain ?? source.doveDomain ?? source.missionDomain,
    status: source.status,
    dependencies: normalizeStringArray24(source.dependencies ?? source.dependencyIds),
    blockedBy: normalizeStringArray24(source.blockedBy ?? source.blockerIds),
    evidenceExpectations: normalizeStringArray24(source.evidenceExpectations),
    artifactRefs: normalizeStringArray24(source.artifactRefs ?? source.artifactPaths),
    nextAction: source.nextAction
  };
}
function shouldAutoCreateChecklist(args = {}) {
  if (args.autoChecklist === false || args.createChecklist === false || args.checklist === false) {
    return false;
  }
  if (args.autoChecklist === true || args.createChecklist === true || args.checklist === true) {
    return true;
  }
  const evidenceCount = normalizeStringArray24(args.evidenceExpectations).length;
  const artifactCount = normalizeStringArray24(args.artifactRefs ?? args.artifactPaths).length;
  const text3 = [args.goal, args.objective, args.prompt, args.title, args.summary].map((value) => String(value ?? "")).join(" ");
  const hasComplexVerb = /(implement|refactor|validate|verify|test|document|workflow|pipeline|loop|review|实现|重构|验证|测试|文档|流程|闭环|审查)/iu.test(text3);
  const hasComposition = /(\band\b|\bthen\b|\bwith\b|,|，|、|并|和|以及|同时|然后)/iu.test(text3);
  return evidenceCount >= 2 || artifactCount >= 3 || hasComplexVerb && hasComposition;
}
function generatedChecklistItems(args = {}, classification, responseLanguage = "zh") {
  const explicit = explicitChecklistItems(args);
  if (explicit.explicit) {
    return explicit.items.map((item, index) => normalizeChecklistItem(item, index, responseLanguage));
  }
  if (!shouldAutoCreateChecklist(args)) {
    return [];
  }
  const validationTitle = classification.stage === "audit" ? doveText(responseLanguage, "checklistReviewTitle") : doveText(responseLanguage, "checklistValidateTitle");
  return [
    { title: doveText(responseLanguage, "checklistClarifyTitle"), summary: doveText(responseLanguage, "checklistClarifySummary") },
    { title: doveText(responseLanguage, "checklistWorkTitle"), summary: doveText(responseLanguage, "checklistWorkSummary") },
    { title: validationTitle, summary: doveText(responseLanguage, "checklistValidateSummary") }
  ];
}
function buildChecklistTasks(root, args, parent, classification, responseLanguage = "zh") {
  const timestamp = nowIso();
  return generatedChecklistItems(args, classification, responseLanguage).map((item, index) => {
    const normalized = normalizeChecklistItem(item, index, responseLanguage);
    const title = normalized.title;
    const id = normalizeTaskPacketId(normalized.id ?? `${parent.id}-checklist-${index + 1}-${slugify14(title)}`);
    const level = normalizeChildLevel(normalized, parent.level);
    const status = normalizeStatus3(normalized.status, normalized.dependencies.length > 0 || normalized.blockedBy.length > 0 ? "blocked" : "ready");
    const baseTask = {
      id,
      title,
      summary: normalizeString10(normalized.summary, title),
      parentId: parent.id,
      rootId: parent.rootId,
      level,
      creatorKind: "system",
      stage: normalizeAllowed3(normalized.stage, DOVE_TASK_STAGES, parent.stage),
      domain: normalizeAllowed3(normalized.domain, DOVE_TASK_DOMAINS, parent.domain),
      status,
      lifecycleStatus: status,
      dependencies: normalized.dependencies,
      blockedBy: normalized.blockedBy,
      createdAt: timestamp,
      updatedAt: timestamp,
      completedAt: null,
      blockedReason: null,
      killedAt: null,
      killReason: null,
      ownerRole: normalizeDovePrimaryRoleId(parent.ownerRole, ownerRoleFor2(parent)),
      nextRole: normalizeDovePrimaryRoleId(parent.nextRole, parent.ownerRole ?? ownerRoleFor2(parent)),
      boundary: null,
      boundaryHistory: [],
      handoff: null,
      lastTransition: null,
      lessonIds: [],
      artifactRefs: normalized.artifactRefs,
      contextPolicy: parent.contextPolicy,
      packetPath: taskPacketPath2(id),
      packetContextPath: taskContextPath2(id),
      currentFocus: normalizeString10(normalized.summary, title),
      nextAction: normalizeString10(normalized.nextAction, parent.nextAction),
      evidenceExpectations: normalized.evidenceExpectations
    };
    const taskClassification = { stage: baseTask.stage, domain: baseTask.domain };
    const workContract = normalizeWorkContract(baseTask, normalized, taskClassification, responseLanguage);
    return {
      ...baseTask,
      workContract,
      executionContract: mergeExecutionContract(baseTask, normalized, taskClassification, workContract, responseLanguage)
    };
  });
}
function checklistContract(tasks = []) {
  return {
    autoSelected: tasks.length > 0,
    itemCount: tasks.length,
    items: tasks.map((task) => ({
      id: task.id,
      title: task.title,
      summary: task.summary,
      parentId: task.parentId,
      rootId: task.rootId,
      level: task.level,
      creatorKind: task.creatorKind,
      stage: task.stage,
      domain: task.domain,
      status: task.status,
      dependencies: task.dependencies,
      blockedBy: task.blockedBy,
      evidenceExpectations: task.evidenceExpectations,
      artifactRefs: task.artifactRefs,
      nextAction: task.nextAction
    }))
  };
}
function initDoveGoal(root, args = {}) {
  assertGovernanceMutationRegistered("init-dove-goal", "guarded");
  ensureWorkspace(root);
  const timestamp = nowIso();
  const state = loadState(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args, { state });
  const index = loadTaskIndex(root);
  const existing = initPacket(index);
  const title = normalizeString10(args.title ?? args.goal ?? state.dove.title, state.dove.title);
  const objective = normalizeString10(args.objective ?? args.goal ?? args.summary, state.dove.objective);
  const packetId = existing?.id ?? normalizeTaskPacketId(args.id ?? "init");
  const packet = {
    ...existing ?? {},
    id: packetId,
    title,
    summary: objective,
    parentId: null,
    rootId: packetId,
    level: 0,
    creatorKind: "user",
    stage: "plan",
    domain: normalizeAllowed3(args.domain ?? args.doveDomain, DOVE_TASK_DOMAINS, "engineering"),
    status: normalizeStatus3(args.status, "ready"),
    lifecycleStatus: normalizeStatus3(args.status, "ready"),
    dependencies: [],
    blockedBy: [],
    createdAt: existing?.createdAt ?? timestamp,
    updatedAt: timestamp,
    completedAt: null,
    blockedReason: null,
    killedAt: null,
    killReason: null,
    ownerRole: "planner",
    nextRole: "planner",
    boundary: null,
    boundaryHistory: [],
    handoff: null,
    lastTransition: null,
    lessonIds: normalizeStringArray24(existing?.lessonIds),
    artifactRefs: normalizeStringArray24(args.artifactRefs ?? existing?.artifactRefs),
    contextPolicy: DOVE_AUDIO_CONTEXT_POLICY,
    packetPath: taskPacketPath2(packetId),
    packetContextPath: taskContextPath2(packetId),
    currentFocus: objective,
    nextAction: "project:dove.mission"
  };
  writePacket2(root, packet);
  const nextIndex = saveTaskIndex(root, upsertIndexItem(index, packet));
  saveState(root, {
    ...state,
    dove: {
      ...state.dove,
      title,
      objective
    },
    pipeline: {
      ...state.pipeline,
      currentStage: "init",
      resumeCommand: "project:dove.mission",
      updatedAt: timestamp
    },
    orchestration: {
      ...state.orchestration,
      activeTaskIds: nextIndex.taskModel.activeTaskIds,
      currentFocus: objective,
      nextAction: doveText(responseLanguage, "initNextActionDisplay")
    }
  });
  return {
    status: existing ? "updated" : "created",
    initId: packet.id,
    init: packet,
    nextAction: "project:dove.mission",
    responseLanguage,
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex
  };
}
function buildProposedInitPacket(root, args = {}, classification = { domain: "engineering" }, responseLanguage = "zh") {
  const state = readStateForTaskContract(root);
  const timestamp = nowIso();
  const title = normalizeString10(args.initTitle ?? args.projectTitle ?? args.workspaceTitle ?? state.dove.title, state.dove.title);
  const objective = normalizeString10(args.initObjective ?? args.initGoal ?? args.projectObjective ?? args.projectGoal ?? state.dove.objective ?? args.goal ?? args.objective ?? args.summary, state.dove.objective ?? doveText(responseLanguage, "projectTitleFallback"));
  const packetId = normalizeTaskPacketId(args.initId ?? "init");
  return {
    id: packetId,
    title,
    summary: objective,
    parentId: null,
    rootId: packetId,
    level: 0,
    creatorKind: "user",
    stage: "plan",
    domain: normalizeAllowed3(args.initDomain ?? args.projectDomain ?? classification.domain, DOVE_TASK_DOMAINS, "engineering"),
    status: normalizeStatus3(args.initStatus, "ready"),
    lifecycleStatus: normalizeStatus3(args.initStatus, "ready"),
    dependencies: [],
    blockedBy: [],
    createdAt: timestamp,
    updatedAt: timestamp,
    completedAt: null,
    blockedReason: null,
    killedAt: null,
    killReason: null,
    ownerRole: "planner",
    nextRole: "planner",
    boundary: null,
    boundaryHistory: [],
    handoff: null,
    lastTransition: null,
    lessonIds: [],
    artifactRefs: normalizeStringArray24(args.initArtifactRefs),
    contextPolicy: DOVE_AUDIO_CONTEXT_POLICY,
    packetPath: taskPacketPath2(packetId),
    packetContextPath: taskContextPath2(packetId),
    currentFocus: objective,
    nextAction: "project:dove.mission"
  };
}
function buildDoveTaskContract(root, args = {}) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const index = loadTaskIndex(root);
  const classification = classifyTask(args);
  const existingInit = initPacket(index);
  const proposedInit = existingInit ? null : buildProposedInitPacket(root, args, classification, responseLanguage);
  const init = existingInit ?? proposedInit;
  const packet = buildPacket(root, args, init, classification);
  const checklistTasks = buildChecklistTasks(root, args, packet, classification, responseLanguage);
  const checklistProposal = checklistContract(checklistTasks);
  return {
    index,
    init,
    proposedInit,
    initMaterializationRequired: proposedInit !== null,
    classification,
    packet,
    checklistTasks,
    checklistProposal,
    blockers: [...packet.dependencies, ...packet.blockedBy],
    executionContract: packet.executionContract,
    executionReadiness: doveExecutionContractReadiness(packet.executionContract),
    applicableLessons: activeLessons(root, packet.id),
    responseLanguage
  };
}
function assertMissionReplayTargetsAvailable(root, contract) {
  const targets = [contract.proposedInit, contract.packet, ...contract.checklistTasks].filter(Boolean);
  const targetIds = targets.map((target) => target.id);
  const duplicateIds = targetIds.filter((id, index) => targetIds.indexOf(id) !== index);
  if (duplicateIds.length > 0) {
    throw new Error(`Dove mission replay contains duplicate target task ids: ${[...new Set(duplicateIds)].join(", ")}.`);
  }
  const currentIndex = loadTaskIndex(root);
  const indexedIds = new Set((currentIndex.items ?? []).map((item) => item.id));
  for (const target of targets) {
    const packetExists = fs23.existsSync(path26.join(root, taskPacketPath2(target.id)));
    const contextExists = fs23.existsSync(path26.join(root, taskContextPath2(target.id)));
    if (indexedIds.has(target.id) || packetExists || contextExists) {
      throw new Error(`Dove mission replay target task id already exists or changed: ${target.id}. Request a fresh proposal.`);
    }
  }
}
function materializeDoveTask(root, contract) {
  const { index, packet, checklistTasks, checklistProposal, classification, blockers, applicableLessons, responseLanguage, proposedInit } = contract;
  let nextIndex = index;
  if (proposedInit) {
    writePacket2(root, proposedInit);
    nextIndex = upsertIndexItem(nextIndex, proposedInit);
  }
  writePacket2(root, packet);
  nextIndex = upsertIndexItem(nextIndex, packet);
  for (const checklistTask of checklistTasks) {
    writePacket2(root, checklistTask);
    nextIndex = upsertIndexItem(nextIndex, checklistTask);
  }
  nextIndex = saveTaskIndex(root, nextIndex);
  const state = loadState(root);
  saveState(root, {
    ...state,
    ...proposedInit ? {
      dove: {
        ...state.dove,
        title: proposedInit.title,
        objective: proposedInit.summary
      },
      pipeline: {
        ...state.pipeline,
        currentStage: state.pipeline?.currentStage ?? "init",
        resumeCommand: "project:dove.mission",
        updatedAt: proposedInit.updatedAt
      }
    } : {},
    orchestration: {
      ...state.orchestration,
      activeTaskIds: nextIndex.taskModel.activeTaskIds,
      currentFocus: packet.currentFocus,
      nextAction: packet.nextAction
    }
  });
  const handoffRoutes = packet.workContract?.recommendedRoutes ?? [];
  return {
    createdInit: proposedInit,
    initMaterializationRequired: proposedInit !== null,
    createdTask: packet,
    createdChecklistTasks: checklistTasks,
    checklistProposal,
    classification,
    blockers,
    evidenceExpectations: packet.evidenceExpectations,
    workContract: packet.workContract,
    executionContract: packet.executionContract,
    executionReadiness: doveExecutionContractReadiness(packet.executionContract),
    nextAction: packet.nextAction,
    recommendedNextCommand: packet.nextAction,
    recommendedRoutes: handoffRoutes,
    handoffRoutes,
    applicableLessons,
    responseLanguage,
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex
  };
}
function objectArray(value) {
  return Array.isArray(value) ? value.filter((item) => item !== null && item !== void 0) : [];
}
function plainObject3(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function normalizePlanMissionSource(item) {
  if (typeof item === "string") {
    return { title: item, summary: item };
  }
  const source = plainObject3(item);
  const title = normalizeString10(source.title ?? source.goal ?? source.objective ?? source.summary, null);
  return {
    ...source,
    title,
    summary: normalizeString10(source.summary ?? source.goal ?? source.objective, title)
  };
}
function planMissionSources(args = {}, planTask, responseLanguage = "zh") {
  const planConversion = plainObject3(args.planConversion);
  const missionInputs = [
    ...objectArray(planConversion.plannedMissions),
    ...objectArray(planConversion.resultingMissions),
    ...objectArray(planConversion.missions),
    ...objectArray(args.plannedMissions),
    ...objectArray(args.resultingMissions),
    ...objectArray(args.missions)
  ];
  const globalChildren = [
    ...objectArray(planConversion.childMissions),
    ...objectArray(args.childMissions)
  ];
  if (missionInputs.length === 0 && globalChildren.length === 0) {
    return [];
  }
  const topMissions = missionInputs.length > 0 ? missionInputs.map((item) => normalizePlanMissionSource(item)) : globalChildren.map((item) => normalizePlanMissionSource(item));
  return topMissions.map((mission, index) => ({
    ...mission,
    childMissions: [
      ...objectArray(mission.childMissions),
      ...missionInputs.length > 0 && index === 0 ? globalChildren : []
    ]
  }));
}
function flattenPlanMissionSources(sources = []) {
  return sources.flatMap((source) => [source, ...flattenPlanMissionSources(objectArray(source.childMissions))]);
}
function planOutputExecutionBlock(task, args = {}, responseLanguage = "zh") {
  if (task.stage !== "plan") {
    return null;
  }
  const sources = planMissionSources(args, task, responseLanguage);
  if (sources.length === 0) {
    return {
      status: "plan-output-not-executable",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "plan-output-not-executable",
      requiredActions: ["provide-executable-child-missions", "include-execution-contracts"],
      message: responseLanguage === "en" ? "A completed planning pass must return explicit child missions with executable contracts." : "\u5B8C\u6210\u89C4\u5212 pass \u5FC5\u987B\u8FD4\u56DE\u5E26\u53EF\u6267\u884C\u5408\u540C\u7684\u663E\u5F0F\u5B50 mission\u3002",
      nextAction: "record_dove_mission_pass",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const notExecutable = flattenPlanMissionSources(sources).map((source, index) => {
    const hasTitle = Boolean(normalizeString10(source.title ?? source.goal ?? source.objective ?? source.summary, null));
    const hasExplicitContract = Object.keys(plainObject3(source.executionContract)).length > 0;
    const readiness = doveExecutionContractReadiness(hasExplicitContract ? source.executionContract : null);
    const missing = [hasTitle ? null : "title", ...readiness.missing].filter(Boolean);
    return hasExplicitContract && readiness.ready && hasTitle ? null : {
      index: index + 1,
      title: source.title,
      missing
    };
  }).filter(Boolean);
  if (notExecutable.length === 0) {
    return null;
  }
  return {
    status: "plan-output-not-executable",
    requestedStatus: "completed",
    packetId: task.id,
    title: task.title,
    boundaryType: "plan-output-not-executable",
    notExecutable,
    requiredActions: ["provide-executable-child-missions", "include-action-implementation-criteria-and-failure-routes"],
    message: responseLanguage === "en" ? "Each child mission returned by a planning pass must include executionContract.action, implementation, convergence.criteria, and failureRoutes." : "\u89C4\u5212 pass \u8FD4\u56DE\u7684\u6BCF\u4E2A\u5B50 mission \u90FD\u5FC5\u987B\u5305\u542B executionContract.action\u3001implementation\u3001convergence.criteria \u548C failureRoutes\u3002",
    nextAction: "record_dove_mission_pass",
    proposalOnly: true,
    noAutoApply: true,
    writes: []
  };
}
function derivedClassification(source = {}, fallback = {}) {
  return {
    stage: normalizeAllowed3(source.stage ?? source.missionStage, DOVE_TASK_STAGES, fallback.stage ?? "execute"),
    domain: normalizeAllowed3(source.domain ?? source.doveDomain ?? source.missionDomain, DOVE_TASK_DOMAINS, fallback.domain ?? "engineering"),
    rationale: ["derived-from-plan-mission"]
  };
}
function deterministicDerivedTaskId(source, prefix, title) {
  const explicitId = source.id ?? source.packetId ?? source.taskPacketId;
  if (explicitId) {
    return normalizeTaskPacketId(explicitId);
  }
  return deterministicBoundedTaskPacketId(prefix, title);
}
function buildDerivedTask(root, init, parent, source, options = {}) {
  const responseLanguage = options.responseLanguage ?? resolveDoveResponseLanguage(root, source);
  const title = normalizeString10(source.title ?? source.goal ?? source.objective ?? source.summary, null);
  if (!title) {
    throw new Error("Derived mission requires an explicit title, goal, objective, or summary.");
  }
  const classification = derivedClassification(source, options.classification);
  const parentLevel = Number.isFinite(parent.level) ? parent.level : 0;
  const level = normalizeLevel2(source.level ?? source.taskLevel ?? source.missionLevel, options.level ?? Math.max(3, parentLevel + 1));
  if (options.minimumLevel !== void 0 && level < options.minimumLevel) {
    throw new Error(`Derived mission ${title} level ${level} must be at least ${options.minimumLevel}.`);
  }
  const artifactRefs = normalizeStringArray24([
    ...normalizeStringArray24(source.artifactRefs ?? source.artifactPaths),
    ...options.sourcePlanTaskId ? [taskPacketPath2(options.sourcePlanTaskId)] : []
  ]);
  return buildPacket(root, {
    ...source,
    title,
    summary: normalizeString10(source.summary ?? source.goal ?? source.objective, title),
    stage: classification.stage,
    domain: classification.domain,
    status: "pending",
    creatorKind: "system",
    artifactRefs,
    nextAction: source.nextAction ?? (classification.stage === "plan" ? "project:dove.mission" : nextCommandFor(classification))
  }, init, classification, {
    id: deterministicDerivedTaskId(source, options.idPrefix ?? "task", title),
    parentId: parent.id,
    rootId: parent.rootId ?? parent.id,
    level,
    status: "pending",
    creatorKind: "system",
    extraFields: {
      sourcePlanTaskId: options.sourcePlanTaskId ?? null,
      sourceMissionPassRunId: options.sourceMissionPassRunId ?? null,
      derivedFrom: options.derivedFrom ?? "plan-mission-pass"
    }
  });
}
function persistDerivedTaskIndex(root, index, focusTask = null) {
  const nextIndex = saveTaskIndex(root, index);
  const state = loadState(root);
  saveState(root, {
    ...state,
    orchestration: {
      ...state.orchestration,
      activeTaskIds: nextIndex.taskModel.activeTaskIds,
      currentFocus: focusTask?.currentFocus ?? state.orchestration.currentFocus,
      nextAction: focusTask?.nextAction ?? state.orchestration.nextAction
    }
  });
  return nextIndex;
}
function materializeOneDerivedTask(root, index, packet, catalog) {
  const existing = catalog.byId.get(packet.id) ?? null;
  if (existing) {
    return { index, packet: existing, created: false };
  }
  writePacket2(root, packet);
  catalog.byId.set(packet.id, packet);
  return { index: upsertIndexItem(index, packet), packet, created: true };
}
function materializePlanResultMissions(root, planTask, args = {}, runId) {
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const index = loadTaskIndex(root);
  const init = initPacket(index);
  if (!init) {
    throw new Error("Plan mission conversion requires the level-0 init task.");
  }
  const catalog = readTaskPacketCatalog(root);
  let nextIndex = index;
  const createdMissions = [];
  const reusedMissions = [];
  const topSources = planMissionSources(args, planTask, responseLanguage);
  for (const [missionIndex, source] of topSources.entries()) {
    const topPacket = buildDerivedTask(root, init, init, source, {
      idPrefix: `task-${planTask.id}-mission-${missionIndex + 1}`,
      level: normalizeLevel2(source.level ?? source.taskLevel ?? source.missionLevel, 3),
      classification: { stage: "execute", domain: planTask.domain },
      sourcePlanTaskId: planTask.id,
      sourceMissionPassRunId: runId,
      derivedFrom: "completed-plan-mission"
    });
    const topResult = materializeOneDerivedTask(root, nextIndex, topPacket, catalog);
    nextIndex = topResult.index;
    (topResult.created ? createdMissions : reusedMissions).push(topResult.packet);
    const childSources = objectArray(source.childMissions).map((item) => normalizePlanMissionSource(item));
    for (const [childIndex, childSource] of childSources.entries()) {
      const childLevel = normalizeLevel2(childSource.level ?? childSource.taskLevel ?? childSource.missionLevel, topResult.packet.level + 1);
      const childPacket = buildDerivedTask(root, init, topResult.packet, childSource, {
        idPrefix: `${topResult.packet.id}-mission-${childIndex + 1}`,
        level: childLevel,
        minimumLevel: topResult.packet.level + 1,
        classification: { stage: childSource.stage ?? topResult.packet.stage, domain: childSource.domain ?? topResult.packet.domain },
        sourcePlanTaskId: planTask.id,
        sourceMissionPassRunId: runId,
        derivedFrom: "completed-plan-mission-child"
      });
      const childResult = materializeOneDerivedTask(root, nextIndex, childPacket, catalog);
      nextIndex = childResult.index;
      (childResult.created ? createdMissions : reusedMissions).push(childResult.packet);
    }
  }
  const changed = createdMissions.length > 0;
  if (changed) {
    persistDerivedTaskIndex(root, nextIndex, createdMissions.at(-1));
  }
  return {
    sourcePlanTaskId: planTask.id,
    sourceMissionPassRunId: runId,
    createdMissions: createdMissions.map((mission) => ({ id: mission.id, title: mission.title, level: mission.level, stage: mission.stage, domain: mission.domain, status: mission.status, parentId: mission.parentId })),
    reusedMissions: reusedMissions.map((mission) => ({ id: mission.id, title: mission.title, level: mission.level, stage: mission.stage, domain: mission.domain, status: mission.status, parentId: mission.parentId })),
    missionCount: createdMissions.length + reusedMissions.length,
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex
  };
}
function createDoveTask(root, args = {}) {
  assertGovernanceMutationRegistered("create-dove-task", "guarded");
  assertUnambiguousConfirmation(args);
  assertInitialDemandDoesNotSetGovernance(args, "create_dove_task");
  assertCanonicalMissionGovernanceReplay(args, "create_dove_task");
  const confirmed = hasExplicitConfirmation(args);
  const mutationMode = missionProposalMutationMode(root, args);
  if (confirmed) {
    const suppliedProposalDigest2 = normalizeString10(args.proposalDigest, "");
    const suppliedPacketId = normalizeString10(args.id ?? args.packetId, "");
    if (!/^[0-9a-f]{64}$/u.test(suppliedProposalDigest2) || !suppliedPacketId) {
      throw new Error("Confirmed Dove mission materialization requires the exact proposalDigest and task id returned by the selected local proposal replay data.");
    }
    if (!currentMutationContext(root)) {
      throw new Error("Confirmed Dove mission materialization requires an active MutationContext; direct core replay cannot write outside the selected mutation mode.");
    }
    if (args.proposalVersion !== MISSION_PROPOSAL_VERSION) {
      throw new Error("The selected local Dove mission proposal replay version is not supported. Request a fresh proposal.");
    }
    if (normalizeString10(args.proposalWorkspace, "") !== canonicalMissionWorkspace(root)) {
      throw new Error("The selected local Dove mission proposal replay belongs to a different canonical workspace. Request a fresh proposal.");
    }
  }
  const contract = buildDoveTaskContract(root, args);
  const { packet, checklistProposal, classification, blockers, applicableLessons, responseLanguage, proposedInit, initMaterializationRequired } = contract;
  const handoffRoutes = packet.workContract?.recommendedRoutes ?? [];
  if (!confirmed) {
    const preActionGuidance = preActionGuidanceForTask(root, "dove.mission", packet, {
      request: requestTextFromArgs(args),
      roleId: "planner",
      nextAction: packet.nextAction,
      workflowKind: "mission"
    }, responseLanguage);
    const confirmArgs = missionConfirmArgs(root, contract, mutationMode);
    return {
      status: "needs-confirmation",
      proposalOnly: true,
      noAutoApply: true,
      writes: [],
      confirmationRequired: true,
      demandConversion: true,
      workflowMode: "mission-contract",
      executionMode: "contract-handoff",
      initMaterializationRequired,
      proposedInit,
      proposedTask: packet,
      workContract: packet.workContract,
      executionContract: packet.executionContract,
      executionReadiness: doveExecutionContractReadiness(packet.executionContract),
      preActionGuidance,
      taskCard: buildTaskConfirmationCard(packet, { firstAction: packet.nextAction, workContract: packet.workContract, executionContract: packet.executionContract, preActionGuidance }, responseLanguage),
      classification,
      blockers,
      evidenceExpectations: packet.evidenceExpectations,
      nextAction: packet.nextAction,
      recommendedNextCommand: packet.nextAction,
      recommendedRoutes: handoffRoutes,
      handoffRoutes,
      checklistProposal,
      applicableLessons,
      proposalVersion: MISSION_PROPOSAL_VERSION,
      proposalDigest: confirmArgs.proposalDigest,
      proposalWorkspace: confirmArgs.proposalWorkspace,
      proposalMutationMode: mutationMode,
      proposalTrust: {
        boundary: "trusted-local-exact-replay-data",
        proofOfHumanApproval: false,
        tamperProof: false
      },
      confirmArgs,
      taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex,
      responseLanguage,
      message: doveText(responseLanguage, "createTaskConfirmMessage")
    };
  }
  const suppliedProposalDigest = normalizeString10(args.proposalDigest, "");
  const replayDigest = missionProposalDigest(root, contract, args, missionReplayFields(args));
  if (suppliedProposalDigest !== replayDigest) {
    throw new Error("The selected local Dove mission proposal replay no longer matches the current contract or exact replay fields. Request a fresh proposal before materialization.");
  }
  assertMissionReplayTargetsAvailable(root, contract);
  ensureWorkspace(root);
  const materialized = materializeDoveTask(root, contract);
  const plannedOnly = materialized && typeof materialized === "object" && isPatchPlanMode(root);
  const preActionGuidanceSummary = preActionGuidanceSummaryForTask(root, "dove.mission", materialized.createdTask, {
    request: requestTextFromArgs(args),
    roleId: "planner",
    nextAction: materialized.createdTask.nextAction,
    workflowKind: "mission"
  }, responseLanguage);
  return {
    status: plannedOnly ? "materialization-planned" : "materialized",
    confirmationRequired: false,
    demandConversion: true,
    workflowMode: "mission-contract",
    executionMode: "contract-handoff",
    contractMaterialized: !plannedOnly,
    foreground: false,
    background: false,
    daemon: false,
    preActionGuidanceSummary,
    message: doveText(responseLanguage, plannedOnly ? "createTaskMaterializationPlannedMessage" : "createTaskMaterializedMessage"),
    responseLanguage,
    ...materialized
  };
}
function candidateTasks(index) {
  return (index.items ?? []).filter((item) => item.level !== 0 && activeStatus2(item.status));
}
function workflowTargetOptions() {
  return {
    targetFields: ["target", "packetTarget", "taskName", "title"],
    artifactFields: ["artifactRefs", "artifactPaths", "artifacts", "evidenceLinks", "evidencePaths", "outputPaths", "validationEvidencePaths"]
  };
}
function evidenceExplanationForResolution(artifactResolution, responseLanguage = "zh") {
  if (!artifactResolution || typeof artifactResolution !== "object" || Array.isArray(artifactResolution)) {
    return null;
  }
  const keyByCode = {
    "no-artifacts": "evidenceResolutionNoArtifacts",
    "no-selected-packet": "evidenceResolutionNoSelectedPacket",
    "accepted-self-artifacts": "evidenceResolutionAcceptedSelf",
    "accepted-descendant-artifacts": "evidenceResolutionAcceptedDescendant",
    "no-existing-artifact-owner": "evidenceResolutionNoExistingOwner",
    "artifact-conflict": "evidenceResolutionConflict"
  };
  const code = normalizeString10(artifactResolution.explanationCode, "no-artifacts");
  return {
    code,
    message: doveText(responseLanguage, keyByCode[code] ?? "evidenceResolutionNoArtifacts"),
    selectedPacketId: normalizeString10(artifactResolution.selectedPacketId, null),
    requestedArtifacts: normalizeStringArray24(artifactResolution.requestedArtifacts),
    acceptedPacketIds: Array.isArray(artifactResolution.acceptedMatches) ? artifactResolution.acceptedMatches.map((match) => match.packetId).filter(Boolean) : [],
    conflictingPacketIds: Array.isArray(artifactResolution.conflictingMatches) ? artifactResolution.conflictingMatches.map((match) => match.packetId).filter(Boolean) : []
  };
}
function chooseTask(root, index, args = {}) {
  const candidates = candidateTasks(index);
  if (Number.isFinite(args.index)) {
    return { selected: candidates[Math.max(0, Math.floor(args.index) - 1)] ?? null, candidates };
  }
  if (hasExplicitTaskSelector(args)) {
    try {
      const resolved = resolveDurableTaskPacket(root, args, workflowTargetOptions());
      const indexed = (index.items ?? []).find((item) => item.id === resolved.packetId) ?? resolved.packet;
      return { selected: indexed, candidates, resolution: resolved.resolution, artifactResolution: resolved.artifactResolution };
    } catch (error) {
      return {
        selected: null,
        candidates: Array.isArray(error.candidates) && error.candidates.length > 0 ? error.candidates : candidates,
        resolutionError: error.message,
        resolutionErrorCode: error.resolutionErrorCode ?? error.code,
        resolutionErrorReason: error.reason,
        artifactResolution: error.artifactResolution ?? null
      };
    }
  }
  return { selected: candidates.length === 1 ? candidates[0] : null, candidates };
}
function normalizeMissionPassStatus(args = {}) {
  const envelope = plainObject3(args.missionPass ?? args.passResult ?? args.result);
  const explicit = normalizeAllowed3(args.resultStatus ?? args.taskStatus ?? args.missionStatus ?? envelope.resultStatus ?? envelope.taskStatus ?? envelope.missionStatus, ["completed", "blocked", "in-progress"], null);
  if (explicit) {
    return explicit;
  }
  if (args.completeTask === true || args.complete === true || args.completeOnSuccess === true || envelope.completeTask === true || envelope.complete === true || envelope.completeOnSuccess === true) {
    return "completed";
  }
  if (args.blocked === true || envelope.blocked === true) {
    return "blocked";
  }
  return "in-progress";
}
var SYSTEM_OWNED_MISSION_PASS_FIELDS = /* @__PURE__ */ new Set([
  "ownerRole",
  "nextRole",
  "handoff",
  "handoffId"
]);
var MISSION_PASS_DESCRIPTION_SUBTREES = /* @__PURE__ */ new Set([
  "workContract"
]);
function collectSystemOwnedMissionPassFields(args = {}) {
  const found = [];
  for (const field of SYSTEM_OWNED_MISSION_PASS_FIELDS) {
    if (Object.hasOwn(args, field)) {
      found.push(field);
    }
  }
  const visitEnvelope = (value, inputPath) => {
    if (!value || typeof value !== "object") {
      return;
    }
    if (Array.isArray(value)) {
      value.forEach((item, index) => visitEnvelope(item, `${inputPath}[${index}]`));
      return;
    }
    for (const [field, nested] of Object.entries(value)) {
      const fieldPath = `${inputPath}.${field}`;
      if (SYSTEM_OWNED_MISSION_PASS_FIELDS.has(field)) {
        found.push(fieldPath);
      }
      if (!MISSION_PASS_DESCRIPTION_SUBTREES.has(field)) {
        visitEnvelope(nested, fieldPath);
      }
    }
  };
  for (const envelopeName of ["missionPass", "passResult", "result"]) {
    visitEnvelope(args[envelopeName], envelopeName);
  }
  for (const planField of ["planConversion", "plannedMissions", "resultingMissions", "missions", "childMissions"]) {
    visitEnvelope(args[planField], planField);
  }
  return found;
}
function assertNoSystemOwnedMissionPassFields(args = {}) {
  const forbidden = collectSystemOwnedMissionPassFields(args);
  if (forbidden.length > 0) {
    throw new Error(`record_dove_mission_pass does not accept system-owned workflow routing fields: ${forbidden.join(", ")}.`);
  }
}
var MISSION_PASS_FIELDS = [
  "runId",
  "resultStatus",
  "taskStatus",
  "missionStatus",
  "completeTask",
  "complete",
  "completeOnSuccess",
  "blocked",
  "resultSummary",
  "outcome",
  "reason",
  "summary",
  "stopReason",
  "blockedReason",
  "boundary",
  "boundaryType",
  "boundaryId",
  "requiredInputs",
  "requiredActions",
  "evidenceLinks",
  "evidencePaths",
  "validationEvidencePaths",
  "verificationEvidencePaths",
  "verifiedCriteria",
  "executionContract",
  "executionReceipt",
  "artifactRefs",
  "artifactPaths",
  "command",
  "workflow",
  "preset",
  "nextCommand",
  "nextAction",
  "startedAt",
  "completedAt",
  "convertPlanToMissions",
  "planConversion",
  "plannedMissions",
  "resultingMissions",
  "missions",
  "childMissions"
];
function missionPassPayload(args = {}) {
  const envelope = plainObject3(args.missionPass ?? args.passResult ?? args.result);
  const payload = { ...envelope };
  for (const field of MISSION_PASS_FIELDS) {
    if (args[field] !== void 0) {
      payload[field] = args[field];
    }
  }
  return payload;
}
function nonEmptyArrayField(value) {
  return Array.isArray(value) && value.length > 0;
}
function hasPlanMissionOutput(args = {}) {
  const payload = missionPassPayload(args);
  return [payload.plannedMissions, payload.resultingMissions, payload.missions, payload.childMissions].some(nonEmptyArrayField) || Object.keys(plainObject3(payload.planConversion)).length > 0;
}
function completionEvidenceForPayload(payload = {}) {
  const receipt = normalizeDoveExecutionReceipt(payload.executionReceipt, null);
  const verifiedCriteria = normalizeDoveVerifiedCriteria([
    ...normalizeDoveVerifiedCriteria(payload.verifiedCriteria),
    ...normalizeDoveVerifiedCriteria(receipt?.verifiedCriteria)
  ]);
  const verificationEvidencePaths = normalizeStringArray24([
    ...normalizeStringArray24(payload.verificationEvidencePaths),
    ...normalizeStringArray24(receipt?.verificationEvidencePaths)
  ]);
  const validationEvidencePaths = normalizeStringArray24([
    ...normalizeStringArray24(payload.validationEvidencePaths),
    ...normalizeStringArray24(receipt?.validationEvidencePaths)
  ]);
  const evidenceLinks = normalizeStringArray24([
    ...normalizeStringArray24(payload.evidenceLinks),
    ...normalizeStringArray24(payload.evidencePaths),
    ...normalizeStringArray24(receipt?.evidenceLinks),
    ...normalizeStringArray24(receipt?.evidencePaths)
  ]);
  const artifactRefs = normalizeStringArray24([
    ...normalizeStringArray24(payload.artifactRefs),
    ...normalizeStringArray24(payload.artifactPaths),
    ...normalizeStringArray24(receipt?.artifactRefs),
    ...normalizeStringArray24(receipt?.artifactPaths)
  ]);
  const criteriaEvidencePaths = normalizeStringArray24(verifiedCriteria.flatMap((item) => item.evidencePaths ?? []));
  const evidencePaths = normalizeStringArray24([
    ...evidenceLinks,
    ...artifactRefs,
    ...validationEvidencePaths,
    ...verificationEvidencePaths,
    ...criteriaEvidencePaths
  ]);
  return {
    evidenceLinks,
    artifactRefs,
    validationEvidencePaths,
    verificationEvidencePaths,
    verifiedCriteria,
    criteriaEvidencePaths,
    evidencePaths,
    executionReceipt: receipt
  };
}
function classifyRequiredEvidenceReference(value) {
  const reference = normalizeString10(value, "");
  if (!reference) {
    return { reference, kind: "invalid", reason: "empty evidence requirement" };
  }
  if (isExternalArtifactReference(reference)) {
    return { reference, kind: "reference", normalizedReference: reference };
  }
  const normalized = normalizeProjectRelativePath(reference);
  if (!normalized.ok) {
    return { reference, kind: "invalid", reason: normalized.reason };
  }
  const looksNarrative = /\s|[，。；！？：]/u.test(normalized.normalizedPath);
  const pathLike = normalized.normalizedPath.startsWith(".") || !looksNarrative && normalized.normalizedPath.includes("/") || !looksNarrative && path26.posix.extname(normalized.normalizedPath).length > 0;
  return pathLike ? { reference, kind: "reference", normalizedReference: normalized.normalizedPath } : { reference, kind: "description" };
}
function contractEvidenceRequirements(contract, evidence = {}) {
  const classified = normalizeStringArray24(contract?.convergence?.evidenceRequired).map(classifyRequiredEvidenceReference);
  const submittedPaths = new Set(normalizeStringArray24(evidence.evidencePaths).map((item) => {
    if (isExternalArtifactReference(item)) {
      return item;
    }
    const normalized = normalizeProjectRelativePath(item);
    return normalized.ok ? normalized.normalizedPath : item;
  }));
  const requiredReferences = classified.filter((item) => item.kind === "reference").map((item) => item.normalizedReference);
  return {
    classified,
    invalidRequirements: classified.filter((item) => item.kind === "invalid"),
    descriptiveRequirements: classified.filter((item) => item.kind === "description").map((item) => item.reference),
    requiredReferences,
    missingRequiredEvidencePaths: requiredReferences.filter((item) => !submittedPaths.has(item))
  };
}
function completionVerificationBlock(root, task, args = {}, responseLanguage = "zh") {
  const payload = missionPassPayload(args);
  const contract = normalizeDoveExecutionContract(task.executionContract, null);
  const readiness = doveExecutionContractReadiness(contract);
  if (!readiness.ready) {
    return {
      status: "missing-executable-contract",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "missing-executable-contract",
      executionReadiness: readiness,
      requiredActions: ["provide-execution-contract", ...readiness.missing.map((item) => `provide-${item}`)],
      message: responseLanguage === "en" ? "A Dove task cannot be completed until it has an executable contract with action, implementation, convergence criteria, and failure routes." : "Dove \u4EFB\u52A1\u5FC5\u987B\u5148\u6709\u5305\u542B action\u3001implementation\u3001convergence.criteria \u548C failureRoutes \u7684\u53EF\u6267\u884C\u5408\u540C\uFF0C\u624D\u80FD\u5B8C\u6210\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const planBlock = planOutputExecutionBlock(task, payload, responseLanguage);
  if (planBlock) {
    return planBlock;
  }
  const evidence = completionEvidenceForPayload(payload);
  const sourceReferences = evidence.evidencePaths.filter((item) => item.startsWith("source:")).map((item) => item.slice("source:".length));
  const sourceEvidence = evaluateSourceReferences(root, sourceReferences);
  const ineligibleSourceEvidence = sourceEvidence.filter((item) => !item.eligible);
  const noteReferences = evidence.evidencePaths.filter((item) => item.startsWith("note:")).map((item) => item.slice("note:".length));
  const noteEvidence = evaluateNoteReferences(root, noteReferences, task.id);
  const ineligibleNoteEvidence = noteEvidence.filter((item) => !item.eligible);
  if (ineligibleSourceEvidence.length > 0 || ineligibleNoteEvidence.length > 0) {
    return {
      status: "verification-failed",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "verification-failed",
      ineligibleSourceEvidence: ineligibleSourceEvidence.map((item) => ({ sourceId: item.reference, reason: item.reason })),
      ineligibleNoteEvidence: ineligibleNoteEvidence.map((item) => ({ noteId: item.reference, reason: item.reason })),
      requiredActions: ["verify-source-material", "bind-note-and-source-to-completion-packet", "retry-completion-with-verified-evidence"],
      message: responseLanguage === "en" ? "Candidate-backed, rejected, missing, identity-mutated, untrusted, material-changed, or packet-mismatched source/note evidence cannot support task completion." : "candidate-backed\u3001rejected\u3001\u7F3A\u5931\u3001\u8EAB\u4EFD\u53D8\u5316\u3001\u6765\u6E90\u4E0D\u53EF\u4FE1\u3001\u6750\u6599\u53D8\u5316\u6216 packet \u4E0D\u5339\u914D\u7684 source/note \u8BC1\u636E\u4E0D\u80FD\u652F\u6491\u4EFB\u52A1\u5B8C\u6210\u3002",
      nextAction: "project:dove.source",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const evidenceRequirements = contractEvidenceRequirements(contract, evidence);
  if (evidenceRequirements.invalidRequirements.length > 0) {
    return {
      status: "verification-failed",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "verification-failed",
      invalidRequiredEvidence: evidenceRequirements.invalidRequirements,
      requiredActions: ["repair-invalid-contract-evidence-requirements", "attach-verification-evidence"],
      message: responseLanguage === "en" ? "The durable execution contract contains an unsafe or invalid convergence.evidenceRequired entry and cannot be completed until the contract is repaired." : "\u6301\u4E45\u5316 executionContract \u7684 convergence.evidenceRequired \u542B\u6709\u4E0D\u5B89\u5168\u6216\u65E0\u6548\u6761\u76EE\uFF0C\u5FC5\u987B\u5148\u4FEE\u6B63\u5408\u540C\u624D\u80FD\u5B8C\u6210\u4EFB\u52A1\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const hasPlanOutput = task.stage === "plan" && hasPlanMissionOutput(payload);
  const integrity = completionEvidenceIntegrity(root, evidence, {
    context: {
      task,
      executionContract: contract,
      verifiedCriteria: evidence.verifiedCriteria,
      eligibleSourceReferences: [
        ...sourceEvidence.filter((item) => item.eligible).map((item) => `source:${item.reference}`),
        ...noteEvidence.filter((item) => item.eligible).map((item) => `note:${item.reference}`)
      ]
    }
  });
  const hasInspectibleEvidence = hasPlanOutput || integrity.hasSubstantiveEvidence === true;
  const hasSummary = Boolean(normalizeString10(payload.resultSummary ?? payload.summary ?? payload.reason, ""));
  if (!hasSummary || !hasInspectibleEvidence) {
    const requiredActions = [
      hasSummary ? null : "provide-result-summary",
      hasInspectibleEvidence ? null : "provide-evidence-links-or-artifact-refs-or-verification-evidence",
      evidence.evidencePaths.length > 0 && integrity.problemPaths.length > 0 ? "attach-existing-non-empty-non-bookkeeping-evidence" : null
    ].filter(Boolean);
    return {
      status: "needs-completion-evidence",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "missing-required-materials",
      requiredActions,
      evidenceRequired: readiness.evidenceRequired,
      evidenceIntegrity: integrity,
      message: responseLanguage === "en" ? "Completing a Dove task requires a result summary plus existing, non-empty, non-bookkeeping evidence, artifact, validation, or verification paths." : "\u5B8C\u6210 Dove \u4EFB\u52A1\u5FC5\u987B\u63D0\u4F9B\u7ED3\u679C\u6458\u8981\uFF0C\u5E76\u9644\u5E26\u5DF2\u5B58\u5728\u3001\u975E\u7A7A\u3001\u975E\u5BFC\u822A\u8D26\u672C\u7C7B\u7684 evidence\u3001artifact\u3001validation \u6216 verification \u8DEF\u5F84\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const coverage = doveExecutionCriteriaCoverage(contract, evidence.verifiedCriteria);
  if (!coverage.complete) {
    return {
      status: "verification-failed",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "verification-failed",
      criteriaCoverage: coverage,
      evidenceIntegrity: integrity,
      requiredActions: ["provide-verified-criteria", "cover-missing-convergence-criteria", "attach-verification-evidence"],
      message: responseLanguage === "en" ? "Completing a Dove task requires verifiedCriteria covering every executionContract.convergence.criteria item." : "\u5B8C\u6210 Dove \u4EFB\u52A1\u5FC5\u987B\u7528 verifiedCriteria \u8986\u76D6 executionContract.convergence.criteria \u4E2D\u7684\u6BCF\u4E00\u9879\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  if (evidenceRequirements.descriptiveRequirements.length > 0 && integrity.uncoveredRequirements.length > 0) {
    return {
      status: "verification-failed",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "verification-failed",
      criteriaCoverage: coverage,
      evidenceIntegrity: integrity,
      descriptiveEvidenceRequirements: evidenceRequirements.descriptiveRequirements,
      uncoveredRequirements: integrity.uncoveredRequirements,
      requiredActions: ["satisfy-described-contract-evidence", "attach-purpose-matched-evidence"],
      message: responseLanguage === "en" ? "Every descriptive execution-contract evidence requirement must be covered by distinct, purpose-matched substantive evidence." : "executionContract \u4E2D\u6BCF\u4E00\u9879\u8BF4\u660E\u6027\u8BC1\u636E\u8981\u6C42\u90FD\u5FC5\u987B\u7531\u72EC\u7ACB\u4E14\u7528\u9014\u5339\u914D\u7684\u5B9E\u8D28\u8BC1\u636E\u8986\u76D6\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const missingRequiredEvidencePaths = evidenceRequirements.missingRequiredEvidencePaths;
  if (missingRequiredEvidencePaths.length > 0) {
    return {
      status: "verification-failed",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "verification-failed",
      criteriaCoverage: coverage,
      evidenceIntegrity: integrity,
      missingRequiredEvidencePaths,
      requiredActions: ["attach-contract-required-evidence", "attach-verification-evidence"],
      message: responseLanguage === "en" ? "Completing a Dove task requires every path declared in executionContract.convergence.evidenceRequired to be included in the submitted completion evidence." : "\u5B8C\u6210 Dove \u4EFB\u52A1\u65F6\uFF0C\u5FC5\u987B\u5728\u63D0\u4EA4\u7684\u5B8C\u6210\u8BC1\u636E\u4E2D\u5305\u542B executionContract.convergence.evidenceRequired \u58F0\u660E\u7684\u6BCF\u4E2A\u8BC1\u636E\u8DEF\u5F84\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  if (integrity && integrity.satisfied !== true) {
    return {
      status: "needs-completion-evidence",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "missing-required-materials",
      criteriaCoverage: coverage,
      evidenceIntegrity: integrity,
      requiredActions: ["attach-existing-non-empty-non-bookkeeping-evidence"],
      message: responseLanguage === "en" ? "Completing a Dove task cannot cite missing, unsafe, empty, directory, unreadable, or unsupported local evidence paths." : "\u5B8C\u6210 Dove \u4EFB\u52A1\u4E0D\u80FD\u5F15\u7528\u7F3A\u5931\u3001\u4E0D\u5B89\u5168\u3001\u7A7A\u6587\u4EF6\u3001\u76EE\u5F55\u3001\u4E0D\u53EF\u8BFB\u6216\u4E0D\u652F\u6301\u7684\u672C\u5730\u8BC1\u636E\u8DEF\u5F84\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  if (integrity && integrity.missingCriteriaEvidence.length > 0) {
    return {
      status: "verification-failed",
      requestedStatus: "completed",
      packetId: task.id,
      title: task.title,
      boundaryType: "verification-failed",
      criteriaCoverage: coverage,
      evidenceIntegrity: integrity,
      requiredActions: ["attach-verification-evidence", "attach-existing-non-empty-non-bookkeeping-evidence"],
      message: responseLanguage === "en" ? "Completing a Dove task requires each verified criterion to cite existing, non-empty, non-bookkeeping evidence." : "\u5B8C\u6210 Dove \u4EFB\u52A1\u65F6\uFF0C\u6BCF\u4E2A verifiedCriteria \u90FD\u5FC5\u987B\u5F15\u7528\u5DF2\u5B58\u5728\u3001\u975E\u7A7A\u3001\u975E\u5BFC\u822A\u8D26\u672C\u7C7B\u7684\u8BC1\u636E\u3002",
      nextAction: "project:dove.status",
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  return null;
}
function buildExecutionReceipt({ payload = {}, taskBefore = {}, taskAfter = {}, runId, surface, command, actionType, status, outcome, summary, nextAction, evidence = {}, startedAt, completedAt, boundary = null }) {
  const contract = taskAfter.executionContract ?? taskBefore.executionContract ?? payload.executionContract;
  const criteriaCoverage = doveExecutionCriteriaCoverage(contract, evidence.verifiedCriteria);
  return normalizeDoveExecutionReceipt(payload.executionReceipt, {
    receiptId: `${runId}-receipt`,
    runId,
    packetId: taskAfter.id ?? taskBefore.id ?? null,
    command,
    surface,
    actionType,
    startedAt,
    completedAt,
    status,
    outcome,
    resultSummary: summary,
    publicSafeSummary: summary,
    nextAction,
    lifecycleTransition: {
      previousStatus: taskBefore.status ?? null,
      nextStatus: taskAfter.status ?? status ?? null
    },
    artifactRefs: evidence.artifactRefs,
    evidenceLinks: evidence.evidenceLinks,
    validationEvidencePaths: evidence.validationEvidencePaths,
    verificationEvidencePaths: evidence.verificationEvidencePaths,
    verifiedCriteria: evidence.verifiedCriteria,
    criteriaCoverage,
    boundary
  });
}
function recordDoveMissionPass(root, args = {}) {
  assertGovernanceMutationRegistered("record-dove-mission-pass", "guarded");
  assertNoSystemOwnedMissionPassFields(args);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const index = loadTaskIndex(root);
  const selection = chooseTask(root, index, args);
  const { selected, candidates } = selection;
  const selectionEvidenceExplanation = evidenceExplanationForResolution(selection.artifactResolution, responseLanguage);
  if (!selected) {
    return {
      status: "needs-task-selection",
      choices: taskSelectionChoices(candidates),
      responseLanguage,
      message: doveText(responseLanguage, "missionPassSelectMessage"),
      resolutionError: selection.resolutionError,
      resolutionErrorCode: selection.resolutionErrorCode,
      resolutionErrorReason: selection.resolutionErrorReason,
      artifactResolution: selection.artifactResolution ?? null,
      evidenceExplanation: selectionEvidenceExplanation
    };
  }
  if (selected.level === 0) {
    throw new Error("The level-0 init task cannot receive a mission pass result. Convert a user demand into a non-init mission first.");
  }
  const taskBefore = loadFullTask(root, selected);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Recording a Dove mission pass"
  );
  const payload = missionPassPayload(args);
  const resultStatus = normalizeMissionPassStatus(payload);
  const completionBlock = checklistCompletionBlock(root, taskBefore, resultStatus, responseLanguage);
  if (completionBlock) {
    return {
      ...completionBlock,
      responseLanguage,
      proposalOnly: true,
      noAutoApply: true,
      writes: []
    };
  }
  const verificationBlock = resultStatus === "completed" ? completionVerificationBlock(root, taskBefore, payload, responseLanguage) : null;
  if (verificationBlock) {
    return {
      ...verificationBlock,
      responseLanguage,
      artifactResolution: selection.artifactResolution ?? null,
      evidenceExplanation: selectionEvidenceExplanation
    };
  }
  ensureWorkspace(root);
  const timestamp = nowIso();
  const runId = normalizeTaskPacketId(payload.runId ?? `mission-${taskBefore.id}-${Date.now().toString(36)}`);
  const nextAction = normalizeString10(payload.nextAction, resultStatus === "completed" ? "project:dove.status" : taskBefore.nextAction ?? "project:dove.status");
  const payloadEvidence = completionEvidenceForPayload(payload);
  const artifactRefs = normalizeStringArray24([...Array.isArray(taskBefore.artifactRefs) ? taskBefore.artifactRefs : [], ...payloadEvidence.artifactRefs]);
  const lifecycleFields = lifecycleFieldsForStatus(resultStatus, payload, timestamp, responseLanguage);
  const task = updateTaskLifecycle(root, taskBefore, resultStatus, {
    ...lifecycleFields,
    runId,
    surface: "dove.mission",
    command: normalizeAutoCommandId(payload.command ?? payload.workflow ?? payload.preset ?? payload.nextCommand),
    summary: normalizeString10(payload.resultSummary ?? payload.summary, ""),
    reason: normalizeString10(payload.reason ?? payload.stopReason, ""),
    nextAction,
    artifactRefs,
    evidenceLinks: payloadEvidence.evidenceLinks
  });
  const evidenceLinks = payloadEvidence.evidenceLinks;
  const summary = normalizeString10(payload.resultSummary ?? payload.summary, resultStatus === "completed" ? doveText(responseLanguage, "missionPassCompletedSummary") : resultStatus === "blocked" ? doveText(responseLanguage, "missionPassBlockedSummary") : doveText(responseLanguage, "missionPassProgressSummary"));
  const command = normalizeAutoCommandId(payload.command ?? payload.workflow ?? payload.preset ?? payload.nextCommand);
  const outcome = normalizeString10(payload.outcome, resultStatus === "completed" ? "task-completed" : resultStatus === "blocked" ? "mission-pass-blocked" : "single-pass-progress-recorded");
  const stopReason = normalizeString10(payload.stopReason, resultStatus === "blocked" ? "mission-pass-blocked" : null);
  const planConversion = taskBefore.stage === "plan" && resultStatus === "completed" && payload.convertPlanToMissions !== false ? materializePlanResultMissions(root, taskBefore, payload, runId) : null;
  const startedAt = normalizeString10(args.startedAt, timestamp);
  const completedAt = normalizeString10(args.completedAt, timestamp);
  const executionReceipt = buildExecutionReceipt({
    payload,
    taskBefore,
    taskAfter: task,
    runId,
    surface: "dove.mission",
    command,
    actionType: taskBefore.stage === "plan" ? "plan" : "build",
    status: resultStatus,
    outcome,
    summary,
    nextAction,
    evidence: payloadEvidence,
    startedAt,
    completedAt,
    boundary: task.boundary ?? null
  });
  const result = {
    id: runId,
    surface: "dove.mission",
    packetId: task.id,
    status: resultStatus,
    outcome,
    foreground: true,
    background: false,
    daemon: false,
    maxIterations: 1,
    iterationCount: 1,
    allowedInternalCommands: AUTO_INTERNAL_COMMANDS,
    stopReason,
    iterations: [
      {
        iteration: 1,
        command,
        status: resultStatus,
        outcome,
        stopReason,
        output: {
          summary,
          evidenceLinks,
          artifactRefs,
          validationEvidencePaths: payloadEvidence.validationEvidencePaths,
          verificationEvidencePaths: payloadEvidence.verificationEvidencePaths,
          verifiedCriteria: payloadEvidence.verifiedCriteria,
          executionReceipt,
          nextAction,
          planConversion
        },
        startedAt,
        completedAt
      }
    ],
    planConversion,
    createdPlanMissions: planConversion?.createdMissions ?? [],
    reusedPlanMissions: planConversion?.reusedMissions ?? [],
    executionReceipt,
    taskStatusBefore: taskBefore.status,
    taskStatusAfter: task.status,
    responseLanguage,
    createdAt: timestamp,
    updatedAt: timestamp
  };
  persistAutoResult(root, result);
  const preActionGuidanceSummary = preActionGuidanceSummaryForTask(root, "dove.mission", task, {
    request: summary,
    roleId: task.ownerRole ?? "builder",
    nextAction,
    workflowKind: "mission-pass"
  }, responseLanguage);
  const resultCard = missionResultCard(task, result, {
    summary,
    evidenceLinks,
    artifactRefs,
    validationEvidence: payloadEvidence.validationEvidencePaths,
    verificationEvidence: payloadEvidence.verificationEvidencePaths,
    verifiedCriteria: payloadEvidence.verifiedCriteria,
    executionReceipt,
    planConversion,
    artifactResolution: selection.artifactResolution,
    evidenceExplanation: selectionEvidenceExplanation,
    preActionGuidanceSummary,
    nextAction,
    nextActions: resultStatus === "completed" ? [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: nextAction, packetId: task.id }] : [{ title: doveText(responseLanguage, "resultCardNextProvideEvidence"), command: "project:dove.status", packetId: task.id, confirmationRequired: true }]
  }, responseLanguage);
  return {
    status: result.status,
    task,
    result,
    resultCard,
    executionReceipt,
    preActionGuidanceSummary,
    evidenceLinks,
    artifactRefs,
    validationEvidencePaths: payloadEvidence.validationEvidencePaths,
    verificationEvidencePaths: payloadEvidence.verificationEvidencePaths,
    verifiedCriteria: payloadEvidence.verifiedCriteria,
    artifactResolution: selection.artifactResolution ?? null,
    evidenceExplanation: selectionEvidenceExplanation,
    planConversion,
    createdPlanMissions: result.createdPlanMissions,
    reusedPlanMissions: result.reusedPlanMissions,
    responseLanguage,
    nextAction
  };
}
function killDoveTask(root, args = {}) {
  assertGovernanceMutationRegistered("kill-dove-task", "guarded");
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Killing a Dove task"
  );
  ensureWorkspace(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const index = loadTaskIndex(root);
  const { selected, candidates } = chooseTask(root, index, args);
  if (!selected) {
    return {
      status: "needs-task-selection",
      choices: taskSelectionChoices(candidates),
      responseLanguage,
      message: doveText(responseLanguage, "killSelectMessage")
    };
  }
  if (selected.level === 0) {
    throw new Error("The level-0 init task cannot be killed. Use /dove:version to change direction while preserving init.");
  }
  const fullPacket = loadFullTask(root, selected);
  const packet = updateTaskLifecycle(root, fullPacket, "killed", lifecycleFieldsForStatus("killed", {
    ...args,
    reason: normalizeString10(args.reason ?? args.killReason, doveText(responseLanguage, "killReason")),
    surface: "dove.status",
    command: "kill_dove_task"
  }, nowIso(), responseLanguage));
  return {
    status: "killed",
    killedTask: packet,
    activeTaskIds: loadTaskIndex(root).taskModel.activeTaskIds,
    responseLanguage
  };
}
function lifecycleFieldsForStatus(status, args = {}, timestamp = nowIso(), responseLanguage = "zh") {
  const fields = {};
  const nextAction = normalizeString10(args.nextAction, null);
  if (nextAction) {
    fields.nextAction = nextAction;
  }
  const artifactRefs = normalizeStringArray24([
    ...normalizeStringArray24(args.artifactRefs),
    ...normalizeStringArray24(args.artifactPaths)
  ]);
  if (artifactRefs.length > 0) {
    fields.artifactRefs = artifactRefs;
  }
  const evidence = completionEvidenceForPayload(args);
  if (evidence.evidenceLinks.length > 0) {
    fields.evidenceLinks = evidence.evidenceLinks;
  }
  if (evidence.validationEvidencePaths.length > 0) {
    fields.validationEvidencePaths = evidence.validationEvidencePaths;
  }
  if (evidence.verificationEvidencePaths.length > 0) {
    fields.verificationEvidencePaths = evidence.verificationEvidencePaths;
  }
  if (evidence.verifiedCriteria.length > 0) {
    fields.verifiedCriteria = evidence.verifiedCriteria;
  }
  const executionContract = normalizeDoveExecutionContract(args.executionContract, null);
  if (executionContract) {
    fields.executionContract = executionContract;
  }
  for (const key of ["runId", "surface", "sourceSurface", "command", "boundary", "boundaryType", "boundaryId", "requiredInputs", "requiredActions", "ownerRole", "nextRole", "handoff", "handoffId", "reason", "summary", "stopReason"]) {
    if (args[key] !== void 0) {
      fields[key] = args[key];
    }
  }
  if (status === "killed") {
    fields.killedAt = timestamp;
    fields.killReason = normalizeString10(args.reason ?? args.killReason, doveText(responseLanguage, "lifecycleKillReason"));
    fields.nextAction = fields.nextAction ?? "project:dove.status";
  }
  if (status === "completed") {
    fields.completedAt = timestamp;
    fields.nextAction = fields.nextAction ?? "project:dove.status";
  }
  if (status === "blocked") {
    fields.blockedReason = normalizeString10(args.reason ?? args.blockReason ?? args.blockedReason, "");
    fields.nextAction = fields.nextAction ?? "project:dove.status";
  }
  if (status === "archived") {
    fields.archivedAt = timestamp;
    fields.archiveReason = normalizeString10(args.reason ?? args.archiveReason, "archived through status adjustment");
    fields.nextAction = fields.nextAction ?? "project:dove.status";
  }
  return fields;
}
function normalizeStatusAdjustment(item, index) {
  const source = plainObject3(item);
  return {
    index: index + 1,
    packetId: normalizeString10(source.packetId ?? source.taskPacketId ?? source.taskId ?? source.id, null),
    status: normalizeAllowed3(source.status ?? source.taskStatus ?? source.missionStatus, DOVE_TASK_STATUSES, null),
    reason: normalizeString10(source.reason ?? source.summary ?? source.resultSummary, ""),
    summary: normalizeString10(source.summary ?? source.resultSummary ?? source.reason, ""),
    nextAction: normalizeString10(source.nextAction, null),
    evidenceLinks: normalizeStringArray24(source.evidenceLinks ?? source.evidencePaths),
    validationEvidencePaths: normalizeStringArray24(source.validationEvidencePaths),
    verificationEvidencePaths: normalizeStringArray24(source.verificationEvidencePaths),
    verifiedCriteria: normalizeDoveVerifiedCriteria(source.verifiedCriteria),
    executionReceipt: normalizeDoveExecutionReceipt(source.executionReceipt, null),
    executionContract: normalizeDoveExecutionContract(source.executionContract, null),
    artifactRefs: normalizeStringArray24(source.artifactRefs ?? source.artifactPaths)
  };
}
function buildStatusAdjustmentPreviewCard(adjustment, responseLanguage = "zh") {
  return {
    presentation: "compact-status-adjustment-card",
    packetId: adjustment.packetId,
    requestedStatus: adjustment.status === "completed" ? "done" : adjustment.status,
    machineStatus: adjustment.status,
    why: adjustment.reason || doveText(responseLanguage, "statusAdjustConfirmMessage"),
    firstAction: "apply_dove_status_adjustments",
    evidenceRequired: adjustment.artifactRefs.length > 0 ? adjustment.artifactRefs : [doveText(responseLanguage, "compactCardNoEvidence")],
    boundaryOrResume: adjustment.nextAction ?? null,
    confirmation: doveText(responseLanguage, "compactCardNoAutomaticExecution"),
    confirmationRequired: true,
    proposalOnly: true,
    noAutoApply: true
  };
}
function applyDoveStatusAdjustments(root, args = {}) {
  assertGovernanceMutationRegistered("apply-dove-status-adjustments", "guarded");
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const adjustments = objectArray(args.adjustments ?? args.statusAdjustments ?? args.items).map(normalizeStatusAdjustment);
  if (!hasExplicitConfirmation(args)) {
    return {
      status: "needs-confirmation",
      proposalOnly: true,
      noAutoApply: true,
      writes: [],
      confirmationRequired: true,
      statusChoices: DOVE_TASK_STATUSES,
      adjustments,
      adjustmentCards: adjustments.map((adjustment) => buildStatusAdjustmentPreviewCard(adjustment, responseLanguage)),
      confirmArgs: {
        ...args,
        confirmed: true,
        adjustments
      },
      responseLanguage,
      message: doveText(responseLanguage, "statusAdjustConfirmMessage")
    };
  }
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Applying Dove status adjustments"
  );
  ensureWorkspace(root);
  if (adjustments.length === 0) {
    const result2 = {
      status: "no-op",
      applied: [],
      skipped: [],
      rejected: [],
      statusChoices: DOVE_TASK_STATUSES,
      activeTaskIds: loadTaskIndex(root).taskModel.activeTaskIds,
      responseLanguage,
      taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex
    };
    return {
      ...result2,
      resultCard: statusAdjustmentResultCard(result2, responseLanguage)
    };
  }
  const catalog = readTaskPacketCatalog(root);
  const adjustmentLevel = (adjustment) => {
    if (!adjustment.packetId) {
      return -1;
    }
    return catalog.byId.get(normalizeTaskPacketId(adjustment.packetId))?.level ?? -1;
  };
  const orderedAdjustments = [...adjustments].sort((left, right) => adjustmentLevel(right) - adjustmentLevel(left) || left.index - right.index);
  const resultId = normalizeTaskPacketId(args.runId ?? `status-adjust-${Date.now().toString(36)}`);
  const executionIterations = [];
  const executionReceipts = [];
  const applied = [];
  const skipped = [];
  const rejected = [];
  for (const adjustment of orderedAdjustments) {
    if (!adjustment.packetId || !adjustment.status) {
      rejected.push({ ...adjustment, reason: doveText(responseLanguage, "statusAdjustMissingReason") });
      continue;
    }
    const packetId = normalizeTaskPacketId(adjustment.packetId);
    const packet = catalog.byId.get(packetId);
    if (!packet) {
      rejected.push({ ...adjustment, packetId, reason: doveText(responseLanguage, "statusAdjustNoPacketReason") });
      continue;
    }
    if (packet.level === 0) {
      rejected.push({ ...adjustment, packetId, reason: doveText(responseLanguage, "statusAdjustInitReason") });
      continue;
    }
    const fullPacket = loadFullTask(root, packet);
    const completionBlock = checklistCompletionBlock(root, fullPacket, adjustment.status, responseLanguage);
    if (completionBlock) {
      rejected.push({ ...adjustment, packetId, reason: completionBlock.message, completionBlock });
      continue;
    }
    const verificationBlock = adjustment.status === "completed" ? completionVerificationBlock(root, fullPacket, { ...adjustment, resultSummary: adjustment.summary || adjustment.reason }, responseLanguage) : null;
    if (verificationBlock) {
      rejected.push({ ...adjustment, packetId, reason: verificationBlock.message, completionBlock: verificationBlock });
      continue;
    }
    const transitionAt = nowIso();
    const fields = lifecycleFieldsForStatus(adjustment.status, { ...adjustment, runId: `${resultId}-${packetId}`, surface: "dove.status", command: "apply_dove_status_adjustments" }, transitionAt, responseLanguage);
    if (fullPacket.status === adjustment.status) {
      skipped.push({ packetId, status: adjustment.status, displayStatus: adjustment.status === "completed" || adjustment.status === "killed" ? "done" : adjustment.status, reason: doveText(responseLanguage, "statusAdjustSameReason") });
      continue;
    }
    const updated = updateTaskLifecycle(root, fullPacket, adjustment.status, fields);
    const payloadEvidence = completionEvidenceForPayload(adjustment);
    const executionReceipt = buildExecutionReceipt({
      payload: { ...adjustment, runId: `${resultId}-${packetId}` },
      taskBefore: fullPacket,
      taskAfter: updated,
      runId: `${resultId}-${packetId}`,
      surface: "dove.status",
      command: "apply_dove_status_adjustments",
      actionType: adjustment.status === "completed" ? "verify" : "cleanup",
      status: adjustment.status,
      outcome: `status-${adjustment.status}`,
      summary: adjustment.summary || adjustment.reason || `status-${adjustment.status}`,
      nextAction: updated.nextAction ?? "project:dove.status",
      evidence: payloadEvidence,
      startedAt: transitionAt,
      completedAt: transitionAt,
      boundary: updated.boundary ?? null
    });
    executionReceipts.push(executionReceipt);
    executionIterations.push({
      iteration: executionIterations.length + 1,
      packetId,
      command: "apply_dove_status_adjustments",
      status: adjustment.status,
      outcome: `status-${adjustment.status}`,
      startedAt: transitionAt,
      completedAt: transitionAt,
      artifactRefs: payloadEvidence.artifactRefs,
      evidenceLinks: payloadEvidence.evidenceLinks,
      validationEvidencePaths: payloadEvidence.validationEvidencePaths,
      verificationEvidencePaths: payloadEvidence.verificationEvidencePaths,
      verifiedCriteria: payloadEvidence.verifiedCriteria,
      executionReceipt
    });
    catalog.byId.set(packetId, updated);
    applied.push({
      packetId,
      fromStatus: fullPacket.status,
      toStatus: updated.status,
      displayStatus: updated.status === "completed" || updated.status === "killed" ? "done" : updated.status,
      title: updated.title,
      level: updated.level,
      executionReceipt
    });
  }
  const completedAt = nowIso();
  const result = {
    id: resultId,
    runId: resultId,
    surface: "dove.status",
    command: "apply_dove_status_adjustments",
    status: rejected.length > 0 && applied.length > 0 ? "partially-applied" : rejected.length > 0 ? "rejected" : "applied",
    outcome: rejected.length > 0 && applied.length > 0 ? "partially-applied" : rejected.length > 0 ? "rejected" : "applied",
    packetIds: applied.map((item) => item.packetId),
    applied,
    skipped,
    rejected,
    iterations: executionIterations,
    iterationCount: executionIterations.length,
    executionReceipts,
    executionReceipt: executionReceipts[0] ?? null,
    statusChoices: DOVE_TASK_STATUSES,
    activeTaskIds: loadTaskIndex(root).taskModel.activeTaskIds,
    foreground: true,
    background: false,
    daemon: false,
    responseLanguage,
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex,
    createdAt: completedAt,
    updatedAt: completedAt
  };
  if (applied.length > 0) {
    persistAutoResult(root, result);
  }
  return {
    ...result,
    resultCard: statusAdjustmentResultCard(result, responseLanguage)
  };
}
function unresolvedTaskDependencies(index, task) {
  const byId = new Map((index.items ?? []).map((item) => [item.id, item]));
  return Array.from(/* @__PURE__ */ new Set([...normalizeStringArray24(task.dependencies), ...normalizeStringArray24(task.blockedBy)])).filter((dependencyId) => {
    const dependency = byId.get(dependencyId);
    return !dependency || dependency.status !== "completed";
  });
}
function operatorQueue(index) {
  const tasks = sortOperatorTasks((index.items ?? []).filter((task) => task.level !== 0 && !["completed", "killed"].includes(task.status))).map((task) => {
    const autoPlan = inferAutoStepsForTask(task, {});
    return {
      ...task,
      unresolvedDependencyIds: unresolvedTaskDependencies(index, task),
      proposedSteps: autoPlan.proposedSteps,
      safeToRun: autoPlan.safeToRun,
      requiresHostPass: autoPlan.requiresHostPass,
      whyThisStep: autoPlan.whyThisStep
    };
  });
  const runnableCandidates = tasks.filter((task) => ["ready", "in-progress"].includes(task.status) && task.unresolvedDependencyIds.length === 0);
  const resumableHostPassTasks = tasks.filter(
    (task) => task.status === "blocked" && task.unresolvedDependencyIds.length === 0 && ["awaiting-host-pass", "awaiting-host-pass-result"].includes(task.boundary?.type)
  );
  const autoRunnable = runnableCandidates.filter((task) => task.safeToRun);
  const hostPassRequired = [
    ...runnableCandidates.filter((task) => !task.safeToRun),
    ...resumableHostPassTasks
  ];
  return {
    autoRunnable,
    hostPassRequired,
    runnable: [...autoRunnable, ...hostPassRequired],
    blocked: tasks.filter((task) => task.status === "blocked" && !resumableHostPassTasks.some((resumable) => resumable.id === task.id) || task.unresolvedDependencyIds.length > 0),
    pending: tasks.filter((task) => task.status === "pending")
  };
}
function blockerInvestigationTargetTasks(blockedTasks = []) {
  return blockedTasks.filter((task) => {
    if (normalizeStringArray24(task.unresolvedDependencyIds).length > 0) {
      return true;
    }
    if (task.status !== "blocked" || task.derivedFrom === "blocked-mission-investigation") {
      return false;
    }
    return Object.keys(plainObject3(task.boundary)).length === 0;
  });
}
function sortOperatorTasks(tasks = []) {
  return [...tasks].sort((left, right) => (left.level ?? 3) - (right.level ?? 3) || String(left.createdAt ?? "").localeCompare(String(right.createdAt ?? "")) || String(left.id).localeCompare(String(right.id)));
}
function operatorTaskSummary(task) {
  const summary = { id: task.id, title: task.title, status: task.status, level: task.level, stage: task.stage, domain: task.domain, parentId: task.parentId ?? null, nextAction: task.nextAction };
  if (Array.isArray(task.unresolvedDependencyIds)) {
    summary.unresolvedDependencyIds = task.unresolvedDependencyIds;
  }
  if (Array.isArray(task.proposedSteps)) {
    summary.proposedSteps = task.proposedSteps;
  }
  if (task.safeToRun !== void 0) {
    summary.safeToRun = task.safeToRun;
  }
  if (task.requiresHostPass !== void 0) {
    summary.requiresHostPass = task.requiresHostPass;
  }
  if (task.whyThisStep !== void 0) {
    summary.whyThisStep = task.whyThisStep;
  }
  return summary;
}
function operatorTaskIds(tasks = []) {
  return tasks.map((task) => task.id).filter(Boolean);
}
function operatorQueueSummary(queue = {}) {
  const autoRunnable = queue.autoRunnable ?? [];
  const hostPassRequired = queue.hostPassRequired ?? [];
  const runnable = queue.runnable ?? [];
  const blocked = queue.blocked ?? [];
  const pending = queue.pending ?? [];
  return {
    autoRunnableCount: autoRunnable.length,
    hostPassRequiredCount: hostPassRequired.length,
    runnableCount: runnable.length,
    blockedCount: blocked.length,
    pendingCount: pending.length,
    autoRunnableTaskIds: operatorTaskIds(autoRunnable),
    hostPassRequiredTaskIds: operatorTaskIds(hostPassRequired),
    runnableTaskIds: operatorTaskIds(runnable),
    blockedTaskIds: operatorTaskIds(blocked),
    pendingTaskIds: operatorTaskIds(pending)
  };
}
function operatorQueueCards(queue = {}, preActionGuidance = null, responseLanguage = "zh", { includeGuidance = false } = {}) {
  const guidance = includeGuidance ? preActionGuidance : null;
  return {
    autoRunnable: (queue.autoRunnable ?? []).map((task) => buildOperatorQueueCard(operatorTaskSummary(task), { queue: "auto-runnable", preActionGuidance: guidance }, responseLanguage)),
    hostPassRequired: (queue.hostPassRequired ?? []).map((task) => buildOperatorQueueCard(operatorTaskSummary(task), { queue: "host-pass-required", why: doveText(responseLanguage, "operatorAwaitingStopReason"), preActionGuidance: guidance }, responseLanguage)),
    runnable: (queue.runnable ?? []).map((task) => buildOperatorQueueCard(operatorTaskSummary(task), { queue: "runnable", preActionGuidance: guidance }, responseLanguage)),
    blocked: (queue.blocked ?? []).map((task) => buildOperatorQueueCard(operatorTaskSummary(task), { queue: "blocked", preActionGuidance: guidance }, responseLanguage)),
    pending: (queue.pending ?? []).map((task) => buildOperatorQueueCard(operatorTaskSummary(task), { queue: "pending", preActionGuidance: guidance }, responseLanguage))
  };
}
function operatorQueuePreview(queue = {}, responseLanguage = "zh") {
  const cards = operatorQueueCards(queue, null, responseLanguage);
  return Object.fromEntries(Object.entries(cards).map(([key, value]) => [key, value.slice(0, 2)]));
}
function normalizeBlockerInvestigationMode(args = {}) {
  const explicit = normalizeString10(args.blockerInvestigationMode, "propose").toLowerCase();
  if (!["none", "propose", "create"].includes(explicit)) {
    throw new Error('blockerInvestigationMode must be "none", "propose", or "create".');
  }
  return explicit;
}
function emptyOperatorBlockerPlanConversion(blockedTasks = [], mode = "propose") {
  const blockedTaskIds = operatorTaskIds(blockedTasks);
  return {
    mode,
    proposalOnly: mode !== "create",
    blockedTaskIds,
    proposedBlockedTaskIds: mode === "propose" ? blockedTaskIds : [],
    createdMissions: [],
    reusedMissions: [],
    createdCount: 0,
    reusedCount: 0,
    missionCount: 0,
    createdMissionIds: [],
    reusedMissionIds: [],
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex
  };
}
function compactOperatorBlockerPlanConversion(conversion = {}) {
  const createdMissionIds = normalizeStringArray24(conversion.createdMissionIds ?? operatorTaskIds(conversion.createdMissions ?? []));
  const reusedMissionIds = normalizeStringArray24(conversion.reusedMissionIds ?? operatorTaskIds(conversion.reusedMissions ?? []));
  const createdCount = conversion.createdCount ?? createdMissionIds.length;
  const reusedCount = conversion.reusedCount ?? reusedMissionIds.length;
  return {
    mode: conversion.mode ?? "propose",
    proposalOnly: conversion.proposalOnly !== false,
    blockedTaskIds: normalizeStringArray24(conversion.blockedTaskIds),
    proposedBlockedTaskIds: normalizeStringArray24(conversion.proposedBlockedTaskIds),
    createdCount,
    reusedCount,
    missionCount: conversion.missionCount ?? createdCount,
    createdMissionIds,
    reusedMissionIds,
    taskIndexPath: conversion.taskIndexPath ?? ARTIFACT_PATHS.taskPacketsIndex
  };
}
function compactOperatorResult(result = {}) {
  const { iterations: _iterations, blockerPlanConversion, ...summary } = result;
  return {
    ...summary,
    blockerPlanConversion: compactOperatorBlockerPlanConversion(blockerPlanConversion)
  };
}
var OPERATOR_TOP_LEVEL_FIELDS = /* @__PURE__ */ new Set([
  "confirmed",
  "includeQueueDetails",
  "blockerInvestigationMode",
  "runId",
  "taskResults"
]);
function assertAllowedOperatorTopLevelArgs(args = {}) {
  const unknownFields = Object.keys(plainObject3(args)).filter((field) => !OPERATOR_TOP_LEVEL_FIELDS.has(field));
  if (unknownFields.length > 0) {
    throw new Error(`run_dove_operator does not accept unknown input: ${unknownFields.join(", ")}.`);
  }
  if (args.taskResults !== void 0 && !Array.isArray(args.taskResults)) {
    throw new Error("run_dove_operator taskResults must be an array.");
  }
}
var OPERATOR_TASK_RESULT_FIELDS = /* @__PURE__ */ new Set([
  "packetId",
  "resultStatus",
  "resultSummary",
  "outcome",
  "reason",
  "stopReason",
  "nextAction",
  "evidenceLinks",
  "artifactRefs",
  "validationEvidencePaths",
  "verificationEvidencePaths",
  "verifiedCriteria",
  "executionContract",
  "executionReceipt",
  "boundaryType",
  "boundaryId",
  "requiredInputs",
  "requiredActions",
  "startedAt",
  "completedAt"
]);
function operatorResultMap(args = {}) {
  const results = objectArray(args.taskResults);
  const entries = results.map((result, index) => {
    const source = plainObject3(result);
    const unknownFields = Object.keys(source).filter((field) => !OPERATOR_TASK_RESULT_FIELDS.has(field));
    if (unknownFields.length > 0) {
      throw new Error(`taskResults[${index}] contains unknown fields: ${unknownFields.join(", ")}.`);
    }
    const id = normalizeString10(source.packetId, null);
    if (!id) {
      throw new Error(`taskResults[${index}].packetId is required.`);
    }
    if (!normalizeAllowed3(source.resultStatus, ["completed", "blocked", "in-progress"], null)) {
      throw new Error(`taskResults[${index}].resultStatus must be "completed", "blocked", or "in-progress".`);
    }
    return [normalizeTaskPacketId(id), source];
  });
  return new Map(entries);
}
function operatorAwaitingHostIteration(task, timestamp, responseLanguage = "zh") {
  return {
    packetId: task.id,
    title: task.title,
    status: "awaiting-host-pass-result",
    outcome: "operator-pass-result-required",
    stopReason: doveText(responseLanguage, "operatorAwaitingStopReason"),
    startedAt: timestamp,
    completedAt: timestamp
  };
}
function applyOperatorHostResult(root, taskItem, taskResult, timestamp, responseLanguage = "zh", runId = null) {
  const task = loadFullTask(root, taskItem);
  const recorded = recordDoveMissionPass(root, {
    ...taskResult,
    packetId: task.id,
    runId: taskResult.runId ?? `${runId ?? "operator"}-${task.id}`,
    command: "host-pass-result"
  });
  const updatedTask = recorded.task ?? (recorded.boundaryType ? updateTaskLifecycle(root, task, "blocked", {
    runId,
    surface: "dove.operator",
    command: "host-pass-result",
    boundaryType: normalizeDoveBoundaryType(recorded.boundaryType ?? recorded.status, "blocked-boundary"),
    reason: recorded.message ?? recorded.status,
    stopReason: recorded.status,
    summary: recorded.message ?? recorded.status,
    requiredActions: recorded.requiredActions ?? [],
    nextAction: recorded.nextAction ?? "project:dove.status"
  }) : loadFullTask(root, task));
  const taskResultEvidence = completionEvidenceForPayload(taskResult);
  const status = recorded.status ?? normalizeMissionPassStatus(taskResult);
  const outcome = recorded.result?.outcome ?? normalizeString10(taskResult.outcome, status === "completed" ? "operator-task-completed" : status === "blocked" ? "operator-task-blocked" : "operator-task-progress");
  return {
    updatedTask,
    iteration: {
      packetId: task.id,
      title: task.title,
      status,
      outcome,
      summary: normalizeString10(taskResult.summary ?? taskResult.resultSummary, doveText(responseLanguage, "operatorResultSummary")),
      stopReason: recorded.result?.stopReason ?? updatedTask.boundary?.reason ?? null,
      boundary: updatedTask.boundary ?? null,
      requiredActions: recorded.requiredActions ?? updatedTask.boundary?.requiredActions ?? [],
      evidenceLinks: recorded.evidenceLinks ?? taskResultEvidence.evidenceLinks,
      artifactRefs: recorded.artifactRefs ?? taskResultEvidence.artifactRefs,
      verificationEvidencePaths: recorded.verificationEvidencePaths ?? taskResultEvidence.verificationEvidencePaths,
      verifiedCriteria: recorded.verifiedCriteria ?? taskResultEvidence.verifiedCriteria,
      executionReceipt: recorded.executionReceipt ?? taskResultEvidence.executionReceipt,
      startedAt: normalizeString10(taskResult.startedAt, timestamp),
      completedAt: normalizeString10(taskResult.completedAt, timestamp)
    }
  };
}
function runOperatorInternalStep(root, taskItem, timestamp, responseLanguage = "zh", runId = null) {
  const task = loadFullTask(root, taskItem);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Running a Dove operator work step"
  );
  const autoPlan = inferAutoStepsForTask(task, {});
  const step = autoPlan.steps[0] ?? null;
  if (!step) {
    const iteration = operatorAwaitingHostIteration(task, timestamp, responseLanguage);
    const updatedTask = updateTaskLifecycle(root, task, "blocked", {
      runId,
      surface: "dove.operator",
      command: "host-pass-result",
      boundaryType: "awaiting-host-pass-result",
      reason: autoPlan.whyThisStep ?? iteration.stopReason,
      stopReason: autoPlan.whyThisStep ?? iteration.stopReason,
      summary: iteration.outcome,
      requiredActions: autoHostPassRequiredActions(autoPlan),
      nextAction: "project:dove.status"
    });
    return {
      awaitingTaskId: task.id,
      updatedTask,
      iteration
    };
  }
  const startedAt = nowIso();
  let workingTask = task.status === "ready" ? updateTaskLifecycle(root, task, "in-progress", { runId, surface: "dove.operator", command: step.command }) : task;
  try {
    const output = executeAutoStep(root, step.command, stepArgsForTask(workingTask, step.args));
    const classified = classifyAutoStepResult(step.command, output);
    const completedAt = nowIso();
    let updatedTask = workingTask;
    if (step.completeTask === true) {
      const completionArgs = {
        runId,
        surface: "dove.operator",
        command: step.command,
        resultSummary: classified.outcome,
        summary: classified.outcome,
        artifactRefs: classified.artifactRefs,
        evidenceLinks: classified.evidenceLinks,
        verificationEvidencePaths: classified.verificationEvidencePaths,
        verifiedCriteria: classified.verifiedCriteria,
        executionReceipt: classified.executionReceipt
      };
      const verificationBlock = completionVerificationBlock(root, workingTask, completionArgs, responseLanguage);
      if (verificationBlock) {
        updatedTask = updateTaskLifecycle(root, workingTask, "blocked", {
          ...completionArgs,
          boundaryType: normalizeDoveBoundaryType(verificationBlock.boundaryType ?? verificationBlock.status, "blocked-boundary"),
          reason: verificationBlock.message,
          stopReason: verificationBlock.status,
          summary: verificationBlock.message,
          requiredActions: verificationBlock.requiredActions ?? [],
          nextAction: "project:dove.status"
        });
      } else {
        updatedTask = updateTaskLifecycle(root, workingTask, "completed", { ...completionArgs, nextAction: "project:dove.status" });
      }
    } else {
      updatedTask = applyPacketStepResult2(root, workingTask, {
        runId,
        surface: "dove.operator",
        command: step.command,
        output,
        classified
      });
    }
    return {
      updatedTask,
      iteration: {
        packetId: task.id,
        title: task.title,
        command: step.command,
        status: classified.status,
        outcome: classified.outcome,
        stopReason: classified.stopReason,
        output: summarizeStepOutput(output),
        artifactRefs: classified.artifactRefs,
        evidenceLinks: classified.evidenceLinks,
        verificationEvidencePaths: classified.verificationEvidencePaths,
        verifiedCriteria: classified.verifiedCriteria,
        executionReceipt: classified.executionReceipt,
        proposedSteps: autoPlan.proposedSteps,
        startedAt,
        completedAt
      }
    };
  } catch (error) {
    const completedAt = nowIso();
    workingTask = updateTaskLifecycle(root, workingTask, "blocked", { runId, surface: "dove.operator", command: step.command, boundaryType: "workflow-error-boundary", reason: error.message, stopReason: error.message, nextAction: "project:dove.status" });
    return {
      updatedTask: workingTask,
      iteration: {
        packetId: task.id,
        title: task.title,
        command: step.command,
        status: "blocked-boundary",
        outcome: "workflow-error-boundary",
        stopReason: error.message,
        proposedSteps: autoPlan.proposedSteps,
        startedAt,
        completedAt
      }
    };
  }
}
function materializeBlockedInvestigationMissions(root, blockedTasks, runId, responseLanguage = "zh") {
  const blockedTaskIds = operatorTaskIds(blockedTasks);
  if (blockedTasks.length === 0) {
    return emptyOperatorBlockerPlanConversion(blockedTasks, "create");
  }
  const index = loadTaskIndex(root);
  const init = initPacket(index);
  if (!init) {
    throw new Error("Dove operator blocker investigation requires the level-0 init task.");
  }
  const catalog = readTaskPacketCatalog(root);
  let nextIndex = index;
  const createdMissions = [];
  const reusedMissions = [];
  for (const blockedTask of blockedTasks) {
    const fullTask = loadFullTask(root, blockedTask);
    const taskLabel = fullTask.title ?? fullTask.id;
    const source = {
      id: `task-investigate-blocker-for-${fullTask.id}`,
      title: doveText(responseLanguage, "blockerTitle", { title: taskLabel }),
      summary: normalizeString10(fullTask.blockedReason, doveText(responseLanguage, "blockerSummary", { title: taskLabel })),
      stage: "plan",
      domain: fullTask.domain,
      level: (Number.isFinite(fullTask.level) ? fullTask.level : 3) + 1,
      evidenceExpectations: [doveText(responseLanguage, "blockerEvidenceWhy", { id: fullTask.id }), doveText(responseLanguage, "blockerEvidenceAction")]
    };
    const packet = buildDerivedTask(root, init, fullTask, source, {
      idPrefix: `task-investigate-blocker-for-${fullTask.id}`,
      level: source.level,
      minimumLevel: source.level,
      classification: { stage: "plan", domain: fullTask.domain },
      sourcePlanTaskId: fullTask.id,
      sourceMissionPassRunId: runId,
      derivedFrom: "blocked-mission-investigation"
    });
    const result = materializeOneDerivedTask(root, nextIndex, packet, catalog);
    nextIndex = result.index;
    (result.created ? createdMissions : reusedMissions).push(result.packet);
  }
  if (createdMissions.length > 0) {
    persistDerivedTaskIndex(root, nextIndex, createdMissions.at(-1));
  }
  const createdSummaries = createdMissions.map(operatorTaskSummary);
  const reusedSummaries = reusedMissions.map(operatorTaskSummary);
  return {
    mode: "create",
    proposalOnly: false,
    blockedTaskIds,
    proposedBlockedTaskIds: [],
    createdMissions: createdSummaries,
    reusedMissions: reusedSummaries,
    createdCount: createdSummaries.length,
    reusedCount: reusedSummaries.length,
    missionCount: createdSummaries.length,
    createdMissionIds: operatorTaskIds(createdSummaries),
    reusedMissionIds: operatorTaskIds(reusedSummaries),
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex
  };
}
function runDoveOperator(root, args = {}) {
  assertGovernanceMutationRegistered("run-dove-operator", "guarded");
  assertAllowedOperatorTopLevelArgs(args);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const index = loadTaskIndex(root);
  const queue = operatorQueue(index);
  const includeQueueDetails = args.includeQueueDetails === true;
  const blockerInvestigationMode = normalizeBlockerInvestigationMode(args);
  const blockerInvestigationTargets = blockerInvestigationTargetTasks(queue.blocked);
  const blockerInvestigationProposal = emptyOperatorBlockerPlanConversion(blockerInvestigationTargets, blockerInvestigationMode);
  if (!hasExplicitConfirmation(args)) {
    const preActionGuidance = buildPreActionGuidance({
      surface: "dove.operator",
      responseLanguage,
      request: requestTextFromArgs(args),
      roleId: "planner",
      operatorLessons: readOperatorLessonsIndex(root),
      nextAction: "project:dove.status",
      routeHint: "project:dove.status",
      workflowKind: "operator",
      statusSummary: {
        autoRunnableCount: queue.autoRunnable.length,
        hostPassRequiredCount: queue.hostPassRequired.length,
        runnableCount: queue.runnable.length,
        blockedCount: queue.blocked.length,
        pendingCount: queue.pending.length,
        blockerInvestigationMode
      }
    });
    const response2 = {
      status: "needs-confirmation",
      proposalOnly: true,
      noAutoApply: true,
      writes: [],
      confirmationRequired: true,
      executionMode: "operator-one-foreground-pass",
      preActionGuidance,
      foreground: true,
      background: false,
      daemon: false,
      queueSummary: operatorQueueSummary(queue),
      queuePreview: operatorQueuePreview(queue, responseLanguage),
      blockerInvestigationMode,
      blockerPlanConversion: compactOperatorBlockerPlanConversion(blockerInvestigationProposal),
      includeQueueDetails,
      confirmArgs: {
        ...args,
        confirmed: true
      },
      responseLanguage,
      message: doveText(responseLanguage, "operatorConfirmMessage")
    };
    if (includeQueueDetails) {
      Object.assign(response2, {
        queueCards: operatorQueueCards(queue, preActionGuidance, responseLanguage, { includeGuidance: true }),
        autoRunnableTasks: queue.autoRunnable.map(operatorTaskSummary),
        hostPassRequiredTasks: queue.hostPassRequired.map(operatorTaskSummary),
        runnableTasks: queue.runnable.map(operatorTaskSummary),
        blockedTasks: queue.blocked.map(operatorTaskSummary),
        pendingTasks: queue.pending.map(operatorTaskSummary)
      });
    }
    return response2;
  }
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "experiments",
    "builder",
    "Running Dove operator"
  );
  const resultMap = operatorResultMap(args);
  ensureWorkspace(root);
  const timestamp = nowIso();
  const runId = normalizeTaskPacketId(args.runId ?? `operator-${Date.now().toString(36)}`);
  const iterations = [];
  const updatedTasks = [];
  const awaitingResults = [];
  for (const taskItem of queue.autoRunnable) {
    const stepResult = runOperatorInternalStep(root, taskItem, timestamp, responseLanguage, runId);
    iterations.push(stepResult.iteration);
    if (stepResult.updatedTask) {
      updatedTasks.push(stepResult.updatedTask);
    }
    if (stepResult.awaitingTaskId) {
      awaitingResults.push(stepResult.awaitingTaskId);
    }
  }
  const skippedHostPassTaskIds = [];
  const skippedHostPassRequiredActions = [];
  for (const taskItem of queue.hostPassRequired) {
    const task = loadFullTask(root, taskItem);
    const autoPlan = inferAutoStepsForTask(task, {});
    const taskResult = resultMap.get(task.id);
    if (!taskResult) {
      skippedHostPassTaskIds.push(task.id);
      skippedHostPassRequiredActions.push(...autoHostPassRequiredActions(autoPlan));
      awaitingResults.push(task.id);
      continue;
    }
    const hostResult = applyOperatorHostResult(root, task, taskResult, timestamp, responseLanguage, runId);
    updatedTasks.push(hostResult.updatedTask);
    iterations.push(hostResult.iteration);
  }
  const blockerPlanConversion = blockerInvestigationMode === "create" ? materializeBlockedInvestigationMissions(root, blockerInvestigationTargets, runId, responseLanguage) : emptyOperatorBlockerPlanConversion(blockerInvestigationTargets, blockerInvestigationMode);
  const awaitingResultSet = new Set(awaitingResults);
  const awaitingRequiredActions = Array.from(/* @__PURE__ */ new Set([
    ...updatedTasks.filter((task) => awaitingResultSet.has(task.id)).flatMap((task) => normalizeStringArray24(task.boundary?.requiredActions ?? task.requiredActions)),
    ...skippedHostPassRequiredActions
  ]));
  const durableWorkHappened = iterations.length > 0 || updatedTasks.length > 0 || (blockerPlanConversion.createdCount ?? 0) > 0;
  const blockerProposalPending = blockerPlanConversion.proposedBlockedTaskIds?.length > 0;
  const boundaryIterations = iterations.filter((iteration) => iteration.boundary || iteration.requiredActions?.length > 0 || ["blocked", "blocked-boundary", "verification-failed", "needs-completion-evidence", "missing-required-materials", "workflow-error-boundary"].includes(String(iteration.status ?? "")));
  const boundaryRequiredActions = Array.from(new Set(boundaryIterations.flatMap((iteration) => normalizeStringArray24(iteration.requiredActions ?? iteration.boundary?.requiredActions))));
  const actionRequired = awaitingResults.length > 0 || blockerProposalPending || boundaryIterations.length > 0;
  const resultStatus = awaitingResults.length > 0 ? durableWorkHappened ? "awaiting-host-results" : "needs-host-results" : boundaryIterations.length > 0 ? "blocked-boundary" : blockerProposalPending ? "blocked-investigation-proposed" : "foreground-pass-complete";
  const resultOutcome = awaitingResults.length > 0 ? durableWorkHappened ? "operator-pass-results-required" : "operator-pass-needs-host-results" : boundaryIterations.length > 0 ? "operator-pass-action-required" : blockerProposalPending ? "operator-blocker-investigation-proposed" : durableWorkHappened ? "operator-pass-recorded" : "operator-queue-has-no-runnable-work";
  const result = {
    id: runId,
    surface: "dove.operator",
    status: resultStatus,
    outcome: resultOutcome,
    foreground: true,
    background: false,
    daemon: false,
    runtimeRecorded: durableWorkHappened,
    maxIterations: 1,
    iterationCount: iterations.length,
    autoRunnableTaskIds: queue.autoRunnable.map((task) => task.id),
    hostPassRequiredTaskIds: queue.hostPassRequired.map((task) => task.id),
    runnableTaskIds: queue.runnable.map((task) => task.id),
    updatedTaskIds: updatedTasks.map((task) => task.id),
    awaitingResultTaskIds: awaitingResults,
    skippedHostPassTaskIds,
    awaitingRequiredActions,
    boundaryRequiredActions,
    actionRequired,
    blockedTaskIds: queue.blocked.map((task) => task.id),
    pendingTaskIds: queue.pending.map((task) => task.id),
    blockerInvestigationMode,
    blockerPlanConversion,
    iterations,
    stopReason: awaitingResults.length > 0 ? "host-pass-results-required" : boundaryIterations.length > 0 ? "operator-task-boundary-requires-action" : blockerProposalPending ? "blocked-investigation-requires-explicit-create" : "foreground-pass-complete",
    responseLanguage,
    createdAt: timestamp,
    updatedAt: nowIso()
  };
  if (durableWorkHappened) {
    persistAutoResult(root, result);
  }
  const preActionGuidanceSummary = summarizePreActionGuidance(buildPreActionGuidance({
    surface: "dove.operator",
    responseLanguage,
    request: requestTextFromArgs(args),
    roleId: "planner",
    operatorLessons: readOperatorLessonsIndex(root),
    nextAction: "project:dove.status",
    routeHint: "project:dove.status",
    workflowKind: "operator",
    statusSummary: {
      autoRunnableCount: queue.autoRunnable.length,
      hostPassRequiredCount: queue.hostPassRequired.length,
      runnableCount: queue.runnable.length,
      blockedCount: queue.blocked.length,
      pendingCount: queue.pending.length,
      blockerInvestigationMode
    }
  }));
  const resultCard = operatorResultCard(result, { nextAction: "project:dove.status", preActionGuidanceSummary }, responseLanguage);
  const response = {
    status: result.status,
    operatorResultSummary: compactOperatorResult(result),
    resultCard,
    preActionGuidanceSummary,
    queueSummary: operatorQueueSummary(queue),
    updatedTaskIds: updatedTasks.map((task) => task.id),
    awaitingResultTaskIds: awaitingResults,
    skippedHostPassTaskIds,
    awaitingRequiredActions,
    boundaryRequiredActions,
    actionRequired,
    blockerInvestigationMode,
    blockerPlanConversion: compactOperatorBlockerPlanConversion(blockerPlanConversion),
    includeQueueDetails,
    responseLanguage,
    nextAction: "project:dove.status"
  };
  if (includeQueueDetails) {
    Object.assign(response, {
      result,
      autoRunnableTasks: queue.autoRunnable.map(operatorTaskSummary),
      hostPassRequiredTasks: queue.hostPassRequired.map(operatorTaskSummary),
      updatedTasks: updatedTasks.map(operatorTaskSummary),
      blockerPlanConversionDetails: blockerPlanConversion
    });
  }
  return response;
}
function resetDoveVersion(root, args = {}) {
  assertGovernanceMutationRegistered("reset-dove-version", "guarded");
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    "versions",
    "planner",
    "Resetting the Dove version"
  );
  ensureWorkspace(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const timestamp = nowIso();
  const index = loadTaskIndex(root);
  const init = initPacket(index);
  if (!init) {
    throw new Error("/dove:version requires an init task to preserve. Run /dove:init first.");
  }
  const versionId = normalizeTaskPacketId(args.versionId ?? args.id ?? `version-${slugify14(args.reason ?? args.title ?? timestamp)}`);
  const snapshotPath = path26.join(ARTIFACT_PATHS.versionSnapshotsDir, `${versionId}-task-index.json`);
  writeJson(root, snapshotPath, { versionId, createdAt: timestamp, reason: normalizeString10(args.reason ?? args.summary, doveText(responseLanguage, "versionReason")), taskIndex: index });
  const resetIndex = saveTaskIndex(root, {
    ...index,
    items: [init],
    updatedAt: timestamp
  });
  const versions = readJson(root, ARTIFACT_PATHS.versionsIndex, { version: 1, items: [], updatedAt: null });
  const item = {
    id: versionId,
    title: normalizeString10(args.title, doveText(responseLanguage, "versionTitle")),
    reason: normalizeString10(args.reason ?? args.summary, doveText(responseLanguage, "versionReason")),
    preservedInitId: init.id,
    clearedTaskIds: (index.items ?? []).filter((task) => task.level !== 0).map((task) => task.id),
    preservedLessonIds: activeLessons(root).map((lesson) => lesson.id),
    snapshotPath,
    createdAt: timestamp
  };
  writeJson(root, ARTIFACT_PATHS.versionsIndex, {
    ...versions,
    items: [...Array.isArray(versions.items) ? versions.items.filter((existing) => existing.id !== versionId) : [], item],
    updatedAt: timestamp
  });
  const state = loadState(root);
  saveState(root, {
    ...state,
    pipeline: {
      ...state.pipeline,
      resumeCommand: "project:dove.mission",
      updatedAt: timestamp
    },
    orchestration: {
      ...state.orchestration,
      activeTaskIds: resetIndex.taskModel.activeTaskIds,
      currentVersionId: versionId,
      nextAction: doveText(responseLanguage, "versionNextActionDisplay")
    }
  });
  return {
    status: "reset",
    version: item,
    init,
    activeTaskIds: resetIndex.taskModel.activeTaskIds,
    responseLanguage,
    nextAction: "project:dove.mission"
  };
}
var AUTO_INTERNAL_COMMANDS = ["dove.source", "dove.note", "dove.experience", "dove.figure", "dove.draft", "dove.review", "dove.review-loop", "dove.rebuttal", "dove.lessons", "dove.status"];
var AUTO_READ_ONLY_COMMANDS = /* @__PURE__ */ new Set(["dove.lessons", "dove.status"]);
var RUN_DOVE_AUTO_PUBLIC_KEYS = /* @__PURE__ */ new Set([
  "packetId",
  "taskPacketId",
  "missionPacketId",
  "taskId",
  "target",
  "packetTarget",
  "taskName",
  "index",
  "proposalVersion",
  "proposalWorkspace",
  "proposalKind",
  "mutationMode",
  "responseLanguage",
  "initId",
  "initTitle",
  "initObjective",
  "initGoal",
  "initDomain",
  "initStatus",
  "initArtifactRefs",
  "projectTitle",
  "workspaceTitle",
  "projectObjective",
  "projectGoal",
  "projectDomain",
  "id",
  "goal",
  "objective",
  "prompt",
  "title",
  "summary",
  "stage",
  "domain",
  "level",
  "creatorKind",
  "status",
  "dependencies",
  "blockedBy",
  "ownerRole",
  "nextRole",
  "boundary",
  "handoff",
  "currentFocus",
  "nextAction",
  "evidenceExpectations",
  "workContract",
  "executionContract",
  "validationEvidencePaths",
  "verificationEvidencePaths",
  "verifiedCriteria",
  "artifactRefs",
  "contextPolicy",
  "lessonIds",
  "checklist",
  "autoChecklist",
  "createChecklist",
  "checklistItems",
  "proposalDigest",
  "confirmed",
  "confirm",
  "maxIterations",
  "maxSteps",
  "completeTask",
  "completeOnSuccess",
  "steps",
  "runId"
]);
var RETIRED_AUTO_TOP_LEVEL_KEYS = /* @__PURE__ */ new Set([
  "actions",
  "autoSteps",
  "command",
  "complete",
  "nextCommand",
  "preset",
  "workflow"
]);
var AUTO_STEP_WRAPPER_KEYS = /* @__PURE__ */ new Set([
  "command",
  "args",
  "completeTask",
  "requiredMaterials",
  "outputArtifacts",
  "convergenceChecks",
  "failureRoutes",
  "executionContract",
  "validationEvidencePaths",
  "verificationEvidencePaths",
  "verifiedCriteria"
]);
var AUTO_STEP_ARGS_BY_COMMAND = new Map(Object.entries({
  "dove.source": ["sourceId", "citationKey", "title", "authors", "year", "locator", "sourceType", "abstract", "origin", "sources"],
  "dove.note": ["noteId", "title", "sectionId", "sourceIds", "summary", "quotes", "claims", "openQuestions"],
  "dove.experience": ["id", "experimentId", "goal", "idea", "title", "methodology", "method", "successMetric", "metric", "comparisonTargets", "baselines", "claimId", "result", "resultId", "outcome", "summary", "resultSummary", "evidenceLinks", "artifactPaths", "plan"],
  "dove.figure": ["intent", "description", "name", "title", "figureId", "id", "purpose", "captionIntent", "targetClaimIds", "claimIds", "sourceSections", "sectionIds", "sourceArtifactPaths", "artifactPaths", "relatedExperimentIds", "experimentIds", "reviewConcernIds", "rebuttalIssueIds", "requiredVisualElements", "materialHints", "materialRequirements", "providerId", "executeProvider", "allowMissingMaterials", "runId", "outputFormat", "constraints", "outputManifestPath", "sourceSvgPath", "targetFinalSvgPath", "svgContent", "caption", "captionDraft", "captionId", "semanticCoverage", "semanticReview"],
  "dove.draft": ["sectionId", "title", "body", "status", "summary"],
  "dove.review": ["scope", "stage", "reviewer"],
  "dove.review-loop": ["runId", "scope", "instructions", "stage", "summary", "artifactPaths", "reviewedArtifactPaths", "responseLanguage", "language"],
  "dove.rebuttal": ["issues"],
  "dove.lessons": [],
  "dove.status": []
}).map(([command, keys]) => [command, new Set(keys)]));
function assertAllowedAutoKeys(value, allowedKeys, inputPath) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`run_dove_auto requires an object at ${inputPath}.`);
  }
  const unknown = Object.keys(value).filter((key) => !allowedKeys.has(key));
  if (unknown.length > 0) {
    throw new Error(`run_dove_auto does not accept unknown input ${unknown.map((key) => `${inputPath}.${key}`).join(", ")}.`);
  }
}
function assertAllowedAutoTopLevelArgs(args = {}) {
  if (!args || typeof args !== "object" || Array.isArray(args)) {
    throw new Error("run_dove_auto arguments must be a plain object.");
  }
  const retired = Object.keys(args).filter((key) => RETIRED_AUTO_TOP_LEVEL_KEYS.has(key));
  if (retired.length > 0) {
    throw new Error(`run_dove_auto no longer accepts retired top-level input: ${retired.join(", ")}. Use steps[].command for explicit work.`);
  }
  assertAllowedAutoKeys(args, RUN_DOVE_AUTO_PUBLIC_KEYS, "$");
}
function hasTaskIntent(args = {}) {
  return [args.goal, args.objective, args.prompt, args.title, args.summary].some((value) => typeof value === "string" && value.trim());
}
function hasExplicitTaskSelector(args = {}) {
  return [args.packetId, args.taskPacketId, args.missionPacketId, args.taskId, args.target, args.packetTarget, args.taskName].some((value) => typeof value === "string" && value.trim()) || Number.isFinite(args.index);
}
function normalizePositiveInteger3(value, fallback) {
  return Number.isFinite(value) ? Math.max(1, Math.floor(value)) : fallback;
}
function resolveAutoMaxIterations(state, args = {}) {
  return normalizePositiveInteger3(args.maxIterations ?? args.maxSteps, normalizePositiveInteger3(state.settings?.auto?.maxIterations, 3));
}
function normalizeAutoCommandId(value) {
  const raw = typeof value === "string" ? value.trim() : "";
  if (!raw) {
    return null;
  }
  let normalized = raw.replace(/^project:/, "").replace(/^\/dove:/, "dove.").replace(/^dove:/, "dove.").replace(/_/g, "-");
  if (!normalized.startsWith("dove.")) {
    normalized = `dove.${normalized}`;
  }
  return AUTO_INTERNAL_COMMANDS.includes(normalized) ? normalized : null;
}
function normalizeConcreteAutoCommandId(value) {
  const command = normalizeAutoCommandId(value);
  return command && !["dove.status", "dove.auto"].includes(command) ? command : null;
}
function projectContinuationRoute(value) {
  const raw = typeof value === "string" ? value.trim().toLowerCase().replace(/^project:/, "") : "";
  return ["dove.status", "dove.auto"].includes(raw) ? raw : null;
}
function explicitStepsFrom(args = {}) {
  const retiredAliases = ["autoSteps", "actions"].filter((key) => Object.hasOwn(args, key));
  if (retiredAliases.length > 0) {
    throw new Error(`run_dove_auto no longer accepts retired step aliases: ${retiredAliases.join(", ")}.`);
  }
  if (!Object.hasOwn(args, "steps")) {
    return [];
  }
  if (!Array.isArray(args.steps)) {
    throw new Error("run_dove_auto requires steps to be an array.");
  }
  return args.steps;
}
var AUTO_SOURCE_ITEM_KEYS = /* @__PURE__ */ new Set([
  "sourceId",
  "citationKey",
  "title",
  "authors",
  "year",
  "locator",
  "sourceType",
  "abstract",
  "origin"
]);
var AUTO_SOURCE_STRING_KEYS = /* @__PURE__ */ new Set([
  "sourceId",
  "citationKey",
  "title",
  "locator",
  "sourceType",
  "abstract",
  "origin"
]);
var AUTO_EXPERIENCE_PLAN_KEYS = /* @__PURE__ */ new Set([
  "id",
  "experimentId",
  "goal",
  "idea",
  "title",
  "methodology",
  "method",
  "successMetric",
  "metric",
  "comparisonTargets",
  "baselines",
  "claimId"
]);
var AUTO_EXPERIENCE_PLAN_STRING_KEYS = /* @__PURE__ */ new Set([
  "id",
  "experimentId",
  "goal",
  "idea",
  "title",
  "methodology",
  "method",
  "successMetric",
  "metric",
  "claimId"
]);
var AUTO_EXPERIENCE_RESULT_KEYS = /* @__PURE__ */ new Set([
  "id",
  "resultId",
  "experimentId",
  "claimId",
  "outcome",
  "summary",
  "resultSummary",
  "evidenceLinks",
  "artifactPaths",
  "comparisonTargets"
]);
var AUTO_EXPERIENCE_RESULT_STRING_KEYS = /* @__PURE__ */ new Set([
  "id",
  "resultId",
  "experimentId",
  "claimId",
  "outcome",
  "summary",
  "resultSummary"
]);
var AUTO_FIGURE_MATERIAL_KEYS = /* @__PURE__ */ new Set([
  "id",
  "type",
  "label",
  "summary",
  "artifactPath",
  "path"
]);
var AUTO_FIGURE_SEMANTIC_COVERAGE_KEYS = /* @__PURE__ */ new Set([
  "observations",
  "evidencePaths",
  "artifactPaths"
]);
var AUTO_FIGURE_SEMANTIC_REVIEW_KEYS = /* @__PURE__ */ new Set([
  "summary",
  "observations",
  "evidencePaths",
  "artifactPaths"
]);
var AUTO_WORK_CONTRACT_KEYS = /* @__PURE__ */ new Set([
  "purpose",
  "deliverables",
  "outOfScope",
  "outOfScopeItems",
  "evidenceContract",
  "doneCriteria",
  "practicalImpact",
  "recommendedRoutes"
]);
var AUTO_WORK_CONTRACT_STRING_KEYS = /* @__PURE__ */ new Set([
  "purpose",
  "practicalImpact"
]);
var AUTO_WORK_CONTRACT_STRING_ARRAY_KEYS = /* @__PURE__ */ new Set([
  "deliverables",
  "outOfScope",
  "outOfScopeItems",
  "evidenceContract",
  "doneCriteria"
]);
var AUTO_WORK_CONTRACT_ROUTE_KEYS = /* @__PURE__ */ new Set([
  "label",
  "title",
  "command",
  "nextAction",
  "workflow",
  "copyableCommand",
  "copyCommand",
  "packetId",
  "taskPacketId",
  "missionPacketId",
  "when",
  "reason",
  "role",
  "ownerRole",
  "evidenceRequired",
  "evidenceContract",
  "evidenceExpectations",
  "doneCriteria",
  "rank"
]);
var AUTO_WORK_CONTRACT_ROUTE_STRING_KEYS = /* @__PURE__ */ new Set([
  "label",
  "title",
  "command",
  "nextAction",
  "workflow",
  "copyableCommand",
  "copyCommand",
  "packetId",
  "taskPacketId",
  "missionPacketId",
  "when",
  "reason",
  "role",
  "ownerRole"
]);
var AUTO_WORK_CONTRACT_ROUTE_STRING_ARRAY_KEYS = /* @__PURE__ */ new Set([
  "evidenceRequired",
  "evidenceContract",
  "evidenceExpectations",
  "doneCriteria"
]);
var AUTO_EXECUTION_CONTRACT_KEYS = /* @__PURE__ */ new Set([
  "chainType",
  "roleSequence",
  "readFirst",
  "action",
  "implementation",
  "files",
  "materials",
  "convergence",
  "failureRoutes"
]);
var AUTO_EXECUTION_FILE_KEYS = /* @__PURE__ */ new Set([
  "path",
  "action",
  "target",
  "change"
]);
var AUTO_EXECUTION_MATERIALS_KEYS = /* @__PURE__ */ new Set([
  "requiredInputs",
  "requiredArtifacts",
  "sourceRefs",
  "artifactRefs"
]);
var AUTO_EXECUTION_CONVERGENCE_KEYS = /* @__PURE__ */ new Set([
  "criteria",
  "verificationCommands",
  "evidenceRequired",
  "definitionOfDone"
]);
var AUTO_FAILURE_ROUTE_KEYS = /* @__PURE__ */ new Set([
  "on",
  "boundaryType",
  "nextAction",
  "requiredActions"
]);
var AUTO_CHECKLIST_ITEM_KEYS = /* @__PURE__ */ new Set([
  "id",
  "title",
  "summary",
  "level",
  "stage",
  "domain",
  "status",
  "dependencies",
  "blockedBy",
  "evidenceExpectations",
  "artifactRefs",
  "nextAction",
  "workContract",
  "executionContract"
]);
var AUTO_CHECKLIST_ITEM_STRING_KEYS = /* @__PURE__ */ new Set([
  "id",
  "title",
  "summary",
  "stage",
  "domain",
  "status",
  "nextAction"
]);
var AUTO_CHECKLIST_ITEM_STRING_ARRAY_KEYS = /* @__PURE__ */ new Set([
  "dependencies",
  "blockedBy",
  "evidenceExpectations",
  "artifactRefs"
]);
var AUTO_REVIEW_LOOP_DRAFT_KEYS = /* @__PURE__ */ new Set(["body"]);
var AUTO_REVIEW_LOOP_EXPERIENCE_KEYS = /* @__PURE__ */ new Set([
  "id",
  "experimentId",
  "goal",
  "idea",
  "title"
]);
var AUTO_REBUTTAL_ISSUE_KEYS = /* @__PURE__ */ new Set([
  "id",
  "reviewer",
  "summary",
  "severity",
  "status",
  "evidenceLinks",
  "claimIds",
  "experimentIds",
  "responseDirection"
]);
var AUTO_REBUTTAL_ISSUE_STRING_KEYS = /* @__PURE__ */ new Set([
  "id",
  "reviewer",
  "summary",
  "severity",
  "status",
  "responseDirection"
]);
function assertAutoString(value, inputPath) {
  if (typeof value !== "string") {
    throw new Error(`run_dove_auto requires a string at ${inputPath}.`);
  }
}
function assertAutoStringArray(value, inputPath) {
  if (!Array.isArray(value)) {
    throw new Error(`run_dove_auto requires an array at ${inputPath}.`);
  }
  value.forEach((item, index) => assertAutoString(item, `${inputPath}[${index}]`));
}
function assertAutoOptionalStringFields(value, keys, inputPath) {
  for (const key of keys) {
    if (Object.hasOwn(value, key)) {
      assertAutoString(value[key], `${inputPath}.${key}`);
    }
  }
}
function assertAutoOptionalStringArrayFields(value, keys, inputPath) {
  for (const key of keys) {
    if (Object.hasOwn(value, key)) {
      assertAutoStringArray(value[key], `${inputPath}.${key}`);
    }
  }
}
function assertAutoSourceItem(source, inputPath) {
  assertAllowedAutoKeys(source, AUTO_SOURCE_ITEM_KEYS, inputPath);
  assertAutoOptionalStringFields(source, AUTO_SOURCE_STRING_KEYS, inputPath);
  if (Object.hasOwn(source, "year") && typeof source.year !== "string" && typeof source.year !== "number") {
    throw new Error(`run_dove_auto requires a string or number at ${inputPath}.year.`);
  }
  if (Object.hasOwn(source, "authors")) {
    assertAutoStringArray(source.authors, `${inputPath}.authors`);
  }
}
function assertAutoExperiencePlan(plan, inputPath) {
  assertAllowedAutoKeys(plan, AUTO_EXPERIENCE_PLAN_KEYS, inputPath);
  assertAutoOptionalStringFields(plan, AUTO_EXPERIENCE_PLAN_STRING_KEYS, inputPath);
  assertAutoOptionalStringArrayFields(plan, /* @__PURE__ */ new Set(["comparisonTargets", "baselines"]), inputPath);
}
function assertAutoExperienceResult(result, inputPath) {
  assertAllowedAutoKeys(result, AUTO_EXPERIENCE_RESULT_KEYS, inputPath);
  assertAutoOptionalStringFields(result, AUTO_EXPERIENCE_RESULT_STRING_KEYS, inputPath);
  assertAutoOptionalStringArrayFields(result, /* @__PURE__ */ new Set(["evidenceLinks", "artifactPaths", "comparisonTargets"]), inputPath);
}
function assertAutoFigureMaterialItem(item, inputPath) {
  assertAllowedAutoKeys(item, AUTO_FIGURE_MATERIAL_KEYS, inputPath);
  assertAutoOptionalStringFields(item, AUTO_FIGURE_MATERIAL_KEYS, inputPath);
}
function assertAutoFigureSemanticObservation(value, allowedKeys, inputPath) {
  assertAllowedAutoKeys(value, allowedKeys, inputPath);
  if (Object.hasOwn(value, "summary")) {
    assertAutoString(value.summary, `${inputPath}.summary`);
  }
  assertAutoOptionalStringArrayFields(value, /* @__PURE__ */ new Set(["observations", "evidencePaths", "artifactPaths"]), inputPath);
}
function assertAutoFailureRoute(route, inputPath) {
  assertAllowedAutoKeys(route, AUTO_FAILURE_ROUTE_KEYS, inputPath);
  assertAutoOptionalStringFields(route, /* @__PURE__ */ new Set(["on", "boundaryType", "nextAction"]), inputPath);
  assertAutoOptionalStringArrayFields(route, /* @__PURE__ */ new Set(["requiredActions"]), inputPath);
}
function assertAutoWorkContractRoute(route, inputPath) {
  assertAllowedAutoKeys(route, AUTO_WORK_CONTRACT_ROUTE_KEYS, inputPath);
  assertAutoOptionalStringFields(route, AUTO_WORK_CONTRACT_ROUTE_STRING_KEYS, inputPath);
  assertAutoOptionalStringArrayFields(route, AUTO_WORK_CONTRACT_ROUTE_STRING_ARRAY_KEYS, inputPath);
  if (Object.hasOwn(route, "rank") && typeof route.rank !== "number") {
    throw new Error(`run_dove_auto requires a number at ${inputPath}.rank.`);
  }
}
function assertAutoWorkContract(contract, inputPath) {
  assertAllowedAutoKeys(contract, AUTO_WORK_CONTRACT_KEYS, inputPath);
  assertAutoOptionalStringFields(contract, AUTO_WORK_CONTRACT_STRING_KEYS, inputPath);
  assertAutoOptionalStringArrayFields(contract, AUTO_WORK_CONTRACT_STRING_ARRAY_KEYS, inputPath);
  if (Object.hasOwn(contract, "recommendedRoutes")) {
    if (!Array.isArray(contract.recommendedRoutes)) {
      throw new Error(`run_dove_auto requires an array at ${inputPath}.recommendedRoutes.`);
    }
    contract.recommendedRoutes.forEach((route, index) => {
      assertAutoWorkContractRoute(route, `${inputPath}.recommendedRoutes[${index}]`);
    });
  }
}
function assertAutoExecutionContract(contract, inputPath) {
  assertAllowedAutoKeys(contract, AUTO_EXECUTION_CONTRACT_KEYS, inputPath);
  assertAutoOptionalStringFields(contract, /* @__PURE__ */ new Set(["chainType", "action"]), inputPath);
  assertAutoOptionalStringArrayFields(
    contract,
    /* @__PURE__ */ new Set(["roleSequence", "readFirst", "implementation"]),
    inputPath
  );
  if (Object.hasOwn(contract, "chainType") && !DOVE_EXECUTION_CHAIN_TYPES.includes(contract.chainType)) {
    throw new Error(
      `run_dove_auto requires one of ${DOVE_EXECUTION_CHAIN_TYPES.join(", ")} at ${inputPath}.chainType.`
    );
  }
  if (Object.hasOwn(contract, "roleSequence")) {
    contract.roleSequence.forEach((role, index) => {
      if (!DOVE_PRIMARY_ROLE_IDS.includes(role)) {
        throw new Error(
          `run_dove_auto requires one of ${DOVE_PRIMARY_ROLE_IDS.join(", ")} at ${inputPath}.roleSequence[${index}].`
        );
      }
    });
  }
  if (Object.hasOwn(contract, "files")) {
    if (!Array.isArray(contract.files)) {
      throw new Error(`run_dove_auto requires an array at ${inputPath}.files.`);
    }
    contract.files.forEach((file, index) => {
      const filePath = `${inputPath}.files[${index}]`;
      assertAllowedAutoKeys(file, AUTO_EXECUTION_FILE_KEYS, filePath);
      assertAutoOptionalStringFields(file, AUTO_EXECUTION_FILE_KEYS, filePath);
    });
  }
  if (Object.hasOwn(contract, "materials")) {
    assertAllowedAutoKeys(contract.materials, AUTO_EXECUTION_MATERIALS_KEYS, `${inputPath}.materials`);
    assertAutoOptionalStringArrayFields(
      contract.materials,
      AUTO_EXECUTION_MATERIALS_KEYS,
      `${inputPath}.materials`
    );
  }
  if (Object.hasOwn(contract, "convergence")) {
    assertAllowedAutoKeys(contract.convergence, AUTO_EXECUTION_CONVERGENCE_KEYS, `${inputPath}.convergence`);
    assertAutoOptionalStringArrayFields(
      contract.convergence,
      /* @__PURE__ */ new Set(["criteria", "verificationCommands", "evidenceRequired"]),
      `${inputPath}.convergence`
    );
    if (Object.hasOwn(contract.convergence, "definitionOfDone")) {
      assertAutoString(
        contract.convergence.definitionOfDone,
        `${inputPath}.convergence.definitionOfDone`
      );
    }
  }
  if (Object.hasOwn(contract, "failureRoutes")) {
    if (!Array.isArray(contract.failureRoutes)) {
      throw new Error(`run_dove_auto requires an array at ${inputPath}.failureRoutes.`);
    }
    contract.failureRoutes.forEach((route, index) => {
      assertAutoFailureRoute(route, `${inputPath}.failureRoutes[${index}]`);
    });
  }
}
function assertAutoChecklistItem(item, inputPath) {
  assertAllowedAutoKeys(item, AUTO_CHECKLIST_ITEM_KEYS, inputPath);
  assertAutoOptionalStringFields(item, AUTO_CHECKLIST_ITEM_STRING_KEYS, inputPath);
  assertAutoOptionalStringArrayFields(item, AUTO_CHECKLIST_ITEM_STRING_ARRAY_KEYS, inputPath);
  if (Object.hasOwn(item, "level") && typeof item.level !== "number") {
    throw new Error(`run_dove_auto requires a number at ${inputPath}.level.`);
  }
  if (Object.hasOwn(item, "workContract")) {
    assertAutoWorkContract(item.workContract, `${inputPath}.workContract`);
  }
  if (Object.hasOwn(item, "executionContract")) {
    assertAutoExecutionContract(item.executionContract, `${inputPath}.executionContract`);
  }
}
function assertAutoNestedContracts(args = {}) {
  if (Object.hasOwn(args, "workContract")) {
    assertAutoWorkContract(args.workContract, "$.workContract");
  }
  if (Object.hasOwn(args, "executionContract")) {
    assertAutoExecutionContract(args.executionContract, "$.executionContract");
  }
  if (Object.hasOwn(args, "checklistItems")) {
    if (!Array.isArray(args.checklistItems)) {
      throw new Error("run_dove_auto requires an array at $.checklistItems.");
    }
    args.checklistItems.forEach((item, index) => {
      assertAutoChecklistItem(item, `$.checklistItems[${index}]`);
    });
  }
}
function assertAutoReviewLoopDraft(draft, inputPath) {
  assertAllowedAutoKeys(draft, AUTO_REVIEW_LOOP_DRAFT_KEYS, inputPath);
  assertAutoOptionalStringFields(draft, AUTO_REVIEW_LOOP_DRAFT_KEYS, inputPath);
}
function assertAutoReviewLoopExperience(experience, inputPath) {
  assertAllowedAutoKeys(experience, AUTO_REVIEW_LOOP_EXPERIENCE_KEYS, inputPath);
  assertAutoOptionalStringFields(experience, AUTO_REVIEW_LOOP_EXPERIENCE_KEYS, inputPath);
}
function assertAutoRebuttalIssue(issue, inputPath) {
  assertAllowedAutoKeys(issue, AUTO_REBUTTAL_ISSUE_KEYS, inputPath);
  assertAutoOptionalStringFields(issue, AUTO_REBUTTAL_ISSUE_STRING_KEYS, inputPath);
  assertAutoOptionalStringArrayFields(issue, /* @__PURE__ */ new Set(["evidenceLinks", "claimIds", "experimentIds"]), inputPath);
}
function assertAllowedAutoNestedArgs(command, stepArgs, stepPath) {
  const argsPath = `${stepPath}.args`;
  if (command === "dove.source") {
    assertAutoSourceItem(Object.fromEntries(Object.entries(stepArgs).filter(([key]) => key !== "sources")), argsPath);
    if (Object.hasOwn(stepArgs, "sources")) {
      if (!Array.isArray(stepArgs.sources)) {
        throw new Error(`run_dove_auto requires an array at ${argsPath}.sources.`);
      }
      stepArgs.sources.forEach((source, sourceIndex) => {
        assertAutoSourceItem(source, `${argsPath}.sources[${sourceIndex}]`);
      });
    }
  }
  if (command === "dove.experience") {
    if (Object.hasOwn(stepArgs, "plan")) {
      assertAutoExperiencePlan(stepArgs.plan, `${argsPath}.plan`);
    }
    if (Object.hasOwn(stepArgs, "result")) {
      assertAutoExperienceResult(stepArgs.result, `${argsPath}.result`);
    }
  }
  if (command === "dove.figure") {
    if (Object.hasOwn(stepArgs, "materialRequirements")) {
      if (!Array.isArray(stepArgs.materialRequirements)) {
        throw new Error(`run_dove_auto requires an array at ${argsPath}.materialRequirements.`);
      }
      stepArgs.materialRequirements.forEach((item, index) => assertAutoFigureMaterialItem(item, `${argsPath}.materialRequirements[${index}]`));
    }
    if (Object.hasOwn(stepArgs, "materialHints")) {
      if (!Array.isArray(stepArgs.materialHints)) {
        throw new Error(`run_dove_auto requires an array at ${argsPath}.materialHints.`);
      }
      stepArgs.materialHints.forEach((item, index) => {
        if (typeof item !== "string") {
          assertAutoFigureMaterialItem(item, `${argsPath}.materialHints[${index}]`);
        }
      });
    }
    if (Object.hasOwn(stepArgs, "semanticCoverage")) {
      assertAutoFigureSemanticObservation(
        stepArgs.semanticCoverage,
        AUTO_FIGURE_SEMANTIC_COVERAGE_KEYS,
        `${argsPath}.semanticCoverage`
      );
    }
    if (Object.hasOwn(stepArgs, "semanticReview")) {
      assertAutoFigureSemanticObservation(
        stepArgs.semanticReview,
        AUTO_FIGURE_SEMANTIC_REVIEW_KEYS,
        `${argsPath}.semanticReview`
      );
    }
  }
  if (command === "dove.review-loop") {
    if (Object.hasOwn(stepArgs, "draft")) {
      assertAutoReviewLoopDraft(stepArgs.draft, `${argsPath}.draft`);
    }
    if (Object.hasOwn(stepArgs, "experience")) {
      assertAutoReviewLoopExperience(stepArgs.experience, `${argsPath}.experience`);
    }
  }
  if (command === "dove.rebuttal" && Object.hasOwn(stepArgs, "issues")) {
    if (!Array.isArray(stepArgs.issues)) {
      throw new Error(`run_dove_auto requires an array at ${argsPath}.issues.`);
    }
    stepArgs.issues.forEach((issue, index) => {
      assertAutoRebuttalIssue(issue, `${argsPath}.issues[${index}]`);
    });
  }
}
function normalizeExplicitAutoSteps(args = {}) {
  return explicitStepsFrom(args).map((step, index) => {
    const stepPath = `$.steps[${index}]`;
    assertAllowedAutoKeys(step, AUTO_STEP_WRAPPER_KEYS, stepPath);
    const command = normalizeAutoCommandId(step.command);
    if (!command) {
      throw new Error(`run_dove_auto requires a supported command at ${stepPath}.command.`);
    }
    const stepArgs = step.args ?? {};
    const allowedArgs = AUTO_STEP_ARGS_BY_COMMAND.get(command);
    assertAllowedAutoKeys(stepArgs, allowedArgs, `${stepPath}.args`);
    assertAllowedAutoNestedArgs(command, stepArgs, stepPath);
    if (Object.hasOwn(step, "failureRoutes") && !Array.isArray(step.failureRoutes)) {
      throw new Error(`run_dove_auto requires an array at ${stepPath}.failureRoutes.`);
    }
    const failureRoutes = objectArray(step.failureRoutes);
    failureRoutes.forEach((route, routeIndex) => {
      assertAutoFailureRoute(route, `${stepPath}.failureRoutes[${routeIndex}]`);
    });
    if (Object.hasOwn(step, "executionContract")) {
      assertAutoExecutionContract(step.executionContract, `${stepPath}.executionContract`);
    }
    const executionContract = normalizeDoveExecutionContract(step.executionContract, null);
    return {
      command,
      args: stepArgs,
      completeTask: step.completeTask === true,
      requiredMaterials: normalizeStringArray24(step.requiredMaterials),
      outputArtifacts: normalizeStringArray24(step.outputArtifacts),
      convergenceChecks: normalizeStringArray24(step.convergenceChecks ?? executionContract?.convergence?.criteria),
      failureRoutes,
      executionContract,
      validationEvidencePaths: normalizeStringArray24(step.validationEvidencePaths),
      verificationEvidencePaths: normalizeStringArray24(step.verificationEvidencePaths),
      verifiedCriteria: normalizeDoveVerifiedCriteria(step.verifiedCriteria)
    };
  });
}
function hasNonEmptyString4(value) {
  return typeof value === "string" && value.trim();
}
function hasSourceItemProvenance(args = {}) {
  return [args.title, args.locator].some(hasNonEmptyString4);
}
function hasAutoSourceProvenanceArgs(args = {}) {
  if (Array.isArray(args.sources)) {
    return args.sources.length > 0 && args.sources.every((item) => item && typeof item === "object" && !Array.isArray(item) && hasSourceItemProvenance(item));
  }
  return hasNonEmptyString4(args.locator) || hasNonEmptyString4(args.title) && [args.sourceId, args.citationKey].some(hasNonEmptyString4);
}
function hasAutoNoteSynthesisArgs(args = {}) {
  return hasNonEmptyString4(args.summary) || normalizeStringArray24(args.quotes).length > 0 || normalizeStringArray24(args.claims).length > 0 || normalizeStringArray24(args.openQuestions).length > 0;
}
function hasAutoDraftContentArgs(args = {}) {
  return hasNonEmptyString4(args.body);
}
function hasAutoExperienceObjectiveArgs(args = {}) {
  const plan = args.plan && typeof args.plan === "object" && !Array.isArray(args.plan) ? args.plan : args;
  return [plan.experimentId, plan.id, plan.goal, plan.idea, args.idea, plan.title].some(hasNonEmptyString4);
}
function autoStepHostPassReason(command, stepArgs = {}) {
  if (command === "dove.source" && !hasAutoSourceProvenanceArgs(stepArgs)) {
    return "source-requires-host-provenance";
  }
  if (command === "dove.note" && !hasAutoNoteSynthesisArgs(stepArgs)) {
    return "note-requires-host-synthesis";
  }
  if (command === "dove.draft" && !hasAutoDraftContentArgs(stepArgs)) {
    return "draft-requires-host-content";
  }
  if (command === "dove.experience" && !hasAutoExperienceObjectiveArgs(stepArgs)) {
    return "experience-requires-host-objective";
  }
  return null;
}
function autoPlanFromSteps(steps, whyThisStep) {
  const hostPassReason = steps.map((step) => autoStepHostPassReason(step.command, step.args)).find(Boolean);
  if (hostPassReason) {
    return {
      steps: [],
      proposedSteps: summarizeAutoSteps(steps),
      safeToRun: false,
      requiresHostPass: true,
      whyThisStep: hostPassReason
    };
  }
  return {
    steps,
    proposedSteps: summarizeAutoSteps(steps),
    safeToRun: true,
    requiresHostPass: false,
    whyThisStep
  };
}
function taskIntentText(task = {}, args = {}) {
  return [args.goal, args.objective, args.prompt, args.title, args.summary, task.title, task.summary, task.currentFocus, task.nextAction, task.stage, task.domain].map((value) => String(value ?? "").toLowerCase()).join(" ");
}
function autoHostPassRequiredActions(autoPlan = {}) {
  if (autoPlan.whyThisStep === "source-requires-host-provenance") {
    return ["collect-source-provenance", "call-register-source-with-sources-array", "provide-explicit-auto-step"];
  }
  if (autoPlan.whyThisStep === "note-requires-host-synthesis") {
    return ["synthesize-note-content", "call-upsert-note-with-summary-or-claims", "provide-explicit-auto-step"];
  }
  if (autoPlan.whyThisStep === "draft-requires-host-content") {
    return ["write-draft-body", "call-upsert-draft-with-body", "provide-explicit-auto-step"];
  }
  if (autoPlan.whyThisStep === "experience-requires-host-objective") {
    return ["define-experience-objective", "call-run-experience-workflow-with-goal-or-experimentId", "provide-explicit-auto-step"];
  }
  if (autoPlan.whyThisStep === "review-loop-requires-host-material") {
    return ["provide-review-loop-draft-or-experience-material", "provide-explicit-auto-step"];
  }
  return ["provide-host-pass-result", "provide-explicit-auto-step"];
}
function readOnlyAutoBlock(task, autoPlan = {}, args = {}, responseLanguage = "zh") {
  const steps = Array.isArray(autoPlan.steps) ? autoPlan.steps : [];
  if (steps.length === 0 || !steps.every((step) => AUTO_READ_ONLY_COMMANDS.has(step.command))) {
    return null;
  }
  const completionRequested = steps.some((step) => step.completeTask === true) || args.completeTask === true || args.completeOnSuccess === true;
  return {
    status: "needs-explicit-progress-step",
    outcome: "auto-read-only-step-no-progress",
    requestedStatus: completionRequested ? "completed" : null,
    packetId: task.id,
    title: task.title,
    proposedSteps: summarizeAutoSteps(steps),
    safeToRun: false,
    requiresHostPass: false,
    requiredActions: ["provide-explicit-auto-step", "use-project-dove-status-for-status-queries"],
    message: responseLanguage === "en" ? "Read-only Dove auto steps can inspect context, but they cannot complete or advance a durable task. Provide an explicit write/generation/review step with evidence." : "\u53EA\u8BFB Dove auto \u6B65\u9AA4\u53EA\u80FD\u67E5\u770B\u4E0A\u4E0B\u6587\uFF0C\u4E0D\u80FD\u5B8C\u6210\u6216\u63A8\u8FDB durable task\u3002\u8BF7\u63D0\u4F9B\u5E26\u8BC1\u636E\u7684\u5199\u5165\u3001\u751F\u6210\u6216 review \u6B65\u9AA4\u3002",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    responseLanguage,
    nextAction: "project:dove.auto"
  };
}
function summarizeAutoSteps(steps = []) {
  return steps.map((step, index) => {
    const summary = {
      index: index + 1,
      command: step.command,
      completeTask: step.completeTask === true
    };
    if (normalizeStringArray24(step.requiredMaterials).length > 0) {
      summary.requiredMaterials = normalizeStringArray24(step.requiredMaterials);
    }
    if (normalizeStringArray24(step.outputArtifacts).length > 0) {
      summary.outputArtifacts = normalizeStringArray24(step.outputArtifacts);
    }
    if (normalizeStringArray24(step.convergenceChecks).length > 0) {
      summary.convergenceChecks = normalizeStringArray24(step.convergenceChecks);
    }
    if (objectArray(step.failureRoutes).length > 0) {
      summary.failureRoutes = objectArray(step.failureRoutes);
    }
    if (step.executionContract) {
      summary.executionContract = step.executionContract;
    }
    if (normalizeStringArray24(step.validationEvidencePaths).length > 0) {
      summary.validationEvidencePaths = normalizeStringArray24(step.validationEvidencePaths);
    }
    if (normalizeStringArray24(step.verificationEvidencePaths).length > 0) {
      summary.verificationEvidencePaths = normalizeStringArray24(step.verificationEvidencePaths);
    }
    const verifiedCriteria = normalizeDoveVerifiedCriteria(step.verifiedCriteria);
    if (verifiedCriteria.length > 0) {
      summary.verifiedCriteria = verifiedCriteria;
    }
    return summary;
  });
}
function inferAutoStepsForTask(task = {}, args = {}) {
  const explicitSteps = normalizeExplicitAutoSteps(args);
  if (explicitSteps.length > 0) {
    return autoPlanFromSteps(explicitSteps, "explicit-auto-steps");
  }
  const taskCommand = normalizeConcreteAutoCommandId(task.nextAction);
  if (taskCommand) {
    const steps = [{ command: taskCommand, args: {}, completeTask: args.completeTask === true }];
    return autoPlanFromSteps(steps, `task-next-action:${taskCommand}`);
  }
  const text3 = taskIntentText(task, args);
  const signals = workflowSignals(text3);
  const inferredCommand = workflowCommandFromSignals({
    ...signals,
    experiment: task.domain === "experiment" || signals.experiment
  });
  if (inferredCommand) {
    const steps = [{ command: inferredCommand, args: {}, completeTask: args.completeTask === true }];
    return autoPlanFromSteps(steps, `inferred-safe-workflow:${inferredCommand}`);
  }
  const continuationRoute = projectContinuationRoute(task.nextAction);
  return {
    steps: [],
    proposedSteps: [],
    safeToRun: false,
    requiresHostPass: true,
    whyThisStep: continuationRoute ? `project-continuation-requires-status-triage:${continuationRoute}` : "requires-host-pass-or-explicit-workflow-step"
  };
}
function stepArgsForTask(task, rawStepArgs = {}) {
  const stepArgs = rawStepArgs && typeof rawStepArgs === "object" && !Array.isArray(rawStepArgs) ? rawStepArgs : {};
  return {
    ...stepArgs,
    packetId: task.id
  };
}
function executeAutoStep(root, command, stepArgs) {
  switch (command) {
    case "dove.source":
      return registerSource(root, stepArgs);
    case "dove.note":
      return upsertNote(root, stepArgs);
    case "dove.experience":
      return runExperienceWorkflow(root, stepArgs);
    case "dove.figure":
      return runFigureWorkflow(root, stepArgs);
    case "dove.draft":
      return upsertDraft(root, stepArgs);
    case "dove.review":
      return runReviewLoop(root, stepArgs);
    case "dove.review-loop":
      return runDoveReviewLoop(root, stepArgs);
    case "dove.rebuttal": {
      const issues = Array.isArray(stepArgs.issues) ? normalizeRebuttalIssues(root, stepArgs) : null;
      const strategy = buildRebuttalStrategy(root, stepArgs);
      const draft = buildRebuttal(root, stepArgs);
      return { status: "drafted", issues, strategy, draft };
    }
    case "dove.lessons":
      return { status: "queried", lessons: activeLessons(root, stepArgs.packetId) };
    case "dove.status":
      return { status: "queried", packetId: stepArgs.packetId, taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex };
    default:
      throw new Error(`Unsupported /dove:auto internal command: ${command}`);
  }
}
function summarizeStepOutput(output) {
  if (!output || typeof output !== "object" || Array.isArray(output)) {
    return { status: output == null ? null : String(output) };
  }
  const summary = {};
  for (const key of ["status", "packetId", "runId", "figureId", "finalSvgPath", "qaPath", "draftPath", "sectionId", "reviewStatePath", "reviewLogPath", "nextAction"]) {
    if (output[key] !== void 0) {
      summary[key] = output[key];
    }
  }
  if (Array.isArray(output.artifacts)) {
    summary.artifacts = output.artifacts;
  }
  if (Array.isArray(output.missingRequirementIds)) {
    summary.missingRequirementIds = output.missingRequirementIds;
  }
  if (output.boundary?.type) {
    summary.boundaryType = output.boundary.type;
  }
  const executionReceipt = normalizeDoveExecutionReceipt(output.executionReceipt, null);
  if (executionReceipt) {
    summary.executionReceipt = executionReceipt;
  }
  if (output.importArgs) {
    summary.importArgs = output.importArgs;
  }
  return summary;
}
function autoStepArtifactRefs(command, output = {}) {
  const refs = [];
  for (const key of ["finalSvgPath", "qaPath", "draftPath", "reviewStatePath", "reviewLogPath", "planPath", "outlinePath", "taskIndexPath"]) {
    if (typeof output?.[key] === "string" && output[key].trim()) {
      refs.push(output[key]);
    }
  }
  if (Array.isArray(output?.artifacts)) {
    refs.push(...output.artifacts);
  }
  refs.push(...normalizeStringArray24(output?.reviewedArtifactPaths ?? output?.reviewedArtifactSet));
  if (command === "dove.source") refs.push(ARTIFACT_PATHS.sources, ARTIFACT_PATHS.citationLog);
  if (command === "dove.note") refs.push(ARTIFACT_PATHS.notes);
  if (command === "dove.experience") refs.push(ARTIFACT_PATHS.experimentPlans, ARTIFACT_PATHS.experimentResults, ARTIFACT_PATHS.experimentAudits);
  if (command === "dove.figure") refs.push(ARTIFACT_PATHS.figureQa);
  if (command === "dove.review" || command === "dove.review-loop") {
    refs.push(ARTIFACT_PATHS.reviewState, ARTIFACT_PATHS.reviewLog);
    return normalizeStringArray24(refs);
  }
  return normalizeStringArray24(refs).filter((item) => !isBookkeepingArtifactPath(item));
}
function authoritativeIndependentReviewProof(proof) {
  return Boolean(proof?.verified === true && proof?.authoritative === true);
}
function proofReviewedArtifactPaths(proof = {}) {
  return normalizeStringArray24([
    ...normalizeStringArray24(proof.reviewedArtifactPaths),
    ...Array.isArray(proof.reviewedArtifacts) ? proof.reviewedArtifacts.map((artifact) => typeof artifact === "string" ? artifact : artifact?.path) : []
  ]);
}
function reviewProofCoversFinalSvg(proof, finalSvgPath, finalSha256) {
  if (!authoritativeIndependentReviewProof(proof) || typeof finalSvgPath !== "string" || !finalSvgPath.trim()) {
    return false;
  }
  const reviewedArtifact = Array.isArray(proof.reviewedArtifacts) ? proof.reviewedArtifacts.find((artifact) => artifact?.path === finalSvgPath) : null;
  if (reviewedArtifact) {
    return typeof finalSha256 === "string" && finalSha256.length > 0 && reviewedArtifact.sha256 === finalSha256;
  }
  return proofReviewedArtifactPaths(proof).includes(finalSvgPath) && typeof finalSha256 === "string" && finalSha256.length > 0 && proof.finalSha256 === finalSha256;
}
function reviewProofFromAutoOutput(command, output = {}) {
  if (command === "dove.review-loop") {
    return output?.review?.independentReviewProof ?? null;
  }
  return output?.independentReviewProof ?? null;
}
function autoCompletionPacketGate(task, command, output, classified) {
  const intendedCommand = normalizeConcreteAutoCommandId(task.nextAction);
  const signals = workflowSignals(taskIntentText(task));
  const figurePacket = intendedCommand === "dove.figure" || !intendedCommand && task.stage !== "audit" && signals.figure;
  const reviewPacket = !figurePacket && (task.stage === "audit" || ["dove.review", "dove.review-loop"].includes(intendedCommand) || signals.review || signals.reviewLoop);
  if (reviewPacket) {
    const proof = reviewProofFromAutoOutput(command, output);
    const validReviewResult = ["dove.review", "dove.review-loop"].includes(command) && classified.canCompleteTask === true && authoritativeIndependentReviewProof(proof);
    if (!validReviewResult) {
      return {
        status: "verification-failed",
        boundaryType: "verification-failed",
        message: "auto-review-packet-requires-current-authoritative-review-result",
        requiredActions: ["run-dove-review-or-review-loop", "provide-current-authoritative-reviewer-proof"]
      };
    }
  }
  if (figurePacket) {
    const finalSvgPath = output?.imported?.finalSvgPath ?? output?.finalSvgPath ?? null;
    const proof = output?.imported?.independentReviewProof ?? null;
    const validFigureResult = command === "dove.figure" && output?.status === "validated" && classified.canCompleteTask === true && reviewProofCoversFinalSvg(proof, finalSvgPath, output?.imported?.finalSha256 ?? output?.finalSha256);
    if (!validFigureResult) {
      return {
        status: "verification-failed",
        boundaryType: "verification-failed",
        message: "auto-figure-packet-requires-current-validated-final-svg-proof",
        requiredActions: ["run-dove-figure", "validate-current-final-svg", "provide-current-authoritative-reviewer-proof"]
      };
    }
  }
  return null;
}
function completedAutoStep(command, output, outcomeStatus, options = {}) {
  const executionReceipt = normalizeDoveExecutionReceipt(output?.executionReceipt, null);
  const artifactRefs = normalizeStringArray24([
    ...autoStepArtifactRefs(command, output),
    ...normalizeStringArray24(executionReceipt?.artifactRefs),
    ...normalizeStringArray24(executionReceipt?.artifactPaths)
  ]);
  const evidenceLinks = normalizeStringArray24([
    ...normalizeStringArray24(output?.evidenceLinks ?? output?.evidencePaths),
    ...normalizeStringArray24(output?.reviewedArtifactPaths ?? output?.reviewedArtifactSet),
    ...normalizeStringArray24(executionReceipt?.evidenceLinks),
    ...normalizeStringArray24(executionReceipt?.evidencePaths)
  ]);
  const verificationEvidencePaths = normalizeStringArray24([
    ...normalizeStringArray24(output?.verificationEvidencePaths),
    ...normalizeStringArray24(executionReceipt?.verificationEvidencePaths),
    ...normalizeStringArray24(executionReceipt?.validationEvidencePaths)
  ]);
  const verifiedCriteria = normalizeDoveVerifiedCriteria([
    ...normalizeDoveVerifiedCriteria(output?.verifiedCriteria),
    ...normalizeDoveVerifiedCriteria(executionReceipt?.verifiedCriteria)
  ]);
  return {
    status: "step-completed",
    outcome: `${command}-${outcomeStatus}`,
    stopReason: null,
    terminal: false,
    canCompleteTask: options.canCompleteTask === true && (artifactRefs.length > 0 || evidenceLinks.length > 0 || verificationEvidencePaths.length > 0 || verifiedCriteria.length > 0),
    artifactRefs,
    evidenceLinks,
    verificationEvidencePaths,
    verifiedCriteria,
    executionReceipt
  };
}
function classifyAutoStepResult(command, output) {
  const status = typeof output?.status === "string" && output.status.trim() ? output.status.trim() : null;
  if (AUTO_READ_ONLY_COMMANDS.has(command)) {
    return { status: "step-no-progress", outcome: `${command}-read-only`, stopReason: "read-only-auto-step", terminal: true, canCompleteTask: false, artifactRefs: [], evidenceLinks: [] };
  }
  if (status === "prepared-awaiting-output") {
    return { status: "blocked-boundary", outcome: "awaiting-provider-output", stopReason: "awaiting-figure-provider-output", terminal: true, canCompleteTask: false, artifactRefs: [], evidenceLinks: [] };
  }
  if (status === "blocked-missing-materials") {
    return { status: "blocked-boundary", outcome: "missing-required-materials", stopReason: "missing-figure-materials", terminal: true, canCompleteTask: false, artifactRefs: [], evidenceLinks: [] };
  }
  if (status === "missing-secret-env") {
    return { status: "blocked-boundary", outcome: "missing-secret-env", stopReason: "figure-provider-missing-secret-env", terminal: true, canCompleteTask: false, artifactRefs: [], evidenceLinks: [] };
  }
  if (status === "provider-failed") {
    return { status: "blocked-boundary", outcome: "provider-failed", stopReason: "figure-provider-failed", terminal: true, canCompleteTask: false, artifactRefs: [], evidenceLinks: [] };
  }
  if (status === "blocked-boundary" && output?.boundary?.type) {
    return {
      status: "blocked-boundary",
      outcome: output.boundary.type,
      stopReason: output.stopReason ?? output.boundary.type,
      terminal: true,
      canCompleteTask: false,
      artifactRefs: [],
      evidenceLinks: [],
      requiredActions: normalizeStringArray24(output.requiredActions ?? output.boundary.requiredActions)
    };
  }
  if (command === "dove.source" && (status === "needs-source-verification" || output?.outcome === "source-provenance-unverified")) {
    return {
      status: "blocked-boundary",
      outcome: output?.boundary?.type ?? "host-tool-blocked",
      stopReason: output?.stopReason ?? "source-provenance-unverified",
      terminal: true,
      canCompleteTask: false,
      artifactRefs: [],
      evidenceLinks: [],
      requiredActions: normalizeStringArray24(output?.requiredActions ?? output?.boundary?.requiredActions)
    };
  }
  if (status === "qa-needs-attention" || status === "blocked" || status === "needs-review" || status === "max-iterations-exhausted" || status?.startsWith("blocked-")) {
    return { status: "blocked-boundary", outcome: status, stopReason: `${command}-${status}`, terminal: true, canCompleteTask: false, artifactRefs: [], evidenceLinks: [] };
  }
  if (command === "dove.source" && (status === "registered" || Array.isArray(output?.sourceIds) || output?.id)) {
    return completedAutoStep(command, output, status ?? "registered", { canCompleteTask: false });
  }
  if (command === "dove.note" && (output?.id || output?.noteId)) {
    return completedAutoStep(command, output, status ?? "recorded", { canCompleteTask: true });
  }
  if (command === "dove.draft" && output?.draftPath) {
    return completedAutoStep(command, output, status ?? "drafted", { canCompleteTask: true });
  }
  if (command === "dove.experience" && ["planned", "recorded", "bridged"].includes(status)) {
    return completedAutoStep(command, output, status, { canCompleteTask: status === "bridged" });
  }
  if (command === "dove.figure" && status === "validated" && output?.imported?.finalSvgPath && authoritativeIndependentReviewProof(output.imported.independentReviewProof)) {
    return completedAutoStep(command, output, status, { canCompleteTask: true });
  }
  if (command === "dove.review" && output?.verdict === "coherent" && authoritativeIndependentReviewProof(output?.independentReviewProof)) {
    return completedAutoStep(command, output, "coherent", { canCompleteTask: true });
  }
  if (command === "dove.review" && ["needs-evidence", "needs-revision"].includes(output?.verdict)) {
    return {
      status: "blocked-boundary",
      outcome: output.verdict,
      stopReason: `dove.review-${output.verdict}`,
      terminal: true,
      canCompleteTask: false,
      artifactRefs: autoStepArtifactRefs(command, output),
      evidenceLinks: [],
      requiredActions: normalizeStringArray24(output.actionItems)
    };
  }
  if (command === "dove.review-loop" && status === "coherent" && authoritativeIndependentReviewProof(output?.review?.independentReviewProof)) {
    return completedAutoStep(command, output, status, { canCompleteTask: true });
  }
  if (command === "dove.rebuttal" && status === "drafted") {
    return completedAutoStep(command, output, status, { canCompleteTask: true });
  }
  return {
    status: "blocked-boundary",
    outcome: "unexpected-step-status",
    stopReason: `${command}-unexpected-status:${status ?? "missing"}`,
    terminal: true,
    canCompleteTask: false,
    artifactRefs: [],
    evidenceLinks: []
  };
}
function loadFullTask(root, task) {
  const catalog = readTaskPacketCatalog(root);
  return catalog.byId.get(task.id) ?? task;
}
function assertChecklistCompletionReady(root, task, status, responseLanguage = "zh") {
  const block = checklistCompletionBlock(root, task, status, responseLanguage);
  if (block) {
    const error = new Error(block.message);
    error.code = block.status;
    error.openChecklistChildIds = block.openChecklistChildIds;
    throw error;
  }
}
var updateTaskLifecycle = (root, task, status, fields = {}) => updatePacketLifecycle(root, task, status, fields, {
  assertCompletionReady: assertChecklistCompletionReady
});
function applyPacketStepResult2(root, task, options = {}) {
  const command = normalizeAutoCommandId(options.command);
  return applyPacketStepResult(root, task, {
    ...options,
    command,
    classified: options.classified ?? classifyAutoStepResult(command, plainObject3(options.output))
  });
}
function persistAutoResult(root, result) {
  const artifacts = loadRuntimeArtifacts(root);
  const entry = {
    ...result,
    runId: result.runId ?? result.id,
    recordedAt: result.recordedAt ?? result.updatedAt ?? nowIso()
  };
  appendResult(artifacts, entry);
  saveRuntimeArtifacts(root, artifacts);
}
function terminalAutoTaskStatus(status) {
  return status === "completed" || status === "killed" || status === "blocked";
}
function taskSelectionChoices(candidates = []) {
  return candidates.map((task, itemIndex) => ({
    index: itemIndex + 1,
    id: task.id ?? task.packetId,
    packetId: task.packetId ?? task.id,
    title: task.title,
    status: task.status,
    level: task.level,
    score: task.score,
    matchedBy: task.matchedBy
  }));
}
var AUTO_PROPOSAL_VERSION = 1;
var AUTO_PROPOSAL_KINDS = /* @__PURE__ */ new Set(["selection", "demand"]);
var AUTO_REPLAY_CONTROL_FIELDS = /* @__PURE__ */ new Set([
  "confirm",
  "confirmed",
  "index",
  "runId",
  "missionPacketId",
  "packetId",
  "packetTarget",
  "proposalDigest",
  "proposalKind",
  "proposalVersion",
  "proposalWorkspace",
  "target",
  "taskId",
  "taskName",
  "taskPacketId"
]);
function autoReplayFields(args = {}) {
  return Object.fromEntries(
    Object.entries(args).filter(([key]) => !AUTO_REPLAY_CONTROL_FIELDS.has(key) && key !== "mutationMode")
  );
}
function autoProposalEnvelope(root, proposalKind, task, args = {}, contract = null) {
  return {
    version: AUTO_PROPOSAL_VERSION,
    action: "run-dove-auto",
    proposalKind,
    workspace: canonicalMissionWorkspace(root),
    mutationMode: missionProposalMutationMode(root, args),
    task: stableMissionPacket(task),
    contract: contract ? {
      initMaterializationRequired: contract.proposedInit !== null,
      proposedInit: stableMissionPacket(contract.proposedInit),
      proposedTask: stableMissionPacket(contract.packet),
      checklistTasks: contract.checklistTasks.map(stableMissionPacket)
    } : null
  };
}
function autoProposalDigest(root, proposalKind, task, args = {}, replayFields = autoReplayFields(args), contract = null) {
  return crypto13.createHash("sha256").update(JSON.stringify(stableContractValue({
    envelope: autoProposalEnvelope(root, proposalKind, task, args, contract),
    replayFields
  }))).digest("hex");
}
function autoConfirmArgs(root, proposalKind, task, args, maxIterations, mutationMode, values = {}, contract = null) {
  const returnedFields = {
    ...autoReplayFields(args),
    ...values,
    maxIterations
  };
  const replayFields = autoReplayFields(returnedFields);
  return {
    confirmed: true,
    mutationMode,
    proposalVersion: AUTO_PROPOSAL_VERSION,
    proposalWorkspace: canonicalMissionWorkspace(root),
    proposalKind,
    ...returnedFields,
    proposalDigest: autoProposalDigest(
      root,
      proposalKind,
      task,
      { mutationMode },
      replayFields,
      contract
    )
  };
}
function assertAutoReplayHeader(root, args = {}) {
  if (!currentMutationContext(root)) {
    throw new Error("Confirmed Dove auto execution requires an active MutationContext; direct core replay cannot write outside the selected mutation mode.");
  }
  if (args.proposalVersion !== AUTO_PROPOSAL_VERSION) {
    throw new Error("The selected local Dove auto proposal replay version is not supported. Request a fresh proposal.");
  }
  if (normalizeString10(args.proposalWorkspace, "") !== canonicalMissionWorkspace(root)) {
    throw new Error("The selected local Dove auto proposal replay belongs to a different canonical workspace. Request a fresh proposal.");
  }
  if (!AUTO_PROPOSAL_KINDS.has(args.proposalKind)) {
    throw new Error("Confirmed Dove auto execution requires the exact proposalKind returned by the selected local proposal replay data.");
  }
  if (!/^[0-9a-f]{64}$/u.test(normalizeString10(args.proposalDigest, ""))) {
    throw new Error("Confirmed Dove auto execution requires the exact proposalDigest returned by the selected local proposal replay data.");
  }
}
function autoSelectionConfirmation(root, args, selected, maxIterations, responseLanguage = "zh") {
  const task = loadFullTask(root, selected);
  const mutationMode = missionProposalMutationMode(root, args);
  const autoPlan = inferAutoStepsForTask(task, args);
  const preActionGuidance = preActionGuidanceForTask(root, "dove.auto", task, {
    request: requestTextFromArgs(args),
    roleId: "builder",
    nextAction: task.nextAction,
    workflowKind: "auto"
  }, responseLanguage);
  const classification = {
    stage: task.stage,
    domain: task.domain,
    rationale: [`stage=${task.stage}`, `domain=${task.domain}`, "selected-existing-task"]
  };
  return {
    status: "needs-confirmation",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    confirmationRequired: true,
    demandConversion: false,
    executionMode: "multi-round-foreground-auto",
    selectedTask: task,
    preActionGuidance,
    taskCard: buildTaskConfirmationCard(task, { firstAction: task.nextAction ?? nextCommandFor(classification), preActionGuidance }, responseLanguage),
    autoCard: buildAutoConfirmationCard(task, autoPlan, { maxIterations, preActionGuidance }, responseLanguage),
    classification,
    blockers: [...normalizeStringArray24(task.dependencies), ...normalizeStringArray24(task.blockedBy)],
    evidenceExpectations: normalizeStringArray24(task.evidenceExpectations),
    proposedNextCommand: normalizeString10(task.nextAction, nextCommandFor(classification)),
    proposedSteps: autoPlan.proposedSteps,
    safeToRun: autoPlan.safeToRun,
    requiresHostPass: autoPlan.requiresHostPass,
    whyThisStep: autoPlan.whyThisStep,
    proposalVersion: AUTO_PROPOSAL_VERSION,
    proposalWorkspace: canonicalMissionWorkspace(root),
    proposalKind: "selection",
    proposalMutationMode: mutationMode,
    proposalDigest: autoConfirmArgs(
      root,
      "selection",
      task,
      args,
      maxIterations,
      mutationMode,
      { packetId: task.id }
    ).proposalDigest,
    proposalTrust: {
      boundary: "trusted-local-exact-replay-data",
      proofOfHumanApproval: false,
      tamperProof: false
    },
    confirmArgs: autoConfirmArgs(
      root,
      "selection",
      task,
      args,
      maxIterations,
      mutationMode,
      { packetId: task.id }
    ),
    foreground: true,
    background: false,
    daemon: false,
    maxIterations,
    responseLanguage,
    message: doveText(responseLanguage, "autoSelectionConfirmMessage")
  };
}
function autoDemandConfirmation(root, args, maxIterations) {
  const contract = buildDoveTaskContract(root, args);
  const { packet, checklistProposal, classification, blockers, applicableLessons, responseLanguage, proposedInit, initMaterializationRequired } = contract;
  const mutationMode = missionProposalMutationMode(root, args);
  const autoPlan = inferAutoStepsForTask(packet, args);
  const contractReplayFields = missionContractReplayFields(root, contract, AUTO_PROPOSAL_VERSION);
  const confirmArgs = autoConfirmArgs(
    root,
    "demand",
    packet,
    args,
    maxIterations,
    mutationMode,
    contractReplayFields,
    contract
  );
  const preActionGuidance = preActionGuidanceForTask(root, "dove.auto", packet, {
    request: requestTextFromArgs(args),
    roleId: "builder",
    nextAction: packet.nextAction,
    workflowKind: "auto"
  }, responseLanguage);
  return {
    status: "needs-confirmation",
    proposalOnly: true,
    noAutoApply: true,
    writes: [],
    confirmationRequired: true,
    demandConversion: true,
    executionMode: "multi-round-foreground-auto",
    initMaterializationRequired,
    proposedInit,
    proposedTask: packet,
    preActionGuidance,
    taskCard: buildTaskConfirmationCard(packet, { firstAction: packet.nextAction, preActionGuidance }, responseLanguage),
    autoCard: buildAutoConfirmationCard(packet, autoPlan, { maxIterations, preActionGuidance }, responseLanguage),
    classification,
    blockers,
    evidenceExpectations: packet.evidenceExpectations,
    proposedNextCommand: packet.nextAction,
    proposedSteps: autoPlan.proposedSteps,
    safeToRun: autoPlan.safeToRun,
    requiresHostPass: autoPlan.requiresHostPass,
    whyThisStep: autoPlan.whyThisStep,
    checklistProposal,
    applicableLessons,
    proposalVersion: AUTO_PROPOSAL_VERSION,
    proposalWorkspace: canonicalMissionWorkspace(root),
    proposalKind: "demand",
    proposalMutationMode: mutationMode,
    proposalDigest: confirmArgs.proposalDigest,
    proposalTrust: {
      boundary: "trusted-local-exact-replay-data",
      proofOfHumanApproval: false,
      tamperProof: false
    },
    confirmArgs,
    foreground: true,
    background: false,
    daemon: false,
    maxIterations,
    taskIndexPath: ARTIFACT_PATHS.taskPacketsIndex,
    responseLanguage,
    message: doveText(responseLanguage, "autoDemandConfirmMessage")
  };
}
function runDoveAuto(root, args = {}) {
  assertGovernanceMutationRegistered("run-dove-auto", "guarded");
  assertNoRetiredAutoControls(args);
  assertAllowedAutoTopLevelArgs(args);
  assertAutoNestedContracts(args);
  normalizeExplicitAutoSteps(args);
  assertUnambiguousConfirmation(args);
  assertInitialDemandDoesNotSetGovernance(args, "run_dove_auto");
  assertCanonicalMissionGovernanceReplay(args, "run_dove_auto");
  const confirmed = hasExplicitConfirmation(args);
  const mutationMode = missionProposalMutationMode(root, args);
  if (confirmed) {
    assertAutoReplayHeader(root, args);
    assertReviewProofBoundaryTransition(
      root,
      loadBoard(root),
      "experiments",
      "builder",
      "Running Dove auto"
    );
  }
  const state = readStateForTaskContract(root);
  const responseLanguage = resolveDoveResponseLanguage(root, args, { state });
  const maxIterations = resolveAutoMaxIterations(state, args);
  if (!confirmed) {
    const index = loadTaskIndex(root);
    const selection = chooseTask(root, index, args);
    if (selection.selected && hasExplicitTaskSelector(args)) {
      return autoSelectionConfirmation(root, args, selection.selected, maxIterations, responseLanguage);
    }
    if (hasExplicitTaskSelector(args) && !selection.selected) {
      return {
        status: "needs-task-selection",
        choices: taskSelectionChoices(selection.candidates),
        responseLanguage,
        message: doveText(responseLanguage, "autoSelectExistingMessage")
      };
    }
    if (!hasTaskIntent(args)) {
      return {
        status: "needs-task-selection",
        choices: taskSelectionChoices(selection.candidates),
        responseLanguage,
        message: doveText(responseLanguage, "autoSelectOrDemandMessage")
      };
    }
    return autoDemandConfirmation(root, args, maxIterations);
  }
  let task;
  if (args.proposalKind === "selection") {
    const packetId = normalizeString10(args.packetId, "");
    if (!packetId) {
      throw new Error("Confirmed Dove auto selection replay requires the canonical packetId returned by the selected local proposal replay data.");
    }
    const catalog = readTaskPacketCatalog(root);
    task = catalog.byId.get(packetId) ?? null;
    if (!task || !activeStatus2(task.status)) {
      throw new Error(`The selected Dove auto task ${packetId} no longer exists or is not active. Request a fresh proposal.`);
    }
    const replayDigest = autoProposalDigest(
      root,
      "selection",
      task,
      { mutationMode },
      autoReplayFields(args)
    );
    if (normalizeString10(args.proposalDigest, "") !== replayDigest) {
      throw new Error("The selected local Dove auto proposal replay no longer matches the current task snapshot or exact replay fields. Request a fresh proposal.");
    }
  } else {
    const contract = buildDoveTaskContract(root, args);
    const replayDigest = autoProposalDigest(
      root,
      "demand",
      contract.packet,
      { mutationMode },
      autoReplayFields(args),
      contract
    );
    if (normalizeString10(args.proposalDigest, "") !== replayDigest) {
      throw new Error("The selected local Dove auto proposal replay no longer matches the current demand contract or exact replay fields. Request a fresh proposal.");
    }
    assertMissionReplayTargetsAvailable(root, contract);
    ensureWorkspace(root);
    task = materializeDoveTask(root, contract).createdTask;
  }
  ensureWorkspace(root);
  const timestamp = nowIso();
  const resultId = normalizeTaskPacketId(args.runId ?? `auto-${task.id}-${Date.now().toString(36)}`);
  const result = {
    id: resultId,
    packetId: task.id,
    status: "in-progress",
    outcome: "foreground-iterations-started",
    foreground: true,
    background: false,
    daemon: false,
    maxIterations,
    iterationCount: 0,
    allowedInternalCommands: AUTO_INTERNAL_COMMANDS,
    stopReason: null,
    iterations: [],
    taskStatusBefore: task.status,
    taskStatusAfter: task.status,
    responseLanguage,
    createdAt: timestamp,
    updatedAt: timestamp
  };
  const preActionGuidanceSummary = preActionGuidanceSummaryForTask(root, "dove.auto", task, {
    request: requestTextFromArgs(args),
    roleId: "builder",
    nextAction: task.nextAction,
    workflowKind: "auto"
  }, responseLanguage);
  if (task.status === "killed") {
    result.status = "stopped-killed";
    result.outcome = "task-already-killed";
    result.stopReason = "task-killed";
    persistAutoResult(root, result);
    return { status: result.status, task, result, resultCard: autoResultCard(task, result, { nextAction: task.nextAction, preActionGuidanceSummary }, responseLanguage), applicableLessons: activeLessons(root, task.id), responseLanguage, nextAction: task.nextAction };
  }
  if (task.status === "completed") {
    result.status = "completed";
    result.outcome = "task-already-completed";
    result.stopReason = "task-completed";
    persistAutoResult(root, result);
    return { status: result.status, task, result, resultCard: autoResultCard(task, result, { nextAction: task.nextAction, preActionGuidanceSummary }, responseLanguage), applicableLessons: activeLessons(root, task.id), responseLanguage, nextAction: task.nextAction };
  }
  if (task.status === "blocked" && !["awaiting-host-pass", "awaiting-host-pass-result"].includes(task.boundary?.type)) {
    result.status = "blocked-boundary";
    result.outcome = task.boundary?.type ?? "task-already-blocked";
    result.stopReason = task.boundary?.reason ?? "task-blocked";
    result.boundary = task.boundary ?? null;
    persistAutoResult(root, result);
    return { status: result.status, task, result, resultCard: autoResultCard(task, result, { nextAction: task.nextAction, preActionGuidanceSummary }, responseLanguage), applicableLessons: activeLessons(root, task.id), responseLanguage, nextAction: task.nextAction };
  }
  const autoPlan = inferAutoStepsForTask(task, args);
  const steps = autoPlan.steps;
  result.proposedSteps = autoPlan.proposedSteps;
  result.safeToRun = autoPlan.safeToRun;
  result.requiresHostPass = autoPlan.requiresHostPass;
  result.whyThisStep = autoPlan.whyThisStep;
  const readOnlyBlock = readOnlyAutoBlock(task, autoPlan, args, responseLanguage);
  if (readOnlyBlock) {
    return {
      ...readOnlyBlock,
      result: {
        ...result,
        status: readOnlyBlock.status,
        outcome: readOnlyBlock.outcome,
        stopReason: "read-only-auto-step",
        safeToRun: false,
        iterationCount: 0,
        taskStatusAfter: task.status,
        updatedAt: nowIso()
      },
      task,
      applicableLessons: activeLessons(root, task.id)
    };
  }
  if (steps.length === 0) {
    const completedAt = nowIso();
    result.iterations.push({
      iteration: 1,
      command: null,
      status: "awaiting-host-pass",
      outcome: "host-pass-required",
      stopReason: autoPlan.whyThisStep,
      startedAt: timestamp,
      completedAt
    });
    result.status = "awaiting-host-pass";
    result.outcome = "host-pass-required";
    result.stopReason = autoPlan.whyThisStep;
    task = updateTaskLifecycle(root, task, "blocked", {
      runId: resultId,
      surface: "dove.auto",
      command: "run_dove_auto",
      boundaryType: "awaiting-host-pass",
      reason: result.stopReason,
      stopReason: result.stopReason,
      summary: result.outcome,
      requiredActions: autoHostPassRequiredActions(autoPlan),
      nextAction: task.nextAction
    });
    result.boundary = task.boundary;
    result.iterationCount = result.iterations.length;
    result.taskStatusAfter = task.status;
    result.updatedAt = completedAt;
    persistAutoResult(root, result);
    return {
      status: result.status,
      task,
      result,
      resultCard: autoResultCard(task, result, { nextAction: task.nextAction, preActionGuidanceSummary }, responseLanguage),
      boundary: task.boundary,
      applicableLessons: activeLessons(root, task.id),
      responseLanguage,
      proposedSteps: autoPlan.proposedSteps,
      safeToRun: autoPlan.safeToRun,
      requiresHostPass: autoPlan.requiresHostPass,
      whyThisStep: autoPlan.whyThisStep,
      nextAction: task.nextAction
    };
  }
  task = updateTaskLifecycle(root, task, "in-progress", { runId: resultId, surface: "dove.auto", command: "run_dove_auto" });
  let finalTaskStatus = "in-progress";
  for (let index = 0; index < maxIterations; index += 1) {
    task = loadFullTask(root, task);
    if (terminalAutoTaskStatus(task.status)) {
      result.status = task.status === "completed" ? "completed" : task.status === "killed" ? "stopped-killed" : "blocked";
      result.outcome = `task-${task.status}`;
      result.stopReason = `task-${task.status}`;
      finalTaskStatus = task.status;
      break;
    }
    const iterationNumber = index + 1;
    const startedAt = nowIso();
    const step = steps[index] ?? null;
    if (!step) {
      result.status = "foreground-pass-complete";
      result.outcome = "approved-steps-exhausted";
      result.stopReason = "approved-steps-exhausted";
      finalTaskStatus = task.status;
      break;
    }
    try {
      const taskBeforeStep = task;
      const output = executeAutoStep(root, step.command, stepArgsForTask(task, step.args));
      const classified = classifyAutoStepResult(step.command, output);
      const executionReceipt = normalizeDoveExecutionReceipt(classified.executionReceipt, null);
      const completedAt = nowIso();
      result.iterations.push({
        iteration: iterationNumber,
        command: step.command,
        status: classified.status,
        outcome: classified.outcome,
        stopReason: classified.stopReason,
        output: summarizeStepOutput(output),
        artifactRefs: classified.artifactRefs,
        evidenceLinks: classified.evidenceLinks,
        verificationEvidencePaths: classified.verificationEvidencePaths,
        verifiedCriteria: classified.verifiedCriteria,
        executionReceipt,
        startedAt,
        completedAt
      });
      task = applyPacketStepResult2(root, task, {
        runId: resultId,
        surface: "dove.auto",
        command: step.command,
        output,
        classified
      });
      if (classified.terminal) {
        result.status = classified.status;
        result.outcome = classified.outcome;
        result.stopReason = classified.stopReason;
        result.boundary = task.boundary;
        finalTaskStatus = task.status;
        break;
      }
      if (step.completeTask === true || args.completeTask === true || args.completeOnSuccess === true) {
        const completionArgs = {
          runId: resultId,
          surface: "dove.auto",
          command: step.command,
          resultSummary: classified.outcome,
          summary: classified.outcome,
          artifactRefs: normalizeStringArray24([...normalizeStringArray24(task.artifactRefs), ...classified.artifactRefs ?? [], ...normalizeStringArray24(step.outputArtifacts)]).filter((item) => !isBookkeepingArtifactPath(item)),
          evidenceLinks: normalizeStringArray24([
            ...normalizeStringArray24(task.evidenceLinks),
            ...classified.evidenceLinks ?? [],
            ...normalizeStringArray24(step.outputArtifacts),
            ...normalizeStringArray24(step.validationEvidencePaths),
            ...normalizeStringArray24(step.verificationEvidencePaths)
          ]).filter((item) => !isBookkeepingArtifactPath(item)),
          validationEvidencePaths: normalizeStringArray24(step.validationEvidencePaths),
          verificationEvidencePaths: normalizeStringArray24([...classified.verificationEvidencePaths ?? [], ...normalizeStringArray24(step.verificationEvidencePaths)]),
          verifiedCriteria: normalizeDoveVerifiedCriteria([...classified.verifiedCriteria ?? [], ...normalizeDoveVerifiedCriteria(step.verifiedCriteria)]),
          executionReceipt,
          executionContract: step.executionContract ?? task.executionContract
        };
        const packetGateBlock = autoCompletionPacketGate(taskBeforeStep, step.command, output, classified);
        const verificationBlock = packetGateBlock ?? completionVerificationBlock(root, task, completionArgs, responseLanguage);
        const completionEvidence = completionEvidenceForPayload(completionArgs);
        const canCompleteWithEvidence = classified.canCompleteTask === true || completionEvidence.evidencePaths.length > 0 || completionEvidence.verifiedCriteria.length > 0;
        if (!canCompleteWithEvidence || verificationBlock) {
          const block = verificationBlock ?? {
            status: "needs-completion-evidence",
            boundaryType: "missing-required-materials",
            message: "auto-step-did-not-produce-completion-evidence",
            requiredActions: ["provide-evidence-links-or-artifact-refs-or-verification-evidence"]
          };
          task = updateTaskLifecycle(root, task, "blocked", {
            ...completionArgs,
            boundaryType: normalizeDoveBoundaryType(block.boundaryType ?? block.status, "blocked-boundary"),
            reason: block.message,
            stopReason: block.status,
            summary: block.message,
            requiredActions: block.requiredActions ?? [],
            nextAction: "project:dove.status"
          });
          result.status = block.status;
          result.outcome = block.status;
          result.stopReason = block.message;
          result.boundary = task.boundary;
          finalTaskStatus = task.status;
          break;
        }
        task = updateTaskLifecycle(root, task, "completed", {
          ...completionArgs,
          artifactRefs: normalizeStringArray24(task.artifactRefs),
          evidenceLinks: normalizeStringArray24(task.evidenceLinks),
          reason: "completion-confirmed-by-auto-step",
          nextAction: "project:dove.status"
        });
        result.status = "completed";
        result.outcome = "task-completed";
        result.stopReason = "completion-confirmed-by-auto-step";
        finalTaskStatus = task.status;
        break;
      }
      if (iterationNumber === maxIterations) {
        const approvedStepsRemain = steps.length > maxIterations;
        result.status = approvedStepsRemain ? "step-budget-exhausted" : "foreground-pass-complete";
        result.outcome = approvedStepsRemain ? "step-budget-exhausted" : "approved-steps-exhausted";
        result.stopReason = result.outcome;
        finalTaskStatus = task.status;
      }
    } catch (error) {
      const completedAt = nowIso();
      result.iterations.push({
        iteration: iterationNumber,
        command: step.command,
        status: "blocked-boundary",
        outcome: "workflow-error-boundary",
        stopReason: error.message,
        startedAt,
        completedAt
      });
      result.status = "blocked-boundary";
      result.outcome = "workflow-error-boundary";
      result.stopReason = error.message;
      task = updateTaskLifecycle(root, task, "blocked", {
        runId: resultId,
        surface: "dove.auto",
        command: step.command,
        boundaryType: "workflow-error-boundary",
        reason: error.message,
        stopReason: error.message,
        summary: "workflow-error-boundary",
        nextAction: "project:dove.status"
      });
      result.boundary = task.boundary;
      finalTaskStatus = task.status;
      break;
    }
  }
  if (finalTaskStatus === "blocked" && task.status !== "blocked") {
    task = updateTaskLifecycle(root, task, "blocked", {
      runId: resultId,
      surface: "dove.auto",
      command: "run_dove_auto",
      boundaryType: normalizeDoveBoundaryType(result.outcome, "blocked-boundary"),
      reason: result.stopReason,
      stopReason: result.stopReason,
      summary: result.outcome,
      nextAction: "project:dove.status"
    });
    result.boundary = task.boundary;
    finalTaskStatus = task.status;
  }
  result.iterationCount = result.iterations.length;
  result.taskStatusAfter = finalTaskStatus;
  result.updatedAt = nowIso();
  persistAutoResult(root, result);
  return {
    status: result.status,
    task,
    result,
    resultCard: autoResultCard(task, result, { nextAction: task.nextAction, preActionGuidanceSummary }, responseLanguage),
    boundary: task.boundary ?? null,
    applicableLessons: activeLessons(root, task.id),
    responseLanguage,
    nextAction: task.nextAction
  };
}

// src/core/audio-review.mjs
import crypto14 from "node:crypto";
import fs24 from "node:fs";
import path27 from "node:path";
var REVIEW_VERDICTS3 = /* @__PURE__ */ new Set(["coherent", "needs-revision", "needs-evidence", "blocked"]);
var HANDOFF_STATUSES2 = /* @__PURE__ */ new Set(["completed", "blocked", "failed"]);
function slugify15(value) {
  return String(value ?? "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "audio-review";
}
function normalizeString11(value, fallback = "") {
  return typeof value === "string" && value.trim() ? value.trim() : fallback;
}
function normalizeStringArray25(value) {
  return Array.isArray(value) ? Array.from(new Set(value.map((item) => String(item).trim()).filter(Boolean))) : [];
}
function audioGuidanceSummary(root, args = {}, target = {}, details = {}) {
  return summarizePreActionGuidance(buildPreActionGuidance({
    surface: "dove.review",
    responseLanguage: resolveDoveResponseLanguage(root, args),
    request: args.instructions ?? args.scope ?? "audio review",
    roleId: "reviewer",
    packet: target.packet,
    currentContext: {
      domain: target.packet?.domain ?? null,
      stage: target.packet?.stage ?? "audit",
      primaryRole: "reviewer"
    },
    operatorLessons: readJson(root, ARTIFACT_PATHS.metaOperatorLessons, { lessons: [] }),
    nextAction: details.nextAction ?? "project:dove.review",
    routeHint: "project:dove.review",
    workflowKind: "audio-review",
    domain: target.packet?.domain ?? null,
    stage: target.packet?.stage ?? "audit",
    tags: ["review", "audio", "isolated-handoff"],
    statusSummary: details.statusSummary
  }));
}
function stableJson3(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function sha256Text2(value) {
  return crypto14.createHash("sha256").update(value).digest("hex");
}
function hashFile2(fullPath) {
  if (!fs24.existsSync(fullPath)) {
    return null;
  }
  return crypto14.createHash("sha256").update(fs24.readFileSync(fullPath)).digest("hex");
}
function relativeRunPath2(runId, leaf) {
  return path27.posix.join(ARTIFACT_PATHS.audioReviewsDir, runId, leaf);
}
function normalizeRunId2(value) {
  const candidate = slugify15(value ?? `audio-${(/* @__PURE__ */ new Date()).toISOString().replace(/[:.]/g, "-")}`);
  if (!/^[a-z0-9][a-z0-9-]{0,79}$/.test(candidate)) {
    throw new Error("audio review runId must be 1-80 lowercase letters, digits, or hyphens");
  }
  return candidate;
}
function safeArtifactPath2(root, relativePath) {
  const normalized = path27.posix.normalize(String(relativePath).replaceAll(path27.sep, "/"));
  if (normalized.startsWith("../") || normalized === ".." || path27.isAbsolute(normalized)) {
    throw new Error(`Refusing unsafe audio review artifact path: ${relativePath}`);
  }
  const fullPath = path27.resolve(root, normalized);
  const rootPath = path27.resolve(root);
  if (fullPath !== rootPath && !fullPath.startsWith(`${rootPath}${path27.sep}`)) {
    throw new Error(`Refusing audio review artifact outside workspace: ${relativePath}`);
  }
  return normalized;
}
function artifactEntry2(root, relativePath) {
  const safePath = safeArtifactPath2(root, relativePath);
  const inspection = inspectDeclaredPath(root, safePath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  return {
    path: safePath,
    exists: inspection.exists,
    usable: inspection.status === "existing",
    status: inspection.status,
    reason: inspection.reason ?? null,
    sizeBytes: inspection.sizeBytes ?? null,
    sha256: inspection.status === "existing" ? hashFile2(resolvePath(root, safePath)) : null
  };
}
function assertSubstantiveArtifactEntries2(artifacts, label) {
  const usable = artifacts.filter((item) => item.usable);
  if (usable.length === 0) {
    const reasons = artifacts.map((item) => `${item.path}:${item.reason ?? item.status}`).join(", ") || "no artifact paths provided";
    throw new Error(`${label} requires at least one existing non-empty non-bookkeeping reviewed artifact (${reasons}).`);
  }
  return usable;
}
function requiredStringField2(raw, field, label = field) {
  if (typeof raw[field] !== "string" || !raw[field].trim()) {
    throw new Error(`audio review handoff requires ${label}`);
  }
  return raw[field].trim();
}
function normalizeHandoff2(root, raw = {}) {
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    throw new Error("audio review handoff must be a JSON object");
  }
  const runId = normalizeRunId2(requiredStringField2(raw, "runId"));
  const verdict = requiredStringField2(raw, "verdict");
  if (!REVIEW_VERDICTS3.has(verdict)) {
    throw new Error(`audio review handoff has unsupported verdict: ${verdict}`);
  }
  const status = requiredStringField2(raw, "status");
  if (!HANDOFF_STATUSES2.has(status)) {
    throw new Error(`audio review handoff has unsupported status: ${status}`);
  }
  if (status !== "completed" && verdict === "coherent") {
    throw new Error(`audio review handoff status ${status} cannot import a coherent verdict.`);
  }
  const findings = Array.isArray(raw.findings) ? raw.findings.map((finding, index) => ({
    id: slugify15(finding.id ?? `${finding.severity ?? "medium"}-${finding.summary ?? index}`),
    severity: ["low", "medium", "high"].includes(finding.severity) ? finding.severity : "medium",
    summary: normalizeString11(finding.summary, `Audio review finding ${index + 1}`),
    linkedArtifactPaths: normalizeStringArray25(finding.linkedArtifactPaths).map((item) => safeArtifactPath2(root, item)),
    claimIds: normalizeStringArray25(finding.claimIds),
    experimentIds: normalizeStringArray25(finding.experimentIds)
  })) : [];
  const reviewedArtifactPaths = normalizeStringArray25(raw.reviewedArtifactPaths ?? raw.artifactPaths).map((item) => safeArtifactPath2(root, item));
  return {
    version: 1,
    runId,
    status,
    verdict,
    reviewerId: requiredStringField2(raw, "reviewerId"),
    timestamp: normalizeString11(raw.timestamp, nowIso()),
    summary: requiredStringField2(raw, "summary"),
    inputPath: safeArtifactPath2(root, requiredStringField2(raw, "inputPath")),
    inputSha256: requiredStringField2(raw, "inputSha256"),
    reportPath: safeArtifactPath2(root, requiredStringField2(raw, "reportPath")),
    reviewedArtifactPaths,
    findings,
    actionItems: normalizeStringArray25(raw.actionItems ?? findings.map((finding) => finding.summary)),
    privateTranscriptImported: false
  };
}
function audioReviewPreparedHandoffSuggestion(prepared = {}, responseLanguage = "zh") {
  const requiredInputs = normalizeStringArray25([prepared.inputPath, prepared.handoffPath, prepared.reportPath]);
  return {
    presentation: "dove-handoff-suggestion",
    boundaryType: "awaiting-review-output",
    ownerRole: "builder",
    nextRole: "reviewer",
    requiredInputs,
    requiredActions: ["complete-isolated-review-handoff"],
    requires: requiredInputs,
    detail: { implementationBoundaryType: "awaiting-audio-review-output" },
    summary: doveText(responseLanguage, "resultCardHandoffImportReview")
  };
}
function audioReviewImportedHandoffSuggestion(imported = {}, responseLanguage = "zh") {
  if (imported.reviewProofRequired) {
    const requiredActions2 = ["submit-authoritative-reviewer-runtime-proof"];
    return {
      presentation: "dove-handoff-suggestion",
      boundaryType: "review-proof-required",
      ownerRole: "reviewer",
      nextRole: "reviewer",
      requiredActions: requiredActions2,
      requires: requiredActions2,
      detail: {
        implementationBoundaryType: "audio-review-proof-required",
        reviewVerdict: imported.importedVerdict ?? imported.verdict ?? null,
        reviewSummary: imported.summary ?? null
      },
      summary: "Authoritative Reviewer proof is still required for the imported audio review."
    };
  }
  if (imported.verdict === "coherent") {
    return null;
  }
  const requiredActions = normalizeStringArray25(imported.actionItems).length > 0 ? normalizeStringArray25(imported.actionItems) : ["address-audio-review-findings"];
  const implementationBoundaryType = `audio-review-${imported.verdict ?? "needs-revision"}`;
  return {
    presentation: "dove-handoff-suggestion",
    boundaryType: "verification-failed",
    ownerRole: "builder",
    nextRole: "builder",
    requiredActions,
    requires: requiredActions,
    detail: {
      implementationBoundaryType,
      reviewVerdict: imported.verdict ?? null,
      reviewSummary: imported.summary ?? null
    },
    summary: doveText(responseLanguage, "resultCardHandoffAddressReview")
  };
}
function audioReviewPreparedCard(prepared = {}, responseLanguage = "zh") {
  const handoffSuggestion = audioReviewPreparedHandoffSuggestion(prepared, responseLanguage);
  return buildCommandResultCard({
    surface: "dove.review",
    command: "run_audio_review",
    packetId: prepared.packetId,
    runId: prepared.runId,
    status: prepared.status,
    outcome: "awaiting-review-output",
    summary: doveText(responseLanguage, "resultCardReviewInputPrepared"),
    evidenceLinks: [prepared.inputPath, prepared.handoffPath, prepared.reportPath, ...normalizeStringArray25(prepared.reviewedArtifactPaths)],
    durableWrites: [doveText(responseLanguage, "resultCardReviewInputPrepared"), prepared.inputPath, relativeRunPath2(prepared.runId, "manifest.json")],
    nextActions: [{
      title: doveText(responseLanguage, "resultCardNextImportReview"),
      command: "import_audio_review",
      packetId: prepared.packetId,
      boundaryType: handoffSuggestion.boundaryType,
      ownerRole: handoffSuggestion.ownerRole,
      nextRole: handoffSuggestion.nextRole,
      requiredInputs: handoffSuggestion.requiredInputs,
      requiredActions: handoffSuggestion.requiredActions,
      requires: handoffSuggestion.requires,
      handoffSuggestion,
      confirmationRequired: true
    }],
    preActionGuidanceSummary: prepared.preActionGuidanceSummary ?? null,
    foreground: true,
    background: false,
    daemon: false
  }, responseLanguage);
}
function audioReviewImportedCard(imported = {}, packetId = null, responseLanguage = "zh") {
  const handoffSuggestion = audioReviewImportedHandoffSuggestion(imported, responseLanguage);
  const proofBoundary = handoffSuggestion?.boundaryType === "review-proof-required";
  const nextActions = handoffSuggestion ? [{
    title: proofBoundary ? "Submit authoritative Reviewer proof" : doveText(responseLanguage, "resultCardNextCreateFixMission"),
    command: proofBoundary ? "project:dove.review" : "project:dove.mission",
    packetId,
    boundaryType: handoffSuggestion.boundaryType,
    ownerRole: handoffSuggestion.ownerRole,
    nextRole: handoffSuggestion.nextRole,
    requiredActions: handoffSuggestion.requiredActions,
    requires: handoffSuggestion.requires,
    handoffSuggestion,
    confirmationRequired: true
  }] : [{ title: doveText(responseLanguage, "resultCardNextStatus"), command: "project:dove.status", packetId }];
  return buildCommandResultCard({
    surface: "dove.review",
    command: "import_audio_review",
    packetId,
    runId: imported.runId,
    status: imported.status,
    outcome: imported.verdict,
    summary: imported.summary ?? doveText(responseLanguage, "resultCardReviewImported"),
    boundary: imported.boundary,
    evidenceLinks: [imported.handoffPath, imported.reportPath, imported.inputPath].filter(Boolean),
    validationEvidence: [imported.verdict].filter(Boolean),
    durableWrites: [doveText(responseLanguage, "resultCardReviewImported"), ARTIFACT_PATHS.reviewLog, ARTIFACT_PATHS.reviewConcerns, relativeRunPath2(imported.runId, "manifest.json")],
    nextActions,
    preActionGuidanceSummary: imported.preActionGuidanceSummary ?? null,
    foreground: true,
    background: false,
    daemon: false
  }, responseLanguage);
}
function audioReviewVerificationFailure(runId, failures, manifest, packetId, responseLanguage) {
  const result = {
    status: "verification-failed",
    boundaryType: "verification-failed",
    boundary: {
      type: "verification-failed",
      requiredActions: ["reprepare-review-artifacts", "rerun-audio-review"],
      failures
    },
    requiredActions: ["reprepare-review-artifacts", "rerun-audio-review"],
    runId,
    packetId,
    inputPath: manifest.inputPath,
    handoffPath: manifest.handoffPath,
    reportPath: manifest.reportPath,
    imported: false
  };
  return {
    ...result,
    resultCard: buildCommandResultCard({
      surface: "dove.review",
      command: "import_audio_review",
      packetId,
      runId,
      status: result.status,
      outcome: "verification-failed",
      summary: "Prepared review artifacts or input changed; reprepare and rerun the review.",
      evidenceLinks: [manifest.inputPath, manifest.handoffPath, manifest.reportPath],
      durableWrites: [],
      nextActions: [{
        title: "Reprepare and rerun audio review",
        command: "run_audio_review",
        packetId,
        boundaryType: "verification-failed",
        requiredActions: result.requiredActions,
        confirmationRequired: true
      }],
      foreground: true,
      background: false,
      daemon: false
    }, responseLanguage)
  };
}
function renderReviewLogEntry({ handoff, reportPath, reportSha256 }) {
  return [
    `## ${handoff.timestamp} \u2014 audio-review`,
    "",
    `- Run: ${handoff.runId}`,
    `- Reviewer: ${handoff.reviewerId}`,
    `- Status: ${handoff.status}`,
    `- Verdict: ${handoff.verdict}`,
    `- Summary: ${handoff.summary}`,
    `- Input: ${handoff.inputPath} (${handoff.inputSha256 ?? "missing-hash"})`,
    `- Report: ${reportPath ?? "none"}${reportSha256 ? ` (${reportSha256})` : ""}`,
    "- Findings:",
    ...handoff.findings.length > 0 ? handoff.findings.map((item) => `  - [${item.severity}] ${item.summary}`) : ["  - None recorded"],
    "- Action items:",
    ...handoff.actionItems.length > 0 ? handoff.actionItems.map((item) => `  - ${item}`) : ["  - None recorded"],
    ""
  ].join("\n");
}
function upsertConcerns(root, handoff) {
  const current = readJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: [], updatedAt: null });
  const existing = new Map((current.items ?? []).map((item) => [item.id, item]));
  for (const finding of handoff.findings) {
    const id = `audio-${handoff.runId}-${finding.id}`;
    existing.set(id, {
      ...existing.get(id) ?? {},
      id,
      summary: finding.summary,
      severity: finding.severity,
      status: handoff.verdict === "coherent" ? "resolved" : "awaiting-author-response",
      raisedByRole: "reviewer",
      responseOwnerRole: "builder",
      reviewerRationale: finding.summary,
      linkedArtifactPaths: [relativeRunPath2(handoff.runId, "handoff.json"), ...finding.linkedArtifactPaths],
      claimIds: finding.claimIds,
      experimentIds: finding.experimentIds,
      updatedAt: nowIso()
    });
  }
  writeJson(root, ARTIFACT_PATHS.reviewConcerns, { version: 2, items: Array.from(existing.values()).sort((left, right) => left.id.localeCompare(right.id)), updatedAt: nowIso() });
}
function transitionToAudioReviewer(root, args, target, runId, reviewedArtifactPaths) {
  const board = loadBoard(root);
  if (board.currentPhase === "review" && board.assignedRole === "reviewer") {
    return board;
  }
  return upsertSystemOrchestrationBoard(root, {
    ...args,
    phase: "review",
    assignedRole: "reviewer",
    intentType: "review",
    currentFocus: args.scope ?? target.packet?.title ?? `Audio review ${runId}`,
    nextAction: `Complete audio review ${runId} and return only the declared handoff and report artifacts.`,
    handoffSummary: `Handing ${target.packet?.title ?? "the current work"} to the audio reviewer for run ${runId}.`,
    evidenceLinks: Array.from(/* @__PURE__ */ new Set([...board.evidenceLinks ?? [], ...reviewedArtifactPaths])),
    reviewRequiredBeforeFinalize: true
  });
}
function audioReviewReturnTransition(handoff, independentProof = null) {
  if (handoff.verdict === "coherent") {
    if (independentProof?.authoritative !== true) {
      return {
        phase: "review",
        assignedRole: "reviewer",
        intentType: "review",
        nextAction: "Submit authoritative proof from an approved Reviewer runtime execution claim through the private capability.",
        proofBoundary: true
      };
    }
    return {
      phase: "plan",
      assignedRole: "planner",
      intentType: "plan",
      nextAction: "Inspect Dove status and choose the next governed finalization step."
    };
  }
  return {
    phase: "rebuttal",
    assignedRole: "builder",
    intentType: "respond",
    nextAction: handoff.actionItems[0] ?? "Address the audio review findings through governed revision work."
  };
}
function returnImportedAudioReview(root, args, handoff, handoffPath, reportPath, transition) {
  const board = loadBoard(root);
  const evidenceLinks = Array.from(/* @__PURE__ */ new Set([
    ...board.evidenceLinks ?? [],
    handoffPath,
    reportPath,
    ...handoff.reviewedArtifactPaths
  ]));
  const handoffSummary = transition.proofBoundary ? `Audio reviewer ${handoff.reviewerId} returned public coherent material for ${handoff.runId}, but authoritative Reviewer runtime proof is still required; Reviewer ownership is retained.` : `Audio reviewer ${handoff.reviewerId} returned ${handoff.verdict} for ${handoff.runId}; workflow routing now returns to ${transition.assignedRole} for the recorded next action.`;
  if (transition.proofBoundary && board.currentPhase === "review" && board.assignedRole === "reviewer") {
    appendSystemHandoff(root, {
      fromRole: "reviewer",
      toRole: "reviewer",
      phase: "review",
      intentType: "review",
      summary: handoffSummary,
      currentFocus: handoff.summary,
      nextAction: transition.nextAction,
      evidenceLinks
    });
  }
  return upsertSystemOrchestrationBoard(root, {
    ...args,
    ...transition,
    currentFocus: handoff.summary,
    handoffSummary,
    evidenceLinks,
    reviewRequiredBeforeFinalize: transition.proofBoundary ? true : handoff.verdict !== "coherent"
  });
}
function prepareAudioReview(root, args = {}) {
  assertGovernanceMutationRegistered("prepare-audio-review", "guarded");
  const target = assertTaskScopedMutationTarget(root, "prepare-audio-review", args);
  assertFollowThroughReady(root, "Preparing an isolated audio review input bundle", args);
  const runId = normalizeRunId2(args.runId);
  const catalog = readTaskPacketCatalog(root);
  const task = catalog.byId.get(target.packetId) ?? target.packet;
  const finalPlanPaths = normalizeStringArray25(args.finalPlanPaths ?? args.planPaths).map((item) => safeArtifactPath2(root, item));
  const finalResultPaths = normalizeStringArray25(args.finalResultPaths ?? args.resultPaths).map((item) => safeArtifactPath2(root, item));
  const explicitArtifactPaths = normalizeStringArray25(args.artifactPaths ?? args.reviewedArtifactPaths).map((item) => safeArtifactPath2(root, item));
  const classifiedArtifactPaths = Array.from(/* @__PURE__ */ new Set([
    ...finalPlanPaths,
    ...finalResultPaths,
    ...explicitArtifactPaths
  ]));
  if (classifiedArtifactPaths.length > 0) {
    buildReviewScope(root, target, {
      reviewedArtifactPaths: classifiedArtifactPaths
    });
  }
  const reviewScope = assertReviewMaterials(
    buildReviewScope(root, target),
    "prepare_audio_review"
  );
  const reviewedArtifactPaths = reviewScope.substantiveArtifactPaths;
  const classifiedArtifactSet = new Set(classifiedArtifactPaths);
  const canonicalExplicitArtifactPaths = reviewedArtifactPaths.filter((artifactPath) => !classifiedArtifactSet.has(artifactPath));
  const artifacts = reviewedArtifactPaths.map((relativePath) => artifactEntry2(root, relativePath));
  const usableArtifacts = assertSubstantiveArtifactEntries2(artifacts, "prepare_audio_review");
  const snapshot = snapshotReviewedArtifacts(root, usableArtifacts.map((artifact) => artifact.path), "prepare_audio_review");
  transitionToAudioReviewer(root, args, target, runId, snapshot.reviewedArtifacts.map((artifact) => artifact.path));
  const timestamp = nowIso();
  const input = {
    version: 1,
    reviewerKind: "audio",
    runId,
    packetId: reviewScope.packetId,
    includedPacketIds: reviewScope.includedPacketIds,
    createdAt: timestamp,
    isolationModel: "audio-final-plan-results-explicit-artifacts",
    contextPolicy: args.contextPolicy ?? DOVE_AUDIO_CONTEXT_POLICY,
    task: {
      id: task.id,
      title: task.title,
      summary: task.summary,
      stage: task.stage,
      domain: task.domain,
      status: task.status,
      level: task.level
    },
    finalPlanPaths,
    finalResultPaths,
    explicitArtifactPaths: Array.from(/* @__PURE__ */ new Set([
      ...explicitArtifactPaths,
      ...canonicalExplicitArtifactPaths
    ])),
    reviewedArtifactPaths: snapshot.reviewedArtifacts.map((artifact) => artifact.path),
    reviewedArtifacts: snapshot.reviewedArtifacts,
    reviewedArtifactSetSha256: snapshot.reviewedArtifactSetSha256,
    artifacts,
    instructions: normalizeString11(args.instructions, "Review only the supplied final plan, final result, and explicit artifacts. Do not assume access to project context."),
    outputContract: {
      handoffPath: relativeRunPath2(runId, "handoff.json"),
      reportPath: relativeRunPath2(runId, "report.md"),
      requiredHandoffFields: ["runId", "status", "verdict", "reviewerId", "summary", "inputPath", "inputSha256", "reportPath", "reviewedArtifactPaths", "findings", "actionItems"]
    },
    privacyBoundary: {
      writerPrivateTranscriptShared: false,
      projectContextShared: false,
      orchestrationBoardShared: false,
      reviewerPrivateTranscriptShouldReturn: false,
      acceptedExchangeArtifacts: [relativeRunPath2(runId, "input.json"), relativeRunPath2(runId, "handoff.json"), relativeRunPath2(runId, "report.md")]
    }
  };
  const inputText = stableJson3(input);
  const inputSha256 = sha256Text2(inputText);
  const manifest = {
    version: 1,
    reviewerKind: "audio",
    runId,
    packetId: target.packetId,
    includedPacketIds: reviewScope.includedPacketIds,
    status: "prepared",
    createdAt: timestamp,
    updatedAt: timestamp,
    inputPath: relativeRunPath2(runId, "input.json"),
    inputSha256,
    handoffPath: relativeRunPath2(runId, "handoff.json"),
    reportPath: relativeRunPath2(runId, "report.md"),
    finalPlanPaths,
    finalResultPaths,
    explicitArtifactPaths: Array.from(/* @__PURE__ */ new Set([
      ...explicitArtifactPaths,
      ...canonicalExplicitArtifactPaths
    ])),
    reviewedArtifactPaths: snapshot.reviewedArtifacts.map((artifact) => artifact.path),
    reviewedArtifacts: snapshot.reviewedArtifacts,
    reviewedArtifactSetSha256: snapshot.reviewedArtifactSetSha256,
    importedAt: null,
    handoffSha256: null,
    reportSha256: null
  };
  writeText(root, manifest.inputPath, inputText);
  writeJson(root, relativeRunPath2(runId, "manifest.json"), manifest);
  const prepared = {
    status: "prepared",
    runId,
    packetId: target.packetId,
    inputPath: manifest.inputPath,
    inputSha256,
    handoffPath: manifest.handoffPath,
    reportPath: manifest.reportPath,
    reviewedArtifactPaths: snapshot.reviewedArtifacts.map((artifact) => artifact.path),
    reviewedArtifacts: snapshot.reviewedArtifacts,
    reviewedArtifactSetSha256: snapshot.reviewedArtifactSetSha256,
    privacyBoundary: input.privacyBoundary,
    preActionGuidanceSummary: audioGuidanceSummary(root, args, target, {
      nextAction: "import_audio_review",
      statusSummary: {
        status: "prepared",
        runId,
        reviewedArtifactCount: usableArtifacts.length,
        contextPolicy: input.contextPolicy
      }
    })
  };
  return {
    ...prepared,
    resultCard: audioReviewPreparedCard(prepared, resolveDoveResponseLanguage(root, args))
  };
}
function importAudioReview(root, args = {}) {
  assertGovernanceMutationRegistered("import-audio-review", "guarded");
  const target = assertTaskScopedMutationTarget(root, "import-audio-review", args);
  assertFollowThroughReady(root, "Importing an isolated audio review handoff", args);
  const runId = normalizeRunId2(args.runId);
  const manifestPath = relativeRunPath2(runId, "manifest.json");
  const canonicalPaths = {
    inputPath: relativeRunPath2(runId, "input.json"),
    handoffPath: relativeRunPath2(runId, "handoff.json"),
    reportPath: relativeRunPath2(runId, "report.md")
  };
  const manifest = readJson(root, manifestPath, null);
  if (!manifest || typeof manifest !== "object") {
    throw new Error(`Missing audio review manifest for ${runId}`);
  }
  if (manifest.status !== "prepared") {
    throw new Error(`Audio review ${runId} is not importable from manifest status ${manifest.status ?? "unknown"}; prepare a fresh review run.`);
  }
  if (manifest.packetId !== target.packetId) {
    throw new Error(`Audio review ${runId} belongs to packet ${manifest.packetId ?? "unknown"}, not ${target.packetId}.`);
  }
  const handoffPath = args.handoffPath ? safeArtifactPath2(root, args.handoffPath) : canonicalPaths.handoffPath;
  const reportPath = args.reportPath ? safeArtifactPath2(root, args.reportPath) : canonicalPaths.reportPath;
  if (handoffPath !== canonicalPaths.handoffPath || reportPath !== canonicalPaths.reportPath) {
    return audioReviewVerificationFailure(runId, ["noncanonical-exchange-path"], manifest, manifest.packetId, resolveDoveResponseLanguage(root, args));
  }
  const handoffPathFailure = canonicalReviewExchangePathFailure(
    root,
    handoffPath,
    "handoff"
  );
  if (handoffPathFailure) {
    return audioReviewVerificationFailure(
      runId,
      [handoffPathFailure],
      manifest,
      manifest.packetId,
      resolveDoveResponseLanguage(root, args)
    );
  }
  const handoffFullPath = resolvePath(root, handoffPath);
  const handoff = normalizeHandoff2(root, JSON.parse(fs24.readFileSync(handoffFullPath, "utf8")));
  if (handoff.runId !== runId) {
    throw new Error(`Audio review handoff runId mismatch: expected ${runId}, received ${handoff.runId}`);
  }
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const inputPathFailure = canonicalReviewExchangePathFailure(
    root,
    canonicalPaths.inputPath,
    "input"
  );
  if (inputPathFailure) {
    return audioReviewVerificationFailure(
      runId,
      [inputPathFailure],
      manifest,
      manifest.packetId,
      responseLanguage
    );
  }
  const inputFullPath = resolvePath(root, canonicalPaths.inputPath);
  let input;
  let actualInputSha256;
  try {
    actualInputSha256 = sha256File(inputFullPath);
    input = JSON.parse(fs24.readFileSync(inputFullPath, "utf8"));
  } catch (error) {
    return audioReviewVerificationFailure(runId, [`input-unreadable:${error instanceof Error ? error.message : String(error)}`], manifest, manifest.packetId, responseLanguage);
  }
  const verification = verifyPreparedReviewSnapshot(root, {
    manifest,
    input,
    inputPath: manifest.inputPath,
    actualInputSha256,
    handoffInputPath: handoff.inputPath,
    handoffInputSha256: handoff.inputSha256,
    handoffReviewedArtifactPaths: handoff.reviewedArtifactPaths
  });
  if (!verification.ok) {
    return audioReviewVerificationFailure(runId, verification.failures, manifest, manifest.packetId, responseLanguage);
  }
  const canonicalScopeVerification = verifyCanonicalReviewImportScope(root, target, {
    runId,
    manifest,
    input,
    handoff,
    canonicalPaths,
    isolationModel: "audio-final-plan-results-explicit-artifacts",
    contextPolicy: DOVE_AUDIO_CONTEXT_POLICY
  });
  if (!canonicalScopeVerification.ok) {
    return audioReviewVerificationFailure(runId, canonicalScopeVerification.failures, manifest, manifest.packetId, responseLanguage);
  }
  if (handoff.reportPath !== reportPath) {
    return audioReviewVerificationFailure(runId, ["report-path-mismatch"], manifest, manifest.packetId, responseLanguage);
  }
  const reportPathFailure = canonicalReviewExchangePathFailure(
    root,
    reportPath,
    "report"
  );
  if (reportPathFailure) {
    return audioReviewVerificationFailure(
      runId,
      [reportPathFailure],
      manifest,
      manifest.packetId,
      responseLanguage
    );
  }
  const reportInspection = inspectDeclaredPath(root, reportPath, {
    requireNonEmpty: true,
    rejectBookkeeping: true
  });
  if (reportInspection.status !== "existing") {
    throw new Error(`Audio review report is not a usable non-empty file at ${reportPath}: ${reportInspection.reason ?? reportInspection.status}`);
  }
  const reportFullPath = resolvePath(root, reportPath);
  const handoffSha256 = hashFile2(handoffFullPath);
  const reportSha256 = hashFile2(reportFullPath);
  const anticipatedTransition = audioReviewReturnTransition(handoff);
  assertReviewProofBoundaryTransition(
    root,
    loadBoard(root),
    anticipatedTransition.phase,
    anticipatedTransition.assignedRole,
    "Importing an audio review"
  );
  assertSystemOrchestrationBoardUpdate(root, {
    ...args,
    ...anticipatedTransition,
    currentFocus: handoff.summary,
    reviewRequiredBeforeFinalize: true
  });
  appendText(root, ARTIFACT_PATHS.reviewLog, renderReviewLogEntry({ handoff, reportPath, reportSha256 }));
  upsertConcerns(root, handoff);
  const updatedManifest = {
    version: 1,
    reviewerKind: "audio",
    runId,
    packetId: target.packetId,
    includedPacketIds: canonicalScopeVerification.scope.includedPacketIds,
    status: "imported",
    createdAt: manifest.createdAt,
    updatedAt: nowIso(),
    importedAt: nowIso(),
    inputPath: canonicalPaths.inputPath,
    inputSha256: actualInputSha256,
    handoffPath,
    reportPath,
    finalPlanPaths: normalizeStringArray25(input.finalPlanPaths),
    finalResultPaths: normalizeStringArray25(input.finalResultPaths),
    explicitArtifactPaths: normalizeStringArray25(input.explicitArtifactPaths),
    reviewedArtifactPaths: canonicalScopeVerification.scope.substantiveArtifactPaths,
    reviewedArtifacts: verification.reviewedArtifacts,
    reviewedArtifactSetSha256: verification.reviewedArtifactSetSha256,
    handoffSha256,
    reportSha256,
    verdict: handoff.verdict,
    importedVerdict: handoff.verdict,
    reviewerId: handoff.reviewerId,
    authoritative: false,
    independentReviewProof: null,
    reviewProofRequired: handoff.verdict === "coherent"
  };
  writeJson(root, manifestPath, updatedManifest);
  const reviewedArtifactPaths = canonicalScopeVerification.scope.substantiveArtifactPaths;
  const independentProof = handoff.verdict === "coherent" ? verifyImportedReviewProofIntegrity(
    root,
    resolvePath(root, manifestPath),
    {
      packetId: target.packetId,
      reviewedArtifactPaths,
      isolationModel: "audio-final-plan-results-explicit-artifacts",
      handoffSha256Field: "handoffSha256"
    }
  ) : {
    ok: false,
    authoritative: false,
    proof: null,
    failures: []
  };
  const returnTransition = audioReviewReturnTransition(
    handoff,
    independentProof
  );
  if (returnTransition.proofBoundary) {
    recordReviewProofRequiredBoundary(root, {
      reviewKind: "audio",
      runId,
      packetId: target.packetId,
      includedPacketIds: canonicalScopeVerification.scope.includedPacketIds,
      timestamp: handoff.timestamp,
      handoffPath,
      reportPath,
      reviewedArtifactPaths
    });
  }
  returnImportedAudioReview(
    root,
    args,
    handoff,
    handoffPath,
    reportPath,
    returnTransition
  );
  const imported = {
    status: "imported",
    runId,
    verdict: returnTransition.proofBoundary ? "needs-evidence" : handoff.verdict,
    importedVerdict: handoff.verdict,
    authoritative: independentProof.authoritative === true,
    independentReviewProof: null,
    reviewProofRequired: handoff.verdict === "coherent" && independentProof.authoritative !== true,
    proofFailures: independentProof.failures ?? [],
    reviewerId: handoff.reviewerId,
    summary: handoff.summary,
    topConcerns: handoff.findings.slice(0, 5).map((finding) => finding.summary),
    actionItems: handoff.actionItems,
    handoffPath,
    reportPath: updatedManifest.reportPath,
    inputPath: manifest.inputPath,
    inputSha256: manifest.inputSha256,
    handoffSha256,
    reportSha256,
    ...returnTransition.proofBoundary ? {
      boundary: {
        type: "review-proof-required",
        ownerRole: "reviewer",
        nextRole: "reviewer",
        nextAction: returnTransition.nextAction,
        requiredActions: [
          "submit-authoritative-reviewer-runtime-proof"
        ]
      },
      ownerRole: "reviewer",
      nextRole: "reviewer",
      nextAction: returnTransition.nextAction
    } : {},
    privateTranscriptImported: false,
    preActionGuidanceSummary: audioGuidanceSummary(root, args, target, {
      nextAction: returnTransition.proofBoundary ? "project:dove.review" : handoff.verdict === "coherent" ? "project:dove.status" : "project:dove.mission",
      statusSummary: {
        status: "imported",
        runId,
        verdict: returnTransition.proofBoundary ? "needs-evidence" : handoff.verdict,
        importedVerdict: handoff.verdict,
        authoritative: independentProof.authoritative === true,
        reviewProofRequired: returnTransition.proofBoundary === true,
        findingCount: handoff.findings.length,
        actionItemCount: handoff.actionItems.length
      }
    })
  };
  return {
    ...imported,
    resultCard: audioReviewImportedCard(imported, manifest.packetId, resolveDoveResponseLanguage(root, args))
  };
}
function runAudioReview(root, args = {}) {
  assertGovernanceMutationRegistered("run-audio-review", "guarded");
  const target = assertTaskScopedMutationTarget(root, "run-audio-review", args);
  assertFollowThroughReady(root, "Running an isolated audio review", args);
  const responseLanguage = resolveDoveResponseLanguage(root, args);
  const runId = normalizeRunId2(args.runId);
  const handoffPath = args.handoffPath ? safeArtifactPath2(root, args.handoffPath) : relativeRunPath2(runId, "handoff.json");
  if (fs24.existsSync(resolvePath(root, handoffPath))) {
    const imported = importAudioReview(root, {
      ...args,
      runId,
      handoffPath,
      reportPath: args.reportPath
    });
    return {
      ...imported,
      packetId: target.packetId,
      imported,
      resultCard: imported.resultCard
    };
  }
  const prepared = prepareAudioReview(root, { ...args, runId });
  const awaiting = {
    ...prepared,
    status: "prepared-awaiting-audio",
    importArgs: { runId: prepared.runId, handoffPath: prepared.handoffPath, reportPath: prepared.reportPath }
  };
  return {
    ...awaiting,
    resultCard: audioReviewPreparedCard(awaiting, responseLanguage)
  };
}

// src/mcp/tool-definitions.mjs
var taskTargetProps = {
  packetId: { type: "string", description: "Durable Dove task packet id that this task-scoped write must bind before mutating state." },
  taskPacketId: { type: "string", description: "Alias for the durable Dove task packet id." },
  missionPacketId: { type: "string", description: "Alias for the durable Dove mission packet id." },
  taskId: { type: "string", description: "Alias for the durable Dove task packet id." },
  target: { type: "string", description: "Natural-language task target used to resolve an existing durable packet." },
  packetTarget: { type: "string", description: "Natural-language packet target used to resolve an existing durable packet." },
  taskName: { type: "string", description: "Human task name used to resolve an existing durable packet." }
};
function withTaskTarget(properties = {}) {
  return { ...taskTargetProps, ...properties };
}
var canonicalBoundaryProps = {
  id: { type: "string" },
  type: { type: "string" },
  status: { type: "string" },
  packetId: { type: ["string", "null"] },
  runId: { type: ["string", "null"] },
  sourceSurface: { type: ["string", "null"] },
  command: { type: ["string", "null"] },
  reason: { type: "string" },
  summary: { type: "string" },
  requiredInputs: { type: "array", items: { type: "string" } },
  requiredActions: { type: "array", items: { type: "string" } },
  ownerRole: { type: "string" },
  nextRole: { type: "string" },
  createdAt: { type: ["string", "null"] },
  resolvedAt: { type: ["string", "null"] },
  resolution: { type: ["string", "null"] }
};
var canonicalBoundarySchema = {
  type: ["object", "null"],
  properties: canonicalBoundaryProps,
  additionalProperties: false
};
var canonicalHandoffProps = {
  id: { type: "string" },
  status: { type: "string" },
  fromRole: { type: "string" },
  toRole: { type: "string" },
  reason: { type: "string" },
  summary: { type: "string" },
  boundaryId: { type: ["string", "null"] },
  sourceRunId: { type: ["string", "null"] },
  requestedAt: { type: ["string", "null"] },
  acceptedAt: { type: ["string", "null"] },
  completedAt: { type: ["string", "null"] }
};
var canonicalHandoffSchema = {
  type: ["object", "null"],
  properties: canonicalHandoffProps,
  additionalProperties: false
};
var publicReceiptBoundaryProps = {
  type: { type: "string" },
  reason: { type: "string" },
  summary: { type: "string" },
  requiredInputs: { type: "array", items: { type: "string" } },
  requiredActions: { type: "array", items: { type: "string" } },
  nextAction: { type: "string" }
};
var boundaryProps = {
  boundary: {
    type: "object",
    properties: publicReceiptBoundaryProps,
    additionalProperties: false
  },
  boundaryType: { type: "string" },
  boundaryId: { type: "string" },
  requiredInputs: { type: "array", items: { type: "string" } },
  requiredActions: { type: "array", items: { type: "string" } },
  ownerRole: { type: "string" },
  nextRole: { type: "string" },
  handoff: {
    type: "object",
    properties: {
      reason: { type: "string" },
      summary: { type: "string" }
    },
    additionalProperties: false
  },
  handoffId: { type: "string" },
  blockedReason: { type: "string" }
};
var missionPassBoundaryProps = {
  boundary: boundaryProps.boundary,
  boundaryType: boundaryProps.boundaryType,
  boundaryId: boundaryProps.boundaryId,
  requiredInputs: boundaryProps.requiredInputs,
  requiredActions: boundaryProps.requiredActions,
  blockedReason: boundaryProps.blockedReason
};
var workContractRouteProps = {
  label: { type: "string" },
  title: { type: "string" },
  command: { type: "string" },
  nextAction: { type: "string" },
  workflow: { type: "string" },
  copyableCommand: { type: "string" },
  copyCommand: { type: "string" },
  packetId: { type: "string" },
  taskPacketId: { type: "string" },
  missionPacketId: { type: "string" },
  when: { type: "string" },
  reason: { type: "string" },
  role: { type: "string" },
  ownerRole: { type: "string" },
  evidenceRequired: { type: "array", items: { type: "string" } },
  evidenceContract: { type: "array", items: { type: "string" } },
  evidenceExpectations: { type: "array", items: { type: "string" } },
  doneCriteria: { type: "array", items: { type: "string" } },
  rank: { type: "number" }
};
var workContractRouteSchema = {
  type: "object",
  properties: workContractRouteProps,
  additionalProperties: false
};
var workContractProps = {
  purpose: { type: "string" },
  deliverables: { type: "array", items: { type: "string" } },
  outOfScope: { type: "array", items: { type: "string" } },
  outOfScopeItems: { type: "array", items: { type: "string" } },
  evidenceContract: { type: "array", items: { type: "string" } },
  doneCriteria: { type: "array", items: { type: "string" } },
  practicalImpact: { type: "string" },
  recommendedRoutes: { type: "array", items: workContractRouteSchema }
};
var executionFileSchema = {
  type: "object",
  properties: {
    path: { type: "string" },
    action: { type: "string" },
    target: { type: "string" },
    change: { type: "string" }
  },
  additionalProperties: false
};
var executionMaterialsSchema = {
  type: "object",
  properties: {
    requiredInputs: { type: "array", items: { type: "string" } },
    requiredArtifacts: { type: "array", items: { type: "string" } },
    sourceRefs: { type: "array", items: { type: "string" } },
    artifactRefs: { type: "array", items: { type: "string" } }
  },
  additionalProperties: false
};
var executionConvergenceSchema = {
  type: "object",
  properties: {
    criteria: { type: "array", items: { type: "string" } },
    verificationCommands: { type: "array", items: { type: "string" } },
    evidenceRequired: { type: "array", items: { type: "string" } },
    definitionOfDone: { type: "string" }
  },
  additionalProperties: false
};
var executionFailureRouteProps = {
  on: { type: "string" },
  boundaryType: { type: "string" },
  nextAction: { type: "string" },
  requiredActions: { type: "array", items: { type: "string" } }
};
var executionFailureRouteSchema = {
  type: "object",
  properties: executionFailureRouteProps,
  additionalProperties: false
};
var executionContractProps = {
  chainType: { type: "string" },
  roleSequence: { type: "array", items: { type: "string" } },
  readFirst: { type: "array", items: { type: "string" } },
  action: { type: "string" },
  implementation: { type: "array", items: { type: "string" } },
  files: { type: "array", items: executionFileSchema },
  materials: executionMaterialsSchema,
  convergence: executionConvergenceSchema,
  failureRoutes: { type: "array", items: executionFailureRouteSchema }
};
var workContractSchema = {
  type: "object",
  properties: workContractProps,
  additionalProperties: false
};
var executionContractSchema = {
  type: "object",
  properties: executionContractProps,
  additionalProperties: false
};
var missionChecklistItemProps = {
  id: { type: "string" },
  title: { type: "string" },
  summary: { type: "string" },
  level: { type: "number" },
  stage: { type: "string" },
  domain: { type: "string" },
  status: { type: "string" },
  dependencies: { type: "array", items: { type: "string" } },
  blockedBy: { type: "array", items: { type: "string" } },
  evidenceExpectations: { type: "array", items: { type: "string" } },
  artifactRefs: { type: "array", items: { type: "string" } },
  nextAction: { type: "string" },
  workContract: workContractSchema,
  executionContract: executionContractSchema
};
var missionReplayProps = {
  proposalVersion: { type: "number", enum: [1] },
  proposalWorkspace: { type: "string", description: "Canonical realpath identity bound to the local exact replay data." },
  mutationMode: {
    type: "string",
    enum: ["patch-plan", "direct-process"],
    description: "Mutation mode bound into the exact local proposal replay."
  },
  responseLanguage: { type: "string" },
  initId: { type: "string" },
  initTitle: { type: "string" },
  initObjective: { type: "string" },
  initDomain: { type: "string" },
  initStatus: { type: "string" },
  initArtifactRefs: { type: "array", items: { type: "string" } },
  id: { type: "string" },
  goal: { type: "string" },
  title: { type: "string" },
  summary: { type: "string" },
  stage: { type: "string" },
  domain: { type: "string" },
  level: { type: "number" },
  creatorKind: { type: "string" },
  status: { type: "string" },
  dependencies: { type: "array", items: { type: "string" } },
  blockedBy: { type: "array", items: { type: "string" } },
  ownerRole: { type: "string" },
  nextRole: { type: "string" },
  boundary: canonicalBoundarySchema,
  handoff: canonicalHandoffSchema,
  currentFocus: { type: "string" },
  nextAction: { type: "string" },
  evidenceExpectations: { type: "array", items: { type: "string" } },
  workContract: workContractSchema,
  executionContract: executionContractSchema,
  artifactRefs: { type: "array", items: { type: "string" } },
  contextPolicy: { type: "string" },
  lessonIds: { type: "array", items: { type: "string" } },
  checklist: {
    anyOf: [
      { type: "boolean" },
      {
        type: "array",
        items: {
          anyOf: [
            { type: "string" },
            {
              type: "object",
              properties: missionChecklistItemProps,
              additionalProperties: false
            }
          ]
        }
      }
    ]
  },
  autoChecklist: { type: "boolean" },
  createChecklist: { type: "boolean" },
  checklistItems: { type: "array", items: { type: "object", properties: missionChecklistItemProps, additionalProperties: false } },
  proposalDigest: { type: "string", pattern: "^[0-9a-f]{64}$", description: "Exact SHA-256 proposal digest returned with trusted-local replay data. It detects replay drift but is not proof of human approval or tamper-proof authentication." },
  confirmed: { type: "boolean" },
  confirm: { type: "boolean" }
};
var verifiedCriteriaItemProps = {
  criterion: { type: "string" },
  status: { type: "string" },
  evidencePaths: { type: "array", items: { type: "string" } }
};
var verifiedCriteriaItemSchema = {
  type: "object",
  properties: verifiedCriteriaItemProps,
  additionalProperties: false
};
var validationGateResultProps = {
  gate: { type: "string" },
  name: { type: "string" },
  status: { type: "string" },
  outcome: { type: "string" },
  summary: { type: "string" },
  evidencePaths: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } }
};
var experiencePlanProps = {
  id: { type: "string" },
  experimentId: { type: "string" },
  goal: { type: "string" },
  idea: { type: "string" },
  title: { type: "string" },
  methodology: { type: "string" },
  method: { type: "string" },
  successMetric: { type: "string" },
  metric: { type: "string" },
  comparisonTargets: { type: "array", items: { type: "string" } },
  baselines: { type: "array", items: { type: "string" } },
  claimId: { type: "string" }
};
var experienceResultProps = {
  id: { type: "string" },
  resultId: { type: "string" },
  experimentId: { type: "string" },
  claimId: { type: "string" },
  outcome: { type: "string" },
  summary: { type: "string" },
  resultSummary: { type: "string" },
  evidenceLinks: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } },
  comparisonTargets: { type: "array", items: { type: "string" } }
};
var rebuttalIssueProps = {
  id: { type: "string" },
  reviewer: { type: "string" },
  summary: { type: "string" },
  severity: { type: "string" },
  status: { type: "string" },
  evidenceLinks: { type: "array", items: { type: "string" } },
  claimIds: { type: "array", items: { type: "string" } },
  experimentIds: { type: "array", items: { type: "string" } },
  responseDirection: { type: "string" }
};
var rebuttalIssueSchema = {
  type: "object",
  properties: rebuttalIssueProps,
  additionalProperties: false
};
var manualReviewFindingSchema = {
  type: "object",
  properties: {
    id: { type: "string" },
    severity: { type: "string" },
    summary: { type: "string" },
    claimIds: { type: "array", items: { type: "string" } },
    experimentIds: { type: "array", items: { type: "string" } },
    responseOwnerRole: { type: "string" },
    reviewerAction: { type: "string" },
    linkedAuditIds: { type: "array", items: { type: "string" } },
    linkedBridgeIds: { type: "array", items: { type: "string" } },
    linkedArtifactPaths: { type: "array", items: { type: "string" } },
    methodologicalCategory: { type: ["string", "null"] }
  },
  additionalProperties: false
};
var figureMaterialItemProps = {
  id: { type: "string" },
  type: { type: "string" },
  label: { type: "string" },
  summary: { type: "string" },
  artifactPath: { type: "string" },
  path: { type: "string" }
};
var figureMaterialItemSchema = {
  type: "object",
  properties: figureMaterialItemProps,
  additionalProperties: false
};
var figureSemanticCoverageObservationProps = {
  observations: { type: "array", items: { type: "string" } },
  evidencePaths: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } }
};
var figureSemanticReviewObservationProps = {
  summary: { type: "string" },
  observations: { type: "array", items: { type: "string" } },
  evidencePaths: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } }
};
var executionReceiptProps = {
  receiptId: { type: "string" },
  runId: { type: "string" },
  packetId: { type: "string" },
  command: { type: "string" },
  surface: { type: "string" },
  actionType: { type: "string" },
  startedAt: { type: "string" },
  completedAt: { type: "string" },
  status: { type: "string" },
  outcome: { type: "string" },
  resultSummary: { type: "string" },
  publicSafeSummary: { type: "string" },
  nextAction: { type: "string" },
  lifecycleTransition: {
    type: "object",
    properties: { previousStatus: { type: "string" }, nextStatus: { type: "string" } },
    additionalProperties: false
  },
  artifactRefs: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } },
  evidenceLinks: { type: "array", items: { type: "string" } },
  evidencePaths: { type: "array", items: { type: "string" } },
  validationEvidencePaths: { type: "array", items: { type: "string" } },
  verificationEvidencePaths: { type: "array", items: { type: "string" } },
  verifiedCriteria: { type: "array", items: verifiedCriteriaItemSchema },
  criteriaCoverage: {
    type: "object",
    properties: {
      complete: { type: "boolean" },
      required: { type: "array", items: { type: "string" } },
      missing: { type: "array", items: { type: "string" } },
      verified: { type: "array", items: verifiedCriteriaItemSchema }
    },
    additionalProperties: false
  },
  validationGateResults: {
    type: "array",
    items: {
      type: "object",
      properties: validationGateResultProps,
      additionalProperties: false
    }
  },
  boundary: {
    type: "object",
    properties: publicReceiptBoundaryProps,
    additionalProperties: false
  }
};
var executionEvidenceProps = {
  executionContract: executionContractSchema,
  validationEvidencePaths: { type: "array", items: { type: "string" } },
  verificationEvidencePaths: { type: "array", items: { type: "string" } },
  verifiedCriteria: { type: "array", items: verifiedCriteriaItemSchema }
};
var executionVerificationProps = {
  ...executionEvidenceProps,
  executionReceipt: { type: "object", properties: executionReceiptProps, additionalProperties: false }
};
var statusAdjustmentItemProps = {
  index: { type: "number" },
  packetId: { type: "string" },
  taskPacketId: { type: "string" },
  taskId: { type: "string" },
  id: { type: "string" },
  status: { type: "string" },
  taskStatus: { type: "string" },
  missionStatus: { type: "string" },
  reason: { type: "string" },
  summary: { type: "string" },
  nextAction: { type: ["string", "null"] },
  evidenceLinks: { type: "array", items: { type: "string" } },
  evidencePaths: { type: "array", items: { type: "string" } },
  validationEvidencePaths: { type: "array", items: { type: "string" } },
  verificationEvidencePaths: { type: "array", items: { type: "string" } },
  verifiedCriteria: { type: "array", items: verifiedCriteriaItemSchema },
  executionReceipt: {
    anyOf: [
      { type: "null" },
      { type: "object", properties: executionReceiptProps, additionalProperties: false }
    ]
  },
  executionContract: {
    anyOf: [
      { type: "null" },
      executionContractSchema
    ]
  },
  artifactRefs: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } }
};
var statusAdjustmentItemSchema = {
  type: "object",
  properties: statusAdjustmentItemProps,
  additionalProperties: false
};
var autoStepFailureRouteSchema = {
  type: "object",
  properties: executionFailureRouteProps,
  additionalProperties: false
};
var contractAwareAutoStepProps = {
  completeTask: { type: "boolean" },
  requiredMaterials: { type: "array", items: { type: "string" } },
  outputArtifacts: { type: "array", items: { type: "string" } },
  convergenceChecks: { type: "array", items: { type: "string" } },
  failureRoutes: { type: "array", items: autoStepFailureRouteSchema },
  ...executionEvidenceProps
};
var autoSourceItemProps = {
  sourceId: { type: "string" },
  citationKey: { type: "string" },
  title: { type: "string" },
  authors: { type: "array", items: { type: "string" } },
  year: { type: ["string", "number"] },
  locator: { type: "string" },
  sourceType: { type: "string" },
  abstract: { type: "string" },
  origin: { type: "string" }
};
var autoStepArgsByCommand = {
  "dove.source": {
    ...autoSourceItemProps,
    sources: {
      type: "array",
      items: {
        type: "object",
        properties: autoSourceItemProps,
        additionalProperties: false
      }
    }
  },
  "dove.note": {
    noteId: { type: "string" },
    title: { type: "string" },
    sectionId: { type: "string" },
    sourceIds: { type: "array", items: { type: "string" } },
    summary: { type: "string" },
    quotes: { type: "array", items: { type: "string" } },
    claims: { type: "array", items: { type: "string" } },
    openQuestions: { type: "array", items: { type: "string" } }
  },
  "dove.experience": {
    id: { type: "string" },
    experimentId: { type: "string" },
    goal: { type: "string" },
    idea: { type: "string" },
    title: { type: "string" },
    methodology: { type: "string" },
    method: { type: "string" },
    successMetric: { type: "string" },
    metric: { type: "string" },
    comparisonTargets: { type: "array", items: { type: "string" } },
    baselines: { type: "array", items: { type: "string" } },
    claimId: { type: "string" },
    result: { type: "object", properties: experienceResultProps, additionalProperties: false },
    resultId: { type: "string" },
    outcome: { type: "string" },
    summary: { type: "string" },
    resultSummary: { type: "string" },
    evidenceLinks: { type: "array", items: { type: "string" } },
    artifactPaths: { type: "array", items: { type: "string" } },
    plan: { type: "object", properties: experiencePlanProps, additionalProperties: false }
  },
  "dove.figure": {
    intent: { type: "string" },
    description: { type: "string" },
    name: { type: "string" },
    title: { type: "string" },
    figureId: { type: "string" },
    id: { type: "string" },
    purpose: { type: "string" },
    captionIntent: { type: "string" },
    targetClaimIds: { type: "array", items: { type: "string" } },
    claimIds: { type: "array", items: { type: "string" } },
    sourceSections: { type: "array", items: { type: "string" } },
    sectionIds: { type: "array", items: { type: "string" } },
    sourceArtifactPaths: { type: "array", items: { type: "string" } },
    artifactPaths: { type: "array", items: { type: "string" } },
    relatedExperimentIds: { type: "array", items: { type: "string" } },
    experimentIds: { type: "array", items: { type: "string" } },
    reviewConcernIds: { type: "array", items: { type: "string" } },
    rebuttalIssueIds: { type: "array", items: { type: "string" } },
    requiredVisualElements: { type: "array", items: { type: "string" } },
    materialHints: { type: "array", items: { anyOf: [{ type: "string" }, figureMaterialItemSchema] } },
    materialRequirements: { type: "array", items: figureMaterialItemSchema },
    providerId: { type: "string" },
    executeProvider: { type: "boolean" },
    allowMissingMaterials: { type: "boolean" },
    runId: { type: "string" },
    outputFormat: { type: "string" },
    constraints: { type: "array", items: { type: "string" } },
    outputManifestPath: { type: "string" },
    sourceSvgPath: { type: "string" },
    targetFinalSvgPath: { type: "string" },
    svgContent: { type: "string" },
    caption: { type: "string" },
    captionDraft: { type: "string" },
    captionId: { type: "string" },
    semanticCoverage: { type: "object", properties: figureSemanticCoverageObservationProps, additionalProperties: false },
    semanticReview: { type: "object", properties: figureSemanticReviewObservationProps, additionalProperties: false }
  },
  "dove.draft": {
    sectionId: { type: "string" },
    title: { type: "string" },
    body: { type: "string" },
    status: { type: "string" },
    summary: { type: "string" }
  },
  "dove.review": {
    scope: { type: "string" },
    stage: { type: "string" },
    reviewer: { type: "string" }
  },
  "dove.review-loop": {
    runId: { type: "string" },
    scope: { type: "string" },
    instructions: { type: "string" },
    stage: { type: "string" },
    summary: { type: "string" },
    artifactPaths: { type: "array", items: { type: "string" } },
    reviewedArtifactPaths: { type: "array", items: { type: "string" } },
    responseLanguage: { type: "string" },
    language: { type: "string" }
  },
  "dove.rebuttal": {
    issues: { type: "array", items: rebuttalIssueSchema }
  },
  "dove.lessons": {},
  "dove.status": {}
};
var contractAwareAutoStepSchema = {
  type: "object",
  properties: {
    command: { type: "string", enum: Object.keys(autoStepArgsByCommand) },
    args: { type: "object" },
    ...contractAwareAutoStepProps
  },
  required: ["command"],
  additionalProperties: false,
  allOf: Object.entries(autoStepArgsByCommand).map(([command, argsProperties]) => ({
    if: {
      properties: { command: { const: command } },
      required: ["command"]
    },
    then: {
      properties: {
        args: {
          type: "object",
          properties: argsProperties,
          additionalProperties: false
        }
      }
    }
  }))
};
var planMissionProps = {
  id: { type: "string" },
  packetId: { type: "string" },
  taskPacketId: { type: "string" },
  goal: { type: "string" },
  objective: { type: "string" },
  title: { type: "string" },
  summary: { type: "string" },
  stage: { type: "string" },
  missionStage: { type: "string" },
  domain: { type: "string" },
  doveDomain: { type: "string" },
  missionDomain: { type: "string" },
  level: { type: "number" },
  taskLevel: { type: "number" },
  missionLevel: { type: "number" },
  status: { type: "string" },
  dependencies: { type: "array", items: { type: "string" } },
  dependencyIds: { type: "array", items: { type: "string" } },
  blockedBy: { type: "array", items: { type: "string" } },
  blockerIds: { type: "array", items: { type: "string" } },
  evidenceExpectations: { type: "array", items: { type: "string" } },
  evidenceLinks: { type: "array", items: { type: "string" } },
  artifactRefs: { type: "array", items: { type: "string" } },
  artifactPaths: { type: "array", items: { type: "string" } },
  currentFocus: { type: "string" },
  nextAction: { type: "string" },
  workContract: workContractSchema,
  ...executionVerificationProps,
  ...missionPassBoundaryProps,
  childMissions: { type: "array", items: { type: ["object", "string"] } },
  children: { type: "array", items: { type: ["object", "string"] } }
};
var operatorTaskResultProps = {
  packetId: { type: "string" },
  resultStatus: { type: "string", enum: ["completed", "blocked", "in-progress"] },
  resultSummary: { type: "string" },
  outcome: { type: "string" },
  reason: { type: "string" },
  stopReason: { type: "string" },
  nextAction: { type: "string" },
  evidenceLinks: { type: "array", items: { type: "string" } },
  artifactRefs: { type: "array", items: { type: "string" } },
  validationEvidencePaths: { type: "array", items: { type: "string" } },
  verificationEvidencePaths: { type: "array", items: { type: "string" } },
  verifiedCriteria: executionVerificationProps.verifiedCriteria,
  executionContract: executionVerificationProps.executionContract,
  executionReceipt: executionVerificationProps.executionReceipt,
  boundaryType: { type: "string" },
  boundaryId: { type: "string" },
  requiredInputs: { type: "array", items: { type: "string" } },
  requiredActions: { type: "array", items: { type: "string" } },
  startedAt: { type: "string" },
  completedAt: { type: "string" }
};
var resultModeProperty = {
  type: "string",
  enum: ["compact", "full", "debug"],
  description: "MCP response expansion mode. compact returns the stable result contract only; full includes fullResult; debug includes fullResult plus diagnostics."
};
var mutationModeProperty = {
  type: "string",
  enum: ["patch-plan", "direct-process"],
  description: "Canonical Dove mutation mode. patch-plan returns declarative file operations for host-tracked application; direct-process writes from the Dove process and is not verified host rollback-safe. If omitted, host-facing MCP calls keep functional direct-process behavior and return hostRollbackIneligibleReason plus rollbackAdvice when writes are not host-rollback eligible."
};
var operatorSurfaceProperty = {
  type: "string",
  enum: ["operator", "compact", "full", "debug"],
  description: "Tool discovery surface. Use operator or compact for everyday Dove work; request expanded catalogs only when auditing or diagnosing."
};
var OPERATOR_TOOL_NAMES = [
  "query_dove_status",
  "query_dove_orchestrate",
  "query_document_ledger",
  "query_operator_lessons",
  "search_network",
  "query_network_search_providers",
  "create_dove_task",
  "run_dove_auto",
  "run_dove_operator",
  "register_source",
  "verify_source",
  "upsert_note",
  "upsert_draft",
  "record_document_evidence",
  "run_figure_workflow",
  "run_experience_workflow",
  "run_review_loop",
  "build_rebuttal_strategy",
  "query_dove_return"
];
var OPERATOR_WORKFLOW_AREAS = {
  query_dove_status: "status",
  query_dove_orchestrate: "routing",
  query_document_ledger: "evidence",
  query_operator_lessons: "lessons",
  search_network: "search",
  query_network_search_providers: "search",
  create_dove_task: "mission",
  run_dove_auto: "auto",
  run_dove_operator: "operator",
  register_source: "source",
  verify_source: "source",
  upsert_note: "note",
  upsert_draft: "draft",
  record_document_evidence: "document",
  run_figure_workflow: "figure",
  run_experience_workflow: "experience",
  run_review_loop: "review",
  build_rebuttal_strategy: "rebuttal",
  query_dove_return: "verification"
};
var OPERATOR_SCHEMA_FIELDS = {
  query_dove_status: ["intent", "domain", "stage", "status", "detail", "view", "showMissions", "includeMissionDetails", "requestStatusAdjustment", "includeStatusAdjustmentPreview", "resultMode"],
  query_dove_orchestrate: ["request", "userRequest", "goal", "domain", "stage", "targetArtifacts", "acceptanceChecks", "allowAutonomy", "resultMode"],
  query_document_ledger: ["documentKind", "status", "evidenceScope", "publicSafe", "limit", "resultMode"],
  query_operator_lessons: ["domain", "status", "tag", "limit", "resultMode"],
  search_network: ["query", "kind", "limit", "year", "domains", "fieldsOfStudy", "openAccessOnly", "providerIds", "locale", "resultMode"],
  query_network_search_providers: ["kind", "providerIds", "resultMode"],
  create_dove_task: [...Object.keys(missionReplayProps), "objective", "prompt", "resultMode"],
  run_dove_auto: ["target", "goal", "prompt", "steps", "maxIterations", "maxSteps", "confirm", "confirmed", "completeTask", "completeOnSuccess", "resultMode"],
  run_dove_operator: ["confirmed", "includeQueueDetails", "blockerInvestigationMode", "runId", "resultMode"],
  register_source: ["target", "citationKey", "title", "authors", "year", "locator", "sourceType", "abstract", "origin", "sources", "resultMode"],
  verify_source: ["target", "sourceId", "decision", "method", "checkedMaterial", "auditEvidence", "resultMode"],
  upsert_note: ["target", "title", "summary", "quotes", "claims", "openQuestions", "resultMode"],
  upsert_draft: ["target", "title", "body", "status", "summary", "resultMode"],
  record_document_evidence: ["target", "title", "documentKind", "status", "evidenceScope", "publicSafe", "visibility", "summary", "context", "reason", "claims", "createDocument", "appendDocument", "body", "content", "resultMode"],
  run_figure_workflow: ["target", "intent", "description", "name", "title", "purpose", "captionIntent", "requiredVisualElements", "materialHints", "allowMissingMaterials", "outputFormat", "constraints", "caption", "captionDraft", "resultMode"],
  run_experience_workflow: ["target", "goal", "idea", "title", "methodology", "method", "successMetric", "metric", "comparisonTargets", "baselines", "result", "outcome", "summary", "resultSummary", "resultMode"],
  run_review_loop: ["target", "scope", "stage", "resultMode"],
  build_rebuttal_strategy: ["target", "resultMode"],
  query_dove_return: ["goal", "domain", "stage", "scope", "targetArtifacts", "acceptanceChecks", "validationEvidence", "reviewEvidence", "resultMode"]
};
var OPERATOR_PUBLIC_TOOL_DESCRIPTIONS = {
  query_dove_status: "Show the current Dove situation in everyday language, with the next useful action and a note that detailed audit context can be requested.",
  query_dove_orchestrate: "Choose the Dove work surface that fits the user's request without changing project state.",
  query_document_ledger: "List recorded document and evidence summaries without showing document bodies.",
  query_operator_lessons: "Show reusable Dove lessons that may affect the next action.",
  search_network: "Find public no-key web or scholarly candidates without writing Dove state; verify useful results before recording sources, notes, evidence, or claims.",
  query_network_search_providers: "Show which public no-key network search providers are available or disabled without exposing raw provider internals.",
  create_dove_task: "Turn a user goal into a confirmable Dove mission contract, then materialize only its complete trusted-local exact replay data. The digest detects accidental drift but is not proof of human approval or tamper-proof authentication.",
  run_dove_auto: "Run confirmed foreground work that produces or inspects real materials; status/task panels alone are not progress, and missing materials return a clear boundary.",
  run_dove_operator: "Preview concrete required actions, then after explicit confirmation move one safe step only when real results or material are present.",
  register_source: "Record external source metadata as a candidate for the current task; registration alone never makes it claim-eligible.",
  verify_source: "Record a source rejection after checking durable material; public positive verification is disabled.",
  upsert_note: "Save synthesized writing or research notes for the current task.",
  upsert_draft: "Update a section draft when concrete draft text is provided.",
  record_document_evidence: "Record a document or evidence summary without exposing raw private material.",
  run_figure_workflow: "Prepare or update one figure: plan materials, optionally generate or import output, and report what is still missing.",
  run_experience_workflow: "Plan or record an experiment and connect reviewed results back to the claim they support.",
  run_review_loop: "Run an evidence-aware review pass and produce revision guidance.",
  build_rebuttal_strategy: "Organize reviewer concerns into a response strategy and draft language.",
  query_dove_return: "Check whether a mission has enough evidence to be considered returned."
};
var COMPACT_DISCOVERY_INTERNAL_KEY_PATTERN = /^(?:id|packetId|packetIds|taskPacketId|missionPacketId|taskId|runId|receiptId|boundary|boundaryId|boundaryType|implementationBoundaryType|implementationReason|ownerRole|nextRole|handoff|handoffId|handoffSuggestion|actorRole|providerId|providerStatus|providerError|apiKeyEnv|sourceSvgPath|targetFinalSvgPath|finalSvgPath|outputManifestPath|svgContent|qaPath|resultPath|documentPath|path|paths|artifactPath|artifactPaths|sourceArtifactPath|sourceArtifactPaths|evidencePaths|validationEvidencePaths|verificationEvidencePaths|reviewEvidencePaths|reviewedArtifactPaths|finalPlanPaths|planPaths|finalResultPaths|resultPaths|artifactRefs|sourceRefs|claimIds|sourceIds|noteIds|experimentIds|reviewConcernIds|rebuttalIssueIds|relatedExperimentIds|targetClaimIds|dependencyIds|blockerIds|lessonIds|command|workflow|preset|nextCommand|copyableCommand|operatorRoute|mutationMode|queueSummary|queuePreview|preActionGuidance|preActionGuidanceSummary|fullResult|diagnostics)$/u;
function isCompactDiscoveryInternalKey(key) {
  const text3 = String(key ?? "");
  return COMPACT_DISCOVERY_INTERNAL_KEY_PATTERN.test(text3) || /(?:^|[A-Za-z])Ids?$/u.test(text3);
}
function compactPublicSchema(schema) {
  if (Array.isArray(schema)) {
    return schema.map((item) => compactPublicSchema(item));
  }
  if (!schema || typeof schema !== "object") {
    return schema;
  }
  const next = {};
  const requiredProperties = new Set(Array.isArray(schema.required) ? schema.required : []);
  for (const [key, value] of Object.entries(schema)) {
    if (key === "properties" && value && typeof value === "object" && !Array.isArray(value)) {
      const properties = Object.fromEntries(Object.entries(value).filter(([propertyName]) => requiredProperties.has(propertyName) || !isCompactDiscoveryInternalKey(propertyName)).map(([propertyName, propertySchema]) => [propertyName, compactPublicSchema(propertySchema)]));
      if (Object.keys(properties).length > 0) {
        next.properties = properties;
      }
      continue;
    }
    if (key === "description") {
      next.description = typeof value === "string" ? value.replace(/fullResult|preActionGuidanceSummary|preActionGuidance|resultCard|boundaryType|patch-plan|direct-process|providerId|\.dove\//gu, "details") : value;
      continue;
    }
    next[key] = compactPublicSchema(value);
  }
  return next;
}
var toolDiscoveryInputSchema = {
  type: "object",
  properties: {
    surface: operatorSurfaceProperty,
    resultMode: resultModeProperty
  },
  additionalProperties: false
};
var MUTATING_TOOL_NAMES = /* @__PURE__ */ new Set([
  "ensure_workspace",
  "init_project",
  "publish_dove_status",
  "publish_dove_global_status",
  "record_document_evidence",
  "init_dove_goal",
  "create_dove_task",
  "record_dove_mission_pass",
  "apply_dove_status_adjustments",
  "run_dove_auto",
  "run_dove_operator",
  "kill_dove_task",
  "reset_dove_version",
  "run_experience_workflow",
  "prepare_audio_review",
  "import_audio_review",
  "run_audio_review",
  "run_dove_review_loop",
  "launch_dove_mission",
  "summarize_session_journal",
  "upsert_orchestration_board",
  "append_handoff",
  "update_research_brief",
  "register_source",
  "verify_source",
  "upsert_note",
  "upsert_claims",
  "upsert_plan",
  "upsert_outline",
  "upsert_draft",
  "upsert_experiment_plan",
  "upsert_experiment_result",
  "run_experiment_audit",
  "bridge_result_to_claim",
  "run_review_loop",
  "append_review_log",
  "prepare_isolated_review",
  "import_isolated_review",
  "upsert_revision_plan",
  "set_section_status",
  "sync_checklist",
  "sync_citations",
  "refresh_wiki",
  "normalize_rebuttal_issues",
  "build_rebuttal_strategy",
  "build_rebuttal",
  "create_version_snapshot",
  "compare_versions",
  "upsert_figure_plan",
  "run_figure_workflow",
  "prepare_figure_generation",
  "import_figure_generation",
  "validate_figure_pipeline",
  "record_operator_lesson",
  "record_operator_follow_through",
  "plan_campaign",
  "revoke_program_approval",
  "materialize_guidance_packet"
]);
function addMcpControlFields(tool) {
  const properties = {
    ...tool.inputSchema?.properties ?? {},
    resultMode: resultModeProperty
  };
  if (MUTATING_TOOL_NAMES.has(tool.name)) {
    properties.mutationMode = mutationModeProperty;
  }
  return {
    ...tool,
    inputSchema: {
      ...tool.inputSchema,
      properties,
      additionalProperties: false
    }
  };
}
var baseToolDefinitions = [
  { name: "ensure_workspace", description: "Ensure the canonical .dove workspace and starter artifacts exist.", inputSchema: { type: "object", properties: {} } },
  {
    name: "init_project",
    description: "Initialize or refresh project metadata and the research contract.",
    inputSchema: { type: "object", properties: { title: { type: "string" }, venue: { type: "string" }, objective: { type: "string" }, deadline: { type: "string" }, thesis: { type: "string" }, audience: { type: "string" }, strictMode: { type: "boolean" } } }
  },
  { name: "read_state", description: "Read the normalized Dove state.", inputSchema: { type: "object", properties: {} } },
  { name: "query_task_graph", description: "Refresh and read the durable task-packet graph.", inputSchema: { type: "object", properties: {} } },
  { name: "query_open_questions", description: "Refresh and read open questions across notes, review state, and task packets.", inputSchema: { type: "object", properties: {} } },
  { name: "query_decisions", description: "Refresh and read durable operational decisions and comparison decisions.", inputSchema: { type: "object", properties: {} } },
  { name: "query_lineage", description: "Refresh and read version lineage plus comparison targets.", inputSchema: { type: "object", properties: {} } },
  { name: "query_workspace_index", description: "Refresh and read the top-level workspace index for resumable state.", inputSchema: { type: "object", properties: {} } },
  { name: "query_meta_optimize", description: "Refresh and read the proposal-only meta-optimize frontier, taxonomy-aware grouped clusters, family-level operator playbooks, ranked recommendations, durable remediation packs, longer-horizon memory summaries, and report paths.", inputSchema: { type: "object", properties: {} } },
  { name: "query_governance_coverage_report", description: "Refresh and read the durable governance coverage proof report for guarded and exempt mutation paths.", inputSchema: { type: "object", properties: {} } },
  { name: "query_operator_lessons", description: "Read explicit distilled operator lessons and retrospectives; action surfaces also recall applicable lessons automatically as read-only preActionGuidance without importing raw runtime traces or refreshing derived work surfaces.", inputSchema: { type: "object", properties: { domain: { type: "string" }, status: { type: "string" }, tag: { type: "string" }, actorRole: { type: "string" }, limit: { type: "number" } } } },
  { name: "search_network", description: "Run a bounded foreground public no-key network search for candidate materials only. This tool is read-only: it does not write .dove state, does not expose raw provider payloads or secrets, and verified candidates must be recorded through source, note, document evidence, or claim workflows before being treated as evidence.", inputSchema: { type: "object", properties: { query: { type: "string" }, kind: { type: "string", enum: ["scholarly", "web", "all"] }, limit: { type: "number" }, year: { type: ["string", "number"] }, domains: { type: "array", items: { type: "string" } }, fieldsOfStudy: { type: "array", items: { type: "string" } }, openAccessOnly: { type: "boolean" }, providerIds: { type: "array", items: { type: "string" } }, providers: { type: "array", items: { type: "string" } }, locale: { type: "string" } } } },
  { name: "query_network_search_providers", description: "Inspect public no-key Dove network search provider availability without writing state or exposing credentials, headers, raw provider payloads, or internal debug fields.", inputSchema: { type: "object", properties: { kind: { type: "string", enum: ["scholarly", "web", "all"] }, providerIds: { type: "array", items: { type: "string" } }, providers: { type: "array", items: { type: "string" } } } } },
  { name: "query_sources", description: "Read the flat source surface with current lifecycle eligibility derived from the latest verification decision, matching identity fingerprint, and packet binding. The underlying sources and source-verifications ledgers are bookkeeping and are not completion evidence by themselves.", inputSchema: { type: "object", properties: { sourceId: { type: "string" }, id: { type: "string" }, packetId: { type: "string" }, taskPacketId: { type: "string" }, missionPacketId: { type: "string" }, lifecycle: { type: "string", enum: ["candidate", "verified", "rejected"] }, limit: { type: "number" } } } },
  { name: "query_operator_follow_through", description: "Refresh and read the proposal-only operator follow-through ledger for remediation, playbook, and execution-bridge decisions.", inputSchema: { type: "object", properties: {} } },
  { name: "query_paper_audit", description: "Run a strict audit-only paper inspection that reports findings without writing or repairing .dove artifacts.", inputSchema: { type: "object", properties: { scope: { type: "string" } } } },
  { name: "query_dove_onboarding", description: "Map project-local paper artifacts as a proposal-only Dove onboarding query without moving, rewriting, or persisting source assets.", inputSchema: { type: "object", properties: { maxDepth: { type: "number" }, maxFiles: { type: "number" }, excludeDirs: { type: "array", items: { type: "string" } }, writeMap: { type: "boolean" } } } },
  { name: "query_paper_pipeline", description: "Read the paper-domain lifecycle state from durable Dove artifacts without executing commands, running external processes, inspecting git, or writing state.", inputSchema: { type: "object", properties: {} } },
  { name: "query_dove_orchestrate", description: "Route one Dove mission to the next Dove command without writing, refreshing, running tests, or inspecting git.", inputSchema: { type: "object", properties: { request: { type: "string" }, userRequest: { type: "string" }, goal: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, targetArtifacts: { type: "array", items: { type: "string" } }, artifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, acceptanceChecks: { type: "array", items: { type: "string" } }, allowAutonomy: { type: "boolean" } } } },
  { name: "query_dove_mission", description: "Frame one proposal-only Dove mission contract from the current authoritative .dove workspace without writing durable state.", inputSchema: { type: "object", properties: { goal: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, targetArtifacts: { type: "array", items: { type: "string" } }, artifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, acceptanceChecks: { type: "array", items: { type: "string" } }, nextCommand: { type: "string" } } } },
  { name: "query_dove_mission_board", description: "Read the low-level Dove mission board/debug view from authoritative .dove state without writing, refreshing, running tests, or inspecting git; ordinary mission-list prompts should use query_dove_status and expand statusHome.optionalMissionDetails instead.", inputSchema: { type: "object", properties: { domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, packetId: { type: "string" }, packetIds: { type: "array", items: { type: "string" } }, missionPacketId: { type: "string" }, missionPacketIds: { type: "array", items: { type: "string" } }, status: { type: "string" }, statuses: { type: "array", items: { type: "string" } }, includeArchived: { type: "boolean" } } } },
  { name: "query_dove_status", description: "Read the compact Dove status translator by default: the compact MCP result exposes summary/headline, nextStep, needsAttention, changes, and showMore so ordinary operators see one-sentence state, one recommended action, and explicit expansion paths instead of packet/mission/boundary/gap internals. `statusHome.durableContextNotice`, mutationRollbackModel, patch-plan plus host-tracked file-edit requirements, host checkpoint verification limits, unverified direct-process writes, statusHome.preActionGuidance, automatic read-only lesson recall, Planner/Builder/Reviewer role framing, blockers/reconciliation, project state, runtime/dashboard details, and raw action candidates are available only through resultMode: full/debug or detail/full expansion. Default compact status must not render a Missions panel, blocked counts, execution-gap counts, required-evidence blocks, or ask whether to modify mission statuses. Pass showMissions/includeMissionDetails (or detail/view: missions) only for explicit ordinary mission-list prompts; pass requestStatusAdjustment/includeStatusAdjustmentPreview only when the operator explicitly asks to change mission states after a fresh status read; host/context rollback must use mutationMode: patch-plan applied through host-tracked file edits, not git detection, not direct-process, and not reset_dove_version.", inputSchema: { type: "object", properties: { domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, packetId: { type: "string" }, packetIds: { type: "array", items: { type: "string" } }, status: { type: "string" }, statuses: { type: "array", items: { type: "string" } }, includeArchived: { type: "boolean" }, intent: { type: "string", enum: ["project-status", "health-check", "contract-test"] }, detail: { type: "string" }, view: { type: "string" }, resultMode: { type: "string" }, full: { type: "boolean" }, includeDetails: { type: "boolean" }, showMissions: { type: "boolean" }, includeMissionDetails: { type: "boolean" }, requestStatusAdjustment: { type: "boolean" }, includeStatusAdjustmentPreview: { type: "boolean" } } } },
  { name: "publish_dove_status", description: "Publish sanitized public Dove project progress artifacts to .dove/public/status.json, status.md, and index.html without exposing raw transcripts, secrets, runtime entries, or starting external tunnels.", inputSchema: { type: "object", properties: { includeArchived: { type: "boolean" }, responseLanguage: { type: "string" }, generatedAt: { type: "string" } } } },
  { name: "publish_dove_global_status", description: "Publish a single global static Dove status index from explicit or configured project .dove/public artifacts without scanning the computer, exposing local roots, or starting external tunnels.", inputSchema: { type: "object", properties: { projectRoots: { type: "array", items: { type: "string" } }, projects: { type: "array", items: { type: "object" } }, outputDir: { type: "string" }, refresh: { type: "boolean" }, includeConfig: { type: "boolean" }, includeArchived: { type: "boolean" }, responseLanguage: { type: "string" }, generatedAt: { type: "string" } } } },
  { name: "query_document_ledger", description: "Read the proposal-only Dove document/evidence ledger without writing state or exposing document bodies.", inputSchema: { type: "object", properties: { packetId: { type: "string" }, taskPacketId: { type: "string" }, missionPacketId: { type: "string" }, taskId: { type: "string" }, documentKind: { type: "string" }, status: { type: "string" }, evidenceScope: { type: "string" }, publicSafe: { type: "boolean" }, limit: { type: "number" } } } },
  { name: "record_document_evidence", description: "Record an explicit task-scoped Dove document/evidence ledger entry with Builder/researcher preActionGuidanceSummary, optionally creating or appending a .dove/documents archive document for internal summaries, pressure-test reports, and synthesized outputs while preserving source/artifact provenance; do not overwrite existing documents or capture raw transcripts/private reasoning.", inputSchema: { type: "object", properties: withTaskTarget({ id: { type: "string" }, documentId: { type: "string" }, title: { type: "string" }, documentTitle: { type: "string" }, documentPath: { type: "string" }, path: { type: "string" }, documentKind: { type: "string" }, kind: { type: "string" }, status: { type: "string" }, evidenceScope: { type: "string" }, scope: { type: "string" }, publicSafe: { type: "boolean" }, visibility: { type: "string" }, summary: { type: "string" }, context: { type: "string" }, reason: { type: "string" }, sourceRefs: { type: "array", items: { type: "string" } }, sourceIds: { type: "array", items: { type: "string" } }, artifactRefs: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, evidenceLinks: { type: "array", items: { type: "string" } }, evidencePaths: { type: "array", items: { type: "string" } }, claimIds: { type: "array", items: { type: "string" } }, claims: { type: "array", items: { type: "string" } }, createDocument: { type: "boolean" }, appendDocument: { type: "boolean" }, body: { type: "string" }, content: { type: "string" }, createdBy: { type: "string" }, actorRole: { type: "string" } }) } },
  { name: "query_dove_audit", description: "Inspect Dove mission audit findings and return readiness without writing, refreshing, fixing, running tests, or inspecting git.", inputSchema: { type: "object", properties: { scope: { type: "string" }, goal: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, targetArtifacts: { type: "array", items: { type: "string" } }, artifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, acceptanceChecks: { type: "array", items: { type: "string" } }, validationEvidencePaths: { type: "array", items: { type: "string" } }, validationEvidence: { type: "array", items: { type: "string" } }, evidencePaths: { type: "array", items: { type: "string" } }, changedFilePaths: { type: "array", items: { type: "string" } }, changedFiles: { type: "array", items: { type: "string" } }, changedPaths: { type: "array", items: { type: "string" } }, testEvidencePaths: { type: "array", items: { type: "string" } }, testPaths: { type: "array", items: { type: "string" } }, validationOutputPaths: { type: "array", items: { type: "string" } }, validationLogPaths: { type: "array", items: { type: "string" } }, testOutputPaths: { type: "array", items: { type: "string" } }, validationOutputs: { type: "array", items: { type: "string" } }, validationOutput: { type: "string" }, reviewEvidencePaths: { type: "array", items: { type: "string" } }, reviewEvidence: { type: "array", items: { type: "string" } } } } },
  { name: "query_dove_return", description: "Inspect Dove mission return readiness from durable .dove state without writing, fixing, running tests, or inspecting git.", inputSchema: { type: "object", properties: { goal: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, scope: { type: "string" }, targetArtifacts: { type: "array", items: { type: "string" } }, artifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, acceptanceChecks: { type: "array", items: { type: "string" } }, validationEvidencePaths: { type: "array", items: { type: "string" } }, validationEvidence: { type: "array", items: { type: "string" } }, evidencePaths: { type: "array", items: { type: "string" } }, changedFilePaths: { type: "array", items: { type: "string" } }, changedFiles: { type: "array", items: { type: "string" } }, changedPaths: { type: "array", items: { type: "string" } }, testEvidencePaths: { type: "array", items: { type: "string" } }, testPaths: { type: "array", items: { type: "string" } }, validationOutputPaths: { type: "array", items: { type: "string" } }, validationLogPaths: { type: "array", items: { type: "string" } }, testOutputPaths: { type: "array", items: { type: "string" } }, validationOutputs: { type: "array", items: { type: "string" } }, validationOutput: { type: "string" }, reviewEvidencePaths: { type: "array", items: { type: "string" } }, reviewEvidence: { type: "array", items: { type: "string" } } } } },
  { name: "init_dove_goal", description: "Create or update the unique level-0 Dove init goal task.", inputSchema: { type: "object", properties: { id: { type: "string" }, goal: { type: "string" }, title: { type: "string" }, objective: { type: "string" }, summary: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, status: { type: "string" }, artifactRefs: { type: "array", items: { type: "string" } } } } },
  { name: "create_dove_task", description: "Convert a user demand into a proposal-only mission work contract with compact task card and preActionGuidance; materialization must replay the complete returned confirmArgs as trusted-local exact replay data, including canonical proposalWorkspace, proposalVersion, exact id and proposalDigest, and the same mutationMode so a newly inferred, stale, cross-workspace, field-drifted, or mode-switched contract cannot be substituted. The local digest is exact replay data, not proof of human approval or tamper-proof authentication. Returns recommended handoff routes without executing or recording a pass.", inputSchema: { type: "object", properties: { ...missionReplayProps, objective: { type: "string" }, prompt: { type: "string" }, initGoal: { type: "string" }, projectTitle: { type: "string" }, workspaceTitle: { type: "string" }, projectObjective: { type: "string" }, projectGoal: { type: "string" }, projectDomain: { type: "string" }, missionStage: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, taskLevel: { type: "number" }, missionLevel: { type: "number" }, subtasks: { type: "array", items: { type: ["object", "string"] } }, systemTasks: { type: "array", items: { type: ["object", "string"] } }, dependencyIds: { type: "array", items: { type: "string" } }, blockerIds: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, deliverables: { type: "array", items: { type: "string" } }, outOfScope: { type: "array", items: { type: "string" } }, outOfScopeItems: { type: "array", items: { type: "string" } }, evidenceContract: { type: "array", items: { type: "string" } }, doneCriteria: { type: "array", items: { type: "string" } }, practicalImpact: { type: "string" }, recommendedRoutes: { type: "array", items: { type: "object" } } } } },
  { name: "record_dove_mission_pass", description: "Explicitly record execution results for an existing durable mission/task after the work has already happened, converting completed plan outputs into pending missions when supplied, and returning a localized resultCard summary. This is not part of create_dove_task or /dove:mission materialization. When a host-side search/fetch/shell/MCP safety classifier or tool-availability failure prevents normal completion, record resultStatus blocked with boundaryType host-tool-blocked so the task is visibly blocked instead of left in-progress.", inputSchema: { type: "object", properties: withTaskTarget({ id: { type: "string" }, runId: { type: "string" }, resultStatus: { type: "string" }, taskStatus: { type: "string" }, missionStatus: { type: "string" }, completeTask: { type: "boolean" }, complete: { type: "boolean" }, completeOnSuccess: { type: "boolean" }, blocked: { type: "boolean" }, resultSummary: { type: "string" }, summary: { type: "string" }, reason: { type: "string" }, outcome: { type: "string" }, stopReason: { type: "string" }, evidenceLinks: { type: "array", items: { type: "string" } }, evidencePaths: { type: "array", items: { type: "string" } }, artifactRefs: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, ...executionVerificationProps, command: { type: "string" }, workflow: { type: "string" }, preset: { type: "string" }, nextCommand: { type: "string" }, nextAction: { type: "string" }, ...missionPassBoundaryProps, startedAt: { type: "string" }, completedAt: { type: "string" }, convertPlanToMissions: { type: "boolean" }, planConversion: { type: "object", properties: { plannedMissions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } }, resultingMissions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } }, missions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } }, childMissions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } } } }, plannedMissions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } }, resultingMissions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } }, missions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } }, childMissions: { type: "array", items: { type: ["object", "string"], properties: planMissionProps } } }) } },
  { name: "apply_dove_status_adjustments", description: "Preview compact status adjustment cards and apply explicitly confirmed status choices to non-init Dove task packets only after the single /dove:status confirmation dialog yields clear packetId-to-status adjustments; confirmed calls return a localized resultCard.", inputSchema: { type: "object", properties: { confirmed: { type: "boolean" }, confirm: { type: "boolean" }, runId: { type: "string" }, adjustments: { type: "array", items: statusAdjustmentItemSchema }, statusAdjustments: { type: "array", items: statusAdjustmentItemSchema }, items: { type: "array", items: statusAdjustmentItemSchema } } } },
  {
    name: "run_dove_auto",
    description: "Run demand-to-task intake with compact task/auto cards and preActionGuidance, then after explicit confirmation run bounded foreground iterations until completion or a boundary. Source-research auto runs should collect concrete URLs/templates/guidelines and synthesis text in the foreground host pass, never claim source research succeeded when search/fetch returned zero results or safety errors, then call confirmed run_dove_auto once with explicit source and note steps; calling with only a packet id records a source-requires-host-provenance boundary and does not advance research. Missing source/note/draft/experience/review-loop material becomes an explicit blocked host-pass boundary instead of placeholder writes, and host-side safety classifier or tool-availability failures must be recorded as blocked host-tool-blocked mission results; no hidden continuation, scheduler, or daemon is started.",
    inputSchema: {
      type: "object",
      properties: withTaskTarget({
        ...missionReplayProps,
        index: { type: "number" },
        proposalKind: { type: "string", enum: ["selection", "demand"] },
        confirmed: { type: "boolean" },
        confirm: { type: "boolean" },
        goal: { type: "string" },
        objective: { type: "string" },
        prompt: { type: "string" },
        title: { type: "string" },
        summary: { type: "string" },
        stage: { type: "string" },
        domain: { type: "string" },
        initId: { type: "string" },
        initTitle: { type: "string" },
        initObjective: { type: "string" },
        initGoal: { type: "string" },
        projectTitle: { type: "string" },
        workspaceTitle: { type: "string" },
        projectObjective: { type: "string" },
        projectGoal: { type: "string" },
        initDomain: { type: "string" },
        projectDomain: { type: "string" },
        initStatus: { type: "string" },
        initArtifactRefs: { type: "array", items: { type: "string" } },
        maxIterations: { type: "number" },
        maxSteps: { type: "number" },
        completeTask: { type: "boolean" },
        completeOnSuccess: { type: "boolean" },
        ...executionEvidenceProps,
        steps: { type: "array", items: contractAwareAutoStepSchema },
        runId: { type: "string" }
      })
    }
  },
  { name: "run_dove_operator", description: "Preview compact queue summary/cards with planner preActionGuidance and read-only lesson recall by default, optionally returning full queue details only when includeQueueDetails is true; after explicit confirmation run one foreground operator pass across safe internal steps and canonical host-supplied taskResults. Queue previews, task lists, and status panels are not progress. Host-pass work without taskResults remains unchanged and is returned with material-specific host-pass requiredActions; blocked host results use boundaryType host-tool-blocked when appropriate. Blocked work is proposal-only by default, and pending blocker-investigation plan missions are created only when blockerInvestigationMode is create. Returns created/reused blocker counts and a localized resultCard; no scheduler or hidden runtime.", inputSchema: { type: "object", properties: { confirmed: { type: "boolean" }, includeQueueDetails: { type: "boolean" }, blockerInvestigationMode: { type: "string", enum: ["none", "propose", "create"] }, runId: { type: "string" }, taskResults: { type: "array", items: { type: "object", properties: operatorTaskResultProps, required: ["packetId", "resultStatus"], additionalProperties: false } } } } },
  { name: "kill_dove_task", description: "Internal guarded capability to kill a non-init Dove task; public operators normally choose killed through /dove:status status adjustment UX.", inputSchema: { type: "object", properties: { packetId: { type: "string" }, taskPacketId: { type: "string" }, taskId: { type: "string" }, id: { type: "string" }, target: { type: "string" }, taskName: { type: "string" }, title: { type: "string" }, index: { type: "number" }, reason: { type: "string" }, killReason: { type: "string" } } } },
  { name: "reset_dove_version", description: "Create a direction-change snapshot and clear active tasks except the level-0 init task. This is not a .dove rollback restore entrypoint; host/context rollback should use mutationMode: patch-plan applied through host-tracked file edits before relying on the host native checkpoint.", inputSchema: { type: "object", properties: { id: { type: "string" }, versionId: { type: "string" }, title: { type: "string" }, reason: { type: "string" }, summary: { type: "string" } } } },
  { name: "run_experience_workflow", description: "Plan, record, audit, and bridge experiment experience into claim state after resolving the durable task packet; every new experience requires a real experiment goal, title, idea, or experimentId, and missing objectives should become an explicit host-pass boundary instead of a placeholder plan. Returns Builder/experiment-planner preActionGuidance with read-only lesson recall, audit gate, and claim-bridge boundary.", inputSchema: { type: "object", properties: withTaskTarget({ id: { type: "string" }, experimentId: { type: "string" }, goal: { type: "string" }, idea: { type: "string" }, title: { type: "string" }, methodology: { type: "string" }, method: { type: "string" }, successMetric: { type: "string" }, metric: { type: "string" }, comparisonTargets: { type: "array", items: { type: "string" } }, baselines: { type: "array", items: { type: "string" } }, claimId: { type: "string" }, plan: { type: "object", properties: experiencePlanProps, additionalProperties: false }, result: { type: "object", properties: experienceResultProps, additionalProperties: false }, resultId: { type: "string" }, outcome: { type: "string" }, summary: { type: "string" }, resultSummary: { type: "string" }, evidenceLinks: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } } }) } },
  { name: "prepare_audio_review", description: "Prepare an isolated audio review input bundle containing only task summary, final plan/result paths, explicit artifacts, canonical reviewed-artifact snapshots (path, sizeBytes, sha256), an exact-set hash, instructions, and output contract; result cards include Reviewer preActionGuidanceSummary and preserve the no-private-transcript boundary.", inputSchema: { type: "object", properties: withTaskTarget({ runId: { type: "string" }, instructions: { type: "string" }, finalPlanPaths: { type: "array", items: { type: "string" } }, planPaths: { type: "array", items: { type: "string" } }, finalResultPaths: { type: "array", items: { type: "string" } }, resultPaths: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, reviewedArtifactPaths: { type: "array", items: { type: "string" } }, contextPolicy: { type: "string" } }) } },
  { name: "import_audio_review", description: "Import only a declared audio review handoff and report after verifying the prepared input hash, exact reviewed-artifact path set, and every current artifact size/hash before any review-state write. Public handoff fields, reviewerId, verdict, report, and manifest prove snapshot/report integrity only and cannot authenticate Reviewer authority; a public coherent import remains Reviewer-owned with reviewRequiredBeforeFinalize until current authorized independent proof is submitted through the private capability. Verification failure requires reprepare/review, the result returns Reviewer preActionGuidanceSummary, and private reviewer transcripts are never imported.", inputSchema: { type: "object", properties: withTaskTarget({ runId: { type: "string" }, handoffPath: { type: "string" }, reportPath: { type: "string" } }) } },
  { name: "run_audio_review", description: "Prepare an isolated audio review and import a handoff if one is already present; otherwise return import instructions plus a localized resultCard with Reviewer preActionGuidanceSummary and explicit handoff boundary. An existing caller-authored coherent return cannot transfer ownership to Planner or clear the finalization review gate without current authorized independent Reviewer proof.", inputSchema: { type: "object", properties: withTaskTarget({ runId: { type: "string" }, scope: { type: "string" }, instructions: { type: "string" }, finalPlanPaths: { type: "array", items: { type: "string" } }, finalResultPaths: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, reviewedArtifactPaths: { type: "array", items: { type: "string" } }, handoffPath: { type: "string" }, reportPath: { type: "string" } }) } },
  { name: "run_dove_review_loop", description: "Run one independent local Reviewer pass over the resolved packet and its descendants. The call never edits draft or experience material and never performs an implicit Reviewer-to-Builder-to-Reviewer cycle. A non-coherent verdict returns an explicit Builder handoff; revisions and any later review require new explicit calls.", inputSchema: { type: "object", properties: withTaskTarget({ runId: { type: "string" }, scope: { type: "string" }, instructions: { type: "string" }, stage: { type: "string" }, summary: { type: "string" }, artifactPaths: { type: "array", items: { type: "string" } }, reviewedArtifactPaths: { type: "array", items: { type: "string" } }, responseLanguage: { type: "string" }, language: { type: "string" } }), additionalProperties: false } },
  { name: "launch_dove_mission", description: "Fail-closed public mission launch boundary. Mission packet creation requires a pre-existing, dynamically valid system-owned program approval, program run, runtime execution claim, and trusted authority provenance; caller-supplied roles never grant authority.", inputSchema: { type: "object", properties: { sourceType: { type: "string" }, sourceId: { type: "string" }, goal: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, targetArtifacts: { type: "array", items: { type: "string" } }, artifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, acceptanceChecks: { type: "array", items: { type: "string" } }, acceptanceCriteria: { type: "array", items: { type: "string" } }, returnProtocol: { type: "string" }, packetId: { type: "string" }, missionPacketId: { type: "string" }, followThroughId: { type: "string" }, selectedConversionPathKey: { type: "string" }, title: { type: "string" }, summary: { type: "string" }, currentFocus: { type: "string" }, nextAction: { type: "string" }, dependencies: { type: "array", items: { type: "string" } }, evidenceLinks: { type: "array", items: { type: "string" } }, outputPaths: { type: "array", items: { type: "string" } }, status: { type: "string" }, nextCommand: { type: "string" }, decisionSummary: { type: "string" }, rationale: { type: "string" }, executeBy: { type: "string" }, reviewAfter: { type: "string" } } } },
  { name: "query_program_approvals", description: "Read durable program approvals, runs, and review checkpoints.", inputSchema: { type: "object", properties: { programId: { type: "string" }, programRunId: { type: "string" }, status: { type: "string" } } } },
  { name: "query_campaigns", description: "Read durable multi-cycle campaign plans and their linked program/run state without executing work.", inputSchema: { type: "object", properties: { campaignId: { type: "string" }, status: { type: "string" } } } },
  { name: "query_boundary_report", description: "Read the workflow-pack boundary report for managed versus user-owned state.", inputSchema: { type: "object", properties: {} } },
  { name: "read_role_context_manifest", description: "Refresh and read a narrower per-role context manifest.", inputSchema: { type: "object", properties: { roleId: { type: "string" } } } },
  { name: "read_phase_context_manifest", description: "Refresh and read a phase-scoped context manifest.", inputSchema: { type: "object", properties: { phaseId: { type: "string" } } } },
  { name: "read_packet_context_manifest", description: "Refresh and read a packet-scoped context manifest with dependency and resume guidance.", inputSchema: { type: "object", properties: { packetId: { type: "string" } } } },
  { name: "read_artifact_context_manifest", description: "Refresh and read an artifact-scoped local context manifest tied to a durable path.", inputSchema: { type: "object", properties: { artifactPath: { type: "string" } } } },
  { name: "read_action_context_bundle", description: "Refresh and read an explicit pre-action local-context bundle for the current, role, phase, packet, or artifact scope.", inputSchema: { type: "object", properties: { scopeType: { type: "string" }, roleId: { type: "string" }, phaseId: { type: "string" }, packetId: { type: "string" }, artifactPath: { type: "string" } } } },
  { name: "summarize_session_journal", description: "Refresh and summarize durable session/workspace persistence surfaces.", inputSchema: { type: "object", properties: {} } },
  {
    name: "upsert_orchestration_board",
    description: "Update non-authority fields on the canonical orchestration board under .dove/orchestration/board.json without changing its durable role owner.",
    inputSchema: { type: "object", properties: { objective: { type: "string" }, phase: { type: "string" }, intentType: { type: "string" }, currentFocus: { type: "string" }, nextAction: { type: "string" }, continuationState: { type: "object" }, tasks: { type: "array", items: { type: "object" } }, blockers: { type: "array", items: { type: "object" } }, evidenceLinks: { type: "array", items: { type: "string" } }, experimentIds: { type: "array", items: { type: "string" } }, rebuttalIssueIds: { type: "array", items: { type: "string" } }, activeComparisonTargets: { type: "array", items: { type: "string" } }, versionLineage: { type: "object" } } }
  },
  {
    name: "append_handoff",
    description: "Append a durable checkpoint for the current board owner without accepting caller-selected role ownership or transfer.",
    inputSchema: { type: "object", properties: { phase: { type: "string" }, intentType: { type: "string" }, summary: { type: "string" }, currentFocus: { type: "string" }, nextAction: { type: "string" }, nextActions: { type: "array", items: { type: "string" } }, evidenceLinks: { type: "array", items: { type: "string" } }, blockerIds: { type: "array", items: { type: "string" } } } }
  },
  {
    name: "update_research_brief",
    description: "Update only the durable research objective, agenda, and evidence backlog. This public write cannot change workflow phase or role ownership. Task-scoped writes resolve to a durable packet first; missing or ambiguous targets require explicit packet confirmation unless there is one unique high-confidence autoSelect candidate.",
    inputSchema: { type: "object", properties: withTaskTarget({ objective: { type: "string" }, agenda: { type: "array", items: { type: "string" } }, evidenceBacklog: { type: "array", items: { type: "string" } } }) }
  },
  {
    name: "register_source",
    description: "Register or update one or more external source records as candidates with Builder/researcher preActionGuidanceSummary and packet binding. Registration always creates lifecycle candidate and cannot directly support claims or completion; sources/index.json and sources/verifications.json are bookkeeping ledgers, while typed source:<id> references may support completion only after latest-decision, fingerprint, and packet-binding eligibility checks; final summaries must separate verified registered sources from candidate links. Use verify_source only after independently checking source material; caller-provided verified flags, verification fields, role strings, tokens, or capabilities are rejected. Use `sources: [...]` for batch venue/template/guideline/ranking intake; each new source must include a real title or locator. Do not register sources whose origin/abstract says search returned zero results, safe-domain verification failed, or fetch/retrieval was blocked; surface a host-tool-blocked boundary until verifiable source evidence exists. Final user-facing summaries must separate verified registered sources from candidate links and blocked retrieval candidates instead of putting unverified candidates under generic Sources. Internal pressure-test summaries or writing-preference synthesis belong in `upsert_note` or `record_document_evidence`, not source. Task-scoped writes resolve to a durable packet first; missing or ambiguous targets require explicit packet confirmation unless there is one unique high-confidence autoSelect candidate.",
    inputSchema: { type: "object", properties: withTaskTarget({ sourceId: { type: "string" }, citationKey: { type: "string" }, title: { type: "string" }, authors: { type: "array", items: { type: "string" } }, year: { type: ["string", "number"] }, locator: { type: "string" }, sourceType: { type: "string" }, abstract: { type: "string" }, origin: { type: "string" }, sources: { type: "array", items: { type: "object", properties: { sourceId: { type: "string" }, citationKey: { type: "string" }, title: { type: "string" }, authors: { type: "array", items: { type: "string" } }, year: { type: ["string", "number"] }, locator: { type: "string" }, sourceType: { type: "string" }, abstract: { type: "string" }, origin: { type: "string" } } } } }) }
  },
  {
    name: "verify_source",
    description: "Reject one registered candidate source after checking durable evidence. A public rejection must record its method, checkedMaterial, and auditable evidence. Public positive verification is disabled because caller-minted fields cannot self-attest Reviewer/system provenance. A verified source is eligible only when a trusted internal transition records real captured material, its SHA-256 hash, source identity fingerprint, and matching packet binding; legacy positive records without that provenance fail closed and are never backfilled.",
    inputSchema: { type: "object", properties: withTaskTarget({ sourceId: { type: "string" }, decision: { type: "string", enum: ["rejected"] }, method: { type: "string" }, checkedMaterial: { type: "string" }, auditEvidence: { type: "array", minItems: 1, items: { type: "object", properties: { reference: { type: "string", minLength: 1 }, kind: { type: "string", enum: ["source", "capture"] }, observation: { type: "string", minLength: 1 } }, required: ["reference", "kind", "observation"], additionalProperties: false } } }), required: ["sourceId", "decision", "method", "checkedMaterial", "auditEvidence"] }
  },
  {
    name: "upsert_note",
    description: "Create or update a packet-bound structured note linked to one or more registered sources with Builder/researcher preActionGuidanceSummary and evidence guardrails. New notes require real synthesis content: summary, quote, claim, or open question; empty note requests should stop at a host-pass boundary. Use notes for internal synthesis, pressure-test findings, writing-style summaries, and reviewer-preference analysis after external provenance is registered as sources. Task-scoped writes resolve to a durable packet first; missing or ambiguous targets require explicit packet confirmation unless there is one unique high-confidence autoSelect candidate.",
    inputSchema: { type: "object", properties: withTaskTarget({ noteId: { type: "string" }, title: { type: "string" }, sectionId: { type: "string" }, sourceIds: { type: "array", items: { type: "string" } }, summary: { type: "string" }, quotes: { type: "array", items: { type: "string" } }, claims: { type: "array", items: { type: "string" } }, openQuestions: { type: "array", items: { type: "string" } } }) }
  },
  {
    name: "upsert_claims",
    description: "Write claims derived from results into the evidence store after resolving a durable task packet. Every referenced source must be lifecycle verified by a durable verification record whose fingerprint still matches the source identity; candidate, rejected, missing, or identity-mutated sources are rejected before any write. Returns Builder/researcher preActionGuidanceSummary.",
    inputSchema: { type: "object", properties: withTaskTarget({ claims: { type: "array", items: { type: "object", properties: { id: { type: "string" }, text: { type: "string" }, sectionId: { type: "string" }, sourceIds: { type: "array", items: { type: "string" } }, noteIds: { type: "array", items: { type: "string" } }, experimentIds: { type: "array", items: { type: "string" } }, evidenceLinks: { type: "array", items: { type: "string" } }, status: { type: "string" }, confidence: { type: "string" }, gap: { type: "string" } } } } }) }
  },
  {
    name: "upsert_plan",
    description: "Create or update the current plan artifact with Planner preActionGuidanceSummary and scope/gate guardrails. Task-scoped writes resolve to a durable packet first; missing or ambiguous targets require explicit packet confirmation unless there is one unique high-confidence autoSelect candidate.",
    inputSchema: { type: "object", properties: withTaskTarget({ thesis: { type: "string" }, audience: { type: "string" }, sections: { type: "array", items: { type: "string" } }, evidenceGaps: { type: "array", items: { type: "string" } }, milestones: { type: "array", items: { type: "string" } }, figures: { type: "array", items: { type: "string" } }, notes: { type: "string" } }) }
  },
  {
    name: "upsert_outline",
    description: "Create or update the current section outline with Planner preActionGuidanceSummary and draft gate guardrails. Task-scoped writes resolve to a durable packet first; missing or ambiguous targets require explicit packet confirmation unless there is one unique high-confidence autoSelect candidate.",
    inputSchema: { type: "object", properties: withTaskTarget({ sections: { type: "array", items: { type: "object", properties: { id: { type: "string" }, title: { type: "string" }, status: { type: "string" }, goal: { type: "string" }, evidenceFocus: { type: "string" } } } } }) }
  },
  {
    name: "upsert_draft",
    description: "Create or update a section draft under .dove/drafts with Builder/researcher preActionGuidanceSummary. Draft updates require body content; use set_section_status for metadata-only section updates, and stop at a host-pass boundary when no draft body is available. Task-scoped writes resolve to a durable packet first; missing or ambiguous targets require explicit packet confirmation unless there is one unique high-confidence autoSelect candidate.",
    inputSchema: { type: "object", properties: withTaskTarget({ sectionId: { type: "string" }, title: { type: "string" }, body: { type: "string" }, status: { type: "string" }, summary: { type: "string" } }) }
  },
  {
    name: "upsert_experiment_plan",
    description: "Create or update a claim-driven experiment plan after resolving a durable task packet; board role metadata records workflow routing but is not mutation authority.",
    inputSchema: { type: "object", properties: withTaskTarget({ id: { type: "string" }, title: { type: "string" }, claimId: { type: "string" }, hypothesis: { type: "string" }, methodology: { type: "string" }, successMetric: { type: "string" }, comparisonTargets: { type: "array", items: { type: "string" } }, status: { type: "string" }, owner: { type: "string" } }) }
  },
  {
    name: "upsert_experiment_result",
    description: "Create or update a durable experiment result entry after resolving a durable task packet; plan, claim, evidence, audit, bridge, and follow-through contracts govern the mutation rather than board role metadata.",
    inputSchema: { type: "object", properties: withTaskTarget({ result: { type: "object" }, id: { type: "string" }, experimentId: { type: "string" }, claimId: { type: "string" }, outcome: { type: "string" }, summary: { type: "string" }, evidenceLinks: { type: "array", items: { type: "string" } }, comparisonTargets: { type: "array", items: { type: "string" } } }) }
  },
  {
    name: "run_experiment_audit",
    description: "Create or update a durable experiment audit record distinct from raw results; resolves a durable task packet before writing, requires resultId when an experiment has multiple results, and returns Reviewer preActionGuidanceSummary for the audit gate.",
    inputSchema: { type: "object", properties: withTaskTarget({ resultId: { type: "string" }, experimentId: { type: "string" }, reviewedArtifactRefs: { type: "array", items: { type: "string" } }, auditFindings: { type: "array", items: { type: "string" } }, integrityFlags: { type: "array", items: { type: "string" } }, confidence: { type: "string" }, outcomeMapping: { type: "string" } }) }
  },
  {
    name: "bridge_result_to_claim",
    description: "Persist an explicit result-to-claim bridge event and update claim state with Builder/experiment-planner preActionGuidanceSummary; resolves a durable task packet before writing and requires resultId when an experiment has multiple results.",
    inputSchema: { type: "object", properties: withTaskTarget({ resultId: { type: "string" }, experimentId: { type: "string" }, auditIds: { type: "array", items: { type: "string" } }, reason: { type: "string" } }) }
  },
  {
    name: "run_review_loop",
    description: "Run an evidence-aware independent review pass and generate a revision plan after resolving a durable task packet; review artifact and follow-through contracts govern the mutation, while reviewer role metadata frames workflow routing and role-framed preActionGuidance.",
    inputSchema: { type: "object", properties: withTaskTarget({ scope: { type: "string" }, stage: { type: "string" }, artifactPaths: { type: "array", items: { type: "string" } }, reviewedArtifactPaths: { type: "array", items: { type: "string" } } }), additionalProperties: false }
  },
  {
    name: "append_review_log",
    description: "Append a structured manual review entry after resolving a durable task packet; verdict, summary, reviewed-artifact, report, boundary, and follow-through contracts govern the mutation rather than board role metadata.",
    inputSchema: { type: "object", properties: withTaskTarget({ timestamp: { type: "string" }, stage: { type: "string" }, scope: { type: "string" }, verdict: { type: "string" }, summary: { type: "string" }, findings: { type: "array", items: manualReviewFindingSchema }, actionItems: { type: "array", items: { type: "string" } } }) }
  },
  {
    name: "prepare_isolated_review",
    description: "Prepare an isolated reviewer input bundle from explicit Dove artifacts after resolving the durable task packet target, storing canonical reviewed-artifact snapshots (path, sizeBytes, sha256) and an exact-set hash without invoking an external reviewer process; returns Reviewer preActionGuidanceSummary and explicit isolation boundaries.",
    inputSchema: { type: "object", properties: withTaskTarget({ runId: { type: "string" }, scope: { type: "string" }, instructions: { type: "string" }, mediatorRole: { type: "string" }, reviewerRole: { type: "string" }, reviewedArtifactPaths: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } } }) }
  },
  {
    name: "import_isolated_review",
    description: "Import only an isolated review handoff and report after verifying the prepared input hash, manifest/input snapshot equality, exact handoff path set, and current artifact size/hash before any review-state write; verification failure requires reprepare/review, the result returns Reviewer preActionGuidanceSummary, and private transcripts are never imported.",
    inputSchema: { type: "object", properties: withTaskTarget({ runId: { type: "string" }, handoffPath: { type: "string" }, reportPath: { type: "string" } }) }
  },
  {
    name: "upsert_revision_plan",
    description: "Write a manual revision plan artifact after resolving a durable task packet; board role metadata records the resulting workflow route but is not mutation authority.",
    inputSchema: { type: "object", properties: withTaskTarget({ summary: { type: "string" }, items: { type: "array", items: { type: "string" } }, updatedAt: { type: "string" } }) }
  },
  { name: "set_section_status", description: "Update the status and summary for a section after resolving the durable task packet target; returns Planner preActionGuidanceSummary with section gate context.", inputSchema: { type: "object", properties: withTaskTarget({ sectionId: { type: "string" }, status: { type: "string" }, summary: { type: "string" } }) } },
  { name: "sync_checklist", description: "Regenerate the checklist from current state and review findings; returns Planner preActionGuidanceSummary with status gate context.", inputSchema: { type: "object", properties: {} } },
  { name: "sync_citations", description: "Audit citations and regenerate references.bib plus the citation log; returns Builder/researcher preActionGuidanceSummary with citation and evidence guardrails.", inputSchema: { type: "object", properties: { citedOnly: { type: "boolean" }, preservePhase: { type: "boolean" } } } },
  { name: "refresh_wiki", description: "Regenerate the durable research wiki and typed wiki indexes from current sources, notes, claims, and review state; returns Planner preActionGuidanceSummary for reusable context refresh.", inputSchema: { type: "object", properties: {} } },
  { name: "normalize_rebuttal_issues", description: "Normalize reviewer issues into a durable rebuttal issue board after resolving the durable task packet target; issue and follow-through contracts govern the mutation, while reviewer role metadata only frames workflow routing and preActionGuidanceSummary.", inputSchema: { type: "object", properties: withTaskTarget({ issues: { type: "array", items: rebuttalIssueSchema } }) } },
  { name: "build_rebuttal_strategy", description: "Generate the rebuttal strategy and response draft only when the canonical normalized rebuttal issues index contains at least one real issue; otherwise return a no-write missing-required-materials boundary requiring review/import and issue normalization. Durable issue and follow-through contracts govern the mutation, with Builder/revision-lead metadata used for routing guidance.", inputSchema: { type: "object", properties: withTaskTarget({}) } },
  { name: "build_rebuttal", description: "Generate an artifact-backed rebuttal draft only when the canonical normalized rebuttal issues index contains at least one real issue; otherwise return a no-write missing-required-materials boundary requiring review/import and issue normalization.", inputSchema: { type: "object", properties: withTaskTarget({}) } },
  { name: "create_version_snapshot", description: "Snapshot the current paper state and update version lineage after resolving the durable task packet target; coherent-review and experiment-integrity finalization gates govern the mutation, while board role metadata records workflow routing.", inputSchema: { type: "object", properties: withTaskTarget({ versionId: { type: "string" }, label: { type: "string" }, parentVersionId: { type: "string" }, summary: { type: "string" } }) } },
  { name: "compare_versions", description: "Compare two durable paper snapshots and record the comparison after resolving the durable task packet target; snapshot existence, coherent-review, experiment-integrity, and follow-through gates govern the mutation rather than board role metadata.", inputSchema: { type: "object", properties: withTaskTarget({ fromVersionId: { type: "string" }, toVersionId: { type: "string" } }) } },
  { name: "list_artifacts", description: "List the expected Dove artifacts and whether they exist.", inputSchema: { type: "object", properties: {} } },
  { name: "upsert_figure_plan", description: "Write the figure backlog, linkage metadata, generation intent, and artifact contracts after resolving the durable task packet target; returns Builder preActionGuidanceSummary with provenance and QA gates.", inputSchema: { type: "object", properties: withTaskTarget({ items: { type: "array", items: { type: "object" } } }) } },
  { name: "run_figure_workflow", description: "Turn one user-described figure intent into a packet-scoped figure plan, material discovery, optional generation/import, caption/provenance, and QA status; returns Builder preActionGuidance with artifact-provenance and QA gates. Use providerId none for a first-class plan-only/manual-output path that records backlog, material bundle, generation prompt, and an awaiting-provider-output or missing-required-materials boundary without routing the operator to upsert_figure_plan. Use providerId gpt-image2 only for explicit foreground OpenAI image generation with OPENAI_API_KEY supplied through the environment, never as an inline secret.", inputSchema: { type: "object", properties: withTaskTarget({ intent: { type: "string" }, description: { type: "string" }, name: { type: "string" }, title: { type: "string" }, figureId: { type: "string" }, id: { type: "string" }, purpose: { type: "string" }, captionIntent: { type: "string" }, targetClaimIds: { type: "array", items: { type: "string" } }, claimIds: { type: "array", items: { type: "string" } }, sourceSections: { type: "array", items: { type: "string" } }, sectionIds: { type: "array", items: { type: "string" } }, sourceArtifactPaths: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, relatedExperimentIds: { type: "array", items: { type: "string" } }, experimentIds: { type: "array", items: { type: "string" } }, reviewConcernIds: { type: "array", items: { type: "string" } }, rebuttalIssueIds: { type: "array", items: { type: "string" } }, requiredVisualElements: { type: "array", items: { type: "string" } }, materialHints: { type: "array", items: { anyOf: [{ type: "string" }, figureMaterialItemSchema] } }, materialRequirements: { type: "array", items: figureMaterialItemSchema }, providerId: { type: "string" }, executeProvider: { type: "boolean" }, allowMissingMaterials: { type: "boolean" }, runId: { type: "string" }, outputFormat: { type: "string" }, constraints: { type: "array", items: { type: "string" } }, outputManifestPath: { type: "string" }, sourceSvgPath: { type: "string" }, targetFinalSvgPath: { type: "string" }, svgContent: { type: "string" }, caption: { type: "string" }, captionDraft: { type: "string" }, captionId: { type: "string" } }) } },
  { name: "prepare_figure_generation", description: "Discover required figure materials and write a durable generation input bundle after resolving the durable task packet target; returns Builder preActionGuidanceSummary while keeping material provenance and provider boundaries explicit. providerId gpt-image2 selects the built-in OpenAI image provider only for explicit foreground execution with OPENAI_API_KEY supplied through the environment, never as an inline secret.", inputSchema: { type: "object", properties: withTaskTarget({ figureId: { type: "string" }, id: { type: "string" }, runId: { type: "string" }, providerId: { type: "string" }, constraints: { type: "array", items: { type: "string" } }, outputFormat: { type: "string" }, materialHints: { type: "array", items: { anyOf: [{ type: "string" }, figureMaterialItemSchema] } }, executeProvider: { type: "boolean" }, allowMissingMaterials: { type: "boolean" } }) } },
  { name: "import_figure_generation", description: "Import a declared generated figure output, validate SVG safety, write caption/provenance, refresh figure QA, and return Builder preActionGuidanceSummary with the artifact-provenance gate after resolving the durable task packet target.", inputSchema: { type: "object", properties: withTaskTarget({ figureId: { type: "string" }, id: { type: "string" }, runId: { type: "string" }, outputManifestPath: { type: "string" }, caption: { type: "string" }, captionDraft: { type: "string" }, captionId: { type: "string" }, sourceSvgPath: { type: "string" }, svgContent: { type: "string" } }) } },
  { name: "validate_figure_pipeline", description: "Regenerate durable figure material, generation, caption, and stage-validation QA outputs.", inputSchema: { type: "object", properties: {} } },
  { name: "record_operator_lesson", description: "Record one explicit distilled operator lesson with problem, decisions, pitfalls, validation, and next-time guidance; action surfaces auto-recall lessons read-only, but recording never happens implicitly and raw runtime traces are rejected.", inputSchema: { type: "object", properties: withTaskTarget({ id: { type: "string" }, title: { type: "string" }, problem: { type: "string" }, decisions: { type: "array", items: { type: "string" } }, pitfalls: { type: "array", items: { type: "string" } }, validation: { type: "array", items: { type: "string" } }, nextTime: { type: "array", items: { type: "string" } }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, actorRole: { type: "string" }, tags: { type: "array", items: { type: "string" } }, sourceType: { type: "string" }, sourceId: { type: "string" }, sourceArtifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, sourceArtifactPath: { type: "string" }, relatedPacketIds: { type: "array", items: { type: "string" } }, packetIds: { type: "array", items: { type: "string" } }, taskPacketIds: { type: "array", items: { type: "string" } }, missionPacketIds: { type: "array", items: { type: "string" } }, taskIds: { type: "array", items: { type: "string" } }, recommendationIds: { type: "array", items: { type: "string" } }, playbookIds: { type: "array", items: { type: "string" } }, remediationPackIds: { type: "array", items: { type: "string" } }, status: { type: "string" } }) } },
  { name: "record_operator_follow_through", description: "Record a non-executing manual operator acknowledgement or deferral for a remediation pack, family playbook, or execution-bridge candidate. Public callers cannot self-assign roles, accept execution or risk, plan or bind targets, or control runtime/program state.", inputSchema: { type: "object", properties: { id: { type: "string" }, sourceType: { type: "string" }, sourceId: { type: "string" }, status: { type: "string", enum: ["acknowledged", "deferred"] }, decisionSummary: { type: "string" }, rationale: { type: "string" }, selectedConversionPathKey: { type: "string" }, deferUntil: { type: "string" }, reviewAfter: { type: "string" } } } },
  { name: "plan_campaign", description: "Record or update a planner-supervised multi-cycle campaign plan without approving or executing work.", inputSchema: { type: "object", properties: { campaignId: { type: "string" }, title: { type: "string" }, objective: { type: "string" }, status: { type: "string" }, phase: { type: "string" }, actorRole: { type: "string" }, programIds: { type: "array", items: { type: "string" } }, steps: { type: "array", items: { type: "object", properties: { id: { type: "string" }, title: { type: "string" }, status: { type: "string" }, programId: { type: "string" }, programRunId: { type: "string" }, approvalId: { type: "string" }, packetId: { type: "string" }, allowedStepType: { type: "string" }, objective: { type: "string" }, nextAction: { type: "string" }, evidenceLinks: { type: "array", items: { type: "string" } }, outputPaths: { type: "array", items: { type: "string" } }, executeBy: { type: "string" }, reviewAfter: { type: "string" } } } }, nextAction: { type: "string" }, reviewPolicy: { type: "string" }, explicitApprovalRequired: { type: "boolean" }, noHiddenRuntime: { type: "boolean" } } } },
  { name: "revoke_program_approval", description: "Revoke one explicit program approval without executing work.", inputSchema: { type: "object", properties: { approvalId: { type: "string" }, actorRole: { type: "string" }, summary: { type: "string" }, revokeReason: { type: "string" } } } },
  { name: "materialize_guidance_packet", description: "Fail-closed public packet materialization boundary. Durable packet creation requires a pre-existing, dynamically valid system-owned program approval, program run, runtime execution claim, and trusted authority provenance; caller-supplied roles never grant authority.", inputSchema: { type: "object", properties: { sourceType: { type: "string" }, sourceId: { type: "string" }, selectedConversionPathKey: { type: "string" }, packetId: { type: "string" }, followThroughId: { type: "string" }, title: { type: "string" }, summary: { type: "string" }, status: { type: "string" }, currentFocus: { type: "string" }, nextAction: { type: "string" }, dependencies: { type: "array", items: { type: "string" } }, evidenceLinks: { type: "array", items: { type: "string" } }, outputPaths: { type: "array", items: { type: "string" } }, decisionSummary: { type: "string" }, rationale: { type: "string" }, executeBy: { type: "string" }, reviewAfter: { type: "string" }, domain: { type: "string" }, doveDomain: { type: "string" }, missionDomain: { type: "string" }, stage: { type: "string" }, missionStage: { type: "string" }, goal: { type: "string" }, missionGoal: { type: "string" }, objective: { type: "string" }, targetArtifacts: { type: "array", items: { type: "string" } }, artifacts: { type: "array", items: { type: "string" } }, artifactPaths: { type: "array", items: { type: "string" } }, acceptanceCriteria: { type: "array", items: { type: "string" } }, acceptanceChecks: { type: "array", items: { type: "string" } }, returnProtocol: { type: "string" } } } }
];
var toolDefinitions = baseToolDefinitions.map(addMcpControlFields).map((tool) => ({
  ...tool,
  operatorTier: OPERATOR_TOOL_NAMES.includes(tool.name) ? "operator" : "advanced",
  workflowArea: OPERATOR_WORKFLOW_AREAS[tool.name] ?? "advanced",
  primaryEntry: OPERATOR_TOOL_NAMES.includes(tool.name),
  internalOnly: !OPERATOR_TOOL_NAMES.includes(tool.name)
}));
var TOOL_INPUT_SCHEMAS = new Map(
  toolDefinitions.map((tool) => [
    tool.name,
    tool.inputSchema
  ])
);
var TOOL_INPUT_PROPERTY_NAMES = new Map(
  toolDefinitions.map((tool) => [
    tool.name,
    new Set(Object.keys(tool.inputSchema?.properties ?? {}))
  ])
);
function compactOperatorToolSchema(tool) {
  const properties = tool.inputSchema?.properties ?? {};
  const fieldNames = OPERATOR_SCHEMA_FIELDS[tool.name] ?? Object.keys(properties);
  const compactProperties = Object.fromEntries(fieldNames.filter((fieldName) => properties[fieldName] && !isCompactDiscoveryInternalKey(fieldName)).map((fieldName) => [fieldName, compactPublicSchema(properties[fieldName])]));
  return compactPublicSchema({
    type: tool.inputSchema?.type ?? "object",
    properties: compactProperties,
    additionalProperties: false
  });
}
function compactOperatorTool(tool) {
  return {
    name: tool.name,
    description: OPERATOR_PUBLIC_TOOL_DESCRIPTIONS[tool.name] ?? "Use this Dove capability for everyday project work.",
    inputSchema: compactOperatorToolSchema(tool),
    discoverySurface: "operator",
    workflowArea: OPERATOR_WORKFLOW_AREAS[tool.name] ?? "workflow"
  };
}
function toolDefinitionsForSurface(surface = "operator") {
  const normalized = typeof surface === "string" ? surface.trim().toLowerCase() : "operator";
  if (["full", "debug"].includes(normalized)) {
    return toolDefinitions.map((tool) => ({ ...tool, discoverySurface: normalized }));
  }
  return toolDefinitions.filter((tool) => OPERATOR_TOOL_NAMES.includes(tool.name)).map(compactOperatorTool);
}

// src/mcp/schema-validation.mjs
function isPlainObject6(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value);
}
function valueTypeMatches(value, type) {
  if (type === "null") {
    return value === null;
  }
  if (type === "array") {
    return Array.isArray(value);
  }
  if (type === "object") {
    return isPlainObject6(value);
  }
  if (type === "integer") {
    return Number.isInteger(value);
  }
  if (type === "number") {
    return typeof value === "number" && Number.isFinite(value);
  }
  return typeof value === type;
}
function expectedTypeLabel(type) {
  return Array.isArray(type) ? type.join(" or ") : type;
}
function childPath(inputPath, key) {
  return /^[$A-Z_a-z][$0-9A-Z_a-z]*$/u.test(key) ? `${inputPath}.${key}` : `${inputPath}[${JSON.stringify(key)}]`;
}
function validateSchemaValue(value, schema, inputPath) {
  if (!schema || typeof schema !== "object" || Array.isArray(schema)) {
    return null;
  }
  if (Array.isArray(schema.allOf)) {
    for (const branch of schema.allOf) {
      const error = validateSchemaValue(value, branch, inputPath);
      if (error) {
        return error;
      }
    }
  }
  if (schema.if) {
    const conditionError = validateSchemaValue(value, schema.if, inputPath);
    const branch = conditionError ? schema.else : schema.then;
    if (branch) {
      const error = validateSchemaValue(value, branch, inputPath);
      if (error) {
        return error;
      }
    }
  }
  if (Array.isArray(schema.anyOf)) {
    const errors = schema.anyOf.map((branch) => validateSchemaValue(value, branch, inputPath));
    if (errors.every(Boolean)) {
      return errors.at(-1) ?? `${inputPath} does not match any allowed schema.`;
    }
  }
  if (Array.isArray(schema.oneOf)) {
    const matches = schema.oneOf.map((branch) => validateSchemaValue(value, branch, inputPath)).filter((error) => !error).length;
    if (matches !== 1) {
      return `${inputPath} must match exactly one allowed schema.`;
    }
  }
  if (schema.not && !validateSchemaValue(value, schema.not, inputPath)) {
    return `${inputPath} matches a forbidden schema.`;
  }
  if (schema.const !== void 0 && !Object.is(value, schema.const)) {
    return `${inputPath} must equal ${JSON.stringify(schema.const)}.`;
  }
  if (Array.isArray(schema.enum) && !schema.enum.some((item) => Object.is(item, value))) {
    return `${inputPath} must be one of: ${schema.enum.map((item) => JSON.stringify(item)).join(", ")}.`;
  }
  if (schema.type !== void 0) {
    const types = Array.isArray(schema.type) ? schema.type : [schema.type];
    if (!types.some((type) => valueTypeMatches(value, type))) {
      return `${inputPath} must be ${expectedTypeLabel(schema.type)}.`;
    }
  }
  if (typeof value === "string" && typeof schema.pattern === "string") {
    if (!new RegExp(schema.pattern, "u").test(value)) {
      return `${inputPath} does not match the required pattern.`;
    }
  }
  if (typeof value === "number") {
    if (typeof schema.minimum === "number" && value < schema.minimum) {
      return `${inputPath} must be at least ${schema.minimum}.`;
    }
    if (typeof schema.maximum === "number" && value > schema.maximum) {
      return `${inputPath} must be at most ${schema.maximum}.`;
    }
  }
  if (Array.isArray(value)) {
    if (typeof schema.minItems === "number" && value.length < schema.minItems) {
      return `${inputPath} must contain at least ${schema.minItems} item(s).`;
    }
    if (typeof schema.maxItems === "number" && value.length > schema.maxItems) {
      return `${inputPath} must contain at most ${schema.maxItems} item(s).`;
    }
    if (schema.items) {
      for (let index = 0; index < value.length; index += 1) {
        const error = validateSchemaValue(value[index], schema.items, `${inputPath}[${index}]`);
        if (error) {
          return error;
        }
      }
    }
  }
  if (isPlainObject6(value)) {
    const properties = isPlainObject6(schema.properties) ? schema.properties : {};
    if (Array.isArray(schema.required)) {
      const missing = schema.required.find((key) => !Object.hasOwn(value, key));
      if (missing) {
        return `${childPath(inputPath, missing)} is required.`;
      }
    }
    if (schema.additionalProperties === false) {
      const unknown = Object.keys(value).find((key) => !Object.hasOwn(properties, key));
      if (unknown) {
        return `${childPath(inputPath, unknown)} is not allowed.`;
      }
    }
    for (const [key, propertySchema] of Object.entries(properties)) {
      if (!Object.hasOwn(value, key)) {
        continue;
      }
      const error = validateSchemaValue(value[key], propertySchema, childPath(inputPath, key));
      if (error) {
        return error;
      }
    }
  }
  return null;
}
function assertMcpInputSchema(name, args, schema) {
  const error = validateSchemaValue(args, schema, "$");
  if (error) {
    throw new Error(`${name} input is invalid: ${error}`);
  }
}

// src/mcp/handlers.mjs
function makeTextResult(data, { isError = false } = {}) {
  return {
    content: [{ type: "text", text: JSON.stringify(data, null, 2) }],
    ...isError ? { isError: true } : {}
  };
}
function makeStructuredToolResult(name, args, data) {
  return makeTextResult(
    shapeMcpResult(name, args, data),
    {
      isError: MUTATING_TOOL_NAMES.has(name) && isOperationalFailureOutcome(data, {
        confirmed: args?.confirmed === true || args?.confirm === true
      })
    }
  );
}
function makeErrorResult(message) {
  return {
    content: [{ type: "text", text: message }],
    isError: true
  };
}
function isPlainObject7(value) {
  return value && typeof value === "object" && !Array.isArray(value);
}
function normalizeString12(value, fallback = null) {
  if (typeof value !== "string") {
    return fallback;
  }
  const trimmed = value.trim();
  return trimmed || fallback;
}
function normalizeStringArray26(values) {
  if (!Array.isArray(values)) {
    return [];
  }
  return values.map((value) => normalizeString12(value)).filter(Boolean);
}
function uniqueStrings3(values) {
  return [...new Set(normalizeStringArray26(values))];
}
function compactObject3(fields) {
  return Object.fromEntries(Object.entries(fields).filter(([, value]) => {
    if (value === null || value === void 0) {
      return false;
    }
    if (Array.isArray(value)) {
      return value.length > 0;
    }
    if (isPlainObject7(value)) {
      return Object.keys(value).length > 0;
    }
    return true;
  }));
}
var MCP_COMPACT_INTERNAL_KEY_PATTERN = /^(?:id|packetId|packetIds|taskPacketId|missionPacketId|taskId|runId|receiptId|figureId|boundaryId|boundaryType|implementationBoundaryType|implementationReason|command|copyableCommand|operatorRoute|ownerRole|nextRole|handoff|providerId|providerStatus|providerError|apiKeyEnv|artifactRefs|artifactPaths|evidencePaths|validationEvidencePaths|requiredEvidence|resultPath|qaPath|path|paths|documentPath|packetPath|packetContextPath|sourceSvgPath|targetFinalSvgPath|finalSvgPath|outputManifestPath|svgContent|mutationMode|queueSummary|queuePreview|preActionGuidance|preActionGuidanceSummary|fullDetails|fullResult|diagnostics)$/u;
var MCP_COMPACT_INTERNAL_TEXT_PATTERN = /\.dove\/|\bproject:dove\.[a-z0-9.-]+|--packet-id\b|\b(?:packetId|taskPacketId|missionPacketId|taskId|runId|receiptId|boundaryId|boundaryType|mutationMode|patch-plan|direct-process|ownerRole|nextRole|handoff|providerId|sourceSvgPath|targetFinalSvgPath|finalSvgPath|outputManifestPath|svgContent|queueSummary|queuePreview|preActionGuidance|resultCard)\b|\b(?:missing-secret-env|provider-failed|host-tool-blocked|needs-host-results|awaiting-host-pass|awaiting-host-results|audio-review-[a-z-]+)\b|\b(?:query_dove_status|run_dove_auto|record_dove_mission_pass|run_figure_workflow|run_dove_operator|record_document_evidence|upsert_note|upsert_draft|register_source)\b/u;
function isMcpCompactInternalKey(key) {
  return MCP_COMPACT_INTERNAL_KEY_PATTERN.test(String(key ?? "")) || /(?:^|[A-Za-z])Ids?$/u.test(String(key ?? ""));
}
function publicCompactString(value, fallback = null) {
  const text3 = normalizeString12(value, null);
  if (!text3 || MCP_COMPACT_INTERNAL_TEXT_PATTERN.test(text3)) {
    return fallback;
  }
  return text3;
}
function publicCompactValue(value) {
  if (Array.isArray(value)) {
    return Array.from(new Set(value.map((item) => publicCompactValue(item)).filter((item) => item !== null && item !== void 0 && item !== "")));
  }
  if (isPlainObject7(value)) {
    const entries = Object.entries(value).filter(([key]) => !isMcpCompactInternalKey(key)).map(([key, item]) => [key, publicCompactValue(item)]).filter(([, item]) => {
      if (item === null || item === void 0 || item === "") {
        return false;
      }
      if (Array.isArray(item)) {
        return item.length > 0;
      }
      if (isPlainObject7(item)) {
        return Object.keys(item).length > 0;
      }
      return true;
    });
    return compactObject3(Object.fromEntries(entries));
  }
  if (typeof value === "string") {
    return publicCompactString(value);
  }
  return value === null || value === void 0 ? null : value;
}
function publicCompactObject(value) {
  return isPlainObject7(value) ? publicCompactValue(value) : null;
}
function publicCompactStringArray(values) {
  return uniqueStrings3(values).filter((value) => publicCompactString(value));
}
function publicMcpToolSurface(tool) {
  const surfaces = {
    query_dove_status: "status",
    query_dove_orchestrate: "routing",
    query_document_ledger: "evidence",
    query_operator_lessons: "lessons",
    search_network: "search",
    query_network_search_providers: "search",
    create_dove_task: "mission",
    init_dove_goal: "mission",
    record_dove_mission_pass: "result-recording",
    run_dove_auto: "auto",
    run_dove_operator: "operator",
    register_source: "source",
    upsert_note: "note",
    upsert_draft: "draft",
    record_document_evidence: "document",
    run_figure_workflow: "figure",
    run_experience_workflow: "experience",
    run_review_loop: "review",
    run_dove_review_loop: "review-loop",
    build_rebuttal_strategy: "rebuttal",
    build_rebuttal: "rebuttal",
    create_version_snapshot: "version",
    compare_versions: "version",
    query_dove_return: "verification"
  };
  return surfaces[tool] ?? (MUTATING_TOOL_NAMES.has(tool) ? "workflow" : "read-only");
}
function stripMcpControlArgs(args = {}) {
  if (!isPlainObject7(args)) {
    return {};
  }
  const { mutationMode, resultMode, ...rest } = args;
  return rest;
}
function normalizeMcpResultMode(args = {}) {
  const mode = normalizeString12(args?.resultMode, "compact").toLowerCase();
  return ["compact", "full", "debug"].includes(mode) ? mode : "compact";
}
function extractPacketId(args = {}) {
  return args.packetId ?? args.taskPacketId ?? args.missionPacketId ?? args.taskId ?? null;
}
function extractStatus(data) {
  if (!isPlainObject7(data)) {
    return "ok";
  }
  return normalizeString12(data.status ?? data.resultStatus ?? data.taskStatus ?? data.mode, "ok");
}
function extractScope(data) {
  if (!isPlainObject7(data)) {
    return null;
  }
  const statusScope = isPlainObject7(data.statusHome?.scope) ? data.statusHome.scope : {};
  const explicitScope = isPlainObject7(data.scope) ? data.scope : {};
  const statusContext = isPlainObject7(data.statusHome?.currentContext) ? data.statusHome.currentContext : {};
  return compactObject3({
    kind: publicCompactString(statusScope.kind ?? explicitScope.kind ?? (data.figureId ? "figure" : data.packetId || data.taskPacketId || data.missionPacketId ? "task" : "workspace")),
    domain: publicCompactString(statusScope.domain ?? explicitScope.domain ?? statusContext.domain ?? data.domain ?? data.doveDomain ?? data.missionDomain),
    stage: publicCompactString(statusScope.stage ?? explicitScope.stage ?? statusContext.stage ?? data.stage ?? data.missionStage),
    currentFocus: publicCompactString(statusContext.currentFocus ?? explicitScope.currentFocus ?? data.currentFocus),
    title: publicCompactString(statusContext.title ?? explicitScope.title ?? data.projectTitle),
    status: publicCompactString(explicitScope.status ?? extractStatus(data), "ok")
  });
}
function extractArtifactWritePaths(artifactWrites) {
  if (!isPlainObject7(artifactWrites)) {
    return [];
  }
  return uniqueStrings3([
    ...normalizeStringArray26(artifactWrites.primaryArtifactPaths),
    ...normalizeStringArray26(artifactWrites.synthesisArtifactPaths),
    ...normalizeStringArray26(artifactWrites.refreshOnlyArtifactPaths),
    ...normalizeStringArray26(artifactWrites.artifactPaths),
    ...normalizeStringArray26(artifactWrites.paths)
  ]);
}
function extractWrites(data, args = {}) {
  if (!isPlainObject7(data)) {
    return { applied: false, count: 0, paths: [], writeIntent: "none", rollbackEligible: "not-applicable" };
  }
  const mutationSummaryPaths = isPlainObject7(data.mutationSummary) ? data.mutationSummary.paths : null;
  const paths = uniqueStrings3([
    ...normalizeStringArray26(data.writes),
    ...normalizeStringArray26(data.durableWrites),
    ...normalizeStringArray26(data.outputPaths),
    ...normalizeStringArray26(mutationSummaryPaths),
    ...extractArtifactWritePaths(data.artifactWrites),
    ...extractArtifactWritePaths(data.resultCard?.artifactWrites)
  ]);
  const operationCount = Array.isArray(data.mutationPlan?.operations) ? data.mutationPlan.operations.length : 0;
  const mutationMode = normalizeString12(data.mutationMode ?? args?.mutationMode);
  const writesApplied = typeof data.writesApplied === "boolean" ? data.writesApplied : paths.length > 0;
  const hasProposedPatch = !writesApplied && (operationCount > 0 || mutationMode === "patch-plan" && paths.length > 0);
  const writeIntent = writesApplied ? "applied" : hasProposedPatch ? "proposed" : "none";
  const rollbackEligible = writeIntent === "none" ? "not-applicable" : normalizeString12(data.rollbackEligible ?? data.mutationSummary?.rollbackEligible, mutationMode === "patch-plan" ? "host-tracked" : "unverified");
  return compactObject3({
    applied: writesApplied,
    count: paths.length || operationCount,
    paths: paths.slice(0, 10),
    writeIntent,
    rollbackEligible,
    mutationMode: writeIntent === "none" ? null : mutationMode
  });
}
function extractSummary(data) {
  if (!isPlainObject7(data)) {
    return "ok";
  }
  return publicCompactString(
    data.summary ?? data.headline ?? data.statusHome?.headline ?? data.resultCard?.happened ?? data.resultSummary ?? data.outcome,
    extractStatus(data)
  );
}
function publicNextStep(value) {
  if (!isPlainObject7(value)) {
    return null;
  }
  return compactObject3({
    label: publicCompactString(value.label ?? value.title ?? value.summary ?? value.message),
    why: publicCompactString(value.why ?? value.summary ?? value.reason),
    requiredInputs: publicCompactStringArray(value.requiredInputs),
    requiredActions: publicCompactStringArray(value.requiredActions),
    confirmationRequired: value.confirmationRequired === true ? true : void 0
  });
}
function extractNextStep(data) {
  if (!isPlainObject7(data)) {
    return null;
  }
  const statusNextStep = isPlainObject7(data.statusHome?.nextStep) ? data.statusHome.nextStep : null;
  if (statusNextStep) {
    return publicNextStep(statusNextStep);
  }
  const explicitNextStep = isPlainObject7(data.nextStep) ? data.nextStep : null;
  if (explicitNextStep) {
    return publicNextStep(explicitNextStep);
  }
  const resultCardAction = Array.isArray(data.resultCard?.nextActions) ? data.resultCard.nextActions[0] : null;
  return publicNextStep(resultCardAction);
}
function buildChangesContract(writes) {
  return compactObject3({
    intent: writes.writeIntent,
    applied: writes.applied === true,
    count: writes.count ?? 0,
    rollback: writes.rollbackEligible
  });
}
function extractNeedsAttention(data, operatorUnblock) {
  if (!isPlainObject7(data)) {
    return null;
  }
  const statusNeedsAttention = isPlainObject7(data.statusHome?.needsAttention) ? data.statusHome.needsAttention : null;
  if (statusNeedsAttention) {
    return publicCompactObject(statusNeedsAttention);
  }
  const explicitNeedsAttention = isPlainObject7(data.needsAttention) ? data.needsAttention : null;
  if (explicitNeedsAttention) {
    return publicCompactObject(explicitNeedsAttention);
  }
  if (!operatorUnblock) {
    return null;
  }
  return compactObject3({
    status: "blocked",
    summary: publicCompactString(operatorUnblock.summary ?? operatorUnblock.blockedSummary),
    why: publicCompactString(operatorUnblock.why ?? operatorUnblock.cannotContinueBecause),
    needs: publicCompactStringArray(operatorUnblock.needs ?? operatorUnblock.requiredEvidence).slice(0, 8)
  });
}
function extractShowMore(data) {
  const source = isPlainObject7(data?.statusHome?.showMore) ? data.statusHome.showMore : isPlainObject7(data?.showMore) ? data.showMore : null;
  return {
    text: publicCompactString(source?.text, "Ask for full details or diagnostics only when you need the audit view.")
  };
}
function buildMcpResultContract(tool, resultMode, data, args = {}) {
  const writes = extractWrites(data, args);
  const operatorUnblock = buildOperatorUnblock(data);
  const missionConfirmation = ["create_dove_task", "run_dove_auto"].includes(tool) && data?.status === "needs-confirmation" && data?.confirmArgs ? {
    required: true,
    proposalVersion: data.proposalVersion,
    proposalDigest: data.proposalDigest,
    proposalWorkspace: data.proposalWorkspace,
    ...data.proposalKind ? { proposalKind: data.proposalKind } : {},
    mutationMode: data.proposalMutationMode,
    trustBoundary: data.proposalTrust?.boundary ?? "trusted-local-exact-replay-data",
    proofOfHumanApproval: false,
    tamperProof: false,
    confirmArgs: data.confirmArgs
  } : null;
  return compactObject3({
    presentation: "dove-mcp-result-contract",
    tool: publicMcpToolSurface(tool),
    resultMode,
    summary: extractSummary(data),
    scope: extractScope(data),
    nextStep: extractNextStep(data),
    needsAttention: extractNeedsAttention(data, operatorUnblock),
    confirmation: missionConfirmation,
    changes: buildChangesContract(writes),
    showMore: extractShowMore(data),
    detailsAvailable: true
  });
}
function shapeMcpResult(tool, args, data) {
  const resultMode = normalizeMcpResultMode(args);
  const compact = buildMcpResultContract(tool, resultMode, data, args);
  if (resultMode === "full") {
    return { ...compact, fullResult: data };
  }
  if (resultMode === "debug") {
    return { ...compact, fullResult: data, diagnostics: isPlainObject7(data) ? data.diagnostics ?? null : null };
  }
  return compact;
}
function findRetiredGovernanceInput(value, inputPath = "$", seen = /* @__PURE__ */ new WeakSet()) {
  if (!value || typeof value !== "object") {
    return null;
  }
  if (seen.has(value)) {
    return null;
  }
  seen.add(value);
  for (const [key, item] of Object.entries(value)) {
    const itemPath = Array.isArray(value) ? `${inputPath}[${key}]` : `${inputPath}.${key}`;
    if (key.startsWith("policyOverride") || key === "skipBoardUpdate" || key === "skipRefreshDurableSurfaces" || key === "skipFollowThroughReady" || key === "skipSyncPhase") {
      return { key, path: itemPath };
    }
    const nested = findRetiredGovernanceInput(item, itemPath, seen);
    if (nested) {
      return nested;
    }
  }
  return null;
}
function assertNoRetiredGovernanceInputs(name, args = {}) {
  const retired = findRetiredGovernanceInput(args);
  if (retired) {
    throw new Error(
      `${name} no longer accepts retired governance input ${retired.key} at ${retired.path}.`
    );
  }
}
function assertAllowedToolInput(name, args = {}) {
  if (!isPlainObject7(args)) {
    throw new Error(`${name} arguments must be a plain object.`);
  }
  const allowed = TOOL_INPUT_PROPERTY_NAMES.get(name);
  if (!allowed) {
    throw new Error(`Unknown tool: ${name}`);
  }
  const unknown = Object.keys(args).filter((key) => !allowed.has(key));
  if (unknown.length > 0) {
    throw new Error(`${name} does not accept unknown input: ${unknown.map((key) => `$.${key}`).join(", ")}.`);
  }
}
function dispatchToolData(root, name, args = {}) {
  assertNoRetiredGovernanceInputs(name, args);
  const result = (data) => data;
  switch (name) {
    case "ensure_workspace":
      return result(ensureWorkspace(root));
    case "init_project":
      return result(initProject(root, args));
    case "read_state":
      return result(readState(root));
    case "query_task_graph":
      return result(queryTaskGraph(root));
    case "query_open_questions":
      return result(queryOpenQuestions(root));
    case "query_decisions":
      return result(queryDecisions(root));
    case "query_lineage":
      return result(queryLineage(root));
    case "query_workspace_index":
      return result(queryWorkspaceIndex(root));
    case "query_meta_optimize":
      return result(queryMetaOptimize(root));
    case "query_governance_coverage_report":
      return result(queryGovernanceCoverageReport(root));
    case "query_operator_lessons":
      return result(queryOperatorLessons(root, args));
    case "search_network":
      return result(searchNetwork(root, args));
    case "query_network_search_providers":
      return result(queryNetworkSearchProviders(root, args));
    case "query_sources":
      return result(querySources(root, args));
    case "query_operator_follow_through":
      return result(queryOperatorFollowThrough(root));
    case "query_paper_audit":
      return result(queryPaperAudit(root, args));
    case "query_dove_onboarding":
      return result(queryDoveOnboarding(root, args));
    case "query_paper_pipeline":
      return result(queryPaperPipeline(root, args));
    case "query_dove_orchestrate":
      return result(queryDoveOrchestrate(root, args));
    case "query_dove_mission":
      return result(queryDoveMission(root, args));
    case "query_dove_mission_board":
      return result(queryDoveMissionBoard(root, args));
    case "query_dove_status":
      return result(queryDoveStatus(root, args));
    case "publish_dove_status":
      return result(publishDoveStatus(root, args));
    case "publish_dove_global_status":
      return result(publishDoveGlobalStatus(root, args));
    case "query_document_ledger":
      return result(queryDocumentLedger(root, args));
    case "record_document_evidence":
      return result(recordDocumentEvidence(root, args));
    case "query_dove_audit":
      return result(queryDoveAudit(root, args));
    case "query_dove_return":
      return result(queryDoveReturn(root, args));
    case "init_dove_goal":
      return result(initDoveGoal(root, args));
    case "create_dove_task":
      return result(createDoveTask(root, args));
    case "record_dove_mission_pass":
      return result(recordDoveMissionPass(root, args));
    case "run_dove_auto":
      return result(runDoveAuto(root, args));
    case "apply_dove_status_adjustments":
      return result(applyDoveStatusAdjustments(root, args));
    case "run_dove_operator":
      return result(runDoveOperator(root, args));
    case "kill_dove_task":
      return result(killDoveTask(root, args));
    case "reset_dove_version":
      return result(resetDoveVersion(root, args));
    case "run_experience_workflow":
      return result(runExperienceWorkflow(root, args));
    case "prepare_audio_review":
      return result(prepareAudioReview(root, args));
    case "import_audio_review":
      return result(importAudioReview(root, args));
    case "run_audio_review":
      return result(runAudioReview(root, args));
    case "run_dove_review_loop":
      return result(runDoveReviewLoop(root, args));
    case "launch_dove_mission":
      return result(launchDoveMission(root, args));
    case "query_program_approvals":
      return result(queryProgramApprovals(root, args));
    case "query_campaigns":
      return result(queryCampaigns(root, args));
    case "query_boundary_report":
      return result(readBoundaryReport(root));
    case "read_role_context_manifest":
      return result(readRoleContextManifest(root, args.roleId));
    case "read_phase_context_manifest":
      return result(readPhaseContextManifest(root, args.phaseId));
    case "read_packet_context_manifest":
      return result(readPacketContextManifest(root, args.packetId));
    case "read_artifact_context_manifest":
      return result(readArtifactContextManifest(root, args.artifactPath));
    case "read_action_context_bundle":
      return result(readActionContextBundle(root, args));
    case "summarize_session_journal":
      return result(summarizeSessionJournal(root));
    case "upsert_orchestration_board":
      return result(upsertOrchestrationBoard(root, args));
    case "append_handoff":
      return result(appendHandoff(root, args));
    case "update_research_brief":
      return result(updateResearchBrief(root, args));
    case "register_source":
      return result(registerSource(root, args));
    case "verify_source":
      return result(verifySource(root, args));
    case "upsert_note":
      return result(upsertNote(root, args));
    case "upsert_claims":
      return result(upsertClaims(root, args));
    case "upsert_experiment_plan":
      return result(upsertExperimentPlan(root, args));
    case "upsert_experiment_result":
      return result(upsertExperimentResult(root, args));
    case "run_experiment_audit":
      return result(runExperimentAudit(root, args));
    case "bridge_result_to_claim":
      return result(bridgeExperimentResultToClaim(root, args));
    case "upsert_plan":
      return result(upsertPlan(root, args));
    case "upsert_outline":
      return result(upsertOutline(root, args));
    case "upsert_draft":
      return result(upsertDraft(root, args));
    case "run_review_loop":
      return result(runReviewLoop(root, args));
    case "append_review_log":
      return result(appendReviewLog(root, args));
    case "prepare_isolated_review":
      return result(prepareIsolatedReview(root, args));
    case "import_isolated_review":
      return result(importIsolatedReview(root, args));
    case "upsert_revision_plan":
      return result(upsertRevisionPlan(root, args));
    case "set_section_status":
      return result(setSectionStatus(root, args));
    case "sync_checklist":
      return result(syncChecklist(root));
    case "sync_citations":
      return result(syncCitations(root, args));
    case "refresh_wiki":
      return result(refreshWiki(root));
    case "normalize_rebuttal_issues":
      return result(normalizeRebuttalIssues(root, args));
    case "build_rebuttal_strategy":
      return result(buildRebuttalStrategy(root, args));
    case "build_rebuttal":
      return result(buildRebuttal(root, args));
    case "create_version_snapshot":
      return result(createVersionSnapshot(root, args));
    case "compare_versions":
      return result(compareVersions(root, args));
    case "list_artifacts":
      return result(listWorkspaceArtifacts(root));
    case "upsert_figure_plan":
      return result(upsertFigurePlan(root, args));
    case "run_figure_workflow":
      return result(runFigureWorkflow(root, args));
    case "prepare_figure_generation":
      return result(prepareFigureGeneration(root, args));
    case "import_figure_generation":
      return result(importFigureGeneration(root, args));
    case "validate_figure_pipeline":
      return result(validateFigurePipeline(root));
    case "record_operator_follow_through":
      return result(recordOperatorFollowThrough(root, args));
    case "record_operator_lesson":
      return result(recordOperatorLesson(root, args));
    case "plan_campaign":
      return result(planCampaign(root, args));
    case "revoke_program_approval":
      return result(revokeProgramApproval(root, args));
    case "materialize_guidance_packet":
      return result(materializeGuidancePacket(root, args));
    default:
      throw new Error(`Unknown tool: ${name}`);
  }
}
function dispatchTool(root, name, args = {}) {
  try {
    assertNoRetiredGovernanceInputs(name, args);
    assertAllowedToolInput(name, args);
    assertMcpInputSchema(name, args, TOOL_INPUT_SCHEMAS.get(name));
    const cleanArgs = stripMcpControlArgs(args);
    const existingContext = currentMutationContext(root);
    if (existingContext && MUTATING_TOOL_NAMES.has(name) && isPlainObject7(args) && Object.prototype.hasOwnProperty.call(args, "mutationMode")) {
      const explicitMode = normalizeMutationMode(args.mutationMode);
      if (explicitMode !== existingContext.mutationMode) {
        throw new Error(
          `MCP mutationMode ${explicitMode} does not match the active mutation context mode ${existingContext.mutationMode}.`
        );
      }
    }
    const mutatingTool = MUTATING_TOOL_NAMES.has(name);
    const needsContext = !existingContext && mutatingTool;
    const data = needsContext ? runWithMutationContext(root, {
      actionId: name,
      mutationMode: args?.mutationMode,
      hostId: "mcp",
      packetId: extractPacketId(args)
    }, (context) => dispatchToolData(root, name, {
      ...cleanArgs,
      ...["create_dove_task", "run_dove_auto"].includes(name) ? { mutationMode: context.mutationMode } : {}
    })) : dispatchToolData(root, name, {
      ...cleanArgs,
      ...["create_dove_task", "run_dove_auto"].includes(name) && existingContext ? { mutationMode: existingContext.mutationMode } : {}
    });
    if (data && typeof data.then === "function") {
      return data.then((resolved) => makeStructuredToolResult(name, args, resolved)).catch((error) => makeErrorResult(error instanceof Error ? error.message : String(error)));
    }
    return makeStructuredToolResult(name, args, data);
  } catch (error) {
    return makeErrorResult(error instanceof Error ? error.message : String(error));
  }
}

// src/mcp/server.mjs
function invalidParams(message) {
  const error = new Error(message);
  error.code = -32602;
  return error;
}
function normalizeToolDiscoveryParams(params) {
  if (params === void 0) {
    return {};
  }
  if (!params || typeof params !== "object" || Array.isArray(params)) {
    throw invalidParams("tools/list params must be a plain object.");
  }
  const allowed = new Set(
    Object.keys(toolDiscoveryInputSchema.properties ?? {})
  );
  const unknown = Object.keys(params).filter((key) => !allowed.has(key));
  if (unknown.length > 0) {
    throw invalidParams(
      `tools/list does not accept unknown input: ${unknown.map((key) => `$.${key}`).join(", ")}.`
    );
  }
  const surface = params.surface;
  if (surface !== void 0 && !toolDiscoveryInputSchema.properties.surface.enum.includes(surface)) {
    throw invalidParams(
      `tools/list surface must be one of: ${toolDiscoveryInputSchema.properties.surface.enum.join(", ")}.`
    );
  }
  const resultMode = params.resultMode;
  if (resultMode !== void 0 && !toolDiscoveryInputSchema.properties.resultMode.enum.includes(resultMode)) {
    throw invalidParams(
      `tools/list resultMode must be one of: ${toolDiscoveryInputSchema.properties.resultMode.enum.join(", ")}.`
    );
  }
  return params;
}
function startServer(root = process2.cwd()) {
  let buffer = Buffer.alloc(0);
  let responseFraming = "content-length";
  function sendMessage(message) {
    const body = JSON.stringify(message);
    if (responseFraming === "jsonl") {
      process2.stdout.write(`${body}
`);
      return;
    }
    process2.stdout.write(`Content-Length: ${Buffer.byteLength(body, "utf8")}\r
\r
${body}`);
  }
  function sendResponse(id, result) {
    sendMessage({ jsonrpc: "2.0", id, result });
  }
  function sendError(id, code, message) {
    sendMessage({ jsonrpc: "2.0", id, error: { code, message } });
  }
  async function handleMessage(message) {
    const { id, method, params } = message ?? {};
    if (method === "notifications/initialized") {
      return;
    }
    if (method === "initialize") {
      sendResponse(id, {
        protocolVersion: "2024-11-05",
        capabilities: { tools: {} },
        serverInfo: { name: "dove", version: "0.2.0" }
      });
      return;
    }
    if (method === "ping") {
      sendResponse(id, {});
      return;
    }
    if (method === "tools/list") {
      const discoveryParams = normalizeToolDiscoveryParams(params);
      sendResponse(id, {
        tools: toolDefinitionsForSurface(discoveryParams.surface)
      });
      return;
    }
    if (method === "tools/call") {
      sendResponse(id, await dispatchTool(root, params?.name, params?.arguments ?? {}));
      return;
    }
    if (id !== void 0) {
      sendError(id, -32601, `Method not found: ${method}`);
    }
  }
  function handleJsonText(body) {
    let message;
    try {
      message = JSON.parse(body);
    } catch {
      return;
    }
    handleMessage(message).catch((error) => {
      if (message?.id !== void 0) {
        const code = Number.isInteger(error?.code) ? error.code : -32603;
        sendError(
          message.id,
          code,
          code === -32602 && error instanceof Error ? error.message : "Internal error"
        );
      }
    });
  }
  function parseContentLengthMessage() {
    const headerEnd = buffer.indexOf("\r\n\r\n");
    if (headerEnd === -1) {
      return false;
    }
    const headerText = buffer.slice(0, headerEnd).toString("utf8");
    const match = headerText.match(/Content-Length:\s*(\d+)/i);
    if (!match) {
      return false;
    }
    const length = Number(match[1]);
    const totalLength = headerEnd + 4 + length;
    if (buffer.length < totalLength) {
      return false;
    }
    responseFraming = "content-length";
    handleJsonText(buffer.slice(headerEnd + 4, totalLength).toString("utf8"));
    buffer = buffer.slice(totalLength);
    return true;
  }
  function parseJsonLineMessage() {
    const lineEnd = buffer.indexOf("\n");
    if (lineEnd === -1) {
      return false;
    }
    responseFraming = "jsonl";
    const body = buffer.slice(0, lineEnd).toString("utf8").trim();
    buffer = buffer.slice(lineEnd + 1);
    if (body) {
      handleJsonText(body);
    }
    return true;
  }
  function parseMessages() {
    while (buffer.length > 0) {
      const prefix = buffer.toString("utf8", 0, Math.min(buffer.length, 32)).trimStart();
      const parsed = prefix.startsWith("{") ? parseJsonLineMessage() : parseContentLengthMessage();
      if (!parsed) {
        return;
      }
    }
  }
  process2.stdin.on("data", (chunk) => {
    buffer = Buffer.concat([buffer, chunk]);
    parseMessages();
  });
  process2.stdin.on("end", () => {
    process2.exit(0);
  });
}

// mcp/dove-state-server.mjs
startServer();
